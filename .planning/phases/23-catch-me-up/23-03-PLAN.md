---
phase: 23-catch-me-up
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - apps/memory-api/tests/test_catch_me_up_gate.py
autonomous: true
requirements: [CATCHUP-01]
must_haves:
  truths:
    - "Against a REAL Postgres: mark-read moves the caller's cursor; the unread-since count matches only post-cursor messages and excludes the caller's own"
    - "catch-me-up gathers EXACTLY the since-window and no other team's messages (team_scope isolation), with the agent streaming stubbed to a recorder and gathering/cursor NOT mocked"
    - "A non-member gets 403 on mark-read / unread-summary / catch-me-up; catch-me-up persists NO team_messages row"
    - "Migration 0026 applies forward-only under EDITION=oss AND EDITION=saas, leaving a NULLABLE team_members.last_read_at"
  artifacts:
    - path: "apps/memory-api/tests/test_catch_me_up_gate.py"
      provides: "The definitive real-Postgres catch-me-up gate + migration proof"
      contains: "pytest.mark.integration"
  key_links:
    - from: "the gate"
      to: "the real /mark-read + /unread-summary + /catch-me-up path"
      via: "httpx client against the FastAPI app on a testcontainer Postgres"
      pattern: "catch-me-up"
    - from: "the gate"
      to: "team_chat_agent._stream_via_anthropic_api"
      via: "monkeypatch to a recorder that captures the gathered since-window"
      pattern: "monkeypatch.setattr"
---

<objective>
THE GATE. A "catch-me-up works" claim proves nothing until a real POST traverses
the real `/mark-read` -> `/unread-summary` -> `/catch-me-up` path against a REAL
Postgres testcontainer. This plan writes `tests/test_catch_me_up_gate.py`:
seed a team + messages; a member marks read at T0; new messages arrive after T0;
assert the unread-since count counts only post-T0 messages and excludes the
caller's own; assert mark-read moved the cursor; assert catch-me-up gathers
EXACTLY the since-window (the agent streaming stubbed to a RECORDER — the
message-gathering + cursor logic are NOT mocked); assert a non-member -> 403;
assert team_scope isolation (a different team's messages never appear); assert
NO `team_messages` row is persisted by the summary. Plus a Group-2 migration
proof: 0026 forward-only + edition-agnostic under oss AND saas.

Purpose: SC#1, SC#2, and the ephemerality/isolation half of SC#3 — all made
executable. SKIP=FAIL: a clean skip is legitimate ONLY when Docker is genuinely
absent; under Docker this file MUST run green. A mocked cursor/gathering proves
nothing.

Output: `apps/memory-api/tests/test_catch_me_up_gate.py`.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-catch-me-up/23-CONTEXT.md
@.planning/phases/23-catch-me-up/23-01-PLAN.md
@.planning/phases/23-catch-me-up/23-02-PLAN.md

<interfaces>
<!-- The proven-good patterns to MIRROR verbatim. Do not invent new fixtures. -->

Fixtures (tests/conftest.py): `client`, `session`, `seeded_two_teams`
(alice=admin/member of team-a, bob=admin/member of team-b), `pg_url`
(session-scoped testcontainer, `alembic upgrade head`). `_docker_available()`
gate lives in conftest and in the migration tests.

The RECORDER + principal-override pattern to copy EXACTLY
(tests/test_agent_aliases_gate.py + tests/test_nudge_open_gate.py):
```python
pytestmark = [pytest.mark.integration, pytest.mark.asyncio]
def _install_principal(user, *, kind="user"): ...   # overrides app.deps.get_current_principal
def _clear_principal(): ...
# monkeypatch the fire-and-forget network callers to inert / recorders:
monkeypatch.setattr("app.services.centrifugo_client.publish", _recorder_or_noop)
# then POST, then flush the loop so create_task runs:
for _ in range(5): await asyncio.sleep(0)
```

Endpoints under test (Plan 23-02):
```
POST /v1/teams/{team_id}/mark-read            -> 200 {"status":"ok"}   (403 non-member)
GET  /v1/teams/{team_id}/unread-summary       -> {"count","since","threshold"}  (403 non-member)
POST /v1/teams/{team_id}/catch-me-up          -> 202 {"status":"accepted"} | 200 {"status":"nothing_to_summarize"} | 403 | 429
```

catch_me_up reuses these — stub to a RECORDER (the gathering runs for real):
```python
# app/services/team_chat_agent.py
async def catch_me_up(*, team_id, caller_user_sub, since): ...
async def _user_has_live_bridge(user_sub) -> bool          # force False so the anthropic path is taken
async def _stream_via_anthropic_api(*, system_prompt, cached_memory_block, chat_history_block)  # async-gen (text, usage)
```
The recorder replaces `_stream_via_anthropic_api` and CAPTURES `chat_history_block`
(which contains the gathered since-window) so the test asserts exactly which
messages were gathered — the gather itself (tm_repo.list_messages after cursor) is
NEVER mocked.

Seeding helpers (repos): `users_repo.get_or_create_user`,
`teams_repo.add_member(team_id, user_id, role)`,
`tm_repo.insert_user_message(session, team_id, author_user_id, content)`,
`tm_repo.insert_agent_message(session, team_id, agent_name, content, routed_via)`.
Cursor read-back: `teams_repo.get_membership(session, user_id, team_slug).last_read_at`.

Migration-proof pattern (Group 2) — copy test_agent_aliases_gate.py's Group 2
verbatim, swapping the probed column to `team_members.last_read_at`:
fresh PostgresContainer per edition, `app_config.settings.EDITION`/`DATABASE_URL`
patched DIRECTLY (os.environ is inert — settings frozen at import), `command.upgrade`
in `asyncio.to_thread`, probe `information_schema.columns`, no `downgrade()`.
`_EDITIONS = ["oss","saas"]` (NO pro).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group 1 — the real-Postgres cursor / since-window / isolation / 403 gate</name>
  <files>apps/memory-api/tests/test_catch_me_up_gate.py</files>
  <read_first>
    - apps/memory-api/tests/test_nudge_open_gate.py (the whole file — recorder + _post_as + loop-flush + ordered-rejection assertions; copy the structure)
    - apps/memory-api/tests/test_agent_aliases_gate.py (the RECORDER-replaces-the-agent pattern + _install_principal/_clear_principal)
    - apps/memory-api/tests/conftest.py (client, session, seeded_two_teams fixtures — signatures)
    - apps/memory-api/app/services/team_chat_agent.py (catch_me_up + _stream_via_anthropic_api + _user_has_live_bridge — what to stub)
  </read_first>
  <action>
    Create `apps/memory-api/tests/test_catch_me_up_gate.py` with
    `pytestmark = [pytest.mark.integration, pytest.mark.asyncio]` and the
    `_install_principal`/`_clear_principal` helpers (copied from the sibling gates).
    Write ONE async gate test `test_catch_me_up_gate(client, seeded_two_teams, session, monkeypatch)`:

    SETUP:
      - alice, bob, team_a, team_b from seeded_two_teams. Seed carol as a 2nd member
        of team-a (`teams_repo.add_member`). Seed the message timeline with EXPLICIT
        ordering (insert + commit between phases so created_at strictly increases):
          * Phase A (before cursor): 2 user messages in team-a from carol, with
            distinctive marker strings (e.g. "PHASE_A_1", "PHASE_A_2").
          * Team-b noise: >=2 user messages in team-b with markers "TEAMB_1"/"TEAMB_2"
            (the isolation decoys).
          * alice marks read (T0) — via `POST /mark-read` (the real endpoint).
          * Phase B (after cursor): in team-a — 2 from carol ("PHASE_B_1"/"PHASE_B_2"),
            1 from ALICE herself ("PHASE_B_ALICE_OWN"), plus 1 agent message via
            `insert_agent_message`. These exercise "exclude own + exclude agent".
      - RECORDERS: `captured = {}`; stub `team_chat_agent._stream_via_anthropic_api`
        with an async-generator that records the `chat_history_block` kwarg into
        `captured["history"]` and yields one `("summary text", {})`. Stub
        `team_chat_agent._user_has_live_bridge` -> async False (force the anthropic path).
        Stub `centrifugo_client.publish` with a recorder capturing (channel, data).
        Do NOT stub tm_repo / set_last_read / get_membership / count_unread_since —
        the gathering + cursor logic run for real (that is the whole point).

    ASSERTIONS (each async POST/GET via _install_principal + a loop-flush
    `for _ in range(5): await asyncio.sleep(0)` before asserting a fire-and-forget effect):

      1. MARK-READ MOVED THE CURSOR (SC#1): after alice's `POST /mark-read` -> 200;
         re-read `get_membership(session, user_id=alice.id, team_slug='team-a').last_read_at`
         is not None and lies between the Phase-A and Phase-B timestamps.
      2. UNREAD COUNT EXCLUDES OWN + AGENT (SC#1): `GET /unread-summary` as alice ->
         `count == 2` (only carol's 2 post-T0 USER messages; NOT alice's own, NOT the
         agent frame). `since` equals the cursor iso; `threshold` == CATCHUP_UNREAD_THRESHOLD.
      3. NON-MEMBER 403 (SC#2): bob (team-b only) -> `POST /mark-read`, `GET /unread-summary`,
         and `POST /catch-me-up` on team-a each return 403; and after the 403 catch-me-up,
         the stream recorder captured NOTHING (no summary started).
      4. CATCH-ME-UP GATHERS EXACTLY THE SINCE-WINDOW + ISOLATION (SC#2): alice ->
         `POST /catch-me-up` on team-a -> 202; flush loop; assert
         `_stream_via_anthropic_api` was invoked once and `captured["history"]`
         CONTAINS "PHASE_B_1"/"PHASE_B_2" and CONTAINS NONE of "TEAMB_1"/"TEAMB_2"
         (team_scope isolation) and CONTAINS NONE of "PHASE_A_1"/"PHASE_A_2"
         (strictly after the cursor).
      5. EPHEMERAL — NO PERSISTED ROW (SC#3): count `team_messages` rows for team-a
         via a direct `select(func.count())` BEFORE and AFTER the catch-me-up; the
         count is UNCHANGED (catch_me_up wrote no agent row); and every publish the
         recorder captured used a `user:<alice_sub>` channel, never `team:<team_a.id>`.
      6. EMPTY-WINDOW NO-OP: alice marks read AGAIN (`POST /mark-read`, cursor -> now,
         no new messages after), then `POST /catch-me-up` -> 200 `nothing_to_summarize`
         and the stream recorder gained no new invocation (no LLM call).

    Keep the SKIP=FAIL docstring banner (mirror the sibling gates): the integration
    marker lets CI's skip-grep capture this; a skip is legitimate ONLY when Docker
    is absent.
  </action>
  <acceptance_criteria>
    - File exists with `pytestmark = [pytest.mark.integration, pytest.mark.asyncio]`.
    - `grep -c 'monkeypatch.setattr' apps/memory-api/tests/test_catch_me_up_gate.py` >= 3 (stream recorder, bridge-false, publish recorder).
    - The test stubs `_stream_via_anthropic_api` and does NOT stub `list_messages`/`count_unread_since`/`set_last_read` (no `monkeypatch.setattr` line names those three).
    - Assertions present for: cursor moved (`last_read_at`), `count == 2`, three 403s for a non-member, gathered window contains "PHASE_B" markers and excludes "TEAMB"/"PHASE_A" markers, team_messages row count UNCHANGED, and `nothing_to_summarize`.
    - Under Docker: `python -m pytest tests/test_catch_me_up_gate.py::test_catch_me_up_gate -x` PASSES (not skipped). Without Docker it skips cleanly.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -m pytest tests/test_catch_me_up_gate.py::test_catch_me_up_gate -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>A real POST traverses mark-read -> unread-summary -> catch-me-up against a real Postgres: the cursor moves, the count excludes own+agent, a non-member is 403, catch-me-up gathers exactly the since-window (agent streaming stubbed, gathering real), no other team's messages appear, and no team_messages row is persisted. SKIP=FAIL under Docker.</done>
</task>

<task type="auto">
  <name>Task 2: Group 2 — migration 0026 forward-only + edition-agnostic proof (oss AND saas)</name>
  <files>apps/memory-api/tests/test_catch_me_up_gate.py</files>
  <read_first>
    - apps/memory-api/tests/test_agent_aliases_gate.py (Group 2 — `_upgrade_and_probe_agent_aliases` + `test_migration_0025_agent_aliases_forward_only`; copy the whole per-edition-container + singleton-patch + information_schema probe pattern)
    - apps/memory-api/tests/test_migration_editions.py (the load-bearing singleton-patch rationale + `_EDITIONS=["oss","saas"]`, NO pro)
  </read_first>
  <action>
    Append a Group-2 block to `tests/test_catch_me_up_gate.py` mirroring
    test_agent_aliases_gate.py's migration group, swapping the probed column:
      - `_EDITIONS = ["oss", "saas"]`; `_docker_available()`, `_alembic_config()`
        helpers (copy verbatim — dirname-relative alembic.ini/script_location paths).
      - `async def _upgrade_and_probe_last_read(edition) -> tuple[str, str]`: fresh
        `PostgresContainer("postgres:17", ...).with_command("postgres -c shared_preload_libraries=pgcrypto")`;
        save + restore `app_config.settings.EDITION`/`DATABASE_URL` and os.environ in
        `finally`; patch the singleton DIRECTLY (os.environ alone is inert);
        `await asyncio.to_thread(command.upgrade, cfg, "head")`; then
        `SELECT data_type, is_nullable FROM information_schema.columns WHERE
        table_name='team_members' AND column_name='last_read_at'`; assert exactly ONE
        row; `pg.stop()` even on raise. NO `downgrade()` anywhere.
      - `@pytest.mark.parametrize("edition", _EDITIONS)`
        `async def test_migration_0026_last_read_forward_only(edition)`: skip only if
        Docker absent; assert `is_nullable == "YES"` and
        `data_type == "timestamp with time zone"` (0026 declares TIMESTAMPTZ).
      Keep the SKIP=FAIL note: under Docker (CI) a skip is a FAILURE signal.
  </action>
  <acceptance_criteria>
    - `grep -n '_EDITIONS = \["oss", "saas"\]' apps/memory-api/tests/test_catch_me_up_gate.py` matches; the token `pro` does NOT appear as an edition value.
    - The information_schema probe targets `table_name='team_members'` AND `column_name='last_read_at'`.
    - `grep -c 'downgrade' apps/memory-api/tests/test_catch_me_up_gate.py` == 0 (forward-only).
    - Asserts `is_nullable == "YES"` and a `timestamp with time zone` data_type.
    - Under Docker: `python -m pytest tests/test_catch_me_up_gate.py -k migration -q` PASSES for both editions (2 parametrized cases).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && grep -q '_EDITIONS = \["oss", "saas"\]' tests/test_catch_me_up_gate.py && ! grep -q 'downgrade' tests/test_catch_me_up_gate.py && python -m pytest tests/test_catch_me_up_gate.py -k migration -q 2>&1 | tail -15</automated>
  </verify>
  <done>Migration 0026 is proven forward-only and edition-agnostic: `alembic upgrade head` under EDITION=oss AND EDITION=saas each leaves a NULLABLE `team_members.last_read_at` timestamptz column, with no downgrade invoked. SKIP=FAIL under Docker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ↔ real path | The gate's value is that ONLY the terminal network callers are stubbed; membership, cursor, gathering, count, and isolation all run for real against Postgres. |
| release matrix | The migration must reach the same nullable column under oss AND saas — a fork is a broken self-host upgrade. |

## STRIDE Threat Register

This plan is the executable PROOF of the threats mitigated in 23-01/23-02. Each
row maps a threat to the assertion that would go RED if the mitigation regressed.

| Threat ID | Category | Component | Disposition | Mitigation Plan (asserted here) |
|-----------|----------|-----------|-------------|---------------------------------|
| T-23-01 | Elevation of privilege | non-member access | mitigate | Assertion 3: bob (non-member) gets 403 on all three endpoints and starts no summary. |
| T-23-02 | Information disclosure | team_scope isolation | mitigate | Assertion 4: the gathered since-window contains team-a Phase-B markers and NONE of the team-b decoys. |
| T-23-03 | Denial of service / cost | opt-in + empty-window | mitigate | Assertion 6: a 0-unread catch-me-up returns `nothing_to_summarize` with NO stream invocation (no LLM call). |
| T-23-04 | Tampering / cursor | mark-read | mitigate | Assertion 1: mark-read moves ONLY alice's cursor; assertion 2's count proves it is her own private cursor. |
| T-23-06 | Repudiation / persistence | ephemerality | mitigate | Assertion 5: team_messages row count is UNCHANGED and publishes go to `user:<sub>`, never the team channel. |
| T-23-07 | Tampering / schema fork | migration 0026 | mitigate | Group 2: nullable last_read_at reached under oss AND saas, no downgrade. |
</threat_model>

<verification>
- Under Docker: `python -m pytest tests/test_catch_me_up_gate.py -q` runs green — the Group-1 gate + both Group-2 parametrized migration cases pass (not skipped).
- Without Docker: the file skips cleanly (the `integration` marker + `_docker_available()` gate). CI treats a skip-under-Docker as a failure.
- grep confirms: integration marker, >=3 recorder stubs, no stub on the gathering/cursor repos, no `downgrade`, `_EDITIONS = ["oss","saas"]`.
</verification>

<success_criteria>
- The Group-1 gate proves, against a real Postgres with agent streaming stubbed to a recorder (gathering NOT mocked): cursor set by mark-read, unread count excludes own + agent, non-member 403, catch-me-up gathers exactly the since-window, team_scope isolation, and no persisted team_messages row.
- The Group-2 proof: migration 0026 applies forward-only and edition-agnostic under oss AND saas, leaving a nullable `team_members.last_read_at`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-catch-me-up/23-03-SUMMARY.md`.
</output>
