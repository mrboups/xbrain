---
phase: 23-catch-me-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memory-api/alembic/versions/0026_team_member_last_read.py
  - apps/memory-api/app/models/team.py
  - apps/memory-api/app/repos/teams.py
  - apps/memory-api/app/repos/team_messages.py
autonomous: true
requirements: [CATCHUP-01]
must_haves:
  truths:
    - "A per-member read cursor column exists on team_members (nullable, forward-only)"
    - "The cursor can be set to now() for one (team, member) pair"
    - "Messages after a timestamp can be fetched chronologically (symmetric to before)"
    - "Unread-since count can be computed excluding the caller's own messages and agent frames"
  artifacts:
    - path: "apps/memory-api/alembic/versions/0026_team_member_last_read.py"
      provides: "Migration 0026 — nullable team_members.last_read_at"
      contains: "last_read_at"
    - path: "apps/memory-api/app/models/team.py"
      provides: "TeamMember.last_read_at ORM mapping"
      contains: "last_read_at"
    - path: "apps/memory-api/app/repos/teams.py"
      provides: "set_last_read cursor mutator"
      contains: "async def set_last_read"
    - path: "apps/memory-api/app/repos/team_messages.py"
      provides: "after_created_at window + count_unread_since"
      contains: "count_unread_since"
  key_links:
    - from: "0026_team_member_last_read.py"
      to: "0025_team_agent_aliases"
      via: "down_revision"
      pattern: "down_revision.*0025_team_agent_aliases"
    - from: "app/repos/team_messages.list_messages"
      to: "TeamMessage.created_at"
      via: "after_created_at (created_at > after)"
      pattern: "after_created_at"
---

<objective>
Lay the data foundation for "Catch me up": a per-member `last_read_at` cursor on
`team_members` (migration 0026 + ORM), plus the repo primitives the endpoints and
the gate consume — a `set_last_read` mutator, an `after_created_at` window on
`list_messages` (symmetric to the existing `before_created_at`), and a
`count_unread_since` that excludes the caller's own messages and agent frames.

Purpose: SC#1 (a cursor exists, forward-only under oss AND saas) and SC#2 (a
`since` window symmetric to `before`) both bottom out in this layer. Everything
downstream (mark-read, unread-summary, catch-me-up, the gate) imports these
contracts — define them first so no executor has to reverse-engineer them.

Output: migration 0026, `TeamMember.last_read_at`, `teams.set_last_read`,
`team_messages.list_messages(after_created_at=...)`, `team_messages.count_unread_since`.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-catch-me-up/23-CONTEXT.md

<interfaces>
<!-- The exact shapes this plan MUST mirror. Extracted from the codebase — use
     these directly, do not go exploring. -->

The migration head to base 0026 on (apps/memory-api/alembic/versions/0025_team_agent_aliases.py):
```python
revision: str = "0025_team_agent_aliases"
down_revision: Union[str, None] = "0024_local_credentials"
def upgrade() -> None:
    op.execute("ALTER TABLE teams ADD COLUMN IF NOT EXISTS agent_aliases TEXT")
def downgrade() -> None:
    op.execute("ALTER TABLE teams DROP COLUMN IF EXISTS agent_aliases")
```
0026 mirrors this EXACTLY (additive, `IF NOT EXISTS`, no EDITION branch, no
backfill, symmetric downgrade for form only).

TeamMember today (app/models/team.py) — add `last_read_at` alongside blocked_at:
```python
class TeamMember(Base):
    __tablename__ = "team_members"
    team_id: Mapped[UUID] = mapped_column(..., primary_key=True)
    user_id: Mapped[UUID] = mapped_column(..., primary_key=True)
    role: Mapped[str]
    joined_at: Mapped[datetime] = mapped_column(..., server_default=func.now())
    blocked_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    blocked_by: Mapped[UUID | None] = ...
```

list_messages today (app/repos/team_messages.py:74) — has `before_created_at`
(`created_at < before`), newest-first. Add `after_created_at` (`created_at > after`).
count query does NOT exist yet.

TeamMessage columns used here: `team_id`, `author_user_id` (NULL for agent),
`kind` ('user'|'agent'), `created_at`, `deleted_at` (soft-delete → filter IS NULL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0026 + TeamMember.last_read_at ORM</name>
  <files>apps/memory-api/alembic/versions/0026_team_member_last_read.py, apps/memory-api/app/models/team.py</files>
  <read_first>
    - apps/memory-api/alembic/versions/0025_team_agent_aliases.py (the head + the exact additive/edition-agnostic pattern to copy)
    - apps/memory-api/app/models/team.py (TeamMember — add the column next to blocked_at/blocked_by)
  </read_first>
  <action>
    Create `apps/memory-api/alembic/versions/0026_team_member_last_read.py` copying
    the 0025 shape EXACTLY (Phase-17 forward-only, edition-agnostic):
      - `revision = "0026_team_member_last_read"`
      - `down_revision = "0025_team_agent_aliases"`
      - `upgrade()`: `op.execute("ALTER TABLE team_members ADD COLUMN IF NOT EXISTS last_read_at TIMESTAMPTZ")`
      - `downgrade()`: `op.execute("ALTER TABLE team_members DROP COLUMN IF EXISTS last_read_at")` (symmetry only; never invoked in release validation)
      - NO branch on `settings.EDITION`, NO backfill, NO default value. The word
        `EDITION` must NOT appear in the file (test_migration_editions.py::test_no_migration_branches_on_edition greps for it).
    Then add to `TeamMember` in app/models/team.py, immediately after `joined_at`:
      `last_read_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)`
    with a Phase-23 comment: private per-member read cursor (D-23-01); NULL = never
    read (first-time visitor); set via repos.teams.set_last_read (Python datetime,
    not server_default). Column name is EXACTLY `last_read_at`.
  </action>
  <acceptance_criteria>
    - `grep -n 'down_revision.*0025_team_agent_aliases' apps/memory-api/alembic/versions/0026_team_member_last_read.py` matches.
    - `grep -c 'ADD COLUMN IF NOT EXISTS last_read_at TIMESTAMPTZ' apps/memory-api/alembic/versions/0026_team_member_last_read.py` == 1.
    - `grep -v '^#' apps/memory-api/alembic/versions/0026_team_member_last_read.py | grep -c EDITION` == 0 (no edition branch).
    - `grep -n 'last_read_at: Mapped\[datetime | None\]' apps/memory-api/app/models/team.py` matches inside class TeamMember.
    - `python -c "import ast,sys; ast.parse(open('apps/memory-api/alembic/versions/0026_team_member_last_read.py').read()); ast.parse(open('apps/memory-api/app/models/team.py').read())"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -c "import ast; ast.parse(open('alembic/versions/0026_team_member_last_read.py').read()); ast.parse(open('app/models/team.py').read()); print('ok')" && grep -q 'down_revision.*0025_team_agent_aliases' alembic/versions/0026_team_member_last_read.py && grep -q 'ADD COLUMN IF NOT EXISTS last_read_at TIMESTAMPTZ' alembic/versions/0026_team_member_last_read.py && ! grep -v '^#' alembic/versions/0026_team_member_last_read.py | grep -q EDITION && echo PASS</automated>
  </verify>
  <done>0026 chains off 0025, adds nullable team_members.last_read_at idempotently with no edition branch; TeamMember maps last_read_at as nullable. (Forward-only proof under oss AND saas is asserted by the gate in Plan 23-03.)</done>
</task>

<task type="auto">
  <name>Task 2: Repo primitives — set_last_read, after_created_at window, count_unread_since</name>
  <files>apps/memory-api/app/repos/teams.py, apps/memory-api/app/repos/team_messages.py</files>
  <read_first>
    - apps/memory-api/app/repos/teams.py (block_member/get_membership — mirror the "resolve row then mutate, caller commits" style; use datetime.now(tz=timezone.utc) like block_member does)
    - apps/memory-api/app/repos/team_messages.py:74 (list_messages — add after_created_at symmetric to before_created_at; note deleted_at IS NULL filter)
    - apps/memory-api/app/models/team_message.py (kind, author_user_id, created_at, deleted_at)
  </read_first>
  <action>
    In app/repos/teams.py add:
      `async def set_last_read(session, *, team_id: UUID, user_id: UUID) -> TeamMember | None:`
      — SELECT the (team_id, user_id) TeamMember; if None return None; set
      `m.last_read_at = datetime.now(tz=timezone.utc)`; `await session.flush()`;
      return m. Caller commits (mirrors block_member exactly — Python datetime so the
      returned row is `.isoformat()`-safe). Idempotent by construction (repeated calls
      just move the cursor forward to now()).

    In app/repos/team_messages.py:
    1. Extend `list_messages` with a new keyword-only param `after_created_at: datetime | None = None`.
       When set, add `stmt = stmt.where(TeamMessage.created_at > after_created_at)`.
       Keep `before_created_at` working unchanged. Preserve the existing
       `deleted_at IS None` filter and the newest-first `order_by(desc(created_at))`.
       (Callers that need chronological order reverse client-side, as
       get_recent_messages_chronological already does — do NOT change ordering here.)
    2. Add `async def count_unread_since(session, *, team_id: UUID, after_created_at: datetime | None, exclude_user_id: UUID) -> int:`
       — `SELECT count(*)` over TeamMessage WHERE team_id == team_id AND
       deleted_at IS None AND `kind == 'user'` AND `author_user_id != exclude_user_id`
       AND (after_created_at is None OR created_at > after_created_at). This is the
       "unread that matters" count (D-23-02): excludes the caller's OWN messages and
       ALL agent frames (kind='agent'). NULL cursor ⇒ counts every other member's
       user message. Return `int(result.scalar_one() or 0)`.
    Use `sqlalchemy.func.count()` (import sa or func as the file already does).
  </action>
  <acceptance_criteria>
    - `grep -n 'async def set_last_read' apps/memory-api/app/repos/teams.py` matches; body assigns `last_read_at` and calls `session.flush()`, NOT `session.commit()`.
    - `grep -n 'after_created_at' apps/memory-api/app/repos/team_messages.py` matches inside `list_messages` with a `created_at > after_created_at` predicate.
    - `grep -n 'async def count_unread_since' apps/memory-api/app/repos/team_messages.py` matches; the query filters `kind == 'user'` AND `author_user_id != exclude_user_id` AND `deleted_at`.
    - No `commit()` added in either repo function (repos flush; routes commit).
    - `python -c "import ast; ast.parse(open('apps/memory-api/app/repos/teams.py').read()); ast.parse(open('apps/memory-api/app/repos/team_messages.py').read())"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -c "import ast; ast.parse(open('app/repos/teams.py').read()); ast.parse(open('app/repos/team_messages.py').read()); print('ok')" && grep -q 'async def set_last_read' app/repos/teams.py && grep -q 'after_created_at' app/repos/team_messages.py && grep -q 'async def count_unread_since' app/repos/team_messages.py && echo PASS</automated>
  </verify>
  <done>set_last_read moves one member's cursor to now() (caller commits); list_messages fetches a since-window via after_created_at; count_unread_since counts only other members' user messages after the cursor. (Behavioral proof against real Postgres lands in Plan 23-03.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration ↔ release matrix | A schema change must apply identically under EDITION=oss and EDITION=saas — a fork here silently breaks one edition's install. |
| repo ↔ callers | These functions carry NO authZ; membership/ownership gating is the routes' job (Plan 23-02). They must not widen a query beyond a single team_id. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | migration 0026 | mitigate | Forward-only additive `ADD COLUMN IF NOT EXISTS`, no EDITION branch (grep gate in Task 1; oss/saas proof in 23-03). No backfill → no data mutation. |
| T-23-02 | Information disclosure | count_unread_since / list_messages | mitigate | Every query is bound by a single `team_id` param; no cross-team predicate exists. Team-scope enforcement (the caller may only pass their own team) is the route's boundary (23-02). |
| T-23-03 | Elevation of privilege | set_last_read | mitigate | Writes exactly the (team_id, user_id) pair the caller passes; the route derives user_id from the authenticated principal, never from the body (23-02) — the repo never trusts a body-supplied user. |
</threat_model>

<verification>
- `python -c "import ast; ..."` parses all four files (no syntax errors).
- grep confirms: 0026 down_revision=0025, `ADD COLUMN IF NOT EXISTS last_read_at`, no EDITION token; `TeamMember.last_read_at` nullable; `set_last_read`, `after_created_at`, `count_unread_since` present.
- Full forward-only-under-oss-AND-saas + behavioral cursor/window/count proof is delivered by the real-Postgres gate in Plan 23-03 (not mocked).
</verification>

<success_criteria>
- Migration 0026 adds a nullable `team_members.last_read_at`, chains off 0025, is edition-agnostic and forward-only.
- `TeamMember.last_read_at` is mapped nullable.
- `teams.set_last_read`, `team_messages.list_messages(after_created_at=...)`, and `team_messages.count_unread_since` exist with the exact signatures the endpoints (23-02) and the gate (23-03) consume.
</success_criteria>

<output>
After completion, create `.planning/phases/23-catch-me-up/23-01-SUMMARY.md`.
</output>
