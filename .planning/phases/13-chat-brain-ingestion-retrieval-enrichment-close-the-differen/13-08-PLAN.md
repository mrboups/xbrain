---
phase: 13-chat-brain-ingestion-retrieval-enrichment-close-the-differen
plan: 08
type: execute
wave: 6
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06, 13-07]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - marketing-site/docs/memory.html
  - marketing-site/docs/chat.html
autonomous: true
requirements: [MEM-04, CHAT-03, CHAT-07]
must_haves:
  truths:
    - "REQUIREMENTS.md MEM-04, CHAT-03, CHAT-07 are ticked from `[ ]` to `[x]`"
    - "REQUIREMENTS.md traceability table marks MEM-04 / CHAT-03 / CHAT-07 status as 'Done' (with parenthetical 'closed Phase 13' in the Phase column)"
    - "ROADMAP.md Phase 13 row in the Progress table shows 8/8 plans complete and status LIVE with a date stamp"
    - "ROADMAP.md Phase 13 plan list has all 8 plans marked `[x]`"
    - "STATE.md `stopped_at` reflects Phase 13 LIVE state and `progress.completed_phases` is incremented"
    - "Marketing site memory.html documents the chat-to-brain ingestion pipeline + Haiku relevance filter"
    - "Marketing site chat.html documents the per-turn enrichment behavior"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "MEM-04 / CHAT-03 / CHAT-07 ticked [x]; traceability table updated"
      contains: "[x] **MEM-04**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 row updated to LIVE with date + plan list all [x]"
      contains: "13. Chat → Brain Ingestion + Retrieval Enrichment"
    - path: ".planning/STATE.md"
      provides: "Updated stopped_at + progress counters reflecting Phase 13 LIVE"
      contains: "Phase 13"
    - path: "marketing-site/docs/memory.html"
      provides: "Documentation of the new chat-to-brain ingestion pipeline + relevance filter"
      contains: "Haiku"
  key_links:
    - from: ".planning/REQUIREMENTS.md (status column)"
      to: ".planning/ROADMAP.md Phase 13 success criteria"
      via: "Traceability — REQ-ID mapping confirmed"
      pattern: "MEM-04.*(closed Phase 13|Done)"
---

<objective>
Close Phase 13. Update planning artifacts to reflect that MEM-04, CHAT-03, and CHAT-07 — the three unchecked v1 requirements that defined the xbrain differentiator gap — are DONE. Update marketing/docs pages so the public-facing description of the memory layer and chat reflects the new behavior.

Purpose: Without this plan, Phase 13 LIVE is incomplete from a project-state perspective. The next operator/agent reading STATE.md must see "Phase 13 LIVE — MEM-04 / CHAT-03 / CHAT-07 ticked, 8/8 plans complete". The orchestrator's full-integration-check (standing order from Phase 12) reads these markers to know it can proceed.

Output: 5 file edits — three planning artifacts (REQUIREMENTS.md, ROADMAP.md, STATE.md) and two docs pages (memory.html, chat.html). No code changes, no tests beyond grep-based verification that the artifacts are syntactically valid.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/VSC/xbrain/.planning/PROJECT.md
@D:/VSC/xbrain/.planning/REQUIREMENTS.md
@D:/VSC/xbrain/.planning/ROADMAP.md
@D:/VSC/xbrain/.planning/STATE.md

<interfaces>
<!-- REQUIREMENTS.md MEM-04 / CHAT-03 / CHAT-07 current state (verbatim — from Grep): -->
```
Line 35: - [ ] **MEM-04**: Conversations from any frontend (LibreChat, Open WebUI) are persisted via memory-api and indexed for retrieval
Line 47: - [ ] **CHAT-03**: Conversation history persists per user and is queryable as team memory
Line 51: - [ ] **CHAT-07**: Chat replies are auto-enriched with relevant CANONICAL facts from the user's team/project memory before the LLM call

Traceability table (lines 217-231 area):
Line 218: | MEM-04 | Phase 1 | Pending |
Line 227: | CHAT-03 | Phase 1 | Pending |
Line 231: | CHAT-07 | Phase 2 | Pending |
```

<!-- ROADMAP.md Phase 13 row in Progress table (verbatim — currently): -->
```
| 13. Chat → Brain Ingestion + Retrieval Enrichment | 0/0 | Planned | 2026-05-24 (decisions D1-D6 locked, ready for plan-phase 13) |
```

<!-- Plan list under "Plans: TBD" in Phase 13 section (line 333): -->
```
**Plans**: TBD (populated by /gsd:plan-phase 13 — estimated 7-9 sub-plans: ...)
```

<!-- STATE.md current head (lines 1-15): -->
```
stopped_at: "Phase 12 LIVE 2026-05-17 — GitHub App Migration fully shipped..."
progress:
  total_phases: 12
  completed_phases: 12
  total_plans: 101
  completed_plans: 101
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tick MEM-04, CHAT-03, CHAT-07 in REQUIREMENTS.md (item rows + traceability table)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — confirm exact text of items + traceability rows)
  </read_first>
  <action>
    Make exactly six edits to `.planning/REQUIREMENTS.md`:

    **Edits 1-3** — top item-list rows: change `- [ ] **MEM-04**:` to `- [x] **MEM-04**:`. Same for CHAT-03 and CHAT-07. The description text after the bold identifier MUST stay identical to the existing text — do not rephrase.

    **Edits 4-6** — traceability table rows. Use the `Edit` tool (not Write) to surgically swap three lines:
    - `| MEM-04 | Phase 1 | Pending |` becomes `| MEM-04 | Phase 1 (closed Phase 13) | Done |`
    - `| CHAT-03 | Phase 1 | Pending |` becomes `| CHAT-03 | Phase 1 (closed Phase 13) | Done |`
    - `| CHAT-07 | Phase 2 | Pending |` becomes `| CHAT-07 | Phase 2 (closed Phase 13) | Done |`

    The "Pending" → "Done" + the parenthetical "(closed Phase 13)" preserves the historical record that the requirement was originally scoped to Phase 1/2 but actually closed in Phase 13.

    Validate: open the file in your head and ensure adjacent rows in the traceability table have the same column count.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*MEM-04\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*MEM-04\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*CHAT-03\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*CHAT-07\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "MEM-04 | Phase 1 (closed Phase 13) | Done" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "CHAT-03 | Phase 1 (closed Phase 13) | Done" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "CHAT-07 | Phase 2 (closed Phase 13) | Done" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[ \] \*\*MEM-04\*\*" .planning/REQUIREMENTS.md` returns 0 (the unchecked variant no longer exists)
    - `grep -c "MEM-04 | Phase 1 | Pending" .planning/REQUIREMENTS.md` returns 0 (the original traceability row no longer exists)
  </acceptance_criteria>
  <done>
    Three core differentiator requirements ticked; traceability preserved; old "Pending" rows removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md — Phase 13 plan list + Progress table row to LIVE</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read offset 310-340 — Phase 13 section; offset 365-385 — Progress table)
  </read_first>
  <action>
    Make two edits to `.planning/ROADMAP.md`:

    **Edit 1 — Phase 13 plan list.** Find the line `**Plans**: TBD (populated by /gsd:plan-phase 13 — estimated 7-9 sub-plans: ...)` under the Phase 13 section (around line 333). Replace it with:

    ```
    **Plans**: 8 plans
    Plans:
    - [x] 13-01-PLAN.md — relevance_filter (Haiku 4.5 + cache + budget cap) + POST /v1/brain/ingest endpoint
    - [x] 13-02-PLAN.md — team_chat ingest uses relevance_filter.classify (swap heuristic gate)
    - [x] 13-03-PLAN.md — native_provider upsert race fix (INSERT … ON CONFLICT DO UPDATE)
    - [x] 13-04-PLAN.md — LibreChat brain ingest hook in mongo_watcher.messages_watch_loop
    - [x] 13-05-PLAN.md — LibreChat per-turn enricher (message_enricher.enrich_turn) + conv_enricher VALIDATED upgrade
    - [x] 13-06-PLAN.md — Open WebUI ingest + enrichment in openwebui-pipeline.main.chat()
    - [x] 13-07-PLAN.md — verify-phase13.sh + cross-frontend integration test + .env.example
    - [x] 13-08-PLAN.md — Tick MEM-04 / CHAT-03 / CHAT-07; ROADMAP + STATE; marketing docs
    ```

    **Edit 2 — Progress table row.** Find the Progress table row for Phase 13:

    `| 13. Chat → Brain Ingestion + Retrieval Enrichment | 0/0 | 🟡 Planned | 2026-05-24 (decisions D1-D6 locked, ready for /gsd:plan-phase 13) |`

    Replace with (use TODAY's date YYYY-MM-DD when executing — the executor will fill in the real value):

    `| 13. Chat → Brain Ingestion + Retrieval Enrichment | 8/8 | ✅ LIVE | YYYY-MM-DD (verify-phase13.sh PASS X/8 + SKIP Y, MEM-04 / CHAT-03 / CHAT-07 ticked) |`

    Where YYYY-MM-DD is the real shipping date and X/Y are the actual verify-phase13.sh outcomes.

    The execution-phase orchestrator typically does this update at end-of-phase via its standard hook. If the orchestrator has already done it, this task is a no-op safety net.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] 13-08-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] 13-01-PLAN.md" .planning/ROADMAP.md` returns 1
    - `grep -c "^- \[x\] 13-08-PLAN.md" .planning/ROADMAP.md` returns 1
    - `grep -c "13. Chat → Brain Ingestion + Retrieval Enrichment | 8/8" .planning/ROADMAP.md` returns 1
    - `grep -c "13. Chat → Brain Ingestion + Retrieval Enrichment | 0/0" .planning/ROADMAP.md` returns 0 (old row gone)
    - `grep -c "Plans\\*\\*: 8 plans" .planning/ROADMAP.md` shows at least 1 occurrence under the Phase 13 section
    - `grep -c "Plans\\*\\*: TBD" .planning/ROADMAP.md` no longer matches anywhere in the Phase 13 block
  </acceptance_criteria>
  <done>
    ROADMAP.md reflects 8/8 plans complete + LIVE status. The Progress table mirrors the plan-list section.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — bump completed_phases + completed_plans + stopped_at</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (entire file — focus on lines 1-15 frontmatter, lines 26-33 Current Position)
  </read_first>
  <action>
    Edit `.planning/STATE.md`:

    **Edit 1 — frontmatter `stopped_at`:**
    Replace the existing `stopped_at: "Phase 12 LIVE 2026-05-17 — ..."` value with:
    ```
    stopped_at: "Phase 13 LIVE YYYY-MM-DD — Chat → Brain Ingestion + Retrieval Enrichment shipped 8/8 plans. MEM-04 / CHAT-03 / CHAT-07 (the three unchecked v1 differentiator requirements) ticked [x]. New: Haiku 4.5 relevance filter + budget cap (memory-api); race-free INSERT … ON CONFLICT upsert in native_provider; LibreChat brain ingest hook + per-turn enricher; Open WebUI ingest + enrichment; verify-phase13.sh PASS X/8 + SKIP Y. Standing order: auto-run full integration check now."
    ```
    Substitute YYYY-MM-DD with the real shipping date during execution.

    **Edit 2 — frontmatter `last_updated`:** set to current ISO datetime (e.g. `"YYYY-MM-DDTHH:MM:SSZ"`).

    **Edit 3 — frontmatter `progress`:**
    Replace:
    ```yaml
    progress:
      total_phases: 12
      completed_phases: 12
      total_plans: 101
      completed_plans: 101
      percent: 100
    ```
    With:
    ```yaml
    progress:
      total_phases: 13
      completed_phases: 13
      total_plans: 109
      completed_plans: 109
      percent: 100
    ```
    Where 109 = 101 (pre-Phase-13) + 8 (Phase 13 plans).

    **Edit 4 — Current Position section (around line 28):**
    Replace the existing `Phase:`, `Plan:`, `Status:`, and `Last activity:` lines with the Phase 13 LIVE equivalent. Match the existing format. The "Last activity" line should summarize Phase 13: "2026-MM-DD — Phase 13 SHIPPED end-to-end (8/8 plans). MEM-04 / CHAT-03 / CHAT-07 ticked. New endpoints: POST /v1/brain/ingest. New services: relevance_filter.py, message_enricher.py. New verify: verify-phase13.sh. New env vars: RELEVANCE_HAIKU_ENABLED, BRAIN_INGEST_ENABLED, CHAT07_TOP_K, CHAT07_TRUTH_FILTER_MIN_LEVEL."

    **Edit 5 — Progress bar:**
    Update the textual progress bar around line 33: `Progress: [████████░░] 83% — 10 of 12 phases COMPLETE (Phase 11 PLANNED, Phase 12 ROADMAPPED)` → update to reflect 13/13 phases COMPLETE. Use solid blocks for all 10 cells.

    **Edit 6 — Decisions section append (around line 119):**
    Append three new dated decision lines for traceability (use today's date YYYY-MM-DD):
    ```
    - YYYY-MM-DD (13-01): Relevance classifier = Claude 4.5 Haiku with ephemeral prompt cache + per-team daily token budget (50K input tokens/day default); fail-soft to ≥15-char heuristic
    - YYYY-MM-DD (13-03): native_provider.upsert now uses INSERT … ON CONFLICT (id) DO UPDATE — fixes SELECT+INSERT race exposed by deterministic UUID5 idempotency keys
    - YYYY-MM-DD (13-05/06): Per-turn enrichment uses min_level=VALIDATED (>= semantics: includes VALIDATED+CANONICAL+PUBLIC); CHAT07_TOP_K=5 default
    ```

    The STATE.md frontmatter is YAML. Validate manually that the indentation matches the existing structure (2-space indent under `progress`).
  </action>
  <verify>
    <automated>grep -c "Phase 13 LIVE" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 13 LIVE" .planning/STATE.md` returns at least 2 (frontmatter `stopped_at` + Current Position section + new "Last activity" line)
    - `grep -c "completed_phases: 13" .planning/STATE.md` returns 1
    - `grep -c "total_phases: 13" .planning/STATE.md` returns 1
    - `grep -c "completed_plans: 109" .planning/STATE.md` returns 1
    - `grep -c "13-01" .planning/STATE.md` returns at least 1 (the new decision row)
    - `grep -c "RELEVANCE_HAIKU_ENABLED" .planning/STATE.md` returns at least 1 (mentioned in stopped_at or last activity)
    - YAML frontmatter is parseable: `python -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"` exits 0 (run this from project root with python3)
  </acceptance_criteria>
  <done>
    STATE.md reflects Phase 13 LIVE; progress counters incremented; decision log appended; YAML frontmatter still parses.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update marketing-site/docs/memory.html — document chat-to-brain ingestion + Haiku relevance filter</name>
  <files>marketing-site/docs/memory.html</files>
  <read_first>
    - marketing-site/docs/memory.html (entire file — confirm existing structure and style)
    - marketing-site/docs/architecture.html (if exists — reference for "section" patterns)
  </read_first>
  <action>
    Add a new section to `marketing-site/docs/memory.html`. The section should be placed AFTER the existing 7-field tagging contract section and BEFORE the closing main content area.

    Insertion content:

    ```html
    <section class="mb-12" id="chat-to-brain">
      <h2 class="text-2xl font-bold mb-4">Chat → Brain Ingestion</h2>
      <p class="mb-3">
        Every substantive chat message — across team chat, LibreChat, and Open WebUI — is automatically
        ingested into the team brain as a <code>WORKING</code> memory item. This is the core xbrain
        contract: any chat content, human or agent, any frontend, lands in a shared team-scoped brain
        and is reusable on the next conversation.
      </p>
      <h3 class="text-xl font-semibold mt-6 mb-2">Relevance filter (Claude Haiku 4.5)</h3>
      <p class="mb-3">
        Before any upsert, the message passes through a Haiku 4.5 binary classifier with ephemeral
        prompt caching. Trivial messages (greetings, single-word acks, questions without content,
        <code>@claude</code> command prefixes) are filtered out so the brain stores facts, not chatter.
        A per-team daily token budget (default 50&#x2009;000 input tokens) bounds cost.
      </p>
      <p class="mb-3">
        On Haiku error, timeout, or budget exhaustion the system falls back to a heuristic
        (≥15 chars + non-<code>@claude</code> prefix). The ingest hook is fire-and-forget — chat
        responses are never delayed by classifier latency, and a memory-api outage does not break
        the chat send.
      </p>
      <h3 class="text-xl font-semibold mt-6 mb-2">Per-turn retrieval enrichment</h3>
      <p class="mb-3">
        On every user turn (LibreChat, Open WebUI, team chat), xbrain fetches the team's top
        <code>VALIDATED</code> + <code>CANONICAL</code> facts relevant to the message (top-K=5) and
        injects them as a system message before the LLM call. Promotions via the
        <a href="brain-monitor.html" class="text-purple-600 hover:underline">Brain Monitor</a>
        are reflected in the next chat turn — no caching, no rebuild.
      </p>
      <h3 class="text-xl font-semibold mt-6 mb-2">Implementation reference</h3>
      <ul class="list-disc list-inside text-sm text-gray-700 mb-3">
        <li><code>apps/memory-api/app/services/relevance_filter.py</code> — Haiku classifier + budget cap</li>
        <li><code>POST /v1/brain/ingest</code> — fire-and-forget upsert endpoint (bridge JWT auth)</li>
        <li><code>apps/librechat-bridge/app/message_enricher.py</code> — LibreChat per-turn enricher</li>
        <li><code>apps/openwebui-pipeline/app/main.py</code> — Open WebUI ingest + Anthropic system-param injection</li>
      </ul>
    </section>
    ```

    Match the existing TailwindCSS class palette (`text-2xl`, `mb-4`, `text-purple-600`, etc. — confirm by reading the existing file first). If the file uses a slightly different class set, adapt while preserving the section structure.

    Validate: open the file mentally and confirm the closing `</main>` / `</body>` tags are still balanced.
  </action>
  <verify>
    <automated>grep -c "id=\"chat-to-brain\"" marketing-site/docs/memory.html</automated>
  </verify>
  <acceptance_criteria>
    - File `marketing-site/docs/memory.html` contains `id="chat-to-brain"` (the new section anchor — grep returns 1)
    - File contains the literal `Haiku 4.5` (grep returns at least 1)
    - File contains `relevance_filter.py` (grep returns at least 1)
    - File contains `message_enricher.py`
    - File contains `POST /v1/brain/ingest`
    - HTML is still balanced: count opening `<section` tags vs closing `</section>` tags — they must be equal (use `grep -c "<section" marketing-site/docs/memory.html` and `grep -c "</section>" marketing-site/docs/memory.html` — must match)
  </acceptance_criteria>
  <done>
    memory.html surfaces the new chat-to-brain story for public-facing docs.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update marketing-site/docs/chat.html — document per-turn enrichment behavior</name>
  <files>marketing-site/docs/chat.html</files>
  <read_first>
    - marketing-site/docs/chat.html (entire file)
  </read_first>
  <action>
    Add a small section to `marketing-site/docs/chat.html` near the bottom of the main content, summarizing the Phase 13 chat behavior in user-facing language. The page is for end-users and product teams, not developers, so keep it conceptual.

    Insertion content:

    ```html
    <section class="mb-12" id="auto-enrichment">
      <h2 class="text-2xl font-bold mb-4">Auto-enriched chat (Phase 13)</h2>
      <p class="mb-3">
        Since Phase 13, every chat reply across LibreChat and Open WebUI is automatically
        enriched with relevant team facts (validated or canonical truth level) before the model
        replies. You do not need to copy-paste context — your team's memory is already in the
        system prompt.
      </p>
      <p class="mb-3">
        Substantive user messages also automatically populate the team brain. The
        <a href="brain-monitor.html" class="text-purple-600 hover:underline">Brain Monitor</a>
        is the canonical place to promote a fact from <code>WORKING</code> to <code>VALIDATED</code>
        or <code>CANONICAL</code>. Once promoted, the fact is retrievable in any chat turn on any
        frontend (LibreChat, Open WebUI, team chat) for the same team scope.
      </p>
      <p class="mb-3">
        Trivial messages ("ok", "thanks", "@claude what's up") are filtered by a Haiku 4.5
        relevance classifier and never pollute the brain. Cost is bounded by a per-team daily
        token budget.
      </p>
    </section>
    ```

    Match the existing class palette in chat.html. Validate balanced HTML as before.
  </action>
  <verify>
    <automated>grep -c "id=\"auto-enrichment\"" marketing-site/docs/chat.html</automated>
  </verify>
  <acceptance_criteria>
    - File `marketing-site/docs/chat.html` contains `id="auto-enrichment"`
    - File contains `Phase 13`
    - File contains `Brain Monitor` (linked to brain-monitor.html)
    - HTML balanced (section open/close counts match)
  </acceptance_criteria>
  <done>
    chat.html introduces auto-enrichment to end users in plain language.
  </done>
</task>

</tasks>

<verification>
- All grep-based acceptance criteria above pass
- `python -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"` exits 0 (YAML frontmatter still parses)
- `bash -n` on no script needed (this plan touches no scripts)
- The Phase 13 ROADMAP row reflects LIVE; STATE.md reflects 13/13 phases complete; REQUIREMENTS.md is clean

The orchestrator's standing-order full-integration-check should be triggered automatically by the next reading of ROADMAP.md showing Phase 13 LIVE (per the standing order from project_xbrain_phase12_post_ship_integration_check.md).
</verification>

<success_criteria>
- MEM-04 / CHAT-03 / CHAT-07 ticked in REQUIREMENTS.md (item rows + traceability table)
- ROADMAP.md Phase 13 row reads `8/8 ✅ LIVE`
- STATE.md `progress` advances to 13/13 phases, 109/109 plans
- Marketing docs (memory.html + chat.html) describe the new behavior in user-facing terms
- All artifacts syntactically valid (YAML, Markdown, HTML)
</success_criteria>

<output>
After completion, create `.planning/phases/13-chat-brain-ingestion-retrieval-enrichment-close-the-differen/13-08-SUMMARY.md` and the phase-level `.planning/phases/13-chat-brain-ingestion-retrieval-enrichment-close-the-differen/PHASE-13-SUMMARY.md` documenting:
- Final 8/8 plan status with shipping date
- The three ticked REQ-IDs and where they're satisfied in the code
- Pointer to verify-phase13.sh PASS output
</output>
