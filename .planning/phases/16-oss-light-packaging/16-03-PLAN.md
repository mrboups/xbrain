---
phase: 16-oss-light-packaging
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - docs/INSTALL.md
  - README.md
autonomous: true
requirements: [PKG-01]

must_haves:
  truths:
    - "Install docs walk the full path from docs alone: prereqs -> provision VM -> clone -> generate secrets (make oss-init) -> docker compose up -> register via local auth -> verify — with zero external keys"
    - "Docs state the D-16-01 zero-key split: doc ingest + local embeddings + semantic retrieval are keyless; a single LLM key (Anthropic OR OpenAI OR Grok) is optional and only adds LLM extraction + the in-chat agent"
    - "Docs document the SC#4 release-artifact shape: the build-on-VM path (make deploy) + the light compose file + these install docs, and explicitly defer registry/CI multi-arch publishing to Phase 17, the extension zero-key sign-in UI to Phase 20, and the real amd64-VM run to a follow-up"
  artifacts:
    - path: "docs/INSTALL.md"
      provides: "self-contained OSS-light install guide (docs alone)"
      contains: "make oss-init"
    - path: "README.md"
      provides: "Quickstart rewritten to remove the stale Google-OAuth-client + GitHub-App hard requirement"
      min_lines: 100
  key_links:
    - from: "docs/INSTALL.md"
      to: "infrastructure/scripts/oss-init.sh"
      via: "the documented secret-generation step"
      pattern: "oss-init"
---

<objective>
Deliver the install documentation (SC#1 "install docs alone") and formalize the SC#4 release-artifact
shape (D-16-06). Today `README.md`'s Quickstart is stale: it lists "a Google OAuth client, and a
GitHub App (for auth)" as hard requirements — the exact opposite of the phase goal after Phase 18
(local auth) + Phase 19 (local embeddings) made a zero-external-key install the default.

Write `docs/INSTALL.md` as a self-contained, no-source-reading walkthrough, and rewrite the README
Quickstart to match. Document the SC#4 artifact shape honestly: the established build-on-VM path
(`make deploy`) + the light compose + these docs, with registry/CI publishing deferred to Phase 17.

Purpose: SC#1 (docs alone) + SC#4 (release-artifact shape).
Output: `docs/INSTALL.md` + a corrected README Quickstart. Pure documentation — no runtime change.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-oss-light-packaging/16-CONTEXT.md
@.planning/phases/16-oss-light-packaging/16-RESEARCH.md

@README.md
@Makefile
@docs/local-auth-recovery.md
@infrastructure/scripts/oss-init.sh

<interfaces>
<!-- Documented facts the install guide must reflect (verified in 16-RESEARCH.md): -->
- 10 core services (COMPOSE_PROFILES unset): nginx, postgres, qdrant, memory-api, minio, centrifugo, mcp-brain, mcp-gateway, mcp-scraper, brain-janitor.
- Secret generation: `make oss-init` (Plan 02) OR documented `openssl rand -hex 32` / Fernet one-liners.
- Boot: `docker compose -f infrastructure/docker-compose.yml --env-file .env up -d` (or `make up`); readiness via `make ps` / `docker compose ps` + curl http://localhost:8000/v1/healthz.
- First-run: register via `POST /v1/auth/local/register` (email/password — no OAuth). Recovery: docs/local-auth-recovery.md.
- nginx never listens on :443 in this compose (TLS terminates externally, e.g. Cloudflare, looping back to :80) — a load-bearing fact to document, not solve.
- Neo4j ~8.5s DNS-timeout at memory-api boot when NEO4J_URI is set-but-absent is EXPECTED (Phase 15), not a defect.
- SC#4 build path: `make deploy` rsyncs to the VM and runs `docker compose build && up` on the target host (build-on-VM). Future path: `docker buildx --platform linux/amd64,linux/arm64 --push` (Phase 17).
- OPEN ITEM (do NOT silently assert): LICENSE says MIT but the locked open-core design says AGPLv3+CLA — do not print a license claim in new docs without flagging it; leave the README License section as-is and add a note that the license is under review, OR omit a new license assertion entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/INSTALL.md — the docs-alone OSS-light install guide</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - .planning/phases/16-oss-light-packaging/16-RESEARCH.md (§Architecture Patterns system diagram; §Common Pitfalls 4/5/6; §Don't Hand-Roll TLS/readiness rows; §Security Domain CORS/secret-placeholder rows)
    - infrastructure/scripts/oss-init.sh (the exact command + what it generates — Plan 02)
    - docs/local-auth-recovery.md (link as the account-recovery story)
    - Makefile (up/ps/logs/deploy targets to cite)
  </read_first>
  <action>
    Create `docs/INSTALL.md` (English only) with these sections, each with copy-pasteable commands:
      1. **What you get with zero external keys** — the D-16-01 split spelled out: register + team,
         document upload/analysis, KEYLESS local embeddings + semantic retrieval, truth-levels, the
         ChatGPT-web/Claude.ai connector (via local auth — Plan 01), and API-level clip-to-memory.
         State plainly: a single LLM key (Anthropic OR OpenAI OR Grok) is OPTIONAL and only adds
         LLM-based extraction + the in-chat @agent reply; it is NOT needed for the flow above.
         Note the polished in-extension zero-key sign-in UI is Phase 20 (today's clip is proven at
         the API level / curl).
      2. **Prerequisites** — an Ubuntu 24.04 VM (or any Docker host), Docker Engine + Docker Compose v2,
         ~4 GB RAM for the OSS-light core, ports. NO Google OAuth client, NO GitHub App required.
      3. **Provision + clone** — clone the repo, `cd xbrain`.
      4. **Generate secrets (zero external keys)** — `make oss-init` (writes a bootable `.env` with
         CSPRNG secrets, EDITION=oss, COMPOSE_PROFILES empty). Show the manual `openssl rand -hex 32`
         alternative for operators who prefer editing `.env.example` by hand, and WARN never to ship
         the literal `__FILL__` placeholders. Note MINIO_ROOT_PASSWORD must be >= 8 chars.
      5. **Boot the core** — `docker compose -f infrastructure/docker-compose.yml --env-file .env up -d`
         (or `make up`). Note: expect a ~8.5s Neo4j DNS-timeout at memory-api startup with no Neo4j
         container — this is expected (Phase 15), not a failure.
      6. **Verify** — `make ps` / `docker compose ps` all core healthy; `curl -fsS http://localhost:8000/v1/healthz`.
         Point to `bash infrastructure/scripts/verify-phase16.sh` (Plan 04) as the automated proof.
      7. **First-run: register** — `curl -X POST http://localhost:8000/v1/auth/local/register -H 'Content-Type: application/json' -d '{"email":"you@example.com","password":"<>=10 chars>"}'`
         returns an `xbt_` token + a solo team_scope. Link docs/local-auth-recovery.md for lockout/reset.
      8. **Connect the ChatGPT-web / Claude.ai connector (zero-key)** — point the connector at the
         deployment's `/.well-known/oauth-authorization-server`; the sign-in leg now presents a local
         email/password form (Plan 01) instead of requiring GitHub.
      9. **Real deploy notes** — set XBRAIN_BASE_DOMAIN + OAUTH_ISSUER_URL + OAUTH_RESOURCE_URL to the
         real domain; anchor CORS_ALLOWED_ORIGIN_REGEX to your origins (never `.*`); TLS terminates
         externally (nginx listens on :80 only — front it with Cloudflare/Caddy/your reverse proxy).
     10. **Opt-in profiles** — one line each: `COMPOSE_PROFILES=integrations` (Neo4j/Graphiti/Langfuse/
         Drive/Granola/agent-runtime, ~+4 GB) and `saas` (LibreChat/Open WebUI/session-bridge, set EDITION=saas).
    Do NOT assert an MIT/AGPL license claim in this doc (open item — see interfaces).
  </action>
  <acceptance_criteria>
    - `test -f docs/INSTALL.md` and `wc -l < docs/INSTALL.md` >= 60.
    - `grep -q 'make oss-init' docs/INSTALL.md` and `grep -q '/v1/auth/local/register' docs/INSTALL.md`.
    - `grep -q 'v1/healthz' docs/INSTALL.md` and `grep -qi 'zero external key' docs/INSTALL.md`.
    - `grep -qi 'Neo4j' docs/INSTALL.md` (the expected ~8.5s timeout note) and `grep -qi 'TLS' docs/INSTALL.md`.
    - Doc does NOT list a Google OAuth client or GitHub App as a prerequisite: `grep -iq 'require.*github app\|require.*google oauth' docs/INSTALL.md` returns non-zero (absent).
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/INSTALL.md && grep -q 'make oss-init' docs/INSTALL.md && grep -q '/v1/auth/local/register' docs/INSTALL.md && grep -qi 'zero external key' docs/INSTALL.md && echo INSTALL_DOC_OK</automated>
  </verify>
  <done>An operator with no prior xbrain knowledge can stand up the OSS-light core and register, from docs/INSTALL.md alone, with zero external keys.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README Quickstart + document the SC#4 release-artifact shape</name>
  <files>README.md</files>
  <read_first>
    - README.md (whole file — Quickstart L73-102, Status L122-131, License L138-140)
    - Makefile (deploy target L64-75 — the build-on-VM path to document)
    - .planning/phases/16-oss-light-packaging/16-CONTEXT.md (D-16-06) + 16-RESEARCH.md (§State of the Art "README is stale"; §Alternatives Considered build-on-host row)
  </read_first>
  <action>
    Rewrite the README "Quickstart (self-host)" section so its Requirements line reads: an Ubuntu VM
    with Docker + Docker Compose, and ONE optional LLM key (Anthropic OR OpenAI OR Grok) for chat —
    NO Google OAuth client, NO GitHub App. Replace the `cp .env.example .env` + "Fill every __FILL__
    (secrets, OAuth, GitHub App)" flow with:
      `make oss-init` -> `docker compose ... up -d` (or `make up`) -> `make ps` -> register via
      `POST /v1/auth/local/register`. Link to `docs/INSTALL.md` for the full walkthrough.
    Add a short **Release artifacts (OSS-light)** subsection documenting SC#4 honestly:
      - The reproducible artifact bundle = (a) the light compose file `infrastructure/docker-compose.yml`
        (COMPOSE_PROFILES unset = the 10-service core), (b) the corrected `.env.example` + `make oss-init`,
        (c) `docs/INSTALL.md`, (d) the build-on-VM path `make deploy` (rsync to host + `docker compose
        build && up` on the target, so images are built on the correct architecture).
      - Explicitly DEFER: registry-hosted multi-arch image publishing + CI to Phase 17 (show the future
        `docker buildx --platform linux/amd64,linux/arm64 --push` command as the intended path, not built here);
        the standalone web app + in-extension zero-key sign-in UI to Phase 20; the fresh amd64-VM
        clean-install run to a documented follow-up (dev host is arm64; the automated proof is the
        local arm64 `docker compose up` proxy — D-16-04). NEVER build-and-deploy a locally-built image cross-arch.
    Leave the License section unchanged (the MIT/AGPLv3 discrepancy is an open item for the user — do
    not silently rewrite it; optionally add a one-line "license under review for the open-core edition" note only if you flag it as such).
  </action>
  <acceptance_criteria>
    - `grep -q 'make oss-init' README.md` and `grep -q '/v1/auth/local/register' README.md`.
    - The stale hard requirement is gone: `grep -iq 'a Google OAuth client, and a' README.md` returns non-zero (the old Requirements sentence removed).
    - `grep -qi 'build-on-VM\|build on the target\|make deploy' README.md` and `grep -qi 'Phase 17' README.md` (deferral documented).
    - `grep -qi 'buildx' README.md` (future multi-arch path shown).
    - `python -c "import ast" ` is N/A; instead assert the file is non-empty markdown: `wc -l < README.md` >= 100.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'make oss-init' README.md && grep -q '/v1/auth/local/register' README.md && grep -qi 'Phase 17' README.md && ! grep -iq 'a Google OAuth client, and a' README.md && echo README_OK</automated>
  </verify>
  <done>The README Quickstart matches the zero-external-key reality and the SC#4 release-artifact shape (build-on-VM + light compose + docs) is documented with honest deferrals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator actions | wrong guidance leads to exposed/weak deployments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering / Elevation | placeholder secrets never rotated | mitigate | INSTALL.md instructs `make oss-init` (CSPRNG) or `openssl rand` for every required secret and warns explicitly against shipping literal `__FILL__` placeholders |
| T-16-03-02 | Spoofing (CORS/TLS) | real-deploy guidance | mitigate | docs require anchoring CORS_ALLOWED_ORIGIN_REGEX (never `.*`) and document external TLS termination (nginx :80 only) so an operator does not expose a wildcard or plaintext origin |
| T-16-03-03 | Information Disclosure | stale license claim | accept | the MIT/AGPLv3 discrepancy is surfaced as an OPEN ITEM for the user, not silently resolved; new docs assert no license |
</threat_model>

<verification>
- `docs/INSTALL.md` and `README.md` pass every acceptance grep above.
- No prerequisite mentions a Google OAuth client or GitHub App as required.
- SC#4 artifact shape + Phase 17/20 deferrals documented.
</verification>

<success_criteria>
SC#1 install-docs-alone path is complete and accurate for a zero-external-key OSS-light stand-up;
SC#4's release-artifact shape (build-on-VM + light compose + install docs) is documented and reproducible,
with registry/CI, the web app, and the amd64-VM run deferred to their owning phases.
</success_criteria>

<output>
After completion, create `.planning/phases/16-oss-light-packaging/16-03-SUMMARY.md`
</output>
