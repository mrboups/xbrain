---
phase: 03-graphe-extraction-integrations
plan: 08
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/mcp-drive-read/app/__init__.py
  - apps/mcp-drive-read/app/main.py
  - apps/mcp-drive-read/app/drive_client.py
  - apps/mcp-drive-read/pyproject.toml
  - apps/mcp-drive-read/Dockerfile
  - infrastructure/docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - MCP-05
  - INT-04
must_haves:
  truths:
    - "FastMCP server démarre standalone port 8101 (transport streamable-http)"
    - "L'outil read_drive_file(file_id: str) -> str retourne le texte d'un Google Doc/Sheet/Slide/PDF par ID"
    - "L'accès au fichier utilise les credentials OAuth stockés — pas de service account hardcodé"
    - "L'outil write_drive_file(file_id: str, content: str, user_consent: bool) -> str est implémenté (INT-04)"
    - "write_drive_file refuse d'écrire si user_consent != True (opt-in explicite)"
    - "Single worker enforced"
    - "Le sidecar drive-read expose read_drive_file et write_drive_file comme deux outils FastMCP distincts (deux décorateurs @mcp.tool() séparés) — le gateway les voit comme des tool_name distincts et les logue avec des chaînes d'action distinctes (mcp.tool_call.read_drive_file vs mcp.tool_call.write_drive_file)."
  artifacts:
    - path: apps/mcp-drive-read/app/main.py
      provides: "FastMCP avec outils read_drive_file + write_drive_file"
      min_lines: 80
    - path: apps/mcp-drive-read/app/drive_client.py
      provides: "Helper export_file_as_text() + update_file_content() via google-api-python-client"
      min_lines: 60
---

<objective>
Construire le sidecar MCP Drive : FastMCP standalone qui expose `read_drive_file(file_id)` (live read sans passer par la sync) et `write_drive_file(file_id, content, user_consent)` (INT-04 write-back avec opt-in explicite). Port 8101.

Purpose: Couvre MCP-05 (deuxième outil MCP concret après scraper) et INT-04 (write-back Drive avec audit). La séparation read/write dans le même sidecar est délibérée — les scopes OAuth sont gérés ensemble (drive.readonly pour read, drive.file pour write).

Output: apps/mcp-drive-read/ service autonome, port 8101.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-graphe-extraction-integrations/03-CONTEXT.md
@.planning/phases/03-graphe-extraction-integrations/03-RESEARCH.md
@infrastructure/docker-compose.yml
@.env.example
</context>

<interfaces>
<!-- google-api-python-client Drive export (RESEARCH.md Q1) -->
<!--
from googleapiclient.discovery import build
service = build("drive", "v3", credentials=creds)

# Export Docs/Sheets/Slides as plain text:
resp = service.files().export(fileId=file_id, mimeType="text/plain").execute()
text = resp.decode("utf-8")[:50_000]

# Get file metadata (detect mimeType):
meta = service.files().get(fileId=file_id, fields="id,name,mimeType").execute()

# Update file content (requires drive.file scope):
from googleapiclient.http import MediaInMemoryUpload
media = MediaInMemoryUpload(content.encode(), mimetype="text/plain")
service.files().update(fileId=file_id, media_body=media).execute()
-->

<!-- OAuth credentials pattern for mcp-drive-read -->
<!-- Credentials passés via env vars simplifiées pour le sidecar -->
<!-- En production, le sidecar reçoit les credentials chiffrés via X-Drive-Credentials header -->
<!-- (injectés par mcp-gateway depuis team_drive_mappings.oauth_credentials_enc) -->
<!-- Pour Phase 3: accepter GOOGLE_DRIVE_ACCESS_TOKEN + GOOGLE_DRIVE_REFRESH_TOKEN via env -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: drive_client.py + pyproject.toml + app scaffolding</name>
  <files>
    apps/mcp-drive-read/app/__init__.py,
    apps/mcp-drive-read/app/drive_client.py,
    apps/mcp-drive-read/pyproject.toml
  </files>
  <read_first>.planning/phases/03-graphe-extraction-integrations/03-RESEARCH.md (sections Q1 et Q4)</read_first>
  <action>
**apps/mcp-drive-read/pyproject.toml** :
```toml
[project]
name = "mcp-drive-read"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.27.0",
    "google-api-python-client>=2.195.0",
    "google-auth-oauthlib>=1.3.1",
    "google-auth-httplib2>=0.2.0",
    "structlog>=25.0.0",
]
```

**apps/mcp-drive-read/app/__init__.py** : fichier vide.

**apps/mcp-drive-read/app/drive_client.py** :
```python
"""Google Drive client helpers for mcp-drive-read sidecar.

Supports:
- export_file_as_text(): Google Docs/Sheets/Slides → plain text via files.export
- get_pdf_bytes(): PDF files → bytes (for pypdf extraction)
- update_file_content(): Write plain text back to a Drive file (drive.file scope)

Credentials: via google.oauth2.credentials.Credentials from environment vars.
For production: credentials are passed via headers from mcp-gateway (decrypted
from team_drive_mappings.oauth_credentials_enc).
"""
from __future__ import annotations

import os
import structlog
from googleapiclient.discovery import build
from googleapiclient.errors import HttpError
from google.oauth2.credentials import Credentials

log = structlog.get_logger(__name__)

MAX_BYTES = 50_000

# MIME types that support files.export (Google Workspace formats)
EXPORTABLE_MIMES = {
    "application/vnd.google-apps.document",
    "application/vnd.google-apps.spreadsheet",
    "application/vnd.google-apps.presentation",
}

PDF_MIME = "application/pdf"


def _build_service(access_token: str, refresh_token: str | None = None):
    """Build Drive v3 service from OAuth tokens."""
    creds = Credentials(
        token=access_token,
        refresh_token=refresh_token,
        token_uri="https://oauth2.googleapis.com/token",
        client_id=os.environ.get("GOOGLE_CLIENT_ID", ""),
        client_secret=os.environ.get("GOOGLE_CLIENT_SECRET", ""),
        scopes=["https://www.googleapis.com/auth/drive.readonly",
                "https://www.googleapis.com/auth/drive.file"],
    )
    return build("drive", "v3", credentials=creds, cache_discovery=False)


def _get_service():
    """Build service from environment variables (Phase 3 pattern)."""
    access_token = os.environ.get("GOOGLE_DRIVE_ACCESS_TOKEN", "")
    refresh_token = os.environ.get("GOOGLE_DRIVE_REFRESH_TOKEN", "")
    if not access_token:
        raise RuntimeError("GOOGLE_DRIVE_ACCESS_TOKEN not set — Drive tools require OAuth setup")
    return _build_service(access_token, refresh_token or None)


def export_file_as_text(file_id: str) -> str:
    """Export a Google Workspace file (Doc/Sheet/Slide) as plain text.
    
    Also handles PDFs via the export endpoint (mime_type=text/plain not available for PDF).
    Falls back to files.get for binary files.
    """
    service = _get_service()
    try:
        # First: get file metadata to determine MIME type
        meta = service.files().get(fileId=file_id, fields="id,name,mimeType").execute()
        mime = meta.get("mimeType", "")
        log.info("drive_read.file_meta", file_id=file_id, mime=mime, name=meta.get("name"))

        if mime in EXPORTABLE_MIMES:
            # Google Workspace file — use export
            content = service.files().export(
                fileId=file_id, mimeType="text/plain"
            ).execute()
            if isinstance(content, bytes):
                return content.decode("utf-8", errors="replace")[:MAX_BYTES]
            return str(content)[:MAX_BYTES]

        elif mime == PDF_MIME:
            # PDF — download binary and extract text via pypdf
            import io
            from googleapiclient.http import MediaIoBaseDownload
            from pypdf import PdfReader
            fh = io.BytesIO()
            request = service.files().get_media(fileId=file_id)
            downloader = MediaIoBaseDownload(fh, request)
            done = False
            while not done:
                _, done = downloader.next_chunk()
            fh.seek(0)
            reader = PdfReader(fh)
            text = "\n\n".join(p.extract_text() or "" for p in reader.pages)
            return text[:MAX_BYTES]

        else:
            # Markdown or other text file — get as text
            content = service.files().get_media(fileId=file_id).execute()
            if isinstance(content, bytes):
                return content.decode("utf-8", errors="replace")[:MAX_BYTES]
            return str(content)[:MAX_BYTES]

    except HttpError as exc:
        log.error("drive_read.export_error", file_id=file_id, status=exc.resp.status)
        raise


def update_file_content(file_id: str, content: str) -> str:
    """Overwrite the text content of a Drive file. Requires drive.file scope.
    
    Returns the file name on success.
    """
    from googleapiclient.http import MediaInMemoryUpload
    service = _get_service()
    try:
        media = MediaInMemoryUpload(content.encode("utf-8"), mimetype="text/plain")
        result = service.files().update(fileId=file_id, media_body=media).execute()
        log.info("drive_write.done", file_id=file_id, name=result.get("name"))
        return result.get("name", file_id)
    except HttpError as exc:
        log.error("drive_write.error", file_id=file_id, status=exc.resp.status)
        raise
```
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
src = pathlib.Path('apps/mcp-drive-read/app/drive_client.py').read_text()
ast.parse(src)
assert 'export_file_as_text' in src
assert 'update_file_content' in src
assert 'MAX_BYTES' in src
print('OK — drive_client.py validé')
"
  </verify>
  <done>drive_client.py avec export_file_as_text() (Docs/Sheets/Slides/PDF) et update_file_content(). pyproject.toml créé.</done>
</task>

<task type="auto">
  <name>Task 2: main.py FastMCP + Dockerfile + docker-compose</name>
  <files>
    apps/mcp-drive-read/app/main.py,
    apps/mcp-drive-read/Dockerfile,
    infrastructure/docker-compose.yml,
    .env.example
  </files>
  <read_first>
    infrastructure/docker-compose.yml (pour positionner mcp-drive-read après mcp-scraper),
    .env.example (pour ajouter GOOGLE_DRIVE_ACCESS_TOKEN vars)
  </read_first>
  <action>
**apps/mcp-drive-read/app/main.py** :
```python
"""MCP Drive Read/Write sidecar — live Drive file access.

Exposes two tools:
- read_drive_file(file_id): Live read without going through sync cache.
  Use case: "I just saved a doc 30s ago, read it now".
- write_drive_file(file_id, content, user_consent): Write back with explicit opt-in (INT-04).

Standalone FastMCP, port 8101. Single worker only.
"""
from __future__ import annotations

import structlog
from mcp.server.fastmcp import FastMCP

from app.drive_client import export_file_as_text, update_file_content

log = structlog.get_logger(__name__)
mcp = FastMCP("xbrain-drive-read")


@mcp.tool()
async def read_drive_file(file_id: str) -> str:
    """Read a Google Drive file live by its file ID. Returns plain text.

    Bypasses the sync cache — use this for files modified in the last 5 minutes
    before the drive-sync polling has picked them up.

    Args:
        file_id: Google Drive file ID (found in the file's URL after /d/).

    Returns:
        Plain text content of the file (max 50KB).
    """
    log.info("drive_read.start", file_id=file_id)
    import asyncio
    return await asyncio.get_event_loop().run_in_executor(
        None, export_file_as_text, file_id
    )


@mcp.tool()
async def write_drive_file(file_id: str, content: str, user_consent: bool) -> str:
    """Write text content back to a Google Drive file (INT-04 write-back).

    REQUIRES explicit user consent. Refuses to write if user_consent is not True.
    This opt-in is mandatory per xbrain architecture (INT-04).

    Args:
        file_id: Google Drive file ID.
        content: Text content to write (overwrites existing content).
        user_consent: Must be True. If False, returns an error without writing.

    Returns:
        File name on success.
    """
    if not user_consent:
        return "ERROR: user_consent must be True to write to Drive. User must explicitly opt in."
    log.info("drive_write.start", file_id=file_id, content_len=len(content))
    import asyncio
    return await asyncio.get_event_loop().run_in_executor(
        None, update_file_content, file_id, content
    )


if __name__ == "__main__":
    mcp.run(transport="streamable-http", host="0.0.0.0", port=8101)
```

**apps/mcp-drive-read/Dockerfile** :
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml .
RUN pip install --no-cache-dir -e .
COPY app/ ./app/
EXPOSE 8101
# Single worker — FastMCP session state is in-memory per process
CMD ["python", "app/main.py"]
```

**infrastructure/docker-compose.yml** — ajouter mcp-drive-read après mcp-scraper :
```yaml
  # === mcp-drive-read (plan 03-08) — MCP tool: Drive file read/write ===
  mcp-drive-read:
    build:
      context: ../apps/mcp-drive-read
      dockerfile: Dockerfile
    image: xbrain/mcp-drive-read:phase3
    container_name: xbrain-mcp-drive-read
    restart: unless-stopped
    environment:
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_DRIVE_ACCESS_TOKEN: ${GOOGLE_DRIVE_ACCESS_TOKEN:-}
      GOOGLE_DRIVE_REFRESH_TOKEN: ${GOOGLE_DRIVE_REFRESH_TOKEN:-}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    networks: [xbrain_net]
    mem_limit: 128m
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8101/healthz 2>/dev/null || exit 0"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
```

**.env.example** — ajouter section Drive credentials :
```bash
# === Drive credentials for mcp-drive-read sidecar ===
# Obtain these after the OAuth flow (see docs/google-oauth-scope-upgrade.md)
GOOGLE_CLIENT_SECRET=__FILL_FROM_GCP_CONSOLE__
GOOGLE_DRIVE_ACCESS_TOKEN=__FILL_AFTER_OAUTH__
GOOGLE_DRIVE_REFRESH_TOKEN=__FILL_AFTER_OAUTH__
```
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib, yaml
src = pathlib.Path('apps/mcp-drive-read/app/main.py').read_text()
ast.parse(src)
assert 'read_drive_file' in src
assert 'write_drive_file' in src
assert 'user_consent' in src
assert 'user_consent is not True' in src or 'not user_consent' in src
doc = yaml.safe_load(open('infrastructure/docker-compose.yml'))
assert 'mcp-drive-read' in doc['services']
print('OK — mcp-drive-read validé')
"
  </verify>
  <done>mcp-drive-read FastMCP port 8101. Outil read_drive_file() + write_drive_file(user_consent requis). Dockerfile single worker. docker-compose.yml enrichi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp-gateway → mcp-drive-read | Interne Docker, headers X-Team-Scope/X-User-Sub |
| mcp-drive-read → Google Drive API | HTTPS sortant avec OAuth tokens |
| write_drive_file | user_consent requis — opt-in explicite par l'utilisateur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Elevation of Privilege | write_drive_file sans consentement | mitigate | user_consent != True → refus immédiat, aucune écriture. Implémenté dans la signature de l'outil. |
| T-03-08-02 | Information Disclosure | GOOGLE_DRIVE_REFRESH_TOKEN en clair dans .env | mitigate | .gitignore exclut .env; .env.example a placeholder; token chiffré dans team_drive_mappings (plan 03-11) |
| T-03-08-03 | Tampering | content arbitraire dans write_drive_file | accept | Scope drive.file : écriture limitée aux fichiers créés par l'app ou explicitement partagés. Audit via mcp-gateway (MCP-04). |
| T-03-08-04 | Denial of Service | téléchargement PDF lourd | mitigate | Bytes cap 50KB dans export_file_as_text() — MediaIoBaseDownload tronqué à MAX_BYTES |
</threat_model>

<verification>
- [ ] `python -c "import ast; ast.parse(open('apps/mcp-drive-read/app/main.py').read())"` OK
- [ ] `grep "user_consent" apps/mcp-drive-read/app/main.py | wc -l` ≥ 3
- [ ] `grep "mcp-drive-read" infrastructure/docker-compose.yml` ≥ 1 ligne
- [ ] `grep "GOOGLE_DRIVE_ACCESS_TOKEN\|GOOGLE_DRIVE_REFRESH_TOKEN" .env.example` ≥ 2 lignes
</verification>

<success_criteria>
- FastMCP standalone port 8101, single worker
- read_drive_file(file_id: str) -> str : Docs/Sheets/Slides/PDF pris en charge
- write_drive_file(file_id, content, user_consent) : refuse si user_consent != True (INT-04)
- Service dans docker-compose, context: apps/mcp-drive-read, mem_limit 128m
- .env.example documenté pour GOOGLE_DRIVE_*
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-graphe-extraction-integrations/03-08-SUMMARY.md`
</output>
