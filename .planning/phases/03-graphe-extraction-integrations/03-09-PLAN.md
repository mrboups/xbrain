---
phase: 03-graphe-extraction-integrations
plan: 09
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/mcp-calendar/app/__init__.py
  - apps/mcp-calendar/app/main.py
  - apps/mcp-calendar/app/calendar_client.py
  - apps/mcp-calendar/pyproject.toml
  - apps/mcp-calendar/Dockerfile
  - infrastructure/docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - MCP-06
must_haves:
  truths:
    - "FastMCP server démarre standalone port 8102 (transport streamable-http)"
    - "L'outil list_events(date_range: str) -> str retourne les événements Google Calendar"
    - "Le date_range par défaut est 'today+7days' si non précisé"
    - "Retour JSON-sérialisé avec id, summary, start, end, attendees pour chaque event"
    - "Single worker enforced"
    - "Service dans docker-compose avec GOOGLE_CALENDAR_ACCESS_TOKEN env var"
  artifacts:
    - path: apps/mcp-calendar/app/main.py
      provides: "FastMCP avec outil list_events(date_range: str) -> str"
      min_lines: 50
    - path: apps/mcp-calendar/app/calendar_client.py
      provides: "Helper list_user_events() via google-api-python-client calendar.events.list"
      min_lines: 50
---

<objective>
Construire le sidecar MCP Calendar : FastMCP standalone qui expose `list_events(date_range)` pour requêter Google Calendar. Port 8102. Scope calendar.readonly uniquement.

Purpose: MCP-06 exige un outil calendar queryable depuis le chat et les agents. Use case principal : l'agent contextualise sa réponse avec "vous avez une réunion avec Acme demain".

Output: apps/mcp-calendar/ service autonome, port 8102.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@infrastructure/docker-compose.yml
@.env.example
@.planning/phases/03-graphe-extraction-integrations/03-RESEARCH.md
</context>

<interfaces>
<!-- google-api-python-client Calendar v3 -->
<!--
from googleapiclient.discovery import build
service = build("calendar", "v3", credentials=creds)

# List events for a time range:
events_result = service.events().list(
    calendarId="primary",
    timeMin=time_min_rfc3339,    # e.g. "2026-05-04T00:00:00Z"
    timeMax=time_max_rfc3339,
    maxResults=50,
    singleEvents=True,           # Expand recurring events
    orderBy="startTime",
).execute()
items = events_result.get("items", [])
# Each item: {id, summary, start: {dateTime|date}, end: {dateTime|date}, attendees: [{email}]}
-->

<!-- date_range parsing contract -->
<!-- "today" → timeMin=today_00:00Z, timeMax=today_23:59Z -->
<!-- "today+7days" → timeMin=today_00:00Z, timeMax=today+7_23:59Z -->
<!-- "2026-05-04" → timeMin=that day 00:00Z, timeMax=that day 23:59Z -->
<!-- "2026-05-04:2026-05-10" → explicit range -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: calendar_client.py + pyproject.toml + scaffolding</name>
  <files>
    apps/mcp-calendar/app/__init__.py,
    apps/mcp-calendar/app/calendar_client.py,
    apps/mcp-calendar/pyproject.toml
  </files>
  <read_first>.planning/phases/03-graphe-extraction-integrations/03-RESEARCH.md (section dépendances mcp-calendar)</read_first>
  <action>
**apps/mcp-calendar/pyproject.toml** :
```toml
[project]
name = "mcp-calendar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.27.0",
    "google-api-python-client>=2.195.0",
    "google-auth-oauthlib>=1.3.1",
    "google-auth-httplib2>=0.2.0",
    "structlog>=25.0.0",
]
```

**apps/mcp-calendar/app/__init__.py** : fichier vide.

**apps/mcp-calendar/app/calendar_client.py** :
```python
"""Google Calendar v3 client helper for mcp-calendar sidecar.

Scope: calendar.readonly — read-only access to user's primary calendar.
Credentials: via GOOGLE_CALENDAR_ACCESS_TOKEN + GOOGLE_CALENDAR_REFRESH_TOKEN env vars.
"""
from __future__ import annotations

import json
import os
from datetime import datetime, timezone, timedelta
from typing import Any

import structlog
from googleapiclient.discovery import build
from googleapiclient.errors import HttpError
from google.oauth2.credentials import Credentials

log = structlog.get_logger(__name__)

MAX_EVENTS = 50


def _build_service():
    access_token = os.environ.get("GOOGLE_CALENDAR_ACCESS_TOKEN", "")
    refresh_token = os.environ.get("GOOGLE_CALENDAR_REFRESH_TOKEN", "")
    if not access_token:
        raise RuntimeError("GOOGLE_CALENDAR_ACCESS_TOKEN not set — Calendar tool requires OAuth setup")
    creds = Credentials(
        token=access_token,
        refresh_token=refresh_token or None,
        token_uri="https://oauth2.googleapis.com/token",
        client_id=os.environ.get("GOOGLE_CLIENT_ID", ""),
        client_secret=os.environ.get("GOOGLE_CLIENT_SECRET", ""),
        scopes=["https://www.googleapis.com/auth/calendar.readonly"],
    )
    return build("calendar", "v3", credentials=creds, cache_discovery=False)


def _parse_date_range(date_range: str) -> tuple[str, str]:
    """Parse date_range string into (time_min_rfc3339, time_max_rfc3339).
    
    Supported formats:
    - "today" → today 00:00Z – 23:59Z
    - "today+7days" → today 00:00Z – today+7 23:59Z (DEFAULT)
    - "2026-05-04" → that day 00:00Z – 23:59Z
    - "2026-05-04:2026-05-10" → explicit range
    """
    now = datetime.now(timezone.utc)
    today = now.replace(hour=0, minute=0, second=0, microsecond=0)

    def fmt(dt: datetime) -> str:
        return dt.strftime("%Y-%m-%dT%H:%M:%SZ")

    if ":" in date_range and not date_range.startswith("today"):
        # Explicit range: "2026-05-04:2026-05-10"
        parts = date_range.split(":", 1)
        start = datetime.fromisoformat(parts[0]).replace(tzinfo=timezone.utc)
        end = datetime.fromisoformat(parts[1]).replace(tzinfo=timezone.utc, hour=23, minute=59, second=59)
        return fmt(start), fmt(end)
    elif date_range.startswith("today+"):
        days_str = date_range.replace("today+", "").replace("days", "").strip()
        days = int(days_str)
        return fmt(today), fmt(today + timedelta(days=days, hours=23, minutes=59))
    elif date_range == "today":
        return fmt(today), fmt(today.replace(hour=23, minute=59, second=59))
    else:
        # Try ISO date
        try:
            d = datetime.fromisoformat(date_range).replace(tzinfo=timezone.utc)
            return fmt(d), fmt(d.replace(hour=23, minute=59, second=59))
        except ValueError:
            # Fallback: today + 7 days
            log.warning("calendar.invalid_date_range", input=date_range)
            return fmt(today), fmt(today + timedelta(days=7, hours=23, minutes=59))


def list_user_events(date_range: str = "today+7days") -> list[dict[str, Any]]:
    """List Google Calendar events for the given date range.
    
    Returns a list of event dicts with: id, summary, start, end, attendees.
    """
    service = _build_service()
    time_min, time_max = _parse_date_range(date_range)
    log.info("calendar.list_events", time_min=time_min, time_max=time_max)

    try:
        result = service.events().list(
            calendarId="primary",
            timeMin=time_min,
            timeMax=time_max,
            maxResults=MAX_EVENTS,
            singleEvents=True,
            orderBy="startTime",
        ).execute()
    except HttpError as exc:
        log.error("calendar.list_error", status=exc.resp.status)
        raise

    events = []
    for item in result.get("items", []):
        events.append({
            "id": item.get("id"),
            "summary": item.get("summary", "(no title)"),
            "start": item.get("start", {}).get("dateTime") or item.get("start", {}).get("date"),
            "end": item.get("end", {}).get("dateTime") or item.get("end", {}).get("date"),
            "attendees": [a.get("email") for a in item.get("attendees", []) if a.get("email")],
        })
    log.info("calendar.events_returned", count=len(events))
    return events
```
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
src = pathlib.Path('apps/mcp-calendar/app/calendar_client.py').read_text()
ast.parse(src)
assert 'list_user_events' in src
assert '_parse_date_range' in src
assert 'today+7days' in src
print('OK — calendar_client.py validé')
"
  </verify>
  <done>calendar_client.py avec list_user_events() + _parse_date_range() (today, today+Ndays, ISO date, range). pyproject.toml créé.</done>
</task>

<task type="auto">
  <name>Task 2: main.py FastMCP + Dockerfile + docker-compose</name>
  <files>
    apps/mcp-calendar/app/main.py,
    apps/mcp-calendar/Dockerfile,
    infrastructure/docker-compose.yml,
    .env.example
  </files>
  <read_first>
    infrastructure/docker-compose.yml (pour positionner mcp-calendar après mcp-drive-read)
  </read_first>
  <action>
**apps/mcp-calendar/app/main.py** :
```python
"""MCP Calendar sidecar — Google Calendar read-only.

Standalone FastMCP, port 8102. Single worker only.
Use case: agent contextualizes responses with upcoming meetings.
"""
from __future__ import annotations

import json
import asyncio
import structlog
from mcp.server.fastmcp import FastMCP
from app.calendar_client import list_user_events

log = structlog.get_logger(__name__)
mcp = FastMCP("xbrain-calendar")


@mcp.tool()
async def list_events(date_range: str = "today+7days") -> str:
    """List Google Calendar events for the given date range.

    Args:
        date_range: When to look for events. Formats:
            - "today" — events today only
            - "today+7days" — events from today for the next 7 days (DEFAULT)
            - "today+30days" — events for the next 30 days
            - "2026-05-04" — events on a specific date
            - "2026-05-04:2026-05-10" — explicit date range

    Returns:
        JSON string with a list of events.
        Each event: {id, summary, start, end, attendees: [email, ...]}.
        Returns '[]' if no events found.
    """
    events = await asyncio.get_event_loop().run_in_executor(
        None, list_user_events, date_range
    )
    return json.dumps(events, ensure_ascii=False)


if __name__ == "__main__":
    mcp.run(transport="streamable-http", host="0.0.0.0", port=8102)
```

**apps/mcp-calendar/Dockerfile** :
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml .
RUN pip install --no-cache-dir -e .
COPY app/ ./app/
EXPOSE 8102
# Single worker — FastMCP session state must not be split across processes
CMD ["python", "app/main.py"]
```

**infrastructure/docker-compose.yml** — ajouter mcp-calendar après mcp-drive-read :
```yaml
  # === mcp-calendar (plan 03-09) — MCP tool: Google Calendar read-only ===
  mcp-calendar:
    build:
      context: ../apps/mcp-calendar
      dockerfile: Dockerfile
    image: xbrain/mcp-calendar:phase3
    container_name: xbrain-mcp-calendar
    restart: unless-stopped
    environment:
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_CALENDAR_ACCESS_TOKEN: ${GOOGLE_CALENDAR_ACCESS_TOKEN:-}
      GOOGLE_CALENDAR_REFRESH_TOKEN: ${GOOGLE_CALENDAR_REFRESH_TOKEN:-}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    networks: [xbrain_net]
    mem_limit: 128m
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8102/healthz 2>/dev/null || exit 0"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
```

**.env.example** — ajouter section Calendar credentials :
```bash
# === Calendar credentials for mcp-calendar sidecar ===
# Obtain these after Google OAuth consent for calendar.readonly scope
# See docs/google-oauth-scope-upgrade.md for setup steps
GOOGLE_CALENDAR_ACCESS_TOKEN=__FILL_AFTER_OAUTH__
GOOGLE_CALENDAR_REFRESH_TOKEN=__FILL_AFTER_OAUTH__
```
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib, yaml
src = pathlib.Path('apps/mcp-calendar/app/main.py').read_text()
ast.parse(src)
assert 'list_events' in src
assert 'today+7days' in src
doc = yaml.safe_load(open('infrastructure/docker-compose.yml'))
assert 'mcp-calendar' in doc['services']
assert doc['services']['mcp-calendar']['mem_limit'] == '128m'
print('OK — mcp-calendar validé')
"
  </verify>
  <done>mcp-calendar FastMCP port 8102. Outil list_events(date_range) -> str JSON. Dockerfile single worker. docker-compose.yml enrichi. .env.example documenté.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp-gateway → mcp-calendar | Interne Docker, headers X-Team-Scope/X-User-Sub |
| mcp-calendar → Google Calendar API | HTTPS sortant, OAuth tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Information Disclosure | attendees emails dans retour | accept | Données calendrier de l'utilisateur authentifié — périmètre identique à ce que l'utilisateur voit dans son calendrier |
| T-03-09-02 | Information Disclosure | GOOGLE_CALENDAR_REFRESH_TOKEN en env | mitigate | .gitignore exclut .env; placeholder dans .env.example; token chiffré en Phase 4 si besoin |
| T-03-09-03 | Denial of Service | maxResults=50 | accept | Cap sur le nombre d'events — requête légère |
</threat_model>

<verification>
- [ ] `python -c "import ast; ast.parse(open('apps/mcp-calendar/app/main.py').read())"` OK
- [ ] `grep "list_events\|today+7days" apps/mcp-calendar/app/main.py | wc -l` ≥ 2
- [ ] `grep "mcp-calendar" infrastructure/docker-compose.yml` ≥ 1 ligne
- [ ] `grep "GOOGLE_CALENDAR_ACCESS_TOKEN" .env.example` ≥ 1 ligne
</verification>

<success_criteria>
- FastMCP standalone port 8102, single worker, transport streamable-http
- Outil list_events(date_range: str = "today+7days") -> str (JSON)
- Formats date_range pris en charge : today, today+Ndays, ISO date, range explicite
- Service dans docker-compose, context: apps/mcp-calendar, mem_limit 128m
- .env.example documenté GOOGLE_CALENDAR_*
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-graphe-extraction-integrations/03-09-SUMMARY.md`
</output>
