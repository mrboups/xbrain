---
phase: 03-graphe-extraction-integrations
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-04"]
files_modified:
  - apps/memory-api/app/routes/graph.py
  - apps/memory-api/app/main.py
  - apps/agent-runtime/app/tools/extract_facts.py
  - apps/memory-api/app/routes/memory.py
autonomous: true
requirements:
  - SRCH-05
must_haves:
  truths:
    - "GET /v1/graph/traverse?entity=X&depth=2&team_scope=T retourne les entités dépendantes (jusqu'à depth 2)"
    - "GET /v1/graph/lineage?fact_id=X&team_scope=T retourne les noeuds DERIVED_FROM d'un fait"
    - "Aucun endpoint n'expose du Cypher brut — memory-api est le seul client Neo4j"
    - "extract_facts() retourne maintenant entities: list[str] en plus des facts existants"
    - "Les entités extraites déclenchent un INSERT dans neo4j_outbox avec les bons Cypher MERGE"
    - "Les endpoints graph retournent 503 si Neo4j n'est pas connecté (graceful degrade)"
    - "Chaque upsert de fait avec metadata.entities non-vide INSERT aussi des lignes entity + relation DEPENDS_ON dans neo4j_outbox dans la même transaction que l'écriture du fait."
  artifacts:
    - path: apps/memory-api/app/routes/graph.py
      provides: "GET /v1/graph/traverse + GET /v1/graph/lineage endpoints"
      min_lines: 80
    - path: apps/agent-runtime/app/tools/extract_facts.py
      provides: "extract_facts() étendu pour retourner entities: list[str] en plus des facts"
      contains: "entities"
---

<objective>
Deux livrables complémentaires dans ce plan : (1) les endpoints `/v1/graph/*` dans memory-api qui proxifient les graph traversals Neo4j, et (2) l'extension de `extract_facts.py` dans agent-runtime pour émettre les entités NER en plus des faits — ce sont ces entités qui alimentent l'outbox pour peupler Neo4j.

Purpose: SRCH-05 exige que les traversals graph soient accessibles via memory-api (pas directement via Cypher). Sans l'extension extract_facts, Neo4j resterait vide malgré le worker.

Output: /v1/graph/* opérationnels + extract_facts() émettant des entités.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/memory-api/app/routes/memory.py
@apps/memory-api/app/main.py
@apps/agent-runtime/app/tools/extract_facts.py
@.planning/phases/03-graphe-extraction-integrations/03-CONTEXT.md
@.planning/phases/03-graphe-extraction-integrations/03-RESEARCH.md
</context>

<interfaces>
<!-- Neo4j data model (from CONTEXT.md) -->
<!-- Nodes: Entity{name, team_scope}, Fact{id, content, team_scope}, User{sub}, Conversation{id} -->
<!-- Edges: (:Entity)-[:DEPENDS_ON]->(:Entity) -->
<!--        (:Fact)-[:MENTIONS]->(:Entity) -->
<!--        (:Fact)-[:DERIVED_FROM]->(:Conversation) -->
<!--        (:User)-[:PROPOSED]->(:Fact) -->
<!--        (:User)-[:APPROVED]->(:Fact) -->

<!-- neo4j driver API (plan 03-04) -->
<!-- get_driver() -> AsyncDriver | None -->
<!-- await driver.execute_query(cypher, database_="neo4j", **params) -->

<!-- existing memory-api deps (from deps.py) -->
<!-- get_team_scope(request) -> str -->
<!-- get_current_principal(request) -> dict -->

<!-- extract_facts() current signature (from extract_facts.py) -->
<!-- async def extract_facts(document_text: str, source_url: str) -> list[dict] -->
<!-- Returns: [{content, suggested_truth_level, confidence, source, metadata}] -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Étendre extract_facts.py pour émettre des entités NER</name>
  <files>apps/agent-runtime/app/tools/extract_facts.py</files>
  <read_first>apps/agent-runtime/app/tools/extract_facts.py (entier)</read_first>
  <action>
Modifier `apps/agent-runtime/app/tools/extract_facts.py` pour étendre le retour avec une clé `entities`.

**Changements requis :**

1. Modifier le `SYSTEM_PROMPT` pour demander aussi les entités nommées :
```
Return strict JSON: {"facts": [...], "entities": [...]}

entities: list of named entities mentioned across all facts.
Each entity: {"name": "...", "type": "person|org|project|technology|concept"}
Max 15 entities. Only include entities with strong significance (skip generic terms).
```

2. Modifier `_parse_json_loose()` : inchangé, mais le résultat `data` contiendra maintenant `data.get("entities", [])`.

3. Modifier la valeur de retour de `extract_facts()` :
   - Retourner un dict avec deux clés : `{"facts": [...], "entities": [...]}`
   - Les facts ont le même format qu'avant (backward compat pour ingestion.py)
   - Les entités : `[{"name": str, "type": str}]`

4. Ajouter une fonction helper `_coerce_entity_type(raw: Any) -> str` qui normalise les types vers `person|org|project|technology|concept` (fallback: `concept`).

5. La signature de retour passe de `list[dict]` à `dict` avec clés `facts` et `entities`. Mettre à jour le type hint.

**Important — compatibilité** : les appellants actuels de `extract_facts()` dans `apps/agent-runtime/app/graphs/ingestion.py` appellent `result = await extract_facts(...)` et itèrent sur le résultat. Il faut trouver ces usages et les mettre à jour pour accéder à `result["facts"]`.

Chercher dans `apps/agent-runtime/app/graphs/ingestion.py` tous les `await extract_facts(` et les mettre à jour :
```python
# Avant
facts = await extract_facts(text, source_url)
# Après
extraction = await extract_facts(text, source_url)
facts = extraction["facts"]
entities = extraction.get("entities", [])
# entities sera utilisé par le caller pour peupler neo4j_outbox (plan 03-11 / drive-sync)
```

Note : dans la Phase 3, l'agent-runtime ingestion graph émet les entités vers memory-api via un appel séparé qui les écrit dans neo4j_outbox. Ce wiring exact est fait dans le plan 03-11 (drive-sync) qui est le principal caller Drive. Pour la Phase 3, l'ingestion agent stocke simplement les entities dans les métadonnées du fait (`metadata.entities`), et memory-api, lors du upsert, lit ces métadonnées pour enqueuer le Cypher outbox.
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
src = pathlib.Path('apps/agent-runtime/app/tools/extract_facts.py').read_text()
ast.parse(src)
assert 'entities' in src
assert '_coerce_entity_type' in src or 'entity_type' in src
# Vérifier que la signature de retour est bien un dict maintenant
assert 'facts' in src and 'dict' in src
print('OK — extract_facts.py étendu avec entities')
"
  </verify>
  <done>extract_facts.py étendu : retourne dict{facts, entities}. SYSTEM_PROMPT inclut NER. ingestion.py mis à jour pour accéder à result["facts"]. Backward compat maintenu.</done>
</task>

<task type="auto">
  <name>Task 2: Créer /v1/graph/* endpoints dans memory-api</name>
  <files>apps/memory-api/app/routes/graph.py, apps/memory-api/app/main.py</files>
  <read_first>
    apps/memory-api/app/routes/memory.py (pour le pattern de routing, deps, et auth),
    apps/memory-api/app/main.py (pour savoir comment inclure le nouveau router),
    apps/memory-api/app/neo4j_client.py (pour get_driver() — créé plan 03-04)
  </read_first>
  <action>
Créer `apps/memory-api/app/routes/graph.py` :

```python
"""/v1/graph/* — graph traversal endpoints backed by Neo4j.

All queries are scoped by team_scope (extracted from JWT). No raw Cypher exposed.
Returns 503 if Neo4j driver is not connected (graceful degrade).
"""
from __future__ import annotations

from typing import Any

from fastapi import APIRouter, Depends, HTTPException, Query
from pydantic import BaseModel

from app.deps import get_team_scope
from app.neo4j_client import get_driver

router = APIRouter()


class EntityNode(BaseModel):
    name: str
    type: str = "concept"


class TraverseResult(BaseModel):
    root: str
    depth: int
    entities: list[EntityNode]


class LineageNode(BaseModel):
    source_id: str
    source_type: str   # "Conversation" or "Drive_Document"
    fact_id: str


class LineageResult(BaseModel):
    fact_id: str
    derived_from: list[LineageNode]


def _require_driver():
    driver = get_driver()
    if driver is None:
        raise HTTPException(status_code=503, detail="Graph service unavailable — Neo4j not connected")
    return driver


@router.get("/graph/traverse", response_model=TraverseResult)
async def traverse_entity(
    entity: str = Query(..., min_length=1, max_length=256),
    depth: int = Query(default=2, ge=1, le=4),
    team_scope: str = Depends(get_team_scope),
):
    """Traverse DEPENDS_ON relationships from a named entity.
    
    Returns all entities reachable within `depth` hops from `entity`,
    scoped to `team_scope`. Used for SRCH-05: "what depends on entity X?"
    """
    driver = _require_driver()
    cypher = """
        MATCH (root:Entity {name: $name, team_scope: $team_scope})
        CALL apoc.path.subgraphNodes(root, {
            relationshipFilter: "DEPENDS_ON>",
            maxLevel: $depth
        }) YIELD node
        WHERE node <> root
        RETURN node.name AS name, coalesce(node.type, 'concept') AS type
    """
    # Fallback cypher without APOC (Community Edition — APOC not available by default)
    cypher_no_apoc = """
        MATCH (root:Entity {name: $name, team_scope: $team_scope})
              -[:DEPENDS_ON*1..$depth]->(dep:Entity)
        RETURN DISTINCT dep.name AS name, coalesce(dep.type, 'concept') AS type
    """
    try:
        result = await driver.execute_query(
            cypher_no_apoc,
            name=entity,
            team_scope=team_scope,
            depth=depth,
            database_="neo4j",
        )
        entities = [
            EntityNode(name=r["name"], type=r["type"])
            for r in result.records
        ]
        return TraverseResult(root=entity, depth=depth, entities=entities)
    except Exception as exc:
        raise HTTPException(status_code=500, detail=f"Graph traversal error: {exc}") from exc


@router.get("/graph/lineage", response_model=LineageResult)
async def get_fact_lineage(
    fact_id: str = Query(..., min_length=1, max_length=64),
    team_scope: str = Depends(get_team_scope),
):
    """Get provenance lineage for a fact node.
    
    Returns all DERIVED_FROM sources for the given fact_id,
    scoped to team_scope. Used for SRCH-05: "show lineage of fact Y".
    """
    driver = _require_driver()
    cypher = """
        MATCH (f:Fact {id: $fact_id, team_scope: $team_scope})-[:DERIVED_FROM]->(src)
        RETURN f.id AS fact_id, src.id AS source_id,
               labels(src)[0] AS source_type
    """
    try:
        result = await driver.execute_query(
            cypher,
            fact_id=fact_id,
            team_scope=team_scope,
            database_="neo4j",
        )
        derived = [
            LineageNode(
                source_id=r["source_id"],
                source_type=r["source_type"],
                fact_id=r["fact_id"],
            )
            for r in result.records
        ]
        return LineageResult(fact_id=fact_id, derived_from=derived)
    except Exception as exc:
        raise HTTPException(status_code=500, detail=f"Lineage query error: {exc}") from exc
```

**apps/memory-api/app/main.py** : inclure le router graph.
Lire le fichier, trouver où les autres routers sont inclus (`app.include_router(...)`), ajouter :
```python
from app.routes.graph import router as graph_router
app.include_router(graph_router, prefix="/v1")
```

Note sur le Cypher : le plan utilise la syntaxe `[:DEPENDS_ON*1..$depth]` (Cypher variable-length path). APOC n'est pas disponible en Community Edition sans plugin. Le cypher_no_apoc est le bon chemin pour Phase 3.
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
src = pathlib.Path('apps/memory-api/app/routes/graph.py').read_text()
ast.parse(src)
assert '/graph/traverse' in src or 'graph/traverse' in src
assert '/graph/lineage' in src or 'graph/lineage' in src
assert '503' in src
main = pathlib.Path('apps/memory-api/app/main.py').read_text()
assert 'graph_router' in main or 'graph' in main
print('OK — graph.py + router wiring validés')
"
  </verify>
  <done>routes/graph.py créé avec GET /v1/graph/traverse et GET /v1/graph/lineage. 503 si Neo4j absent. Router inclus dans main.py.</done>
</task>

<task type="auto">
  <name>Task 3: Wire outbox INSERT into memory upsert path</name>
  <files>apps/memory-api/app/routes/memory.py</files>
  <read_first>
    apps/memory-api/app/routes/memory.py (entier — pour comprendre le upsert handler et la session SQLAlchemy),
    apps/memory-api/app/db/models.py (pour la table NeoOutboxEntry — créée plan 03-04)
  </read_first>
  <action>
Modifier `apps/memory-api/app/routes/memory.py` pour que le handler `upsert_item` insère des lignes dans `neo4j_outbox` lorsque `body.item.metadata` contient une clé `entities` non-vide.

**Insertion après le `await provider.upsert(body.item)` réussi et avant `await session.commit()` :**

```python
# Neo4j outbox INSERT — enqueue Cypher rows for each entity + MENTIONS edge
entities = (body.item.metadata or {}).get("entities", [])
if entities:
    from app.db.models import NeoOutboxEntry
    for entity in entities:
        entity_name = entity.get("name", "")
        entity_type = entity.get("type", "concept")
        if not entity_name:
            continue
        # MERGE Entity node
        session.add(NeoOutboxEntry(
            cypher="MERGE (e:Entity {name: $name, team_scope: $team_scope}) ON CREATE SET e.type = $type",
            params={"name": entity_name, "type": entity_type, "team_scope": team_scope},
        ))
        # MERGE Fact node + MENTIONS edge
        session.add(NeoOutboxEntry(
            cypher=(
                "MERGE (f:Fact {id: $fact_id, team_scope: $team_scope}) "
                "MERGE (e:Entity {name: $entity_name, team_scope: $team_scope}) "
                "MERGE (f)-[:MENTIONS]->(e)"
            ),
            params={"fact_id": item_id, "entity_name": entity_name, "team_scope": team_scope},
        ))
```

Le `await session.commit()` existant en fin de handler committe toutes les lignes en une seule transaction — atomicité garantie.

**Important :** ne pas ajouter un second `commit()`. L'INSERT outbox et l'audit existant partagent la même transaction.

**Import à ajouter en tête du fichier si absent :**
```python
from app.db.models import NeoOutboxEntry  # (import local dans le bloc if suffit — voir code ci-dessus)
```
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
src = pathlib.Path('apps/memory-api/app/routes/memory.py').read_text()
ast.parse(src)
assert 'neo4j_outbox' in src or 'NeoOutboxEntry' in src, 'NeoOutboxEntry INSERT manquant'
assert 'entities' in src, 'metadata.entities lookup manquant'
assert 'MENTIONS' in src, 'edge MENTIONS manquant dans le Cypher'
print('OK — outbox INSERT câblé dans memory.py')
"
  </verify>
  <done>upsert_item dans memory.py insère des lignes NeoOutboxEntry (MERGE Entity + MERGE Fact + MERGE MENTIONS) pour chaque entité dans metadata.entities, dans la même transaction SQLAlchemy que l'audit existant.</done>
</task>

</tasks>

<key_links>
- apps/memory-api/app/routes/memory.py → apps/memory-api/app/db/models.py (NeoOutboxEntry table) via "outbox INSERT after fact upsert"
</key_links>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend/agent → /v1/graph/* | Requêtes authentifiées via JWT, team_scope extrait du token |
| memory-api → neo4j | Cypher paramétré — pas d'interpolation de string user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | /v1/graph/traverse | mitigate | team_scope extrait du JWT (pas du query param) — un user ne peut pas traverser le graphe d'une autre équipe |
| T-03-05-02 | Tampering | Cypher injection | mitigate | Tous les paramètres passés via `$name`, `$team_scope`, `$depth` — jamais interpolés dans le string Cypher |
| T-03-05-03 | Denial of Service | depth param | mitigate | depth capped à 4 (Query ge=1, le=4) — évite les traversals exponentiels |
| T-03-05-04 | Information Disclosure | entities NER dans extract_facts | accept | Entités extraites par Claude depuis des documents déjà ingérés et scopés team — pas de fuite cross-team |
</threat_model>

<verification>
- [ ] `python -c "import ast; ast.parse(open('apps/memory-api/app/routes/graph.py').read())"` OK
- [ ] `grep "503" apps/memory-api/app/routes/graph.py` retourne la ligne HTTP 503
- [ ] `grep "graph_router\|graph.router" apps/memory-api/app/main.py` retourne ≥ 1 ligne
- [ ] `python -c "import ast; ast.parse(open('apps/agent-runtime/app/tools/extract_facts.py').read())"` OK
- [ ] `grep "entities" apps/agent-runtime/app/tools/extract_facts.py | wc -l` ≥ 3
</verification>

<success_criteria>
- GET /v1/graph/traverse?entity=X&depth=2 retourne les entités DEPENDS_ON dans la limite depth
- GET /v1/graph/lineage?fact_id=Y retourne les sources DERIVED_FROM
- Les deux endpoints sont scopés team_scope via JWT (pas de param public)
- 503 si Neo4j absent — memory-api reste utilisable sans Neo4j
- extract_facts() retourne dict{facts, entities} — NER Claude, max 15 entités
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-graphe-extraction-integrations/03-05-SUMMARY.md`
</output>
