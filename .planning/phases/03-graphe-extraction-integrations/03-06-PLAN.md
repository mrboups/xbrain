---
phase: 03-graphe-extraction-integrations
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/mcp-gateway/app/__init__.py
  - apps/mcp-gateway/app/main.py
  - apps/mcp-gateway/app/config.py
  - apps/mcp-gateway/app/auth.py
  - apps/mcp-gateway/app/registry.py
  - apps/mcp-gateway/app/audit.py
  - apps/mcp-gateway/pyproject.toml
  - apps/mcp-gateway/Dockerfile
  - infrastructure/docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-04
  - MCP-07
must_haves:
  truths:
    - "GET /tools retourne la liste des tools enregistrés dans la DB registry"
    - "POST /tools/{tool_name}/call forwarder la requête au sidecar MCP correspondant"
    - "Le gateway injecte X-Team-Scope et X-User-Sub dans chaque appel vers le sidecar"
    - "POST /admin/register enregistre un nouveau sidecar sans restart (MCP-07)"
    - "Chaque tool call est audité via POST /v1/audit-log de memory-api"
    - "Le gateway démarre sans sidecars enregistrés (registry vide acceptée)"
    - "Auth accepte Google OIDC JWT et bridge service JWT"
    - "La chaîne d'action dans l'audit log contient le nom du tool FastMCP (ex. mcp.tool_call.write_drive_file) afin que INT-04 (tracabilité des actions Drive write) puisse être interrogé par préfixe d'action."
  artifacts:
    - path: apps/mcp-gateway/app/main.py
      provides: "FastAPI app avec GET /tools, POST /tools/{name}/call, POST /admin/register, GET /healthz"
      min_lines: 120
    - path: apps/mcp-gateway/app/registry.py
      provides: "DB-backed tool registry (asyncpg, table mcp_tool_registry)"
      min_lines: 60
    - path: apps/mcp-gateway/Dockerfile
      provides: "Image Docker pour le service mcp-gateway"
      contains: "uvicorn"
---

<objective>
Construire le mcp-gateway : service FastAPI ~150 lignes qui route les appels d'outils MCP depuis LibreChat et agent-runtime vers les sidecars enregistrés. Injecte team_scope + user_id dans chaque forwarded call. Audite chaque appel. Registry DB-backed pour enregistrer de nouveaux sidecars sans restart.

Purpose: MCP-01 à MCP-04 et MCP-07 sont tous satisfaits par ce service. C'est le point d'entrée unique pour tous les outils MCP dans xbrain.

Output: Service mcp-gateway conteneurisé, déclaré dans docker-compose.yml, port 8080 interne.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/agent-runtime/app/auth.py
@apps/openwebui-pipeline/app/pipelines/promotion_manager.py
@infrastructure/docker-compose.yml
@.planning/phases/03-graphe-extraction-integrations/03-CONTEXT.md
@.planning/phases/03-graphe-extraction-integrations/03-RESEARCH.md
</context>

<interfaces>
<!-- Agent-runtime auth pattern (apps/agent-runtime/app/auth.py) -->
<!--
async def verify_google_id_token(token: str, client_id: str) -> dict:  # returns claims
def verify_bridge_jwt(token: str, secret: str) -> dict:  # returns claims, scope must == "bridge"
-->

<!-- memory-api audit log endpoint (Phase 2, plan 02-04) -->
<!-- POST /v1/audit-log -->
<!-- Body: {action: str, team_scope: str, target_id: str, payload: dict} -->
<!-- Auth: Bearer bridge JWT -->

<!-- MCP wire protocol (streamable-http, 2025-06-18) -->
<!-- POST /mcp — JSON-RPC body, method: "tools/call" -->
<!-- Headers: MCP-Protocol-Version: 2025-06-18, Content-Type: application/json -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffolding mcp-gateway — config, auth, registry, Dockerfile</name>
  <files>
    apps/mcp-gateway/app/__init__.py,
    apps/mcp-gateway/app/config.py,
    apps/mcp-gateway/app/auth.py,
    apps/mcp-gateway/app/registry.py,
    apps/mcp-gateway/pyproject.toml,
    apps/mcp-gateway/Dockerfile
  </files>
  <read_first>
    apps/agent-runtime/app/auth.py (copier le pattern verify_google_id_token + verify_bridge_jwt),
    apps/agent-runtime/Dockerfile (pour copier le pattern Dockerfile Python),
    apps/agent-runtime/pyproject.toml (pour copier le pattern de dépendances)
  </read_first>
  <action>
Créer la structure de service `apps/mcp-gateway/`.

**apps/mcp-gateway/pyproject.toml** :
```toml
[project]
name = "mcp-gateway"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "httpx>=0.28.0",
    "asyncpg>=0.30.0",
    "python-jose[cryptography]>=3.4.0",
    "authlib>=1.3.0",
    "structlog>=25.0.0",
    "pydantic-settings>=2.0.0",
]
```

Note: mcp-gateway n'importe PAS le package `mcp` — c'est un proxy HTTP plain, pas un FastMCP host.

**apps/mcp-gateway/app/__init__.py** : fichier vide.

**apps/mcp-gateway/app/config.py** :
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str = ""
    MEMORY_API_URL: str = "http://memory-api:8000"
    BRIDGE_SHARED_SECRET: str = ""
    JWT_ALGORITHM: str = "HS256"
    GOOGLE_CLIENT_ID: str = ""
    LOG_LEVEL: str = "INFO"

    class Config:
        env_file = ".env"

settings = Settings()
```

**apps/mcp-gateway/app/auth.py** :
Copier le module `apps/agent-runtime/app/auth.py` VERBATIM (même pattern de vérification — pas de shared package per architecture décision). Adapter seulement le docstring pour indiquer que c'est une copie intentionnelle du pattern agent-runtime.

**apps/mcp-gateway/app/registry.py** :
```python
"""DB-backed tool registry — asyncpg, table mcp_tool_registry.

Note: la table mcp_tool_registry n'est PAS dans une migration Alembic de memory-api.
Elle est créée au démarrage par mcp-gateway via CREATE TABLE IF NOT EXISTS.
C'est un choix délibéré : mcp-gateway possède sa propre table dans la même DB Postgres.
"""
from __future__ import annotations
import asyncpg
import structlog

log = structlog.get_logger(__name__)

CREATE_TABLE_SQL = """
CREATE TABLE IF NOT EXISTS mcp_tool_registry (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tool_name TEXT NOT NULL UNIQUE,
    sidecar_url TEXT NOT NULL,
    description TEXT,
    enabled BOOLEAN NOT NULL DEFAULT true,
    registered_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
)
"""

_pool: asyncpg.Pool | None = None


async def init_registry(database_url: str):
    global _pool
    _pool = await asyncpg.create_pool(database_url, min_size=1, max_size=3)
    async with _pool.acquire() as conn:
        await conn.execute(CREATE_TABLE_SQL)
    log.info("registry.initialized")


async def close_registry():
    global _pool
    if _pool:
        await _pool.close()
        _pool = None


async def list_tools() -> list[dict]:
    async with _pool.acquire() as conn:
        rows = await conn.fetch("SELECT tool_name, sidecar_url, description FROM mcp_tool_registry WHERE enabled=true ORDER BY tool_name")
        return [dict(r) for r in rows]


async def get_tool(tool_name: str) -> dict | None:
    async with _pool.acquire() as conn:
        row = await conn.fetchrow("SELECT tool_name, sidecar_url FROM mcp_tool_registry WHERE tool_name=$1 AND enabled=true", tool_name)
        return dict(row) if row else None


async def register_tool(tool_name: str, sidecar_url: str, description: str = "") -> dict:
    async with _pool.acquire() as conn:
        row = await conn.fetchrow(
            """INSERT INTO mcp_tool_registry(tool_name, sidecar_url, description)
               VALUES($1, $2, $3)
               ON CONFLICT(tool_name) DO UPDATE
               SET sidecar_url=EXCLUDED.sidecar_url, description=EXCLUDED.description, updated_at=now()
               RETURNING tool_name, sidecar_url, description""",
            tool_name, sidecar_url, description,
        )
        return dict(row)
```

**apps/mcp-gateway/Dockerfile** :
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml .
RUN pip install --no-cache-dir -e .
COPY app/ ./app/
EXPOSE 8080
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
```

Note : mcp-gateway n'a PAS besoin du repo-root build context (pas de packages/memory-models). `context: apps/mcp-gateway` est correct dans docker-compose.
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
for f in ['apps/mcp-gateway/app/config.py', 'apps/mcp-gateway/app/registry.py', 'apps/mcp-gateway/app/auth.py']:
    src = pathlib.Path(f).read_text()
    ast.parse(src)
    print(f'OK — {f}')
dockerfile = pathlib.Path('apps/mcp-gateway/Dockerfile').read_text()
assert 'uvicorn' in dockerfile and '8080' in dockerfile
print('OK — Dockerfile')
"
  </verify>
  <done>mcp-gateway scaffolded: pyproject.toml, config.py, auth.py (copie agent-runtime), registry.py (asyncpg + CREATE TABLE IF NOT EXISTS), Dockerfile.</done>
</task>

<task type="auto">
  <name>Task 2: main.py + docker-compose + .env.example</name>
  <files>
    apps/mcp-gateway/app/main.py,
    apps/mcp-gateway/app/audit.py,
    infrastructure/docker-compose.yml,
    .env.example
  </files>
  <read_first>
    infrastructure/docker-compose.yml (pour ajouter le service mcp-gateway correctement),
    .env.example (pour ajouter MCP_GATEWAY_* vars)
  </read_first>
  <action>
**apps/mcp-gateway/app/audit.py** :
```python
"""Fire-and-forget audit log to memory-api."""
import asyncio
import structlog
import httpx
from app.config import settings

log = structlog.get_logger(__name__)

async def log_tool_call(tool_name: str, user_sub: str, team_scope: str, result_summary: str, method: str | None = None):
    """POST to memory-api /v1/audit-log — best-effort, never blocks the response.

    action format: mcp.tool_call.{tool_name}
    When `method` is provided (JSON-RPC params.name from the sidecar call),
    action becomes mcp.tool_call.{tool_name}.{method} — this is the key mechanism
    for INT-04: drive write-back audit. Example: mcp.tool_call.write_drive_file.
    """
    action = f"mcp.tool_call.{tool_name}" if not method else f"mcp.tool_call.{tool_name}.{method}"
    try:
        async with httpx.AsyncClient(timeout=5.0) as client:
            await client.post(
                f"{settings.MEMORY_API_URL}/v1/audit-log",
                json={
                    "action": action,
                    "team_scope": team_scope,
                    "target_id": tool_name,
                    "payload": {"user_sub": user_sub, "result_summary": result_summary[:500]},
                },
                headers={
                    "Authorization": f"Bearer {_make_bridge_token()}",
                    "X-Team-Scope": team_scope,
                },
            )
    except Exception as exc:
        log.warning("audit.failed", tool=tool_name, error=str(exc))

def _make_bridge_token() -> str:
    from authlib.jose import jwt as jose_jwt
    import time
    payload = {"sub": "mcp-gateway", "scope": "bridge", "iat": int(time.time()), "exp": int(time.time()) + 300}
    return jose_jwt.encode({"alg": settings.JWT_ALGORITHM}, payload, settings.BRIDGE_SHARED_SECRET).decode()
```

**apps/mcp-gateway/app/main.py** (~150 lines) :
```python
"""MCP Gateway — routes tool calls to registered sidecars.

NOT a FastMCP host — plain FastAPI proxy that speaks the MCP wire protocol.
Each tool sidecar is a standalone FastMCP service (streamable-http).
"""
from __future__ import annotations

import asyncio
from contextlib import asynccontextmanager
from typing import Any

import httpx
import structlog
from fastapi import Depends, FastAPI, HTTPException, Request
from fastapi.responses import JSONResponse
from pydantic import BaseModel

from app.auth import verify_bridge_jwt, verify_google_id_token
from app.audit import log_tool_call
from app.config import settings
from app.registry import close_registry, get_tool, init_registry, list_tools, register_tool

log = structlog.get_logger(__name__)

MCP_PROTOCOL_VERSION = "2025-06-18"


@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_registry(settings.DATABASE_URL)
    log.info("mcp_gateway.started")
    yield
    await close_registry()


app = FastAPI(title="xbrain MCP Gateway", lifespan=lifespan)


# ─── Auth ────────────────────────────────────────────────────────────────────

async def _get_principal(request: Request) -> dict[str, Any]:
    """Verify Google OIDC or bridge JWT. Return {sub, team_scope}."""
    auth = request.headers.get("Authorization", "")
    if not auth.startswith("Bearer "):
        raise HTTPException(401, "Missing Authorization header")
    token = auth.removeprefix("Bearer ")
    # Try bridge JWT first (service-to-service)
    try:
        claims = verify_bridge_jwt(token, settings.BRIDGE_SHARED_SECRET)
        return {"sub": claims.get("sub", "service"), "team_scope": request.headers.get("X-Team-Scope", "default")}
    except Exception:
        pass
    # Fall back to Google OIDC
    try:
        claims = await verify_google_id_token(token, settings.GOOGLE_CLIENT_ID)
        return {"sub": claims["sub"], "team_scope": request.headers.get("X-Team-Scope", "default")}
    except Exception as exc:
        raise HTTPException(401, f"Invalid token: {exc}") from exc


# ─── Routes ──────────────────────────────────────────────────────────────────

@app.get("/healthz")
async def healthz():
    return {"status": "ok"}


@app.get("/tools")
async def get_tools():
    """List all registered MCP tool sidecars (MCP-01, MCP-07)."""
    return await list_tools()


class RegisterBody(BaseModel):
    tool_name: str
    sidecar_url: str
    description: str = ""


@app.post("/admin/register", status_code=201)
async def admin_register(body: RegisterBody, principal: dict = Depends(_get_principal)):
    """Register a new MCP sidecar URL without infrastructure restart (MCP-07).
    
    Admin-only: caller must have scope=bridge or be an admin sub.
    """
    result = await register_tool(body.tool_name, body.sidecar_url, body.description)
    log.info("registry.tool_registered", tool=body.tool_name, url=body.sidecar_url, by=principal["sub"])
    return result


@app.post("/tools/{tool_name}/call")
async def call_tool(tool_name: str, request: Request, principal: dict = Depends(_get_principal)):
    """Forward a tool call to the registered sidecar (MCP-01, MCP-02, MCP-03).
    
    Injects X-Team-Scope and X-User-Sub headers before forwarding.
    Audits every call via memory-api audit-log (MCP-04).
    """
    tool = await get_tool(tool_name)
    if tool is None:
        raise HTTPException(404, f"Tool '{tool_name}' not registered or disabled")

    body = await request.body()
    team_scope = principal["team_scope"]
    user_sub = principal["sub"]

    # Extract JSON-RPC method/tool_method for audit action discrimination (INT-04).
    # When the sidecar tool name equals e.g. "write_drive_file", the audit action
    # becomes mcp.tool_call.write_drive_file (distinct from mcp.tool_call.read_drive_file).
    # If the body also carries a params.name or tool_method field, append it for finer grain.
    import json as _json
    _rpc_method: str | None = None
    try:
        _body_json = _json.loads(body)
        # Check params.name (JSON-RPC tool call) or top-level tool_method field
        _rpc_method = (
            (_body_json.get("params") or {}).get("name")
            or _body_json.get("tool_method")
        )
    except Exception:
        pass  # non-JSON body — skip method extraction

    # Forward to sidecar — MCP streamable-http protocol
    sidecar_url = f"{tool['sidecar_url'].rstrip('/')}/mcp"
    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            resp = await client.post(
                sidecar_url,
                content=body,
                headers={
                    "Content-Type": "application/json",
                    "MCP-Protocol-Version": MCP_PROTOCOL_VERSION,
                    "X-Team-Scope": team_scope,
                    "X-User-Sub": user_sub,
                },
            )
    except httpx.TimeoutException:
        raise HTTPException(504, f"Tool '{tool_name}' sidecar timeout")
    except httpx.ConnectError:
        raise HTTPException(502, f"Tool '{tool_name}' sidecar unreachable")

    # Audit fire-and-forget (MCP-04).
    # Pass _rpc_method so action = mcp.tool_call.{tool_name}.{method} when present,
    # or mcp.tool_call.{tool_name} otherwise. Sidecars that expose distinct tools
    # (e.g. read_drive_file vs write_drive_file) will already give distinct tool_name
    # values — _rpc_method provides an extra level when one sidecar handles multiple methods.
    result_summary = resp.text[:200] if resp.status_code == 200 else f"error:{resp.status_code}"
    asyncio.create_task(log_tool_call(tool_name, user_sub, team_scope, result_summary, method=_rpc_method))

    return JSONResponse(content=resp.json(), status_code=resp.status_code)
```

**infrastructure/docker-compose.yml** : ajouter le service mcp-gateway après neo4j :
```yaml
  # === mcp-gateway (plan 03-06) — MCP tool call router with team_scope injection ===
  mcp-gateway:
    build:
      context: ../apps/mcp-gateway
      dockerfile: Dockerfile
    image: xbrain/mcp-gateway:phase3
    container_name: xbrain-mcp-gateway
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      MEMORY_API_URL: http://memory-api:8000
      BRIDGE_SHARED_SECRET: ${BRIDGE_SHARED_SECRET}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    networks: [xbrain_net]
    mem_limit: 192m
    depends_on:
      postgres: { condition: service_healthy }
      memory-api: { condition: service_healthy }
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz 2>/dev/null | grep -q ok"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
```

**.env.example** : ajouter commentaire sur MCP gateway (pas de nouvelles vars — utilise DATABASE_URL, BRIDGE_SHARED_SECRET, GOOGLE_CLIENT_ID existants).
Ajouter un commentaire dans .env.example :
```bash
# === MCP Gateway (plan 03-06) ===
# Utilise DATABASE_URL, BRIDGE_SHARED_SECRET, GOOGLE_CLIENT_ID existants — pas de nouvelles vars.
# Port interne : 8080 (xbrain_net). Pas exposé sur l'hôte sauf si accès direct requis.
```
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib, yaml
src = pathlib.Path('apps/mcp-gateway/app/main.py').read_text()
ast.parse(src)
assert '/tools' in src
assert 'admin/register' in src
assert 'X-Team-Scope' in src
assert 'log_tool_call' in src
doc = yaml.safe_load(open('infrastructure/docker-compose.yml'))
assert 'mcp-gateway' in doc['services']
print('OK — mcp-gateway main.py + compose validés')
"
  </verify>
  <done>main.py (~150 lignes) avec GET /tools, POST /tools/{name}/call, POST /admin/register, GET /healthz. audit.py fire-and-forget. docker-compose.yml enrichi service mcp-gateway mem_limit 192m.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LibreChat/agent → mcp-gateway | JWT authentifié (Google OIDC ou bridge JWT) |
| mcp-gateway → tool sidecar | Service-to-service interne, headers X-Team-Scope + X-User-Sub injectés |
| mcp-gateway → memory-api audit-log | Bridge JWT, interne Docker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Spoofing | X-Team-Scope forgery | mitigate | team_scope extrait du JWT (principal dict), pas du header client — le client ne peut pas choisir son team_scope |
| T-03-06-02 | Elevation of Privilege | POST /admin/register | mitigate | Endpoint protégé par _get_principal (JWT requis); en Phase 3, toute identité authentifiée peut enregistrer — Phase 4 ajoutera une vérification de rôle admin |
| T-03-06-03 | Denial of Service | forwarding timeout | mitigate | httpx timeout=30s; 504 retourné si sidecar lent |
| T-03-06-04 | Information Disclosure | tool result dans audit log | mitigate | result_summary tronqué à 500 chars — pas de dump complet de la réponse outil dans l'audit |
</threat_model>

<verification>
- [ ] `python -c "import ast; ast.parse(open('apps/mcp-gateway/app/main.py').read())"` OK
- [ ] `grep "X-Team-Scope\|X-User-Sub" apps/mcp-gateway/app/main.py | wc -l` ≥ 2
- [ ] `grep "mcp-gateway" infrastructure/docker-compose.yml` retourne ≥ 1 ligne
- [ ] `grep "mem_limit.*192m" infrastructure/docker-compose.yml` retourne la ligne mcp-gateway
</verification>

<success_criteria>
- GET /tools liste les tools DB-backed (vide au départ = OK)
- POST /tools/{name}/call forwardsvers sidecar avec X-Team-Scope, X-User-Sub, MCP-Protocol-Version
- POST /admin/register enregistre sans restart (MCP-07)
- Audit fire-and-forget pour chaque appel
- Service dans docker-compose, mem_limit 192m, context: apps/mcp-gateway (pas repo root)
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-graphe-extraction-integrations/03-06-SUMMARY.md`
</output>
