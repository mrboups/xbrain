---
phase: 03-graphe-extraction-integrations
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - apps/memory-api/app/config.py
  - apps/memory-api/app/main.py
  - apps/memory-api/app/neo4j_client.py
  - apps/memory-api/app/outbox_worker.py
  - apps/memory-api/pyproject.toml
  - infrastructure/docker-compose.yml
autonomous: true
requirements:
  - SRCH-05
must_haves:
  truths:
    - "memory-api démarre avec neo4j driver AsyncGraphDatabase initialisé dans lifespan"
    - "Si NEO4J_URI est absent, memory-api démarre quand même (graceful degrade — Neo4j optionnel en dev)"
    - "Le background worker drains neo4j_outbox toutes les 2s, SKIP LOCKED, batch de 50 lignes max"
    - "Le worker fonctionne correctement avec --workers 2 grâce à FOR UPDATE SKIP LOCKED"
    - "Les erreurs Cypher sont capturées, stockées dans outbox.error, et ne bloquent pas les autres rows"
  artifacts:
    - path: apps/memory-api/app/neo4j_client.py
      provides: "AsyncGraphDatabase driver, get_neo4j_driver() accessor, verify_connectivity() on startup"
      min_lines: 40
    - path: apps/memory-api/app/outbox_worker.py
      provides: "drain_outbox() coroutine, asyncio.Task lancé dans lifespan"
      min_lines: 50
    - path: apps/memory-api/app/main.py
      provides: "lifespan context intègre neo4j driver init + outbox worker task"
      contains: "drain_outbox"
---

<objective>
Intégrer le driver Neo4j async dans memory-api et démarrer le background outbox worker en lifespan. Le worker draine `neo4j_outbox` toutes les 2 secondes et exécute les Cypher queries accumulées.

Purpose: C'est la connexion entre Postgres (source of truth) et Neo4j (graph read-replica). Sans ce worker, les faits upsertés dans memory-api ne se propagent jamais vers Neo4j. Le plan 03-05 (graph endpoints) dépend de ce worker pour avoir des données à requêter.

Output: memory-api avec driver neo4j actif + worker en fond + graceful degrade si Neo4j non configuré.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/memory-api/app/main.py
@apps/memory-api/app/config.py
@apps/memory-api/pyproject.toml
@infrastructure/docker-compose.yml
@.planning/phases/03-graphe-extraction-integrations/03-RESEARCH.md
</context>

<interfaces>
<!-- memory-api lifespan pattern (from app/main.py) -->
<!-- Le lifespan FastAPI est déjà utilisé pour initialiser le MemoryProvider -->
<!-- Pattern attendu : asynccontextmanager, yield, cleanup -->

<!-- neo4j driver API (neo4j==6.1.0) -->
<!--
from neo4j import AsyncGraphDatabase

driver = AsyncGraphDatabase.driver(uri, auth=(user, password))
await driver.verify_connectivity()  # fail fast on boot
async with driver.session(database="neo4j") as session:
    await session.run("MATCH (n) RETURN count(n)")
# ou plus simplement:
await driver.execute_query("MERGE (e:Entity {name: $name})", name=entity, database_="neo4j")
await driver.close()
-->

<!-- asyncpg pool existant dans memory-api (accessible via app.state.pool ou via get_session) -->
<!-- outbox worker doit acquérir une connexion PG séparée du pool FastAPI -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: neo4j_client.py — driver factory + graceful degrade</name>
  <files>apps/memory-api/app/neo4j_client.py, apps/memory-api/app/config.py, apps/memory-api/pyproject.toml</files>
  <read_first>
    apps/memory-api/app/config.py (pour le pattern Settings existant et les champs existants),
    apps/memory-api/pyproject.toml (pour vérifier si neo4j est déjà dans les dépendances)
  </read_first>
  <action>
**apps/memory-api/pyproject.toml** : ajouter `neo4j>=6.1.0` dans les dépendances si absent.

**apps/memory-api/app/config.py** : ajouter dans la classe Settings :
```python
# Neo4j (optional — graceful degrade if not set)
NEO4J_URI: str = ""          # e.g. bolt://neo4j:7687
NEO4J_USER: str = "neo4j"
NEO4J_PASSWORD: str = ""
```

**apps/memory-api/app/neo4j_client.py** (nouveau fichier) :
```python
"""Neo4j AsyncGraphDatabase driver — singleton, graceful degrade if not configured.

If NEO4J_URI or NEO4J_PASSWORD is empty, all functions are no-ops (returns None).
This lets memory-api boot in dev environments without a running Neo4j.
"""
from __future__ import annotations

import structlog
from app.config import settings

log = structlog.get_logger(__name__)

_driver = None


async def init_driver():
    """Create and verify AsyncGraphDatabase driver. Called from lifespan.
    
    Returns driver if configured, None if not (graceful degrade).
    """
    global _driver
    if not (settings.NEO4J_URI and settings.NEO4J_PASSWORD):
        log.warning("neo4j.disabled", reason="NEO4J_URI or NEO4J_PASSWORD not set — graph sync disabled")
        return None
    from neo4j import AsyncGraphDatabase
    _driver = AsyncGraphDatabase.driver(
        settings.NEO4J_URI,
        auth=(settings.NEO4J_USER, settings.NEO4J_PASSWORD),
    )
    try:
        await _driver.verify_connectivity()
        log.info("neo4j.connected", uri=settings.NEO4J_URI)
    except Exception as exc:
        log.error("neo4j.connectivity_failed", error=str(exc))
        # Don't crash — memory-api is still usable without Neo4j
        await _driver.close()
        _driver = None
    return _driver


async def close_driver():
    global _driver
    if _driver is not None:
        await _driver.close()
        _driver = None


def get_driver():
    """Return the driver or None if not configured/connected."""
    return _driver
```

Points critiques :
- Graceful degrade : si `NEO4J_URI` ou `NEO4J_PASSWORD` est vide (string vide), le driver n'est pas initialisé et toutes les fonctions sont des no-ops. memory-api boot normalement.
- `verify_connectivity()` en startup pour fail-fast en production mais sans crasher — juste un warning si Neo4j est down.
- Singleton `_driver` global : partagé entre le worker et les routes graph.
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
src = pathlib.Path('apps/memory-api/app/neo4j_client.py').read_text()
ast.parse(src)
assert 'init_driver' in src
assert 'get_driver' in src
assert 'graceful' in src.lower() or 'NEO4J_URI' in src
print('OK — neo4j_client.py syntaxe valide')
"
  </verify>
  <done>neo4j_client.py créé avec init_driver(), close_driver(), get_driver(). Settings enrichi NEO4J_*. Graceful degrade si non configuré.</done>
</task>

<task type="auto">
  <name>Task 2: outbox_worker.py + lifespan wiring dans main.py</name>
  <files>apps/memory-api/app/outbox_worker.py, apps/memory-api/app/main.py</files>
  <read_first>
    apps/memory-api/app/main.py (pour le pattern lifespan existant et comment le modifier),
    apps/memory-api/app/config.py (pour DATABASE_URL)
  </read_first>
  <action>
**apps/memory-api/app/outbox_worker.py** (nouveau fichier) :
```python
"""Background asyncio task — drains neo4j_outbox to Neo4j every 2s.

Uses SELECT ... FOR UPDATE SKIP LOCKED so multiple memory-api workers (--workers 2)
don't race on the same outbox rows.

Pattern:
  - Fetch up to 50 unprocessed rows per tick
  - Execute Cypher query via neo4j driver
  - Mark processed=true, set processed_at=now()
  - On Cypher error: set error=<message>, processed=true (don't retry indefinitely)
  - Sleep 2s between ticks
"""
from __future__ import annotations

import asyncio
import json
from datetime import datetime, timezone

import structlog

log = structlog.get_logger(__name__)

BATCH_SIZE = 50
DRAIN_INTERVAL_SECONDS = 2


async def drain_outbox(pg_dsn: str) -> None:
    """Long-running coroutine. Launched as asyncio.Task in FastAPI lifespan."""
    import asyncpg
    from app.neo4j_client import get_driver

    pool = await asyncpg.create_pool(pg_dsn, min_size=1, max_size=2)
    log.info("outbox_worker.started")
    while True:
        try:
            driver = get_driver()
            if driver is None:
                # Neo4j not configured — nothing to drain
                await asyncio.sleep(DRAIN_INTERVAL_SECONDS)
                continue

            async with pool.acquire() as conn:
                async with conn.transaction():
                    rows = await conn.fetch(
                        """
                        SELECT id, cypher, params FROM neo4j_outbox
                        WHERE processed = false
                        ORDER BY created_at
                        LIMIT $1
                        FOR UPDATE SKIP LOCKED
                        """,
                        BATCH_SIZE,
                    )

                    for row in rows:
                        cypher = row["cypher"]
                        params = json.loads(row["params"]) if isinstance(row["params"], str) else dict(row["params"])
                        try:
                            await driver.execute_query(cypher, database_="neo4j", **params)
                            await conn.execute(
                                "UPDATE neo4j_outbox SET processed=true, processed_at=$2 WHERE id=$1",
                                row["id"],
                                datetime.now(timezone.utc),
                            )
                        except Exception as exc:
                            # Mark as processed with error — avoids infinite retry on bad Cypher
                            log.error("outbox_worker.cypher_error", id=str(row["id"]), error=str(exc))
                            await conn.execute(
                                "UPDATE neo4j_outbox SET processed=true, processed_at=$2, error=$3 WHERE id=$1",
                                row["id"],
                                datetime.now(timezone.utc),
                                str(exc)[:2000],
                            )

            if rows:
                log.info("outbox_worker.drained", count=len(rows))

        except asyncio.CancelledError:
            log.info("outbox_worker.cancelled")
            break
        except Exception as exc:
            log.error("outbox_worker.tick_error", error=str(exc))

        await asyncio.sleep(DRAIN_INTERVAL_SECONDS)

    await pool.close()
```

**apps/memory-api/app/main.py** : modifier le lifespan pour intégrer neo4j init + outbox worker.

Lire le fichier actuel, puis ajouter dans le lifespan asynccontextmanager (avant le yield) :
```python
from app.neo4j_client import init_driver, close_driver
from app.outbox_worker import drain_outbox
from app.config import settings

# Init Neo4j driver (graceful degrade if not configured)
await init_driver()

# Start outbox worker background task
_outbox_task = asyncio.create_task(drain_outbox(settings.DATABASE_URL))
```

Et après le yield (cleanup) :
```python
_outbox_task.cancel()
try:
    await _outbox_task
except asyncio.CancelledError:
    pass
await close_driver()
```

Ajouter également `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD` dans la section environment de memory-api dans `infrastructure/docker-compose.yml` :
```yaml
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
```

Et ajouter `neo4j: { condition: service_healthy }` dans `depends_on` de memory-api (optionnel — si Neo4j est absent, memory-api démarre quand même grâce au graceful degrade).
  </action>
  <verify>
    cd D:/VSC/xbrain && python -c "
import ast, pathlib
src = pathlib.Path('apps/memory-api/app/outbox_worker.py').read_text()
ast.parse(src)
assert 'SKIP LOCKED' in src
assert 'drain_outbox' in src
assert 'CancelledError' in src
main = pathlib.Path('apps/memory-api/app/main.py').read_text()
assert 'drain_outbox' in main
assert 'init_driver' in main
print('OK — outbox_worker.py et main.py wiring validés')
"
  </verify>
  <done>outbox_worker.py créé avec SKIP LOCKED, batch 50, error capture. main.py lifespan intègre neo4j init + worker task. docker-compose.yml enrichi NEO4J_* vars sur memory-api.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outbox_worker → neo4j | Bolt interne, worker utilise le même driver que les routes graph |
| outbox_worker → postgres | Pool asyncpg séparé (pas le pool FastAPI) — évite la contention |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Denial of Service | outbox_worker busy-loop | mitigate | asyncio.sleep(2) entre ticks; CancelledError géré proprement |
| T-03-04-02 | Tampering | Cypher injection via outbox.cypher | accept | Seul memory-api écrit dans neo4j_outbox (pas d'endpoint public); paramètres Cypher passés via `**params` (pas de string interpolation) |
| T-03-04-03 | Information Disclosure | outbox.error field | accept | Erreur stockée en DB interne — pas exposée via API publique |
</threat_model>

<verification>
- [ ] `python -c "import ast; ast.parse(open('apps/memory-api/app/neo4j_client.py').read())"` OK
- [ ] `python -c "import ast; ast.parse(open('apps/memory-api/app/outbox_worker.py').read())"` OK
- [ ] `grep "SKIP LOCKED" apps/memory-api/app/outbox_worker.py` retourne la ligne SQL
- [ ] `grep "drain_outbox\|init_driver" apps/memory-api/app/main.py` retourne ≥ 2 lignes
- [ ] `grep "NEO4J_URI\|NEO4J_PASSWORD" infrastructure/docker-compose.yml` retourne ≥ 2 lignes
</verification>

<success_criteria>
- neo4j_client.py : init_driver() + close_driver() + get_driver() avec graceful degrade
- outbox_worker.py : drain_outbox() avec SKIP LOCKED, batch 50, sleep 2s, error capture
- main.py lifespan : neo4j init + outbox task lancé, cleanup propre sur shutdown
- docker-compose.yml : memory-api a NEO4J_URI, NEO4J_USER, NEO4J_PASSWORD
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-graphe-extraction-integrations/03-04-SUMMARY.md`
</output>
