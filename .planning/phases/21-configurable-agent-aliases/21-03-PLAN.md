---
phase: 21-configurable-agent-aliases
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - apps/memory-api/tests/test_agent_aliases_gate.py
autonomous: true
requirements: [AGENT-01]

must_haves:
  truths:
    - "A team's custom alias summons the agent through the REAL team_chat POST path"
    - "A different team that did not set that alias does NOT summon on it"
    - "@agent summons on every team regardless of custom config"
    - "@claude summons on no team"
    - "Migration 0025 applies forward-only under EDITION=oss AND saas, leaving teams.agent_aliases present"
  artifacts:
    - path: "apps/memory-api/tests/test_agent_aliases_gate.py"
      provides: "Real-Postgres per-team summon gate + edition migration proof"
      contains: "handle_claude_mention"
  key_links:
    - from: "POST /v1/teams/{id}/messages"
      to: "team_chat_agent.handle_claude_mention (recorded)"
      via: "mention_detector.detect on the team's effective aliases"
      pattern: "handle_claude_mention"
    - from: "alembic upgrade head (oss + saas)"
      to: "information_schema.columns teams.agent_aliases"
      via: "column exists post-upgrade under both editions"
      pattern: "agent_aliases"
---

<objective>
The single definitive gate for Phase 21: prove per-team summoning through the REAL
`team_chat` POST path against a REAL Postgres, and prove migration 0025 applies
forward-only under both editions. This is the "gate lesson" made executable — a
mocked detector or an in-memory assertion proves nothing; SKIP=FAIL.

Purpose: make SC#1 (custom alias summons for THAT team, not another), SC#2
(@agent-always, @claude-never), and D-21-03 (forward-only, edition-agnostic
migration) undeniable at the integration boundary.
Output: `tests/test_agent_aliases_gate.py` — test-only, no production changes.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-configurable-agent-aliases/21-CONTEXT.md
@.planning/phases/21-configurable-agent-aliases/21-02-SUMMARY.md

<interfaces>
<!-- Everything this gate needs already exists after 21-01/21-02. -->

POST body + route (app/routes/team_chat.py):
```
POST /v1/teams/{team_id}/messages  body {"content": str, "parent_message_id": null, "media": null}
  -> on mention: asyncio.create_task(team_chat_agent.handle_claude_mention(team_id=..., triggering_message_id=..., triggering_user_sub=...))
```
The mention decision uses `mention_detector.detect(body.content, mention_detector.effective_aliases(team.agent_aliases))`.

Test scaffolding to reuse:
- conftest.py: `client` fixture (httpx.ASGITransport bound to app, get_session overridden to the test session), `seeded_two_teams` (alice=admin team-a, bob=admin team-b), `session`.
- test_admin_brain.py:44-101 — install/remove a `get_current_principal` override returning `{"kind":"user","user":<user>}`.
- test_migration_editions.py:104-199 — `_run_upgrade_head(edition)` spins a fresh Postgres, patches the config singleton to the edition, runs `alembic upgrade head`; reuse this helper's PATTERN (fresh container per edition, singleton patch — os.environ alone is inert).
- repos/teams.py::set_agent_aliases — set a team's custom alias in the test DB.
</interfaces>

Anchors (already read in planning):
- team_chat.py:203-259 (POST handler, the create_task(handle_claude_mention) branch).
- conftest.py:161-217 (client/session/seeded_two_teams fixtures).
- test_migration_editions.py:104-199 (_run_upgrade_head + _script_head).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-path per-team summon gate (POST → detect → enqueue)</name>
  <files>apps/memory-api/tests/test_agent_aliases_gate.py</files>
  <read_first>
    - apps/memory-api/app/routes/team_chat.py:203-259 (POST handler; how handle_claude_mention is scheduled)
    - apps/memory-api/tests/conftest.py:161-217 (client + seeded_two_teams + session)
    - apps/memory-api/tests/test_admin_brain.py:44-101 (get_current_principal override)
    - apps/memory-api/app/repos/teams.py::set_agent_aliases (from 21-02)
  </read_first>
  <action>
    Create `tests/test_agent_aliases_gate.py` (`@pytest.mark.integration`). Strategy:
    the agent handler + Centrifugo publish + brain ingest are fire-and-forget
    `asyncio.create_task` calls; the deterministic signal is WHETHER
    `team_chat_agent.handle_claude_mention` gets scheduled. Record it without running
    the real handler:
    - `monkeypatch.setattr("app.services.team_chat_agent.handle_claude_mention", <async recorder>)`
      where the recorder appends `kwargs["team_id"]` to a list and returns immediately.
      (team_chat.py calls it via the module reference, so patching the attribute intercepts it.)
    - Also monkeypatch `centrifugo_client.publish` and `brain_ingest.ingest_team_message`
      to inert async no-ops so the POST's background tasks don't emit noise/errors.
    - Install a `get_current_principal` override to act AS a given seeded user.
    Then, using `seeded_two_teams` (team-a alice-admin, team-b bob-admin) with alice and
    bob each a member of their own team:
    1. `await teams_repo.set_agent_aliases(session, team_id=team_a.id, aliases_csv="wizard")`; commit.
    2. As alice, POST `/v1/teams/{team_a.id}/messages` content `"@wizard summarize"` ->
       200 AND the recorder captured team_a.id (custom alias summons for team A).
    3. As bob, POST `/v1/teams/{team_b.id}/messages` content `"@wizard summarize"` ->
       200 AND the recorder did NOT fire for team B (team B never set "wizard").
    4. As alice, POST team-a content `"@agent hi"` -> recorder fires (universal default).
       As bob, POST team-b content `"@agent hi"` -> recorder fires (universal default on
       a team with NO custom alias).
    5. As alice, POST team-a content `"@claude hi"` -> recorder does NOT fire.
       As bob, POST team-b content `"@claude hi"` -> recorder does NOT fire.
    Assert on a fresh recorder list per case (clear between POSTs). Give background
    tasks a chance to be scheduled: since the decision + create_task happen inline in the
    handler before returning, the recorder is populated synchronously via the patched
    coroutine being awaited-or-scheduled — assert after `await` of the client POST and,
    if needed, one `await asyncio.sleep(0)` to let a scheduled task start. Prefer asserting
    the create_task was ISSUED (patch `asyncio.create_task`? no — simpler: the recorder is
    an async fn; the scheduled task runs on the next loop tick, so `await asyncio.sleep(0)`
    after the POST is sufficient and deterministic).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_agent_aliases_gate.py -q -k summon` passes and is NOT skipped when Docker is present.
    - Case 2 asserts team_a.id in the recorder; Case 3 asserts the recorder is empty for the "@wizard" POST to team B.
    - Case 4 asserts the recorder fired for BOTH teams on "@agent".
    - Case 5 asserts the recorder is empty for "@claude" on both teams.
    - The real `team_chat_agent.handle_claude_mention` is NOT executed (recorder replaces it) — no Anthropic call, no network.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -m pytest tests/test_agent_aliases_gate.py -q -k summon</automated>
  </verify>
  <done>The real POST → detect → enqueue path proves: custom alias summons for its team only, @agent summons everywhere, @claude summons nowhere — against real Postgres, no mock detector.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0025 forward-only + edition-agnostic column proof</name>
  <files>apps/memory-api/tests/test_agent_aliases_gate.py</files>
  <read_first>
    - apps/memory-api/tests/test_migration_editions.py:104-199 (_run_upgrade_head fresh-container-per-edition + singleton patch; _docker_available gate)
    - apps/memory-api/alembic/versions/0025_team_agent_aliases.py (from 21-01)
  </read_first>
  <action>
    Add a second test group to `tests/test_agent_aliases_gate.py` proving 0025 is
    forward-only and edition-agnostic for the agent_aliases column specifically.
    - Reuse the PATTERN from test_migration_editions.py: for each edition in
      ["oss", "saas"], spin a fresh Postgres 17 container (with pgcrypto preload), patch
      `app.config.settings.EDITION` + `settings.DATABASE_URL` on the singleton DIRECTLY
      (os.environ alone is inert — the singleton is frozen at import), run
      `command.upgrade(cfg, "head")` in a worker thread (env.py calls asyncio.run internally).
    - After upgrade, connect with asyncpg and assert:
      `SELECT data_type, is_nullable FROM information_schema.columns
       WHERE table_name='teams' AND column_name='agent_aliases'`
      returns exactly one row with `is_nullable = 'YES'` (nullable, additive) under BOTH editions.
    - Restore the singleton + os.environ in a `finally` (mirror test_migration_editions.py
      resource hygiene — never leak a container, never pollute other session tests).
    - Gate on `_docker_available()`; a SKIP where Docker IS present (CI) is a failure signal,
      not a pass — mirror the note in test_migration_editions.py.
    Do NOT call `downgrade()` — this is upgrade-only (forward-only proof).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_agent_aliases_gate.py -q -k migration` passes and is NOT skipped when Docker is present.
    - The column assertion runs once per edition (oss AND saas) and asserts is_nullable == 'YES'.
    - No `downgrade` call anywhere in the test; containers are stopped in `finally`.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -m pytest tests/test_agent_aliases_gate.py -q -k migration</automated>
  </verify>
  <done>alembic upgrade head under oss AND saas leaves a nullable teams.agent_aliases column — forward-only, edition-agnostic, proven on real Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real Postgres/ASGI | The gate exercises the real detection + migration path, not a mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering (test integrity) | mocked detector giving false PASS | mitigate | The detector + team_chat resolution run REAL against real Postgres; only the downstream agent handler / Centrifugo / ingest are stubbed (they make network calls) — the mention DECISION is never mocked. |
| T-21-03-02 | DoS (CI) | leaked testcontainers | mitigate | Every container stopped in `finally`; singleton/env restored so other session tests are unaffected (mirrors test_migration_editions.py). |
| T-21-03-03 | Repudiation (silent skip) | Docker-absent SKIP masquerading as PASS | mitigate | Documented in-file: a SKIP where Docker is present (CI) is treated as a failure signal, not green (SKIP=FAIL). |
</threat_model>

<verification>
- `python -m pytest tests/test_agent_aliases_gate.py -q` — all pass (both groups), not skipped under Docker.
- Full phase suite green: `python -m pytest tests/test_mention_detector.py tests/test_agent_aliases_api.py tests/test_agent_aliases_gate.py -q`.
</verification>

<success_criteria>
- Real-path proof: custom alias → summons for its team only; @agent → summons everywhere; @claude → nowhere (SC#1, SC#2).
- Migration 0025 forward-only + edition-agnostic column present under oss and saas (D-21-03).
- No mock detector; SKIP under CI Docker is a failure.
</success_criteria>

<output>
After completion, create `.planning/phases/21-configurable-agent-aliases/21-03-SUMMARY.md`.
Record: gate PASS/SKIP status and whether Docker was available in the run environment.
</output>
