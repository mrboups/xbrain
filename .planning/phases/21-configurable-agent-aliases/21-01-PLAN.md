---
phase: 21-configurable-agent-aliases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memory-api/alembic/versions/0025_team_agent_aliases.py
  - apps/memory-api/app/models/team.py
  - apps/memory-api/app/config.py
  - apps/memory-api/app/services/mention_detector.py
  - apps/memory-api/tests/test_mention_detector.py
autonomous: true
requirements: [ALIAS-01]

must_haves:
  truths:
    - "@agent is detected on any effective alias list (universal default, never absent)"
    - "A custom alias passed to detect() fires for that alias; a team that did not set it does not"
    - "@claude is never a trigger regardless of the alias list"
    - "detect() with the same alias set does not recompile its regex on every call (per-set cache)"
  artifacts:
    - path: "apps/memory-api/alembic/versions/0025_team_agent_aliases.py"
      provides: "Nullable teams.agent_aliases column; down_revision 0024_local_credentials"
      contains: "0024_local_credentials"
    - path: "apps/memory-api/app/models/team.py"
      provides: "Team.agent_aliases Mapped[str | None] nullable Text column"
      contains: "agent_aliases"
    - path: "apps/memory-api/app/services/mention_detector.py"
      provides: "effective_aliases() resolver + team-aware cached detect()"
      contains: "def effective_aliases"
  key_links:
    - from: "app/services/mention_detector.py::effective_aliases"
      to: "settings.AGENT_MENTION_ALIASES"
      via: "defaults union custom, agent guaranteed"
      pattern: "AGENT_MENTION_ALIASES"
    - from: "app/services/mention_detector.py::detect"
      to: "_build_mention_regex (existing escape+sort)"
      via: "per-alias-set compiled-regex cache"
      pattern: "_build_mention_regex"
---

<objective>
Add the per-team alias data layer and make the mention detector team-aware — the
foundation every other Phase 21 plan builds on.

This plan delivers three contracts consumed downstream: (1) a nullable
`teams.agent_aliases` column (migration 0025), (2) an `effective_aliases()`
resolver that is the SINGLE server-side source of truth for a team's alias list
(defaults ∪ custom, `@agent` always present), and (3) a `detect(content,
aliases)` overload with a per-alias-set compiled-regex cache. No route wiring or
endpoints yet — those land in 21-02 against these contracts.

Purpose: honor D-21-03 (persistence), D-21-04 (team-aware cached detector), and
the `@agent`-always / `@claude`-gone half of D-21-01, without a scavenger hunt
for downstream executors.
Output: migration 0025, Team.agent_aliases, config default `agent,chad,a`,
`effective_aliases()`, cached `detect()`, extended unit tests.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-configurable-agent-aliases/21-CONTEXT.md

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (21-02 wiring/API, 21-03 gate)
     import these directly — do not re-derive them from the codebase. -->

app/services/mention_detector.py (after this plan):
```python
# EXISTING, unchanged — already re.escape()s each alias + sorts longest-first:
def _build_mention_regex(aliases_csv: str) -> re.Pattern[str]: ...

# NEW — the single server-side resolver. defaults (settings.AGENT_MENTION_ALIASES)
# ∪ custom, "agent" guaranteed present, deduped case-insensitively, order:
# defaults first then custom, each kept once. NEVER contains "claude".
def effective_aliases(custom_csv: str | None) -> list[str]: ...

# NEW OVERLOAD — team-aware. When `aliases` is provided, use a per-alias-set
# cached compiled regex keyed by the normalized (sorted, lowercased) csv, so we
# don't recompile every message. When `aliases` is None -> today's module-level
# _MENTION_RE (unchanged callers keep working).
def detect(content: str, aliases: list[str] | None = None) -> Mention | None: ...
```

app/models/team.py — Team gains:
```python
agent_aliases: Mapped[str | None] = mapped_column(Text, nullable=True)
# comma-separated CUSTOM aliases only (no leading '@'); NULL/empty -> env default only
```
</interfaces>

Existing code anchors (already read during planning — do not re-read whole files):
- mention_detector.py:23-49 — `_build_mention_regex` (escape+sort+empty-fallback) and the module-level `_MENTION_RE = _build_mention_regex(settings.AGENT_MENTION_ALIASES)`.
- config.py:240-243 — `AGENT_MENTION_ALIASES: str = "agent"`.
- alembic/versions/0024_local_credentials.py:24-25 — revision/down_revision shape to mirror.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0025 + Team.agent_aliases column + config default</name>
  <files>apps/memory-api/alembic/versions/0025_team_agent_aliases.py, apps/memory-api/app/models/team.py, apps/memory-api/app/config.py</files>
  <read_first>
    - apps/memory-api/alembic/versions/0024_local_credentials.py (revision/down_revision/op.execute idempotent DDL pattern)
    - apps/memory-api/app/models/team.py:13-31 (Team columns; Text already imported line 6)
    - apps/memory-api/app/config.py:240-243 (AGENT_MENTION_ALIASES default)
  </read_first>
  <action>
    1. Create `alembic/versions/0025_team_agent_aliases.py`:
       - `revision = "0025_team_agent_aliases"`, `down_revision = "0024_local_credentials"`.
       - `upgrade()`: `op.execute("ALTER TABLE teams ADD COLUMN IF NOT EXISTS agent_aliases TEXT")`.
         Additive, nullable, NO backfill, NO default — a plain forward-only column
         (D-21-03, Phase-17 forward-only pattern). MUST NOT reference the EDITION flag
         (test_migration_editions.py::test_no_migration_branches_on_edition forbids it).
       - `downgrade()`: `op.execute("ALTER TABLE teams DROP COLUMN IF EXISTS agent_aliases")`
         (present for symmetry; never invoked in release validation).
    2. In `app/models/team.py`, add to the `Team` class (Text is already imported):
       `agent_aliases: Mapped[str | None] = mapped_column(Text, nullable=True)`
       with a comment: comma-separated CUSTOM aliases only, no leading '@', NULL/empty
       => env default only.
    3. In `app/config.py:243`, change the default to `AGENT_MENTION_ALIASES: str = "agent,chad,a"`
       (D-21-01 — adds the short `a`). Update the adjacent comment: default now
       `@agent, @chad, @a`; env still overrides. (Ops note for SUMMARY: the VM `.env`
       AGENT_MENTION_ALIASES=agent,chad should be updated to agent,chad,a on next deploy;
       the code default already delivers it for fresh OSS installs.)
  </action>
  <acceptance_criteria>
    - `grep -c "down_revision" apps/memory-api/alembic/versions/0025_team_agent_aliases.py` >= 1 AND the value is exactly `"0024_local_credentials"`.
    - `grep -q "agent_aliases" apps/memory-api/app/models/team.py` succeeds inside the Team class.
    - `grep -q 'AGENT_MENTION_ALIASES: str = "agent,chad,a"' apps/memory-api/app/config.py` succeeds.
    - The migration file contains NO occurrence of `EDITION`.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -c "import ast,sys; ast.parse(open('alembic/versions/0025_team_agent_aliases.py').read()); ast.parse(open('app/models/team.py').read()); print('parse-ok')"</automated>
  </verify>
  <done>Migration 0025 exists chained to 0024, Team has a nullable agent_aliases column, config default is agent,chad,a, and no migration branches on EDITION.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: effective_aliases() resolver + cached team-aware detect()</name>
  <files>apps/memory-api/app/services/mention_detector.py, apps/memory-api/tests/test_mention_detector.py</files>
  <read_first>
    - apps/memory-api/app/services/mention_detector.py (full — 72 lines; keep _build_mention_regex + module-level _MENTION_RE exactly)
    - apps/memory-api/tests/test_mention_detector.py:1-60 (existing _build_mention_regex + detect test style; parametrize idiom)
    - apps/memory-api/tests/conftest.py:29 (AGENT_MENTION_ALIASES=agent default in tests — monkeypatch settings.AGENT_MENTION_ALIASES for chad/a-default assertions)
  </read_first>
  <behavior>
    effective_aliases:
    - Test: effective_aliases(None) -> includes "agent" (always), plus each default token from settings.AGENT_MENTION_ALIASES.
    - Test: effective_aliases("wizard") -> contains "agent" AND "wizard"; defaults precede custom.
    - Test: effective_aliases("Agent, wizard, WIZARD") -> deduped case-insensitively (one "agent", one "wizard").
    - Test: with settings.AGENT_MENTION_ALIASES monkeypatched to "agent,chad,a", effective_aliases(None) contains "agent","chad","a".
    - Test: "claude" never appears in the output even if somehow passed (defense in depth; validation in 21-02 also rejects it).
    detect (team-aware):
    - Test: detect("@wizard hi", ["agent","wizard"]) -> trigger "wizard".
    - Test: detect("@wizard hi", ["agent"]) -> None (team without the custom alias does not fire).
    - Test: detect("@agent hi", ["agent","wizard"]) -> trigger "agent" (universal default).
    - Test: detect("@claude hi", ["agent","wizard","chad","a"]) -> None (@claude never triggers).
    - Test: detect("@a hi", ["agent","a"]) -> trigger "a" (short alias, boundary-correct); detect("@apple hi", ["agent","a"]) -> None.
    - Test (cache identity): calling detect twice with the same alias list reuses one compiled Pattern (assert the internal cache dict has exactly one entry for that key, or that the same Pattern object is returned by an exposed _regex_for helper).
    - Test (backward compat): detect("@agent hi") with no aliases arg still works via module default.
  </behavior>
  <action>
    1. Add `effective_aliases(custom_csv: str | None) -> list[str]`:
       - Parse defaults from `settings.AGENT_MENTION_ALIASES` (split "," strip, drop empties).
       - Parse custom from `custom_csv` the same way (may be None -> []).
       - Merge: start from `["agent"]`, then defaults, then custom; keep first occurrence,
         dedup case-insensitively (compare `.lower()`), preserve original casing of the kept token.
       - Filter out any token equal (case-insensitive) to "claude" (defense in depth).
       - Return the list.
    2. Add a module-level `_regex_cache: dict[str, re.Pattern[str]] = {}` and a helper
       `_regex_for(aliases: list[str]) -> re.Pattern[str]` that keys on
       `",".join(sorted(a.lower() for a in aliases))`, building+caching via the EXISTING
       `_build_mention_regex(",".join(aliases))` on miss. (Reuses the server's escape+sort.)
    3. Change `detect(content, aliases=None)`: when `aliases` is a non-empty list, use
       `_regex_for(aliases)`; else use `_MENTION_RE`. Keep the return shape
       `{"agent_name": "claude-sonnet-4-6", "trigger": m.group(1).lower()}`.
    4. Extend `tests/test_mention_detector.py` with the behaviors above. Use
       `monkeypatch.setattr("app.services.mention_detector.settings.AGENT_MENTION_ALIASES", ...)`
       for default-set assertions. These are unit tests (no DB) — the real-Postgres summon
       gate lives in 21-03.
  </action>
  <acceptance_criteria>
    - `detect("@wizard x", ["agent","wizard"])["trigger"] == "wizard"` and `detect("@wizard x", ["agent"]) is None`.
    - `detect("@claude x", ["agent","wizard","chad","a"]) is None`.
    - `detect("@agent x", ["agent","wizard"])["trigger"] == "agent"`.
    - `"claude" not in [a.lower() for a in effective_aliases("claude, wizard")]`.
    - Second detect() call with an identical alias list adds no new key to `_regex_cache`.
    - `pytest tests/test_mention_detector.py -q` passes (all new + existing cases).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -m pytest tests/test_mention_detector.py -q</automated>
  </verify>
  <done>effective_aliases() is the sole resolver (agent-always, claude-never, deduped), detect() is team-aware with a per-alias-set regex cache, unchanged callers still work, and unit tests prove per-team fire/non-fire + @claude-gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| team.agent_aliases (DB) → regex compilation | A stored custom alias string becomes a compiled regex at detect time |
| settings.AGENT_MENTION_ALIASES (env) → regex | Operator-controlled default list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Tampering/DoS | `_build_mention_regex` on stored aliases | mitigate | `_build_mention_regex` already `re.escape`s each alias — no metacharacter reaches the compiled pattern even if a bad string is persisted. Edge validation added in 21-02. |
| T-21-01-02 | DoS | `_regex_cache` unbounded growth | accept | Key space is bounded by distinct alias-sets ≈ team count; each set ≤ (defaults + 8 custom) per 21-02's cap. Internal, low-value target; no per-request key explosion. |
| T-21-01-03 | Spoofing | `@claude` re-entering the trigger set | mitigate | `effective_aliases` filters "claude" case-insensitively (defense in depth); 21-02 validation also rejects it → SC#2 invariant holds absolutely. |
| T-21-01-04 | Info disclosure | schema fork per edition | mitigate | Migration 0025 is edition-agnostic (no EDITION branch); asserted by test_migration_editions.py::test_no_migration_branches_on_edition. |
</threat_model>

<verification>
- `python -m pytest tests/test_mention_detector.py -q` — all pass.
- `ast.parse` clean on the migration, model, and detector.
- Migration file free of `EDITION`.
</verification>

<success_criteria>
- Nullable `teams.agent_aliases` column defined via migration 0025 (down_revision 0024).
- `effective_aliases()` returns defaults ∪ custom with `agent` always and `claude` never.
- `detect(content, aliases)` fires per-team correctly, caches per alias-set, stays backward-compatible.
- Config default expanded to `agent,chad,a`.
</success_criteria>

<output>
After completion, create `.planning/phases/21-configurable-agent-aliases/21-01-SUMMARY.md`.
Record the ops note: VM `.env` AGENT_MENTION_ALIASES should be updated to `agent,chad,a` on next deploy.
</output>
