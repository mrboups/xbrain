---
phase: 17-ci-lockstep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memory-api/tests/test_migration_editions.py
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "`alembic upgrade head` under EDITION=oss reaches the script head with no error, forward-only (no downgrade required)"
    - "`alembic upgrade head` under EDITION=saas reaches the SAME head as oss"
    - "`alembic upgrade head` under EDITION=pro reaches the SAME head as oss and saas"
    - "No migration in alembic/versions branches its schema on the EDITION flag (edition-agnostic by construction)"
    - "The test RUNS for real against a testcontainers Postgres — it is not a mock or a `config` parse"
  artifacts:
    - path: "apps/memory-api/tests/test_migration_editions.py"
      provides: "Parametrized EDITION=oss/saas/pro real-Postgres alembic upgrade-head test + static edition-agnostic guard"
      contains: "parametrize"
      min_lines: 60
  key_links:
    - from: "apps/memory-api/tests/test_migration_editions.py"
      to: "alembic upgrade head (real Postgres testcontainer)"
      via: "command.upgrade(cfg, 'head') in a worker thread, one fresh container per edition"
      pattern: "command\\.upgrade"
    - from: "apps/memory-api/tests/test_migration_editions.py"
      to: "ScriptDirectory head (the alembic chain's declared head)"
      via: "assert DB alembic_version == script head, and all editions equal"
      pattern: "get_current_head"
---

<objective>
Author the load-bearing, non-mocked proof for REL-03 (SC4 + SC5): a real-Postgres
test that runs `alembic upgrade head` under EDITION=oss AND EDITION=saas AND
EDITION=pro, asserting every edition reaches the SAME migration head with no error,
forward-only (no `downgrade()` is ever invoked in release validation), and that no
migration branches its schema on the edition flag.

Purpose: This is the honest, locally-verifiable core of Phase 17 — a check that
traverses the REAL migration path (testcontainers Postgres 17, the actual Alembic
chain), not a `config` parse. It is the single most important gate: a migration that
would break one edition must never reach a release. It runs identically locally and,
later, as the `test-migrations` job in the CI workflow (Plan 17-03).

Output: `apps/memory-api/tests/test_migration_editions.py` — a new, self-contained test
file that reuses the codebase's existing testcontainers pattern (per D-17-05), plus a
recorded local run proving all three editions reach head green.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ci-lockstep/17-CONTEXT.md
@.planning/phases/17-ci-lockstep/17-RESEARCH.md

<interfaces>
<!-- The EXACT existing patterns this test extends. Do NOT invent a new mechanism (D-17-05,
     Don't-Hand-Roll). Read these two files first; mirror them. -->

From apps/memory-api/tests/conftest.py::pg_url (the canonical testcontainers + alembic pattern):
```python
from testcontainers.postgres import PostgresContainer
pg = PostgresContainer("postgres:17", username="test", password="test", dbname="test").with_command(
    "postgres -c shared_preload_libraries=pgcrypto"   # migrations use gen_random_uuid() → pgcrypto required
)
pg.start()
raw = pg.get_connection_url()                          # postgresql+psycopg2://...
asyncpg_url = raw.replace("postgresql+psycopg2://", "postgresql+asyncpg://")
# alembic/env.py runs asyncio.run() internally → command.upgrade MUST run in a worker thread,
# never inline under pytest-asyncio's loop:
from alembic import command
from alembic.config import Config
cfg = Config(os.path.join(os.path.dirname(__file__), "..", "alembic.ini"))
cfg.set_main_option("sqlalchemy.url", asyncpg_url)
cfg.set_main_option("script_location", os.path.join(os.path.dirname(__file__), "..", "alembic"))
await asyncio.to_thread(command.upgrade, cfg, "head")
```

From apps/memory-api/tests/test_migration_0019.py (the assertion + marker style to mirror):
```python
pytestmark = [pytest.mark.integration, pytest.mark.asyncio]
# head is read from the DB: SELECT version_num FROM alembic_version
```

Runtime facts (verified this session):
- The current Alembic head is `0024_local_credentials` (down_revision chain ends there).
  Do NOT hardcode `0024` — read the declared head from `ScriptDirectory.from_config(cfg).get_current_head()`
  so this test never goes brittle when Plan-N adds `0025`.
- `grep -rl EDITION apps/memory-api/alembic/` returns NOTHING today — edition-agnosticism is
  presently true by omission. The static guard in Task 1 turns "true by omission" into
  "asserted", catching a FUTURE migration that silently branches on edition.
- Tests run from `apps/memory-api` (Makefile: `cd apps/memory-api && pytest`). Use
  `os.path.dirname(__file__)`-relative paths (as conftest does), never bare relative strings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author test_migration_editions.py (runtime edition-agnostic upgrade test + static guard)</name>
  <files>apps/memory-api/tests/test_migration_editions.py</files>
  <read_first>
    - apps/memory-api/tests/conftest.py (the pg_url fixture body — copy its container + worker-thread alembic pattern verbatim in spirit)
    - apps/memory-api/tests/test_migration_0019.py (marker style, DB-head assertion style)
    - apps/memory-api/alembic/env.py (confirm run_migrations_online uses asyncio.run — the reason upgrade runs in a thread)
  </read_first>
  <action>
    Create a NEW self-contained test file (do NOT modify conftest.py's session-scoped
    pg_url — it is shared by 20+ files; this test needs a FRESH, function-scoped container
    per edition so EDITION is set before each upgrade). Implement, per D-17-05:

    1. `pytestmark = [pytest.mark.integration, pytest.mark.asyncio]`.
    2. `EDITIONS = ["oss", "saas", "pro"]` — SC4 explicitly names oss, saas, AND pro; test all three.
    3. A module-level `_run_upgrade_head(edition) -> str` helper (async) that:
       - `monkeypatch`/`os.environ["EDITION"] = edition` BEFORE the upgrade,
       - spins `PostgresContainer("postgres:17", ...).with_command("postgres -c shared_preload_libraries=pgcrypto")`,
       - builds the asyncpg URL, sets `os.environ["DATABASE_URL"]`,
       - builds an alembic `Config` with `os.path.dirname(__file__)`-relative `alembic.ini` +
         `script_location`, sets `sqlalchemy.url`,
       - runs `await asyncio.to_thread(command.upgrade, cfg, "head")` (NEVER inline — env.py nests asyncio.run),
       - reads the applied head via a direct psycopg2/asyncpg `SELECT version_num FROM alembic_version`
         (or via the returned connection), returns it,
       - stops the container in a `finally:` (resource hygiene — no leaked containers).
       Gate on Docker availability the same way conftest does (`pytest.skip` only when Docker is
       genuinely absent — NOTE: in CI Docker is present, so a skip there is a real failure signal,
       not a pass; document this in a docstring).
    4. `@pytest.mark.parametrize("edition", EDITIONS)` test `test_alembic_upgrade_head_per_edition`
       that calls the helper and asserts the applied DB head EQUALS
       `ScriptDirectory.from_config(cfg).get_current_head()` (the chain's declared head) for that
       edition — proving upgrade reached head forward-only, with NO `downgrade()` called anywhere.
    5. `test_all_editions_reach_same_head` that runs the helper for all three editions and asserts
       the three applied heads are IDENTICAL (edition-agnostic: identical schema regardless of EDITION).
    6. `test_no_migration_branches_on_edition` (static, no container — cheap forward-only guard):
       walk every `apps/memory-api/alembic/versions/*.py`, read its source, and assert NONE reference
       the edition flag in a way that would branch schema — i.e. assert no version file contains
       `EDITION` (matching today's `grep -rl EDITION alembic/ == none`). Fail with the offending
       filename(s) if any do, instructing the author to make the migration edition-agnostic.
    All code, comments, and docstrings in English (project rule).
  </action>
  <verify>
    <automated>See <acceptance_criteria> below — every grep/pytest/actionlint/compose-config gate must pass.</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f apps/memory-api/tests/test_migration_editions.py</automated>
    <automated>python -c "import ast,sys; ast.parse(open('apps/memory-api/tests/test_migration_editions.py',encoding='utf-8').read()); print('AST OK')"</automated>
    <automated>grep -q 'EDITIONS = \["oss", "saas", "pro"\]' apps/memory-api/tests/test_migration_editions.py</automated>
    <automated>grep -q 'get_current_head' apps/memory-api/tests/test_migration_editions.py</automated>
    <automated>grep -q 'asyncio.to_thread' apps/memory-api/tests/test_migration_editions.py</automated>
    <automated>grep -q 'shared_preload_libraries=pgcrypto' apps/memory-api/tests/test_migration_editions.py</automated>
    - File imports `testcontainers.postgres.PostgresContainer` and `alembic.command` (real path, not mocked).
    - Head is read dynamically via ScriptDirectory — the string `0024` is NOT hardcoded as the expected head.
    - Every container is stopped in a `finally:` (grep shows `finally` + `.stop()`).
  </acceptance_criteria>
  <done>test_migration_editions.py exists, parses, parametrizes oss/saas/pro, upgrades a real
  testcontainer per edition, asserts DB head == script head and all editions equal, and statically
  forbids EDITION-branching migrations. No hardcoded head string.</done>
</task>

<task type="auto">
  <name>Task 2: Run it for real and record the forward-only / edition-agnostic result</name>
  <files>apps/memory-api/tests/test_migration_editions.py</files>
  <read_first>
    - apps/memory-api/pyproject.toml (confirm `testcontainers[postgres]` is a dev dep — it is, per research)
    - Makefile (the `test` target: `cd apps/memory-api && pytest`)
  </read_first>
  <action>
    Execute the test against a REAL Postgres testcontainer (Docker daemon must be up — start
    Docker Desktop first if needed). Run from `apps/memory-api`:
    `pytest -m integration tests/test_migration_editions.py -v`.
    All THREE editions (oss, saas, pro) must PASS with the head-equality and same-head assertions
    green. If Docker is genuinely unavailable in the execution environment, record that the test is
    authored + AST-valid + pattern-identical to the proven conftest fixture, and mark the live run
    as the one deferred step (Docker required) — but do NOT claim it green if it did not run.
    Capture the pytest summary line into the plan SUMMARY. This is the "RUNS for real" gate the
    CONTEXT gate-lesson demands (SKIP≠PASS in CI).
  </action>
  <verify>
    <automated>See <acceptance_criteria> below — every grep/pytest/actionlint/compose-config gate must pass.</automated>
  </verify>
  <acceptance_criteria>
    <automated>cd apps/memory-api && pytest -m integration tests/test_migration_editions.py -v 2>&1 | tail -20</automated>
    - pytest reports `test_alembic_upgrade_head_per_edition[oss]`, `[saas]`, `[pro]` all PASSED (when Docker present).
    - `test_all_editions_reach_same_head` PASSED.
    - `test_no_migration_branches_on_edition` PASSED (zero EDITION-branching migrations today).
    - The exact pass/skip counts are recorded in the SUMMARY; a Docker-absent SKIP is reported honestly, not as a pass.
  </acceptance_criteria>
  <done>The migration-both-editions test has been executed (or its inability to run is honestly
  recorded with the reason). When Docker is present, all oss/saas/pro parametrizations pass and the
  result is captured in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → ephemeral Postgres testcontainer | Throwaway DB, throwaway credentials (test/test), destroyed at test end — no production data, no real secret crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Denial of Service | leaked testcontainers (Postgres) if a test aborts mid-run | mitigate | Every container `.start()` is paired with `.stop()` in a `finally:`; one container per edition, torn down before the next. |
| T-17-01-02 | Information Disclosure | `DATABASE_URL` / container creds in env | accept | Credentials are the fixed throwaway `test/test` on an ephemeral localhost container; nothing sensitive is ever written. No real secret is referenced by this plan. |
| T-17-01-03 | Tampering | a future migration silently branching schema on EDITION | mitigate | `test_no_migration_branches_on_edition` statically forbids any `EDITION` reference in `alembic/versions/*.py`, failing the gate before such a migration can ship. |
</threat_model>

<verification>
- `pytest -m integration tests/test_migration_editions.py` passes all oss/saas/pro parametrizations against a real Postgres 17 testcontainer.
- The applied DB head equals the Alembic chain's declared head for every edition, and all three editions produce the identical head (edition-agnostic, forward-only).
- No `alembic/versions/*.py` references `EDITION`.
</verification>

<success_criteria>
REL-03 / SC4 / SC5 are proven locally and non-mocked: a released migration applies cleanly under
oss, saas, and pro; the path is forward-only (upgrade head, no downgrade) and edition-agnostic (same
head, no edition branch). This exact test becomes the CI `test-migrations` job (Plan 17-03).
</success_criteria>

<output>
After completion, create `.planning/phases/17-ci-lockstep/17-01-SUMMARY.md`
(record the exact pytest pass/skip counts for oss/saas/pro).
</output>
