---
phase: 17-ci-lockstep
plan: 04
type: execute
wave: 3
depends_on: ["01", "02", "03"]
files_modified:
  - apps/memory-api/tests/test_ci_lockstep_graph.py
  - infrastructure/scripts/verify-phase17-workflow.sh
  - Makefile
  - docs/ci-lockstep.md
autonomous: true
requirements: [REL-01, REL-02, REL-03]
must_haves:
  truths:
    - "A test PARSES ci-lockstep.yml and proves — structurally, not by comment — that publish AND deploy both `needs:` all three test jobs (SC3, D-17-01)"
    - "The same test proves build is a single job with no needs, deploy-saas is gated by SAAS_DEPLOY_ENABLED, no job rebuilds on deploy, and every `uses:` is pinned"
    - "`actionlint` passes on the authored workflow with zero findings (SKIP=FAIL)"
    - "Make targets expose every Phase-17 gate (migrations, full, workflow, umbrella verify-phase17)"
    - "The honest residual is DOCUMENTED, not claimed green: real GH-Actions run, GHCR visibility flip, live SaaS deploy, full-boot dry-run, LICENSE MIT→AGPLv3+CLA follow-up"
  artifacts:
    - path: "apps/memory-api/tests/test_ci_lockstep_graph.py"
      provides: "The SC3 structural proof — YAML needs-graph parse assertions"
      contains: "needs"
      min_lines: 60
    - path: "infrastructure/scripts/verify-phase17-workflow.sh"
      provides: "actionlint(SKIP=FAIL) + graph-parse wrapper for the authored workflow"
      contains: "actionlint"
      min_lines: 30
    - path: "docs/ci-lockstep.md"
      provides: "The honest residual + operator enable-steps documentation"
      contains: "Residual"
      min_lines: 50
    - path: "Makefile"
      provides: "verify-phase17-* targets"
      contains: "verify-phase17"
  key_links:
    - from: "apps/memory-api/tests/test_ci_lockstep_graph.py"
      to: ".github/workflows/ci-lockstep.yml (the needs: graph)"
      via: "yaml.safe_load + assert publish/deploy needs ⊇ the three test jobs"
      pattern: "yaml\\.safe_load"
    - from: "infrastructure/scripts/verify-phase17-workflow.sh"
      to: "actionlint on ci-lockstep.yml"
      via: "download-actionlint.bash + run; zero findings = pass, SKIP=FAIL"
      pattern: "actionlint"
    - from: "Makefile: verify-phase17"
      to: "the three Phase-17 gates"
      via: "umbrella target running migrations + full + workflow"
      pattern: "verify-phase17:"
---

<objective>
Turn the lockstep pipeline from "authored" into "proven": a test that PARSES
`ci-lockstep.yml` and asserts the `needs:` graph structurally enforces SC3 (publish AND
deploy both `needs:` all three test jobs — not trusted from a comment, D-17-01); an
`actionlint`-runs-clean wrapper (SKIP=FAIL); Make targets exposing every Phase-17 gate;
and `docs/ci-lockstep.md` recording the HONEST residual — the real GitHub-Actions run,
the GHCR visibility flip, the live SaaS deploy, the full-boot dry-run, and the
LICENSE MIT→AGPLv3+CLA follow-up — none of which are claimed green this phase.

Purpose: This is the CONTEXT gate-lesson made concrete. SC3 is proven by parsing the
graph, not by reading comments; actionlint proves the YAML is valid; and the residual
is documented truthfully rather than faked. Closes the "at least one plan asserts
lockstep by PARSING the needs-graph" requirement.

Output: `apps/memory-api/tests/test_ci_lockstep_graph.py`,
`infrastructure/scripts/verify-phase17-workflow.sh`, Makefile targets, `docs/ci-lockstep.md`.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ci-lockstep/17-CONTEXT.md
@.planning/phases/17-ci-lockstep/17-RESEARCH.md
@.github/workflows/ci-lockstep.yml

<interfaces>
<!-- The job/needs contract this plan asserts (authored by Plan 17-03). If Plan 17-03 renamed a
     job, update the expected names here to match — the parse must reflect the real file. -->
Expected jobs in ci-lockstep.yml:
  build                 → needs: (none)
  test-oss-subset       → needs: build
  test-full-profile     → needs: build
  test-migrations       → needs: (none or build)  — independent-of-image-build is allowed
  publish-oss-release   → needs: [test-oss-subset, test-full-profile, test-migrations]
  deploy-saas           → needs: [test-oss-subset, test-full-profile, test-migrations]
                          if: contains 'SAAS_DEPLOY_ENABLED'

Pinned-action rule to assert: every `uses:` value matches `@v<major>.<...>` or a 40-char SHA — never `@main`/`@master`.
No-rebuild-on-deploy rule: the deploy-saas job's `run:` blocks must not contain `--build` or `make deploy`.

actionlint: `curl -fsSL https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash | bash`
  → produces `./actionlint`; run `./actionlint .github/workflows/ci-lockstep.yml`. Zero findings from this
  phase's content = pass. (It found 2 PRE-EXISTING issues in deploy-dashboard.yml in research — do NOT let this
  phase's wrapper fail on that OTHER file; scope the wrapper to ci-lockstep.yml.)

Makefile convention (existing verify targets, lines 129-139): `.PHONY` + `## help text` + `@bash <script>`.
Existing Phase-17 gates to wire: apps/memory-api/tests/test_migration_editions.py (via pytest),
  infrastructure/scripts/verify-phase17-full.sh, this plan's verify-phase17-workflow.sh.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author test_ci_lockstep_graph.py (the SC3 structural needs-graph proof)</name>
  <files>apps/memory-api/tests/test_ci_lockstep_graph.py</files>
  <read_first>
    - .github/workflows/ci-lockstep.yml (the ACTUAL job names + needs edges — assert against reality)
    - 17-CONTEXT.md D-17-01 (lockstep is the needs: graph), D-17-02 (no rebuild on deploy), D-17-04 (deploy gated)
  </read_first>
  <action>
    Create a pure-parse pytest module (NO Docker, NO network — runs anywhere, fast) that loads
    `.github/workflows/ci-lockstep.yml` with `yaml.safe_load` (resolve the repo-root-relative path via
    `os.path` from `__file__`) and asserts the STRUCTURE that makes SC3 real. A helper normalizes `needs:`
    (GitHub allows a string or a list) to a set. Tests:
      1. `test_build_is_single_and_unblocked`: a `build` job exists and has empty/absent `needs`.
      2. `test_three_test_jobs_exist`: `test-oss-subset`, `test-full-profile`, `test-migrations` all present.
      3. `test_publish_needs_all_three_tests`: `publish-oss-release.needs` ⊇ the three test-job names.
      4. `test_deploy_needs_all_three_tests`: `deploy-saas.needs` ⊇ the three test-job names.
         (3+4 together ARE the SC3 lockstep proof — a single failing test job skips BOTH shipping jobs.)
      5. `test_deploy_is_gated_off`: `deploy-saas.if` contains `SAAS_DEPLOY_ENABLED` (D-17-04).
      6. `test_deploy_never_rebuilds`: no `run:` string in `deploy-saas` contains `--build` or `make deploy` (D-17-02).
      7. `test_all_actions_pinned`: every `uses:` across all jobs matches `@v\d` or a 40-hex SHA — never `@main`/`@master` (supply-chain).
      8. `test_no_pull_request_trigger`: the `on:` block has no `pull_request`/`pull_request_target` (fork-secret hygiene).
    Each assertion message must name the offending job/edge so a failure is self-explaining. Mark with
    `pytest.mark.unit` (or leave unmarked — it needs no integration fixtures). English only.
  </action>
  <verify>
    <automated>See <acceptance_criteria> below — every grep/pytest/actionlint/compose-config gate must pass.</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f apps/memory-api/tests/test_ci_lockstep_graph.py</automated>
    <automated>python -c "import ast; ast.parse(open('apps/memory-api/tests/test_ci_lockstep_graph.py',encoding='utf-8').read()); print('AST OK')"</automated>
    <automated>grep -q 'yaml.safe_load' apps/memory-api/tests/test_ci_lockstep_graph.py</automated>
    <automated>grep -q 'SAAS_DEPLOY_ENABLED' apps/memory-api/tests/test_ci_lockstep_graph.py</automated>
    <automated>cd apps/memory-api && pytest tests/test_ci_lockstep_graph.py -v 2>&1 | tail -20</automated>
    - All 8 tests PASS against the real ci-lockstep.yml (publish + deploy both gate on all three test jobs; deploy gated; actions pinned; no PR trigger).
    - The test needs no Docker/network — it is a pure YAML parse.
  </acceptance_criteria>
  <done>test_ci_lockstep_graph.py parses the real workflow and proves SC3 structurally: build unblocked,
  three test jobs gate both publish and deploy, deploy is SAAS_DEPLOY_ENABLED-gated + never rebuilds,
  every action pinned, no fork-PR trigger. All assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author verify-phase17-workflow.sh (actionlint SKIP=FAIL + graph parse)</name>
  <files>infrastructure/scripts/verify-phase17-workflow.sh</files>
  <read_first>
    - infrastructure/scripts/verify-phase16.sh (verify-script style: set -uo pipefail, ok/ko counters, exit on FAIL)
    - .github/workflows/ci-lockstep.yml (the file this lints)
  </read_first>
  <action>
    Create a bash gate, project verify-script style, that:
      (a) Ensures `actionlint` is available: if `./actionlint` / `actionlint` on PATH is absent, download it via
          `curl -fsSL https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash | bash`.
          If the download is genuinely blocked (offline), fall back to a `python -c "import yaml; yaml.safe_load(...)"`
          syntax check AND print a clear NOTE that full actionlint validation is the (weaker-fallback) residual —
          but treat an actionlint run that FINDS issues as FAIL (SKIP=FAIL: a skip of actionlint when it COULD run is a fail).
      (b) Runs `actionlint .github/workflows/ci-lockstep.yml` (scope to THIS file only — deploy-dashboard.yml has
          2 pre-existing findings that are out of scope) and asserts ZERO findings.
      (c) Runs the graph-parse proof: `cd apps/memory-api && pytest tests/test_ci_lockstep_graph.py -q` (Task 1).
    Summary line + exit 0 only when FAIL==0. `chmod +x`. English only.
  </action>
  <verify>
    <automated>See <acceptance_criteria> below — every grep/pytest/actionlint/compose-config gate must pass.</automated>
  </verify>
  <acceptance_criteria>
    <automated>bash -n infrastructure/scripts/verify-phase17-workflow.sh && echo "bash syntax OK"</automated>
    <automated>grep -q 'actionlint' infrastructure/scripts/verify-phase17-workflow.sh</automated>
    <automated>grep -q 'ci-lockstep.yml' infrastructure/scripts/verify-phase17-workflow.sh</automated>
    <automated>grep -q 'test_ci_lockstep_graph.py' infrastructure/scripts/verify-phase17-workflow.sh</automated>
    <automated>bash infrastructure/scripts/verify-phase17-workflow.sh; echo "exit=$?"</automated>
    - Running the script runs actionlint on ci-lockstep.yml with zero findings and passes the graph-parse test; exit 0.
    - actionlint findings on ci-lockstep.yml are treated as FAIL (not skipped); scoped to that one file.
  </acceptance_criteria>
  <done>verify-phase17-workflow.sh lints the authored workflow (actionlint, zero findings, SKIP=FAIL)
  and runs the needs-graph proof; exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Makefile targets + author the honest residual doc (docs/ci-lockstep.md)</name>
  <files>Makefile, docs/ci-lockstep.md</files>
  <read_first>
    - Makefile (lines 129-139 — the verify-phase15/16/18 target style to mirror)
    - 17-CONTEXT.md <deferred> + <specifics> (the exact residual list), 17-RESEARCH.md §Open Questions + §Common Pitfalls (Pitfall 1 GHCR visibility)
  </read_first>
  <action>
    A) Makefile — add `.PHONY` targets mirroring the existing verify-phase* style:
       - `verify-phase17-migrations` → `cd apps/memory-api && pytest -m integration tests/test_migration_editions.py -v`
       - `verify-phase17-full`       → `bash infrastructure/scripts/verify-phase17-full.sh`
       - `verify-phase17-workflow`   → `bash infrastructure/scripts/verify-phase17-workflow.sh`
       - `verify-phase17`            → umbrella: runs workflow + full + migrations (the locally-verifiable Phase-17 gate set)
       Each with a `## help` comment. Do NOT modify any existing target.
    B) docs/ci-lockstep.md — the honest boundary doc (English). Sections:
       - "What CI proves locally now (SKIP=FAIL)": actionlint clean; needs-graph parsed (SC3 structural);
         migration-both-editions RUNS (oss/saas/pro testcontainers); verify-phase16 still passes; full profile (33) resolves.
       - "Documented residual — NOT claimed green": the real push-triggered GitHub-Actions run; the `build` job's
         18-image amd64 build on a hosted runner; the full-profile 33-container disk-fit measurement
         (a `workflow_dispatch` dry-run of build+test-full-profile — Open Question 1); the GHCR publish + the
         one-time per-package visibility→Public flip (Pitfall 1, incl. the `gh api /user/packages/container/<name>/visibility` command);
         and the live SaaS deploy.
       - "Enable steps for deploy-saas (operator)": restart the prod VM + confirm its `.env` currency (Open Question 2);
         add repo secrets VM_SSH_HOST / VM_SSH_USER / VM_SSH_KEY (Secrets, never Variables); set repo Variable
         `SAAS_DEPLOY_ENABLED=true`; then the gated job pulls the SHA images and `up -d` (never `--build`).
       - "LICENSE follow-up (D-17-07)": `LICENSE` is still MIT while REQUIREMENTS records a locked AGPLv3+CLA
         decision — publish ships whatever file exists and asserts no license string; reconcile BEFORE any real
         public OSS release. Swapping the file later needs no workflow change.
       - "Future upgrade (optional)": true arm64 multi-arch via `ubuntu-24.04-arm` runners; xbrain-backup would
         remain amd64-only (base has no arm64 manifest).
    Cross-reference: this doc is the residual bundled/linked from publish-oss-release.
  </action>
  <verify>
    <automated>See <acceptance_criteria> below — every grep/pytest/actionlint/compose-config gate must pass.</automated>
  </verify>
  <acceptance_criteria>
    <automated>grep -q 'verify-phase17:' Makefile && grep -q 'verify-phase17-migrations:' Makefile && grep -q 'verify-phase17-full:' Makefile && grep -q 'verify-phase17-workflow:' Makefile && echo "make targets OK"</automated>
    <automated>bash -n Makefile 2>/dev/null; make -n verify-phase17 >/dev/null 2>&1 && echo "umbrella target resolves" || echo "make dry-run (make may be absent on this host — targets present per grep)"</automated>
    <automated>test -f docs/ci-lockstep.md</automated>
    <automated>grep -qi 'Residual' docs/ci-lockstep.md && grep -qi 'SAAS_DEPLOY_ENABLED' docs/ci-lockstep.md && grep -qi 'visibility' docs/ci-lockstep.md && grep -qi 'AGPL' docs/ci-lockstep.md && echo "residual doc sections OK"</automated>
    - docs/ci-lockstep.md names all five residual items and does NOT claim a green live run.
    - Makefile has all four verify-phase17-* targets; no existing target changed (git diff shows additions only).
  </acceptance_criteria>
  <done>Makefile exposes verify-phase17{,-migrations,-full,-workflow}; docs/ci-lockstep.md truthfully
  documents what is proven locally vs the residual (live run, GHCR publish + visibility flip, disk-fit
  dry-run, live deploy, LICENSE follow-up) with concrete operator enable-steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → operator | docs/ci-lockstep.md tells the operator which secrets/variables to set; wrong guidance could leak or mis-scope a credential. |
| graph-parse test → workflow YAML | The test is the authority that SC3 holds; a weak assertion would let a broken graph pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Information Disclosure | residual doc leaking a real secret VALUE | mitigate | The doc names secret/variable NAMES only (VM_SSH_KEY, SAAS_DEPLOY_ENABLED) and instructs storing keys as repo Secrets (never Variables, never committed); no real key value appears. |
| T-17-04-02 | Tampering | a future edit weakens the needs: graph and ships an edition untested | mitigate | test_ci_lockstep_graph.py asserts publish AND deploy `needs:` all three test jobs by parse; a graph regression fails the gate in CI + locally. |
| T-17-04-03 | Repudiation | claiming the pipeline is "green" without a real run | mitigate | The doc explicitly separates locally-proven from residual; no plan in this phase marks the live run/publish/deploy green (honest-boundary discipline). |
| T-17-04-04 | Elevation of Privilege | operator arms deploy against a stale/unowned VM | mitigate | Enable-steps require confirming VM state + `.env` currency BEFORE setting SAAS_DEPLOY_ENABLED (Open Question 2). |
</threat_model>

<verification>
- `pytest tests/test_ci_lockstep_graph.py` passes all 8 structural assertions against the real workflow.
- `bash infrastructure/scripts/verify-phase17-workflow.sh` exits 0 (actionlint zero findings + graph parse).
- `make verify-phase17` runs the migrations + full + workflow gates.
- docs/ci-lockstep.md documents the five residual items and the deploy enable-steps; no green live run is claimed.
</verification>

<success_criteria>
SC3 is proven STRUCTURALLY by parsing the `needs:` graph (not by trusting comments); the workflow is
actionlint-clean; every Phase-17 local gate is one `make` target away; and the honest residual (live
run, GHCR publish + visibility flip, full-boot dry-run, live deploy, LICENSE reconciliation) is
documented truthfully rather than faked — closing REL-01/REL-02/REL-03 at the locally-verifiable bar.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ci-lockstep/17-04-SUMMARY.md`.
</output>
