---
phase: "07"
plan: "07-09"
type: "execute"
wave: 7
depends_on: ["07-03", "07-06", "07-08"]
files_modified:
  - "apps/librechat-bridge/app/task_intent_detector.py"
  - "apps/librechat-bridge/app/mongo_watcher.py"
  - "apps/librechat-bridge/app/config.py"
  - "apps/librechat-bridge/pyproject.toml"
  - "infrastructure/docker-compose.yml"
autonomous: true
requirements: ["D5", "D6"]

must_haves:
  truths:
    - "Quand un message LibreChat contient une intention de tâche détectable (\"fais ça\", \"quelqu'un doit\", \"@nom fait X\", \"TODO ...\"), une tâche est créée automatiquement dans /v1/tasks avec source='chat' (D5 trigger 3)"
    - "La détection est faite via Claude (appel court, modèle Haiku) — fail-soft : aucune erreur ne bloque le forward du message standard vers memory-api"
    - "L'extraction retourne {has_task: bool, title: str, assignee_email: str|null, description: str|null} — strict JSON parsing"
    - "L'email de l'assigné (s'il est résolvable depuis les contacts du team_scope) est utilisé pour set assigned_to ; sinon null"
    - "La création est silencieuse (D5 — \"création silencieuse recommandée\" — RESEARCH.md L587). source_ref pointe vers le memory_item du message"
    - "Si bridge ne peut pas hooker (config désactivée TASK_INTENT_DETECTION=false) → fall-back : marquer le memory_item upserted via memory-api avec metadata.contains_action: true → laisser le hook _maybe_create_task_from_action de 07-06 gérer la création (chemin déjà testé)"
  artifacts:
    - path: "apps/librechat-bridge/app/task_intent_detector.py"
      provides: "Module Claude pour détection d'intention de tâche dans les messages"
      contains: "async def detect_task_intent"
    - path: "apps/librechat-bridge/app/mongo_watcher.py"
      provides: "Hook de détection branché dans le flow message → memory-api"
      contains: "from app.task_intent_detector import"
    - path: "apps/librechat-bridge/app/config.py"
      provides: "Settings TASK_INTENT_DETECTION + ANTHROPIC_API_KEY + MEMORY_API_TASKS_URL"
      contains: "TASK_INTENT_DETECTION"
    - path: "infrastructure/docker-compose.yml"
      provides: "Env vars ajoutés au service librechat-bridge"
      contains: "TASK_INTENT_DETECTION"
  key_links:
    - from: "mongo_watcher.py"
      to: "task_intent_detector.detect_task_intent"
      via: "appel après le forward standard du message vers memory-api"
      pattern: "detect_task_intent"
    - from: "task_intent_detector.py"
      to: "Claude API (anthropic)"
      via: "messages.create avec system prompt structuré"
      pattern: "AsyncAnthropic"
    - from: "task_intent_detector.py"
      to: "memory-api /v1/tasks"
      via: "POST avec bridge JWT après détection positive"
      pattern: "/v1/tasks"
---

<objective>
Implémenter le déclencheur D5 #3 (mentions LibreChat → tasks auto). Étendre `librechat-bridge` (sidecar MongoDB change-stream existant) pour détecter les intentions de tâche dans les messages chat via Claude, et créer une task via memory-api `/v1/tasks` (avec bridge JWT) quand une intention est détectée.

Pourquoi `librechat-bridge` ? C'est le seul service qui voit déjà les messages LibreChat en temps réel via MongoDB change streams. Il forward déjà chaque message vers memory-api. Ajouter un hook après ce forward (sans bloquer) est l'extension la plus naturelle.

Purpose: D5 trigger 3 (création silencieuse de tâche depuis chat) + D6 (notification email automatique via 07-06 si assigné résolu).

Output: 1 module nouveau (`task_intent_detector.py`) + extension de `mongo_watcher.py` + 3 nouveaux env vars + service mis à jour dans docker-compose. Création silencieuse, fail-soft, fallback graceful via metadata.contains_action si désactivé.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-crm-granola-tasks/07-CONTEXT.md
@.planning/phases/07-crm-granola-tasks/07-RESEARCH.md
@.planning/phases/07-crm-granola-tasks/07-PATTERNS.md
@apps/librechat-bridge/app/main.py
@apps/librechat-bridge/app/mongo_watcher.py
@apps/librechat-bridge/app/config.py
@apps/librechat-bridge/app/memory_api_client.py
@apps/librechat-bridge/app/bridge_token.py
@apps/librechat-bridge/pyproject.toml

<interfaces>
<!-- Existing librechat-bridge MongoDB change-stream watcher (mongo_watcher.py) -->
- `map_message(mongo_doc, mongo_db)` returns a payload dict with `{sub, conversation_id, role, content, source, metadata}`
- The forward loop calls `MemoryApiClient.forward_message(payload, team_scope)` which POSTs to `/v1/conversations/messages` (or equivalent — check existing client)

<!-- Existing bridge JWT helper (bridge_token.py — likely) -->
- The bridge already has a function to mint a bridge JWT scoped to a team. Reuse it for the new POST /v1/tasks call.
- If a helper doesn't exist, look at the pattern in `apps/granola-sync/app/memory_client.py` (`_make_bridge_jwt`).

<!-- memory-api /v1/tasks contract (created in 07-03) -->
POST /v1/tasks
  Headers: Authorization: Bearer <bridge JWT>, X-Team-Scope: <team>
  Body (JSON): {
    "title": "string (max 512)",
    "description": "string|null",
    "status": "todo",            // default
    "priority": "normal",        // default
    "assigned_to": "<contact UUID>|null",
    "source": "chat",
    "source_ref": "<memory_item UUID>|null",
    "project_scope": "string|null"
  }
  Response: 201 + TaskOut
  NOTE: the endpoint requires `_user_id_from_principal` to return a user (it raises 401 for bridge JWT in 07-03).
  Pour Phase 7, le bridge ne peut donc PAS appeler /v1/tasks directement avec son JWT bridge.

  → Stratégie correcte : utiliser le hook 07-06 `_maybe_create_task_from_action`. Le bridge marque `metadata.contains_action = true` lors du forward du message → memory-api le voit, déclenche le background task → tâche créée avec created_by = NULL (system attribution, migration 0010 nullable).

<!-- memory-api /v1/contacts pour resolve assignee email (uses bridge JWT acceptable here) -->
GET /v1/crm/contacts?email={email}
  Headers: Authorization: Bearer <bridge JWT>, X-Team-Scope: <team>
  Returns a list[ContactOut] — match by email[0].id si trouvé.

<!-- Anthropic Claude detection prompt -->
SYSTEM = """Detect whether the user message expresses a task assignment or commitment.
Return STRICT JSON (no markdown):
{
  "has_task": bool,
  "title": "concise actionable title <80 chars OR null",
  "description": "details OR null",
  "assignee_email": "email OR null",
  "assignee_name": "name OR null"
}

Triggers:
- Imperative: "fais ça", "envoie-moi", "prépare", "send the recap"
- Mention: "@alice fait X", "Bob doit terminer Y"
- TODO/action explicite: "TODO: ...", "action requise: ...", "à faire: ..."

If unclear or just informational: has_task=false (return rest as null).

Output ONLY the JSON object, no surrounding text or fences."""
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module task_intent_detector.py — détection Claude + dispatch via metadata.contains_action</name>
  <files>apps/librechat-bridge/app/task_intent_detector.py</files>
  <read_first>
    - apps/librechat-bridge/app/mongo_watcher.py (pour comprendre map_message et signature de l'enrichment)
    - apps/librechat-bridge/app/config.py (pour ajouter ensuite TASK_INTENT_DETECTION + ANTHROPIC_API_KEY)
    - apps/granola-sync/app/extractor.py (pattern Claude structured-output similaire — créé en 07-05)
    - .planning/phases/07-crm-granola-tasks/07-CONTEXT.md (D5 spec)
  </read_first>
  <action>
**Stratégie d'intégration** : `librechat-bridge` ne peut pas créer la tâche directement via `POST /v1/tasks` parce que cet endpoint (07-03) rejette les bridge JWT (401 — "user identity required for task creation"). À la place, on délègue la création au pattern existant de 07-06 : marquer le `metadata.contains_action = true` quand on détecte une intention de tâche, et laisser le background task `_maybe_create_task_from_action` (07-06) gérer la création server-side avec `created_by = NULL`.

Cela garantit :
- Une seule source de truth pour la création auto (07-06 hook)
- Pas de duplication de logique Claude entre bridge et memory-api (`_maybe_create_task_from_action` ré-extrait le titre, mais c'est OK : c'est le pattern existant et le bridge n'a pas accès aux contacts pour résoudre `assigned_to`)
- Le bridge reste un "détecteur d'intention" léger qui flagge → memory-api gère le détail

Créer `apps/librechat-bridge/app/task_intent_detector.py` :

```python
"""Task intent detection in LibreChat messages (D5 trigger 3).

Strategy: lightweight Claude call to detect whether a chat message expresses
a task assignment / commitment. If yes, set metadata.contains_action = true
on the memory_item being forwarded. The actual task creation is delegated
to memory-api's _maybe_create_task_from_action background hook (plan 07-06).

Fail-soft: any error returns False (no task flag added) and is logged at WARN level.
The standard message forward to memory-api is NEVER blocked by this hook.
"""

import json
from typing import Any

import structlog

from app.config import settings

log = structlog.get_logger(__name__)

SYSTEM_PROMPT = """Detect whether the user message expresses a task assignment or commitment.

Return STRICT JSON (no markdown, no prose, no fences):
{
  "has_task": true_or_false,
  "title": "concise actionable title <80 chars or null",
  "description": "details or null",
  "assignee_email": "email or null",
  "assignee_name": "name or null"
}

Triggers (positive cases):
- Imperative addressed to a specific person: "Alice, send the recap by Friday"
- Mention with action: "@bob fait X", "@alice doit terminer Y"
- Explicit TODO/action: "TODO: ...", "action requise: ...", "à faire: ..."
- Commitment: "I'll prepare the deck", "je vais envoyer le résumé"

Negative cases (informational, has_task=false):
- Question: "What's the status?"
- Discussion: "We talked about X yesterday"
- Greeting/banter: "Hi everyone"

If has_task=false, set all other fields to null.

Output ONLY the JSON object."""


_anthropic_client: Any | None = None


def _get_client():
    global _anthropic_client
    if _anthropic_client is not None:
        return _anthropic_client
    if not settings.ANTHROPIC_API_KEY:
        return None
    try:
        from anthropic import AsyncAnthropic
    except ImportError:
        log.warning("task_intent.anthropic_not_installed")
        return None
    _anthropic_client = AsyncAnthropic(api_key=settings.ANTHROPIC_API_KEY)
    return _anthropic_client


async def detect_task_intent(message_content: str) -> dict[str, Any] | None:
    """Detect task intent in a chat message. Fail-soft: returns None on any error.

    Returns:
        - dict {has_task, title, description, assignee_email, assignee_name} on success
        - None if disabled, no API key, content too short, or any error

    The caller decides what to do: typically, set metadata.contains_action=true
    when result["has_task"] is True, optionally include the extracted title in
    the metadata for downstream hooks.
    """
    if not settings.TASK_INTENT_DETECTION:
        return None
    if not message_content or len(message_content.strip()) < 8:
        return None

    client = _get_client()
    if client is None:
        return None

    try:
        msg = await client.messages.create(
            model=settings.ANTHROPIC_TASK_INTENT_MODEL,
            max_tokens=512,
            system=SYSTEM_PROMPT,
            messages=[{"role": "user", "content": message_content[:4000]}],
        )
        text = (msg.content[0].text if msg.content else "{}").strip()
        if text.startswith("```"):
            text = text.split("```", 2)[1].lstrip("json").strip()
        parsed = json.loads(text)
        if not isinstance(parsed, dict):
            return None
        # Sanity-check shape
        return {
            "has_task": bool(parsed.get("has_task")),
            "title": parsed.get("title") or None,
            "description": parsed.get("description") or None,
            "assignee_email": parsed.get("assignee_email") or None,
            "assignee_name": parsed.get("assignee_name") or None,
        }
    except json.JSONDecodeError as exc:
        log.warning("task_intent.json_decode_failed", error=str(exc))
        return None
    except Exception as exc:
        log.warning("task_intent.detection_failed", error=str(exc))
        return None
```
  </action>
  <verify>
    <automated>cd apps/librechat-bridge && python -c "from app.task_intent_detector import detect_task_intent; assert callable(detect_task_intent)"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/librechat-bridge/app/task_intent_detector.py` exists
    - File contains `async def detect_task_intent(message_content: str) -> dict[str, Any] | None:` (exact signature)
    - File contains `SYSTEM_PROMPT = """Detect whether the user message expresses a task` (or equivalent — system prompt as constant)
    - File contains `if not settings.TASK_INTENT_DETECTION:` (kill-switch check)
    - File contains `if not settings.ANTHROPIC_API_KEY:` (fail-soft if no key)
    - File contains `from anthropic import AsyncAnthropic` (lazy-imported inside _get_client)
    - File contains `client.messages.create(` (Anthropic Messages API call)
    - File contains `json.loads(text)` (strict JSON parse)
    - File contains `except json.JSONDecodeError` (fail-soft on bad JSON)
    - File contains `except Exception as exc:` with `log.warning("task_intent.detection_failed"` (fail-soft)
    - File NEVER raises (all error paths return `None` — verified by absence of unguarded `raise` outside __main__)
    - SYSTEM_PROMPT explicitly mentions: `has_task`, `title`, `description`, `assignee_email`, `assignee_name` (5 keys)
    - `python -c "from app.task_intent_detector import detect_task_intent"` succeeds (assumes anthropic installed; otherwise lazy import handles)
  </acceptance_criteria>
  <done>task_intent_detector.py expose detect_task_intent. Fail-soft à 4 niveaux : disabled, no key, short content, exception. Output strict 5 clés.</done>
</task>

<task type="auto">
  <name>Task 2: Wirer task_intent_detector dans mongo_watcher.py + ajouter config + deps</name>
  <files>apps/librechat-bridge/app/mongo_watcher.py, apps/librechat-bridge/app/config.py, apps/librechat-bridge/pyproject.toml</files>
  <read_first>
    - apps/librechat-bridge/app/mongo_watcher.py (lire l'intégralité — repérer le point d'appel à memory_api_client.forward_message ou équivalent)
    - apps/librechat-bridge/app/config.py (lire pour ajouter sans casser les vars existantes)
    - apps/librechat-bridge/pyproject.toml (lire deps — éviter doublons d'anthropic)
  </read_first>
  <action>
**1. Étendre `apps/librechat-bridge/app/config.py`** : ajouter à la classe `Settings` 3 nouveaux env vars :

```python
    # Phase 7 plan 07-09 — Task intent detection (D5 trigger 3)
    TASK_INTENT_DETECTION: bool = False  # opt-in — set TASK_INTENT_DETECTION=true to enable
    ANTHROPIC_API_KEY: str = ""
    ANTHROPIC_TASK_INTENT_MODEL: str = "claude-3-5-haiku-20241022"
```

NE PAS supprimer les vars existantes. Conserver l'ordre du fichier.

**2. Étendre `apps/librechat-bridge/pyproject.toml`** : ajouter `"anthropic>=0.50.0"` à la liste `dependencies` (vérifier qu'elle n'est pas déjà présente avec `grep`). Si elle l'est, ne pas dupliquer.

**3. Modifier `apps/librechat-bridge/app/mongo_watcher.py`** : Importer le détecteur et l'appeler APRÈS le mapping standard d'un message, AVANT le forward vers memory-api. Le résultat positif modifie le `metadata` du payload pour ajouter `contains_action: true` (et optionnellement `intent_title` pour le log).

Localiser dans le fichier la fonction qui forward un message (typiquement après `map_message(...)` retourne un payload, juste avant le `await memory_api_client.forward_message(payload, team_scope)`). Insérer :

```python
from app.task_intent_detector import detect_task_intent

# ... existing code ...

# Inside the watch_loop or wherever map_message is followed by forward:
payload = await map_message(doc, mongo_db)
if payload is None:
    continue

# Phase 7 plan 07-09 — Task intent detection (D5 trigger 3)
# Only consider user-authored messages (assistant outputs go through agent path / 07-06 trigger 2)
if payload.get("role") == "user" and payload.get("content"):
    intent = await detect_task_intent(payload["content"])
    if intent and intent.get("has_task"):
        # Flag the memory_item so memory-api's _maybe_create_task_from_action (07-06) creates the task.
        # We don't call /v1/tasks directly — bridge JWT is rejected there (07-03 line 257).
        payload.setdefault("metadata", {})
        payload["metadata"]["contains_action"] = True
        if intent.get("title"):
            payload["metadata"]["task_intent_title"] = intent["title"]
        if intent.get("assignee_email"):
            payload["metadata"]["task_intent_assignee_email"] = intent["assignee_email"]
        log.info(
            "task_intent.detected",
            sub=payload.get("sub"),
            conv=payload.get("conversation_id"),
            title=intent.get("title"),
        )
```

Le hook est strictement additif : si `detect_task_intent` retourne None ou `has_task=false`, le payload reste inchangé. Le forward standard continue.

NE PAS toucher au flow du conversations watcher ni au heartbeat. Le hook doit être placé UNIQUEMENT dans le path messages.

  </action>
  <verify>
    <automated>grep -q "TASK_INTENT_DETECTION" apps/librechat-bridge/app/config.py && grep -q "from app.task_intent_detector import detect_task_intent" apps/librechat-bridge/app/mongo_watcher.py && grep -q "anthropic" apps/librechat-bridge/pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/librechat-bridge/app/config.py` contains `TASK_INTENT_DETECTION: bool = False` (exact)
    - config.py contains `ANTHROPIC_API_KEY: str = ""` (exact)
    - config.py contains `ANTHROPIC_TASK_INTENT_MODEL: str = "claude-3-5-haiku-20241022"` (exact)
    - All previously existing settings preserved
    - File `apps/librechat-bridge/pyproject.toml` contains `anthropic` (with version pin >=0.50.0)
    - File `apps/librechat-bridge/app/mongo_watcher.py` contains `from app.task_intent_detector import detect_task_intent` (exact import)
    - mongo_watcher.py contains `if payload.get("role") == "user"` (guard limiting to user-authored)
    - mongo_watcher.py contains `intent = await detect_task_intent(` (await call)
    - mongo_watcher.py contains `payload["metadata"]["contains_action"] = True` (exact flag set)
    - mongo_watcher.py contains `task_intent.detected` (log key)
    - The hook is INSIDE the existing watch_loop / message processing path (not in conversations watcher)
    - `cd apps/librechat-bridge && python -c "from app.mongo_watcher import watch_loop; from app.task_intent_detector import detect_task_intent"` succeeds
  </acceptance_criteria>
  <done>config.py + mongo_watcher.py + pyproject étendus. Hook user-only, additif (None → no-op), fail-soft. Délégation à 07-06 via metadata.contains_action.</done>
</task>

<task type="auto">
  <name>Task 3: Env vars librechat-bridge dans docker-compose.yml</name>
  <files>infrastructure/docker-compose.yml</files>
  <read_first>
    - infrastructure/docker-compose.yml (lire le bloc librechat-bridge existant — service de Phase 1/2)
  </read_first>
  <action>
Ajouter trois env vars au service `librechat-bridge` dans `infrastructure/docker-compose.yml`. Localiser le bloc service `librechat-bridge:` et insérer dans son `environment:` section, en respectant l'indentation existante :

```yaml
      TASK_INTENT_DETECTION: ${TASK_INTENT_DETECTION:-false}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      ANTHROPIC_TASK_INTENT_MODEL: ${ANTHROPIC_TASK_INTENT_MODEL:-claude-3-5-haiku-20241022}
```

Notes :
- `TASK_INTENT_DETECTION:-false` : par défaut désactivé (kill-switch). L'opérateur active explicitement via .env.
- `ANTHROPIC_API_KEY` : pass-through, partagé avec memory-api et granola-sync.
- NE PAS modifier d'autres env vars du service librechat-bridge.
- NE PAS toucher aux autres services.

Vérifier après modification :
```bash
docker compose -f infrastructure/docker-compose.yml config | grep -A2 librechat-bridge | grep TASK_INTENT
```
  </action>
  <verify>
    <automated>grep -A 30 "  librechat-bridge:" infrastructure/docker-compose.yml | grep -q "TASK_INTENT_DETECTION" && (cd infrastructure && docker compose config > /dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - File `infrastructure/docker-compose.yml` contains `TASK_INTENT_DETECTION: ${TASK_INTENT_DETECTION:-false}` (exact)
    - File contains `ANTHROPIC_TASK_INTENT_MODEL: ${ANTHROPIC_TASK_INTENT_MODEL:-claude-3-5-haiku-20241022}` (exact)
    - These env vars are within the `librechat-bridge` service block (verify with `grep -A 30 "librechat-bridge:" ... | grep TASK_INTENT_DETECTION`)
    - All other services in compose are unchanged
    - `docker compose -f infrastructure/docker-compose.yml config` returns 0 exit code (YAML + interpolation valid)
  </acceptance_criteria>
  <done>librechat-bridge expose les 3 env vars Phase 7 task-intent. Default désactivé (kill-switch). Compose valide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LibreChat user message → librechat-bridge | User-controlled content. Forward path already trusted (Phase 1+). |
| librechat-bridge ↔ Anthropic API (external) | Outbound HTTPS to api.anthropic.com. Sends user message text for intent detection. |
| metadata.contains_action flag → memory-api | Server-internal trust. The bridge can SET this flag (it's metadata it controls), but memory-api re-validates the content via its own Claude call before INSERT (07-06 `_maybe_create_task_from_action`). |
| ANTHROPIC_API_KEY in env → librechat-bridge process | Same risk profile as memory-api. Container compromise = key exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Prompt Injection | Adversarial user message manipulating Claude detection prompt | mitigate | The detector returns ONLY a structured boolean+title, never executes code. Even if prompt-injection forces `has_task=true` with adversarial title, the downstream task creation in 07-06 re-validates via its own Claude call AND the title is bound as parameterized SQL value (not concatenated). Worst case: a spurious task created with `created_by=NULL` — easily filterable + auditable. |
| T-07-09-02 | IDOR | Bridge sets metadata.contains_action for messages of other teams | mitigate | The bridge already operates within a `team_scope` resolved by `resolve_team_scope` in mongo_watcher.py (existing). Each message forward includes the resolved team_scope; memory-api validates membership server-side. The bridge cannot escalate to set flags for arbitrary teams. |
| T-07-09-03 | Information Disclosure | Chat content sent to Anthropic | accept | Same accepted risk as 07-05 / 07-06 (LLM extraction). Documented in privacy disclosure. The `TASK_INTENT_DETECTION=false` kill-switch lets ops disable per environment. |
| T-07-09-04 | DoS | Claude API timeout cascades into MongoDB watcher | mitigate | `detect_task_intent` is awaited within the watch loop, but the underlying Anthropic SDK has built-in timeouts. Worst case: a slow Claude call delays one message forward. To bound impact, the message content is capped at 4000 chars before sending. The next iteration of the watcher proceeds normally. Future hardening: wrap in `asyncio.wait_for` with explicit timeout. |
| T-07-09-05 | Information Disclosure | Detection result logged including assignee email | mitigate | Log keys `task_intent.detected` log only `sub`, `conv`, `title`. Assignee email is NOT logged — it's only added to metadata (which goes to memory-api over the existing TLS-internal-net path). |
| T-07-09-06 | Spoofing | Bridge mints JWT for arbitrary team_scope | accept | Same trust assumption as Phase 1+ — bridge JWT secret is shared between bridge and memory-api. Compromise of either → both compromised. No new attack surface from this plan. |
| T-07-09-07 | Tampering | LibreChat user crafts message that auto-creates a task with admin-only privileges | mitigate | Auto-created tasks have `created_by = NULL` and `source = 'chat'` — they are NOT user-attributed. Any sensitive task workflow (approvals, escalations) MUST filter by `created_by IS NOT NULL` to exclude system-generated entries. v1: this is documented; v2 may add a separate `system_generated` boolean. |
| T-07-09-08 | DoS | Spamming chat with TODO-like messages → Claude bill explosion | accept | TASK_INTENT_DETECTION is opt-in (default false). When enabled, ops monitor Anthropic billing. Cap per message (4000 chars) limits per-call cost. Phase 7 v1 acceptable; future rate-limit at bridge layer if needed. |
| T-07-09-09 | Repudiation | No audit trail when bridge sets contains_action flag | mitigate | The downstream task creation in 07-06 produces an audit_log entry (`task.created` with source='chat'). Combined with `source_ref=memory_item.id`, the chain is: chat message → memory_item (logged) → task (audit_log). Full provenance. |
| T-07-09-10 | Tampering | Disabling TASK_INTENT_DETECTION still allows manual `metadata.contains_action` setters | mitigate | The 07-06 hook re-runs Claude to extract title regardless of who set the flag. The flag is just an opt-in for the detection to run. Adversarial setting at most causes a false-positive task — visible, auditable, deletable. |
</threat_model>

<verification>
- `cd apps/librechat-bridge && python -c "from app.task_intent_detector import detect_task_intent"` succeed
- Avec `TASK_INTENT_DETECTION=false` : aucun appel Claude, aucun changement de metadata. Vérifier `docker logs xbrain-librechat-bridge | grep task_intent.detected` retourne 0 ligne.
- Avec `TASK_INTENT_DETECTION=true` + `ANTHROPIC_API_KEY` valide :
  - Envoyer dans LibreChat : "Alice, envoie-moi le rapport demain" → un memory_item est créé avec `metadata.contains_action=true`. Quelques secondes plus tard, vérifier `SELECT count(*) FROM tasks WHERE source='chat' AND created_at > now() - interval '2 minutes'` >= 1.
  - Envoyer un message neutre ("Hi everyone, how's it going?") → aucune tâche créée. `metadata.contains_action` n'est pas set.
- Avec `ANTHROPIC_API_KEY=""` (vide) : detector retourne None silencieusement. Aucune tâche créée. Aucune erreur 500.
- `docker compose -f infrastructure/docker-compose.yml config | grep -A2 librechat-bridge | grep TASK_INTENT_DETECTION` montre la variable propagée.
- Verify-phase7.sh continue de PASS 8/8 (cette plan ne casse pas les tests existants).
</verification>

<success_criteria>
- 1 module nouveau (`task_intent_detector.py`) dans librechat-bridge
- 3 env vars ajoutés à `config.py` (TASK_INTENT_DETECTION, ANTHROPIC_API_KEY, ANTHROPIC_TASK_INTENT_MODEL)
- 1 dep ajoutée à pyproject.toml (anthropic>=0.50.0)
- mongo_watcher.py étendu avec hook user-message-only, additif, fail-soft
- 3 env vars propagés au service librechat-bridge dans docker-compose
- Default désactivé (kill-switch via TASK_INTENT_DETECTION=false)
- Aucune régression sur le forward standard de messages vers memory-api
- Création silencieuse de tâche quand intent détecté (pas de notification chat — conforme RESEARCH.md L587)
</success_criteria>

<output>
After completion, create `.planning/phases/07-crm-granola-tasks/07-09-SUMMARY.md`. Le SUMMARY doit documenter explicitement le choix d'utiliser `metadata.contains_action` plutôt qu'un appel direct à `/v1/tasks` (raison : 07-03 rejette les bridge JWT).
</output>
