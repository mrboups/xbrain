---
phase: "07"
plan: "07-02"
type: "execute"
wave: 2
depends_on: ["07-01"]
files_modified:
  - "apps/memory-api/app/deps.py"
  - "apps/memory-api/app/routes/crm.py"
  - "apps/memory-api/app/main.py"
autonomous: true
requirements: ["D1", "D2"]

must_haves:
  truths:
    - "Un user d'une team `starter` reçoit 403 sur tout endpoint /v1/crm/*"
    - "Un user d'une team `team` ou `enterprise` peut faire CRUD sur ses contacts"
    - "Les contacts sont scopés par team_scope — Team A ne voit pas les contacts de Team B"
    - "POST /v1/crm/contacts upsert sur (team_scope, email) — même email écrit deux fois incrémente interaction_count"
    - "Toutes les mutations CRM produisent une entrée audit_log"
  artifacts:
    - path: "apps/memory-api/app/deps.py"
      provides: "Dependency `require_paid_tier`"
      contains: "async def require_paid_tier"
    - path: "apps/memory-api/app/routes/crm.py"
      provides: "Router /v1/crm/contacts (CRUD)"
      contains: "router = APIRouter()"
    - path: "apps/memory-api/app/main.py"
      provides: "include_router(crm.router)"
      contains: "from app.routes import crm"
  key_links:
    - from: "crm.py"
      to: "deps.py:require_paid_tier"
      via: "Depends(require_paid_tier) on every endpoint"
      pattern: "Depends\\(require_paid_tier\\)"
    - from: "deps.py:require_paid_tier"
      to: "teams.plan column (migration 0008)"
      via: "SELECT plan FROM teams WHERE slug=:slug"
      pattern: "SELECT plan FROM teams"
    - from: "crm.py"
      to: "audit.write_audit"
      via: "every mutation logs action=crm.contact.*"
      pattern: "action=\"crm\\.contact\\."
---

<objective>
Implémenter le router CRM contacts avec paid tier enforcement (D2). Créer le helper `require_paid_tier` dans deps.py, le router CRUD `/v1/crm/contacts` dans routes/crm.py, et l'enregistrer dans main.py.

Purpose: Les endpoints CRM sont la première matérialisation du contrat D1+D2 — sans eux, les contacts ne sont ni accessibles ni protégés.

Output: Router fonctionnel avec 5 endpoints (list, get, create, update, delete) — tous protégés par `require_paid_tier` et audit-loggés.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-crm-granola-tasks/07-CONTEXT.md
@.planning/phases/07-crm-granola-tasks/07-RESEARCH.md
@.planning/phases/07-crm-granola-tasks/07-PATTERNS.md
@apps/memory-api/app/deps.py
@apps/memory-api/app/routes/teams.py
@apps/memory-api/app/routes/admin_drive.py
@apps/memory-api/app/main.py
@apps/memory-api/app/audit.py

<interfaces>
<!-- Existing dependencies extracted from deps.py -->
From `apps/memory-api/app/deps.py`:
```python
async def get_session() -> AsyncIterator[AsyncSession]: ...
async def get_current_principal(...) -> dict[str, Any]: ...
async def get_team_scope(
    principal: dict[str, Any] = Depends(get_current_principal),
    x_team_scope: str = Header(..., alias="X-Team-Scope"),
    session: AsyncSession = Depends(get_session),
) -> str: ...  # raises 403 if user not in team
```

From `apps/memory-api/app/audit.py`:
```python
async def write_audit(
    session: AsyncSession,
    *,
    actor_user_id: UUID | None,
    team_scope: str,
    action: str,
    target_id: str | None = None,
    payload: dict | None = None,
) -> None: ...
```

From `apps/memory-api/app/routes/teams.py` (response model pattern):
```python
class TeamOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: UUID
    slug: str
    display_name: str
```

Tagging contract for contacts (from migration 0009):
- team_scope (required), contact_type ('direct'|'mass'), project_scope (optional)
- truth_level (default 'EPHEMERAL'), confidence (default 1.0), source (required)
- full_name, email, company, role (optional)
- list_source, opt_in_status, list_added_at (mass-specific)
- interaction_count (default 0), last_interaction_at
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper `require_paid_tier` dans deps.py</name>
  <files>apps/memory-api/app/deps.py</files>
  <read_first>
    - apps/memory-api/app/deps.py (lire l'existant complet — pattern get_team_scope lignes ~116-136)
    - .planning/phases/07-crm-granola-tasks/07-RESEARCH.md (paid tier pattern, lignes 291-323)
    - .planning/phases/07-crm-granola-tasks/07-PATTERNS.md (require_paid_tier exact code, lignes 593-629)
  </read_first>
  <action>
Ajouter une nouvelle dépendance `require_paid_tier` dans `apps/memory-api/app/deps.py`, juste après `get_team_scope`. Utilise la chaîne de dépendances `get_team_scope` (qui fait déjà la membership check) puis SELECT le plan dans la table teams.

Code exact à ajouter (à la fin du fichier ou après get_team_scope) :

```python
async def require_paid_tier(
    session: AsyncSession = Depends(get_session),
    team_scope: str = Depends(get_team_scope),
) -> str:
    """Raises 403 if the team's plan is 'starter'. Used for /v1/crm/* and /v1/tasks/* (D2).

    The dependency chain (require_paid_tier → get_team_scope → get_current_principal)
    means membership and authentication are already validated. This adds the plan check.
    """
    import sqlalchemy as sa
    row = (await session.execute(
        sa.text("SELECT plan FROM teams WHERE slug = :slug"),
        {"slug": team_scope},
    )).fetchone()
    if row is None or row.plan == "starter":
        raise HTTPException(
            status_code=403,
            detail="CRM and task tracking require a Team or Enterprise plan",
        )
    return team_scope
```

S'assurer que `HTTPException` et `Depends` sont déjà importés (ils le sont — `get_team_scope` les utilise).

**Ajouter aussi le helper partagé `_user_id_from_principal`** dans `deps.py` (s'il n'existe pas déjà — vérifier au préalable avec `grep "_user_id_from_principal" apps/memory-api/app/deps.py`). Pattern :

```python
from uuid import UUID


def _user_id_from_principal(principal: dict[str, Any]) -> UUID | None:
    """Extract the user.id from a resolved principal, or None for bridge JWTs."""
    user = principal.get("user")
    if user is None:
        return None
    return user.id
```

Ce helper est réutilisé par `crm.py`, `tasks.py`, `granola_integration.py` — DRY.
  </action>
  <verify>
    <automated>cd apps/memory-api && python -c "from app.deps import require_paid_tier; assert require_paid_tier.__name__ == 'require_paid_tier'"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/memory-api/app/deps.py` contains `async def require_paid_tier(`
    - File contains `team_scope: str = Depends(get_team_scope)` (chained dependency)
    - File contains `SELECT plan FROM teams WHERE slug = :slug` (exact SQL)
    - File contains `row.plan == "starter"` (the rejection condition)
    - File contains `status_code=403` and `"CRM and task tracking require a Team or Enterprise plan"` (exact error message)
    - `python -c "from app.deps import require_paid_tier"` succeeds without ImportError
    - `grep -c "async def require_paid_tier" apps/memory-api/app/deps.py` returns `1` (no duplicate)
    - File contains `def _user_id_from_principal(principal: dict[str, Any]) -> UUID | None:` (shared helper)
    - `python -c "from app.deps import _user_id_from_principal"` succeeds without ImportError
  </acceptance_criteria>
  <done>Helper `require_paid_tier` importable depuis app.deps. Chaîne de dépendances correcte. Aucun duplicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Router /v1/crm/contacts — CRUD complet avec audit</name>
  <files>apps/memory-api/app/routes/crm.py, apps/memory-api/app/routes/__init__.py</files>
  <read_first>
    - apps/memory-api/app/routes/teams.py (pattern complet — APIRouter, BaseModel ConfigDict, write_audit, session.commit)
    - apps/memory-api/app/routes/admin_drive.py (pattern SQL upsert ON CONFLICT, lignes 130-144)
    - apps/memory-api/app/routes/promotions.py (pattern ConfigDict from_attributes, lignes 44-56)
    - apps/memory-api/app/audit.py (signature write_audit)
    - .planning/phases/07-crm-granola-tasks/07-PATTERNS.md (Section crm.py, lignes 429-524)
  </read_first>
  <behavior>
    - GET /v1/crm/contacts?contact_type=direct&limit=50 → 200, list[ContactOut] filtrée par team_scope (header X-Team-Scope) et contact_type optionnel
    - GET /v1/crm/contacts/{id} → 200 ContactOut si appartient à team, 404 sinon
    - POST /v1/crm/contacts {contact_type, email?, full_name?, source, ...} → 201 ContactOut. Si email existe déjà pour ce team_scope, fait UPDATE (interaction_count+=1, updated_at=now()) et retourne 200 avec la row existante
    - PATCH /v1/crm/contacts/{id} → 200 ContactOut mise à jour (champs partiels autorisés : full_name, email, company, role, project_scope, truth_level, opt_in_status)
    - DELETE /v1/crm/contacts/{id} → 204 (suppression hard ; tasks.assigned_to passera à NULL via FK)
    - Toutes les routes : 403 si plan = 'starter' (require_paid_tier)
    - Toutes les routes : 403 si user pas membre du team_scope (cascade depuis get_team_scope)
    - Toutes les mutations : write_audit avec action `crm.contact.created` | `crm.contact.updated` | `crm.contact.deleted` | `crm.contact.merged`
  </behavior>
  <action>
Créer `apps/memory-api/app/routes/crm.py` avec un APIRouter complet pour CRUD contacts. Suivre exactement le pattern de `teams.py` :

```python
"""/v1/crm — CRM contacts CRUD (paid tier only — D1, D2)."""

from typing import Any
from uuid import UUID

import sqlalchemy as sa
from fastapi import APIRouter, Depends, HTTPException, Query
from pydantic import BaseModel, ConfigDict, Field
from sqlalchemy.ext.asyncio import AsyncSession

from app.audit import write_audit
from app.deps import _user_id_from_principal, get_current_principal, get_session, require_paid_tier

router = APIRouter()


# ── Pydantic models ─────────────────────────────────────────────────────

class ContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    contact_type: str = Field(..., pattern=r"^(direct|mass)$")
    full_name: str | None = Field(None, max_length=256)
    email: str | None = Field(None, max_length=256)
    company: str | None = Field(None, max_length=256)
    role: str | None = Field(None, max_length=256)
    project_scope: str | None = Field(None, max_length=64)
    source: str = Field(..., min_length=1, max_length=128)
    truth_level: str = Field(default="EPHEMERAL", pattern=r"^(EPHEMERAL|WORKING|VALIDATED|CANONICAL|PUBLIC)$")
    confidence: float = Field(default=1.0, ge=0.0, le=1.0)
    list_source: str | None = Field(None, max_length=256)
    opt_in_status: str | None = Field(default="unknown", pattern=r"^(unknown|opted_in|opted_out)$")


class ContactPatchBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    full_name: str | None = None
    email: str | None = None
    company: str | None = None
    role: str | None = None
    project_scope: str | None = None
    truth_level: str | None = Field(None, pattern=r"^(EPHEMERAL|WORKING|VALIDATED|CANONICAL|PUBLIC)$")
    opt_in_status: str | None = Field(None, pattern=r"^(unknown|opted_in|opted_out)$")


class ContactOut(BaseModel):
    model_config = ConfigDict(from_attributes=True, extra="ignore")
    id: UUID
    team_scope: str
    contact_type: str
    full_name: str | None
    email: str | None
    company: str | None
    role: str | None
    project_scope: str | None
    truth_level: str
    confidence: float
    source: str
    list_source: str | None
    opt_in_status: str | None
    interaction_count: int


# ── Endpoints ───────────────────────────────────────────────────────────
# (note: `_user_id_from_principal` is imported from app.deps — added there in Task 1
# if not already present. Pattern: `return principal.get("user").id if principal.get("user") else None`.)


@router.get("/crm/contacts", response_model=list[ContactOut])
async def list_contacts(
    session: AsyncSession = Depends(get_session),
    team_scope: str = Depends(require_paid_tier),
    contact_type: str | None = Query(None, pattern=r"^(direct|mass)$"),
    limit: int = Query(default=50, ge=1, le=200),
    offset: int = Query(default=0, ge=0),
):
    sql = "SELECT * FROM contacts WHERE team_scope = :ts"
    params: dict[str, Any] = {"ts": team_scope}
    if contact_type:
        sql += " AND contact_type = :ct"
        params["ct"] = contact_type
    sql += " ORDER BY last_interaction_at DESC NULLS LAST, created_at DESC LIMIT :lim OFFSET :off"
    params["lim"] = limit
    params["off"] = offset
    rows = (await session.execute(sa.text(sql), params)).mappings().all()
    return [ContactOut(**dict(r)) for r in rows]


@router.get("/crm/contacts/{contact_id}", response_model=ContactOut)
async def get_contact(
    contact_id: UUID,
    session: AsyncSession = Depends(get_session),
    team_scope: str = Depends(require_paid_tier),
):
    row = (await session.execute(
        sa.text("SELECT * FROM contacts WHERE id = :id AND team_scope = :ts"),
        {"id": str(contact_id), "ts": team_scope},
    )).mappings().fetchone()
    if row is None:
        raise HTTPException(404, "contact not found in this team")
    return ContactOut(**dict(row))


@router.post("/crm/contacts", response_model=ContactOut, status_code=201)
async def create_contact(
    body: ContactCreateBody,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
    team_scope: str = Depends(require_paid_tier),
):
    user_id = _user_id_from_principal(principal)
    # If email provided, upsert on (team_scope, email)
    if body.email:
        result = (await session.execute(sa.text("""
            INSERT INTO contacts (
                team_scope, contact_type, full_name, email, company, role,
                project_scope, truth_level, confidence, source,
                list_source, opt_in_status, interaction_count, last_interaction_at
            ) VALUES (
                :ts, :ct, :fn, :em, :co, :ro,
                :ps, :tl, :cf, :sr,
                :ls, :ois, 1, now()
            )
            ON CONFLICT (team_scope, email) WHERE email IS NOT NULL DO UPDATE
            SET interaction_count = contacts.interaction_count + 1,
                last_interaction_at = now(),
                full_name = COALESCE(EXCLUDED.full_name, contacts.full_name),
                company = COALESCE(EXCLUDED.company, contacts.company),
                role = COALESCE(EXCLUDED.role, contacts.role),
                truth_level = CASE
                    WHEN EXCLUDED.truth_level = 'WORKING' AND contacts.truth_level = 'EPHEMERAL'
                    THEN 'WORKING' ELSE contacts.truth_level
                END,
                updated_at = now()
            RETURNING *
        """), {
            "ts": team_scope, "ct": body.contact_type, "fn": body.full_name,
            "em": body.email, "co": body.company, "ro": body.role,
            "ps": body.project_scope, "tl": body.truth_level, "cf": body.confidence,
            "sr": body.source, "ls": body.list_source, "ois": body.opt_in_status,
        })).mappings().fetchone()
    else:
        # No email — straight INSERT, dedup tolerated for v1
        result = (await session.execute(sa.text("""
            INSERT INTO contacts (
                team_scope, contact_type, full_name, email, company, role,
                project_scope, truth_level, confidence, source,
                list_source, opt_in_status, interaction_count, last_interaction_at
            ) VALUES (
                :ts, :ct, :fn, NULL, :co, :ro,
                :ps, :tl, :cf, :sr,
                :ls, :ois, 1, now()
            )
            RETURNING *
        """), {
            "ts": team_scope, "ct": body.contact_type, "fn": body.full_name,
            "co": body.company, "ro": body.role, "ps": body.project_scope,
            "tl": body.truth_level, "cf": body.confidence, "sr": body.source,
            "ls": body.list_source, "ois": body.opt_in_status,
        })).mappings().fetchone()

    await write_audit(
        session,
        actor_user_id=user_id,
        team_scope=team_scope,
        action="crm.contact.created",
        target_id=str(result["id"]),
        payload={"source": result["source"], "type": result["contact_type"]},
    )
    await session.commit()
    return ContactOut(**dict(result))


@router.patch("/crm/contacts/{contact_id}", response_model=ContactOut)
async def update_contact(
    contact_id: UUID,
    body: ContactPatchBody,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
    team_scope: str = Depends(require_paid_tier),
):
    fields = body.model_dump(exclude_unset=True)
    if not fields:
        raise HTTPException(400, "no fields to update")
    set_clause = ", ".join(f"{k} = :{k}" for k in fields)
    fields["id"] = str(contact_id)
    fields["ts"] = team_scope
    result = (await session.execute(sa.text(
        f"UPDATE contacts SET {set_clause}, updated_at = now() "
        f"WHERE id = :id AND team_scope = :ts RETURNING *"
    ), fields)).mappings().fetchone()
    if result is None:
        raise HTTPException(404, "contact not found in this team")
    await write_audit(
        session,
        actor_user_id=_user_id_from_principal(principal),
        team_scope=team_scope,
        action="crm.contact.updated",
        target_id=str(contact_id),
        payload={"fields": list(body.model_dump(exclude_unset=True).keys())},
    )
    await session.commit()
    return ContactOut(**dict(result))


@router.delete("/crm/contacts/{contact_id}", status_code=204)
async def delete_contact(
    contact_id: UUID,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
    team_scope: str = Depends(require_paid_tier),
):
    result = await session.execute(sa.text(
        "DELETE FROM contacts WHERE id = :id AND team_scope = :ts RETURNING id"
    ), {"id": str(contact_id), "ts": team_scope})
    if result.fetchone() is None:
        raise HTTPException(404, "contact not found in this team")
    await write_audit(
        session,
        actor_user_id=_user_id_from_principal(principal),
        team_scope=team_scope,
        action="crm.contact.deleted",
        target_id=str(contact_id),
        payload={},
    )
    await session.commit()
    return None
```

S'assurer que `apps/memory-api/app/routes/__init__.py` n'a pas besoin de modification (vérifier — les autres routes ne sont pas listées dedans, l'import se fait depuis main.py).
  </action>
  <verify>
    <automated>cd apps/memory-api && python -c "from app.routes.crm import router; print(len(router.routes))" | grep -E "^[5-9]$|^1[0-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/memory-api/app/routes/crm.py` exists
    - File contains `router = APIRouter()` (exact)
    - File contains `from app.deps import _user_id_from_principal, get_current_principal, get_session, require_paid_tier`
    - File contains `from app.audit import write_audit`
    - File contains 5 endpoints: `@router.get("/crm/contacts"`, `@router.get("/crm/contacts/{contact_id}"`, `@router.post("/crm/contacts"`, `@router.patch("/crm/contacts/{contact_id}"`, `@router.delete("/crm/contacts/{contact_id}"`
    - File imports `_user_id_from_principal` from `app.deps` (NOT redefined locally)
    - `grep -c "def _user_id_from_principal" apps/memory-api/app/routes/crm.py` returns `0` (helper imported, not local)
    - File contains `team_scope: str = Depends(require_paid_tier)` on EVERY endpoint (grep -c returns 5)
    - File contains `ON CONFLICT (team_scope, email) WHERE email IS NOT NULL DO UPDATE` (upsert pattern)
    - File contains `action="crm.contact.created"`, `action="crm.contact.updated"`, `action="crm.contact.deleted"` (audit actions)
    - File contains `await session.commit()` after every write_audit call
    - File contains `class ContactOut(BaseModel)` with `model_config = ConfigDict(from_attributes=True`
    - `python -c "from app.routes.crm import router"` succeeds without ImportError
    - The router has exactly 5 routes (`len(router.routes) == 5`)
  </acceptance_criteria>
  <done>Router CRM importable, 5 endpoints CRUD avec require_paid_tier + audit + commit. Pattern teams.py respecté.</done>
</task>

<task type="auto">
  <name>Task 3: Enregistrer le router dans main.py</name>
  <files>apps/memory-api/app/main.py</files>
  <read_first>
    - apps/memory-api/app/main.py (lire l'existant — voir les import + include_router actuels)
    - .planning/phases/07-crm-granola-tasks/07-PATTERNS.md (Section main.py, lignes 633-660)
  </read_first>
  <action>
Modifier `apps/memory-api/app/main.py` pour importer et enregistrer le router CRM.

1. Dans le bloc d'imports `from app.routes import (...)` — ajouter `crm` à la liste alphabétique (entre `conversations` et `drive_webhook`)

2. Dans le bloc des `app.include_router(...)` — ajouter une ligne :
```python
app.include_router(crm.router, prefix="/v1", tags=["crm"])
```

Cette ligne doit être positionnée près des autres routers (par exemple juste après `admin_projects` ou par ordre logique du fichier — pas important, juste qu'elle soit présente AVANT `app.include_router(health.router, ...)` n'est pas exigé, mais cohérent avec l'ordre alphabétique du module).

NE PAS toucher aux autres lignes existantes. NE PAS supprimer d'autres routers.
  </action>
  <verify>
    <automated>cd apps/memory-api && python -c "from app.main import app; routes = [r.path for r in app.routes if hasattr(r, 'path')]; assert any('/v1/crm/contacts' in p for p in routes), f'crm routes missing: {routes}'"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/memory-api/app/main.py` contains `crm` in the `from app.routes import (...)` block
    - File contains `app.include_router(crm.router, prefix="/v1", tags=["crm"])` (exact line)
    - File still contains all previously existing imports (no router accidentally removed)
    - `python -c "from app.main import app"` succeeds without error
    - `python -c "from app.main import app; print([r.path for r in app.routes])"` includes `/v1/crm/contacts` and `/v1/crm/contacts/{contact_id}`
  </acceptance_criteria>
  <done>App importable, routes /v1/crm/contacts visibles dans le routing FastAPI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → memory-api `/v1/crm/*` | User-controlled input crosses HTTP boundary. Must validate body, enforce auth + paid tier + team membership. |
| memory-api → PostgreSQL | All queries must use parameterized SQL (sa.text + params), never string concatenation. |
| Audit log | Every mutation must be logged with actor_user_id + team_scope to support compliance/debugging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Spoofing | /v1/crm/* without auth | mitigate | `Depends(require_paid_tier)` chains to `get_team_scope` which chains to `get_current_principal` — all 3 must pass. No endpoint bypasses this. |
| T-07-02-02 | Tampering | Pydantic body validation | mitigate | All bodies use `ConfigDict(extra="forbid")` + Field constraints (pattern, max_length). FastAPI rejects malformed JSON with 422 before reaching handler. |
| T-07-02-03 | IDOR | GET/PATCH/DELETE /crm/contacts/{id} | mitigate | Every query includes `WHERE id = :id AND team_scope = :ts` — a user from Team A querying a Team B contact ID returns 404, not 403 (no info leak about existence). |
| T-07-02-04 | SQL Injection | sa.text + dynamic SET clause in PATCH | mitigate | Dynamic `set_clause` is built from `body.model_dump(exclude_unset=True)` keys — keys are bound to Pydantic model field names (already validated). Values bound via params dict. Never concatenates user input into SQL. |
| T-07-02-05 | Elevation of Privilege | Starter team accessing CRM | mitigate | `require_paid_tier` checks `teams.plan != 'starter'` — non-bypassable, applied as Depends() on every endpoint. ASVS L1 §V8.1.2 (access control enforced server-side). |
| T-07-02-06 | Repudiation | Mutations without audit | mitigate | Every POST/PATCH/DELETE calls `await write_audit(...)` BEFORE `session.commit()` — atomicity guaranteed (audit + mutation in same transaction). |
| T-07-02-07 | Information Disclosure | Listing all contacts in team | accept | Listing within own team is by design (D1). team_scope filter enforces isolation. No PII leakage cross-team. |
| T-07-02-08 | Denial of Service | Unbounded list query | mitigate | `limit: int = Query(default=50, ge=1, le=200)` caps response size. `offset` paginated. |
| T-07-02-09 | Tampering | Direct truth_level downgrade via PATCH | accept | v1 allows admin to PATCH truth_level freely on contacts. The truth-level workflow (TRUTH-01..09) applies to memory_items, not contacts. Future RBAC on contacts is in Deferred Ideas. |
</threat_model>

<verification>
- `curl -X GET https://api.dejavu.cat/v1/crm/contacts -H "X-Team-Scope: starter-team" -H "Authorization: Bearer ..."` retourne 403 avec message "CRM and task tracking require a Team or Enterprise plan"
- Avec une team `team` ou `enterprise` : `curl -X POST /v1/crm/contacts -d '{"contact_type":"direct","email":"alice@co.com","full_name":"Alice","source":"manual"}'` retourne 201 + ContactOut
- POST le même email deux fois : la deuxième renvoie l'ID existant avec `interaction_count=2`
- `SELECT action FROM audit_log WHERE action LIKE 'crm.contact.%' LIMIT 5` retourne les actions logguées
- `curl -X DELETE /v1/crm/contacts/{wrong-team-id}` retourne 404 (pas 403, pour ne pas leaker l'existence)
- pytest unit: `cd apps/memory-api && pytest tests/test_crm.py -x` (à créer si fichier de tests existe — sinon skip)
</verification>

<success_criteria>
- 3 fichiers modifiés, tous se compilent sans ImportError
- Router CRM exposé sous `/v1/crm/contacts` avec 5 méthodes HTTP
- `require_paid_tier` rejette plan='starter' avec 403
- Toutes les mutations sont audit-loggées avec action `crm.contact.*`
- Aucun endpoint CRM ne contourne `require_paid_tier`
</success_criteria>

<output>
After completion, create `.planning/phases/07-crm-granola-tasks/07-02-SUMMARY.md`
</output>
