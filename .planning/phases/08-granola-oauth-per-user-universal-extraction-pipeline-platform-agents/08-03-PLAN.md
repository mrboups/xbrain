---
phase: 08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/memory-api/app/routes/me.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Un user authentifié (kind=user) peut POST sa clé Granola via POST /v1/me/granola-key — la clé est chiffrée Fernet avant stockage"
    - "Un user authentifié peut GET /v1/me/granola-key qui retourne {connected: bool, last_polled_at}, JAMAIS la clé en clair"
    - "Un user authentifié peut DELETE /v1/me/granola-key — soft delete via enabled=false"
    - "Tous les endpoints retournent 403 pour les bridge JWTs (kind!='user')"
    - "L'UPSERT ON CONFLICT(user_id) garantit qu'un user a au plus une connexion active"
  artifacts:
    - path: "apps/memory-api/app/routes/me.py"
      provides: "Endpoints user-level pour gérer la clé Granola personnelle"
      contains: "/me/granola-key"
  key_links:
    - from: "POST /v1/me/granola-key"
      to: "Fernet encryption"
      via: "_require_granola_fernet() reused from granola_integration.py"
      pattern: "_require_granola_fernet"
    - from: "POST /v1/me/granola-key"
      to: "granola_user_connections table"
      via: "INSERT ... ON CONFLICT (user_id) DO UPDATE"
      pattern: "ON CONFLICT \\(user_id\\) DO UPDATE"
---

<objective>
Étendre `apps/memory-api/app/routes/me.py` avec trois endpoints user-facing pour gérer la clé API Granola personnelle :
1. `POST /v1/me/granola-key` — body `{api_key, team_scope}`, chiffre Fernet, UPSERT
2. `GET /v1/me/granola-key` — retourne `{connected, last_polled_at, team_scope}`, JAMAIS la clé
3. `DELETE /v1/me/granola-key` — soft delete (`enabled=false`)

Tous accessibles UNIQUEMENT aux principals `kind='user'` (pas aux bridge — un service interne ne devrait pas s'enregistrer une clé Granola).

Purpose: Implémente D1 — l'utilisateur saisit sa clé Granola dans son onboarding (plan 08-07) qui appelle ces endpoints. La clé est ensuite consommée par la nouvelle boucle granola-sync per-user (plan 08-04).

Output: 3 endpoints ajoutés à `me.py`, sans toucher à l'endpoint `/me` existant.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-CONTEXT.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-RESEARCH.md

@apps/memory-api/app/routes/me.py
@apps/memory-api/app/routes/granola_integration.py
@apps/memory-api/app/audit.py
@apps/memory-api/app/deps.py

<interfaces>
<!-- Pattern Fernet helper à réutiliser via import. -->

From apps/memory-api/app/routes/granola_integration.py:
```python
from cryptography.fernet import Fernet, InvalidToken

def _require_granola_fernet() -> Fernet:
    """Return a Fernet instance using FERNET_KEY, falling back to OAUTH_CREDENTIALS_ENCRYPTION_KEY."""
    key = settings.FERNET_KEY or settings.OAUTH_CREDENTIALS_ENCRYPTION_KEY
    if not key:
        raise HTTPException(500, "FERNET_KEY (or OAUTH_CREDENTIALS_ENCRYPTION_KEY) not configured")
    return Fernet(key.encode())
```

From apps/memory-api/app/routes/me.py (état actuel):
```python
from typing import Any
from fastapi import APIRouter, Depends
from app.deps import get_current_principal

router = APIRouter()

@router.get("/me")
async def me(principal: dict[str, Any] = Depends(get_current_principal)) -> dict[str, Any]:
    if principal["kind"] == "user":
        u = principal["user"]
        return {
            "kind": "user",
            "id": str(u.id),
            "source_user_id": u.source_user_id,
            "email": u.email,
            "display_name": u.display_name,
        }
    return {
        "kind": "bridge",
        "sub": principal.get("sub"),
        "team_scope": principal.get("team_scope"),
        "iss": principal["claims"].get("iss"),
    }
```

From apps/memory-api/alembic/versions/0012_granola_user_agents.py (table schema):
```
granola_user_connections columns:
  id UUID PK, user_id UUID FK CASCADE, team_scope VARCHAR(64),
  api_key_enc TEXT, last_polled_at TIMESTAMPTZ NULL, enabled BOOLEAN DEFAULT true,
  created_at, updated_at
  UNIQUE(user_id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les 3 endpoints granola-key à me.py (POST/GET/DELETE)</name>
  <files>apps/memory-api/app/routes/me.py</files>
  <read_first>
    - apps/memory-api/app/routes/me.py (état actuel — ne pas casser l'endpoint /me existant)
    - apps/memory-api/app/routes/granola_integration.py (pattern complet : _require_granola_fernet helper, write_audit, Pydantic models)
    - apps/memory-api/app/audit.py (signature write_audit)
    - apps/memory-api/alembic/versions/0012_granola_user_agents.py (colonnes exactes de granola_user_connections)
  </read_first>
  <action>
Réécrire entièrement `apps/memory-api/app/routes/me.py` avec ce contenu (l'endpoint `/me` existant est conservé tel quel) :

```python
"""/v1/me — current authenticated principal + per-user Granola API key management (D1 Phase 8)."""

from datetime import datetime
from typing import Any

import sqlalchemy as sa
import structlog
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel, ConfigDict, Field
from sqlalchemy.ext.asyncio import AsyncSession

from app.audit import write_audit
from app.deps import get_current_principal, get_session
from app.routes.granola_integration import _require_granola_fernet

log = structlog.get_logger(__name__)
router = APIRouter()


# ── /v1/me — existing endpoint (unchanged) ──────────────────────────────────


@router.get("/me")
async def me(principal: dict[str, Any] = Depends(get_current_principal)) -> dict[str, Any]:
    if principal["kind"] == "user":
        u = principal["user"]
        return {
            "kind": "user",
            "id": str(u.id),
            "source_user_id": u.source_user_id,
            "email": u.email,
            "display_name": u.display_name,
        }
    return {
        "kind": "bridge",
        "sub": principal.get("sub"),
        "team_scope": principal.get("team_scope"),
        "iss": principal["claims"].get("iss"),
    }


# ── /v1/me/granola-key — D1 Phase 8 ────────────────────────────────────────


class GranolaKeyBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    api_key: str = Field(..., min_length=8, max_length=512)
    team_scope: str = Field(..., min_length=1, max_length=64)


class GranolaKeyStatus(BaseModel):
    connected: bool
    team_scope: str | None
    last_polled_at: datetime | None


def _require_user(principal: dict[str, Any]) -> Any:
    """Reject bridge JWTs — Granola key belongs to a real user."""
    if principal.get("kind") != "user":
        raise HTTPException(403, "User authentication required")
    user = principal.get("user")
    if user is None:
        raise HTTPException(403, "User authentication required")
    return user


@router.post("/me/granola-key", response_model=GranolaKeyStatus, status_code=201)
async def set_my_granola_key(
    body: GranolaKeyBody,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """Store/replace the user's personal Granola API key (Fernet-encrypted at rest).

    UPSERT on user_id — one key per user. If the user re-submits, the previous
    encrypted ciphertext is replaced atomically. Re-enables the connection if it
    was previously soft-disabled via DELETE.

    Auth: user JWT only — bridge service principals are rejected.
    """
    user = _require_user(principal)
    fernet = _require_granola_fernet()
    encrypted = fernet.encrypt(body.api_key.encode()).decode()

    row = (await session.execute(sa.text("""
        INSERT INTO granola_user_connections (user_id, team_scope, api_key_enc, enabled)
        VALUES (:uid, :ts, :key, true)
        ON CONFLICT (user_id) DO UPDATE
        SET api_key_enc = EXCLUDED.api_key_enc,
            team_scope = EXCLUDED.team_scope,
            enabled = true,
            updated_at = now()
        RETURNING team_scope, last_polled_at
    """), {
        "uid": str(user.id),
        "ts": body.team_scope,
        "key": encrypted,
    })).mappings().fetchone()

    await write_audit(
        session,
        actor_user_id=user.id,
        team_scope=body.team_scope,
        action="me.granola_key.upserted",
        target_id=str(user.id),
        payload={"team_scope": body.team_scope},
    )
    await session.commit()

    log.info("me.granola_key.upserted", user_id=str(user.id), team_scope=body.team_scope)
    return GranolaKeyStatus(
        connected=True,
        team_scope=row["team_scope"],
        last_polled_at=row["last_polled_at"],
    )


@router.get("/me/granola-key", response_model=GranolaKeyStatus)
async def get_my_granola_key_status(
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """Return the user's Granola connection status. Never returns the plaintext key.

    Auth: user JWT only.
    """
    user = _require_user(principal)
    row = (await session.execute(sa.text("""
        SELECT team_scope, last_polled_at, enabled
        FROM granola_user_connections
        WHERE user_id = :uid
        LIMIT 1
    """), {"uid": str(user.id)})).mappings().fetchone()

    if row is None or not row["enabled"]:
        return GranolaKeyStatus(connected=False, team_scope=None, last_polled_at=None)
    return GranolaKeyStatus(
        connected=True,
        team_scope=row["team_scope"],
        last_polled_at=row["last_polled_at"],
    )


@router.delete("/me/granola-key", status_code=204)
async def delete_my_granola_key(
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """Soft-delete the user's Granola connection (enabled=false).

    The encrypted key remains stored but the poller skips it (filter enabled=true
    in 08-04). A subsequent POST re-enables the connection with a new key.

    Auth: user JWT only.
    """
    user = _require_user(principal)
    row = (await session.execute(sa.text("""
        UPDATE granola_user_connections
        SET enabled = false, updated_at = now()
        WHERE user_id = :uid AND enabled = true
        RETURNING team_scope
    """), {"uid": str(user.id)})).fetchone()

    if row is None:
        # Nothing to delete — return 204 idempotently
        return None

    await write_audit(
        session,
        actor_user_id=user.id,
        team_scope=row.team_scope,
        action="me.granola_key.disabled",
        target_id=str(user.id),
        payload={},
    )
    await session.commit()
    log.info("me.granola_key.disabled", user_id=str(user.id))
    return None
```

Notes implémentation :
- `_require_user()` est un guard local qui rejette explicitement `kind='bridge'` — un service interne n'a pas de clé Granola personnelle (aussi protège contre une mauvaise utilisation du bridge JWT).
- `_require_granola_fernet` est importé depuis `granola_integration.py` (DRY — single source de la helper Fernet).
- L'UPSERT utilise `ON CONFLICT (user_id) DO UPDATE` — la contrainte UNIQUE(user_id) du plan 08-01 rend ce UPSERT possible.
- Soft delete (`enabled=false`) plutôt que hard DELETE — permet une réactivation par POST sans perdre l'audit trail.
- DELETE est idempotent : 204 même si rien à désactiver (pas de 404 sur "déjà déconnecté").
- GET retourne `connected=False` (pas 404) si pas de connexion ou enabled=false — friendly pour le UI onboarding qui veut juste savoir l'état.
- JAMAIS de plaintext key dans une réponse GET (T-08-03-02 mitigated by design — l'endpoint ne SELECT même pas `api_key_enc`).
- L'`audit_log` est écrit pour POST et DELETE (pas GET — lecture non-sensible).
- Le router agents enregistré en plan 08-02 est indépendant de me.py — pas de couplage à craindre.

ATTENTION : NE PAS TOUCHER au router include dans main.py — `me.router` est déjà enregistré (vérifié dans main.py existant). Les nouveaux endpoints sous `/me/granola-key` sont automatiquement exposés au prochain restart.
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/memory-api/app/routes/me.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@router.get(\"/me\")" apps/memory-api/app/routes/me.py` retourne 1 (endpoint existant conservé).
    - `grep -c "@router.post(\"/me/granola-key\"" apps/memory-api/app/routes/me.py` retourne 1.
    - `grep -c "@router.get(\"/me/granola-key\"" apps/memory-api/app/routes/me.py` retourne 1.
    - `grep -c "@router.delete(\"/me/granola-key\"" apps/memory-api/app/routes/me.py` retourne 1.
    - `grep -c "_require_granola_fernet" apps/memory-api/app/routes/me.py` retourne au moins 2 (1 import + 1 usage).
    - `grep -c "fernet.encrypt" apps/memory-api/app/routes/me.py` retourne 1.
    - `grep -c "ON CONFLICT (user_id) DO UPDATE" apps/memory-api/app/routes/me.py` retourne 1.
    - `grep -c "principal.get(\"kind\") != \"user\"" apps/memory-api/app/routes/me.py` retourne au least 1 (guard bridge rejection).
    - `grep -v '^#' apps/memory-api/app/routes/me.py | grep -c "api_key_enc" | grep -q -v "^0$"` — il y a au moins une référence (dans le UPSERT) MAIS pas dans une SELECT response (vérifier qu'aucun endpoint GET ne fait `SELECT api_key_enc`).
    - `grep -c "SELECT.*api_key_enc" apps/memory-api/app/routes/me.py` retourne 0 (la GET status ne SELECT pas la clé chiffrée — T-08-03-02).
    - Le fichier parse comme Python valide.
  </acceptance_criteria>
  <done>3 endpoints granola-key fonctionnels, l'endpoint /me original intact, prêts pour exposition au prochain restart memory-api.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External user (LibreChat onboarding) → /v1/me/granola-key | User JWT (Google OIDC ou librechat-onboarding bridge issuer) — kind=user enforced |
| memory-api → PostgreSQL | INSERT/UPDATE granola_user_connections.api_key_enc (already Fernet-ciphered) |
| Bridge service → /v1/me/granola-key | REJECTED par _require_user — un service ne possède pas de clé Granola personnelle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Spoofing | /v1/me/granola-key | mitigate | _require_user() rejette kind=bridge avec 403 — un attacker doit posséder un user JWT valide. |
| T-08-03-02 | Information Disclosure | api_key_enc en GET response | mitigate | GET response model `GranolaKeyStatus` ne contient PAS `api_key_enc`. La query SELECT ne récupère même pas la colonne (verify via grep). |
| T-08-03-03 | Information Disclosure | api_key en log | mitigate | log.info ne logge JAMAIS body.api_key — uniquement user_id et team_scope. Le ciphertext est stocké en colonne mais pas loggé. |
| T-08-03-04 | Tampering | UPSERT cross-user | mitigate | WHERE user_id = principal.user.id — impossible de modifier la connexion d'un autre user. ON CONFLICT (user_id) implique l'unicité. |
| T-08-03-05 | Repudiation | Connect/disconnect actions | mitigate | write_audit appelé sur POST (`me.granola_key.upserted`) et DELETE (`me.granola_key.disabled`). |
| T-08-03-06 | Denial of Service | Spam POST | accept | Pas de rate-limit côté endpoint. Un user spam POST n'a aucun impact (UPSERT atomique). Phase 9 pourra ajouter rate-limit global. |
| T-08-03-07 | Tampering | api_key_enc decryption error | mitigate | Le decrypt côté granola-sync (plan 08-04) gère InvalidToken — si la FERNET_KEY change, la clé devient illisible et le poller skip silencieusement avec log.error. |
</threat_model>

<verification>
- `me.py` parse comme Python valide
- 3 nouveaux endpoints (POST/GET/DELETE /me/granola-key) présents
- `_require_user()` est appelé sur les 3 endpoints
- Aucun endpoint ne fait `SELECT api_key_enc`
- L'endpoint `/me` existant n'est pas cassé (toujours présent et inchangé fonctionnellement)
</verification>

<success_criteria>
- [ ] `apps/memory-api/app/routes/me.py` étendu avec POST/GET/DELETE /me/granola-key
- [ ] L'endpoint `/me` original reste fonctionnellement identique (mêmes champs response)
- [ ] _require_granola_fernet importé depuis `granola_integration.py` (DRY)
- [ ] UPSERT ON CONFLICT (user_id) DO UPDATE implémenté
- [ ] GET response ne contient JAMAIS la clé en clair
- [ ] DELETE est idempotent (soft delete via enabled=false)
- [ ] Bridge JWTs rejetés avec 403 sur les 3 endpoints granola-key
- [ ] Au prochain restart memory-api, `curl -i https://api.grooveos.app/v1/me/granola-key` retourne 401/403/422 (route enregistrée)
</success_criteria>

<output>
After completion, create `.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-03-SUMMARY.md` documenting:
- Endpoints créés (POST/GET/DELETE /me/granola-key)
- Pattern Fernet réutilisé via import (pas duplication)
- Soft delete vs hard delete decision
- Comment le bridge est rejeté (kind=bridge → 403)
</output>
