---
phase: 08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/memory-api/app/routes/agents.py
  - apps/memory-api/app/main.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Un admin peut CRUD les agents via POST/GET/PATCH/DELETE /v1/admin/agents"
    - "Un user authentifié (kind=user) ou un service bridge peut invoquer un agent enabled via POST /v1/agents/{id}/invoke"
    - "L'invocation appelle Anthropic synchrone et retourne {memory_item_id, recap}"
    - "Le recap généré est stocké dans memory_items avec source='agent', truth_level='WORKING' et metadata.agent_id"
    - "tools_json est stocké mais NON passé à Anthropic en Phase 8 (Pitfall 6)"
  artifacts:
    - path: "apps/memory-api/app/routes/agents.py"
      provides: "Router CRUD admin + invoke endpoint pour agent_definitions"
      exports: ["router"]
    - path: "apps/memory-api/app/main.py"
      provides: "Registration du router agents avec prefix /v1"
      contains: "agents.router"
  key_links:
    - from: "agents.py invoke handler"
      to: "Anthropic API"
      via: "_get_anthropic() singleton + client.messages.create"
      pattern: "client\\.messages\\.create"
    - from: "agents.py invoke handler"
      to: "memory_items table"
      via: "INSERT memory_item with source='agent', truth_level='WORKING'"
      pattern: "INSERT INTO memory_items"
    - from: "main.py"
      to: "agents router"
      via: "app.include_router(agents.router, prefix='/v1', tags=['agents'])"
      pattern: "include_router\\(agents\\.router"
---

<objective>
Créer le router FastAPI `apps/memory-api/app/routes/agents.py` qui expose :
1. `POST /v1/admin/agents` (admin only) — créer un agent
2. `GET /v1/admin/agents` (admin only) — lister
3. `PATCH /v1/admin/agents/{id}` (admin only) — éditer
4. `DELETE /v1/admin/agents/{id}` (admin only) — supprimer (hard delete)
5. `POST /v1/agents/{id}/invoke` (user OR bridge) — invoquer Claude synchronously, stocker le recap

Enregistrer le router dans `main.py`.

Purpose: Implémente D4 (CRUD admin + invoke synchrone via Anthropic direct depuis memory-api) et D5 (l'agent meeting-recap pourra être invoqué par granola-sync via bridge JWT après chaque ingest).

Output: 5 endpoints fonctionnels, gates auth correctes, tools_json stocké mais NON exécuté (Pitfall 6 RESEARCH.md).
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-CONTEXT.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-RESEARCH.md

@apps/memory-api/app/routes/granola_integration.py
@apps/memory-api/app/routes/admin_projects.py
@apps/memory-api/app/routes/memory.py
@apps/memory-api/app/deps.py
@apps/memory-api/app/main.py
@apps/memory-api/app/audit.py

<interfaces>
<!-- Patterns établis à reproduire — extraits du codebase. -->

From apps/memory-api/app/deps.py:
```python
def _is_admin(principal: dict[str, Any]) -> bool:
    """Bridge JWTs sont admin. Les users doivent être dans ADMIN_USER_SUBS."""
    if principal.get("kind") in ("service", "bridge"):
        return True
    sub = principal.get("sub", "")
    admin_subs = [s.strip() for s in (settings.ADMIN_USER_SUBS or "").split(",") if s.strip()]
    return sub in admin_subs

async def get_current_principal(authorization: str = Header(...)) -> dict[str, Any]: ...
async def get_session() -> AsyncGenerator[AsyncSession, None]: ...
```

From apps/memory-api/app/routes/memory.py (Anthropic client singleton — à dupliquer dans agents.py OU à réutiliser via import):
```python
from anthropic import AsyncAnthropic
_anthropic_client: AsyncAnthropic | None = None

def _get_anthropic() -> AsyncAnthropic | None:
    global _anthropic_client
    if _anthropic_client is not None:
        return _anthropic_client
    if not settings.ANTHROPIC_API_KEY:
        return None
    _anthropic_client = AsyncAnthropic(api_key=settings.ANTHROPIC_API_KEY)
    return _anthropic_client
```

From apps/memory-api/app/routes/granola_integration.py (memory_item insertion pattern with 7-field tagging contract):
```python
await session.execute(sa.text("""
    INSERT INTO memory_items (
        id, team_scope, project_scope, content, source, source_ref,
        truth_level, confidence, visibility, validation_status, metadata
    ) VALUES (
        :id, :ts, :ps, :content, 'granola', :sref,
        'WORKING', 0.9, 'team', 'pending', :meta::jsonb
    )
"""), {...})
```

From apps/memory-api/app/audit.py:
```python
async def write_audit(session, *, actor_user_id, team_scope, action, target_id, payload): ...
```

From apps/memory-api/app/main.py (router registration pattern):
```python
from app.routes import (..., granola_integration, ..., me, ...)
app.include_router(granola_integration.router, prefix="/v1", tags=["granola"])
app.include_router(me.router, prefix="/v1", tags=["me"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer le router agents.py avec 5 endpoints (CRUD admin + invoke)</name>
  <files>apps/memory-api/app/routes/agents.py</files>
  <read_first>
    - apps/memory-api/app/routes/granola_integration.py (pattern complet : Pydantic models, _is_admin guard, INSERT memory_items, write_audit, ON CONFLICT upsert)
    - apps/memory-api/app/routes/admin_projects.py (pattern admin CRUD avec _is_admin)
    - apps/memory-api/app/routes/memory.py (pattern _get_anthropic singleton + client.messages.create + asyncio.create_task)
    - apps/memory-api/app/deps.py (signatures get_current_principal, get_session, _is_admin)
    - apps/memory-api/app/audit.py (signature write_audit)
    - apps/memory-api/alembic/versions/0012_granola_user_agents.py (colonnes exactes à manipuler dans les CRUD)
  </read_first>
  <action>
Créer `apps/memory-api/app/routes/agents.py` avec ce contenu exact :

```python
"""/v1/admin/agents (CRUD) + /v1/agents/{id}/invoke — Phase 8 D4 platform agents registry.

Endpoints:
  - POST /v1/admin/agents          (admin)   — create agent
  - GET  /v1/admin/agents          (admin)   — list agents
  - PATCH /v1/admin/agents/{id}    (admin)   — update agent
  - DELETE /v1/admin/agents/{id}   (admin)   — hard delete
  - POST /v1/agents/{id}/invoke    (user|bridge) — invoke Claude synchronously, store recap

Notes:
  - tools_json est stocké mais NON passé à Anthropic en Phase 8 (Pitfall 6 RESEARCH.md).
    Phase 9+ ajoutera l'exécution réelle via mcp-gateway.
  - Le recap est stocké dans memory_items (source='agent', truth_level='WORKING').
  - L'invocation accepte user OU bridge — granola-sync (bridge) auto-déclenche meeting-recap (D5).
"""

import json
from datetime import datetime
from typing import Any
from uuid import UUID, uuid4

import sqlalchemy as sa
import structlog
from anthropic import AsyncAnthropic
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel, ConfigDict, Field
from sqlalchemy.ext.asyncio import AsyncSession

from app.audit import write_audit
from app.config import settings
from app.deps import _is_admin, get_current_principal, get_session

log = structlog.get_logger(__name__)
router = APIRouter()


# ── Anthropic client (singleton, lazy-init — pattern memory.py) ─────────────

_anthropic_client: AsyncAnthropic | None = None


def _get_anthropic() -> AsyncAnthropic | None:
    global _anthropic_client
    if _anthropic_client is not None:
        return _anthropic_client
    if not settings.ANTHROPIC_API_KEY:
        return None
    _anthropic_client = AsyncAnthropic(api_key=settings.ANTHROPIC_API_KEY)
    return _anthropic_client


# ── Pydantic models ─────────────────────────────────────────────────────────


class AgentCreateBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str = Field(..., min_length=1, max_length=128)
    description: str | None = Field(None, max_length=4000)
    system_prompt: str = Field(..., min_length=1, max_length=20000)
    model: str = Field(default="claude-haiku-4-5-20251001", min_length=1, max_length=128)
    tools_json: list[dict[str, Any]] | None = None
    enabled: bool = True
    auto_trigger: bool = True


class AgentPatchBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    description: str | None = Field(None, max_length=4000)
    system_prompt: str | None = Field(None, min_length=1, max_length=20000)
    model: str | None = Field(None, min_length=1, max_length=128)
    tools_json: list[dict[str, Any]] | None = None
    enabled: bool | None = None
    auto_trigger: bool | None = None


class AgentOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: UUID
    name: str
    description: str | None
    system_prompt: str
    model: str
    tools_json: list[dict[str, Any]] | None
    enabled: bool
    auto_trigger: bool
    created_at: datetime
    updated_at: datetime


class AgentInvokeBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    content: str = Field(..., min_length=1, max_length=200000)
    team_scope: str = Field(..., min_length=1, max_length=64)
    source_ref: str | None = Field(None, max_length=256)
    project_scope: str | None = Field(None, max_length=64)


class AgentInvokeOut(BaseModel):
    memory_item_id: UUID
    recap: str
    agent_name: str


# ═══════════════════════════════════════════════════════════════════════════
# Admin CRUD
# ═══════════════════════════════════════════════════════════════════════════


@router.post("/admin/agents", response_model=AgentOut, status_code=201)
async def create_agent(
    body: AgentCreateBody,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """Create a platform agent. Admin only."""
    if not _is_admin(principal):
        raise HTTPException(403, "Admin access required")

    user = principal.get("user")
    created_by = user.id if user else None
    tools_payload = json.dumps(body.tools_json) if body.tools_json is not None else None

    try:
        row = (await session.execute(sa.text("""
            INSERT INTO agent_definitions (
                name, description, system_prompt, model, tools_json,
                enabled, auto_trigger, created_by
            ) VALUES (
                :name, :desc, :prompt, :model, :tools::jsonb,
                :enabled, :auto, :cb
            )
            RETURNING id, name, description, system_prompt, model, tools_json,
                      enabled, auto_trigger, created_at, updated_at
        """), {
            "name": body.name,
            "desc": body.description,
            "prompt": body.system_prompt,
            "model": body.model,
            "tools": tools_payload,
            "enabled": body.enabled,
            "auto": body.auto_trigger,
            "cb": str(created_by) if created_by else None,
        })).mappings().fetchone()
    except sa.exc.IntegrityError as exc:
        # UNIQUE(name) violation
        raise HTTPException(409, f"Agent name '{body.name}' already exists") from exc

    await write_audit(
        session,
        actor_user_id=created_by,
        team_scope="_platform",
        action="agent.created",
        target_id=str(row["id"]),
        payload={"name": body.name, "model": body.model},
    )
    await session.commit()
    return AgentOut(**dict(row))


@router.get("/admin/agents", response_model=list[AgentOut])
async def list_agents(
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """List all agents (enabled and disabled). Admin only."""
    if not _is_admin(principal):
        raise HTTPException(403, "Admin access required")

    rows = (await session.execute(sa.text("""
        SELECT id, name, description, system_prompt, model, tools_json,
               enabled, auto_trigger, created_at, updated_at
        FROM agent_definitions
        ORDER BY name ASC
    """))).mappings().all()
    return [AgentOut(**dict(r)) for r in rows]


@router.patch("/admin/agents/{agent_id}", response_model=AgentOut)
async def update_agent(
    agent_id: UUID,
    body: AgentPatchBody,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """Patch an agent. Admin only. Only sent fields are updated."""
    if not _is_admin(principal):
        raise HTTPException(403, "Admin access required")

    fields: dict[str, Any] = {}
    sets: list[str] = []
    if body.description is not None:
        sets.append("description = :description")
        fields["description"] = body.description
    if body.system_prompt is not None:
        sets.append("system_prompt = :system_prompt")
        fields["system_prompt"] = body.system_prompt
    if body.model is not None:
        sets.append("model = :model")
        fields["model"] = body.model
    if body.tools_json is not None:
        sets.append("tools_json = :tools_json::jsonb")
        fields["tools_json"] = json.dumps(body.tools_json)
    if body.enabled is not None:
        sets.append("enabled = :enabled")
        fields["enabled"] = body.enabled
    if body.auto_trigger is not None:
        sets.append("auto_trigger = :auto_trigger")
        fields["auto_trigger"] = body.auto_trigger

    if not sets:
        raise HTTPException(400, "No fields provided")

    sets.append("updated_at = now()")
    fields["agent_id"] = str(agent_id)
    sql = f"""
        UPDATE agent_definitions SET {', '.join(sets)}
        WHERE id = :agent_id
        RETURNING id, name, description, system_prompt, model, tools_json,
                  enabled, auto_trigger, created_at, updated_at
    """
    row = (await session.execute(sa.text(sql), fields)).mappings().fetchone()
    if row is None:
        raise HTTPException(404, "Agent not found")

    user = principal.get("user")
    await write_audit(
        session,
        actor_user_id=user.id if user else None,
        team_scope="_platform",
        action="agent.updated",
        target_id=str(agent_id),
        payload={"changed_fields": list(fields.keys() - {"agent_id"})},
    )
    await session.commit()
    return AgentOut(**dict(row))


@router.delete("/admin/agents/{agent_id}", status_code=204)
async def delete_agent(
    agent_id: UUID,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """Hard-delete an agent. Admin only."""
    if not _is_admin(principal):
        raise HTTPException(403, "Admin access required")

    row = (await session.execute(
        sa.text("DELETE FROM agent_definitions WHERE id = :id RETURNING name"),
        {"id": str(agent_id)},
    )).fetchone()
    if row is None:
        raise HTTPException(404, "Agent not found")

    user = principal.get("user")
    await write_audit(
        session,
        actor_user_id=user.id if user else None,
        team_scope="_platform",
        action="agent.deleted",
        target_id=str(agent_id),
        payload={"name": row.name},
    )
    await session.commit()
    return None


# ═══════════════════════════════════════════════════════════════════════════
# Invoke (user OR bridge)
# ═══════════════════════════════════════════════════════════════════════════


@router.post("/agents/{agent_id}/invoke", response_model=AgentInvokeOut)
async def invoke_agent(
    agent_id: UUID,
    body: AgentInvokeBody,
    principal: dict[str, Any] = Depends(get_current_principal),
    session: AsyncSession = Depends(get_session),
):
    """Invoke an agent synchronously via Anthropic. Stores result as memory_item.

    Auth: user OR bridge (granola-sync uses bridge JWT to auto-trigger meeting-recap).
    Pitfall 1 RESEARCH.md: team_scope is read from body, NOT enforced by header.
    Pitfall 6 RESEARCH.md: tools_json is stored but NEVER passed to Anthropic in Phase 8.
    """
    # Bridge cross-team check (Pitfall 1) — if bridge JWT carries team_scope, must match body
    if principal.get("kind") == "bridge":
        bridge_team = principal.get("team_scope")
        if bridge_team and bridge_team != body.team_scope:
            raise HTTPException(
                403,
                f"Bridge JWT team_scope '{bridge_team}' does not match body team_scope '{body.team_scope}'",
            )

    # Fetch agent definition
    agent_row = (await session.execute(sa.text("""
        SELECT id, name, system_prompt, model, enabled
        FROM agent_definitions
        WHERE id = :id
    """), {"id": str(agent_id)})).mappings().fetchone()

    if agent_row is None:
        raise HTTPException(404, "Agent not found")
    if not agent_row["enabled"]:
        raise HTTPException(409, f"Agent '{agent_row['name']}' is disabled")

    # Anthropic call
    client = _get_anthropic()
    if client is None:
        raise HTTPException(503, "ANTHROPIC_API_KEY not configured")

    try:
        msg = await client.messages.create(
            model=agent_row["model"],
            max_tokens=4096,
            system=agent_row["system_prompt"],
            # tools_json NOT passed — Phase 8 stores but does not execute (Pitfall 6)
            messages=[{"role": "user", "content": body.content[:50000]}],
        )
    except Exception as exc:
        log.error("agent.invoke.anthropic_failed", agent_id=str(agent_id), error=str(exc))
        raise HTTPException(502, f"Anthropic call failed: {exc}") from exc

    recap_text = ""
    if msg.content:
        # Concatenate all TextBlock items (Anthropic SDK returns list of blocks)
        for block in msg.content:
            if hasattr(block, "text"):
                recap_text += block.text
    if not recap_text:
        recap_text = "(empty agent response)"

    # Store as memory_item (7-field tagging contract — D4 + project tagging contract)
    memory_item_id = uuid4()
    metadata_payload = {
        "agent_id": str(agent_id),
        "agent_name": agent_row["name"],
        "agent_model": agent_row["model"],
    }
    await session.execute(sa.text("""
        INSERT INTO memory_items (
            id, team_scope, project_scope, content, source, source_ref,
            truth_level, confidence, visibility, validation_status, metadata
        ) VALUES (
            :id, :ts, :ps, :content, 'agent', :sref,
            'WORKING', 0.85, 'team', 'pending', :meta::jsonb
        )
    """), {
        "id": str(memory_item_id),
        "ts": body.team_scope,
        "ps": body.project_scope,
        "content": recap_text,
        "sref": body.source_ref,
        "meta": json.dumps(metadata_payload),
    })

    user = principal.get("user")
    await write_audit(
        session,
        actor_user_id=user.id if user else None,
        team_scope=body.team_scope,
        action="agent.invoked",
        target_id=str(agent_id),
        payload={
            "agent_name": agent_row["name"],
            "memory_item_id": str(memory_item_id),
            "source_ref": body.source_ref,
        },
    )
    await session.commit()

    log.info(
        "agent.invoked",
        agent_id=str(agent_id),
        agent_name=agent_row["name"],
        team_scope=body.team_scope,
        memory_item_id=str(memory_item_id),
    )
    return AgentInvokeOut(
        memory_item_id=memory_item_id,
        recap=recap_text,
        agent_name=agent_row["name"],
    )
```

Notes implémentation :
- `tools_json` est stocké comme JSONB (`json.dumps(...)::jsonb`), accepté en input comme `list[dict]` côté Pydantic — JAMAIS passé à `client.messages.create()` (Pitfall 6).
- Le client Anthropic est un singleton local au module (pattern indépendant de `memory.py` pour éviter les couplages).
- L'invocation accepte aussi bien user que bridge — granola-sync poll-loop appelle ce endpoint avec bridge JWT (D5).
- Le `team_scope` vient du body, pas du header `X-Team-Scope` — choix Pitfall 1 option A (le bridge JWT n'a pas de team_scope fixe par invocation).
- `audit_log.team_scope='_platform'` pour les CRUD admin (les agents sont une ressource globale, pas team-scoped). Pour invoke, on log avec le `body.team_scope` réel.
- Hard delete (pas soft) : choix simple — un agent ne devrait pas être présent dans agent_definitions s'il n'est plus utilisé. La soft-disable se fait via `enabled=false` dans PATCH.
- Le model par défaut Pydantic est `claude-haiku-4-5-20251001` (cohérent avec le seed migration et avec le model existant du Phase 7 extractor.py). Si Anthropic renomme/déprécie, le PATCH endpoint permet la mise à jour.
- `recap_text` agrège tous les TextBlock retournés (l'API peut renvoyer plusieurs blocks).
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/memory-api/app/routes/agents.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `apps/memory-api/app/routes/agents.py` existe.
    - `grep -c "router = APIRouter()" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -c "@router.post(\"/admin/agents\"" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -c "@router.get(\"/admin/agents\"" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -c "@router.patch(\"/admin/agents/{agent_id}\"" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -c "@router.delete(\"/admin/agents/{agent_id}\"" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -c "@router.post(\"/agents/{agent_id}/invoke\"" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -c "_is_admin(principal)" apps/memory-api/app/routes/agents.py` retourne au moins 4 (4 admin endpoints).
    - `grep -c "client.messages.create" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -v '^#' apps/memory-api/app/routes/agents.py | grep -c "tools=" | grep -q "^0$"` PASS — pas de paramètre `tools=` passé à messages.create (Pitfall 6).
    - `grep -c "INSERT INTO memory_items" apps/memory-api/app/routes/agents.py` retourne 1.
    - `grep -c "'agent'" apps/memory-api/app/routes/agents.py` retourne au moins 1 (source='agent').
    - `grep -c "'WORKING'" apps/memory-api/app/routes/agents.py` retourne au moins 1 (truth_level).
    - Le fichier parse comme Python valide (AST).
  </acceptance_criteria>
  <done>Router agents.py complet avec 5 endpoints conformes à D4/D5/Pitfall 6, prêt à être enregistré dans main.py.</done>
</task>

<task type="auto">
  <name>Task 2: Enregistrer le router agents dans main.py</name>
  <files>apps/memory-api/app/main.py</files>
  <read_first>
    - apps/memory-api/app/main.py (état actuel — voir comment les autres routers sont importés et enregistrés)
    - apps/memory-api/app/routes/agents.py (le router créé en Task 1)
  </read_first>
  <action>
Modifier `apps/memory-api/app/main.py` :

1. Dans le bloc `from app.routes import (...)`, ajouter `agents,` dans l'import (en respectant l'ordre alphabétique — donc juste après `admin_projects,` et avant `audit,`). État cible :

```python
from app.routes import (
    admin_drive,
    admin_projects,
    agents,
    audit,
    conversations,
    crm,
    drive_webhook,
    granola_integration,
    graph,
    health,
    me,
    me_github,
    memory,
    messages,
    promotions,
    system_prompt,
    tasks,
    teams,
    waitlist,
)
```

2. Ajouter une ligne `app.include_router(agents.router, prefix="/v1", tags=["agents"])` à la fin du bloc des `app.include_router(...)` calls (juste avant ou après `waitlist.router` — peu importe, l'ordre n'affecte pas le routing FastAPI). État cible — ajouter cette ligne :

```python
app.include_router(agents.router, prefix="/v1", tags=["agents"])
```

à la fin, juste avant la fin du fichier (après `app.include_router(waitlist.router, ...)`).

NE PAS toucher au reste du fichier (lifespan, CORS middleware, autres routers).

Note : `agents.router` enregistre AUSSI `/v1/admin/agents/*` car le path dans le router est `/admin/agents` (le prefix `/v1` du include_router vient s'ajouter). On ne change PAS le prefix du router agents (laisser `/v1`, pas `/v1/admin`) — sinon les routes user-facing `/v1/agents/{id}/invoke` ne seraient pas correctement préfixées.
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/memory-api/app/main.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "    agents," apps/memory-api/app/main.py` retourne 1 (import présent dans le bloc multi-ligne).
    - `grep -c "app.include_router(agents.router" apps/memory-api/app/main.py` retourne 1.
    - `grep -c "prefix=\"/v1\"" apps/memory-api/app/main.py` augmente bien de 1 par rapport à l'état initial.
    - Le fichier `main.py` parse comme Python valide.
    - Aucune autre ligne du fichier n'est altérée (les autres `include_router` calls existants restent identiques).
  </acceptance_criteria>
  <done>Le router agents est enregistré et FastAPI exposera /v1/admin/agents/* et /v1/agents/{id}/invoke au prochain redémarrage memory-api.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External client (admin user / granola-sync bridge) → /v1/admin/agents | Admin guard via _is_admin() |
| External client (user / bridge) → /v1/agents/{id}/invoke | Auth required (user kind=user OR kind=bridge) |
| memory-api → Anthropic API | Outbound httpx via anthropic SDK avec ANTHROPIC_API_KEY |
| Invoke handler → memory_items table | INSERT atomic + audit_log dans une seule transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Spoofing | /v1/admin/agents | mitigate | _is_admin() rejette tout principal sans bridge JWT ni sub dans ADMIN_USER_SUBS — 403 retourné. |
| T-08-02-02 | Tampering | tools_json field | mitigate | Validation Pydantic `list[dict[str, Any]] | None` empêche l'injection de strings arbitraires; JSONB validation côté Postgres. tools_json JAMAIS passé à client.messages.create (Pitfall 6). |
| T-08-02-03 | Repudiation | agent CRUD operations | mitigate | write_audit appelé sur create/update/delete/invoke — actor_user_id capturé pour les users, NULL pour bridge. |
| T-08-02-04 | Information Disclosure | system_prompt en GET response | accept | system_prompt est exposé en GET /v1/admin/agents (admin only) — c'est nécessaire pour le UI d'édition admin. Pas de secret métier dans system_prompt par design. |
| T-08-02-05 | Denial of Service | Anthropic API rate limits | accept | Aucun rate-limit côté memory-api. Si Anthropic rate-limit, l'exception bubble en 502. Phase 9 pourra ajouter un quota par user/team. |
| T-08-02-06 | Elevation of Privilege | Cross-team via bridge JWT mismatch | mitigate | Pitfall 1 RESEARCH.md : si bridge JWT carries team_scope, must match body.team_scope (403 sinon). |
| T-08-02-07 | Information Disclosure | Recap content stored team-scoped | mitigate | INSERT memory_items avec body.team_scope — le recap est invisible pour les autres teams via team_scope filter (pattern Phase 1). |
| T-08-02-08 | Tampering | Anthropic SDK content extraction | mitigate | Itère `msg.content` avec `hasattr(block, "text")` — un block sans text est ignoré sans crash. Fallback "(empty agent response)" si tout vide. |
</threat_model>

<verification>
- Le fichier agents.py parse comme Python valide
- main.py importe et enregistre le router agents
- Tous les endpoints attendus sont présents (5)
- _is_admin() est appelé sur les 4 endpoints admin
- tools_json n'est PAS passé à client.messages.create (verify via grep -v de comments + grep tools=)
- Le memory_item inséré a source='agent' et truth_level='WORKING'
</verification>

<success_criteria>
- [ ] `apps/memory-api/app/routes/agents.py` créé avec 5 endpoints
- [ ] `_is_admin()` appelé sur les 4 endpoints admin (POST/GET/PATCH/DELETE)
- [ ] `/v1/agents/{id}/invoke` accepte user OU bridge (Pitfall 1 mitigé)
- [ ] `tools_json` stocké en JSONB MAIS jamais passé à Anthropic (Pitfall 6)
- [ ] Recap inséré dans memory_items avec source='agent', truth_level='WORKING'
- [ ] Audit log écrit pour create/update/delete/invoke
- [ ] Router enregistré dans `apps/memory-api/app/main.py`
- [ ] Au prochain restart memory-api, `curl -i https://api.example.com/v1/admin/agents` retourne 401/403/422 (route enregistrée)
</success_criteria>

<output>
After completion, create `.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-02-SUMMARY.md` documenting:
- Endpoints créés et leurs auth requirements
- Comment tools_json est traité (stocké, non exécuté)
- Pattern memory_item insertion (source='agent', truth_level='WORKING')
- Notes sur ANTHROPIC_API_KEY requirement (déjà configuré Phase 7)
</output>
