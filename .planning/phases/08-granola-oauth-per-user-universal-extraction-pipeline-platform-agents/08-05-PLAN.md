---
phase: 08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents
plan: 05
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - apps/memory-api/app/routes/github_repos.py
  - apps/memory-api/app/main.py
  - apps/memory-api/app/config.py
  - apps/librechat/patches/xbrain-routes.js
  - .env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "Un user authentifié (LibreChat session JWT) peut GET /api/xbrain/github-repos sur LibreChat → liste de ses repos privés + repos d'orgs liées"
    - "Un user authentifié peut GET https://api.example.com/v1/github/repos qui proxy avec son Bearer token vers /api/xbrain/github-repos"
    - "Si LIBRECHAT_INTERNAL_URL n'est pas configurée, /v1/github/repos retourne 503"
    - "Le scope OAuth GitHub stocké inclut 'repo' (les repos privés sont visibles)"
    - "Les repos d'un user A sont invisibles à un user B — chaque appel utilise le github_access_token du user authentifié (Bearer token transmis tel quel par le proxy)"
  artifacts:
    - path: "apps/librechat/patches/xbrain-routes.js"
      provides: "GET /api/xbrain/github-repos endpoint utilisant github_access_token de MongoDB"
      contains: "/api/xbrain/github-repos"
    - path: "apps/memory-api/app/routes/github_repos.py"
      provides: "GET /v1/github/repos qui proxy le Bearer token caller vers /api/xbrain/github-repos via LIBRECHAT_INTERNAL_URL"
      exports: ["router"]
    - path: "apps/memory-api/app/main.py"
      provides: "Router github_repos enregistré"
      contains: "github_repos.router"
    - path: "apps/memory-api/app/config.py"
      provides: "Setting LIBRECHAT_INTERNAL_URL"
      contains: "LIBRECHAT_INTERNAL_URL"
    - path: ".env.example"
      provides: "Documentation LIBRECHAT_INTERNAL_URL"
      contains: "LIBRECHAT_INTERNAL_URL"
  key_links:
    - from: "xbrain-routes.js /api/xbrain/github-repos"
      to: "MongoDB users.github_access_token"
      via: "mongoose.connection.collection('users').findOne"
      pattern: "github_access_token"
    - from: "xbrain-routes.js /api/xbrain/github-repos"
      to: "GitHub API /user/repos"
      via: "fetch with Authorization Bearer ghToken"
      pattern: "api.github.com/user/repos"
    - from: "memory-api /v1/github/repos"
      to: "LibreChat /api/xbrain/github-repos"
      via: "httpx proxy passing caller's Bearer token via LIBRECHAT_INTERNAL_URL"
      pattern: "LIBRECHAT_INTERNAL_URL"
---

<objective>
Implémenter le **success criterion 7 ROADMAP Phase 8** — Dashboard GitHub dynamique :

> "Un user authentifié voit ses repos privés + ceux de son org GitHub liée — le dashboard appelle `GET /v1/github/repos` (memory-api) qui utilise le `github_access_token` stocké en MongoDB ; le scope OAuth GitHub inclut `repo` ; les repos privés sont visibles uniquement par le user concerné"

Architecture choisie (proxy réel — pas de stub) :

1. **`apps/librechat/patches/xbrain-routes.js`** — ajouter `GET /api/xbrain/github-repos` qui lit `github_access_token` depuis MongoDB `users` collection et appelle `https://api.github.com/user/repos?visibility=all&affiliation=owner,collaborator,organization_member` (scope `repo` requis).

2. **`apps/memory-api/app/routes/github_repos.py`** — nouveau router exposant `GET /v1/github/repos`. **Implémente un vrai proxy** : récupère le header `Authorization: Bearer ...` du caller (LibreChat session JWT), forward vers `LIBRECHAT_INTERNAL_URL/api/xbrain/github-repos` avec ce même Bearer token, retourne la JSON de la réponse upstream. Si `LIBRECHAT_INTERNAL_URL` n'est pas configurée → 503.

3. **`apps/memory-api/app/main.py`** — enregistrer le router.

4. **`apps/memory-api/app/config.py`** — ajouter `LIBRECHAT_INTERNAL_URL: str = ""` au Settings.

5. **`.env.example`** — documenter `LIBRECHAT_INTERNAL_URL=http://librechat:3080` (URL interne Docker network).

Note : Le frontend `projects-dashboard/scripts/generate_dashboard.py` est un **build-time generator** appelé par GitHub Actions, pas un endpoint user-facing dynamique. Le ROADMAP criterion 7 demande un endpoint API dynamique — c'est exactement ce que `GET /v1/github/repos` fournit (proxy réel vers la source de vérité GitHub token = MongoDB LibreChat).

Output: 2 endpoints (LibreChat + memory-api proxy), router enregistré, verify test #7 passe (auth required, 200/401/403 — PAS 501).
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/librechat/patches/xbrain-routes.js
@apps/librechat/patches/socialLogin.js
@apps/memory-api/app/main.py
@apps/memory-api/app/deps.py
@apps/memory-api/app/config.py

<interfaces>
<!-- Patterns existants : github-orgs endpoint à dupliquer pour github-repos. -->

From apps/librechat/patches/xbrain-routes.js:
```javascript
app.get('/api/xbrain/github-orgs', requireJwtAuth, async (req, res) => {
  res.setHeader('Cache-Control', 'no-store');
  try {
    const mongoose = require('mongoose');
    const userId = req.user._id || req.user.id;
    const user = await mongoose.connection.collection('users').findOne({ _id: userId });
    const ghToken = user?.github_access_token;
    if (!ghToken) return res.json([]);
    const r = await fetch('https://api.github.com/user/orgs?per_page=100', {
      headers: {
        Authorization: `Bearer ${ghToken}`,
        Accept: 'application/vnd.github+json',
        'X-GitHub-Api-Version': '2022-11-28',
        'User-Agent': 'xbrain-librechat',
      },
    });
    if (!r.ok) return res.json([]);
    const orgs = await r.json();
    res.json(Array.isArray(orgs) ? orgs.map(o => ({ login: o.login, description: o.description || null })) : []);
  } catch (err) {
    console.warn('[xbrain] github-orgs error:', err?.message);
    res.json([]);
  }
});
```

From apps/librechat/patches/socialLogin.js (proves github_access_token is stored in MongoDB users collection at GitHub OAuth callback):
```javascript
function saveGithubToken(userId, token) {
  mongoose.connection.collection('users').updateOne(
    { _id: userId },
    { $set: { github_access_token: token } },
  ).catch(() => {});
}
```

From apps/memory-api/app/main.py — pattern d'enregistrement router :
```python
from app.routes import (..., me_github, ..., waitlist)
app.include_router(me_github.router, prefix="/v1", tags=["me"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter GET /api/xbrain/github-repos dans xbrain-routes.js (LibreChat patch)</name>
  <files>apps/librechat/patches/xbrain-routes.js</files>
  <read_first>
    - apps/librechat/patches/xbrain-routes.js (état actuel — pattern github-orgs à dupliquer)
    - apps/librechat/patches/socialLogin.js (confirme que github_access_token est dans collection users)
  </read_first>
  <action>
Modifier `apps/librechat/patches/xbrain-routes.js` pour ajouter le nouvel endpoint `GET /api/xbrain/github-repos` JUSTE APRÈS l'endpoint `github-orgs` existant et AVANT l'endpoint `token`.

État cible du fichier complet :

```javascript
/**
 * xbrain-routes.js — mounted on LibreChat's Express app at image build time.
 *
 * Exposes:
 *   GET /api/xbrain/token        — short-lived bridge JWT for memory-api
 *   GET /api/xbrain/github-orgs  — user's GitHub orgs (Phase 5)
 *   GET /api/xbrain/github-repos — user's GitHub repos incl. private (Phase 8 D7 ROADMAP criterion 7)
 *
 * Security: all endpoints require requireJwtAuth (LibreChat's session middleware).
 */

const jwt = require('jsonwebtoken');

module.exports = function mountXbrainRoutes(app) {
  const secret = process.env.BRIDGE_SHARED_SECRET;
  if (!secret) {
    console.warn('[xbrain] BRIDGE_SHARED_SECRET not set — /api/xbrain/token will return 503');
  }

  let requireJwtAuth;
  try {
    requireJwtAuth = require('~/middleware/requireJwtAuth');
  } catch {
    try {
      requireJwtAuth = require('./middleware/requireJwtAuth');
    } catch {
      console.warn('[xbrain] requireJwtAuth not found — /api/xbrain/token unprotected, returning 503');
      requireJwtAuth = (_req, _res, next) => next();
    }
  }

  app.get('/api/xbrain/github-orgs', requireJwtAuth, async (req, res) => {
    res.setHeader('Cache-Control', 'no-store');
    try {
      const mongoose = require('mongoose');
      const userId = req.user._id || req.user.id;
      const user = await mongoose.connection.collection('users').findOne({ _id: userId });
      const ghToken = user?.github_access_token;
      if (!ghToken) return res.json([]);
      const r = await fetch('https://api.github.com/user/orgs?per_page=100', {
        headers: {
          Authorization: `Bearer ${ghToken}`,
          Accept: 'application/vnd.github+json',
          'X-GitHub-Api-Version': '2022-11-28',
          'User-Agent': 'xbrain-librechat',
        },
      });
      if (!r.ok) return res.json([]);
      const orgs = await r.json();
      res.json(Array.isArray(orgs) ? orgs.map(o => ({ login: o.login, description: o.description || null })) : []);
    } catch (err) {
      console.warn('[xbrain] github-orgs error:', err?.message);
      res.json([]);
    }
  });

  // Phase 8 D7 — ROADMAP success criterion 7: dashboard GitHub dynamique
  // Returns the user's repos including private + org repos. Requires GitHub OAuth scope `repo`.
  // Each user only sees their own — the github_access_token is per-user (stored at GitHub login).
  app.get('/api/xbrain/github-repos', requireJwtAuth, async (req, res) => {
    res.setHeader('Cache-Control', 'no-store');
    try {
      const mongoose = require('mongoose');
      const userId = req.user._id || req.user.id;
      const user = await mongoose.connection.collection('users').findOne({ _id: userId });
      const ghToken = user?.github_access_token;
      if (!ghToken) {
        // No GitHub token linked — return empty list (not an error).
        return res.json({ repos: [], linked: false });
      }
      // affiliation=owner,collaborator,organization_member fetches all visible repos
      // visibility=all returns both public and private repos (requires scope=repo)
      const url = 'https://api.github.com/user/repos?per_page=100&sort=updated&visibility=all&affiliation=owner,collaborator,organization_member';
      const r = await fetch(url, {
        headers: {
          Authorization: `Bearer ${ghToken}`,
          Accept: 'application/vnd.github+json',
          'X-GitHub-Api-Version': '2022-11-28',
          'User-Agent': 'xbrain-librechat',
        },
      });
      if (!r.ok) {
        console.warn('[xbrain] github-repos upstream error:', r.status);
        return res.status(502).json({ repos: [], linked: true, error: 'github_api_error', status: r.status });
      }
      const repos = await r.json();
      const mapped = Array.isArray(repos)
        ? repos.map(repo => ({
            id: repo.id,
            name: repo.name,
            full_name: repo.full_name,
            owner_login: repo.owner?.login || null,
            private: !!repo.private,
            html_url: repo.html_url,
            description: repo.description || null,
            updated_at: repo.updated_at,
            language: repo.language || null,
            archived: !!repo.archived,
          }))
        : [];
      res.json({ repos: mapped, linked: true });
    } catch (err) {
      console.warn('[xbrain] github-repos error:', err?.message);
      res.status(500).json({ repos: [], linked: false, error: 'internal' });
    }
  });

  app.get('/api/xbrain/token', requireJwtAuth, (req, res) => {
    if (!secret) return res.status(503).json({ error: 'BRIDGE_SHARED_SECRET not configured' });

    const user = req.user;
    if (!user || !user.email) {
      return res.status(401).json({ error: 'no authenticated user' });
    }

    const now = Math.floor(Date.now() / 1000);
    const githubId = user.githubId || user.github_id || null;
    const token = jwt.sign(
      {
        iss: 'librechat-onboarding',
        sub: String(user._id || user.id),
        email: user.email,
        ...(githubId ? { github_id: String(githubId) } : {}),
        scope: 'bridge',
        iat: now,
        exp: now + 300,
      },
      secret,
      { algorithm: 'HS256' },
    );

    res.json({ token });
  });
};
```

Notes implémentation :
- `affiliation=owner,collaborator,organization_member` + `visibility=all` retourne TOUS les repos visibles par le token (privés inclus), à condition que le scope OAuth `repo` ait été demandé au login GitHub LibreChat.
- Réponse normalisée `{ repos: [...], linked: bool }` plutôt que tableau brut — différencie "user n'a pas linké GitHub" (linked=false) de "linké mais pas de repos" (linked=true, repos=[]).
- `Cache-Control: no-store` — pas de cache car les repos privés ne doivent pas leak entre sessions.
- Pas de pagination en Phase 8 — `per_page=100` cap.
- `err?.message` dans console.warn (jamais le token, jamais le user payload complet).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('apps/librechat/patches/xbrain-routes.js', 'utf8'); new Function(src); console.log('parsed_ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/xbrain/github-orgs" apps/librechat/patches/xbrain-routes.js` retourne 1 (existant conservé).
    - `grep -c "/api/xbrain/github-repos" apps/librechat/patches/xbrain-routes.js` retourne au moins 1 (nouveau).
    - `grep -c "/api/xbrain/token" apps/librechat/patches/xbrain-routes.js` retourne 1 (existant conservé).
    - `grep -c "github_access_token" apps/librechat/patches/xbrain-routes.js` retourne au moins 2 (lecture dans github-orgs + github-repos).
    - `grep -c "api.github.com/user/repos" apps/librechat/patches/xbrain-routes.js` retourne 1.
    - `grep -c "visibility=all" apps/librechat/patches/xbrain-routes.js` retourne 1 (private repos via scope=repo).
    - `grep -c "requireJwtAuth" apps/librechat/patches/xbrain-routes.js` retourne au moins 4 (1 import-block + 3 endpoint guards).
    - Le fichier est un module Node.js valide (syntaxiquement parseable via `new Function(src)`).
  </acceptance_criteria>
  <done>L'endpoint /api/xbrain/github-repos est disponible dans LibreChat après rebuild de l'image — appelé directement par les UIs OU via le proxy memory-api.</done>
</task>

<task type="auto">
  <name>Task 2: Créer github_repos.py (proxy réel) + Settings.LIBRECHAT_INTERNAL_URL + main.py + .env.example</name>
  <files>apps/memory-api/app/routes/github_repos.py, apps/memory-api/app/main.py, apps/memory-api/app/config.py, .env.example</files>
  <read_first>
    - apps/memory-api/app/routes/me_github.py (pattern user JWT guard, httpx call)
    - apps/memory-api/app/deps.py (get_current_principal signature)
    - apps/memory-api/app/main.py (router registration pattern)
    - apps/memory-api/app/config.py (vérifier les settings disponibles, pattern Pydantic Settings)
    - .env.example (où ajouter LIBRECHAT_INTERNAL_URL — section Phase 8 ou bloc dédié)
  </read_first>
  <action>
**Étape A** — Créer `apps/memory-api/app/routes/github_repos.py` avec une **vraie implémentation proxy** :

```python
"""GET /v1/github/repos — proxy to LibreChat /api/xbrain/github-repos for ROADMAP Phase 8 criterion 7.

Architecture:
  Le github_access_token vit dans MongoDB LibreChat (stocké par socialLogin.js patch).
  memory-api ne peut pas lire MongoDB directement (boundary respect).
  Donc memory-api fait un proxy : il prend le Bearer token du caller (LibreChat session JWT),
  l'envoie tel quel à /api/xbrain/github-repos via LIBRECHAT_INTERNAL_URL (Docker network).
  LibreChat valide le token via requireJwtAuth, lit github_access_token de MongoDB pour CE user,
  appelle GitHub API, et retourne la liste de repos. Le proxy memory-api retourne cette JSON.

Auth:
  - Bearer token requis (n'importe quel JWT — LibreChat valide le sien côté upstream).
  - Pas de get_current_principal — on délègue l'auth à LibreChat.
  - Si LIBRECHAT_INTERNAL_URL pas configurée → 503 (proxy désactivé).
"""

from typing import Any

import httpx
import structlog
from fastapi import APIRouter, HTTPException, Request

from app.config import settings

log = structlog.get_logger(__name__)
router = APIRouter()


@router.get("/github/repos")
async def list_github_repos(request: Request) -> dict[str, Any]:
    """Proxy le Bearer token du caller vers LibreChat /api/xbrain/github-repos.

    Returns la JSON upstream telle quelle: {repos: [...], linked: bool}.
    """
    # Étape 1: vérifier la config proxy
    librechat_url = getattr(settings, "LIBRECHAT_INTERNAL_URL", "") or ""
    if not librechat_url:
        log.warning("github_repos.proxy_disabled", reason="LIBRECHAT_INTERNAL_URL not set")
        raise HTTPException(503, "github_repos proxy disabled (LIBRECHAT_INTERNAL_URL not configured)")

    # Étape 2: extraire le Bearer token du caller (LibreChat session JWT)
    auth_header = request.headers.get("Authorization", "")
    if not auth_header.startswith("Bearer "):
        raise HTTPException(401, "Bearer token required (LibreChat session JWT)")

    # Étape 3: forward vers LibreChat
    upstream_url = f"{librechat_url.rstrip('/')}/api/xbrain/github-repos"
    try:
        async with httpx.AsyncClient(timeout=10.0) as client:
            resp = await client.get(
                upstream_url,
                headers={
                    "Authorization": auth_header,
                    "Accept": "application/json",
                    "User-Agent": "xbrain-memory-api-proxy",
                },
            )
    except httpx.RequestError as exc:
        log.error("github_repos.upstream_unreachable", error=str(exc))
        raise HTTPException(502, "LibreChat upstream unreachable")

    # Étape 4: relayer le statut + body
    if resp.status_code == 401:
        raise HTTPException(401, "GitHub not connected or LibreChat session expired")
    if resp.status_code == 403:
        raise HTTPException(403, "Forbidden upstream")
    if not resp.is_success:
        log.warning(
            "github_repos.upstream_error",
            status=resp.status_code,
            body=resp.text[:200],
        )
        raise HTTPException(resp.status_code, f"LibreChat proxy error: {resp.status_code}")

    try:
        return resp.json()
    except ValueError:
        log.error("github_repos.upstream_not_json", body=resp.text[:200])
        raise HTTPException(502, "LibreChat upstream returned non-JSON")
```

**Étape B** — Modifier `apps/memory-api/app/config.py` pour ajouter le setting `LIBRECHAT_INTERNAL_URL`. Ajouter à la classe Settings (Pydantic BaseSettings) :

```python
    # Phase 8 — proxy /v1/github/repos vers LibreChat (interne Docker network)
    LIBRECHAT_INTERNAL_URL: str = ""
```

Placer cette ligne dans une zone cohérente (à côté des autres URLs/settings de Phase 8 ou à la fin de la classe). NE PAS toucher aux autres settings existants.

**Étape C** — Modifier `apps/memory-api/app/main.py` pour enregistrer ce router. Ajouter `github_repos` dans le bloc d'import (en respectant l'ordre alphabétique) ET ajouter `app.include_router(github_repos.router, prefix="/v1", tags=["github"])` à la fin :

1. Dans le bloc `from app.routes import (...)`, ajouter `github_repos,` à sa place alphabétique. État cible :

```python
from app.routes import (
    admin_drive,
    admin_projects,
    agents,                  # added by plan 08-02
    audit,
    conversations,
    crm,
    drive_webhook,
    github_repos,            # added by plan 08-05
    granola_integration,
    graph,
    health,
    me,
    me_github,
    memory,
    messages,
    promotions,
    system_prompt,
    tasks,
    teams,
    waitlist,
)
```

2. Ajouter en bas du bloc des include_router :

```python
app.include_router(github_repos.router, prefix="/v1", tags=["github"])
```

NE PAS toucher au reste du fichier.

**Étape D** — Étendre `.env.example` pour documenter `LIBRECHAT_INTERNAL_URL`. Ajouter à la section Phase 8 (ou créer la section si elle n'existe pas — voir plan 08-08 pour la section complète) :

```
# Phase 8 — proxy memory-api → LibreChat pour /v1/github/repos
# URL interne Docker network (PAS https://...example.com — c'est le proxy interne).
# Si non configurée, /v1/github/repos retourne 503.
LIBRECHAT_INTERNAL_URL=http://librechat:3080
```

Notes implémentation :
- L'endpoint memory-api délègue l'authentification à LibreChat. Le `get_current_principal` n'est PAS utilisé ici — c'est le LibreChat upstream qui valide la session JWT du user via `requireJwtAuth` middleware.
- Sur erreur upstream, on relaye le code (401/403/4xx/5xx) — le caller voit le bon status.
- timeout 10s — appel interne Docker network rapide.
- Pas de cache (privacy).
- Si `LIBRECHAT_INTERNAL_URL` est vide en dev/local, l'endpoint retourne 503 → verify-phase8 test 7 acceptera 401 (pas de Bearer) ou 503 (URL pas configurée). On valide donc 401 dans verify-phase8 (puisque la VM aura `LIBRECHAT_INTERNAL_URL=http://librechat:3080` configurée par l'orchestrateur dans .env).
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/memory-api/app/routes/github_repos.py').read()); ast.parse(open('apps/memory-api/app/main.py').read()); ast.parse(open('apps/memory-api/app/config.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `apps/memory-api/app/routes/github_repos.py` existe.
    - `grep -c "@router.get(\"/github/repos\")" apps/memory-api/app/routes/github_repos.py` retourne 1.
    - `grep -c "LIBRECHAT_INTERNAL_URL" apps/memory-api/app/routes/github_repos.py` retourne au moins 1 (proxy URL utilisée).
    - `grep -c "LIBRECHAT_INTERNAL_URL" apps/memory-api/app/config.py` retourne 1 (setting ajoutée).
    - `grep -c "LIBRECHAT_INTERNAL_URL" .env.example` retourne au moins 1 (variable documentée).
    - `grep -c "/api/xbrain/github-repos" apps/memory-api/app/routes/github_repos.py` retourne 1 (URL upstream).
    - `grep -c "    github_repos," apps/memory-api/app/main.py` retourne 1 (import bloc).
    - `grep -c "app.include_router(github_repos.router" apps/memory-api/app/main.py` retourne 1.
    - `grep -c "raise HTTPException(503" apps/memory-api/app/routes/github_repos.py` retourne 1 (URL pas configurée).
    - `grep -c "501" apps/memory-api/app/routes/github_repos.py` retourne 0 (PAS un stub — vrai proxy).
    - Tous les fichiers parsent comme Python valide.
  </acceptance_criteria>
  <done>Le router github_repos est enregistré comme un proxy réel. Au prochain restart memory-api : `curl -i https://api.example.com/v1/github/repos` retourne 401 (sans Bearer) ; avec un Bearer LibreChat valide, retourne 200 + repos JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/UI → LibreChat /api/xbrain/github-repos | requireJwtAuth (LibreChat session JWT) |
| LibreChat → MongoDB | Internal — read user.github_access_token (stocké par socialLogin.js) |
| LibreChat → GitHub API | Outbound HTTPS avec user's github_access_token |
| Browser/UI → memory-api /v1/github/repos | Bearer token caller (forwarded as-is upstream) |
| memory-api → LibreChat (interne Docker network) | LIBRECHAT_INTERNAL_URL — pas exposé publiquement, isolation Docker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Spoofing | /v1/github/repos | mitigate | Le Bearer token est forwarded tel quel — c'est LibreChat qui valide. Un caller sans Bearer reçoit 401. |
| T-08-05-02 | Information Disclosure | Cross-user repo leakage | mitigate | xbrain-routes.js fait `users.findOne({_id: req.user._id})` — un user ne peut accéder qu'à SES repos. Le proxy ne touche jamais le mapping user-token. |
| T-08-05-03 | Information Disclosure | github_access_token in logs | mitigate | console.warn n'affiche QUE `err?.message` côté LibreChat. Le proxy memory-api logge body upstream tronqué à 200 chars en cas d'erreur (jamais le token, qui est dans Authorization header). |
| T-08-05-04 | Tampering | GitHub API response | mitigate | LibreChat fait un mapping explicite des champs (id, name, full_name, etc.) — un repo malicieux ne peut pas injecter des champs arbitraires. Le proxy memory-api relaye telle quelle (déjà sanitized upstream). |
| T-08-05-05 | Information Disclosure | Browser cache of private repos | mitigate | `Cache-Control: no-store` côté LibreChat. Le proxy memory-api ne cache pas non plus. |
| T-08-05-06 | Denial of Service | GitHub rate-limit per user | accept | Si un user fait beaucoup d'appels, GitHub rate-limit son token, pas le serveur global. La fail-soft retourne 502 au UI. |
| T-08-05-07 | Elevation of Privilege | Scope OAuth `repo` peut être abusé | accept | Le user a explicitement consenti au scope `repo` lors du login GitHub. Le risque d'abuse est sur le user lui-même. |
| T-08-05-08 | Information Disclosure | LIBRECHAT_INTERNAL_URL leak via misconfig | mitigate | LIBRECHAT_INTERNAL_URL pointe sur le hostname Docker network (`http://librechat:3080`) — non résolvable hors du compose network. Si quelqu'un fait fuiter le .env c'est un autre problème. |
| T-08-05-09 | Tampering | Bearer token replay via proxy | accept | Le proxy forward le token tel quel — c'est le design (déléguer auth à LibreChat). Un token volé est utilisable directement contre LibreChat aussi ; le proxy n'augmente pas la surface d'attaque. |
</threat_model>

<verification>
- xbrain-routes.js parse comme JS valide (Function constructor)
- github_repos.py parse comme Python valide
- config.py contient LIBRECHAT_INTERNAL_URL
- main.py importe et enregistre le router github_repos
- L'endpoint /v1/github/repos retourne 200/401/403/503 selon le contexte (PLUS jamais 501) — verify-phase8 test #7 PASS
- L'endpoint /api/xbrain/github-repos (LibreChat) est implémenté avec full proxy logic
</verification>

<success_criteria>
- [ ] `apps/librechat/patches/xbrain-routes.js` étendu avec GET /api/xbrain/github-repos
- [ ] L'endpoint LibreChat lit github_access_token depuis MongoDB et appelle GitHub API
- [ ] Réponse normalisée `{ repos: [...], linked: bool }`
- [ ] `apps/memory-api/app/routes/github_repos.py` créé comme **proxy réel** (PAS stub 501)
- [ ] `apps/memory-api/app/config.py` contient `LIBRECHAT_INTERNAL_URL`
- [ ] `apps/memory-api/app/main.py` enregistre le router github_repos
- [ ] `.env.example` documente `LIBRECHAT_INTERNAL_URL=http://librechat:3080`
- [ ] Au prochain restart memory-api : `curl -i https://api.example.com/v1/github/repos` retourne 401 (sans Bearer)
- [ ] Avec un Bearer LibreChat valide : retourne 200 + repos JSON
- [ ] LibreChat image rebuild requis (xbrain-routes.js patché) — à faire en plan 08-08 ou par l'orchestrateur
</success_criteria>

<output>
After completion, create `.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-05-SUMMARY.md` documenting:
- Architecture choisie : proxy réel memory-api → LibreChat (Docker internal network)
- Pourquoi déléguer l'auth à LibreChat (source de vérité github_access_token = MongoDB)
- Pourquoi LIBRECHAT_INTERNAL_URL plutôt qu'un appel direct à GitHub API depuis memory-api
- Note sur LibreChat OAuth scope `repo` requis
- Rebuild commands : `docker compose up -d --build librechat memory-api`
</output>
</output>
