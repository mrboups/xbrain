---
phase: 08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents
plan: 06
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - apps/librechat-bridge/app/contact_extractor.py
  - apps/librechat-bridge/app/mongo_watcher.py
  - apps/librechat-bridge/app/config.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Quand mongo_watcher détecte un nouveau message LibreChat (user OU assistant) avec content non vide, contact_extractor.py extract les contacts via Claude haiku"
    - "Chaque contact extrait est POST vers POST /v1/crm/contacts (upsert team-scoped) avec bridge JWT"
    - "L'extraction est fire-and-forget (asyncio.create_task) — n'interrompt PAS le forward du message"
    - "L'extraction est fail-soft : toute erreur (Claude down, API 500, JSON malformé) est logged.warning et swallowed"
    - "L'extraction est opt-in via env var CONTACT_EXTRACTION (défaut: false) — pattern identique à TASK_INTENT_DETECTION (Phase 7)"
  artifacts:
    - path: "apps/librechat-bridge/app/contact_extractor.py"
      provides: "Fonction extract_contacts_from_message qui appelle Claude + upsert /v1/crm/contacts"
      contains: "extract_contacts_from_message"
    - path: "apps/librechat-bridge/app/mongo_watcher.py"
      provides: "Hook fire-and-forget après détection nouveau message"
      contains: "extract_contacts_from_message"
    - path: "apps/librechat-bridge/app/config.py"
      provides: "Settings CONTACT_EXTRACTION + ANTHROPIC_CONTACT_MODEL"
      contains: "CONTACT_EXTRACTION"
  key_links:
    - from: "mongo_watcher.messages_watch_loop"
      to: "contact_extractor.extract_contacts_from_message"
      via: "asyncio.create_task fire-and-forget after task_intent_detector hook"
      pattern: "asyncio\\.create_task\\(extract_contacts_from_message"
    - from: "contact_extractor"
      to: "memory-api /v1/crm/contacts"
      via: "httpx POST with bridge JWT + X-Team-Scope header"
      pattern: "/v1/crm/contacts"
---

<objective>
Implémenter le **success criterion 3 ROADMAP Phase 8** — Tout output LibreChat (message assistant ou user) déclenche l'extraction contact → CRM.

Architecture (D3 RESEARCH.md) :
- Pattern miroir de `task_intent_detector.py` (Phase 7 plan 07-09).
- Nouveau fichier `apps/librechat-bridge/app/contact_extractor.py` — appelle Claude haiku pour extraire les contacts mentionnés, puis POST vers `/v1/crm/contacts` (upsert).
- Hook dans `mongo_watcher.messages_watch_loop` — fire-and-forget après le hook task_intent_detector existant.
- Process BOTH user AND assistant messages (réponse Open Question 2 RESEARCH.md — les assistants mentionnent aussi des personnes).
- Opt-in via `CONTACT_EXTRACTION=true` env var (défaut false — comme `TASK_INTENT_DETECTION`).

Note importante : Pour le **Chrome extension** path (D3 partie 2), AUCUN changement requis — le handler `/v1/memory/upsert` appelle déjà `_extract_crm_contacts` en fire-and-forget depuis Phase 7 (vérifié dans `apps/memory-api/app/routes/memory.py` lines 344-352). Le criterion 4 ROADMAP est donc déjà satisfait par le code existant.

Output: Service librechat-bridge rebuild → extract contacts depuis chaque message LibreChat → POST /v1/crm/contacts.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-CONTEXT.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-RESEARCH.md

@apps/librechat-bridge/app/task_intent_detector.py
@apps/librechat-bridge/app/mongo_watcher.py
@apps/librechat-bridge/app/config.py
@apps/librechat-bridge/app/memory_api_client.py
@apps/librechat-bridge/app/bridge_token.py

<interfaces>
<!-- Pattern task_intent_detector.py — à reproduire exactement pour contact_extractor.py. -->

From apps/librechat-bridge/app/task_intent_detector.py:
```python
import json
from typing import Any
import structlog
from app.config import settings
log = structlog.get_logger(__name__)

_anthropic_client: Any | None = None

def _get_client() -> Any | None:
    """Lazy-init the Anthropic async client. Returns None if disabled or package absent."""
    global _anthropic_client
    if _anthropic_client is not None:
        return _anthropic_client
    if not settings.ANTHROPIC_API_KEY:
        return None
    try:
        from anthropic import AsyncAnthropic
    except ImportError:
        log.warning("task_intent.anthropic_not_installed")
        return None
    _anthropic_client = AsyncAnthropic(api_key=settings.ANTHROPIC_API_KEY)
    return _anthropic_client


async def detect_task_intent(message_content: str) -> dict[str, Any] | None:
    """Fail-soft: returns None on any error."""
    if not settings.TASK_INTENT_DETECTION:
        return None
    if not message_content or len(message_content.strip()) < 8:
        return None
    client = _get_client()
    if client is None:
        return None
    try:
        msg = await client.messages.create(
            model=settings.ANTHROPIC_TASK_INTENT_MODEL,
            max_tokens=512,
            system=SYSTEM_PROMPT,
            messages=[{"role": "user", "content": message_content[:4000]}],
        )
        text = (msg.content[0].text if msg.content else "{}").strip()
        if text.startswith("```"):
            text = text.split("```", 2)[1].lstrip("json").strip()
        parsed = json.loads(text)
        ...
    except Exception as exc:
        log.warning("task_intent.detection_failed", error=str(exc))
        return None
```

From apps/librechat-bridge/app/bridge_token.py (signature):
```python
def make_bridge_jwt(*, sub: str, team_scope: str) -> str: ...
```

From apps/memory-api/app/routes/crm.py — endpoint /v1/crm/contacts upsert (Phase 7) accepts bridge JWT.

From apps/librechat-bridge/app/mongo_watcher.py — hook point in messages_watch_loop:
```python
if payload.get("role") == "user" and payload.get("content"):
    intent = await detect_task_intent(payload["content"])
    if intent and intent.get("has_task"):
        payload.setdefault("metadata", {})
        payload["metadata"]["contains_action"] = True
        ...
team_scope = await resolve_team_scope(mem, payload["sub"])
await mem.post_message(team_scope=team_scope, **payload)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre apps/librechat-bridge/app/config.py avec CONTACT_EXTRACTION + ANTHROPIC_CONTACT_MODEL</name>
  <files>apps/librechat-bridge/app/config.py</files>
  <read_first>
    - apps/librechat-bridge/app/config.py (état actuel — pattern Phase 7 TASK_INTENT_DETECTION à dupliquer)
  </read_first>
  <action>
Modifier `apps/librechat-bridge/app/config.py` pour ajouter deux nouveaux champs Settings juste après les champs Phase 7. État cible :

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    LIBRECHAT_MONGO_URI: str
    MEMORY_API_URL: str = "http://memory-api:8000"
    BRIDGE_SHARED_SECRET: str
    JWT_ALGORITHM: str = "HS256"
    BRIDGE_DEFAULT_TEAM_SCOPE: str = "default"
    BRIDGE_BACKFILL_FROM: str = "startup"  # "startup" | "never"
    LOG_LEVEL: str = "INFO"
    BRIDGE_HEARTBEAT_PATH: str = "/tmp/bridge-alive"

    # Phase 7 plan 07-09 — Task intent detection (D5 trigger 3)
    TASK_INTENT_DETECTION: bool = False  # opt-in — set TASK_INTENT_DETECTION=true to enable
    ANTHROPIC_API_KEY: str = ""
    ANTHROPIC_TASK_INTENT_MODEL: str = "claude-3-5-haiku-20241022"

    # Phase 8 plan 08-06 — Contact extraction from LibreChat messages (D3 RESEARCH.md)
    CONTACT_EXTRACTION: bool = False  # opt-in — set CONTACT_EXTRACTION=true to enable
    ANTHROPIC_CONTACT_MODEL: str = "claude-3-5-haiku-20241022"


settings = Settings()  # type: ignore[call-arg]
```

NE PAS toucher aux champs existants. NE PAS supprimer le commentaire Phase 7.
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/librechat-bridge/app/config.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CONTACT_EXTRACTION: bool = False" apps/librechat-bridge/app/config.py` retourne 1.
    - `grep -c "ANTHROPIC_CONTACT_MODEL" apps/librechat-bridge/app/config.py` retourne 1.
    - `grep -c "TASK_INTENT_DETECTION" apps/librechat-bridge/app/config.py` retourne 1 (existant conservé).
    - Le fichier parse comme Python valide.
  </acceptance_criteria>
  <done>Config étendue avec deux nouveaux champs opt-in pour Phase 8.</done>
</task>

<task type="auto">
  <name>Task 2: Créer contact_extractor.py — extraction Claude + upsert /v1/crm/contacts</name>
  <files>apps/librechat-bridge/app/contact_extractor.py</files>
  <read_first>
    - apps/librechat-bridge/app/task_intent_detector.py (pattern complet à reproduire — lazy client, fail-soft, JSON parse)
    - apps/librechat-bridge/app/bridge_token.py (signature make_bridge_jwt — sub + team_scope)
    - apps/librechat-bridge/app/config.py (settings.MEMORY_API_URL, ANTHROPIC_API_KEY, ANTHROPIC_CONTACT_MODEL, CONTACT_EXTRACTION)
  </read_first>
  <action>
Créer `apps/librechat-bridge/app/contact_extractor.py` :

```python
"""Contact extraction in LibreChat messages (Phase 8 D3 RESEARCH.md).

Strategy: lightweight Claude call to detect person mentions in a chat message
(both user AND assistant messages). Each mention is upserted into the CRM via
POST /v1/crm/contacts using a bridge JWT scoped to the message's team.

Pattern mirrored from apps/librechat-bridge/app/task_intent_detector.py:
  - Lazy AsyncAnthropic singleton
  - Fail-soft: any error logs WARN and returns silently
  - Opt-in via CONTACT_EXTRACTION env var (default false)
  - Bridge JWT for service-to-service auth with memory-api

Why both user + assistant messages?
  Open Question 2 RESEARCH.md — assistant responses also mention people
  (e.g. "Alice has approved the deck"). Symmetric extraction prevents
  one-way visibility gaps.
"""

import json
from typing import Any

import httpx
import structlog

from app.bridge_token import make_bridge_jwt
from app.config import settings

log = structlog.get_logger(__name__)

SYSTEM_PROMPT = """Extract distinct person mentions from the message content.

Return STRICT JSON array (no markdown fences, no prose):
[
  {"name": "Full name or null", "email": "email@example.com or null", "company": "company name or null", "role": "their role or null"}
]

Rules:
- Only include people who are clearly mentioned by name, email, or @mention
- Skip generic references ("the user", "they", pronouns)
- Skip the chat participants themselves unless they're discussing OTHERS
- If both name and email are absent, skip the entry
- Maximum 10 entries per message
- If no people mentioned, return []

Output ONLY the JSON array."""


_anthropic_client: Any | None = None


def _get_client() -> Any | None:
    """Lazy-init Anthropic async client. Returns None if disabled or package absent."""
    global _anthropic_client
    if _anthropic_client is not None:
        return _anthropic_client
    if not settings.ANTHROPIC_API_KEY:
        return None
    try:
        from anthropic import AsyncAnthropic  # noqa: PLC0415
    except ImportError:
        log.warning("contact_extractor.anthropic_not_installed")
        return None
    _anthropic_client = AsyncAnthropic(api_key=settings.ANTHROPIC_API_KEY)
    return _anthropic_client


async def _upsert_contact(
    *,
    sub: str,
    team_scope: str,
    name: str | None,
    email: str | None,
    company: str | None,
    role: str | None,
    source: str,
    source_ref: str | None,
) -> None:
    """POST /v1/crm/contacts with bridge JWT. Fail-soft."""
    token = make_bridge_jwt(sub=sub, team_scope=team_scope)
    body = {
        "team_scope": team_scope,
        "contact_type": "direct",
        "full_name": name,
        "email": email,
        "company": company,
        "role": role,
        "source": source,
        "truth_level": "EPHEMERAL",
        "confidence": 0.6,
    }
    if source_ref:
        body["source_ref"] = source_ref
    url = f"{settings.MEMORY_API_URL}/v1/crm/contacts"
    try:
        async with httpx.AsyncClient(timeout=15.0) as client:
            resp = await client.post(
                url,
                json=body,
                headers={
                    "Authorization": f"Bearer {token}",
                    "X-Team-Scope": team_scope,
                    "Content-Type": "application/json",
                },
            )
        if resp.status_code >= 400:
            log.warning(
                "contact_extractor.upsert_non_2xx",
                status=resp.status_code,
                body=resp.text[:200],
                team_scope=team_scope,
            )
    except Exception as exc:
        log.warning(
            "contact_extractor.upsert_failed",
            error=str(exc),
            team_scope=team_scope,
        )


async def extract_contacts_from_message(
    *,
    content: str,
    sub: str,
    team_scope: str,
    source: str,
    source_ref: str | None = None,
) -> None:
    """Extract person mentions from a chat message and upsert into CRM. Fail-soft.

    Triggered fire-and-forget from mongo_watcher.messages_watch_loop after the
    standard message forward to memory-api. Never blocks the forward.

    Args:
        content: message text (user OR assistant)
        sub: principal sub for bridge JWT (the LibreChat user identity)
        team_scope: team_scope to attach contacts to
        source: e.g. "librechat:gpt-4" — passed to /v1/crm/contacts as source
        source_ref: optional reference (e.g. message_id) for traceability
    """
    if not settings.CONTACT_EXTRACTION:
        return
    if not content or len(content.strip()) < 30:
        # Skip very short messages — usually no person mention anyway
        return

    client = _get_client()
    if client is None:
        return

    try:
        msg = await client.messages.create(
            model=settings.ANTHROPIC_CONTACT_MODEL,
            max_tokens=1024,
            system=SYSTEM_PROMPT,
            messages=[{"role": "user", "content": content[:6000]}],
        )
        text = (msg.content[0].text if msg.content else "[]").strip()
        if text.startswith("```"):
            text = text.split("```", 2)[1].lstrip("json").strip()
        parsed = json.loads(text)
        if not isinstance(parsed, list):
            return
    except json.JSONDecodeError as exc:
        log.warning("contact_extractor.json_decode_failed", error=str(exc))
        return
    except Exception as exc:
        log.warning("contact_extractor.detection_failed", error=str(exc))
        return

    contacts_upserted = 0
    for person in parsed[:10]:  # cap at 10 per message (matches SYSTEM_PROMPT)
        if not isinstance(person, dict):
            continue
        name = person.get("name") or None
        email = person.get("email") or None
        if not (name or email):
            continue
        await _upsert_contact(
            sub=sub,
            team_scope=team_scope,
            name=name,
            email=email,
            company=person.get("company") or None,
            role=person.get("role") or None,
            source=source,
            source_ref=source_ref,
        )
        contacts_upserted += 1

    if contacts_upserted > 0:
        log.info(
            "contact_extractor.upserted",
            count=contacts_upserted,
            team_scope=team_scope,
            source=source,
        )
```

Notes implémentation :
- `confidence=0.6` (vs 0.9 pour Granola attendees) — l'extraction LibreChat est moins fiable qu'une liste d'attendees structurée, donc confidence plus basse.
- `truth_level=EPHEMERAL` (vs WORKING pour Granola) — un contact extrait d'un chat reste éphémère jusqu'à confirmation manuelle ou répétition.
- Cap 10 contacts par message — empêche un message qui mentionne 50 personnes de spam le CRM.
- Min length 30 chars — heuristic pour éviter les "ok", "yes", "thanks" inutiles.
- Le content est cap à 6000 chars (vs 4000 pour task_intent) — un peu plus large pour capturer plus de contexte.
- L'endpoint `/v1/crm/contacts` doit accepter le bridge JWT pour ce upsert. Le pattern `make_bridge_jwt(sub=sub, team_scope=team_scope)` produit un JWT user-impersonating (cf. `bridge_token.py`).
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/librechat-bridge/app/contact_extractor.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `apps/librechat-bridge/app/contact_extractor.py` existe.
    - `grep -c "async def extract_contacts_from_message" apps/librechat-bridge/app/contact_extractor.py` retourne 1.
    - `grep -c "async def _upsert_contact" apps/librechat-bridge/app/contact_extractor.py` retourne 1.
    - `grep -c "if not settings.CONTACT_EXTRACTION" apps/librechat-bridge/app/contact_extractor.py` retourne 1 (opt-in gate).
    - `grep -c "/v1/crm/contacts" apps/librechat-bridge/app/contact_extractor.py` retourne 1.
    - `grep -c "make_bridge_jwt" apps/librechat-bridge/app/contact_extractor.py` retourne at least 1.
    - `grep -c "log.warning" apps/librechat-bridge/app/contact_extractor.py` retourne at least 3 (fail-soft logging multi-step).
    - Le fichier parse comme Python valide.
  </acceptance_criteria>
  <done>contact_extractor.py prêt à être importé et appelé en fire-and-forget depuis mongo_watcher.</done>
</task>

<task type="auto">
  <name>Task 3: Hooker contact_extractor dans mongo_watcher.py — fire-and-forget après le forward</name>
  <files>apps/librechat-bridge/app/mongo_watcher.py</files>
  <read_first>
    - apps/librechat-bridge/app/mongo_watcher.py (état actuel — emplacement exact du hook task_intent_detector pour insérer le nouveau hook)
    - apps/librechat-bridge/app/contact_extractor.py (créé en Task 2)
  </read_first>
  <action>
Modifier `apps/librechat-bridge/app/mongo_watcher.py` :

1. Dans le bloc d'imports en haut, ajouter `from app.contact_extractor import extract_contacts_from_message` (juste après `from app.task_intent_detector import detect_task_intent`).

2. Dans `messages_watch_loop`, ajouter le fire-and-forget hook **APRÈS** `await mem.post_message(...)` (donc après que le message a été forwardé avec succès), mais avant le `save_resume_token(...)` ou dans la même section. Le hook traite BOTH user et assistant messages.

3. **AJOUTER** `import asyncio` si pas déjà présent (vérifier — il devrait l'être car c'est utilisé pour `asyncio.gather` dans `watch_loop`).

État cible des modifications dans `messages_watch_loop` (les lignes inchangées sont gardées, le hook s'ajoute APRÈS le forward) :

```python
                    team_scope = await resolve_team_scope(mem, payload["sub"])
                    await mem.post_message(team_scope=team_scope, **payload)
                    log.info(
                        "forwarded_message",
                        lc_id=payload["metadata"]["librechat_id"],
                        team=team_scope,
                        source=payload["source"],
                    )
                    # Phase 8 plan 08-06 — fire-and-forget contact extraction (D3 RESEARCH.md)
                    # Process BOTH user AND assistant messages (Open Question 2 resolution).
                    # Fail-soft, opt-in via CONTACT_EXTRACTION env var.
                    if payload.get("content"):
                        asyncio.create_task(
                            extract_contacts_from_message(
                                content=payload["content"],
                                sub=payload["sub"],
                                team_scope=team_scope,
                                source=payload.get("source") or "librechat",
                                source_ref=payload["metadata"].get("librechat_id"),
                            )
                        )
                save_resume_token(change.get("_id"))
```

Attention : NE PAS modifier le hook task_intent_detector existant. NE PAS modifier `conversations_watch_loop` (l'extraction contacts s'applique aux messages, pas aux conversations).

Le bloc complet final de `messages_watch_loop` (sans le `_heartbeat()` qui suit) doit ressembler à :

```python
async def messages_watch_loop(db, mem: MemoryApiClient) -> None:
    """Forward every new `messages` insert to memory-api."""
    resume_token = load_resume_token()
    pipeline = [{"$match": {"operationType": "insert", "ns.coll": "messages"}}]
    log.info("starting_messages_change_stream", resume=bool(resume_token))

    async with db.watch(pipeline=pipeline, resume_after=resume_token) as stream:
        async for change in stream:
            try:
                doc = change["fullDocument"]
                payload = await map_message(doc, db)
                if payload:
                    # Phase 7 plan 07-09 — Task intent detection (D5 trigger 3)
                    if payload.get("role") == "user" and payload.get("content"):
                        intent = await detect_task_intent(payload["content"])
                        if intent and intent.get("has_task"):
                            payload.setdefault("metadata", {})
                            payload["metadata"]["contains_action"] = True
                            if intent.get("title"):
                                payload["metadata"]["task_intent_title"] = intent["title"]
                            if intent.get("assignee_email"):
                                payload["metadata"]["task_intent_assignee_email"] = intent[
                                    "assignee_email"
                                ]
                            log.info(
                                "task_intent.detected",
                                sub=payload.get("sub"),
                                conv=payload.get("conversation_id"),
                                title=intent.get("title"),
                            )
                    team_scope = await resolve_team_scope(mem, payload["sub"])
                    await mem.post_message(team_scope=team_scope, **payload)
                    log.info(
                        "forwarded_message",
                        lc_id=payload["metadata"]["librechat_id"],
                        team=team_scope,
                        source=payload["source"],
                    )
                    # Phase 8 plan 08-06 — fire-and-forget contact extraction (D3 RESEARCH.md)
                    # Process BOTH user AND assistant messages (Open Question 2 resolution).
                    # Fail-soft, opt-in via CONTACT_EXTRACTION env var.
                    if payload.get("content"):
                        asyncio.create_task(
                            extract_contacts_from_message(
                                content=payload["content"],
                                sub=payload["sub"],
                                team_scope=team_scope,
                                source=payload.get("source") or "librechat",
                                source_ref=payload["metadata"].get("librechat_id"),
                            )
                        )
                save_resume_token(change.get("_id"))
                _heartbeat()
            except Exception as e:  # noqa: BLE001
                log.error(
                    "messages_watch_event_failed",
                    err=str(e),
                    err_type=type(e).__name__,
                )
                continue
```

Vérifier que le bloc `import asyncio` existe en haut du fichier (il devrait — déjà utilisé par `asyncio.gather` dans `watch_loop`).
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/librechat-bridge/app/mongo_watcher.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from app.contact_extractor import extract_contacts_from_message" apps/librechat-bridge/app/mongo_watcher.py` retourne 1.
    - `grep -c "asyncio.create_task(" apps/librechat-bridge/app/mongo_watcher.py` retourne at least 1.
    - `grep -c "extract_contacts_from_message(" apps/librechat-bridge/app/mongo_watcher.py` retourne at least 2 (1 import + 1 usage).
    - `grep -c "from app.task_intent_detector import detect_task_intent" apps/librechat-bridge/app/mongo_watcher.py` retourne 1 (existant conservé).
    - `grep -c "import asyncio" apps/librechat-bridge/app/mongo_watcher.py` retourne 1.
    - `grep -A 10 "extract_contacts_from_message(" apps/librechat-bridge/app/mongo_watcher.py | grep -c "team_scope=team_scope"` retourne at least 1 (le hook utilise le team_scope résolu).
    - Le fichier parse comme Python valide.
    - L'ordre des appels est correct : `await mem.post_message(...)` AVANT `asyncio.create_task(extract_contacts_from_message(...))` AVANT `save_resume_token(...)`.
  </acceptance_criteria>
  <done>mongo_watcher.py forward chaque message + déclenche fire-and-forget l'extraction contacts. Service librechat-bridge nécessite rebuild + restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MongoDB change stream → librechat-bridge | Internal — change stream contient le contenu user/assistant des messages |
| librechat-bridge → Anthropic API | Outbound HTTPS avec ANTHROPIC_API_KEY |
| librechat-bridge → memory-api /v1/crm/contacts | Bridge JWT (HS256, sub=user, team_scope from resolve_team_scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Information Disclosure | message content sent to Anthropic | accept | Le content message est envoyé à Anthropic pour extraction — c'est aligné avec le pattern Phase 7 (extract_crm_contacts dans memory-api fait pareil). Les utilisateurs sont informés via le ToS. |
| T-08-06-02 | Spoofing | bridge JWT acting on behalf of user | mitigate | make_bridge_jwt(sub=user_sub) émet un JWT avec le sub réel du user — le contact upsert est attribué au bon team_scope. Pas de cross-team. |
| T-08-06-03 | Denial of Service | Spam Anthropic via volume messages | mitigate | Min length 30 chars + cap 6000 chars + cap 10 contacts/message. Default OFF (opt-in). Si abuse, set CONTACT_EXTRACTION=false côté env. |
| T-08-06-04 | Tampering | Malicious message injects fake JSON | mitigate | json.loads() catch JSONDecodeError. isinstance(parsed, list) check. Cap à 10 entries. Champs name/email validés via skip si tous deux null. |
| T-08-06-05 | Information Disclosure | Cross-team contact leak | mitigate | team_scope vient de resolve_team_scope(mem, sub) — la même resolution que le forward du message. Un user de team A ne peut pas faire upsert dans team B. |
| T-08-06-06 | Repudiation | Auto-extraction sans audit | accept | L'upsert /v1/crm/contacts a déjà write_audit côté memory-api (Phase 7) — actor_user_id=user.id, action='contact.upserted'. Trace complète. |
| T-08-06-07 | DoS | Anthropic spend explosion | accept | Phase 8 : pas de quota. Default OFF. Phase 9+ ajoutera quota Langfuse-based. |
| T-08-06-08 | Tampering | source_ref leak librechat_id | accept | source_ref=librechat_id permet le traçage — c'est utile pour de-dup et debug, pas une fuite (l'id est interne). |
</threat_model>

<verification>
- contact_extractor.py parse comme Python valide
- mongo_watcher.py parse comme Python valide
- config.py contient CONTACT_EXTRACTION et ANTHROPIC_CONTACT_MODEL
- Le hook fire-and-forget est positionné APRÈS le forward du message (pour ne pas bloquer)
- Default opt-in : CONTACT_EXTRACTION=False — pas de side effect avant que l'utilisateur l'active
</verification>

<success_criteria>
- [ ] `apps/librechat-bridge/app/config.py` étendu avec CONTACT_EXTRACTION + ANTHROPIC_CONTACT_MODEL
- [ ] `apps/librechat-bridge/app/contact_extractor.py` créé avec extract_contacts_from_message + _upsert_contact
- [ ] `apps/librechat-bridge/app/mongo_watcher.py` hook fire-and-forget après le forward (BOTH user et assistant)
- [ ] Default opt-in : CONTACT_EXTRACTION=False, n'a aucun effet tant que pas activé
- [ ] Service librechat-bridge rebuild + redémarrage avec CONTACT_EXTRACTION=true (à faire en plan 08-08 ou par l'orchestrateur sur la VM)
- [ ] Avec CONTACT_EXTRACTION=true, un message LibreChat avec mention "Alice (alice@example.com)" déclenche un POST /v1/crm/contacts visible dans memory-api logs
</success_criteria>

<output>
After completion, create `.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-06-SUMMARY.md` documenting:
- Pattern miroir task_intent_detector → contact_extractor
- Décision both user + assistant messages (Open Question 2)
- Default OFF — opt-in pour éviter les coûts inattendus
- Note rebuild requis : `docker compose up -d --build librechat-bridge`
</output>
