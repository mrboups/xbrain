---
phase: 08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memory-api/alembic/versions/0012_granola_user_agents.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "PostgreSQL contient une table granola_user_connections avec FK vers users(id) ON DELETE CASCADE"
    - "PostgreSQL contient une table agent_definitions avec contrainte UNIQUE sur name"
    - "L'agent meeting-recap est seedé dans agent_definitions au moment de la migration (idempotent)"
    - "alembic_version dans la base est avancé à 0012 après docker compose exec memory-api alembic upgrade head"
  artifacts:
    - path: "apps/memory-api/alembic/versions/0012_granola_user_agents.py"
      provides: "Alembic migration creating granola_user_connections + agent_definitions + seeding meeting-recap"
      contains: "revision = \"0012\""
  key_links:
    - from: "0012 migration"
      to: "users table"
      via: "ForeignKey(users.id, ondelete=CASCADE) on granola_user_connections.user_id"
      pattern: "ForeignKey.*users.id.*CASCADE"
    - from: "0012 migration"
      to: "agent_definitions seed"
      via: "INSERT ... ON CONFLICT (name) DO NOTHING for 'meeting-recap'"
      pattern: "ON CONFLICT.*name.*DO NOTHING"
---

<objective>
Créer la migration Alembic 0012 qui ajoute les deux tables fondatrices Phase 8 :
1. `granola_user_connections` (clé Granola per-user chiffrée Fernet, FK vers users)
2. `agent_definitions` (registry agents de plateforme, avec seed idempotent de l'agent `meeting-recap`)

Puis appliquer la migration via `alembic upgrade head` (étape [BLOCKING]).

Purpose: Toutes les autres plans Phase 8 dépendent de ces deux tables — c'est le prérequis Wave 0/1 unique. La migration doit être idempotente (le seed `meeting-recap` utilise `ON CONFLICT (name) DO NOTHING` pour permettre une ré-exécution sans erreur).

Output: Fichier `apps/memory-api/alembic/versions/0012_granola_user_agents.py` + base de données avec `alembic_version='0012'` + 1 row seedé dans `agent_definitions` (name=`meeting-recap`).
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-CONTEXT.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-RESEARCH.md

@apps/memory-api/alembic/versions/0009_crm_contacts.py
@apps/memory-api/alembic/versions/0011_team_onboarding.py

<interfaces>
<!-- Pattern des migrations existantes — à reproduire EXACTEMENT (revision string, imports, postgresql.UUID(as_uuid=True), server_default sa.func.now()). -->

From apps/memory-api/alembic/versions/0011_team_onboarding.py:
```python
from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

revision: str = "0011"
down_revision: Union[str, None] = "0010"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None

# Pattern colonne UUID PK :
sa.Column(
    "id",
    postgresql.UUID(as_uuid=True),
    server_default=sa.text("gen_random_uuid()"),
    primary_key=True,
)
# Pattern FK CASCADE :
sa.Column(
    "user_id",
    postgresql.UUID(as_uuid=True),
    sa.ForeignKey("users.id", ondelete="CASCADE"),
    nullable=False,
)
# Pattern timestamps :
sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False)
sa.Column("updated_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False)
```

From apps/memory-api/alembic/versions/0009_crm_contacts.py (granola_integrations reference):
```python
op.create_table(
    "granola_integrations",
    sa.Column("id", postgresql.UUID(as_uuid=True), server_default=sa.text("gen_random_uuid()"), primary_key=True),
    sa.Column("team_scope", sa.String(64), nullable=False),
    sa.Column("api_key_enc", sa.Text, nullable=False),
    sa.Column("last_polled_at", sa.DateTime(timezone=True), nullable=True),
    sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
    sa.Column("updated_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer la migration Alembic 0012 (granola_user_connections + agent_definitions + seed meeting-recap)</name>
  <files>apps/memory-api/alembic/versions/0012_granola_user_agents.py</files>
  <read_first>
    - apps/memory-api/alembic/versions/0011_team_onboarding.py (pattern exact à reproduire — imports, revision string, postgresql.UUID, sa.func.now)
    - apps/memory-api/alembic/versions/0009_crm_contacts.py (pattern granola_integrations table — référence pour granola_user_connections)
    - apps/memory-api/alembic/versions/0010_tasks.py (pattern FK ON DELETE SET NULL pour created_by — référence pour agent_definitions.created_by)
    - .planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-CONTEXT.md (D1 + D4 + D6 — schémas exacts)
    - .planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-RESEARCH.md (Pitfall 4 — seed via ON CONFLICT)
  </read_first>
  <action>
Créer le fichier `apps/memory-api/alembic/versions/0012_granola_user_agents.py` avec ce contenu exact :

```python
"""granola_user_agents — granola_user_connections (per-user) + agent_definitions registry + seed meeting-recap (D1+D4+D5+D6 Phase 8).

Revision ID: 0012
Revises: 0011
Create Date: 2026-05-09
"""
from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

revision: str = "0012"
down_revision: Union[str, None] = "0011"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None


_MEETING_RECAP_SYSTEM_PROMPT = """Tu es un agent de résumé de réunion. À partir d'un transcript ou d'un résumé brut de meeting, génère un compte-rendu structuré au format Markdown contenant :

## Résumé exécutif
2-3 phrases sur l'essentiel discuté.

## Participants
Liste à puces des personnes présentes (nom, et email/rôle si mentionné).

## Décisions
Liste numérotée des décisions actées pendant la réunion.

## Action items
Liste à puces : "[Assignee] — Action à entreprendre — Échéance si mentionnée".

## Points ouverts
Sujets non tranchés à reprendre plus tard.

Reste factuel — n'invente pas de participants, de décisions ou d'actions absentes du texte source. Si une section est vide, écris "(aucun)" sous le titre."""


def upgrade() -> None:
    # ── Table: granola_user_connections (D1 — clé Granola per-user) ─────────────
    op.create_table(
        "granola_user_connections",
        sa.Column(
            "id",
            postgresql.UUID(as_uuid=True),
            server_default=sa.text("gen_random_uuid()"),
            primary_key=True,
        ),
        sa.Column(
            "user_id",
            postgresql.UUID(as_uuid=True),
            sa.ForeignKey("users.id", ondelete="CASCADE"),
            nullable=False,
        ),
        sa.Column("team_scope", sa.String(64), nullable=False),
        sa.Column("api_key_enc", sa.Text, nullable=False),
        sa.Column("last_polled_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column("enabled", sa.Boolean, nullable=False, server_default=sa.text("true")),
        sa.Column(
            "created_at",
            sa.DateTime(timezone=True),
            server_default=sa.func.now(),
            nullable=False,
        ),
        sa.Column(
            "updated_at",
            sa.DateTime(timezone=True),
            server_default=sa.func.now(),
            nullable=False,
        ),
        sa.UniqueConstraint("user_id", name="guc_user_uniq"),
    )
    op.create_index("idx_guc_user", "granola_user_connections", ["user_id"])

    # ── Table: agent_definitions (D4 — registry agents plateforme) ──────────────
    op.create_table(
        "agent_definitions",
        sa.Column(
            "id",
            postgresql.UUID(as_uuid=True),
            server_default=sa.text("gen_random_uuid()"),
            primary_key=True,
        ),
        sa.Column("name", sa.String(128), nullable=False),
        sa.Column("description", sa.Text, nullable=True),
        sa.Column("system_prompt", sa.Text, nullable=False),
        sa.Column(
            "model",
            sa.String(128),
            nullable=False,
            server_default="claude-haiku-4-5-20251001",
        ),
        sa.Column("tools_json", postgresql.JSONB, nullable=True),
        sa.Column(
            "enabled",
            sa.Boolean,
            nullable=False,
            server_default=sa.text("true"),
        ),
        sa.Column(
            "auto_trigger",
            sa.Boolean,
            nullable=False,
            server_default=sa.text("true"),
        ),
        sa.Column(
            "created_by",
            postgresql.UUID(as_uuid=True),
            sa.ForeignKey("users.id", ondelete="SET NULL"),
            nullable=True,
        ),
        sa.Column(
            "created_at",
            sa.DateTime(timezone=True),
            server_default=sa.func.now(),
            nullable=False,
        ),
        sa.Column(
            "updated_at",
            sa.DateTime(timezone=True),
            server_default=sa.func.now(),
            nullable=False,
        ),
        sa.UniqueConstraint("name", name="agent_definitions_name_uniq"),
    )
    op.create_index("idx_agent_definitions_enabled", "agent_definitions", ["enabled"])

    # ── Seed: agent meeting-recap (D5 — auto_trigger=true) ──────────────────────
    # Idempotent: ON CONFLICT (name) DO NOTHING — re-running does not duplicate.
    op.execute(
        sa.text(
            """
            INSERT INTO agent_definitions (name, description, system_prompt, model, auto_trigger, enabled)
            VALUES (:name, :desc, :prompt, :model, true, true)
            ON CONFLICT (name) DO NOTHING
            """
        ).bindparams(
            name="meeting-recap",
            desc="Génère un résumé structuré de réunion (résumé exécutif, participants, décisions, action items, points ouverts) depuis un transcript Granola.",
            prompt=_MEETING_RECAP_SYSTEM_PROMPT,
            model="claude-haiku-4-5-20251001",
        )
    )


def downgrade() -> None:
    op.drop_index("idx_agent_definitions_enabled", table_name="agent_definitions")
    op.drop_table("agent_definitions")
    op.drop_index("idx_guc_user", table_name="granola_user_connections")
    op.drop_table("granola_user_connections")
```

Notes implémentation :
- `revision = "0012"`, `down_revision = "0011"` — chaîne strictement consécutive.
- FK `users.id ON DELETE CASCADE` pour `granola_user_connections.user_id` — un user supprimé efface sa clé Granola (RGPD-friendly).
- FK `users.id ON DELETE SET NULL` pour `agent_definitions.created_by` — un user supprimé n'efface pas l'agent qu'il a créé (cohérent avec migration 0010 tasks.created_by).
- `UniqueConstraint("user_id")` — un user a au plus une clé Granola active (Pitfall 3 + Open Question 1 résolue par recommandation RESEARCH.md).
- `UniqueConstraint("name")` sur `agent_definitions` — nécessaire pour `ON CONFLICT (name) DO NOTHING` du seed (Pitfall 4 + A4 RESEARCH.md).
- `tools_json JSONB nullable` — réservé Phase 9+, pas exécuté en Phase 8 (D4).
- `auto_trigger=true` server_default + valeur explicite dans le seed — l'agent meeting-recap est auto-déclenché par défaut (D5).
- Le system_prompt est en français pour cohérence avec la langue produit (la team utilise le français).
- Ne pas créer de doublon `_MEETING_RECAP_SYSTEM_PROMPT` ailleurs — il vit uniquement dans cette migration (single source pour le seed).
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; tree = ast.parse(open('apps/memory-api/alembic/versions/0012_granola_user_agents.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `apps/memory-api/alembic/versions/0012_granola_user_agents.py` existe.
    - `grep -c "revision: str = \"0012\"" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 1.
    - `grep -c "down_revision: Union\[str, None\] = \"0011\"" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 1.
    - `grep -c "create_table(" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 2 (granola_user_connections + agent_definitions).
    - `grep -c "ForeignKey(\"users.id\", ondelete=\"CASCADE\")" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 1 (granola_user_connections.user_id).
    - `grep -c "ForeignKey(\"users.id\", ondelete=\"SET NULL\")" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 1 (agent_definitions.created_by).
    - `grep -c "ON CONFLICT (name) DO NOTHING" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 1 (seed idempotent).
    - `grep -c "meeting-recap" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne au moins 1.
    - `grep -c "guc_user_uniq" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 1 (UNIQUE user_id).
    - `grep -c "agent_definitions_name_uniq" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne 1 (UNIQUE name).
    - `grep -c "tools_json" apps/memory-api/alembic/versions/0012_granola_user_agents.py` retourne au moins 1 (colonne JSONB présente).
    - Le fichier est un module Python valide (parse AST sans erreur).
  </acceptance_criteria>
  <done>Migration 0012 fichier complet, valide syntaxiquement, conforme aux schémas D1+D4+D5+D6, prête à être appliquée.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Appliquer la migration via alembic upgrade head</name>
  <files>(no file modification — runtime DB state change)</files>
  <read_first>
    - apps/memory-api/alembic/versions/0012_granola_user_agents.py (le fichier créé en Task 1 — vérifier qu'il existe avant exec)
    - infrastructure/docker-compose.yml (vérifier que memory-api container est nommé xbrain-memory-api ou similaire)
  </read_first>
  <action>
**[BLOCKING]** Cette étape doit s'exécuter sur la VM (__VM_HOST__) où Docker Compose tourne. Si l'agent ne peut pas exécuter sur la VM directement, marquer ce gate comme requérant une exécution manuelle SSH.

Étapes exactes (à exécuter dans cet ordre) :

1. SSH dans la VM : `ssh ubuntu@__VM_HOST__`
2. Aller dans le répertoire du projet : `cd ~/xbrain/infrastructure` (ou équivalent où vit `docker-compose.yml`)
3. S'assurer que les sources sont à jour : `git pull origin main`
4. Appliquer la migration : `docker compose exec memory-api alembic upgrade head`
5. Vérifier l'état : `docker compose exec postgres psql -U xbrain -d xbrain -tAc "SELECT version_num FROM alembic_version"` → doit retourner `0012`
6. Vérifier les deux tables : `docker compose exec postgres psql -U xbrain -d xbrain -tAc "SELECT to_regclass('granola_user_connections')::text, to_regclass('agent_definitions')::text"` → doit retourner `granola_user_connections|agent_definitions`
7. Vérifier le seed : `docker compose exec postgres psql -U xbrain -d xbrain -tAc "SELECT count(*) FROM agent_definitions WHERE name='meeting-recap'"` → doit retourner `1`

Si la step 4 échoue avec une erreur Alembic (par ex. "Multiple head revisions"), c'est qu'une migration parallèle existe — investiguer avant de continuer (NE PAS forcer avec `alembic stamp`).

Si l'agent exécute depuis Windows (D:\\VSC\\xbrain) sans accès SSH, créer un fichier marker `.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/.MIGRATE_0012_REQUIRED` contenant les commandes ci-dessus, et signaler à l'utilisateur via le summary que la migration doit être appliquée manuellement sur la VM avant de lancer les plans 08-02..08-08.
  </action>
  <verify>
    <automated>ssh ubuntu@__VM_HOST__ "cd ~/xbrain/infrastructure && docker compose exec -T postgres psql -U xbrain -d xbrain -tAc \"SELECT version_num FROM alembic_version\"" 2>&1 | grep -q "^0012$" && echo PASS || echo "FALLBACK: marker file required"</automated>
  </verify>
  <acceptance_criteria>
    - Soit `alembic_version` table contient version_num=0012 (migration appliquée), soit le fichier marker `.planning/phases/08-.../.MIGRATE_0012_REQUIRED` existe avec les commandes documentées.
    - Si appliqué : `SELECT count(*) FROM agent_definitions WHERE name='meeting-recap'` retourne 1.
    - Si appliqué : `SELECT to_regclass('granola_user_connections')::text` retourne `granola_user_connections`.
    - Si appliqué : `SELECT to_regclass('agent_definitions')::text` retourne `agent_definitions`.
    - Aucune autre migration n'est passée à `head` accidentellement (la migration 0012 est bien la dernière).
  </acceptance_criteria>
  <done>Migration 0012 appliquée sur la base PostgreSQL de la VM, OU marker file créé documentant l'action manuelle requise. Toute autre tâche Phase 8 sera bloquée par verify-phase8.sh tant que la migration n'est pas appliquée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory-api → PostgreSQL | DDL execution via Alembic — schema changes apply atomically dans une transaction Postgres |
| Migration runtime → seed text | Le system_prompt seedé est un string littéral dans la migration — pas d'input utilisateur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | Migration file | mitigate | Migration commitée en git, signée par revision string `0012`. Toute altération post-deploy serait détectée par alembic check. |
| T-08-01-02 | Information Disclosure | api_key_enc column | mitigate | `api_key_enc` est sa.Text — la valeur stockée est ciphertext Fernet (chiffrement at rest). Aucune route ne SELECT api_key_enc en GET (cf. plan 08-03). |
| T-08-01-03 | Repudiation | agent_definitions.created_by | accept | FK ON DELETE SET NULL — un user supprimé devient NULL, mais audit_log conserve la trace de la création initiale (cohérent avec le pattern tasks.created_by Phase 7). |
| T-08-01-04 | Denial of Service | Seed insertion | mitigate | `ON CONFLICT (name) DO NOTHING` — un re-run de migration n'échoue pas, ne crée pas de doublon. |
| T-08-01-05 | Elevation of Privilege | granola_user_connections | mitigate | Pas de colonne `is_admin` ou équivalent — la table ne porte AUCUN privilège, juste une clé chiffrée. Le seul usage est par le poller granola-sync qui décrypte pour appeler l'API Granola du user. |
| T-08-01-06 | Spoofing | UNIQUE(user_id) constraint | mitigate | Empêche un attaquant ayant compromis une session user d'enregistrer plusieurs clés Granola sous le même user_id (Pitfall 3 RESEARCH.md). |
</threat_model>

<verification>
- Migration file parses as valid Python (Task 1 acceptance)
- After Task 2 : `alembic_version=0012` + 2 tables présentes + meeting-recap seedé
- Aucune autre migration n'est appliquée par effet de bord
- Re-run de `alembic upgrade head` reste idempotent (le seed n'insère pas de doublon)
</verification>

<success_criteria>
- [ ] Fichier `apps/memory-api/alembic/versions/0012_granola_user_agents.py` créé avec les schémas D1+D4
- [ ] Migration applique upgrade() sans erreur sur PostgreSQL
- [ ] `granola_user_connections` table existe avec FK CASCADE vers users + UNIQUE user_id
- [ ] `agent_definitions` table existe avec UNIQUE name + tools_json JSONB nullable
- [ ] Agent `meeting-recap` seedé avec auto_trigger=true et system_prompt non vide
- [ ] `alembic_version` = `0012` après upgrade head
- [ ] Re-run idempotent : seconde exécution ne lève pas d'erreur de duplicate
</success_criteria>

<output>
After completion, create `.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-01-SUMMARY.md` documenting:
- Migration file path
- Tables created (granola_user_connections, agent_definitions)
- Constraints created (FK, UNIQUE)
- Whether docker compose exec ran on VM or marker file was used
- Final alembic_version value (0012 expected)
</output>
