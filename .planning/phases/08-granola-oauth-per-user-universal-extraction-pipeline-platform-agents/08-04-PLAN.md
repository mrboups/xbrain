---
phase: 08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents
plan: 04
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - apps/granola-sync/app/granola_poller.py
  - apps/granola-sync/app/memory_client.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "granola-sync poll-loop interroge granola_user_connections en plus de granola_integrations à chaque tick"
    - "Pour chaque user enabled, le poller décrypte api_key_enc, fetch les notes Granola, ingère via /v1/integrations/granola/ingest"
    - "last_polled_at est UPDATE BEFORE _fetch_notes (Pitfall 5 — at-most-once delivery + dedup note_id = exactly-once-effective)"
    - "Après ingestion réussie d'un meeting, si l'agent meeting-recap a auto_trigger=true, le poller appelle POST /v1/agents/{id}/invoke avec bridge JWT"
    - "Erreur 401/403 Granola → log.warning + skip (plan insuffisant), autres users continuent"
  artifacts:
    - path: "apps/granola-sync/app/granola_poller.py"
      provides: "Deuxième boucle per-user dans run_poll_loop, fonction _process_user_connection"
      contains: "_process_user_connection"
    - path: "apps/granola-sync/app/memory_client.py"
      provides: "Helper post_agent_invoke pour appeler /v1/agents/{id}/invoke"
      contains: "post_agent_invoke"
  key_links:
    - from: "granola_poller.run_poll_loop"
      to: "granola_user_connections"
      via: "SELECT id, user_id, api_key_enc, last_polled_at, team_scope FROM granola_user_connections WHERE enabled = true"
      pattern: "FROM granola_user_connections WHERE enabled"
    - from: "_process_user_connection"
      to: "/v1/agents/{id}/invoke"
      via: "post_agent_invoke after successful ingest if auto_trigger"
      pattern: "post_agent_invoke"
    - from: "memory_client.post_agent_invoke"
      to: "memory-api /v1/agents/{id}/invoke"
      via: "bridge JWT + body {content, team_scope, source_ref}"
      pattern: "/v1/agents/.*/invoke"
---

<objective>
Étendre granola-sync pour ajouter une deuxième boucle de polling per-user (D2 + D5) :

1. Ajouter `_process_user_connection(conn, row)` dans `granola_poller.py` — miroir de `_process_team_integration` mais pour `granola_user_connections`.
2. Modifier `run_poll_loop()` pour itérer sur les deux tables à chaque tick.
3. Après chaque ingestion réussie, si l'agent `meeting-recap` a `auto_trigger=true` ET `enabled=true`, appeler `POST /v1/agents/{id}/invoke` (D5 auto-trigger).
4. Ajouter `post_agent_invoke()` dans `memory_client.py` — helper bridge JWT pour appeler le nouvel endpoint invoke.

Purpose: Implémente D2 (deuxième boucle cohabitante per-user) + D5 (auto-trigger meeting-recap après chaque meeting ingéré). Le pattern at-most-once delivery via UPDATE last_polled_at BEFORE fetch (Pitfall 5) est conservé — combiné avec dedup note_id Phase 7, exactly-once-effective.

Output: Service granola-sync rebuild → poll-loop traite les deux tables → meeting-recap auto-déclenché.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-CONTEXT.md
@.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-RESEARCH.md

@apps/granola-sync/app/granola_poller.py
@apps/granola-sync/app/memory_client.py
@apps/granola-sync/app/extractor.py
@apps/granola-sync/app/config.py

<interfaces>
<!-- Patterns existants à reproduire pour la boucle per-user. -->

From apps/granola-sync/app/granola_poller.py:
```python
async def _process_team_integration(conn: asyncpg.Connection, row: asyncpg.Record) -> None:
    """Existing pattern — UPDATE last_polled_at BEFORE _fetch_notes (T-07-08-11)."""
    integration_id = row["id"]
    team_scope = row["team_scope"]
    api_key_enc = row["api_key_enc"]
    last_polled_at: datetime | None = row["last_polled_at"]

    try:
        api_key = _decrypt_api_key(api_key_enc)
    except (InvalidToken, RuntimeError) as exc:
        log.error("granola.decrypt_failed", team=team_scope, error=str(exc))
        return

    new_polled_at = datetime.now(timezone.utc)
    await conn.execute(
        "UPDATE granola_integrations SET last_polled_at = $1, updated_at = now() WHERE id = $2",
        new_polled_at, integration_id,
    )

    try:
        notes = await _fetch_notes(api_key, last_polled_at)
    except httpx.HTTPStatusError as exc:
        if exc.response.status_code in (401, 403):
            log.warning("granola.fetch_unauthorized", team=team_scope, status=exc.response.status_code)
            return
        log.error("granola.fetch_http_error", team=team_scope, status=exc.response.status_code)
        return
    # ... extract + post_ingest ...
```

From apps/granola-sync/app/memory_client.py:
```python
def _make_bridge_jwt() -> str:
    payload = {"sub": "granola-sync", "scope": "bridge", "iat": int(time.time()), "exp": int(time.time()) + 3600}
    return jose_jwt.encode({"alg": settings.JWT_ALGORITHM}, payload, settings.BRIDGE_SHARED_SECRET).decode()

async def post_ingest(team_scope: str, payload: dict[str, Any]) -> dict[str, Any] | None:
    """POST to /v1/integrations/granola/ingest with bridge JWT + X-Team-Scope header. Fail-soft."""
    ...
```

From apps/memory-api/app/routes/agents.py (the endpoint we'll call):
```python
# POST /v1/agents/{agent_id}/invoke
# Body: {"content": str, "team_scope": str, "source_ref": str | None, "project_scope": str | None}
# Auth: bridge JWT accepted (kind=bridge OR kind=user)
# Response: {"memory_item_id": str, "recap": str, "agent_name": str}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter post_agent_invoke() dans memory_client.py</name>
  <files>apps/granola-sync/app/memory_client.py</files>
  <read_first>
    - apps/granola-sync/app/memory_client.py (état actuel — pattern post_ingest à reproduire)
    - apps/granola-sync/app/config.py (vérifier settings.MEMORY_API_URL, BRIDGE_SHARED_SECRET, JWT_ALGORITHM)
  </read_first>
  <action>
Ajouter une nouvelle fonction `post_agent_invoke()` à la fin de `apps/granola-sync/app/memory_client.py`, juste après `post_ingest()`. Le contenu final du fichier doit être :

```python
"""HTTP client posting structured Granola payloads to memory-api /v1/integrations/granola/ingest + agent invocation /v1/agents/{id}/invoke."""

import time
from typing import Any

import httpx
import structlog
from authlib.jose import jwt as jose_jwt

from app.config import settings

log = structlog.get_logger(__name__)


def _make_bridge_jwt() -> str:
    """Generate a short-lived bridge JWT for service-to-service auth."""
    payload = {
        "sub": "granola-sync",
        "scope": "bridge",
        "iat": int(time.time()),
        "exp": int(time.time()) + 3600,
    }
    return jose_jwt.encode(
        {"alg": settings.JWT_ALGORITHM},
        payload,
        settings.BRIDGE_SHARED_SECRET,
    ).decode()


async def post_ingest(team_scope: str, payload: dict[str, Any]) -> dict[str, Any] | None:
    """POST a structured Granola payload to memory-api. Fail-soft (returns None on error)."""
    token = _make_bridge_jwt()
    url = f"{settings.MEMORY_API_URL}/v1/integrations/granola/ingest"
    body = {
        "team_scope": team_scope,
        **payload,  # contains "note" + "extracted"
    }
    try:
        async with httpx.AsyncClient(timeout=60.0) as client:
            resp = await client.post(
                url,
                json=body,
                headers={
                    "Authorization": f"Bearer {token}",
                    "X-Team-Scope": team_scope,
                    "Content-Type": "application/json",
                },
            )
        if resp.status_code != 201:
            log.error(
                "ingest.non_201",
                team_scope=team_scope,
                status=resp.status_code,
                body=resp.text[:500],
            )
            return None
        return resp.json()
    except Exception as exc:
        log.error("ingest.exception", team_scope=team_scope, error=str(exc))
        return None


async def post_agent_invoke(
    agent_id: str,
    team_scope: str,
    content: str,
    source_ref: str | None = None,
    project_scope: str | None = None,
) -> dict[str, Any] | None:
    """POST /v1/agents/{agent_id}/invoke with bridge JWT. Fail-soft (returns None on error).

    Used by granola-sync to auto-trigger the meeting-recap agent after ingesting
    a meeting (D5 RESEARCH.md). Pitfall 1: bridge JWT carries no team_scope,
    we send team_scope in the body and the agents.py invoke handler reads it from there.
    """
    token = _make_bridge_jwt()
    url = f"{settings.MEMORY_API_URL}/v1/agents/{agent_id}/invoke"
    body = {
        "team_scope": team_scope,
        "content": content[:50000],  # cap content to match agents.py limit
        "source_ref": source_ref,
        "project_scope": project_scope,
    }
    try:
        async with httpx.AsyncClient(timeout=120.0) as client:
            resp = await client.post(
                url,
                json=body,
                headers={
                    "Authorization": f"Bearer {token}",
                    "Content-Type": "application/json",
                },
            )
        if resp.status_code != 200:
            log.error(
                "agent_invoke.non_200",
                agent_id=agent_id,
                team_scope=team_scope,
                status=resp.status_code,
                body=resp.text[:500],
            )
            return None
        return resp.json()
    except Exception as exc:
        log.error(
            "agent_invoke.exception",
            agent_id=agent_id,
            team_scope=team_scope,
            error=str(exc),
        )
        return None
```

Notes implémentation :
- Le timeout est plus long (120s) que `post_ingest` (60s) car l'invocation Anthropic peut prendre plus de temps qu'un INSERT DB.
- Pas de `X-Team-Scope` header sur invoke — Pitfall 1 RESEARCH.md option A : team_scope passé dans le body, le handler agents.py le lit depuis là.
- Fail-soft : retourne None sur toute erreur HTTP ou exception — la boucle granola-sync continue avec les autres meetings/users.
- `content[:50000]` cap aligné avec la limite Pydantic dans `agents.py` (max_length=200000 mais on cap conservativement à 50k pour réduire les coûts Anthropic).
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/granola-sync/app/memory_client.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def post_ingest" apps/granola-sync/app/memory_client.py` retourne 1 (existant conservé).
    - `grep -c "async def post_agent_invoke" apps/granola-sync/app/memory_client.py` retourne 1 (nouveau).
    - `grep -c "/v1/agents/" apps/granola-sync/app/memory_client.py` retourne 1.
    - `grep -c "/invoke" apps/granola-sync/app/memory_client.py` retourne 1.
    - `grep -c "_make_bridge_jwt" apps/granola-sync/app/memory_client.py` retourne au moins 3 (1 def + 2 usages dans post_ingest et post_agent_invoke).
    - `grep -c "X-Team-Scope" apps/granola-sync/app/memory_client.py` retourne 1 (uniquement dans post_ingest, PAS dans post_agent_invoke — Pitfall 1).
    - Le fichier parse comme Python valide.
  </acceptance_criteria>
  <done>memory_client.py contient les deux helpers (post_ingest existant + post_agent_invoke nouveau), prêt à être importé par granola_poller.py.</done>
</task>

<task type="auto">
  <name>Task 2: Étendre granola_poller.py avec _process_user_connection + auto-trigger meeting-recap</name>
  <files>apps/granola-sync/app/granola_poller.py</files>
  <read_first>
    - apps/granola-sync/app/granola_poller.py (état actuel — pattern _process_team_integration EXACT à dupliquer/adapter)
    - apps/granola-sync/app/memory_client.py (helpers post_ingest + post_agent_invoke disponibles)
    - apps/granola-sync/app/extractor.py (pour comprendre l'usage de extract_from_summary)
    - .planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-RESEARCH.md (Pitfall 5 — UPDATE BEFORE fetch obligatoire)
  </read_first>
  <action>
Modifier `apps/granola-sync/app/granola_poller.py` pour :

1. **Mettre à jour l'import** en haut du fichier :
   - Avant : `from app.memory_client import post_ingest`
   - Après : `from app.memory_client import post_agent_invoke, post_ingest`

2. **Ajouter une nouvelle fonction `_get_meeting_recap_agent` et `_process_user_connection` AVANT `run_poll_loop`** (juste après `_process_team_integration`).

3. **Modifier `run_poll_loop`** pour ajouter une seconde fetch + boucle après les team integrations (mais avant le `SENTINEL_PATH.touch()`).

État cible du fichier (full replacement) :

```python
"""Granola polling loop — fetches new notes per integrated team AND per user (Phase 8 D2), extracts structured content via Claude (07-05 extractor), posts to memory-api (07-05 memory_client). Auto-triggers meeting-recap agent (Phase 8 D5)."""

import asyncio
import pathlib
import random
from datetime import datetime, timezone
from typing import Any

import asyncpg
import httpx
import structlog
from cryptography.fernet import Fernet, InvalidToken

from app.config import settings
from app.extractor import extract_from_summary
from app.memory_client import post_agent_invoke, post_ingest

log = structlog.get_logger(__name__)

SENTINEL_PATH = pathlib.Path("/tmp/granola-sync-alive")


def _decrypt_api_key(enc: str) -> str:
    """Decrypt Fernet-encrypted Granola API key. Raises InvalidToken on tamper/wrong key."""
    if not settings.FERNET_KEY:
        raise RuntimeError("FERNET_KEY not set in granola-sync env")
    f = Fernet(settings.FERNET_KEY.encode())
    return f.decrypt(enc.encode()).decode()


async def _fetch_notes(
    api_key: str,
    created_after: datetime | None,
) -> list[dict[str, Any]]:
    """Fetch new notes from Granola API. Returns list of note dicts.

    Handles:
    - Pagination via cursor (hard cap 20 iterations T-07-08-08)
    - 429 rate-limit with exponential backoff (max 5 attempts, max wait 64s)
    - 5xx with exponential backoff (max 5 attempts, max wait 64s)
    - 401/403 (plan insuffisant): raises httpx.HTTPStatusError — caller logs and skips

    Security: Authorization header value is NEVER logged (T-07-08-01).
    """
    headers = {"Authorization": f"Bearer {api_key}"}
    params: dict[str, Any] = {}
    if created_after is not None:
        params["created_after"] = created_after.astimezone(timezone.utc).isoformat()

    notes: list[dict[str, Any]] = []
    cursor: str | None = None

    async with httpx.AsyncClient(timeout=30.0, base_url=settings.GRANOLA_API_BASE) as client:
        for _ in range(20):  # safety cap on pagination (T-07-08-08)
            q = dict(params)
            if cursor:
                q["cursor"] = cursor
            resp = None
            for attempt in range(5):
                try:
                    resp = await client.get("/v1/notes", headers=headers, params=q)
                    if resp.status_code == 429:
                        wait = min(2**attempt + random.random(), 64)
                        log.warning("granola.rate_limited", wait=round(wait, 1))
                        await asyncio.sleep(wait)
                        continue
                    resp.raise_for_status()
                    break
                except httpx.HTTPStatusError as exc:
                    if exc.response.status_code in (500, 502, 503, 504):
                        wait = min(2**attempt + random.random(), 64)
                        log.warning(
                            "granola.5xx_retry",
                            status=exc.response.status_code,
                            wait=round(wait, 1),
                        )
                        await asyncio.sleep(wait)
                        continue
                    raise
            else:
                raise RuntimeError("Granola API: max retries exceeded")

            if resp is None:
                raise RuntimeError("Granola API: response missing after retry loop")

            data = resp.json()
            notes.extend(data.get("items", []) or [])
            if not data.get("has_more"):
                break
            cursor = data.get("next_cursor")
            if not cursor:
                break

    return notes


async def _get_meeting_recap_agent(conn: asyncpg.Connection) -> dict[str, Any] | None:
    """Return meeting-recap agent row if enabled AND auto_trigger, else None.

    Cached per-tick: caller fetches once at top of run_poll_loop iteration.
    Phase 8 D5: granola-sync auto-invokes meeting-recap after each successful ingest
    if the agent has auto_trigger=true and enabled=true.
    """
    row = await conn.fetchrow(
        "SELECT id::text AS id, name, enabled, auto_trigger "
        "FROM agent_definitions WHERE name = 'meeting-recap' LIMIT 1"
    )
    if row is None:
        return None
    if not row["enabled"] or not row["auto_trigger"]:
        return None
    return dict(row)


async def _maybe_invoke_recap(
    recap_agent: dict[str, Any] | None,
    team_scope: str,
    summary: str,
    note_id: str,
) -> None:
    """Fire-and-forget invocation of meeting-recap agent. Fail-soft."""
    if recap_agent is None:
        return
    if not summary or len(summary) < 40:
        return
    try:
        result = await post_agent_invoke(
            agent_id=recap_agent["id"],
            team_scope=team_scope,
            content=summary,
            source_ref=note_id,
        )
        if result:
            log.info(
                "granola.recap_triggered",
                team=team_scope,
                note_id=note_id,
                memory_item_id=result.get("memory_item_id"),
            )
    except Exception as exc:
        log.warning(
            "granola.recap_skipped",
            team=team_scope,
            note_id=note_id,
            error=str(exc),
        )


async def _process_team_integration(
    conn: asyncpg.Connection,
    row: asyncpg.Record,
    recap_agent: dict[str, Any] | None,
) -> None:
    """Poll Granola for one team integration and ingest new notes into memory-api."""
    integration_id = row["id"]
    team_scope = row["team_scope"]
    api_key_enc = row["api_key_enc"]
    last_polled_at: datetime | None = row["last_polled_at"]

    try:
        api_key = _decrypt_api_key(api_key_enc)
    except (InvalidToken, RuntimeError) as exc:
        log.error("granola.decrypt_failed", team=team_scope, error=str(exc))
        return

    # CRITICAL: Persist last_polled_at BEFORE processing — idempotent on crash restart (T-07-08-11).
    # On crash, restart picks up from this cursor. Combined with note-level dedup in 07-04 → exactly-once-effective.
    new_polled_at = datetime.now(timezone.utc)
    await conn.execute(
        "UPDATE granola_integrations SET last_polled_at = $1, updated_at = now() WHERE id = $2",
        new_polled_at,
        integration_id,
    )

    try:
        notes = await _fetch_notes(api_key, last_polled_at)
    except httpx.HTTPStatusError as exc:
        if exc.response.status_code in (401, 403):
            # Fail-soft: plan Granola insuffisant — log warning only, other teams continue (T-07-08-06)
            log.warning(
                "granola.fetch_unauthorized",
                team=team_scope,
                status=exc.response.status_code,
                hint="Granola Business/Enterprise plan required for API access",
            )
            return
        log.error("granola.fetch_http_error", team=team_scope, status=exc.response.status_code)
        return
    except Exception as exc:
        log.error("granola.fetch_failed", team=team_scope, error=str(exc))
        return

    log.info("granola.poll.fetched", team=team_scope, count=len(notes), since=last_polled_at)

    for note in notes:
        try:
            summary = note.get("summary_text") or note.get("summary_markdown") or ""
            attendees_raw = note.get("attendees") or []
            fallback_participants = [
                {"name": a.get("name"), "email": a.get("email")}
                for a in attendees_raw
                if a.get("name") or a.get("email")
            ]
            extracted = await extract_from_summary(summary, fallback_attendees=fallback_participants)

            payload = {
                "note": {
                    "id": note.get("id"),
                    "title": note.get("title"),
                    "summary_text": note.get("summary_text"),
                    "summary_markdown": note.get("summary_markdown"),
                    "web_url": note.get("web_url"),
                    "created_at": note.get("created_at"),
                    "attendees": fallback_participants,
                },
                "extracted": extracted,
            }
            result = await post_ingest(team_scope, payload)
            if result:
                log.info(
                    "granola.ingested",
                    team=team_scope,
                    note_id=note.get("id"),
                    contacts=result.get("contacts_upserted"),
                    tasks=result.get("tasks_created"),
                )
                # Phase 8 D5 — auto-trigger meeting-recap if agent enabled
                await _maybe_invoke_recap(
                    recap_agent=recap_agent,
                    team_scope=team_scope,
                    summary=summary,
                    note_id=note.get("id") or "",
                )
        except Exception as exc:
            log.error("granola.note_failed", team=team_scope, note_id=note.get("id"), error=str(exc))


async def _process_user_connection(
    conn: asyncpg.Connection,
    row: asyncpg.Record,
    recap_agent: dict[str, Any] | None,
) -> None:
    """Phase 8 D2 — Poll Granola for one per-user connection and ingest new notes.

    Mirror of _process_team_integration, but reads from granola_user_connections.
    Same at-most-once-then-exactly-once-effective pattern (UPDATE last_polled_at BEFORE fetch).
    """
    connection_id = row["id"]
    user_id = row["user_id"]
    team_scope = row["team_scope"]
    api_key_enc = row["api_key_enc"]
    last_polled_at: datetime | None = row["last_polled_at"]

    try:
        api_key = _decrypt_api_key(api_key_enc)
    except (InvalidToken, RuntimeError) as exc:
        log.error(
            "granola.user.decrypt_failed",
            user_id=str(user_id),
            team=team_scope,
            error=str(exc),
        )
        return

    # Pitfall 5 RESEARCH.md: UPDATE last_polled_at BEFORE _fetch_notes — at-most-once delivery.
    new_polled_at = datetime.now(timezone.utc)
    await conn.execute(
        "UPDATE granola_user_connections SET last_polled_at = $1, updated_at = now() WHERE id = $2",
        new_polled_at,
        connection_id,
    )

    try:
        notes = await _fetch_notes(api_key, last_polled_at)
    except httpx.HTTPStatusError as exc:
        if exc.response.status_code in (401, 403):
            log.warning(
                "granola.user.fetch_unauthorized",
                user_id=str(user_id),
                team=team_scope,
                status=exc.response.status_code,
                hint="User's Granola plan does not allow API access OR key is invalid",
            )
            return
        log.error(
            "granola.user.fetch_http_error",
            user_id=str(user_id),
            team=team_scope,
            status=exc.response.status_code,
        )
        return
    except Exception as exc:
        log.error(
            "granola.user.fetch_failed",
            user_id=str(user_id),
            team=team_scope,
            error=str(exc),
        )
        return

    log.info(
        "granola.user.poll.fetched",
        user_id=str(user_id),
        team=team_scope,
        count=len(notes),
        since=last_polled_at,
    )

    for note in notes:
        try:
            summary = note.get("summary_text") or note.get("summary_markdown") or ""
            attendees_raw = note.get("attendees") or []
            fallback_participants = [
                {"name": a.get("name"), "email": a.get("email")}
                for a in attendees_raw
                if a.get("name") or a.get("email")
            ]
            extracted = await extract_from_summary(summary, fallback_attendees=fallback_participants)

            payload = {
                "note": {
                    "id": note.get("id"),
                    "title": note.get("title"),
                    "summary_text": note.get("summary_text"),
                    "summary_markdown": note.get("summary_markdown"),
                    "web_url": note.get("web_url"),
                    "created_at": note.get("created_at"),
                    "attendees": fallback_participants,
                },
                "extracted": extracted,
            }
            result = await post_ingest(team_scope, payload)
            if result:
                log.info(
                    "granola.user.ingested",
                    user_id=str(user_id),
                    team=team_scope,
                    note_id=note.get("id"),
                    contacts=result.get("contacts_upserted"),
                    tasks=result.get("tasks_created"),
                )
                # Phase 8 D5 — auto-trigger meeting-recap for per-user meetings too
                await _maybe_invoke_recap(
                    recap_agent=recap_agent,
                    team_scope=team_scope,
                    summary=summary,
                    note_id=note.get("id") or "",
                )
        except Exception as exc:
            log.error(
                "granola.user.note_failed",
                user_id=str(user_id),
                team=team_scope,
                note_id=note.get("id"),
                error=str(exc),
            )


async def run_poll_loop(database_url: str) -> None:
    """Main polling loop. Runs until cancelled.

    Two cohabiting loops per tick (Phase 8 D2):
      - Team integrations (granola_integrations) — Phase 7
      - Per-user connections (granola_user_connections) — Phase 8

    Resilient: per-row errors are caught and logged; other rows continue.
    Loop-level errors are caught and logged; loop continues after sleep.
    Sentinel file /tmp/granola-sync-alive is touched after each successful tick.
    """
    pg_url = database_url.replace("postgresql+asyncpg://", "postgresql://").replace(
        "postgres+asyncpg://", "postgresql://"
    )
    pool = await asyncpg.create_pool(pg_url, min_size=1, max_size=2)
    log.info("poll_loop.started", interval=settings.GRANOLA_POLL_INTERVAL_SECONDS)

    while True:
        try:
            async with pool.acquire() as conn:
                # Lookup meeting-recap agent once per tick (Phase 8 D5)
                recap_agent = await _get_meeting_recap_agent(conn)

                # Loop 1: team integrations (Phase 7, unchanged behavior)
                team_rows = await conn.fetch(
                    "SELECT id, team_scope, api_key_enc, last_polled_at "
                    "FROM granola_integrations ORDER BY last_polled_at NULLS FIRST"
                )
                for row in team_rows:
                    try:
                        await _process_team_integration(conn, row, recap_agent)
                    except Exception as exc:
                        log.error("poll.team_error", team=row["team_scope"], error=str(exc))

                # Loop 2: per-user connections (Phase 8 D2)
                user_rows = await conn.fetch(
                    "SELECT id, user_id, team_scope, api_key_enc, last_polled_at "
                    "FROM granola_user_connections WHERE enabled = true "
                    "ORDER BY last_polled_at NULLS FIRST"
                )
                for row in user_rows:
                    try:
                        await _process_user_connection(conn, row, recap_agent)
                    except Exception as exc:
                        log.error(
                            "poll.user_error",
                            user_id=str(row["user_id"]),
                            team=row["team_scope"],
                            error=str(exc),
                        )
            SENTINEL_PATH.touch()
            log.info(
                "poll_loop.tick_complete",
                teams=len(team_rows),
                users=len(user_rows),
            )
        except Exception as exc:
            log.error("poll_loop.error", error=str(exc))

        await asyncio.sleep(settings.GRANOLA_POLL_INTERVAL_SECONDS)
```

Notes implémentation :
- Le helper `_get_meeting_recap_agent` est appelé une fois par tick (pas par note) — minimise les SELECTs sur agent_definitions. Le résultat est passé en paramètre à `_process_team_integration` et `_process_user_connection`.
- `_maybe_invoke_recap` est isolé dans une coroutine séparée — fail-soft, n'interrompt jamais la boucle d'ingest.
- La signature de `_process_team_integration` change pour accepter `recap_agent` — c'est un refactor compatible (l'auto-trigger est nouveau pour les team integrations aussi par cohérence avec D5).
- `granola_user_connections` filter `WHERE enabled = true` — les soft-deletes sont skippés.
- ORDER BY `last_polled_at NULLS FIRST` — les nouvelles connexions sont polled en premier.
- Les logs differentiate `granola.user.*` vs `granola.*` pour différencier les deux boucles dans les dashboards.
- Le seuil `len(summary) >= 40` dans `_maybe_invoke_recap` évite d'invoquer Anthropic sur des stubs vides.
  </action>
  <verify>
    <automated>cd D:/VSC/xbrain && python -c "import ast; ast.parse(open('apps/granola-sync/app/granola_poller.py').read()); print('parsed_ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def _process_team_integration" apps/granola-sync/app/granola_poller.py` retourne 1 (existant conservé).
    - `grep -c "async def _process_user_connection" apps/granola-sync/app/granola_poller.py` retourne 1 (nouveau).
    - `grep -c "async def _get_meeting_recap_agent" apps/granola-sync/app/granola_poller.py` retourne 1.
    - `grep -c "async def _maybe_invoke_recap" apps/granola-sync/app/granola_poller.py` retourne 1.
    - `grep -c "post_agent_invoke" apps/granola-sync/app/granola_poller.py` retourne au moins 1 (utilisé par _maybe_invoke_recap).
    - `grep -c "FROM granola_user_connections WHERE enabled = true" apps/granola-sync/app/granola_poller.py` retourne 1.
    - `grep -c "UPDATE granola_user_connections SET last_polled_at" apps/granola-sync/app/granola_poller.py` retourne 1 (Pitfall 5 — UPDATE BEFORE fetch).
    - `grep -c "FROM agent_definitions WHERE name = 'meeting-recap'" apps/granola-sync/app/granola_poller.py` retourne 1.
    - `grep -B 5 "await _fetch_notes(api_key" apps/granola-sync/app/granola_poller.py | grep -c "UPDATE granola"` retourne au moins 2 (Pitfall 5 — UPDATE précède CHAQUE _fetch_notes call : team + user).
    - Le fichier parse comme Python valide.
    - L'import en haut contient `from app.memory_client import post_agent_invoke, post_ingest` (deux helpers).
  </acceptance_criteria>
  <done>granola_poller.py poll les deux tables à chaque tick, auto-déclenche meeting-recap après chaque ingest réussi. Le service granola-sync nécessite un rebuild (`docker compose up -d --build granola-sync`) pour charger ces changements — à effectuer en plan 08-08 (verify) ou par l'orchestrateur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| granola-sync container → PostgreSQL | Read granola_user_connections, agent_definitions; UPDATE last_polled_at |
| granola-sync container → Granola API (external) | Bearer api_key in Authorization header |
| granola-sync container → memory-api (internal) | Bridge JWT (HS256, 1h expiry) |
| Decryption boundary | api_key_enc decrypted in-memory only — never persisted, never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | api_key after decryption | mitigate | api_key passed only to httpx.AsyncClient as Authorization header; NEVER logged. _decrypt_api_key returns string used immediately, no persistence. |
| T-08-04-02 | Denial of Service | One bad user blocks all others | mitigate | Per-row try/except in run_poll_loop — exception in _process_user_connection logs and continues to next user. |
| T-08-04-03 | Tampering | Stale last_polled_at on crash | mitigate | UPDATE last_polled_at BEFORE _fetch_notes (at-most-once) + dedup note_id in /v1/integrations/granola/ingest (Phase 7) = exactly-once-effective. |
| T-08-04-04 | Spoofing | Cross-user team_scope mismatch | accept | Le team_scope vient de la row granola_user_connections (DB authoritative). Le user qui a posé sa clé via plan 08-03 a fourni son team_scope ; pas de validation que l'user est encore membre du team au poll-time (Open Question 1 RESEARCH.md — accepted). |
| T-08-04-05 | Information Disclosure | Granola API rate-limit Bearer in logs | mitigate | _fetch_notes ne logge JAMAIS la value du Authorization header (T-07-08-01). Seul le status code est loggé. |
| T-08-04-06 | Repudiation | Auto-trigger sans audit | mitigate | Le invoke endpoint (plan 08-02) écrit `agent.invoked` dans audit_log avec actor_user_id=NULL pour les bridge calls. |
| T-08-04-07 | Tampering | Compromised FERNET_KEY rotation | accept | Si FERNET_KEY change, _decrypt_api_key échoue InvalidToken — le user voit "déconnecté" et doit re-soumettre sa clé (UX dégradée mais sécurité préservée). |
| T-08-04-08 | DoS | Anthropic spend explosion via auto-trigger | accept | Phase 8 : pas de quota par-team. Si un team ingère 100 meetings/h, 100 invoke calls = 100 Haiku calls. Phase 9+ ajoutera quota Langfuse-based. |
</threat_model>

<verification>
- granola_poller.py et memory_client.py parsent comme Python valide
- Les deux boucles (team + user) cohabitent dans run_poll_loop
- _process_user_connection est un miroir EXACT de _process_team_integration (sauf table source + log keys)
- UPDATE last_polled_at BEFORE _fetch_notes pour les DEUX boucles (Pitfall 5)
- _maybe_invoke_recap est appelé après chaque ingest réussi (les deux boucles)
- post_agent_invoke utilise bridge JWT, NO X-Team-Scope header (Pitfall 1)
</verification>

<success_criteria>
- [ ] `_process_user_connection` ajouté avec pattern at-most-once UPDATE BEFORE fetch
- [ ] `run_poll_loop` itère sur les deux tables à chaque tick
- [ ] `_get_meeting_recap_agent` interroge `agent_definitions` UNE fois par tick
- [ ] `_maybe_invoke_recap` invoke l'agent après chaque ingest réussi (team + user)
- [ ] `post_agent_invoke()` ajouté à memory_client.py avec bridge JWT
- [ ] Erreur 401/403 Granola → log.warning + return (n'interrompt pas la boucle)
- [ ] Service granola-sync rebuild + redémarrage charge ces changements
- [ ] Une row `granola_user_connections` enabled=true est polled au moins une fois (vérifiable dans logs `granola.user.poll.fetched`)
</success_criteria>

<output>
After completion, create `.planning/phases/08-granola-oauth-per-user-universal-extraction-pipeline-platform-agents/08-04-SUMMARY.md` documenting:
- Pattern at-most-once + dedup = exactly-once-effective
- Comment _get_meeting_recap_agent est mis en cache par tick (pas par note)
- Pourquoi pas X-Team-Scope sur post_agent_invoke (Pitfall 1)
- Note rebuild requis : `docker compose up -d --build granola-sync` après merge sur la VM
</output>
