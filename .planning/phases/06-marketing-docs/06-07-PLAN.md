---
phase: 06-marketing-docs
plan: "07"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - marketing-site/docs/deployment.html
  - marketing-site/docs/configuration.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "docs/deployment.html est un guide step-by-step complet pour déployer xbrain sur GCP"
    - "docs/configuration.html documente TOUTES les variables d'environnement avec descriptions"
    - "deployment.html couvre les 5 phases (Phase 1 à Phase 5) avec les prérequis VM par phase"
    - "configuration.html organise les variables par service avec types et valeurs par défaut"
    - "Les 2 pages suivent le template identique (sidebar 14 liens, breadcrumb, code-block)"
  artifacts:
    - path: "marketing-site/docs/deployment.html"
      provides: "Complete deployment guide"
      min_lines: 200
      contains: "docker compose up"
    - path: "marketing-site/docs/configuration.html"
      provides: "Complete environment variable reference"
      min_lines: 150
      contains: "POSTGRES_PASSWORD"
  key_links:
    - from: "marketing-site/docs/deployment.html"
      to: "docker compose up"
      via: "step 3 quickstart"
      pattern: "docker compose up"
    - from: "marketing-site/docs/configuration.html"
      to: "BRIDGE_SHARED_SECRET"
      via: "security section"
      pattern: "BRIDGE_SHARED_SECRET"
---

<objective>
Créer les 2 pages de documentation opérationnelle : Deployment et Configuration.

Purpose: Deployment est le guide pratique step-by-step pour mettre xbrain en production sur GCP — de la création de la VM jusqu'au verify script. Configuration est la référence exhaustive de toutes les variables d'environnement, organisées par service. Ces deux pages sont critiques pour l'adoption par les équipes tech.

Output: 2 fichiers HTML dans marketing-site/docs/ — les pages les plus orientées "ops" du site.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/VSC/xbrain/.planning/phases/06-marketing-docs/06-CONTEXT.md
@D:/VSC/xbrain/.planning/ROADMAP.md

Infrastructure réelle :
- VM GCP : e2-medium (Phase 1, 4GB), e2-standard-2 (Phase 2+, 8GB)
- OS : Ubuntu 24.04 LTS
- Docker 24+ + Docker Compose v2
- Nginx (80/443), Cloudflare DNS (optionnel)
- Port 80/443 ouverts dans firewall GCP
- Domaines utilisés : x.dejavu.cat (LibreChat), ai.dejavu.cat (Open WebUI), api.dejavu.cat (memory-api), obs.dejavu.cat (Langfuse)

Déploiement Phase 1 :
1. Créer VM GCP e2-medium Ubuntu 24.04
2. Installer Docker + Docker Compose
3. Cloner le repo
4. Configurer .env
5. docker compose up -d
6. Vérifier avec verify-phase1.sh

Séquence des phases :
- Phase 1 : 4GB VM — nginx, postgres, qdrant, memory-api, librechat, open-webui + bridges
- Phase 2 : upgrade VM 8GB — agent-runtime, langfuse + stack
- Phase 3 : neo4j, mcp-gateway, mcp-scraper, mcp-drive-read, mcp-calendar, drive-sync
- Phase 4 : mcp-deck, librechat bridge upgrades, webhooks
- Phase 5 : graphiti-service, chrome-extension (manuel), github-auth, projects-dashboard

Variables d'environnement clés (depuis docker-compose.yml) :
- POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB
- DATABASE_URL (postgres+asyncpg://...)
- QDRANT_URL, QDRANT_API_KEY
- GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
- BRIDGE_SHARED_SECRET (32 char hex random)
- JWT_ALGORITHM (HS256)
- ANTHROPIC_API_KEY, OPENAI_API_KEY, XAI_API_KEY
- LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY
- NEO4J_PASSWORD
- OAUTH_CREDENTIALS_ENCRYPTION_KEY (Fernet key)
- MEMORY_API_EXTERNAL_URL
- GITHUB_ORG, GITHUB_API_PAT
- GRAPHITI_SERVICE_URL
- ADMIN_USER_SUBS (comma-separated Google sub IDs)
- MEILI_MASTER_KEY
- PIPELINE_API_KEY
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/deployment.html — Guide de déploiement complet</name>
  <files>marketing-site/docs/deployment.html</files>
  <action>
Créer marketing-site/docs/deployment.html. Lien actif dans la sidebar : "Deployment".

Breadcrumb : Docs > Deployment

Contenu (sections h2) :

**Prerequisites**
Table :
| Requirement | Details |
|-------------|---------|
| Cloud VM | GCP e2-medium (4 GB, 2 vCPU) for Phase 1 |
| OS | Ubuntu 24.04 LTS |
| Docker | 24.0+ |
| Docker Compose | v2 (included with Docker Desktop or `apt install docker-compose-plugin`) |
| Domain (optional) | For HTTPS via Cloudflare |
| Google OAuth App | For SSO authentication |

Callout--info : "Phase 1 runs on a 4 GB VM (~25€/mo on GCP). Before starting Phase 2, upgrade to 8 GB (e2-standard-2, ~49€/mo)."

**Phase 1: Core Stack**

Step 1 — Create a GCP VM
```bash
# Create e2-medium VM (GCP Console or gcloud CLI)
gcloud compute instances create xbrain-vm \
  --zone=europe-west1-b \
  --machine-type=e2-medium \
  --image-family=ubuntu-2404-lts \
  --image-project=ubuntu-os-cloud \
  --boot-disk-size=50GB \
  --tags=http-server,https-server

# Open ports
gcloud compute firewall-rules create xbrain-http \
  --allow tcp:80,tcp:443 \
  --target-tags http-server,https-server
```

Step 2 — Install Docker
```bash
ssh user@YOUR_VM_IP

# Install Docker
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
newgrp docker

# Verify
docker --version  # Docker 24.x+
docker compose version  # v2.x
```

Step 3 — Clone and configure
```bash
git clone https://github.com/mrboups/xbrain.git
cd xbrain/infrastructure
cp .env.example .env
nano .env  # Fill in your values (see Configuration page)
```

Required .env values for Phase 1 :
```bash
POSTGRES_PASSWORD=<strong-random-password>
GOOGLE_CLIENT_ID=<from-google-cloud-console>
GOOGLE_CLIENT_SECRET=<from-google-cloud-console>
BRIDGE_SHARED_SECRET=$(openssl rand -hex 32)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
MEILI_MASTER_KEY=$(openssl rand -hex 32)
PIPELINE_API_KEY=$(openssl rand -hex 32)
```

Step 4 — Start Phase 1
```bash
docker compose up -d

# Monitor startup (takes ~60s for all healthchecks)
docker compose ps
watch docker compose ps  # Wait for all to show "healthy"
```

Step 5 — Configure nginx
```bash
# Edit nginx config with your VM IP or domain
nano infrastructure/nginx/conf.d/10-xbrain.conf
# Restart nginx
docker compose restart nginx
```

Step 6 — Verify Phase 1
```bash
bash infrastructure/scripts/verify-phase1.sh
# Expected: PASS: 6 / 6
```

Callout--info : "Access LibreChat at http://YOUR_VM_IP. Sign in with Google — the email must belong to a configured team scope."

**Phase 2: Intelligent Memory**

"Before starting Phase 2, upgrade your VM to e2-standard-2 (8 GB RAM)."

```bash
# GCP Console: stop VM → Edit → Change machine type → e2-standard-2 → Start
# Or via gcloud:
gcloud compute instances stop xbrain-vm
gcloud compute instances set-machine-type xbrain-vm --machine-type=e2-standard-2
gcloud compute instances start xbrain-vm
```

Add to .env :
```bash
LANGFUSE_PUBLIC_KEY=<from-langfuse-setup>
LANGFUSE_SECRET_KEY=<from-langfuse-setup>
```

```bash
# Phase 2 services are already in docker-compose.yml
docker compose up -d
bash infrastructure/scripts/verify-phase2.sh
```

**Phase 3: Graph + Integrations**

Add to .env :
```bash
NEO4J_PASSWORD=<strong-password>
OAUTH_CREDENTIALS_ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
GOOGLE_CLIENT_SECRET=<your-google-client-secret>
```

```bash
docker compose up -d
bash infrastructure/scripts/register-mcp-tools.sh
bash infrastructure/scripts/verify-phase3.sh
```

**Phase 4: MCP Completion**

No new env vars required. Rebuild and restart:
```bash
docker compose build mcp-deck
docker compose up -d
bash infrastructure/scripts/verify-phase4.sh
# Expected: PASS: 8 / 8
```

**Phase 5: Team Platform**

Add to .env :
```bash
GITHUB_ORG=your-github-org
GITHUB_API_PAT=ghp_...  # read:org scope
GRAPHITI_SERVICE_URL=http://graphiti-service:8300
```

```bash
docker compose up -d
bash infrastructure/scripts/verify-phase5.sh
# Expected: PASS: 8 / 8
```

**HTTPS with Cloudflare**

Callout--info : "Cloudflare provides free HTTPS with their DNS proxy. Add your domain, point A record to VM IP, enable proxy (orange cloud)."

```nginx
# nginx/conf.d/10-xbrain.conf — key section
server {
  listen 80;
  server_name x.yourdomain.com;
  location / {
    proxy_pass http://librechat:3080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
```

**Backups**

```bash
# Backup all data
bash infrastructure/scripts/backup.sh

# Restore (on fresh VM)
bash infrastructure/scripts/restore.sh backup_20260501.tar.gz
```

**Monitoring VM resources**

```bash
# Check container memory usage
docker stats --no-stream

# Check disk space
df -h

# If disk >90%, clean Docker artifacts
docker system prune --volumes -f
```

Callout--warning : "The VM disk can fill up quickly with Docker image layers. If you hit >95% disk usage, run docker system prune or expand the disk before adding new services."
  </action>
  <verify>
    <automated>grep -c "docker compose up\|Step [0-9]\|Phase [0-9]" "D:/VSC/xbrain/marketing-site/docs/deployment.html"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "docker compose up" marketing-site/docs/deployment.html
    - grep -q "verify-phase1.sh" marketing-site/docs/deployment.html
    - grep -q "e2-medium" marketing-site/docs/deployment.html
    - grep -q "e2-standard-2" marketing-site/docs/deployment.html
    - grep -q "BRIDGE_SHARED_SECRET" marketing-site/docs/deployment.html
    - grep -q "Cloudflare\|nginx" marketing-site/docs/deployment.html
    - grep -c "code-block" marketing-site/docs/deployment.html retourne >= 5
    - Fichier > 200 lignes
  </acceptance_criteria>
  <done>docs/deployment.html : guide complet Phase 1-5, VM creation GCP, Docker install, .env config, nginx HTTPS, backups, monitoring</done>
</task>

<task type="auto">
  <name>Task 2: docs/configuration.html — Référence variables d'environnement</name>
  <files>marketing-site/docs/configuration.html</files>
  <action>
Créer marketing-site/docs/configuration.html. Lien actif dans la sidebar : "Configuration".

Breadcrumb : Docs > Configuration

**Overview**
"All xbrain services are configured via environment variables in infrastructure/.env. An .env.example is provided with safe defaults and placeholder values. Never commit your .env file to git."

Callout--warning : "Create infrastructure/.env from .env.example and fill in your values. This file is gitignored."

```bash
cp infrastructure/.env.example infrastructure/.env
```

---

**Core Database**

Table `.docs-table` (colonnes : Variable | Required | Default | Description) :

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| POSTGRES_USER | Yes | xbrain | PostgreSQL username |
| POSTGRES_PASSWORD | Yes | — | PostgreSQL password (use strong random) |
| POSTGRES_DB | Yes | xbrain | PostgreSQL database name |
| DATABASE_URL | Yes | — | Full DSN: postgres+asyncpg://user:pass@postgres:5432/db |

---

**memory-api**

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| QDRANT_URL | Yes | http://qdrant:6333 | Qdrant vector store URL |
| QDRANT_API_KEY | No | (empty) | Qdrant API key (if enabled) |
| BRIDGE_SHARED_SECRET | Yes | — | 32-char hex secret shared with bridges. Generate: `openssl rand -hex 32` |
| JWT_ALGORITHM | No | HS256 | JWT signing algorithm |
| LOG_LEVEL | No | INFO | Logging level (DEBUG, INFO, WARNING, ERROR) |
| ADMIN_USER_SUBS | No | (empty) | Comma-separated Google sub IDs of admin users |
| MEMORY_API_EXTERNAL_URL | No | https://x.dejavu.cat | Public URL (used for Drive webhook callbacks) |
| GOOGLE_CLIENT_ID | Yes | — | Google OAuth client ID |
| GOOGLE_CLIENT_SECRET | No | — | Google OAuth secret (required for Drive sync) |
| OAUTH_CREDENTIALS_ENCRYPTION_KEY | No | — | Fernet key for encrypting Drive credentials. Generate: `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"` |
| NEO4J_URI | No | bolt://neo4j:7687 | Neo4j connection (Phase 3+) |
| NEO4J_USER | No | neo4j | Neo4j username |
| NEO4J_PASSWORD | No | — | Neo4j password (Phase 3+) |
| GRAPHITI_SERVICE_URL | No | http://graphiti-service:8300 | Graphiti service URL (Phase 5) |
| GITHUB_ORG | No | your-github-org | GitHub Org name for membership check |
| GITHUB_API_PAT | No | — | GitHub PAT with read:org scope |

---

**LibreChat**

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| ANTHROPIC_API_KEY | No | — | Claude models access |
| OPENAI_API_KEY | No | — | GPT models access |
| XAI_API_KEY | No | — | Grok (xAI) models access |
| MEILI_MASTER_KEY | Yes | — | MeiliSearch master key (32+ chars) |
| LIBRECHAT_MONGO_URI | Yes | — | MongoDB URI for LibreChat |

---

**Bridge & Pipeline**

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| MEMORY_API_URL | No | http://memory-api:8000 | Internal memory-api URL |
| BRIDGE_DEFAULT_TEAM_SCOPE | No | default | Default team scope for bridge writes |
| BRIDGE_BACKFILL_FROM | No | startup | Backfill strategy (startup or never) |
| PIPELINE_API_KEY | Yes | — | Open WebUI pipeline API key. Generate: `openssl rand -hex 32` |
| PIPELINE_DEFAULT_TEAM_SCOPE | No | default | Default team scope for pipeline writes |
| AGENT_RUNTIME_URL | No | http://agent-runtime:9100 | agent-runtime URL for pipeline |

---

**agent-runtime**

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| MCP_GATEWAY_URL | No | http://mcp-gateway:8080 | MCP gateway URL |
| MCP_TOOL_CACHE_TTL_SECS | No | 300 | MCP tool list cache TTL (seconds) |

---

**Langfuse (Phase 2)**

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| LANGFUSE_PUBLIC_KEY | No | — | Langfuse public key (from Langfuse UI) |
| LANGFUSE_SECRET_KEY | No | — | Langfuse secret key |

Callout--info : "LANGFUSE_HOST in docker-compose.yml is hardcoded to http://langfuse:3000 (internal Docker DNS). This bypasses Cloudflare for SDK calls — do not change this."

---

**MCP Gateway (Phase 3)**

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| MCP_GATEWAY_URL | No | http://mcp-gateway:8080 | Gateway URL (used by agent-runtime) |

---

**Graphiti (Phase 5)**

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| GRAPHITI_SERVICE_URL | No | http://graphiti-service:8300 | URL called by memory-api (fail-soft) |
| SEMAPHORE_LIMIT | No | 3 | Max concurrent Graphiti requests (Tier 1 rate limit) |

---

**Generating Secrets**

Code block "useful commands" :
```bash
# Generate BRIDGE_SHARED_SECRET
openssl rand -hex 32

# Generate MEILI_MASTER_KEY
openssl rand -hex 32

# Generate PIPELINE_API_KEY
openssl rand -hex 32

# Generate OAUTH_CREDENTIALS_ENCRYPTION_KEY (Fernet)
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# Generate NEO4J_PASSWORD (16-char)
openssl rand -base64 16
```

Callout--warning : "Rotate BRIDGE_SHARED_SECRET and regenerate all bridge JWTs after rotation. Secrets stored in .env must never be committed to git. Add infrastructure/.env to .gitignore."

---

**Complete .env.example**

```bash
# === Core DB ===
POSTGRES_USER=xbrain
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=xbrain
DATABASE_URL=postgres+asyncpg://xbrain:CHANGE_ME@postgres:5432/xbrain

# === Qdrant ===
QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=

# === Auth ===
GOOGLE_CLIENT_ID=CHANGE_ME.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=CHANGE_ME
BRIDGE_SHARED_SECRET=CHANGE_ME_32HEX
JWT_ALGORITHM=HS256
ADMIN_USER_SUBS=

# === AI Models ===
ANTHROPIC_API_KEY=sk-ant-CHANGE_ME
OPENAI_API_KEY=sk-CHANGE_ME
XAI_API_KEY=

# === LibreChat ===
MEILI_MASTER_KEY=CHANGE_ME_32HEX

# === Bridges ===
MEMORY_API_URL=http://memory-api:8000
BRIDGE_DEFAULT_TEAM_SCOPE=default
PIPELINE_API_KEY=CHANGE_ME_32HEX

# === Langfuse (Phase 2) ===
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# === Neo4j (Phase 3) ===
NEO4J_PASSWORD=CHANGE_ME
OAUTH_CREDENTIALS_ENCRYPTION_KEY=

# === External URLs ===
MEMORY_API_EXTERNAL_URL=https://api.yourdomain.com

# === GitHub Auth (Phase 5) ===
GITHUB_ORG=your-github-org
GITHUB_API_PAT=
```
  </action>
  <verify>
    <automated>grep -c "POSTGRES_PASSWORD\|BRIDGE_SHARED_SECRET\|LANGFUSE\|NEO4J" "D:/VSC/xbrain/marketing-site/docs/configuration.html"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "POSTGRES_PASSWORD" marketing-site/docs/configuration.html
    - grep -q "BRIDGE_SHARED_SECRET" marketing-site/docs/configuration.html
    - grep -q "OAUTH_CREDENTIALS_ENCRYPTION_KEY" marketing-site/docs/configuration.html
    - grep -q "GITHUB_API_PAT" marketing-site/docs/configuration.html
    - grep -q "SEMAPHORE_LIMIT" marketing-site/docs/configuration.html
    - grep -q "openssl rand" marketing-site/docs/configuration.html
    - grep -q "docs-table" marketing-site/docs/configuration.html
    - Fichier > 150 lignes
  </acceptance_criteria>
  <done>docs/configuration.html : toutes les variables d'environnement organisées par service (core DB, memory-api, LibreChat, bridge/pipeline, agent-runtime, Langfuse, graphiti), commandes de génération de secrets, .env.example complet</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Ops teams | Guide de déploiement public — contient des templates de config sans secrets réels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Information Disclosure | configuration.html | mitigate | Callout--warning explicite : .env ne doit jamais être committé. Toutes les valeurs sont CHANGE_ME/placeholders. |
| T-06-07-02 | Information Disclosure | deployment.html | accept | Commandes gcloud / docker sont standard, aucune credential exposée |
| T-06-07-03 | Tampering | .env.example | accept | .env.example dans le repo est par design sans secrets réels — seul .env (gitignored) a les vraies valeurs |
</threat_model>

<verification>
```bash
# Vérifier les 2 fichiers
ls -la marketing-site/docs/deployment.html marketing-site/docs/configuration.html

# Deployment
grep -q "verify-phase1.sh" marketing-site/docs/deployment.html && echo "deployment phases OK"
grep -q "e2-standard-2" marketing-site/docs/deployment.html && echo "VM sizing OK"

# Configuration
grep -q "BRIDGE_SHARED_SECRET" marketing-site/docs/configuration.html && echo "config secrets OK"
grep -q "docs-table" marketing-site/docs/configuration.html && echo "config tables OK"

# Nombre de variables documentées
grep -c "| [A-Z_]\{3,\}" marketing-site/docs/configuration.html
# Doit retourner >= 25 variables
```
</verification>

<success_criteria>
- deployment.html : guide Phase 1 à Phase 5 complet, commandes gcloud, docker, nginx, backup, monitoring
- configuration.html : >= 25 variables documentées par service, commandes de génération de secrets, .env.example complet
- Aucun secret réel — tous les placeholders sont CHANGE_ME ou openssl rand
- Template identique aux autres pages (sidebar 14 liens, breadcrumb, docs-layout)
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-marketing-docs/06-07-SUMMARY.md`
</output>
