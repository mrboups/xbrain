---
phase: 06-marketing-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - marketing-site/firebase.json
  - marketing-site/.firebaserc
  - marketing-site/assets/style.css
  - marketing-site/assets/docs.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "firebase.json pointe vers le dossier public '.' avec la bonne config hosting"
    - "style.css contient la nav, le footer, la typographie et les boutons avec l'accent violet #7C3AED"
    - "docs.css contient le layout sidebar+content, les blocs de code et les breadcrumbs"
    - "Les deux fichiers CSS sont importables depuis index.html et docs/*.html sans erreur 404"
  artifacts:
    - path: "marketing-site/firebase.json"
      provides: "Firebase Hosting config multi-site"
      contains: "xbrain-marketing"
    - path: "marketing-site/.firebaserc"
      provides: "Projet Firebase et site cible"
      contains: "xbrain-495115"
    - path: "marketing-site/assets/style.css"
      provides: "Styles communs nav/footer/buttons"
      min_lines: 80
    - path: "marketing-site/assets/docs.css"
      provides: "Styles sidebar/code/breadcrumbs"
      min_lines: 60
  key_links:
    - from: "marketing-site/assets/style.css"
      to: "marketing-site/index.html"
      via: "<link rel='stylesheet'>"
      pattern: "assets/style\\.css"
    - from: "marketing-site/assets/docs.css"
      to: "marketing-site/docs/*.html"
      via: "<link rel='stylesheet'>"
      pattern: "assets/docs\\.css"
---

<objective>
Créer la configuration Firebase Hosting et les fichiers CSS fondation pour le site marketing xbrain.

Purpose: Ces fichiers sont la base commune sur laquelle tous les autres plans s'appuient. Firebase config permet le déploiement final (Plan 06-08). Les CSS définissent l'identité visuelle cohérente (blanc + violet #7C3AED) partagée par la landing page et toutes les 13 pages de documentation.

Output: 4 fichiers — firebase.json, .firebaserc, assets/style.css, assets/docs.css dans marketing-site/.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/VSC/xbrain/.planning/ROADMAP.md
@D:/VSC/xbrain/.planning/phases/06-marketing-docs/06-CONTEXT.md
@D:/VSC/xbrain/projects-dashboard/firebase.json

Référence firebase.json existante (projects-dashboard) :
- public: "public"
- headers: Cache-Control no-cache + X-Frame-Options DENY

Le nouveau site Firebase sera un 2e site dans le projet xbrain-495115.
Site ID cible : xbrain-marketing → URL : xbrain-marketing.web.app

Décisions locked (per D1, D2, D9) :
- Zéro build step, déploiement direct firebase deploy
- Projet xbrain-495115 déjà existant
- marketing-site/ à la racine du repo
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firebase config files (firebase.json + .firebaserc)</name>
  <files>marketing-site/firebase.json, marketing-site/.firebaserc</files>
  <read_first>D:/VSC/xbrain/projects-dashboard/firebase.json</read_first>
  <action>
Créer marketing-site/firebase.json avec la configuration suivante (public: "." car le firebase.json est à la racine de marketing-site/) :

```json
{
  "hosting": {
    "site": "xbrain-marketing",
    "public": ".",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [],
    "headers": [
      {
        "source": "**",
        "headers": [
          {"key": "Cache-Control", "value": "no-cache, must-revalidate"},
          {"key": "X-Frame-Options", "value": "DENY"},
          {"key": "X-Content-Type-Options", "value": "nosniff"},
          {"key": "Referrer-Policy", "value": "strict-origin-when-cross-origin"}
        ]
      },
      {
        "source": "**/*.@(css|js)",
        "headers": [
          {"key": "Cache-Control", "value": "public, max-age=3600"}
        ]
      }
    ]
  }
}
```

Créer marketing-site/.firebaserc :

```json
{
  "projects": {
    "default": "xbrain-495115"
  },
  "targets": {
    "xbrain-495115": {
      "hosting": {
        "marketing": ["xbrain-marketing"]
      }
    }
  }
}
```
  </action>
  <verify>
    <automated>grep -q "xbrain-marketing" "D:/VSC/xbrain/marketing-site/firebase.json" && grep -q "xbrain-495115" "D:/VSC/xbrain/marketing-site/.firebaserc" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>firebase.json contient "site": "xbrain-marketing" et "public": "." ; .firebaserc contient le projet xbrain-495115 et le target xbrain-marketing</done>
</task>

<task type="auto">
  <name>Task 2: CSS foundation — style.css (nav, footer, typography, buttons)</name>
  <files>marketing-site/assets/style.css</files>
  <action>
Créer marketing-site/assets/style.css avec les styles communs. Ce fichier est chargé par index.html ET par toutes les docs pages. Il complète TailwindCSS CDN — ne pas re-déclarer ce que Tailwind gère, seulement les patterns personnalisés.

Contenu requis (sections dans cet ordre) :

1. **CSS Variables** — palette de couleurs
```css
:root {
  --violet: #7C3AED;
  --violet-light: #EDE9FE;
  --violet-dark: #5B21B6;
  --gray-900: #111827;
  --gray-600: #4B5563;
  --gray-100: #F3F4F6;
  --white: #FFFFFF;
}
```

2. **Base reset** — box-sizing, font-family system stack, smooth scroll
```css
*, *::before, *::after { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body { font-family: system-ui, -apple-system, 'Segoe UI', sans-serif; }
```

3. **Nav** — classe `.xb-nav` pour la barre de navigation sticky
- Background blanc, border-bottom 1px solid #E5E7EB
- Height 64px, position sticky top-0, z-index 50
- Container max-width 1200px centré
- Logo (texte "xbrain") en violet bold, taille 1.25rem
- Links de nav en gris-600, hover violet, transition 150ms
- Classe `.xb-nav__logo`, `.xb-nav__links`, `.xb-nav__link`

4. **Buttons** — classes `.btn-primary` et `.btn-secondary`
- `.btn-primary` : background #7C3AED, texte blanc, padding 12px 24px, border-radius 8px, font-weight 600, hover background #5B21B6, transition 200ms
- `.btn-secondary` : border 2px solid #7C3AED, texte violet, background transparent, mêmes dimensions, hover background #EDE9FE

5. **Footer** — classe `.xb-footer`
- Background #111827, texte blanc
- Padding top/bottom 48px
- Colonnes : Links (Docs, GitHub), Contact (email), Legal
- Copyright ligne en bas en gris-400
- Classe `.xb-footer__col`, `.xb-footer__link`

6. **Utility** — quelques helpers non-Tailwind si nécessaire
- `.container-xl` : max-width 1200px, margin auto, padding 0 24px
- `.sr-only` : visually hidden pour accessibilité
  </action>
  <verify>
    <automated>grep -c "7C3AED" "D:/VSC/xbrain/marketing-site/assets/style.css"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "7C3AED" marketing-site/assets/style.css retourne >= 3 (variables + buttons + au moins un usage)
    - grep -q "xb-nav" marketing-site/assets/style.css
    - grep -q "xb-footer" marketing-site/assets/style.css
    - grep -q "btn-primary" marketing-site/assets/style.css
    - Fichier > 80 lignes
  </acceptance_criteria>
  <done>style.css contient les variables CSS, nav sticky, boutons primary/secondary, footer dark — prêt à être importé par index.html et les docs pages</done>
</task>

<task type="auto">
  <name>Task 3: CSS docs — docs.css (sidebar, code blocks, breadcrumbs, layout)</name>
  <files>marketing-site/assets/docs.css</files>
  <action>
Créer marketing-site/assets/docs.css avec les styles spécifiques aux pages de documentation. Ce fichier est chargé UNIQUEMENT par docs/*.html (pas par index.html).

Contenu requis (sections dans cet ordre) :

1. **Layout docs** — `.docs-layout`
```css
.docs-layout {
  display: grid;
  grid-template-columns: 260px 1fr;
  gap: 0;
  min-height: calc(100vh - 64px); /* 64px = nav height */
  max-width: 1200px;
  margin: 0 auto;
}
@media (max-width: 768px) {
  .docs-layout { grid-template-columns: 1fr; }
  .sidebar { display: none; }
}
```

2. **Sidebar** — `.sidebar`
- Background #F9FAFB, border-right 1px solid #E5E7EB
- Padding 32px 20px
- Position sticky, top 64px, height calc(100vh - 64px), overflow-y auto
- Section titles en uppercase, letter-spacing, font-size 11px, couleur gris-500
- Link actif (classe `.active`) : background #EDE9FE, couleur violet, border-left 3px solid #7C3AED
- `.sidebar__link` : display block, padding 6px 12px, border-radius 6px, font-size 14px, color gris-700, hover background gris-100
- `.sidebar__section` : margin-bottom 24px

3. **Content** — `.docs-content`
- Padding 48px
- Max-width 800px (pour lisibilité)
- Titres h1 en 2.25rem, h2 en 1.5rem avec padding-top 32px et border-bottom 1px gris-200, h3 en 1.125rem
- Paragraphes : line-height 1.7, color gris-700
- Links dans le contenu : couleur violet, underline au hover

4. **Code blocks** — `.code-block` et inline code
```css
.code-block {
  background: #1F2937;
  color: #F9FAFB;
  border-radius: 8px;
  padding: 20px 24px;
  font-family: 'Courier New', Courier, monospace;
  font-size: 14px;
  line-height: 1.6;
  overflow-x: auto;
  margin: 20px 0;
}
code {
  background: #F3F4F6;
  color: #7C3AED;
  padding: 2px 6px;
  border-radius: 4px;
  font-size: 0.875em;
  font-family: 'Courier New', Courier, monospace;
}
.code-block code {
  background: transparent;
  color: inherit;
  padding: 0;
}
```

5. **Breadcrumbs** — `.breadcrumb`
- Flex row, gap 8px, font-size 14px, couleur gris-500
- Separator ">" en gris-300
- Item actif en gris-900

6. **Callout boxes** — `.callout`, `.callout--info`, `.callout--warning`
- Border-left 4px solid couleur, background couleur-light, padding 16px, border-radius 0 8px 8px 0
- `.callout--info` : border violet, background #EDE9FE
- `.callout--warning` : border amber-500, background amber-50

7. **Tables** — `.docs-table`
- Border-collapse collapse, width 100%
- Th : background gris-50, padding 12px 16px, text-align left, font-size 13px uppercase
- Td : border-top 1px gris-200, padding 12px 16px, font-size 14px
  </action>
  <verify>
    <automated>grep -c "docs-layout\|sidebar\|code-block\|breadcrumb" "D:/VSC/xbrain/marketing-site/assets/docs.css"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "docs-layout" marketing-site/assets/docs.css
    - grep -q "sidebar" marketing-site/assets/docs.css
    - grep -q "code-block" marketing-site/assets/docs.css
    - grep -q "breadcrumb" marketing-site/assets/docs.css
    - grep -q "callout" marketing-site/assets/docs.css
    - Fichier > 60 lignes
  </acceptance_criteria>
  <done>docs.css contient le grid layout 260px+1fr, sidebar sticky, code blocks dark, breadcrumbs et callout boxes — prêt pour toutes les pages docs/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firebase CDN → Browser | Fichiers statiques servis depuis Firebase, aucune logique serveur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | firebase.json | accept | Fichier de config public, aucun secret — site ID xbrain-marketing non sensible |
| T-06-01-02 | Tampering | CSS assets | mitigate | Headers Cache-Control no-cache dans firebase.json — force revalidation à chaque requête |
| T-06-01-03 | Denial of Service | Firebase Hosting | accept | Firebase gère CDN, DDoS protection incluse par Google |
</threat_model>

<verification>
Après création des 4 fichiers :

```bash
# Vérifier firebase.json
grep -q "xbrain-marketing" marketing-site/firebase.json && echo "firebase.json OK"

# Vérifier .firebaserc
grep -q "xbrain-495115" marketing-site/.firebaserc && echo ".firebaserc OK"

# Vérifier style.css
grep -q "7C3AED" marketing-site/assets/style.css && grep -q "xb-nav" marketing-site/assets/style.css && echo "style.css OK"

# Vérifier docs.css
grep -q "docs-layout" marketing-site/assets/docs.css && grep -q "code-block" marketing-site/assets/docs.css && echo "docs.css OK"
```
</verification>

<success_criteria>
- firebase.json valide avec "site": "xbrain-marketing", "public": "."
- .firebaserc référence xbrain-495115 et target xbrain-marketing
- style.css : variables #7C3AED, nav sticky, footer dark, boutons primary/secondary
- docs.css : grid layout docs, sidebar sticky, code blocks dark background, breadcrumbs, callout boxes
- Aucun build step requis — fichiers CSS purs
</success_criteria>

<output>
Après completion, créer `.planning/phases/06-marketing-docs/06-01-SUMMARY.md`
</output>
