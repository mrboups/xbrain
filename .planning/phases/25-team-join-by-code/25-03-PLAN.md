---
phase: 25-team-join-by-code
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - apps/memory-api/tests/test_join_by_code_gate.py
autonomous: true
requirements: [JOINCODE-01]

must_haves:
  truths:
    - "Against a REAL Postgres (testcontainers, NON-mocked on the security paths), mint returns a plaintext ONCE and the DB row stores only its sha256 hash — the plaintext is absent from every column of the row and sha256(plaintext) == the stored code_hash"
    - "A valid join adds a team_members row for the caller in the code's team and increments uses; already-a-member is a 200 no-op with uses UNCHANGED; a garbage code is a generic 404 (no oracle)"
    - "Revoked / expired / max-uses-reached codes are each rejected, AND two racing redemptions of a max_uses=1 code cannot exceed max_uses (the atomic increment proven with concurrent sessions)"
    - "A code minted for team A can never add the caller to team B (decoy team); a non-admin caller gets 403 on mint/revoke/list"
    - "Migration 0027 upgrades clean under EDITION=oss AND saas — team_invite_codes + the UNIQUE code_hash index exist under both; SKIP=FAIL (Docker is up)"
  artifacts:
    - path: "apps/memory-api/tests/test_join_by_code_gate.py"
      provides: "The gate-lesson proof: real-Postgres, non-mocked, mint->join->revoke + every guard + double-spend race + migration-both-editions"
      contains: "pytest.mark.integration"
      min_lines: 220
  key_links:
    - from: "apps/memory-api/tests/test_join_by_code_gate.py"
      to: "app/repos/team_invite_codes.redeem_atomic"
      via: "two concurrent committing sessions redeeming a max_uses=1 code (the race proof)"
      pattern: "redeem_atomic"
    - from: "apps/memory-api/tests/test_join_by_code_gate.py"
      to: "0027_team_invite_codes migration"
      via: "alembic upgrade head under EDITION=oss and saas, probing information_schema"
      pattern: "information_schema"
---

<objective>
Prove JOINCODE-01 the honest way. A join-code guards the team-scoped brain (the
product's sensitive core), so "it works" is not enough — every SECURITY guard must
be PROVEN against a REAL Postgres (testcontainers), non-mocked on the
security-bearing paths. This plan is the gate lesson made executable, mirroring
test_catch_me_up_gate.py: mint returns a plaintext once + the DB stores only the
hash; a valid join adds the member + increments uses; revoked/expired/max-uses
codes are rejected; a concurrent double-redeem cannot exceed max_uses; a team-A
code never adds to team-B; non-admin mint/revoke/list is 403; already-member is a
no-op; garbage is a generic 404; and migration 0027 upgrades clean under oss AND
saas.

Purpose: a mocked-DB test, or one that asserts the plaintext sits in the row, would
pass even with the security broken. This gate cannot. SKIP=FAIL — under Docker it
MUST run green; only a genuinely absent Docker may skip.

Output: `tests/test_join_by_code_gate.py` — the phase's binding acceptance proof.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-team-join-by-code/25-CONTEXT.md
@.planning/phases/25-team-join-by-code/25-01-SUMMARY.md
@.planning/phases/25-team-join-by-code/25-02-SUMMARY.md

@apps/memory-api/tests/test_catch_me_up_gate.py
@apps/memory-api/tests/conftest.py

<interfaces>
<!-- The exact patterns to mirror. Extracted from the live gate. Reuse them. -->

MIRROR test_catch_me_up_gate.py wholesale — it is the template. Reuse VERBATIM in shape:
  - `pytestmark = [pytest.mark.integration, pytest.mark.asyncio]`.
  - `_install_principal(user, *, kind="user")` + `_clear_principal()` — override
     get_current_principal with a SimpleNamespace(id, source_user_id, email, display_name,
     github_username, github_id). `_as(user_p, method, path, **kw)` helper wraps set→call→clear.
  - The `_committing_get_session` override (each request commits straight through so the
     data is visible cross-connection) + `rate_limit._storage.reset()` at the top so a prior
     test can't 429 this one.
  - COMMITTED seed via `db_session.async_session_factory()` (users_repo.get_or_create_user,
     teams_repo.create_team → creator becomes admin+member) with distinct slugs/subs; teardown
     `DELETE FROM teams WHERE id = ANY(:ids)` (CASCADE) + `DELETE FROM users` in `finally`.
  - Group 2 (migration): FRESH PostgresContainer per edition, config singleton patched DIRECTLY
     (os.environ alone is inert), `command.upgrade(cfg, "head")` in `asyncio.to_thread`, probe
     information_schema, restore singleton + env in `finally`, `pytest.skip` ONLY when Docker absent.
     `_EDITIONS = ["oss", "saas"]`, parametrized. `with_command("postgres -c shared_preload_libraries=pgcrypto")`.

From app/repos/team_invite_codes.py (Plan 01):
  generate_code() -> (plaintext, code_hash, code_prefix)
  redeem_atomic(session, *, code_id, now) -> Row(id, team_id, role, uses) | None
From app/repos/teams.py: get_membership, create_team, add_member.

HTTP endpoints under test (Plan 02):
  POST   /v1/teams/{team_id}/invite-codes         (admin)  body {role?, expires_in_days?, max_uses?} -> 201 {id, code, code_prefix, role, expires_at, max_uses}
  GET    /v1/teams/{team_id}/invite-codes         (admin)  -> [ {id, code_prefix, role, uses, max_uses, expires_at, revoked_at, created_at} ]  (NO code_hash)
  DELETE /v1/teams/{team_id}/invite-codes/{id}    (admin)  -> 204
  POST   /v1/teams/join-by-code                    (any)   body {code} -> 200 {team_id, slug, display_name, already_member}; generic 404 on invalid/revoked/expired/exhausted

DB probe: read team_invite_codes rows via `db_session.async_session_factory()` + a raw
`sa.text("SELECT code_hash, code_prefix, uses, revoked_at, expires_at, max_uses FROM team_invite_codes WHERE id = :id")`.
Git Bash docker needs MSYS_NO_PATHCONV=1 for any docker CLI invocation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The HTTP gate — mint->join->revoke + sha256-at-rest + authZ + idempotency + no-oracle + team-isolation + revoked/expired/max-uses rejections</name>
  <read_first>
    - apps/memory-api/tests/test_catch_me_up_gate.py:56-405 (Group 1 — the principal override, _committing_get_session, committed-seed, _as/_wait_until helpers, teardown; copy this scaffolding)
    - apps/memory-api/tests/conftest.py:67-124,181-217 (pg_url session fixture + the `client` fixture + seeded_two_teams shape)
    - apps/memory-api/app/repos/team_invite_codes.py (generate_code + redeem_atomic — for the direct DB assertions)
    - .planning/phases/25-team-join-by-code/25-02-SUMMARY.md (the exact route paths + response fields)
    - .planning/phases/25-team-join-by-code/25-CONTEXT.md <specifics> (the full guard checklist this gate must cover)
  </read_first>
  <files>apps/memory-api/tests/test_join_by_code_gate.py</files>
  <action>
    Create `apps/memory-api/tests/test_join_by_code_gate.py`. Header docstring must state:
    real Postgres (testcontainers), NON-mocked on the security paths, SKIP=FAIL. Copy the
    principal-override + committing-session + committed-seed scaffolding from
    test_catch_me_up_gate.py Group 1.

    Seed (COMMITTED via async_session_factory, distinct slugs/subs, torn down in finally):
      - admin_a (jbc-admin-a-sub) -> creates team_a (slug "jbc-team-a") -> admin+member.
      - admin_b (jbc-admin-b-sub) -> creates team_b (slug "jbc-team-b") -> the DECOY team.
      - joiner (jbc-joiner-sub), joiner2 (jbc-joiner2-sub), outsider (jbc-outsider-sub) — users with NO membership yet.
    Install the `_committing_get_session` override + `rate_limit._storage.reset()`.

    Write ONE comprehensive `async def test_join_by_code_gate(client)` (mirror the single-flow
    style of test_catch_me_up_gate) with these assertion blocks, each labeled by SC:

    A. MINT (SC#1) — as admin_a: POST /v1/teams/{team_a_id}/invite-codes with body
       {"role":"member","max_uses":5}. assert 201; body has `code` starting with "xbi_" and a
       `code_prefix`; assert `body["code_prefix"]` is NOT the full code and NO `code_hash` key exists.
       Then read the row directly (async_session_factory + raw SELECT by id): assert the stored
       `code_hash == hashlib.sha256(body["code"].encode()).hexdigest()`, assert the plaintext
       `body["code"]` does NOT equal code_hash and is not a substring of any stored column value,
       and `uses == 0`. (The plaintext lives only in the mint response — bearer-secret at rest, D-25-01.)
    B. AUTHZ (SC#1, D-25-03) — as `joiner` (a non-admin, non-member): POST the same mint path -> 403;
       GET the list path -> 403; DELETE an invite-codes/{id} path -> 403. (mint/revoke/list are admin-only.)
    C. LIST NO-HASH (D-25-03) — as admin_a: GET /v1/teams/{team_a_id}/invite-codes -> 200; each item
       has code_prefix + uses + role but assert `"code_hash" not in item` and `"code" not in item`.
    D. JOIN valid (SC#2) — as `joiner`: POST /v1/teams/join-by-code {"code": <plaintext from A>} -> 200,
       already_member is False, team_id == team_a_id. assert joiner now has a team_members row in team_a
       (get_membership via async_session_factory) AND is NOT a member of team_b (team_scope integrity, SC#3).
       Re-read the code row: `uses == 1` (incremented).
    E. IDEMPOTENT (SC#2, D-25-04) — as `joiner` again: POST join-by-code with the same code -> 200,
       already_member is True; re-read the row: `uses` is STILL 1 (a no-op must NOT increment).
    F. GARBAGE (SC#2) — as `outsider`: POST join-by-code {"code":"xbi_not-a-real-code"} -> 404 with the
       generic message; assert outsider has NO membership anywhere (no side effect, no oracle).
    G. REVOKED (SC#3) — as admin_a: mint a fresh code; DELETE its /invite-codes/{id} -> 204; then as
       `joiner2`: POST join-by-code with that (now revoked) code -> 404; joiner2 gains no membership.
    H. EXPIRED (SC#3) — mint a code, then via async_session_factory set its `expires_at` to a PAST
       instant (raw UPDATE) + commit; as `joiner2`: join-by-code -> 404; no membership gained.
    I. MAX-USES-REACHED (SC#3) — mint a code with max_uses=1; redeem it once (joiner2 joins team_a -> 200);
       then as `outsider`: join-by-code with the SAME code -> 404 (ceiling reached); outsider gains no membership.
       (The CONCURRENT racing proof of this ceiling is Task 2 — here we prove the sequential ceiling.)

    Every rejection (F/G/H/I) MUST assert the SAME generic 404 message (no oracle distinguishing
    garbage vs revoked vs expired vs exhausted — D-25-02). Close/clean up in finally (DELETE teams+users).
  </action>
  <verify>
    <automated>cd apps/memory-api && MSYS_NO_PATHCONV=1 python -m pytest tests/test_join_by_code_gate.py -q -k "test_join_by_code_gate" -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@pytest.mark.integration\|pytest.mark.integration' apps/memory-api/tests/test_join_by_code_gate.py` >= 1 (the file is integration-marked; SKIP=FAIL captured by CI skip-grep).
    - The file contains NO mock of the repo/DB on the security paths: `grep -E 'MagicMock|unittest.mock|monkeypatch.setattr\("app.repos.team_invite_codes' apps/memory-api/tests/test_join_by_code_gate.py` returns nothing.
    - sha256-at-rest is asserted with a DIRECT DB read: the test computes `hashlib.sha256(<mint response code>.encode()).hexdigest()` and asserts it equals the stored `code_hash`, and asserts the plaintext is absent from the row — `grep -q 'sha256' apps/memory-api/tests/test_join_by_code_gate.py`.
    - Every guard block is present and asserts the right status: mint 201, non-admin 403 (x3), list has no code_hash, valid join 200 + membership in team_a only + uses==1, idempotent 200 + uses still 1, garbage/revoked/expired/max-uses each 404 with the same generic message.
    - When Docker is available, `python -m pytest tests/test_join_by_code_gate.py -k test_join_by_code_gate` PASSES; if Docker is absent it SKIPs via the conftest pg_url guard — but a wrong status/side-effect FAILS (never skips).
  </acceptance_criteria>
  <done>One comprehensive integration test proves, against a real Postgres, mint(plaintext-once + hash-at-rest) -> non-admin 403 -> list-no-hash -> valid join(member added to team_a only + uses incremented) -> idempotent no-op(uses unchanged) -> garbage/revoked/expired/max-uses all rejected with a uniform generic 404. Non-mocked on every security path.</done>
</task>

<task type="auto">
  <name>Task 2: The double-spend race (concurrent redeem) + migration 0027 under EDITION=oss AND saas</name>
  <read_first>
    - apps/memory-api/tests/test_join_by_code_gate.py (Task 1 — reuse its seed/teardown helpers + async_session_factory pattern)
    - apps/memory-api/tests/test_catch_me_up_gate.py:407-541 (Group 2 — the per-edition fresh container + config-singleton patch + information_schema probe to mirror EXACTLY for 0027)
    - apps/memory-api/app/repos/team_invite_codes.py (redeem_atomic — the function the race test drives directly via two concurrent sessions)
    - .planning/phases/25-team-join-by-code/25-CONTEXT.md (D-25-02 no double-spend; D-25-05 migration both editions)
  </read_first>
  <files>apps/memory-api/tests/test_join_by_code_gate.py</files>
  <action>
    Add two more integration tests to the SAME file.

    1. `async def test_double_spend_race_cannot_exceed_max_uses(pg_url)` (@pytest.mark.integration):
       This proves the ATOMIC increment under true concurrency — the crux double-spend guard (D-25-02).
       - COMMITTED seed via async_session_factory: users admin_r (creates team_r), and two DISTINCT
         redeemers racer1, racer2 (no membership). Mint ONE code with max_uses=1 directly via
         invite_codes_repo.mint_code(session, team_id=team_r.id, created_by_user_id=admin_r.id,
         role="member", expires_at=None, max_uses=1); commit; keep `code_id`.
       - Define `async def _attempt(user_id):` that opens its OWN `db_session.async_session_factory()`
         session, calls `redeemed = await invite_codes_repo.redeem_atomic(session, code_id=code_id,
         now=datetime.now(tz=timezone.utc))`; if redeemed is not None: `await teams_repo.add_member(session,
         team_id=redeemed.team_id, user_id=user_id, role=redeemed.role)`; `await session.commit()`; return
         (redeemed is not None). Wrap add_member/commit in a try so a unique-violation on a lost race is
         caught and counted as a FAILURE-to-redeem (defensive) — but the atomic UPDATE alone should already
         gate it.
       - Run BOTH concurrently: `results = await asyncio.gather(_attempt(racer1.id), _attempt(racer2.id),
         return_exceptions=False)`.
       - assert `sum(1 for r in results if r) == 1` — EXACTLY ONE redemption succeeded.
       - Re-read the code row: `uses == 1` (NOT 2 — the ceiling held under the race).
       - assert EXACTLY ONE of {racer1, racer2} has a team_members row in team_r (the other has none).
       - Teardown in finally (DELETE team + users). This is the honest race proof: two real concurrent
         sessions hit the single conditional UPDATE; the row lock serializes them and the loser's
         `uses < max_uses` predicate fails.

    2. `test_migration_0027_team_invite_codes_forward_only(edition)` — parametrized over ["oss","saas"],
       a NEAR-VERBATIM copy of test_catch_me_up_gate.py's `test_migration_0026_last_read_forward_only` /
       `_upgrade_and_probe_last_read`, retargeted to 0027:
       - `_docker_available()` guard -> pytest.skip ONLY if Docker absent.
       - Spin a FRESH PostgresContainer("postgres:17", ...).with_command(
         "postgres -c shared_preload_libraries=pgcrypto"); patch app_config.settings.EDITION +
         DATABASE_URL DIRECTLY (os.environ alone is inert); `command.upgrade(cfg, "head")` in
         asyncio.to_thread; restore singleton + env in finally; pg.stop() in finally.
       - Probe information_schema TWICE:
         (a) SELECT data_type, is_nullable FROM information_schema.columns WHERE table_name='team_invite_codes'
             AND column_name='code_hash' -> assert exactly 1 row, is_nullable == 'NO', data_type is a text type.
         (b) confirm the UNIQUE index on code_hash exists:
             SELECT indexdef FROM pg_indexes WHERE tablename='team_invite_codes' AND indexname='ix_team_invite_codes_code_hash'
             -> assert 1 row and 'UNIQUE' in indexdef.upper().
       - The table + unique index MUST exist identically under BOTH oss and saas (no EDITION fork, D-25-05).
       - No reverse migration is ever invoked.
  </action>
  <verify>
    <automated>cd apps/memory-api && MSYS_NO_PATHCONV=1 python -m pytest tests/test_join_by_code_gate.py -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - The race test runs BOTH redemptions concurrently via `asyncio.gather` on two SEPARATE async_session_factory() sessions calling `redeem_atomic`, and asserts exactly one success, final `uses == 1`, and exactly one new membership — `grep -q 'asyncio.gather' apps/memory-api/tests/test_join_by_code_gate.py` and `grep -q 'redeem_atomic' apps/memory-api/tests/test_join_by_code_gate.py`.
    - The migration test is parametrized over `["oss","saas"]` and patches `app_config.settings.EDITION` directly (not just os.environ) — `grep -q 'settings.EDITION' apps/memory-api/tests/test_join_by_code_gate.py` and `grep -q 'information_schema' apps/memory-api/tests/test_join_by_code_gate.py`.
    - The migration test asserts BOTH the team_invite_codes.code_hash column (NOT NULL) AND the UNIQUE code_hash index exist under each edition — `grep -q 'pg_indexes\|indexdef' apps/memory-api/tests/test_join_by_code_gate.py` and an `assert` on `'UNIQUE'`.
    - Under Docker, the full file passes: `python -m pytest tests/test_join_by_code_gate.py` -> all integration tests PASS (race + both migration params + Task-1 gate); SKIP only when Docker is genuinely absent — a wrong outcome FAILS. Record the run outcome (green under Docker vs environment-skip) in the SUMMARY.
    - `uses == 1` (not 2) is asserted after the concurrent race — the double-spend ceiling held.
  </acceptance_criteria>
  <done>The double-spend guard is proven under true concurrency (two racing sessions on a max_uses=1 code -> exactly one wins, uses stays 1, one membership), and migration 0027 is proven to create team_invite_codes + a UNIQUE code_hash index cleanly under EDITION=oss AND saas — completing the gate; SKIP=FAIL under Docker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real Postgres | The gate exercises the true mint/redeem/revoke path (non-mocked) so a broken guard cannot pass |
| two concurrent redeemers → one max_uses=1 code | The race must resolve to exactly one winner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Repudiation | a green suite that does not actually prove hash-at-rest (asserts against a mocked DB, or the plaintext sits in the row) | mitigate | The sha256-at-rest assertion reads the REAL row and checks sha256(plaintext)==code_hash AND that the plaintext is absent from the row; no repo/DB mock on the security paths; SKIP=FAIL. |
| T-25-13 | Elevation of privilege | double-spend race exceeding max_uses passing unnoticed | mitigate | Two concurrent async_session_factory() sessions drive redeem_atomic on a max_uses=1 code; the test asserts exactly one success + uses==1 + one membership — the atomic-increment claim is proven, not asserted. |
| T-25-14 | Tampering | a schema fork on EDITION silently making saas differ | mitigate | The migration test runs upgrade head under BOTH oss and saas on fresh containers and probes information_schema/pg_indexes for the table + unique code_hash index under each — a fork would fail one edition. |
| T-25-15 | Information disclosure | the list/no-oracle claim regressing (hash leaks, or rejections diverge) | mitigate | The gate asserts list items carry no code_hash/code, and that garbage/revoked/expired/exhausted ALL return the identical generic 404. |
</threat_model>

<verification>
- `MSYS_NO_PATHCONV=1 python -m pytest tests/test_join_by_code_gate.py -q` (Docker up) -> all integration tests PASS; assertions never SKIP.
- No repo/DB mock on the security paths; the sha256-at-rest, atomic-race, and team-isolation proofs are real.
- Migration 0027 proven under oss AND saas (table + unique code_hash index).
</verification>

<success_criteria>
- mint(hash-at-rest + plaintext-once), join(member added + uses incremented), idempotent no-op, generic-404 no-oracle, non-admin 403, team-A-not-team-B isolation, revoked/expired/max-uses rejection, and the concurrent double-spend ceiling are ALL proven against real Postgres.
- Migration 0027 upgrades clean under EDITION=oss AND saas. SKIP=FAIL — a mocked or plaintext-in-row test cannot satisfy this gate.
</success_criteria>

<output>
After completion, create `.planning/phases/25-team-join-by-code/25-03-SUMMARY.md`
(record whether the suite ran green under Docker or was environment-skipped, and where — CI/VM/local).
</output>
