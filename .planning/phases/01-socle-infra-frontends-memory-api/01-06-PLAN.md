---
phase: 01-socle-infra-frontends-memory-api
plan: 06
type: execute
wave: 4
depends_on: ["01-05"]
files_modified:
  - infrastructure/backup/Dockerfile
  - infrastructure/backup/backup.sh
  - infrastructure/backup/restore.sh
  - infrastructure/backup/crontab
  - infrastructure/scripts/restore-test.sh
  - infrastructure/docker-compose.yml
  - .gitignore
autonomous: false
requirements: []
must_haves:
  truths:
    - "Le service xbrain-backup tourne en daemon avec cron interne (1 backup quotidien à 02:00 UTC)"
    - "backup.sh produit 4 artifacts : pg_dump xbrain.sql.gz + mongodump LibreChat + Qdrant snapshot + tarball volumes openwebui_data"
    - "Tous les artifacts sont uploadés vers gs://xbrain-backups-prod/YYYY-MM-DD/ via gsutil"
    - "Le test restore (restore-test.sh) exécute : spinup d'un compose isolé, restore les 4 artifacts depuis le backup le plus récent, healthchecks pass, requête sample memory-api retourne données restaurées"
    - "Le user peut lancer un backup à la demande via `make backup`"
    - "Le test restore complet (success criterion 5) passe via `make restore-test`"
  artifacts:
    - path: infrastructure/backup/backup.sh
      provides: "Script backup quotidien, idempotent"
      contains: "pg_dump"
      min_lines: 40
    - path: infrastructure/backup/restore.sh
      provides: "Script restore depuis un backup donné (date)"
      contains: "pg_restore"
      min_lines: 40
    - path: infrastructure/scripts/restore-test.sh
      provides: "Test E2E restore sur env isolé (success criterion 5)"
      contains: "docker compose"
      min_lines: 50
    - path: infrastructure/backup/Dockerfile
      provides: "Image alpine + gsutil + postgres-client + mongo-tools + cron"
      contains: "gsutil"
  key_links:
    - from: infrastructure/backup/backup.sh
      to: infrastructure/backup/restore.sh
      via: "Format des artifacts compatible (pg_dump custom format, tar.gz)"
      pattern: "pg_dump.*-Fc|pg_restore"
---

<objective>
Implémenter le mécanisme de backup + restore qui satisfait le **success criterion 5** de la Phase 1 : "Un test de restore depuis backup a été exécuté sur un environnement clean et tous les services redémarrent avec les données correctes."

Purpose : Sans ce plan, on ne peut pas déclarer Phase 1 done — c'est un gate explicite de la roadmap. Plus généralement, la mémoire collective de l'équipe doit être protégée contre la perte de données dès le jour 1.

Output : Container `xbrain-backup` qui tourne en cron quotidien + scripts manuels pour backup à la demande et test E2E de restore.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
@D:/VSC/xbrain/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md
@.planning/phases/01-socle-infra-frontends-memory-api/01-05-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-needed>Créer un GCS bucket et un service account pour les backups, hors plan automatique (action GCP console).</what-needed>
  <how-to-do>
**1. Créer le bucket GCS** :
```
gcloud storage buckets create gs://xbrain-backups-prod \
  --project=xbrain-495115 \
  --location=europe-west1 \
  --uniform-bucket-level-access \
  --soft-delete-duration=7d
```

**2. Créer le service account** :
```
gcloud iam service-accounts create xbrain-backup-sa \
  --display-name="xbrain backup writer" \
  --project=xbrain-495115
```

**3. Lui donner les droits sur le bucket UNIQUEMENT** (principe du moindre privilège) :
```
gcloud storage buckets add-iam-policy-binding gs://xbrain-backups-prod \
  --member="serviceAccount:xbrain-backup-sa@xbrain-495115.iam.gserviceaccount.com" \
  --role="roles/storage.objectAdmin"
```

**4. Générer une clé JSON et la copier sur la VM** :
```
gcloud iam service-accounts keys create gcs-backup-sa.json \
  --iam-account=xbrain-backup-sa@xbrain-495115.iam.gserviceaccount.com \
  --project=xbrain-495115

# Copier sur la VM dans /home/user/secrets/
scp -i ~/.ssh/xbrain_key gcs-backup-sa.json user@__VM_HOST__:/home/user/secrets/gcs-backup-sa.json

# Sur la VM, restreindre les perms :
ssh -i ~/.ssh/xbrain_key user@__VM_HOST__ 'chmod 600 /home/user/secrets/gcs-backup-sa.json'
```

**5. Mettre à jour `.env` côté VM** : `GCS_SERVICE_ACCOUNT_KEY=/secrets/gcs-backup-sa.json` (path depuis l'intérieur du container — sera mounted depuis l'host).

**6. Ne PAS commit la clé JSON dans git** (déjà couvert par `.gitignore` qui exclut `*.json` dans secrets/).
  </how-to-do>
  <resume-signal>Type "done" quand le bucket existe, le service account a la clé JSON, le fichier est sur la VM avec perms 600</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Dockerfile + crontab pour le container xbrain-backup</name>
  <files>infrastructure/backup/Dockerfile, infrastructure/backup/crontab</files>
  <read_first>.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md (section Q5 — backup)</read_first>
  <action>
**`infrastructure/backup/Dockerfile`** : alpine + outils nécessaires, basé sur `google/cloud-sdk:slim` (qui ship `gsutil` et `gcloud`).

```dockerfile
FROM google/cloud-sdk:slim
RUN apt-get update && apt-get install -y --no-install-recommends \
      postgresql-client \
      mongodb-mongosh \
      gnupg \
      curl \
      cron \
      tar \
      gzip \
      jq \
      tzdata \
    && curl -fsSL https://www.mongodb.org/static/pgp/server-7.0.asc | apt-key add - 2>/dev/null || true \
    && echo "deb http://repo.mongodb.org/apt/debian bookworm/mongodb-org/7.0 main" > /etc/apt/sources.list.d/mongodb-org-7.0.list \
    && apt-get update && apt-get install -y --no-install-recommends mongodb-database-tools \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /scripts
COPY backup.sh restore.sh ./
COPY crontab /etc/cron.d/xbrain-backup
RUN chmod +x backup.sh restore.sh && chmod 0644 /etc/cron.d/xbrain-backup
ENV TZ=UTC
CMD ["sh", "-c", "cron -f & tail -f /var/log/cron.log"]
```

**`infrastructure/backup/crontab`** :
```
# Daily backup at 02:00 UTC
0 2 * * * root /scripts/backup.sh >> /var/log/cron.log 2>&1
# heartbeat to keep log file alive
*/15 * * * * root echo "[$(date -Iseconds)] backup-cron alive" >> /var/log/cron.log
```
  </action>
  <verify>test -f infrastructure/backup/Dockerfile && grep -q "google/cloud-sdk" infrastructure/backup/Dockerfile && grep -q "0 2" infrastructure/backup/crontab</verify>
  <acceptance_criteria>
    - `Dockerfile` part de `google/cloud-sdk:slim` (gsutil pré-installé)
    - Installe `postgresql-client`, `mongodb-database-tools`, `cron`
    - `crontab` schedule `0 2 * * *` (daily 02:00 UTC)
  </acceptance_criteria>
  <done>Container backup en place</done>
</task>

<task type="auto">
  <name>Task 2: backup.sh — produit pg_dump + mongodump + qdrant snapshot + tarball volumes</name>
  <files>infrastructure/backup/backup.sh</files>
  <read_first>.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md (section Q5 — backup)</read_first>
  <action>
**`infrastructure/backup/backup.sh`** :

```bash
#!/bin/bash
set -euo pipefail

# Vars depuis env du container :
# POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, POSTGRES_HOST
# LIBRECHAT_MONGO_URI
# QDRANT_URL
# GCS_BACKUP_BUCKET (ex: xbrain-backups-prod)
# GCS_SERVICE_ACCOUNT_KEY (path interne du JSON)
# BACKUP_RETENTION_DAILY (défaut 7)

DATE=$(date -u +%Y-%m-%d)
TS=$(date -u +%Y%m%d-%H%M%S)
WORK=/tmp/backup-${TS}
mkdir -p "${WORK}"
echo "[$(date -Iseconds)] === BACKUP START (${TS}) ==="

# --- 1. PostgreSQL (memory-api) ---
echo "[1/4] Postgres dump..."
PGPASSWORD="${POSTGRES_PASSWORD}" pg_dump \
  -h "${POSTGRES_HOST:-postgres}" -U "${POSTGRES_USER}" -d "${POSTGRES_DB}" \
  -Fc -Z 6 -f "${WORK}/postgres-${TS}.dump"
echo "  → $(du -h ${WORK}/postgres-${TS}.dump | cut -f1)"

# --- 2. MongoDB (LibreChat) ---
echo "[2/4] Mongo dump..."
mongodump --uri="${LIBRECHAT_MONGO_URI}" --gzip --archive="${WORK}/librechat-mongo-${TS}.archive.gz"
echo "  → $(du -h ${WORK}/librechat-mongo-${TS}.archive.gz | cut -f1)"

# --- 3. Qdrant snapshot ---
echo "[3/4] Qdrant snapshot..."
# Trigger snapshot via API
COLLECTIONS=$(curl -fsS "${QDRANT_URL}/collections" | jq -r '.result.collections[].name')
mkdir -p "${WORK}/qdrant"
for coll in ${COLLECTIONS}; do
  echo "  - snapshotting ${coll}..."
  SNAP=$(curl -fsS -X POST "${QDRANT_URL}/collections/${coll}/snapshots" | jq -r '.result.name')
  curl -fsS "${QDRANT_URL}/collections/${coll}/snapshots/${SNAP}" -o "${WORK}/qdrant/${coll}-${TS}.snapshot"
  # Cleanup le snapshot côté Qdrant pour ne pas accumuler
  curl -fsS -X DELETE "${QDRANT_URL}/collections/${coll}/snapshots/${SNAP}" >/dev/null
done
tar czf "${WORK}/qdrant-${TS}.tar.gz" -C "${WORK}/qdrant" .
rm -rf "${WORK}/qdrant"
echo "  → $(du -h ${WORK}/qdrant-${TS}.tar.gz | cut -f1)"

# --- 4. Volumes openwebui_data + librechat_uploads + librechat_meili (tarball) ---
echo "[4/4] Tarball volumes..."
# Le container backup mount /backup-volumes/openwebui-data, /backup-volumes/librechat-uploads, /backup-volumes/librechat-meili (read-only) depuis docker-compose
for vol in openwebui-data librechat-uploads librechat-meili; do
  if [ -d "/backup-volumes/${vol}" ]; then
    tar czf "${WORK}/${vol}-${TS}.tar.gz" -C "/backup-volumes/${vol}" .
    echo "  - ${vol}: $(du -h ${WORK}/${vol}-${TS}.tar.gz | cut -f1)"
  fi
done

# --- 5. Auth gcloud + upload GCS ---
echo "[upload] gcloud auth + gsutil cp..."
gcloud auth activate-service-account --key-file="${GCS_SERVICE_ACCOUNT_KEY}" --quiet
gsutil -m cp -r "${WORK}"/* "gs://${GCS_BACKUP_BUCKET}/${DATE}/"

# --- 6. Cleanup local ---
rm -rf "${WORK}"

# --- 7. Retention : delete daily backups > N jours sur GCS ---
RETAIN=${BACKUP_RETENTION_DAILY:-7}
CUTOFF=$(date -u -d "${RETAIN} days ago" +%Y-%m-%d 2>/dev/null || date -u -v -${RETAIN}d +%Y-%m-%d)
echo "[retention] Listing backups older than ${CUTOFF}..."
gsutil ls "gs://${GCS_BACKUP_BUCKET}/" 2>/dev/null | while read line; do
  prefix=$(basename "${line}")
  if [[ "${prefix}" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}/$ ]]; then
    bkup_date=${prefix%/}
    if [[ "${bkup_date}" < "${CUTOFF}" ]]; then
      echo "  - deleting ${line}"
      gsutil -m rm -r "${line}" || true
    fi
  fi
done

echo "[$(date -Iseconds)] === BACKUP DONE ==="
```
  </action>
  <verify>bash -n infrastructure/backup/backup.sh && grep -E "(pg_dump|mongodump|qdrant)" infrastructure/backup/backup.sh | wc -l</verify>
  <acceptance_criteria>
    - `backup.sh` syntaxe shell valide (`bash -n`)
    - Contient `pg_dump`, `mongodump`, Qdrant snapshot via curl, et `gsutil cp`
    - ≥ 40 lignes
    - Implémente la retention (delete daily > BACKUP_RETENTION_DAILY)
  </acceptance_criteria>
  <done>Backup automatique opérationnel</done>
</task>

<task type="auto">
  <name>Task 3: restore.sh — restore depuis un backup donné</name>
  <files>infrastructure/backup/restore.sh</files>
  <read_first>infrastructure/backup/backup.sh</read_first>
  <action>
**`infrastructure/backup/restore.sh`** :

```bash
#!/bin/bash
set -euo pipefail

# Usage: restore.sh <DATE> [--target-postgres <conn-uri>] [--target-mongo <uri>] [--target-qdrant <url>]
# Default targets = container env vars (postgres, mongo, qdrant) — utile pour restore sur env clean
# Si <DATE> = "latest", trouve le dernier backup dispo sur GCS

DATE_ARG="${1:-latest}"
TARGET_PG="${POSTGRES_HOST:-postgres}"
TARGET_MONGO="${LIBRECHAT_MONGO_URI}"
TARGET_QDRANT="${QDRANT_URL}"

WORK=/tmp/restore-$(date +%s)
mkdir -p "${WORK}"
echo "[restore] === START (${DATE_ARG}) ==="

# Auth gcloud
gcloud auth activate-service-account --key-file="${GCS_SERVICE_ACCOUNT_KEY}" --quiet

# Resolve "latest"
if [ "${DATE_ARG}" = "latest" ]; then
  DATE=$(gsutil ls "gs://${GCS_BACKUP_BUCKET}/" | grep -E '[0-9]{4}-[0-9]{2}-[0-9]{2}/$' | sort | tail -1 | awk -F/ '{print $(NF-1)}')
  echo "[resolve] latest = ${DATE}"
else
  DATE="${DATE_ARG}"
fi

# Download all artifacts for this date
echo "[download] gsutil cp gs://${GCS_BACKUP_BUCKET}/${DATE}/* ${WORK}/"
gsutil -m cp "gs://${GCS_BACKUP_BUCKET}/${DATE}/*" "${WORK}/"

# --- 1. Postgres ---
PG_DUMP=$(ls "${WORK}"/postgres-*.dump | head -1)
echo "[1/4] Postgres restore from ${PG_DUMP}..."
PGPASSWORD="${POSTGRES_PASSWORD}" pg_restore --clean --if-exists --no-owner \
  -h "${TARGET_PG}" -U "${POSTGRES_USER}" -d "${POSTGRES_DB}" "${PG_DUMP}"

# --- 2. Mongo ---
MONGO_DUMP=$(ls "${WORK}"/librechat-mongo-*.archive.gz | head -1)
echo "[2/4] Mongo restore from ${MONGO_DUMP}..."
mongorestore --uri="${TARGET_MONGO}" --gzip --archive="${MONGO_DUMP}" --drop

# --- 3. Qdrant ---
QDRANT_TAR=$(ls "${WORK}"/qdrant-*.tar.gz | head -1)
echo "[3/4] Qdrant restore from ${QDRANT_TAR}..."
mkdir -p "${WORK}/qdrant-extracted"
tar xzf "${QDRANT_TAR}" -C "${WORK}/qdrant-extracted"
for snap in "${WORK}/qdrant-extracted"/*.snapshot; do
  coll=$(basename "${snap}" | sed -E 's/-[0-9]{8}-[0-9]{6}\.snapshot$//')
  echo "  - restoring ${coll} from ${snap}"
  # Upload snapshot puis recover via API
  curl -fsS -X POST "${TARGET_QDRANT}/collections/${coll}/snapshots/upload?priority=snapshot" \
    -H "Content-Type: multipart/form-data" \
    -F "snapshot=@${snap}"
done

# --- 4. Volumes : restore tarballs ---
echo "[4/4] Volumes restore..."
for vol in openwebui-data librechat-uploads librechat-meili; do
  TAR=$(ls "${WORK}"/${vol}-*.tar.gz 2>/dev/null | head -1) || true
  if [ -n "${TAR:-}" ] && [ -d "/backup-volumes/${vol}" ]; then
    echo "  - ${vol}: extract to /backup-volumes/${vol}"
    rm -rf "/backup-volumes/${vol}/"*
    tar xzf "${TAR}" -C "/backup-volumes/${vol}"
  fi
done

rm -rf "${WORK}"
echo "[restore] === DONE ==="
```
  </action>
  <verify>bash -n infrastructure/backup/restore.sh && grep -E "(pg_restore|mongorestore|qdrant.*upload)" infrastructure/backup/restore.sh | wc -l</verify>
  <acceptance_criteria>
    - `restore.sh` syntaxe shell valide
    - Contient `pg_restore`, `mongorestore`, et upload snapshot Qdrant
    - Supporte argument `latest` (résout le dernier backup dispo)
    - ≥ 40 lignes
  </acceptance_criteria>
  <done>Restore script opérationnel</done>
</task>

<task type="auto">
  <name>Task 4: Ajouter le service xbrain-backup dans docker-compose.yml</name>
  <files>infrastructure/docker-compose.yml, .gitignore</files>
  <read_first>infrastructure/docker-compose.yml</read_first>
  <action>
Ajouter à `infrastructure/docker-compose.yml` :

```yaml
  xbrain-backup:
    build:
      context: ./backup
      dockerfile: Dockerfile
    image: xbrain/backup:phase1
    container_name: xbrain-backup
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_HOST: postgres
      LIBRECHAT_MONGO_URI: ${LIBRECHAT_MONGO_URI}
      QDRANT_URL: ${QDRANT_URL}
      GCS_BACKUP_BUCKET: ${GCS_BACKUP_BUCKET}
      GCS_SERVICE_ACCOUNT_KEY: /secrets/gcs-backup-sa.json
      BACKUP_RETENTION_DAILY: ${BACKUP_RETENTION_DAILY:-7}
    volumes:
      # Read-only mount des volumes à sauvegarder
      - openwebui_data:/backup-volumes/openwebui-data:ro
      - librechat_uploads:/backup-volumes/librechat-uploads:ro
      - librechat_meili:/backup-volumes/librechat-meili:ro
      # Service account key (host-mounted depuis /home/user/secrets/)
      - /home/user/secrets/gcs-backup-sa.json:/secrets/gcs-backup-sa.json:ro
    networks: [xbrain_net]
    mem_limit: 256m
    depends_on:
      postgres: { condition: service_healthy }
      qdrant: { condition: service_healthy }
      librechat-mongo: { condition: service_healthy }
```

**`.gitignore`** : ajouter `secrets/` (le dossier où on stocke les clés JSON localement avant transfert vers VM) si pas déjà présent.
  </action>
  <verify>grep -q "xbrain-backup:" infrastructure/docker-compose.yml && grep -q "secrets/" .gitignore</verify>
  <acceptance_criteria>
    - `infrastructure/docker-compose.yml` contient le service `xbrain-backup`
    - Mount des 3 volumes en read-only
    - Mount du service account key host-side `/home/user/secrets/gcs-backup-sa.json`
    - `.gitignore` exclut `secrets/`
  </acceptance_criteria>
  <done>Container backup intégré au compose</done>
</task>

<task type="auto">
  <name>Task 5: restore-test.sh — test E2E sur env isolé (success criterion 5)</name>
  <files>infrastructure/scripts/restore-test.sh</files>
  <read_first>infrastructure/backup/restore.sh, .planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md (section Q5)</read_first>
  <action>
**`infrastructure/scripts/restore-test.sh`** : lance un compose isolé avec un suffixe `-restore`, restore le dernier backup, vérifie que les données sont là.

```bash
#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")/.."  # cd infrastructure/

PROJECT_NAME="xbrain-restore-test"
COMPOSE_FILE="docker-compose.restore-test.yml"

echo "[restore-test] === START ==="

# 1. Generate a stripped-down compose for restore test (postgres + qdrant + librechat-mongo + xbrain-backup uniquement)
cat > "${COMPOSE_FILE}" <<'EOF'
networks:
  xbrain_restore_net:
    driver: bridge
volumes:
  rt_pg:        { name: rt_pg }
  rt_qdrant:    { name: rt_qdrant }
  rt_mongo:     { name: rt_mongo }
  rt_openwebui: { name: rt_openwebui }
  rt_uploads:   { name: rt_uploads }
  rt_meili:     { name: rt_meili }
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: $POSTGRES_USER
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_DB: $POSTGRES_DB
    volumes: [rt_pg:/var/lib/postgresql/data]
    networks: [xbrain_restore_net]
    healthcheck:
      test: ["CMD-SHELL","pg_isready -U $POSTGRES_USER"]
      interval: 5s
  qdrant:
    image: qdrant/qdrant:v1.17.1
    volumes: [rt_qdrant:/qdrant/storage]
    networks: [xbrain_restore_net]
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/6333'"]
      interval: 5s
  librechat-mongo:
    image: mongo:7
    command: ["--replSet","rs0","--bind_ip_all"]
    volumes: [rt_mongo:/data/db]
    networks: [xbrain_restore_net]
    healthcheck:
      test: ["CMD","mongosh","--quiet","--eval","try{rs.status().ok?0:1}catch(e){1}"]
      interval: 5s
EOF

# 2. Démarrer l'env isolé
echo "[1/5] Start isolated stack..."
docker compose -p "${PROJECT_NAME}" -f "${COMPOSE_FILE}" --env-file ../.env up -d
sleep 30

# 3. Init Mongo replset
docker compose -p "${PROJECT_NAME}" -f "${COMPOSE_FILE}" exec -T librechat-mongo mongosh --quiet --eval \
  "try{rs.status()}catch(e){rs.initiate({_id:'rs0',members:[{_id:0,host:'librechat-mongo:27017'}]})}"

# 4. Run restore.sh dans un container ad-hoc avec xbrain-backup image, pointant sur les services rt-*
echo "[2/5] Run restore from latest backup..."
docker run --rm \
  --network "${PROJECT_NAME}_xbrain_restore_net" \
  -v openwebui_data:/backup-volumes/openwebui-data \
  -v librechat_uploads:/backup-volumes/librechat-uploads \
  -v librechat_meili:/backup-volumes/librechat-meili \
  -v /home/user/secrets/gcs-backup-sa.json:/secrets/gcs-backup-sa.json:ro \
  -e POSTGRES_USER="$POSTGRES_USER" \
  -e POSTGRES_PASSWORD="$POSTGRES_PASSWORD" \
  -e POSTGRES_DB="$POSTGRES_DB" \
  -e POSTGRES_HOST=postgres \
  -e LIBRECHAT_MONGO_URI="mongodb://librechat-mongo:27017/LibreChat?replicaSet=rs0" \
  -e QDRANT_URL="http://qdrant:6333" \
  -e GCS_BACKUP_BUCKET="$GCS_BACKUP_BUCKET" \
  -e GCS_SERVICE_ACCOUNT_KEY=/secrets/gcs-backup-sa.json \
  xbrain/backup:phase1 \
  /scripts/restore.sh latest

# 5. Smoke tests
echo "[3/5] Smoke test Postgres..."
docker compose -p "${PROJECT_NAME}" -f "${COMPOSE_FILE}" exec -T postgres \
  psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "SELECT count(*) AS msg_count FROM messages; SELECT count(*) AS team_count FROM teams; SELECT count(*) AS user_count FROM users;"

echo "[4/5] Smoke test Mongo..."
docker compose -p "${PROJECT_NAME}" -f "${COMPOSE_FILE}" exec -T librechat-mongo \
  mongosh --quiet LibreChat --eval "print('messages count:', db.messages.countDocuments({}))"

echo "[5/5] Smoke test Qdrant..."
docker compose -p "${PROJECT_NAME}" -f "${COMPOSE_FILE}" exec -T qdrant \
  curl -fsS http://localhost:6333/collections | jq .

# 6. Teardown
echo "[teardown] Removing isolated stack + volumes..."
docker compose -p "${PROJECT_NAME}" -f "${COMPOSE_FILE}" down -v
rm "${COMPOSE_FILE}"

echo "[restore-test] === SUCCESS ==="
echo "Success criterion 5 (Phase 1 done gate): VALIDATED"
```
  </action>
  <verify>bash -n infrastructure/scripts/restore-test.sh && grep -q "SELECT count" infrastructure/scripts/restore-test.sh</verify>
  <acceptance_criteria>
    - `infrastructure/scripts/restore-test.sh` syntaxe valide
    - Crée un compose isolé (`-p xbrain-restore-test`)
    - Lance restore.sh dans un container temporaire
    - Smoke tests Postgres + Mongo + Qdrant après restore
    - Teardown complet (`down -v`) à la fin
    - ≥ 50 lignes
  </acceptance_criteria>
  <done>Test E2E restore en place</done>
</task>

<task type="auto">
  <name>Task 6: Premier backup manuel + premier restore-test</name>
  <action>
Avant de fermer le plan, exécuter manuellement :

```bash
# 1. Build l'image xbrain-backup et up le service sur la VM
make sync && make deploy

# 2. Lancer un backup à la demande (plutôt qu'attendre 02:00 UTC)
make backup
# Doit afficher "BACKUP DONE" et lister les artifacts uploadés

# 3. Vérifier dans GCS
gsutil ls gs://xbrain-backups-prod/$(date -u +%Y-%m-%d)/

# 4. Lancer le restore-test E2E (depuis ton poste, mais ça lance le test sur la VM)
ssh -i ~/.ssh/xbrain_key user@__VM_HOST__ 'cd /home/user/xbrain && bash infrastructure/scripts/restore-test.sh'
```
  </action>
  <verify>ssh -i ~/.ssh/xbrain_key user@__VM_HOST__ 'gsutil ls gs://xbrain-backups-prod/' 2>&1 | grep -q "$(date -u +%Y-%m-%d)"</verify>
  <acceptance_criteria>
    - `make backup` exit 0, log "BACKUP DONE"
    - `gsutil ls gs://xbrain-backups-prod/<today>/` montre 4+ artifacts (postgres dump, mongo archive, qdrant tar, openwebui-data tar)
    - `restore-test.sh` exit 0, log "Success criterion 5 (Phase 1 done gate): VALIDATED"
  </acceptance_criteria>
  <done>Backup + restore validés en conditions réelles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Système backup + restore Phase 1 :
- Container xbrain-backup tourne en cron quotidien (02:00 UTC)
- Premier backup réel uploadé sur gs://xbrain-backups-prod/
- Test E2E restore validé sur env isolé (success criterion 5 → VALIDATED)
  </what-built>
  <how-to-verify>
1. `gsutil ls gs://xbrain-backups-prod/` montre au moins 1 dossier daté d'aujourd'hui
2. `gsutil ls gs://xbrain-backups-prod/<today>/` liste 4+ fichiers (postgres-*.dump, librechat-mongo-*.archive.gz, qdrant-*.tar.gz, openwebui-data-*.tar.gz)
3. Logs `make vm-logs xbrain-backup` montrent "BACKUP DONE" sans erreur
4. La sortie de `restore-test.sh` finit par "Success criterion 5 (Phase 1 done gate): VALIDATED"
5. (Optionnel) Vérifier que le bucket coût + 7-jour retention sont bien configurés
  </how-to-verify>
  <resume-signal>Type "approved" si backup + restore-test passent, sinon décris ce qui a échoué</resume-signal>
</task>

</tasks>

<verification>
- [ ] `gsutil ls gs://xbrain-backups-prod/$(date -u +%Y-%m-%d)/` liste 4+ artifacts
- [ ] `make vm-logs` ne montre pas d'erreur dans le container `xbrain-backup` après un cycle
- [ ] `bash infrastructure/scripts/restore-test.sh` exit 0 + "VALIDATED" dans le log
- [ ] Le service account `xbrain-backup-sa@xbrain-495115.iam.gserviceaccount.com` a `roles/storage.objectAdmin` UNIQUEMENT sur ce bucket (pas sur tout le projet)
- [ ] Aucun `.json` de service account committé dans git (`git ls-files | grep -E "\\.json$" | xargs -I{} grep -l "private_key" {}` doit retourner vide)
</verification>

<success_criteria>
- All tasks completed
- **Success criterion 5 de la roadmap Phase 1 : VALIDATED** (test restore exécuté avec succès sur env clean)
- Backups quotidiens auto-uploadés sur GCS
- Retention 7 jours appliquée
- Phase 1 peut être déclarée DONE
</success_criteria>

<output>
Après completion, créer `.planning/phases/01-socle-infra-frontends-memory-api/01-06-SUMMARY.md`
</output>
