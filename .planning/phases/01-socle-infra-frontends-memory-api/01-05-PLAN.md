---
phase: 01-socle-infra-frontends-memory-api
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - infrastructure/docker-compose.yml
  - infrastructure/librechat/librechat.yaml
  - infrastructure/nginx/conf.d/00-health.conf
  - infrastructure/nginx/conf.d/10-xbrain.conf
  - infrastructure/scripts/init-mongo-replset.sh
  - infrastructure/scripts/deploy-vm.sh
autonomous: false
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-04
  - CHAT-05
  - CHAT-08
must_haves:
  truths:
    - "docker compose up -d sur la VM démarre LibreChat + Open WebUI + MongoDB (replica set rs0) + MeiliSearch + nginx en plus des services déjà présents (postgres + qdrant + memory-api + librechat-bridge + openwebui-pipeline)"
    - "Tous les containers atteignent l'état healthy en moins de 2 min"
    - "http://__VM_HOST__/ retourne la page LibreChat"
    - "http://__VM_HOST__/openwebui/ retourne la page Open WebUI"
    - "http://__VM_HOST__/api/v1/healthz retourne {\"status\":\"ok\"}"
    - "Le MongoDB de LibreChat est en mode replica set (rs0) — change streams fonctionnent (le bridge passe healthy)"
    - "librechat.yaml configure Anthropic + OpenAI + xAI Grok comme endpoints"
    - "Le user peut s'authentifier via Google sur LibreChat ET sur Open WebUI avec le même compte (1 OAuth client partagé)"
  artifacts:
    - path: infrastructure/librechat/librechat.yaml
      provides: "Config LibreChat: 3 LLM endpoints, social login Google"
      contains: "anthropic"
    - path: infrastructure/nginx/conf.d/10-xbrain.conf
      provides: "Routing: / → LibreChat, /openwebui/ → Open WebUI, /api/ → memory-api"
      contains: "openwebui"
    - path: infrastructure/scripts/init-mongo-replset.sh
      provides: "Init le replica set rs0 dans MongoDB single-node"
      contains: "rs.initiate"
    - path: infrastructure/scripts/deploy-vm.sh
      provides: "Script idempotent : rsync + compose pull/build + up -d"
      contains: "docker compose"
  key_links:
    - from: infrastructure/docker-compose.yml
      to: infrastructure/librechat/librechat.yaml
      via: "volume mount ro"
      pattern: "librechat.yaml"
    - from: infrastructure/nginx/conf.d/10-xbrain.conf
      to: infrastructure/docker-compose.yml
      via: "upstream pointing to service names (librechat:3080, openwebui:8080, memory-api:8000)"
      pattern: "upstream"
---

<objective>
Finaliser le `docker-compose.yml` avec les services LibreChat + LibreChat-MongoDB (en mode replica set) + LibreChat-MeiliSearch + Open WebUI ; écrire `librechat.yaml` (config 3 LLMs + Google OAuth) ; configurer nginx pour router les 3 surfaces (LibreChat, Open WebUI, memory-api) ; déployer sur la VM et valider que tout est healthy.

Purpose : C'est le plan qui transforme tous les services individuels en un système déployé accessible publiquement. Sans ce plan, on a du code mais pas de déploiement.

Output : Stack live à `http://__VM_HOST__` avec les 3 surfaces accessibles.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
@D:/VSC/xbrain/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md
@.planning/phases/01-socle-infra-frontends-memory-api/01-02-PLAN.md
@.planning/phases/01-socle-infra-frontends-memory-api/01-03-PLAN.md
@.planning/phases/01-socle-infra-frontends-memory-api/01-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire librechat.yaml (config 3 endpoints LLM + social login)</name>
  <files>infrastructure/librechat/librechat.yaml</files>
  <read_first>.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md (section Q4 — LibreChat OIDC)</read_first>
  <action>
Créer `infrastructure/librechat/librechat.yaml` :

```yaml
version: 1.2.4
cache: true

interface:
  privacyPolicy:
    externalUrl: ""
    openNewTab: false
  termsOfService:
    externalUrl: ""
    openNewTab: false

registration:
  socialLogins: ["google"]
  allowedDomains:
    - "*"

# Endpoints LLM (les API keys viennent de .env via variables référencées)
endpoints:
  custom:
    - name: "Anthropic"
      apiKey: "${ANTHROPIC_API_KEY}"
      baseURL: "https://api.anthropic.com/v1"
      models:
        default: ["claude-3-5-sonnet-latest", "claude-3-5-haiku-latest", "claude-3-opus-latest"]
        fetch: false
      titleConvo: true
      titleModel: "claude-3-5-haiku-latest"
      modelDisplayLabel: "Anthropic"

    - name: "OpenAI"
      apiKey: "${OPENAI_API_KEY}"
      baseURL: "https://api.openai.com/v1"
      models:
        default: ["gpt-4o", "gpt-4o-mini", "gpt-4-turbo"]
        fetch: false
      titleConvo: true
      titleModel: "gpt-4o-mini"
      modelDisplayLabel: "OpenAI"

    - name: "xAI"
      apiKey: "${XAI_API_KEY}"
      baseURL: "https://api.x.ai/v1"
      models:
        default: ["grok-2-latest", "grok-2-mini"]
        fetch: false
      titleConvo: false
      modelDisplayLabel: "Grok"

# File handling (CHAT-04 partiel — upload accepté, RAG vraie en Phase 2)
fileConfig:
  endpoints:
    Anthropic:
      fileLimit: 5
      fileSizeLimit: 20
      totalSizeLimit: 50
      supportedMimeTypes:
        - "image/.+"
        - "application/pdf"
    OpenAI:
      fileLimit: 5
      fileSizeLimit: 20
      totalSizeLimit: 50
      supportedMimeTypes:
        - "image/.+"
        - "application/pdf"
```

Référencer les API keys via `${ANTHROPIC_API_KEY}`, etc. — LibreChat les résout depuis l'env du container.
  </action>
  <verify>test -f infrastructure/librechat/librechat.yaml && grep -c "name:" infrastructure/librechat/librechat.yaml</verify>
  <acceptance_criteria>
    - `infrastructure/librechat/librechat.yaml` existe et contient les 3 endpoints (`Anthropic`, `OpenAI`, `xAI`)
    - `socialLogins: ["google"]` présent
    - `${ANTHROPIC_API_KEY}` référencé (pas de clé hardcodée)
  </acceptance_criteria>
  <done>Config LibreChat en place</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter LibreChat + MongoDB(replica set) + MeiliSearch + Open WebUI dans docker-compose.yml</name>
  <files>infrastructure/docker-compose.yml, infrastructure/scripts/init-mongo-replset.sh</files>
  <read_first>infrastructure/docker-compose.yml, .planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md (section Q5 — services table)</read_first>
  <action>
**Ajouter à `infrastructure/docker-compose.yml`** (après les services déjà présents) :

```yaml
  librechat-mongo:
    image: mongo:7
    container_name: xbrain-librechat-mongo
    restart: unless-stopped
    command: ["--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - librechat_mongo:/data/db
      - ./scripts/init-mongo-replset.sh:/docker-entrypoint-initdb.d/init-replset.sh:ro
    networks: [xbrain_net]
    mem_limit: 256m
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "try { rs.status().ok ? 0 : 1 } catch(e) { 1 }"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  librechat-meili:
    image: getmeili/meilisearch:v1.10
    container_name: xbrain-librechat-meili
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
      MEILI_NO_ANALYTICS: "true"
    volumes:
      - librechat_meili:/meili_data
    networks: [xbrain_net]
    mem_limit: 192m
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:7700/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5

  librechat:
    image: ghcr.io/danny-avila/librechat:v0.8.5
    container_name: xbrain-librechat
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 3080
      MONGO_URI: ${MONGO_URI}
      DOMAIN_CLIENT: ${DOMAIN_CLIENT}
      DOMAIN_SERVER: ${DOMAIN_SERVER}
      MEILI_HOST: ${MEILI_HOST}
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
      ALLOW_REGISTRATION: ${ALLOW_REGISTRATION:-true}
      ALLOW_SOCIAL_LOGIN: ${ALLOW_SOCIAL_LOGIN:-true}
      ALLOW_SOCIAL_REGISTRATION: ${ALLOW_SOCIAL_REGISTRATION:-true}
      JWT_SECRET: ${JWT_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      CREDS_KEY: ${CREDS_KEY}
      CREDS_IV: ${CREDS_IV}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      GOOGLE_CALLBACK_URL: ${GOOGLE_CALLBACK_URL}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      XAI_API_KEY: ${XAI_API_KEY:-}
      APP_TITLE: ${APP_TITLE:-xbrain}
      SESSION_EXPIRY: ${SESSION_EXPIRY}
      REFRESH_TOKEN_EXPIRY: ${REFRESH_TOKEN_EXPIRY}
    volumes:
      - librechat_uploads:/app/client/public/uploads
      - ./librechat/librechat.yaml:/app/librechat.yaml:ro
    networks: [xbrain_net]
    mem_limit: 384m
    depends_on:
      librechat-mongo: { condition: service_healthy }
      librechat-meili: { condition: service_healthy }
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3080/api/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 60s

  openwebui:
    image: ghcr.io/open-webui/open-webui:v0.9.0
    container_name: xbrain-openwebui
    restart: unless-stopped
    environment:
      WEBUI_AUTH: ${WEBUI_AUTH:-true}
      WEBUI_SECRET_KEY: ${OPENWEBUI_SECRET_KEY}
      ENABLE_OAUTH_SIGNUP: ${ENABLE_OAUTH_SIGNUP:-true}
      OAUTH_PROVIDER_NAME: ${OAUTH_PROVIDER_NAME:-Google}
      OAUTH_GOOGLE_CLIENT_ID: ${OAUTH_GOOGLE_CLIENT_ID}
      OAUTH_GOOGLE_CLIENT_SECRET: ${OAUTH_GOOGLE_CLIENT_SECRET}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI}
      OPENAI_API_BASE_URL: ${OPENAI_API_BASE_URL}
      OPENAI_API_KEY: ${PIPELINE_API_KEY}  # Open WebUI passe cette key au pipeline
      WEBUI_URL: http://__VM_HOST__/openwebui
    volumes:
      - openwebui_data:/app/backend/data
    networks: [xbrain_net]
    mem_limit: 512m
    depends_on:
      openwebui-pipeline: { condition: service_healthy }
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
```

**`infrastructure/scripts/init-mongo-replset.sh`** (lancé par MongoDB au premier boot via /docker-entrypoint-initdb.d/) :
```bash
#!/bin/bash
# Init le replica set rs0 (single node) — nécessaire pour les change streams.
sleep 5
mongosh --quiet --eval "
try {
  rs.status();
  print('rs0 already initialized');
} catch (e) {
  rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: 'librechat-mongo:27017' }] });
  print('rs0 initialized');
}
"
```

`chmod +x infrastructure/scripts/init-mongo-replset.sh` après l'écriture.
  </action>
  <verify>cd infrastructure && docker compose --env-file ../.env.example config 2>&1 | grep -E "(librechat|openwebui|mongo|meili)" | head -10 && test -x scripts/init-mongo-replset.sh</verify>
  <acceptance_criteria>
    - `infrastructure/docker-compose.yml` contient les 4 services : `librechat-mongo`, `librechat-meili`, `librechat`, `openwebui`
    - `librechat-mongo` a `command: ["--replSet", "rs0", "--bind_ip_all"]`
    - `librechat` a `volumes` qui mount `librechat.yaml` en read-only
    - `openwebui` a `OPENAI_API_BASE_URL: ${OPENAI_API_BASE_URL}` (qui pointe sur openwebui-pipeline)
    - `infrastructure/scripts/init-mongo-replset.sh` est exécutable et contient `rs.initiate`
  </acceptance_criteria>
  <done>Stack docker-compose complet</done>
</task>

<task type="auto">
  <name>Task 3: nginx routing config (10-xbrain.conf)</name>
  <files>infrastructure/nginx/conf.d/10-xbrain.conf, infrastructure/nginx/conf.d/00-health.conf</files>
  <read_first>infrastructure/nginx/conf.d/00-health.conf, .planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md (section Q5 — nginx config)</read_first>
  <action>
**Remplacer** le `00-health.conf` par une version sans le bloc 503 catch-all (qui bloquait tout en bootstrap) :
```nginx
server {
  listen 80;
  server_name _;
  location /nginx-health { return 200 "ok\n"; access_log off; }
}
```
(garde juste le healthcheck, le routing principal est dans 10-xbrain.conf)

**Créer `infrastructure/nginx/conf.d/10-xbrain.conf`** :
```nginx
upstream librechat   { server librechat:3080; }
upstream openwebui   { server openwebui:8080; }
upstream memory_api  { server memory-api:8000; }

map $http_upgrade $connection_upgrade {
  default upgrade;
  ''      close;
}

server {
  listen 80 default_server;
  server_name _;

  # Healthcheck nginx (utilisé par docker)
  location /nginx-health { return 200 "ok\n"; access_log off; }

  client_max_body_size 50m;

  # === memory-api API admin ===
  location /api/ {
    proxy_pass http://memory_api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Authorization $http_authorization;
    proxy_read_timeout 60s;
  }

  # === Open WebUI sous /openwebui/ ===
  location = /openwebui { return 302 /openwebui/; }
  location /openwebui/ {
    proxy_pass http://openwebui/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /openwebui;

    # WebSocket / SSE pour le streaming des chats
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_buffering off;
    proxy_read_timeout 600s;
  }

  # === LibreChat à la racine ===
  location / {
    proxy_pass http://librechat;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # SSE streaming
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_buffering off;
    proxy_read_timeout 600s;
  }
}
```
  </action>
  <verify>test -f infrastructure/nginx/conf.d/10-xbrain.conf && grep -c "upstream" infrastructure/nginx/conf.d/10-xbrain.conf</verify>
  <acceptance_criteria>
    - `infrastructure/nginx/conf.d/10-xbrain.conf` contient 3 `upstream` (librechat, openwebui, memory_api)
    - `proxy_buffering off` présent (SSE/streaming nécessaire)
    - `client_max_body_size 50m` présent (uploads PDF jusqu'à 20MB par conv)
    - `00-health.conf` ne contient plus de catch-all 503
  </acceptance_criteria>
  <done>nginx routing en place</done>
</task>

<task type="auto">
  <name>Task 4: Script deploy-vm.sh</name>
  <files>infrastructure/scripts/deploy-vm.sh</files>
  <read_first>Makefile</read_first>
  <action>
Créer `infrastructure/scripts/deploy-vm.sh` (idempotent, peut être ré-appelé) :

```bash
#!/bin/bash
set -euo pipefail

VM_HOST="${VM_HOST:-__VM_HOST__}"
VM_USER="${VM_USER:-user}"
SSH_KEY="${SSH_KEY:-$HOME/.ssh/xbrain_key}"
REMOTE_DIR="/home/${VM_USER}/xbrain"

echo "==> [1/5] Sanity check local"
test -f .env || { echo "ERROR: .env missing. Run 'cp .env.example .env' and fill values."; exit 1; }
make env-check

echo "==> [2/5] Rsync code vers ${VM_USER}@${VM_HOST}:${REMOTE_DIR}"
rsync -avz --delete \
  --exclude='.git' \
  --exclude='node_modules' \
  --exclude='__pycache__' \
  --exclude='.venv' \
  --exclude='backups' \
  --exclude='.pytest_cache' \
  --exclude='.mypy_cache' \
  --exclude='.ruff_cache' \
  -e "ssh -i ${SSH_KEY} -o BatchMode=yes" \
  ./ "${VM_USER}@${VM_HOST}:${REMOTE_DIR}/"

echo "==> [3/5] docker compose pull (images upstream)"
ssh -i "${SSH_KEY}" -o BatchMode=yes "${VM_USER}@${VM_HOST}" \
  "cd ${REMOTE_DIR} && docker compose -f infrastructure/docker-compose.yml --env-file .env pull librechat librechat-mongo librechat-meili openwebui postgres qdrant nginx"

echo "==> [4/5] docker compose build (images locales)"
ssh -i "${SSH_KEY}" -o BatchMode=yes "${VM_USER}@${VM_HOST}" \
  "cd ${REMOTE_DIR} && docker compose -f infrastructure/docker-compose.yml --env-file .env build memory-api librechat-bridge openwebui-pipeline"

echo "==> [5/5] docker compose up -d + wait healthy"
ssh -i "${SSH_KEY}" -o BatchMode=yes "${VM_USER}@${VM_HOST}" \
  "cd ${REMOTE_DIR} && docker compose -f infrastructure/docker-compose.yml --env-file .env up -d --remove-orphans"

echo "==> Wait 90s for healthchecks..."
sleep 90

echo "==> Status:"
ssh -i "${SSH_KEY}" -o BatchMode=yes "${VM_USER}@${VM_HOST}" \
  "cd ${REMOTE_DIR} && docker compose -f infrastructure/docker-compose.yml ps"

echo ""
echo "==> Smoke tests:"
echo "    LibreChat:    http://${VM_HOST}/"
echo "    Open WebUI:   http://${VM_HOST}/openwebui/"
echo "    memory-api:   http://${VM_HOST}/api/v1/healthz"
echo ""
echo "Run: curl -fsS http://${VM_HOST}/api/v1/healthz"
```

`chmod +x infrastructure/scripts/deploy-vm.sh`
  </action>
  <verify>test -x infrastructure/scripts/deploy-vm.sh && bash -n infrastructure/scripts/deploy-vm.sh</verify>
  <acceptance_criteria>
    - `infrastructure/scripts/deploy-vm.sh` exécutable, syntaxe shell valide (`bash -n`)
    - Le script fait `rsync` puis `docker compose pull`/`build`/`up -d`
    - Affiche les 3 URLs smoke test à la fin
  </acceptance_criteria>
  <done>Deploy automation en place</done>
</task>

<task type="auto">
  <name>Task 5: Pré-vol — vérifier que le user a configuré .env et OAuth Google</name>
  <action>
Vérifier que l'utilisateur a (a) rempli `.env` avec les vraies valeurs et (b) configuré Google OAuth client avec les bons redirect URIs.

Lancer :
```bash
make env-check
```

Si manque, afficher exactement ce qui manque + comment générer les secrets (`openssl rand -base64 32`, etc.).

Vérifier aussi que la VM est accessible :
```bash
ssh -i "$HOME/.ssh/xbrain_key" -o BatchMode=yes user@__VM_HOST__ 'docker --version && docker compose version'
```
  </action>
  <verify>make env-check && ssh -i "$HOME/.ssh/xbrain_key" -o BatchMode=yes user@__VM_HOST__ 'docker --version'</verify>
  <acceptance_criteria>
    - `make env-check` exit 0 (toutes les vars critiques présentes)
    - SSH vers VM marche, Docker installé
  </acceptance_criteria>
  <done>Pré-requis validés</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-needed>Le user doit avoir configuré le client Google OAuth avec les **deux** redirect URIs : `http://__VM_HOST__/oauth/google/callback` ET `http://__VM_HOST__/openwebui/oauth/google/callback`. Sans ça, l'auth échoue à l'étape "Sign in with Google".</what-needed>
  <how-to-do>
1. https://console.cloud.google.com → projet `xbrain-495115`
2. APIs & Services → Credentials
3. Soit créer un nouvel OAuth client Web, soit éditer celui existant
4. Authorized redirect URIs : ajouter les deux URLs ci-dessus (copier-coller exact)
5. Copier le Client ID + Client Secret dans `.env` (vars `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `OAUTH_GOOGLE_CLIENT_ID`, `OAUTH_GOOGLE_CLIENT_SECRET` — ce sont les mêmes valeurs sous deux noms)
  </how-to-do>
  <resume-signal>Type "done" quand les 2 redirect URIs sont configurés ET le .env contient les bons GOOGLE_CLIENT_ID/SECRET</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Lancer le déploiement</name>
  <action>
Exécuter :
```bash
bash infrastructure/scripts/deploy-vm.sh
```

Surveiller les logs si un container ne devient pas healthy. Commandes utiles :
```bash
make vm-ps              # voir l'état healthy de chaque container
make vm-logs            # tail logs de tous les services
ssh -i ~/.ssh/xbrain_key user@__VM_HOST__ 'cd /home/user/xbrain && docker compose -f infrastructure/docker-compose.yml logs librechat --tail=100'
```

Pitfalls à surveiller :
- **MongoDB rs.initiate** : le script init-replset.sh ne se relance qu'au premier boot du volume. Si tu as déjà un volume `librechat_mongo` existant sans rs0, faut le recréer ou le faire à la main : `docker compose exec librechat-mongo mongosh --eval 'rs.initiate({_id:"rs0",members:[{_id:0,host:"librechat-mongo:27017"}]})'`
- **librechat-bridge healthy** : dépend de Mongo en mode rs0. Si bridge reste unhealthy, vérifier que `rs.status().ok == 1` côté Mongo.
- **OOM** : surveiller `docker stats`. Si Postgres ou Qdrant restartent en boucle, c'est mem_limit trop bas.
  </action>
  <verify>curl -fsS --max-time 10 http://__VM_HOST__/api/v1/healthz | grep -q '"status":"ok"'</verify>
  <acceptance_criteria>
    - `curl http://__VM_HOST__/api/v1/healthz` retourne `{"status":"ok"}`
    - `make vm-ps` montre tous les containers en `Up (healthy)`
    - Aucun container en restart loop (`docker compose ps` ne montre pas de "Restarting")
  </acceptance_criteria>
  <done>Stack déployée et healthy</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Stack xbrain Phase 1 déployé sur la VM. URLs publiques :
- LibreChat : http://__VM_HOST__/
- Open WebUI : http://__VM_HOST__/openwebui/
- memory-api healthz : http://__VM_HOST__/api/v1/healthz
  </what-built>
  <how-to-verify>
1. Visiter http://__VM_HOST__/ — la page LibreChat doit charger (titre "xbrain")
2. Cliquer "Sign in with Google" → écran Google → choisir compte → retour LibreChat connecté
3. Sélectionner Anthropic → Claude 3.5 Sonnet → envoyer "ping" → recevoir une réponse
4. Visiter http://__VM_HOST__/openwebui/ — Open WebUI charge
5. "Sign in with Google" sur Open WebUI → même compte → connecté
6. Sélectionner un model dans le dropdown → envoyer "ping" → recevoir réponse
7. Vérifier que les 2 conversations apparaissent dans memory-api (curl avec un service JWT, ou attendre plan 01-06 pour les tests E2E formels)
  </how-to-verify>
  <resume-signal>Type "approved" si LibreChat + Open WebUI accessibles et chat fonctionne, sinon décris ce qui ne marche pas</resume-signal>
</task>

</tasks>

<verification>
- [ ] `cd infrastructure && docker compose --env-file ../.env config` parse sans erreur
- [ ] Tous les containers `Up (healthy)` après deploy
- [ ] `curl http://__VM_HOST__/api/v1/healthz` retourne 200 + `{"status":"ok"}`
- [ ] `curl http://__VM_HOST__/` retourne 200 (HTML LibreChat)
- [ ] `curl http://__VM_HOST__/openwebui/` retourne 200 (HTML Open WebUI)
- [ ] User a vérifié visuellement que login Google fonctionne sur les 2 frontends
</verification>

<success_criteria>
- All tasks completed
- Couvre success criteria 1 (Google SSO sur les 2 frontends), 4 (docker compose up healthy)
- Préparé pour le success criterion 5 (backup/restore — plan 01-06)
- Stack live à http://__VM_HOST__
</success_criteria>

<output>
Après completion, créer `.planning/phases/01-socle-infra-frontends-memory-api/01-05-SUMMARY.md`
</output>
