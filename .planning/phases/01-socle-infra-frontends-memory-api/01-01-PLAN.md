---
phase: 01-socle-infra-frontends-memory-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
  - Makefile
  - .env.example
  - apps/.gitkeep
  - services/.gitkeep
  - packages/.gitkeep
  - infrastructure/.gitkeep
  - infrastructure/docker-compose.yml
  - infrastructure/nginx/conf.d/.gitkeep
  - packages/schemas/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Le repo a une structure monorepo cohérente : /apps, /services, /packages, /infrastructure"
    - "make help liste tous les targets utiles (deploy, logs, restore, etc.)"
    - ".env.example documente toutes les variables d'environnement nécessaires Phase 1"
    - "docker-compose.yml minimum existe et passe `docker compose config` sans erreur"
  artifacts:
    - path: Makefile
      provides: "Targets: help, build, deploy, logs, restore, backup, fmt, lint, test"
      contains: "deploy"
    - path: .env.example
      provides: "Template env vars Phase 1"
      contains: "POSTGRES_PASSWORD"
    - path: infrastructure/docker-compose.yml
      provides: "Compose skeleton (services seront étoffés en 01-05)"
      contains: "version"
    - path: README.md
      provides: "Quickstart Phase 1: clone, configure .env, deploy"
      min_lines: 30
  key_links: []
---

<objective>
Mettre en place le squelette monorepo pour xbrain : structure de dossiers, fichiers communs (Makefile, .gitignore, .env.example), README de quickstart et docker-compose.yml minimum.

Purpose : Les 5 plans suivants (memory-api, bridges, pipeline, deploy, backup) ont besoin d'un terrain de jeu cohérent. Sans ce scaffold, chaque plan invente sa structure et on finit avec du chaos.

Output : Un repo prêt à recevoir le code Phase 1, sans aucune logique métier encore.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer la structure de dossiers + .gitignore</name>
  <files>.gitignore, apps/.gitkeep, services/.gitkeep, packages/.gitkeep, infrastructure/.gitkeep, infrastructure/nginx/conf.d/.gitkeep, packages/schemas/README.md</files>
  <read_first>CLAUDE.md, .planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md</read_first>
  <action>
Créer la structure monorepo (mkdir -p) :
- `apps/` (memory-api ira ici plan 01-02, librechat-bridge plan 01-03, openwebui-pipeline plan 01-04)
- `services/` (réservé Phase 3 — scraper, calendar, drive-sync)
- `packages/` (schemas partagés)
- `packages/schemas/` (futur tagging-contract.py / .ts partagés)
- `infrastructure/` (docker-compose, nginx, scripts deploy)
- `infrastructure/nginx/conf.d/` (configs nginx, étoffé en 01-05)

Ajouter `.gitkeep` dans chaque dossier vide.

Écrire `packages/schemas/README.md` avec une note 2 lignes : "Shared tagging contract schemas (Phase 1: Pydantic in memory-api/app/models/tagging.py is canonical. Cross-language schemas extracted here in Phase 2)."

Écrire `.gitignore` complet :
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.*.local
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
npm-debug.log*

# Docker
.dockerignore.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# Secrets
*.pem
*.key
secrets/

# Build artifacts
*.log

# OS
.DS_Store
Thumbs.db

# Backups (jamais commit)
backups/
*.tar.gz
*.sql.gz
*.dump

# Local overrides
docker-compose.override.yml
```
  </action>
  <verify>test -d apps && test -d services && test -d packages && test -d infrastructure/nginx/conf.d && test -f .gitignore && test -f packages/schemas/README.md</verify>
  <acceptance_criteria>
    - `apps/`, `services/`, `packages/`, `infrastructure/`, `infrastructure/nginx/conf.d/` existent en tant que dossiers
    - `.gitignore` contient `__pycache__`, `.env`, `node_modules`, `backups/`
    - `packages/schemas/README.md` existe avec mention "tagging contract"
  </acceptance_criteria>
  <done>Structure monorepo en place</done>
</task>

<task type="auto">
  <name>Task 2: Écrire le Makefile (entrée unique des opérations)</name>
  <files>Makefile</files>
  <read_first>.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md, CLAUDE.md</read_first>
  <action>
Créer un Makefile à la racine avec les targets suivants. Premier target = `help`. Utiliser `.PHONY` pour chaque target. Le contenu **exact** :

```makefile
SHELL := /bin/bash
.DEFAULT_GOAL := help

# Charger .env si présent (silencieux si absent)
-include .env
export

# === Variables ===
COMPOSE := docker compose -f infrastructure/docker-compose.yml --env-file .env
VM_HOST ?= __VM_HOST__
VM_USER ?= user
SSH_KEY ?= $$HOME/.ssh/xbrain_key
SSH := ssh -i $(SSH_KEY) -o BatchMode=yes $(VM_USER)@$(VM_HOST)
RSYNC := rsync -avz --delete --exclude='.git' --exclude='node_modules' --exclude='__pycache__' --exclude='.venv' --exclude='backups'

# === Help ===
.PHONY: help
help:  ## Affiche cette aide
	@grep -E '^[a-zA-Z_-]+:.*?## .*$$' $(MAKEFILE_LIST) | sort | awk 'BEGIN {FS = ":.*?## "}; {printf "  \033[36m%-20s\033[0m %s\n", $$1, $$2}'

# === Local dev ===
.PHONY: build
build:  ## Build toutes les images Docker localement
	$(COMPOSE) build

.PHONY: up
up:  ## docker compose up -d (local)
	$(COMPOSE) up -d

.PHONY: down
down:  ## docker compose down (local)
	$(COMPOSE) down

.PHONY: logs
logs:  ## Tail logs de tous les services (local)
	$(COMPOSE) logs -f --tail=100

.PHONY: ps
ps:  ## Liste containers + healthchecks
	$(COMPOSE) ps

# === Tests ===
.PHONY: test
test:  ## Lance les tests memory-api
	cd apps/memory-api && pytest -v

.PHONY: lint
lint:  ## Lint Python (ruff)
	cd apps/memory-api && ruff check .
	cd apps/librechat-bridge && ruff check .
	cd apps/openwebui-pipeline && ruff check .

.PHONY: fmt
fmt:  ## Format Python (ruff format)
	cd apps/memory-api && ruff format .
	cd apps/librechat-bridge && ruff format .
	cd apps/openwebui-pipeline && ruff format .

# === Deploy VM ===
.PHONY: sync
sync:  ## Rsync code vers la VM (sans deploy)
	$(RSYNC) ./ $(VM_USER)@$(VM_HOST):/home/$(VM_USER)/xbrain/

.PHONY: deploy
deploy: sync  ## Sync + (re)build + up sur la VM
	$(SSH) 'cd /home/$(VM_USER)/xbrain && docker compose -f infrastructure/docker-compose.yml --env-file .env up -d --build'

.PHONY: vm-logs
vm-logs:  ## Tail logs sur la VM
	$(SSH) 'cd /home/$(VM_USER)/xbrain && docker compose -f infrastructure/docker-compose.yml logs -f --tail=100'

.PHONY: vm-ps
vm-ps:  ## ps sur la VM
	$(SSH) 'cd /home/$(VM_USER)/xbrain && docker compose -f infrastructure/docker-compose.yml ps'

.PHONY: vm-down
vm-down:  ## Arrête le stack sur la VM (sans détruire les volumes)
	$(SSH) 'cd /home/$(VM_USER)/xbrain && docker compose -f infrastructure/docker-compose.yml down'

.PHONY: ssh
ssh:  ## Ouvre une session SSH interactive sur la VM
	ssh -i $(SSH_KEY) $(VM_USER)@$(VM_HOST)

# === Backup / Restore ===
.PHONY: backup
backup:  ## Lance un backup manuel (cf. plan 01-06)
	$(SSH) 'cd /home/$(VM_USER)/xbrain && docker compose -f infrastructure/docker-compose.yml exec -T xbrain-backup /scripts/backup.sh'

.PHONY: restore-test
restore-test:  ## Test restore sur env clean (cf. plan 01-06, success criterion 5)
	@echo "Voir infrastructure/scripts/restore-test.sh"
	bash infrastructure/scripts/restore-test.sh

# === Utilities ===
.PHONY: env-check
env-check:  ## Vérifie que toutes les vars critiques sont dans .env
	@bash -c 'source .env 2>/dev/null && for v in POSTGRES_PASSWORD GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET BRIDGE_SHARED_SECRET MEILI_MASTER_KEY OPENWEBUI_SECRET_KEY; do \
		if [ -z "$${!v}" ]; then echo "MISSING: $$v"; exit 1; fi; \
	done && echo "All required env vars present."'
```

Indentation = TAB (Make exige TAB, pas spaces).
  </action>
  <verify>make help 2>&1 | head -20 ; test -f Makefile</verify>
  <acceptance_criteria>
    - `make help` affiche une liste de targets formatée (couleurs cyan)
    - `Makefile` contient les targets `deploy`, `vm-logs`, `backup`, `restore-test`, `env-check`
    - `make help` n'erreurise pas (exit 0)
  </acceptance_criteria>
  <done>Makefile fonctionnel, point d'entrée unique des opérations</done>
</task>

<task type="auto">
  <name>Task 3: Écrire .env.example complet (toutes les vars Phase 1)</name>
  <files>.env.example</files>
  <read_first>.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md</read_first>
  <action>
Créer `.env.example` à la racine. Doit lister TOUTES les variables d'environnement utilisées par n'importe quel service Phase 1. Pas de valeurs réelles, juste des placeholders explicites.

Contenu **exact** :

```bash
# ==========================================================
# xbrain Phase 1 — environment template
# Copy to .env and fill in values. Never commit .env.
# ==========================================================

# --- Domain / network ---
# Pas de domaine en Phase 1 — on utilise l'IP brute.
# Ces vars seront utilisées par LibreChat et nginx.
DOMAIN_URL=http://__VM_HOST__
DOMAIN_CLIENT=http://__VM_HOST__
DOMAIN_SERVER=http://__VM_HOST__

# --- Google OAuth (1 client unique partagé entre les 2 frontends) ---
GOOGLE_CLIENT_ID=__FILL__
GOOGLE_CLIENT_SECRET=__FILL__
GOOGLE_CALLBACK_URL=http://__VM_HOST__/oauth/google/callback
GOOGLE_REDIRECT_URI=http://__VM_HOST__/openwebui/oauth/google/callback

# --- Provider API keys (LibreChat & memory-api) ---
ANTHROPIC_API_KEY=__FILL__
OPENAI_API_KEY=__FILL__
XAI_API_KEY=__FILL__

# --- PostgreSQL (memory-api) ---
POSTGRES_USER=xbrain
POSTGRES_PASSWORD=__FILL_RANDOM_32_CHARS__
POSTGRES_DB=xbrain
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://xbrain:__FILL_RANDOM_32_CHARS__@postgres:5432/xbrain

# --- Qdrant (memory-api) ---
QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=

# --- memory-api ---
MEMORY_API_URL=http://memory-api:8000
MEMORY_API_PUBLIC_BASE=http://__VM_HOST__/api
JWT_ALGORITHM=HS256
BRIDGE_SHARED_SECRET=__FILL_RANDOM_64_CHARS__
LOG_LEVEL=INFO

# --- LibreChat ---
APP_TITLE=xbrain
HOST=0.0.0.0
PORT=3080
MONGO_URI=mongodb://librechat-mongo:27017/LibreChat
MEILI_HOST=http://librechat-meili:7700
MEILI_MASTER_KEY=__FILL_RANDOM_32_CHARS__
ALLOW_REGISTRATION=true
ALLOW_SOCIAL_LOGIN=true
ALLOW_SOCIAL_REGISTRATION=true
SESSION_EXPIRY=604800000
REFRESH_TOKEN_EXPIRY=2592000000
JWT_SECRET=__FILL_RANDOM_64_CHARS__
JWT_REFRESH_SECRET=__FILL_RANDOM_64_CHARS__
CREDS_KEY=__FILL_RANDOM_64_HEX__
CREDS_IV=__FILL_RANDOM_32_HEX__

# --- Open WebUI ---
WEBUI_AUTH=true
ENABLE_OAUTH_SIGNUP=true
OAUTH_PROVIDER_NAME=Google
OAUTH_GOOGLE_CLIENT_ID=__FILL__
OAUTH_GOOGLE_CLIENT_SECRET=__FILL__
OPENWEBUI_SECRET_KEY=__FILL_RANDOM_64_CHARS__
OPENAI_API_BASE_URL=http://openwebui-pipeline:9099

# --- librechat-bridge ---
LIBRECHAT_MONGO_URI=mongodb://librechat-mongo:27017/LibreChat?replicaSet=rs0
BRIDGE_BACKFILL_FROM=startup
BRIDGE_DEFAULT_TEAM_SCOPE=default

# --- openwebui-pipeline ---
PIPELINE_API_KEY=__FILL_RANDOM_32_CHARS__
PIPELINE_DEFAULT_TEAM_SCOPE=default

# --- Backups (Phase 1 = local + GCS) ---
GCS_BACKUP_BUCKET=xbrain-backups-prod
GCS_SERVICE_ACCOUNT_KEY=/secrets/gcs-backup-sa.json
BACKUP_RETENTION_DAILY=7
BACKUP_RETENTION_WEEKLY=4

# --- VM (utilisé par Makefile, pas par les containers) ---
VM_HOST=__VM_HOST__
VM_USER=user
SSH_KEY=~/.ssh/xbrain_key
```

Tout `__FILL__` ou `__FILL_RANDOM_*__` est un placeholder à remplacer par le user au moment du `cp .env.example .env`. Pour générer des secrets aléatoires : `openssl rand -hex 32` ou `openssl rand -base64 48`.
  </action>
  <verify>test -f .env.example && grep -c "__FILL" .env.example</verify>
  <acceptance_criteria>
    - `.env.example` contient `POSTGRES_PASSWORD`, `GOOGLE_CLIENT_ID`, `BRIDGE_SHARED_SECRET`, `MEILI_MASTER_KEY`, `JWT_SECRET`, `OPENWEBUI_SECRET_KEY`
    - Au moins 10 placeholders `__FILL` présents (force le user à les remplir)
    - Aucune valeur réelle de secret committée
  </acceptance_criteria>
  <done>Template .env documenté</done>
</task>

<task type="auto">
  <name>Task 4: docker-compose.yml skeleton + nginx conf base</name>
  <files>infrastructure/docker-compose.yml, infrastructure/nginx/conf.d/.gitkeep</files>
  <read_first>.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md</read_first>
  <action>
Créer `infrastructure/docker-compose.yml` avec un squelette **valide** (passe `docker compose config` sans erreur) mais sans services applicatifs. Les services réels (memory-api, postgres, qdrant, librechat...) seront ajoutés dans les plans suivants par concat/append, pas par re-création.

Contenu :

```yaml
# Phase 1 — squelette docker-compose
# Étoffé incrémentalement par plans 01-02 → 01-06
# Points d'entrée : nginx (80/443), tout le reste interne sur xbrain_net.

networks:
  xbrain_net:
    name: xbrain_net
    driver: bridge

volumes:
  xbrain_pg:        { name: xbrain_pg }
  xbrain_qdrant:    { name: xbrain_qdrant }
  librechat_mongo:  { name: librechat_mongo }
  librechat_meili:  { name: librechat_meili }
  librechat_uploads:{ name: librechat_uploads }
  openwebui_data:   { name: openwebui_data }
  openwebui_pipelines: { name: openwebui_pipelines }
  xbrain_backups:   { name: xbrain_backups }

services:
  # === Placeholder ingress (nginx) — détails dans plan 01-05 ===
  nginx:
    image: nginx:1.27-alpine
    container_name: xbrain-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    networks: [xbrain_net]
    mem_limit: 64m
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/nginx-health"]
      interval: 15s
      timeout: 5s
      retries: 5

  # Les autres services seront ajoutés par les plans suivants.
```

Et un nginx conf placeholder `infrastructure/nginx/conf.d/00-health.conf` qui répond juste sur `/nginx-health` (pour que le healthcheck nginx passe avant que les autres backends existent) :

```nginx
server {
  listen 80 default_server;
  server_name _;

  location /nginx-health { return 200 "ok\n"; access_log off; }

  # Tous les autres paths retournent 503 jusqu'à ce que plan 01-05 les configure
  location / { return 503 "xbrain bootstrapping\n"; }
}
```
  </action>
  <verify>cd infrastructure && docker compose -f docker-compose.yml --env-file ../.env.example config 2>&1 | head -20 && test -f nginx/conf.d/00-health.conf</verify>
  <acceptance_criteria>
    - `docker compose -f infrastructure/docker-compose.yml config` passe sans erreur
    - `infrastructure/docker-compose.yml` contient le bloc `networks: xbrain_net` et `volumes` (au moins 6 volumes nommés)
    - `infrastructure/nginx/conf.d/00-health.conf` existe et contient `/nginx-health`
  </acceptance_criteria>
  <done>Squelette compose validé, prêt à être étoffé</done>
</task>

<task type="auto">
  <name>Task 5: README de quickstart Phase 1</name>
  <files>README.md</files>
  <read_first>.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md, CLAUDE.md</read_first>
  <action>
Écrire `README.md` à la racine. Quickstart Phase 1, pas un essai philosophique. ~80-120 lignes max.

Contenu :

```markdown
# xbrain

> Mémoire collective persistante pour humains + agents, organisée par équipe et par niveau de vérité. **Pas un workspace de chatbot** — le différenciateur est la couche mémoire + truth-level + team-scope, pas l'interface.

## État

- **Phase 1** : Socle infra + frontends (LibreChat + Open WebUI) + memory-api Python avec contrat de tagging 7 champs.
- Architecture cible : voir [`CLAUDE.md`](./CLAUDE.md) et [`.planning/PROJECT.md`](./.planning/PROJECT.md).

## Quickstart Phase 1

### Pré-requis

- Une VM Ubuntu 24.04 avec Docker + Docker Compose installés (cf. memory `project_xbrain_phase1_infra.md`)
- Compte Google Cloud avec OAuth client configuré (cf. [Configuration Google OAuth](#configuration-google-oauth))
- Clé SSH pour la VM accessible localement

### Setup local

```bash
git clone https://github.com/mrboups/xbrain.git
cd xbrain
cp .env.example .env
# Éditer .env : remplacer tous les __FILL__ par les vraies valeurs (voir section secrets ci-dessous)
make env-check    # vérifie qu'aucun secret critique ne manque
```

### Génération des secrets

```bash
# Pour chaque __FILL_RANDOM_32_CHARS__ :
openssl rand -base64 32

# Pour chaque __FILL_RANDOM_64_CHARS__ :
openssl rand -base64 48

# Pour chaque __FILL_RANDOM_64_HEX__ :
openssl rand -hex 32

# Pour __FILL_RANDOM_32_HEX__ :
openssl rand -hex 16
```

### Configuration Google OAuth

1. https://console.cloud.google.com → projet `xbrain-495115` → APIs & Services → Credentials
2. Create Credentials → OAuth client ID → Web application
3. Authorized redirect URIs (les deux) :
   - `http://__VM_HOST__/oauth/google/callback`
   - `http://__VM_HOST__/openwebui/oauth/google/callback`
4. Copier `Client ID` → `GOOGLE_CLIENT_ID` et `OAUTH_GOOGLE_CLIENT_ID` dans `.env`
5. Copier `Client secret` → `GOOGLE_CLIENT_SECRET` et `OAUTH_GOOGLE_CLIENT_SECRET`

### Déploiement sur la VM

```bash
make sync       # rsync code vers la VM (sans build)
make deploy     # build + up -d sur la VM
make vm-ps      # vérifier que tous les containers sont healthy
make vm-logs    # tail logs si quelque chose ne démarre pas
```

Une fois déployé : http://__VM_HOST__

- LibreChat à la racine : http://__VM_HOST__/
- Open WebUI : http://__VM_HOST__/openwebui/
- memory-api : http://__VM_HOST__/api/v1/healthz

### Tests

```bash
make test       # tests memory-api (pytest + testcontainers)
make lint       # ruff sur les 3 services Python
```

### Backup / Restore (success criterion 5)

```bash
make backup           # backup manuel vers GCS bucket xbrain-backups-prod
make restore-test     # test E2E restore sur env clean (mandatory gate Phase 1 done)
```

## Structure du repo

```
.
├── apps/                       # Services applicatifs Python
│   ├── memory-api/             # FastAPI core (plan 01-02)
│   ├── librechat-bridge/       # Sidecar MongoDB → memory-api (plan 01-03)
│   └── openwebui-pipeline/     # Pipeline Open WebUI → memory-api (plan 01-04)
├── services/                   # Réservé Phase 3 (scraper, calendar, drive-sync)
├── packages/                   # Schémas partagés (Phase 2+)
│   └── schemas/
├── infrastructure/             # Docker Compose, nginx, scripts deploy
│   ├── docker-compose.yml
│   ├── nginx/conf.d/
│   └── scripts/
├── .planning/                  # GSD planning artifacts (PROJECT.md, ROADMAP.md, phases/)
└── .claude/                    # GSD toolchain
```

## Commandes utiles

```bash
make help         # liste tous les targets
make ssh          # SSH interactive sur la VM
make vm-down      # arrêter le stack (volumes préservés)
```

## Documentation

- Architecture & contraintes : [`CLAUDE.md`](./CLAUDE.md)
- Plan complet : [`.planning/ROADMAP.md`](./.planning/ROADMAP.md)
- Recherche Phase 1 : [`.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md`](./.planning/phases/01-socle-infra-frontends-memory-api/01-RESEARCH.md)

## Licence

À définir (probablement MIT ou Apache 2.0).
```
  </action>
  <verify>test -f README.md && wc -l README.md</verify>
  <acceptance_criteria>
    - `README.md` existe, ≥ 70 lignes
    - Contient les sections "Quickstart Phase 1", "Configuration Google OAuth", "Structure du repo", "Commandes utiles"
    - Référence `make deploy`, `make backup`, `make restore-test`
  </acceptance_criteria>
  <done>Quickstart actionnable</done>
</task>

</tasks>

<verification>
- [ ] `make help` exit 0 et affiche les targets
- [ ] `cd infrastructure && docker compose -f docker-compose.yml --env-file ../.env.example config` passe (parsing yaml + validation refs)
- [ ] `git status` ne montre pas de fichiers oubliés (tous les `.gitkeep` créés)
- [ ] `find . -name '*.env' -not -path './.git/*' -not -name '.env.example'` ne retourne RIEN (pas de .env committé)
</verification>

<success_criteria>
- All tasks completed
- Repo monorepo prêt à recevoir le code des 5 plans suivants
- Aucun secret réel committé
- Makefile = source unique des opérations dev/deploy
</success_criteria>

<output>
Après completion, créer `.planning/phases/01-socle-infra-frontends-memory-api/01-01-SUMMARY.md`
</output>
