---
phase: 18-local-auth
plan: 06
type: execute
wave: 5
depends_on: [18-01, 18-02, 18-03, 18-04, 18-05]
files_modified:
  - infrastructure/scripts/verify-phase18.sh
  - Makefile
  - docs/auth.html
autonomous: true
requirements: [LAUTH-01, LAUTH-02]
must_haves:
  truths:
    - "verify-phase18.sh drives a REAL memory-api against a REAL Postgres with NO Google/GitHub/social-OAuth vars set, and proves register -> login -> authorized GET /v1/tasks 200 end to end (SC#1)"
    - "the gate proves the persisted password_hash is argon2id and never plaintext (SC#2), the local xbt_ authorizes a real team route (SC#3), the existing-email register is 409 (D-18-05), lockout fires and recovers (SC#6), and absent-vs-wrong login are byte-identical (no oracle)"
    - "the gate re-runs the Google/GitHub regression (test_phase10_auth.py) proving SC#4 is not regressed, and the router-classification test proving auth_local is CORE"
    - "a SKIPPED security assertion counts as a FAILURE (the fixtures never engaged the real path); the gate exits non-zero"
    - "any repo-wide grep in the gate excludes the gate's own file (no self-invalidating grep)"
  artifacts:
    - path: "infrastructure/scripts/verify-phase18.sh"
      provides: "SC#1..SC#6 + LAUTH-01/02 acceptance gate, real-Postgres, SKIP-as-FAIL for security checks"
      contains: "verify-phase18"
    - path: "docs/auth.html"
      provides: "documentation of the native email/password path + the no-SMTP recovery story"
      contains: "email"
  key_links:
    - from: "verify-phase18.sh"
      to: "the real memory-api local-auth routes + real Postgres"
      via: "pytest -m integration (testcontainers) with SKIP treated as FAIL, plus a live zero-OAuth boot probe"
      pattern: "test_local_auth|verify-phase18"
    - from: "verify-phase18.sh"
      to: "SC#4 regression"
      via: "re-run test_phase10_auth.py + test_edition_gating.py unmodified"
      pattern: "test_phase10_auth"
---

<objective>
Ship the Phase 18 acceptance gate — `verify-phase18.sh` — that proves SC#1..SC#6 + LAUTH-01/02 by traversing the REAL deployment path (real memory-api routes, real Postgres, a real zero-OAuth boot), never a mock. This is the phase's answer to the governing lesson: five defects across Phases 14-15 shared one cause — a check that never traversed the real path. Here a SKIPPED security assertion is a FAILURE, not a pass. Also document the native auth path in `docs/auth.html`.

Purpose: SC#1 (zero-OAuth end-to-end), SC#2 (no plaintext), SC#3/LAUTH-02 (indistinguishable), SC#4 (Google/GitHub unchanged), SC#6 (abuse resistance), D-18-05 (409). LAUTH-01/02 acceptance.
Output: `verify-phase18.sh` (+ a `make verify-phase18` target) and `docs/auth.html`.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-local-auth/18-CONTEXT.md
@.planning/phases/18-local-auth/18-RESEARCH.md
@infrastructure/scripts/verify-phase15.sh
@apps/memory-api/tests/conftest.py
@apps/memory-api/tests/test_local_auth.py
@apps/memory-api/tests/test_phase10_auth.py

<interfaces>
<!-- Shape of the existing gate + test infra to reuse. -->

verify-phase15.sh is the reference shape (read it): PASS/FAIL/SKIP counters, `ok/ko/skip` helpers,
`set -uo pipefail` (NOT -e), a single summary line `PASS: $PASS / $TOTAL (SKIP: $SKIP)`, exit 0 iff FAIL==0.
Docker realities it already encodes and Phase 18 inherits: host may be arm64 (NEVER `docker build`; both
new deps ship arm64+amd64 wheels); the Git-Bash host-mount trap (a POSIX $PWD path silently fails to mount —
use `cygpath -w` + `MSYS_NO_PATHCONV=1`).

The heavy lifting already exists as integration tests (Plans 01-04): tests/test_local_auth.py,
tests/test_local_auth_set_password.py, tests/test_local_credentials_repo.py. They spin real
testcontainers Postgres (conftest.py pg_url fixture runs `alembic upgrade head`). The gate's job is to
RUN them as the acceptance proof and treat a SKIP (Docker/fixtures absent) as a FAIL — the opposite of the
suite's default graceful-skip, because for the gate a skip means the real path was never exercised.

SC#4 regression (do NOT rewrite it — re-run the existing file): tests/test_phase10_auth.py drives the
real /v1/auth/github/signin route (respx mocks only the GitHub HTTP calls) against real Postgres.
tests/test_edition_gating.py proves every router (incl. auth_local) is classified.

Makefile has existing `verify-phaseNN` targets (see the `make verify-phase15` pattern) — add a
`verify-phase18` target that invokes `bash infrastructure/scripts/verify-phase18.sh`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase18.sh — real-Postgres acceptance gate (SKIP-as-FAIL for security) + make target</name>
  <read_first>
    - infrastructure/scripts/verify-phase15.sh (counter/helper/summary/exit-code shape + Docker/Git-Bash host notes to copy)
    - apps/memory-api/tests/conftest.py (pg_url testcontainers fixture + _docker_available)
    - apps/memory-api/tests/test_local_auth.py + test_local_auth_set_password.py (the tests the gate runs)
    - Makefile (the existing verify-phaseNN target pattern)
  </read_first>
  <files>infrastructure/scripts/verify-phase18.sh, Makefile</files>
  <action>
Create `infrastructure/scripts/verify-phase18.sh` mirroring verify-phase15.sh's structure (PASS/FAIL/SKIP
counters, `set -uo pipefail`, `cd` to repo root, single summary line, exit 0 iff FAIL==0). Checks:

(a) SECURITY SUITE — run `cd apps/memory-api && pytest tests/test_local_auth.py tests/test_local_auth_set_password.py tests/test_local_credentials_repo.py -q`. Parse the output: if Docker is unavailable OR any of these tests reports `SKIPPED`/`no tests ran`, record a FAIL (NOT a skip) with a clear message that the real Postgres path was never exercised — the whole point of this gate. Otherwise PASS on green. These tests already encode SC#1 (test_clean_boot_no_oauth_e2e), SC#2 (test_password_hash_is_argon2id), SC#3/LAUTH-02 (test_local_xbt_authorizes_team_route), D-18-05 (test_register_collision_409), SC#6 (test_lockout_then_recovery), and the no-enumeration-oracle assertion.

(b) SC#4 REGRESSION — run `pytest tests/test_phase10_auth.py tests/test_edition_gating.py -q`. Same SKIP-as-FAIL policy for test_phase10_auth (it must actually exercise the GitHub path). PASS iff green — proving Google/GitHub are unchanged and auth_local is classified CORE.

(c) LIVE ZERO-OAUTH BOOT (mirror verify-phase15 check g's no-build harness) — boot a REAL memory-api inside a stock `python:3.12-slim` container (bind-mount the repo with `cygpath -w` + `MSYS_NO_PATHCONV=1`; MOUNT GUARD: FAIL, never SKIP, if `/repo/apps/memory-api/app/main.py` is absent), against a real postgres container, with the env carrying NO `GOOGLE_CLIENT_ID`, NO `GITHUB_APP_*`, NO `GITHUB_CLIENT_ID` (leave OAUTH_ISSUER_URL/OAUTH_RESOURCE_URL set — required to boot, unrelated to social login). After `alembic upgrade head`, curl `POST /v1/auth/local/register` -> capture the xbt_ + team_scope; curl `POST /v1/auth/local/login` -> 200; curl `GET /v1/tasks` with the xbt_ + `X-Team-Scope` -> 200. This is SC#1 proven against a live, socially-OAuth-less server. If the harness cannot pip-install/boot offline, `skip` THIS check only (it is the belt-and-suspenders live proof; check (a)'s testcontainers suite is the load-bearing one and does NOT skip-pass).

(d) NEGATIVE PROVENANCE — assert this phase added no sixth principal branch and did not touch the live GitHub path: `git log`-independent static check that `app/deps.py` and `app/routes/auth_github.py` contain no Phase-18 marker edits (grep them for `local_credentials`/`auth_local` -> expect 0). SELF-EXCLUSION: any repo-wide grep MUST exclude verify-phase18.sh itself (e.g. `grep -rl ... --exclude=verify-phase18.sh` or filter the script's own path) so the gate never matches its own tokens (the self-invalidating-grep trap).

Add a `verify-phase18` target to the `Makefile` invoking `bash infrastructure/scripts/verify-phase18.sh`.
  </action>
  <acceptance_criteria>
    - `bash infrastructure/scripts/verify-phase18.sh` runs and prints a `PASS: N / M (SKIP: K)` summary and exits 0 when FAIL==0
    - With Docker available, checks (a) and (b) PASS with the underlying tests RUNNING; if Docker is absent the script records those as FAIL (exit non-zero) — a SKIP of a security check is never a pass
    - Check (d) uses `--exclude` (or an equivalent path filter) so a repo-wide grep does not match verify-phase18.sh's own text: `grep -c 'exclude' infrastructure/scripts/verify-phase18.sh` >= 1
    - `grep -c 'GOOGLE_CLIENT_ID' infrastructure/scripts/verify-phase18.sh` >= 1 and the live-boot env sets it (and GITHUB_APP_*/GITHUB_CLIENT_ID) to empty
    - `grep -c 'verify-phase18' Makefile` >= 1
    - The script does NOT `docker build` anything: `grep -c 'docker build' infrastructure/scripts/verify-phase18.sh` returns 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n infrastructure/scripts/verify-phase18.sh && grep -q 'PASS:' infrastructure/scripts/verify-phase18.sh && grep -q 'exclude' infrastructure/scripts/verify-phase18.sh && ! grep -q 'docker build' infrastructure/scripts/verify-phase18.sh && grep -q 'verify-phase18' Makefile && echo 'gate-shape-ok'</automated>
  </verify>
  <done>verify-phase18.sh exists, runs the real-Postgres security + regression suites with SKIP-as-FAIL, adds a live zero-OAuth boot proof, uses self-excluding greps, never docker-builds, and is wired into `make verify-phase18`.</done>
</task>

<task type="auto">
  <name>Task 2: docs/auth.html — document the native email/password path</name>
  <read_first>
    - docs/auth.html (existing auth documentation to extend — the Phase 10/12 GitHub/Google auth doc)
    - docs/local-auth-recovery.md (the recovery runbook from Plan 04 — link to it)
    - 18-CONTEXT.md domain + D-18-05 (what to describe: register/login/logout/set-password, no email reset)
  </read_first>
  <files>docs/auth.html</files>
  <action>
Extend `docs/auth.html` with a "Local (email + password)" section: that a self-hoster can register and
sign in with email+password with ZERO external OAuth configured; the endpoints (`/v1/auth/local/register`,
`/login`, `/set-password`); that the resulting session is the same opaque `xbt_` token as Google/GitHub
(so it authorizes identically); that Google/GitHub remain available and unchanged when configured (a
default, not a replacement); and that password RESET is via the operator recovery runbook
(link `docs/local-auth-recovery.md`), NOT email — email reset is intentionally out of scope. English only.
Keep the existing Google/GitHub content intact.
  </action>
  <acceptance_criteria>
    - `grep -ci 'email' docs/auth.html` increased (a Local email/password section exists) and `grep -ci 'auth/local' docs/auth.html` >= 1
    - the doc references the recovery runbook and states email-reset is out of scope: `grep -ci 'recovery\|reset' docs/auth.html` >= 1
    - the existing GitHub/Google auth content is still present (not deleted): `grep -ci 'github' docs/auth.html` >= 1
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/auth.html && grep -qi 'auth/local' docs/auth.html && grep -qi 'github' docs/auth.html && echo 'auth-doc-ok'</automated>
  </verify>
  <done>docs/auth.html documents the native email/password path, states it authorizes identically and that Google/GitHub are unchanged, and points to the operator recovery runbook in lieu of email reset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification harness → real memory-api + real Postgres | The gate must exercise the real deployment path; a mock here would reproduce the Phase 14-15 failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Repudiation | a green gate that proved nothing (tests silently skipped) | mitigate | SKIP-as-FAIL for every security/regression check; a skip exits non-zero with an explicit message |
| T-18-06-02 | Tampering | a self-matching repo-wide grep (self-invalidating gate) | mitigate | All repo-wide greps exclude verify-phase18.sh's own path |
| T-18-06-03 | Denial of Service | building an image on an arm64 host then shipping to amd64 | mitigate | No `docker build`; live harness runs real source in a stock python:3.12-slim, wheels only |
| T-18-06-04 | Information Disclosure | docs implying an email-reset exists (false recovery expectation) | mitigate | docs/auth.html explicitly routes recovery through the operator runbook and states email reset is out of scope |
</threat_model>

<verification>
- `bash infrastructure/scripts/verify-phase18.sh` exits 0 with the security + regression suites actually running (no security SKIP counted as pass).
- `make verify-phase18` invokes the script.
- `docs/auth.html` documents the local path and links the recovery runbook; GitHub/Google content preserved.
</verification>

<success_criteria>
- The gate proves SC#1..SC#6 + LAUTH-01/02 against the real path, with SKIP-as-FAIL for security assertions.
- SC#4 regression (test_phase10_auth) and router classification are re-run green.
- The gate never docker-builds and never self-matches its own greps.
- The native auth path is documented with an honest, no-SMTP recovery story.
</success_criteria>

<output>
After completion, create `.planning/phases/18-local-auth/18-06-SUMMARY.md`.
</output>
