---
phase: 18-local-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memory-api/alembic/versions/0024_local_credentials.py
  - apps/memory-api/app/config.py
  - apps/memory-api/app/repos/local_credentials.py
  - apps/memory-api/tests/test_local_credentials_repo.py
autonomous: true
requirements: [LAUTH-01]
must_haves:
  truths:
    - "A local_credentials row can be created for a user and read back by email (case-insensitive) against real Postgres"
    - "After N consecutive recorded failures the row's locked_until is set to a future timestamp; a success resets failed_attempts to 0 and clears locked_until"
    - "Deleting a users row cascades and removes its local_credentials row"
  artifacts:
    - path: "apps/memory-api/alembic/versions/0024_local_credentials.py"
      provides: "local_credentials table, migration head, down_revision=0023"
      contains: "local_credentials"
    - path: "apps/memory-api/app/repos/local_credentials.py"
      provides: "create / get_by_email / get_by_user_id / record_failure / reset_failures / update_hash / upsert helpers"
      exports: ["create", "get_by_email", "get_by_user_id", "record_failure", "reset_failures", "update_hash", "upsert"]
    - path: "apps/memory-api/app/config.py"
      provides: "LOCAL_AUTH_* env knobs with safe defaults (no fail-fast validator)"
      contains: "LOCAL_AUTH_MAX_FAILED_ATTEMPTS"
  key_links:
    - from: "app/repos/local_credentials.py"
      to: "local_credentials table"
      via: "sqlalchemy INSERT/SELECT/UPDATE"
      pattern: "local_credentials"
    - from: "app/repos/local_credentials.py::get_by_email"
      to: "users.email column (NOT a constructed source_user_id)"
      via: "JOIN users u ON u.id = lc.user_id WHERE lower(u.email)=lower(:email)"
      pattern: "lower\\("
---

<objective>
Lay the data layer for native email/password auth: the `local_credentials` table (migration **0024**), the repo that reads/writes it (including the DB-backed lockout counters), and the `LOCAL_AUTH_*` config knobs. Everything else in Phase 18 builds on this table.

Purpose: D-18-03 (dedicated `local_credentials` table, no new column on `users`) + the durable half of D-18-06 (persistent lockout that survives restarts and is shared across all uvicorn workers, unlike the in-process rate limiter).
Output: A migrated table, a tested repo, and config defaults that keep a zero-OAuth install booting.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-local-auth/18-CONTEXT.md
@.planning/phases/18-local-auth/18-RESEARCH.md
@apps/memory-api/alembic/versions/0013_api_tokens.py
@apps/memory-api/alembic/versions/0009_crm_contacts.py
@apps/memory-api/app/repos/users.py
@apps/memory-api/app/config.py
@apps/memory-api/tests/conftest.py

<interfaces>
<!-- Contracts the executor needs. Extracted from the live repo — do NOT go exploring. -->

Latest migration is 0023 (verified by listing alembic/versions). The new migration's
down_revision MUST be "0023" and revision "0024".

Migration style to copy — raw op.execute() SQL (apps/memory-api/alembic/versions/0013_api_tokens.py):
```python
revision: str = "0013"
down_revision: Union[str, None] = "0012"
def upgrade() -> None:
    op.execute("""CREATE TABLE IF NOT EXISTS user_api_tokens ( ... )""")
    op.execute("CREATE INDEX IF NOT EXISTS ... ")
def downgrade() -> None:
    op.execute("DROP TABLE IF EXISTS user_api_tokens")
```

Case-insensitive email lookup ALREADY EXISTS — reuse its shape (apps/memory-api/app/repos/users.py:60-73):
```python
async def get_user_by_email(session, email):
    from sqlalchemy import func
    result = await session.execute(select(User).where(func.lower(User.email) == email.strip().lower()))
    return result.scalar_one_or_none()
```

Config pattern (apps/memory-api/app/config.py): plain typed fields with defaults on the
`Settings(BaseSettings)` class. field_validator is used ONLY where a missing value must
fail boot (OAUTH_ISSUER_URL). Phase-18 knobs MUST NOT fail-fast — a zero-OAuth install must boot.

The users PK is UUID (users.id). local_credentials.user_id is PK + FK -> users.id ON DELETE CASCADE.
users.email has NO unique constraint anywhere and MUST stay that way (see anti-patterns).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0024 (local_credentials) + LOCAL_AUTH_* config knobs</name>
  <read_first>
    - apps/memory-api/alembic/versions/0013_api_tokens.py (migration style + index style)
    - apps/memory-api/alembic/versions/0009_crm_contacts.py:87-91 (partial-index style — for reference only; we add NO index here)
    - apps/memory-api/app/config.py (the Settings class + where to append; the OAUTH fail-fast validator you must NOT imitate for these knobs)
  </read_first>
  <files>apps/memory-api/alembic/versions/0024_local_credentials.py, apps/memory-api/app/config.py</files>
  <action>
Create migration `0024_local_credentials.py` with `revision="0024"`, `down_revision="0023"`, using the raw `op.execute()` style of 0013. `upgrade()` creates:
```sql
CREATE TABLE IF NOT EXISTS local_credentials (
    user_id         UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
    password_hash   TEXT NOT NULL,
    algo            VARCHAR(32) NOT NULL DEFAULT 'argon2id',
    failed_attempts INT NOT NULL DEFAULT 0,
    locked_until    TIMESTAMPTZ,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);
```
`downgrade()` drops the table. Do NOT add any UNIQUE index on `users.email` or `lower(users.email)` — research Pitfall 4: it would fail the migration outright on any deployment with pre-existing duplicate emails, and D-18-05's correctness is enforced at the app layer, not by a DB constraint.

In `app/config.py`, append these fields to `Settings` (safe defaults, NO field_validator — a zero-OAuth install must still boot; per D-18-06 research Open Question 2 the 5/15 defaults are defensible and env-overridable):
```python
# === Phase 18 (LAUTH-01/02) — native email/password auth ===
LOCAL_AUTH_MAX_FAILED_ATTEMPTS: int = 5
LOCAL_AUTH_LOCKOUT_MINUTES: int = 15
LOCAL_AUTH_RATE_LIMIT: str = "10/minute"      # per-IP, in-process (NOT durable across uvicorn --workers 2 — Plan 02 rate_limit.py documents this)
LOCAL_AUTH_MIN_PASSWORD_LENGTH: int = 10
```
  </action>
  <acceptance_criteria>
    - `grep -c 'down_revision.*=.*"0023"' apps/memory-api/alembic/versions/0024_local_credentials.py` returns 1
    - `grep -c 'revision.*=.*"0024"' apps/memory-api/alembic/versions/0024_local_credentials.py` returns 1
    - The migration file contains `ON DELETE CASCADE` and does NOT contain `UNIQUE INDEX` on email: `grep -iE 'unique.*index.*email|index.*email' apps/memory-api/alembic/versions/0024_local_credentials.py` returns nothing
    - `grep -c 'LOCAL_AUTH_MAX_FAILED_ATTEMPTS' apps/memory-api/app/config.py` returns >=1
    - No new `field_validator` decorates any `LOCAL_AUTH_*` field (grep the config diff — a validator here would risk breaking SC#1's clean boot)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -c "import app.config as c; s=c.settings; assert s.LOCAL_AUTH_MAX_FAILED_ATTEMPTS==5 and s.LOCAL_AUTH_LOCKOUT_MINUTES==15 and s.LOCAL_AUTH_RATE_LIMIT=='10/minute'; print('config-ok')"</automated>
  </verify>
  <done>Migration 0024 exists (revision 0024 / down_revision 0023, cascade FK, no email unique index); config exposes the four LOCAL_AUTH_* knobs with safe defaults and no fail-fast validator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: local_credentials repo (create / lookup / lockout / rehash helpers) + real-Postgres tests</name>
  <read_first>
    - apps/memory-api/app/repos/users.py (get_or_create_user race-safety + get_user_by_email case-insensitivity — mirror these shapes)
    - apps/memory-api/app/repos/teams.py:13-35 (repo helpers flush(), never commit() — the caller owns the transaction)
    - apps/memory-api/app/config.py (the LOCAL_AUTH_* knobs added in Task 1 — read them here)
    - apps/memory-api/tests/conftest.py (session + seeded_two_teams fixtures; @pytest.mark.integration + testcontainers Postgres)
    - apps/memory-api/alembic/versions/0024_local_credentials.py (the table this repo targets)
  </read_first>
  <behavior>
    - create(session, user_id, password_hash) inserts a row with algo='argon2id', failed_attempts=0, locked_until NULL
    - get_by_email(session, email) JOINs users.email (case-insensitive) -> returns the credential row (with user_id, password_hash, failed_attempts, locked_until) or None; a mismatched-case email still resolves
    - record_failure increments failed_attempts; on reaching LOCAL_AUTH_MAX_FAILED_ATTEMPTS it sets locked_until = now() + LOCAL_AUTH_LOCKOUT_MINUTES
    - reset_failures sets failed_attempts=0, locked_until=NULL
    - update_hash replaces password_hash and bumps updated_at (used by needs-rehash-on-login and set-password change)
    - upsert inserts-or-updates the hash for a user_id (used by set-password first-attach); ON CONFLICT (user_id) DO UPDATE
    - Deleting the users row cascades: the local_credentials row disappears
  </behavior>
  <files>apps/memory-api/app/repos/local_credentials.py, apps/memory-api/tests/test_local_credentials_repo.py</files>
  <action>
Create `app/repos/local_credentials.py` with async helpers that `flush()` (never `commit()` — the caller owns the transaction, mirroring `teams_repo.create_team`):
- `create(session, *, user_id, password_hash, algo="argon2id")` — INSERT.
- `get_by_user_id(session, user_id)` — SELECT one row or None.
- `get_by_email(session, email)` — SELECT lc.* FROM local_credentials lc JOIN users u ON u.id = lc.user_id WHERE `func.lower(u.email) == email.strip().lower()`. This is the ONLY correct collision/lookup key (research Pitfalls 1+2): it compares the canonical `users.email` column, never a constructed `email:<addr>` source_user_id.
- `record_failure(session, user_id)` — `failed_attempts = failed_attempts + 1`; if the new value `>= settings.LOCAL_AUTH_MAX_FAILED_ATTEMPTS`, set `locked_until = now() + (LOCAL_AUTH_LOCKOUT_MINUTES minutes)`. Read the thresholds from `app.config.settings` directly (config lives in this same plan, no cross-plan coupling).
- `reset_failures(session, user_id)` — set `failed_attempts=0, locked_until=NULL`.
- `update_hash(session, user_id, password_hash)` — set `password_hash`, `updated_at=now()`.
- `upsert(session, *, user_id, password_hash, algo="argon2id")` — `INSERT ... ON CONFLICT (user_id) DO UPDATE SET password_hash=EXCLUDED.password_hash, updated_at=now()`.

Write `tests/test_local_credentials_repo.py` (`pytestmark = [pytest.mark.integration, pytest.mark.asyncio]`) driving these against the real testcontainers Postgres via the `session` + `seeded_two_teams` fixtures. Cover every bullet in `<behavior>`, including: a lock test that calls `record_failure` exactly `LOCAL_AUTH_MAX_FAILED_ATTEMPTS` times and asserts `locked_until` becomes non-NULL and future; a reset test; a cascade test (`DELETE FROM users WHERE id=... ` then assert the credential row is gone).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_local_credentials_repo.py -x` PASSES with the tests actually running (NOT `SKIPPED` — a skip means the testcontainers fixture never engaged, which per the governing lesson counts as a FAILURE here)
    - The lock test asserts `locked_until IS NOT NULL AND locked_until > now()` after exactly `settings.LOCAL_AUTH_MAX_FAILED_ATTEMPTS` recorded failures
    - `get_by_email` test seeds an existing user (via `users_repo.get_or_create_user`), then resolves the credential with a DIFFERENT-CASE email string and still finds it
    - The cascade test deletes the users row and asserts `get_by_user_id` returns None afterward
    - `grep -c 'lower(' apps/memory-api/app/repos/local_credentials.py` returns >=1 (email-column, case-insensitive lookup — never a source_user_id lookup)
    - `grep -c 'commit(' apps/memory-api/app/repos/local_credentials.py` returns 0 (repo flushes only; caller commits)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && pytest tests/test_local_credentials_repo.py -x -q -p no:cacheprovider 2>&1 | tee /tmp/p18_01.log; grep -qE 'SKIPPED|no tests ran' /tmp/p18_01.log && { echo 'FAIL: integration test skipped — Docker/fixtures did not engage'; exit 1; } || true</automated>
  </verify>
  <done>The repo exists with all seven helpers, flushes only, and its real-Postgres integration tests pass (not skipped); lockout counters, case-insensitive email lookup, and FK cascade are all proven against the real table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory-api → Postgres | Credential material (argon2 hashes) and lockout counters are written here; only memory-api reads/writes this table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Information Disclosure | `local_credentials.password_hash` at rest | mitigate | Column stores only an argon2id encoded hash (enforced by the hashing service in Plan 02 + the persistence assertion in Plan 03); this plan creates the column as opaque TEXT and never logs it |
| T-18-01-02 | Elevation of Privilege | brute-force / credential stuffing | mitigate | DB-backed `failed_attempts`/`locked_until` — durable across restarts AND across all uvicorn workers (unlike the in-process limiter). Consumed by the login route (Plan 03) |
| T-18-01-03 | Tampering | orphaned credentials after user deletion | mitigate | `user_id ... REFERENCES users(id) ON DELETE CASCADE` — cascade proven by the repo test |
| T-18-01-04 | Denial of Service | a fail-fast validator on a LOCAL_AUTH_* knob breaking clean boot | accept | Deliberately NO validator on these fields; safe defaults keep SC#1's zero-OAuth boot working. Documented in Task 1 |
</threat_model>

<verification>
- `cd apps/memory-api && pytest tests/test_local_credentials_repo.py -x` passes with tests running (no SKIP).
- `cd apps/memory-api && python -m alembic upgrade head` reaches 0024 cleanly against a fresh DB (exercised end-to-end by the conftest testcontainers fixture, which runs `alembic upgrade head` before any integration test).
- `python -c "import app.config"` still succeeds with NO Google/GitHub env vars set (clean-boot guard).
</verification>

<success_criteria>
- Migration 0024 creates `local_credentials` with a cascading FK and no email unique index; it is the alembic head with down_revision 0023.
- The repo provides create/get_by_email/get_by_user_id/record_failure/reset_failures/update_hash/upsert, flushes only, and looks up by the `users.email` column case-insensitively.
- Lockout counters and FK cascade are proven against real Postgres (tests run, not skipped).
- Config exposes the LOCAL_AUTH_* knobs with safe defaults and no boot-breaking validator.
</success_criteria>

<output>
After completion, create `.planning/phases/18-local-auth/18-01-SUMMARY.md`.
</output>
