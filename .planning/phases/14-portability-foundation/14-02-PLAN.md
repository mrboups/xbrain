---
phase: 14-portability-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memory-api/app/knowledge/xbrain_product_kb.md
  - apps/memory-api/app/services/relevance_filter.py
  - apps/librechat/patches/onboarding.js
  - apps/librechat/Dockerfile
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "The @groove agent no longer recites grooveos.app URLs to users of a self-hosted deployment (system-prompt KB is domain-neutral)"
    - "The Haiku relevance classifier's few-shot examples use a neutral example domain (no brand leak into the classifier prompt)"
    - "onboarding.js contains no hardcoded grooveos.app; its memory-api base is build-time configurable with a same-origin fallback"
  artifacts:
    - path: "apps/memory-api/app/knowledge/xbrain_product_kb.md"
      provides: "Domain-neutral product KB injected into the live agent system prompt"
    - path: "apps/librechat/patches/onboarding.js"
      provides: "Config-driven (placeholder-substituted) memory-api base"
      contains: "__XBRAIN_MEMORY_API_BASE__"
  key_links:
    - from: "apps/librechat/Dockerfile"
      to: "apps/librechat/patches/onboarding.js"
      via: "build-time sed substitution of __XBRAIN_MEMORY_API_BASE__"
      pattern: "__XBRAIN_MEMORY_API_BASE__"
---

<objective>
Close the three FUNCTIONAL domain leaks on the **backend/infra request-response path**
(PORT-01, amended ROADMAP SC#4). These are not cosmetic: the KB is read verbatim into the live
@groove agent's system prompt (the agent tells self-hosted users to visit `chat.grooveos.app`),
the relevance-filter few-shots carry the brand into the Haiku classifier prompt, and onboarding.js
posts to a hardcoded `api.grooveos.app` regardless of the operator's configured domain.

SCOPE NOTE (amended ROADMAP, D-01c): `chrome-extension/**` and `app-site/account/**` are the
DEFERRED browser bundles — they are NOT in this plan. `apps/librechat/patches/onboarding.js` IS in
scope: it is not a deferred browser bundle, it is a patch file baked into the LibreChat **container
image at build time**, i.e. part of the server-delivered stack this phase owns.

Purpose: an operator's deployment must never emit xbrain's own domain to end users.
Output: domain-neutral agent KB, neutral classifier few-shots, and a build-configurable
onboarding base URL.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md

<interfaces>
<!-- KB is loaded verbatim (no templating) by team_chat_agent.py: _KB_PATH.read_text().
     grooveos.app occurrences: lines ~141, 165, 167, 171, 182-183 (chat./mcp./apex + /account/teams/). -->
<!-- relevance_filter.py SYSTEM_PROMPT MUST stay >= 16,384 bytes UTF-8 (prompt-cache activation).
     A same-length domain swap (grooveos.app -> example.com) preserves this. -->
<!-- onboarding.js line 14: const MEMORY_API = 'https://api.grooveos.app';  (client bundle, no runtime env).
     Dockerfile applies patches via COPY + RUN sed (build time). Existing placeholder convention: __VM_HOST__. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite xbrain_product_kb.md domain-neutral</name>
  <files>apps/memory-api/app/knowledge/xbrain_product_kb.md</files>
  <read_first>
    - apps/memory-api/app/knowledge/xbrain_product_kb.md (full file — the 5 grooveos.app URL references)
    - apps/memory-api/app/services/team_chat_agent.py lines 60-75 (confirm `_KB_PATH.read_text()` verbatim injection, no `.format()` applied to the KB body)
  </read_first>
  <action>
    Rewrite the grooveos.app references in xbrain_product_kb.md to generic, relative, deployment-agnostic
    prose (14-RESEARCH.md Open Q #3, RESOLVED → generic rewrite, not templating). Specifically:
    - `chat.grooveos.app` → "your LibreChat frontend" (or "the chat frontend of your deployment").
    - `mcp.grooveos.app` → "your deployment's remote MCP server URL".
    - `grooveos.app/account/teams/` and `chat.grooveos.app/account/teams/brain/` → relative paths
      ("/account/teams/", "/account/teams/brain/") described as "on your xbrain web app".
    - `grooveos.app/` sign-in reference → "your xbrain web app, the extension popup, or LibreChat".

    **ALSO — the mention alias (D-08 / plan-checker BLOCKER-2).** The KB (~line 141) documents the agent
    trigger as `@grooveos` / `@groove`. Those are BRAND tokens, and 14-01 Task 5 makes the alias list
    config-driven (`AGENT_MENTION_ALIASES`, neutral default `agent`). The KB must move in LOCKSTEP or it
    will tell a self-hoster to type a trigger their deployment does not answer to.
    - Replace the documented trigger with the neutral default: **`@agent`**.
    - Add one sentence: the mention alias is configurable per deployment via `AGENT_MENTION_ALIASES`
      (comma-separated, no leading `@`); `@agent` is the default.
    - Name no brand. Do NOT keep `@grooveos` or `@groove` in the KB — xbrain's prod deployment restores
      those via its own `.env`, but the KB ships to every operator.

    Keep all other functional guidance (tool names, truth-level workflow) intact — otherwise only the
    absolute brand URLs change. Do NOT introduce a templating placeholder; plain neutral prose.
  </action>
  <acceptance_criteria>
    - `grep -c 'grooveos' apps/memory-api/app/knowledge/xbrain_product_kb.md` returns 0 (bare token — matches `grooveos.app`, the escaped `grooveos\.app` form, AND the `@grooveos` alias).
    - `grep -c 'account/teams' apps/memory-api/app/knowledge/xbrain_product_kb.md` returns >= 1 (relative paths retained).
    - `grep -c '@agent' apps/memory-api/app/knowledge/xbrain_product_kb.md` returns >= 1 (the trigger token is documented — it is a TOOL TRIGGER, and the agent must still tell users how to summon it).
    - `grep -c 'AGENT_MENTION_ALIASES' apps/memory-api/app/knowledge/xbrain_product_kb.md` returns >= 1 (configurability documented).
    - `grep -c '@groove' apps/memory-api/app/knowledge/xbrain_product_kb.md` returns 0 (the brand alias is gone from the shipped KB).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'grooveos' apps/memory-api/app/knowledge/xbrain_product_kb.md)" = "0" && test "$(grep -c '@groove' apps/memory-api/app/knowledge/xbrain_product_kb.md)" = "0" && test "$(grep -c '@agent' apps/memory-api/app/knowledge/xbrain_product_kb.md)" -ge 1</automated>
  </verify>
  <done>The agent KB carries no absolute brand URL and no brand mention alias; it documents the neutral `@agent` trigger and its configurability — in lockstep with 14-01 Task 5's detector.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize relevance_filter few-shot example domains</name>
  <files>apps/memory-api/app/services/relevance_filter.py</files>
  <read_first>
    - apps/memory-api/app/services/relevance_filter.py (the SYSTEM_PROMPT few-shot block containing `grooveos.app` / `langfuse.grooveos.app` in illustrative INPUT text; note the >=16,384-byte cache-activation requirement documented at lines 28-32)
  </read_first>
  <action>
    In the SYSTEM_PROMPT few-shot examples, replace `langfuse.grooveos.app` → `langfuse.example.com`
    and any other `grooveos.app` → `example.com`. These are illustrative example domains inside the
    classifier prompt (D-01 exception: neutralize to a placeholder — NOT keep as grooveos.app, NOT
    delete). A same-length domain swap preserves the prompt-cache byte-length invariant.
  </action>
  <acceptance_criteria>
    - `grep -c 'grooveos' apps/memory-api/app/services/relevance_filter.py` returns 0.
    - Cache invariant preserved: `cd apps/memory-api && python -c "from app.services.relevance_filter import SYSTEM_PROMPT; assert len(SYSTEM_PROMPT.encode('utf-8')) >= 16384, len(SYSTEM_PROMPT.encode('utf-8'))"` exits 0.
    - `python -c "import ast; ast.parse(open('apps/memory-api/app/services/relevance_filter.py').read())"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'grooveos' apps/memory-api/app/services/relevance_filter.py)" = "0" && python -c "import ast; ast.parse(open('apps/memory-api/app/services/relevance_filter.py').read())"</automated>
  </verify>
  <done>Classifier few-shots use example.com; prompt-cache byte-length invariant intact.</done>
</task>

<task type="auto">
  <name>Task 3: onboarding.js build-configurable memory-api base + header comment scrub</name>
  <files>apps/librechat/patches/onboarding.js, apps/librechat/Dockerfile</files>
  <read_first>
    - apps/librechat/patches/onboarding.js lines 1-61 (header comment refs + `const MEMORY_API` + `apiCall` fetch usage)
    - apps/librechat/Dockerfile (the COPY patches/onboarding.js + RUN sed pattern; existing `__VM_HOST__` placeholder convention at line 12)
  </read_first>
  <action>
    In apps/librechat/patches/onboarding.js:
    1. Replace `const MEMORY_API = 'https://api.grooveos.app';` with:
       `const MEMORY_API_RAW = '__XBRAIN_MEMORY_API_BASE__';`
       `const MEMORY_API = MEMORY_API_RAW.indexOf('__') === 0 ? '' : MEMORY_API_RAW;`
       (unsubstituted placeholder → empty string → `apiCall` fetches same-origin relative `/v1/...`).
       Leave `apiCall` using `${MEMORY_API}${path}` unchanged (empty base = same-origin).
    2. Scrub the header comment (lines 4-5) grooveos.app references → neutral phrasing
       ("Runs in the LibreChat frontend context. Uses /api/xbrain/token (same-origin) then calls the
       memory-api /v1/teams/* endpoints at the configured base.").
    In apps/librechat/Dockerfile:
    3. Add `ARG MEMORY_API_BASE_URL=""` before the onboarding COPY, and immediately AFTER
       `COPY patches/onboarding.js /app/client/dist/onboarding.js` add:
       `RUN sed -i "s|__XBRAIN_MEMORY_API_BASE__|${MEMORY_API_BASE_URL}|" /app/client/dist/onboarding.js`
       Empty default → same-origin fallback (boot-safe). Prod reproduction sets this build arg via
       docker-compose's `build.args` (wired in Plan 14-03b) from `${LIBRECHAT_MEMORY_API_BASE}`.
  </action>
  <acceptance_criteria>
    - `grep -c 'grooveos' apps/librechat/patches/onboarding.js` returns 0.
    - `grep -c '__XBRAIN_MEMORY_API_BASE__' apps/librechat/patches/onboarding.js` returns 1.
    - `grep -c 'MEMORY_API_BASE_URL' apps/librechat/Dockerfile` returns >= 2 (ARG + sed).
    - `node --check apps/librechat/patches/onboarding.js` exits 0 (valid JS).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'grooveos' apps/librechat/patches/onboarding.js)" = "0" && node --check apps/librechat/patches/onboarding.js && test "$(grep -c 'MEMORY_API_BASE_URL' apps/librechat/Dockerfile)" -ge 2</automated>
  </verify>
  <done>onboarding.js has no brand hardcode; base is substituted at build with a same-origin fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory-api → LLM system prompt (agent) | KB text is injected verbatim into a prompt the model returns to end users |
| browser (onboarding.js, served by the LibreChat container) → memory-api | client-side fetch of `/v1/teams/*` crosses to the configured memory-api base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | xbrain_product_kb.md leaking operator-foreign `grooveos.app` URLs into agent replies | mitigate | Domain-neutral rewrite; agent references relative paths / "your deployment" only. |
| T-14-06 | Information Disclosure | relevance_filter few-shots carrying brand into the classifier prompt | mitigate | Neutralized to `example.com` (illustrative, not configuration). |
| T-14-07 | Spoofing | onboarding.js posting team data to a hardcoded foreign `api.grooveos.app` | mitigate | Base is build-substituted; empty default posts same-origin relative — never a cross-deployment domain. |
</threat_model>

<verification>
- Zero `grooveos` in all 3 source files (KB, relevance_filter, onboarding.js).
- KB retains functional tokens (`@groove`, relative `/account/teams/` paths).
- SYSTEM_PROMPT stays >= 16,384 bytes; onboarding.js passes `node --check`.
</verification>

<success_criteria>
- No brand domain reaches end users via agent replies, classifier prompt, or onboarding fetches (PORT-01, amended SC#4 — backend/infra request-response path).
- onboarding.js base is config-driven with a boot-safe same-origin fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-02-SUMMARY.md`.
</output>
