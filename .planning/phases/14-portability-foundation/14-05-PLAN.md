---
phase: 14-portability-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/**  (history — excluding phase 14 dir)
  - .planning/features/**  (excluding open-core-edition-design.md)
  - docs/*.md
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "A grep for grooveos.app / aibrussels over .planning (excluding the documented keep-as-is exceptions) and docs/*.md returns zero matches"
    - "The decision record (open-core-edition-design.md Locked Decisions), 14-CONTEXT.md, 14-RESEARCH.md, and the phase-14 PLAN files are untouched (they deliberately name the tokens being removed)"
    - "The pragmatic bound (what was intentionally NOT scrubbed and why) is logged in the SUMMARY for user confirmation"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Domain-neutral product/constraint prose"
  key_links: []
---

<objective>
Mechanical, scriptable de-branding of the repo's documentation + planning history (D-01 full
cleanup scope: "on nettoie l'historique aussi"). Replace `grooveos.app` → `example.com` and
`aibrussels` → `your-team` across `.planning/**` history, `.planning/PROJECT.md`, and repo-root
`docs/*.md`, EXCEPT the documented keep-as-is exceptions. This is a find/replace pass — a Sonnet
executor runs it. `"default"` team_scope is NEUTRAL (D-04) and is NOT a scrub target.

Purpose: an operator cloning the repo does not find xbrain's brand baked into the planning history
or runbooks. Non-blocking for boot — pure prose.
Output: brand-free planning/docs prose + a logged pragmatic bound of what was intentionally skipped.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scriptable scrub of .planning history + PROJECT.md (with keep-as-is exclusions)</name>
  <files>.planning/PROJECT.md, .planning/phases/**, .planning/features/**</files>
  <read_first>
    - .planning/phases/14-portability-foundation/14-CONTEXT.md (D-01 keep-as-is list + placeholder tokens: example.com, your-team)
    - .planning/PROJECT.md (the product/constraint tables carrying grooveos.app)
  </read_first>
  <action>
    Run a scripted find/replace over `.planning/**` with these EXACT exclusions (keep-as-is — they
    deliberately name the tokens being removed; scrubbing them destroys the decision record):
      - `.planning/features/open-core-edition-design.md`  (the whole design/decision record)
      - `.planning/phases/14-portability-foundation/**`    (14-CONTEXT.md, 14-RESEARCH.md, all 14-*-PLAN.md, future SUMMARYs)
      - `.planning/ROADMAP.md` and `.planning/STATE.md`     (GSD-tooling-managed status files — left as historical audit trail to avoid collision with the phase's own ROADMAP/STATE updates; see pragmatic-bound note)
    Replacement rules (case-sensitive):
      - `grooveos.app`  → `example.com`   (covers every subdomain: chat./api./mcp./lang./bridge./centrifugo./projects./adm.)
      - `aibrussels`    → `your-team`
    Do NOT touch the literal `default` / `"default"` team_scope (neutral per D-04) — it is not a scrub target.
    Suggested mechanism (executor may adapt): list candidate files with
      `grep -rl -e 'grooveos.app' -e 'aibrussels' .planning --exclude-dir=14-portability-foundation`
    then filter out ROADMAP.md, STATE.md, open-core-edition-design.md, and apply
      `sed -i -e 's/grooveos\.app/example.com/g' -e 's/aibrussels/your-team/g'` to the remainder.
    Also scrub `.planning/PROJECT.md` product/constraint tables the same way.
  </action>
  <acceptance_criteria>
    - `grep -rl -e 'grooveos.app' -e 'aibrussels' .planning --exclude-dir=14-portability-foundation | grep -v -e 'open-core-edition-design.md' -e 'ROADMAP.md' -e 'STATE.md'` returns 0 files.
    - `grep -c 'grooveos.app' .planning/features/open-core-edition-design.md` is UNCHANGED from before the task (decision record intact — capture the count in read_first and re-assert).
    - `grep -c 'grooveos.app' .planning/phases/14-portability-foundation/14-CONTEXT.md` is UNCHANGED (keep-as-is honored).
    - `grep -c 'grooveos.app' .planning/PROJECT.md` returns 0.
  </acceptance_criteria>
  <verify>Run the exclusion-aware grep (0 files) and confirm the 2 keep-as-is counts are unchanged.</verify>
  <done>.planning history + PROJECT.md are brand-free except the documented decision-record/keep-as-is files.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub repo-root docs/*.md + log the pragmatic bound</name>
  <files>docs/*.md</files>
  <read_first>
    - docs (list `docs/*.md`; note these are operator runbooks that also reference GCP project ids — those are NOT scrub targets, only grooveos.app / aibrussels are)
    - .planning/phases/14-portability-foundation/14-RESEARCH.md "Docs / marketing / planning prose" table (the SKIP recommendation for app-site/docs + marketing-site/docs)
  </read_first>
  <action>
    Apply the same `grooveos.app → example.com` / `aibrussels → your-team` scrub to repo-root
    `docs/*.md` runbooks. Preserve all non-target technical strings (GCP project ids, Cloudflare zone
    names, IPs) — only the two brand tokens change.
    Then LOG the pragmatic bound in the plan SUMMARY (per CONTEXT.md Claude's Discretion —
    "propose a pragmatic bound and log what it skips"): the following are INTENTIONALLY NOT scrubbed
    in Phase 14, with per-area occurrence counts, for the user to confirm or override in a follow-up:
      - `app-site/docs/**`, `app-site/v0-v12/**`, `marketing-site/docs/**` — LIVE hosted-product
        documentation (a separate Firebase deploy); scrubbing to example.com would break the live
        grooveos.app marketing/docs site. Same logic as README's intentional "Hosted product" positioning.
      - `README.md` intentional positioning lines ("The open-source engine behind GrooveOS", "Hosted product").
      - `.planning/ROADMAP.md`, `.planning/STATE.md` — GSD-managed status/audit trail.
      - `chrome-extension/**`, `app-site/account/**` static JS — DEFERRED to Phase 16 (D-01c).
    Capture the occurrence count for each skipped area (`grep -rc` totals) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -rl -e 'grooveos.app' -e 'aibrussels' docs/*.md` returns 0 files (or "no matches").
    - The SUMMARY contains a "Pragmatic bound — intentionally NOT scrubbed" section listing the 4 skipped areas with occurrence counts.
  </acceptance_criteria>
  <verify>grep docs/*.md returns 0; SUMMARY logs the skipped-areas table.</verify>
  <done>Runbooks brand-free; the skipped live-product/deferred surfaces are logged with counts for user review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan edits prose/documentation only, no request/response path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | Over-broad sed corrupting the decision record or GSD-managed status files | mitigate | Explicit exclude list (design doc, 14 dir, ROADMAP.md, STATE.md); acceptance re-asserts keep-as-is counts unchanged. |
| T-14-09 | Information Disclosure | Live marketing/docs site broken to `example.com` if scrubbed | accept | Intentionally out of Phase-14 scrub scope; logged in SUMMARY for user decision (pragmatic bound). |
</threat_model>

<verification>
- Exclusion-aware grep over `.planning` + `docs/*.md` returns 0 files.
- Keep-as-is files (design doc, 14-CONTEXT.md) unchanged.
- SUMMARY logs the intentionally-skipped areas with counts.
</verification>

<success_criteria>
- Planning history + PROJECT.md + docs runbooks carry no `grooveos.app` / `aibrussels` outside the documented exceptions (PORT-01, D-01).
- The pragmatic bound is transparent and logged (Claude's Discretion honored).
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-05-SUMMARY.md`
(include the "Pragmatic bound — intentionally NOT scrubbed" table).
</output>
