---
phase: 14-portability-foundation
plan: 05
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/**  (history — EXCLUDING the 14-portability-foundation dir)
  - .planning/features/**  (EXCLUDING open-core-edition-design.md)
  - docs/**/*.md  (RECURSIVE — round-3 W-F: `docs/*.md` missed docs/superpowers/plans/*.md, which carries a brand token)
  - CLAUDE.md  (round-3 W-D: CLAUDE.md:62 contains team@grooveos.app — it is a technical doc, in SC#1 scope, and was owned by no plan)
  - infrastructure/scripts/PHASE13-HELPER-FIX-NOTES.md
  - apps/spike-mem0/test_data.json
  - apps/memory-api/README.md
  - apps/session-bridge/README.md
  - apps/mcp-brain/chatgpt-actions.json
  - apps/memory-api/tests/test_oauth_as.py
  - apps/memory-api/tests/test_phase12_auto_grant_regression.py
  - apps/memory-api/tests/test_github_catalog.py
  - apps/memory-api/tests/test_internal_resolve_team_scope.py
  - apps/mcp-brain/tests/test_oauth_resolve.py
  - apps/mcp-brain/tests/test_resolve.py
  - apps/librechat-bridge/tests/test_resolve_team_scope.py
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "A grep for grooveos / aibrussels over .planning (minus the documented keep-as-is exceptions), docs/*.md, and the orphan runtime-adjacent surfaces returns zero matches"
    - "Test fixtures + illustrative examples are NEUTRALIZED to a placeholder, not deleted (D-01) — and BOTH pytest suites still pass afterwards"
    - "The decision record (open-core-edition-design.md), 14-CONTEXT.md, 14-RESEARCH.md, the phase-14 PLAN/SUMMARY files, ROADMAP.md and STATE.md are UNTOUCHED (they deliberately name the tokens being removed)"
    - "The APPROVED SCOPE BOUNDS (hosted-product marketing/docs per D-01e; browser bundles per D-01c) are logged in the SUMMARY with occurrence counts — as approved bounds, NOT as gaps"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Domain-neutral product/constraint prose"
  key_links: []
---

<objective>
Mechanical, scriptable de-branding of the repo's documentation, planning history, and the
"orphan" runtime-adjacent surfaces that no other plan owns. Replace `grooveos.app` → `example.com`
and `aibrussels` → `your-team` across `.planning/**` history, `.planning/PROJECT.md`, repo-root
`docs/*.md`, the per-app READMEs, the ChatGPT-actions manifest, the spike fixture, and the test
fixtures — EXCEPT the documented keep-as-is exceptions.

This is a find/replace pass — a **Sonnet executor** runs it. Keep every instruction concrete and
mechanical.

`"default"` team_scope is NEUTRAL and is **NOT a scrub target** (D-04 + amended ROADMAP — explicitly
out of scope). Do not touch it anywhere.

Purpose: an operator cloning the repo does not find xbrain's brand baked into the planning history,
the runbooks, the READMEs, or the test fixtures. Non-blocking for boot — prose + fixtures only.
Output: brand-free docs/history/fixtures + a logged, APPROVED scope bound.

WAVE NOTE: this plan runs in wave 2 (not wave 1) because Task 3 scrubs test fixtures and must run
`pytest` afterwards — and the suites only collect once 14-01 has added the OAuth env defaults to
`conftest.py`. Running this before 14-01 would produce a red suite for reasons unrelated to the scrub.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scriptable scrub of .planning history + PROJECT.md (with keep-as-is exclusions)</name>
  <files>.planning/PROJECT.md, .planning/phases/**, .planning/features/**</files>
  <read_first>
    - .planning/phases/14-portability-foundation/14-CONTEXT.md (D-01 keep-as-is list + D-01e approved bound + the placeholder tokens: `example.com`, `your-team`)
    - .planning/PROJECT.md (the product/constraint tables carrying grooveos.app)
    - BEFORE editing, capture the baseline counts you must NOT change:
      `grep -c 'grooveos' .planning/features/open-core-edition-design.md`
      `grep -c 'grooveos' .planning/phases/14-portability-foundation/14-CONTEXT.md`
  </read_first>
  <action>
    Run a scripted find/replace over `.planning/**` with these EXACT exclusions (keep-as-is — they
    deliberately NAME the tokens being removed; scrubbing them destroys the decision record):
      - `.planning/features/open-core-edition-design.md`   (the whole design/decision record)
      - `.planning/phases/14-portability-foundation/**`     (14-CONTEXT.md, 14-RESEARCH.md, every
        14-*-PLAN.md, and any 14-*-SUMMARY.md written by sibling plans — several of those SUMMARYs
        deliberately record the PROD VALUES as a DEPLOY-PREREQ)
      - `.planning/ROADMAP.md` and `.planning/STATE.md`     (GSD-tooling-managed status files — left as
        the historical audit trail; also avoids colliding with this phase's own ROADMAP/STATE updates)
    Replacement rules (case-sensitive):
      - `grooveos.app`  → `example.com`   (covers every subdomain: chat./api./mcp./lang./bridge./centrifugo./projects./adm.)
      - `aibrussels`    → `your-team`
    Do NOT touch the literal `default` / `"default"` team_scope — NOT a scrub target (D-04).
    Do NOT touch GCP project ids (`xbrain-495115`), Cloudflare zone names, or IPs.
    Suggested mechanism (adapt as needed):
      `grep -rl -e 'grooveos.app' -e 'aibrussels' .planning --exclude-dir=14-portability-foundation \
         | grep -v -e 'open-core-edition-design.md' -e 'ROADMAP.md' -e 'STATE.md' \
         | xargs sed -i -e 's/grooveos\.app/example.com/g' -e 's/aibrussels/your-team/g'`
    Then apply the same two rules to `.planning/PROJECT.md`.
  </action>
  <acceptance_criteria>
    - `grep -rl -e 'grooveos' -e 'aibrussels' .planning --exclude-dir=14-portability-foundation | grep -v -e 'open-core-edition-design.md' -e 'ROADMAP.md' -e 'STATE.md'` returns NOTHING (0 files).
    - The two keep-as-is baseline counts captured in <read_first> are UNCHANGED (re-run both greps and compare).
    - `grep -c 'grooveos' .planning/PROJECT.md` returns 0.
    - No `default` team_scope literal was altered: `git diff --stat .planning/` shows changes only from the two replacement rules (spot-check the diff for any `default` → something edit; there must be none).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rl -e 'grooveos' -e 'aibrussels' .planning --exclude-dir=14-portability-foundation | grep -v -e 'open-core-edition-design.md' -e 'ROADMAP.md' -e 'STATE.md')" && test "$(grep -c 'grooveos' .planning/PROJECT.md)" = "0"</automated>
  </verify>
  <done>.planning history + PROJECT.md are brand-free except the documented decision-record / GSD-managed / phase-14 keep-as-is files.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub docs/**/*.md (RECURSIVE) + CLAUDE.md + the runbook notes</name>
  <files>docs/**/*.md, CLAUDE.md, infrastructure/scripts/PHASE13-HELPER-FIX-NOTES.md</files>
  <read_first>
    - `ls docs/**/*.md` — **RECURSIVE, not `docs/*.md`** (round-4 W-I): `docs/superpowers/plans/2026-05-07-team-onboarding-modal.md` alone carries ~15 brand hits and a non-recursive glob misses it. These are operator runbooks; they also reference GCP project ids and Cloudflare zones — those are NOT scrub targets, only the two brand tokens are.
    - **CLAUDE.md** (round-4 BLOCKER-3): line ~62 contains `` compte `team@grooveos.app` ``. It is a technical doc in SC#1 scope, it was listed in this plan's `files_modified` but **no task actually scrubbed it**, and 14-06 check (a) does not scan the repo root — so the leak would have survived the phase while the gate certified green. It is scrubbed HERE.
    - infrastructure/scripts/PHASE13-HELPER-FIX-NOTES.md (a runbook note carrying `aibrussels`)
  </read_first>
  <action>
    Apply the same two rules (`grooveos.app` → `example.com`, `aibrussels` → `your-team`) RECURSIVELY to
    `docs/**/*.md`, to **`CLAUDE.md`**, and to `infrastructure/scripts/PHASE13-HELPER-FIX-NOTES.md`.
    Preserve every non-target technical string: GCP project ids (`xbrain-495115`), Cloudflare zone
    names, IP addresses, container names, port numbers. ONLY the two brand tokens change.
    Do NOT touch `app-site/docs/**`, `marketing-site/docs/**`, `app-site/v0-v12/**`, or `README.md`
    positioning lines — those are the APPROVED SCOPE BOUND (D-01e: hosted-product marketing/docs
    legitimately keep grooveos.app; they are the live commercial site, not OSS code).
    Do NOT touch `chrome-extension/**` or `app-site/account/**` — DEFERRED to Phase 16 (D-01c).
  </action>
  <acceptance_criteria>
    - `grep -rl -e 'grooveos' -e 'aibrussels' docs/ CLAUDE.md infrastructure/scripts/PHASE13-HELPER-FIX-NOTES.md` returns nothing. (Note `grep -r docs/` IS recursive — this is what forces the `docs/superpowers/**` files to be scrubbed too.)
    - `grep -c 'grooveos' CLAUDE.md` returns **0** (round-4 BLOCKER-3 — this file was claimed but never scrubbed).
    - The approved-bound surfaces are STILL branded (proving no over-scrub): `grep -rc 'grooveos' README.md` returns >= 1, and `grep -rl 'grooveos' app-site/docs/ 2>/dev/null | head -1` is non-empty.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rl -e 'grooveos' -e 'aibrussels' docs/ CLAUDE.md infrastructure/scripts/PHASE13-HELPER-FIX-NOTES.md 2>/dev/null)" && test "$(grep -c 'grooveos' README.md)" -ge 1</automated>
  </verify>
  <done>Runbooks are brand-free; the approved-bound marketing/README surfaces are provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Neutralize the orphan surfaces — READMEs, chatgpt-actions.json, spike fixture, test fixtures (suites must stay green)</name>
  <files>apps/memory-api/README.md, apps/session-bridge/README.md, apps/mcp-brain/chatgpt-actions.json, apps/spike-mem0/test_data.json, apps/memory-api/tests/test_oauth_as.py, apps/memory-api/tests/test_phase12_auto_grant_regression.py, apps/memory-api/tests/test_github_catalog.py, apps/memory-api/tests/test_internal_resolve_team_scope.py, apps/mcp-brain/tests/test_oauth_resolve.py, apps/mcp-brain/tests/test_resolve.py, apps/librechat-bridge/tests/test_resolve_team_scope.py</files>
  <read_first>
    - `grep -rn -e 'grooveos' -e 'aibrussels' apps/ --include=*.md --include=*.json --include=*.py | grep -v '/app/'` — the exact current hit list for these orphan surfaces
    - apps/mcp-brain/tests/test_oauth_resolve.py (the `patch("app.oauth_verify.settings.OAUTH_RESOURCE_URL", "https://mcp.grooveos.app/mcp")` + matching `"aud": "https://mcp.grooveos.app/mcp/"` pair — a CONSISTENT swap keeps them matching)
    - apps/memory-api/tests/test_oauth_as.py (`_RESOURCE = "https://mcp.grooveos.app/mcp"` at :78 and the deliberate MISMATCH value `"https://mcp.grooveos.app/other"` at :368 — a consistent swap keeps them a mismatch)
  </read_first>
  <action>
    These surfaces are named in D-01 ("test fixtures + illustrative few-shot examples → neutral
    placeholder, not deletion") but are owned by no other plan. Cover them here.

    Apply the SAME two case-sensitive rules — `grooveos.app` → `example.com`, `aibrussels` → `your-team` — to:
      1. `apps/memory-api/README.md`, `apps/session-bridge/README.md` (technical docs).
      2. `apps/mcp-brain/chatgpt-actions.json` (`"servers": [{"url": "https://api.grooveos.app"}]` →
         `https://api.example.com`). This file is a MANUAL operator template for configuring a ChatGPT
         Custom GPT — it is loaded by no service (14-RESEARCH.md A3). Add a one-line note to
         `apps/mcp-brain/README.md` if one exists, else to the SUMMARY, saying the operator must edit
         `servers[0].url` to their own domain.
      3. `apps/spike-mem0/test_data.json` (Phase-2 POC fixture).
      4. The 7 test files listed in <files>.

    CRITICAL — do NOT touch these (owned by 14-01, already merged):
      `apps/memory-api/tests/conftest.py` and `apps/mcp-brain/tests/conftest.py`.
    They contain NO brand token, so a token-based sed will not match them anyway — but never
    rewrite/restructure them here.

    The swaps are CONSISTENT (same token → same replacement everywhere), so every fixture relationship
    holds: an assertion that two values MATCH still matches; the deliberate audience-MISMATCH pair in
    `test_oauth_as.py` (`.../mcp` vs `.../other`) stays a mismatch.
    Do NOT touch `"default"` team_scope literals in any test (D-04).
  </action>
  <acceptance_criteria>
    - `grep -rl -e 'grooveos' -e 'aibrussels' apps/ --include=*.md --include=*.json --include=*.py` returns NOTHING (0 files) — this covers the orphan surfaces AND confirms the 14-01/14-02 plans already cleared `apps/*/app/**`.
    - `grep -c 'example.com' apps/mcp-brain/chatgpt-actions.json` returns >= 1 and `python -c "import json; json.load(open('apps/mcp-brain/chatgpt-actions.json'))"` exits 0 (still valid JSON).
    - `python -c "import json; json.load(open('apps/spike-mem0/test_data.json'))"` exits 0.
    - **SUITES STILL GREEN (the whole point of the wave-2 placement):**
      `cd apps/memory-api && python -m pytest -q` exits 0, and
      `cd apps/mcp-brain && python -m pytest -q` exits 0, and
      `cd apps/librechat-bridge && python -m pytest -q` exits 0.
      (Integration tests may SKIP without Docker — skips are not failures. A FAILURE here means the
      sed broke a fixture relationship: fix the fixture, do not revert the scrub.)
    - `git diff --stat apps/memory-api/tests/conftest.py apps/mcp-brain/tests/conftest.py` shows NO changes (14-01's files untouched).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rl -e 'grooveos' -e 'aibrussels' apps/ --include=*.md --include=*.json --include=*.py)" && cd apps/mcp-brain && python -m pytest -q && cd ../memory-api && python -m pytest -q && cd ../librechat-bridge && python -m pytest -q</automated>
  </verify>
  <done>Every orphan surface named in D-01 is neutralized to a placeholder; all three pytest suites still pass; 14-01's conftest files are untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Log the APPROVED scope bounds with occurrence counts</name>
  <files>.planning/phases/14-portability-foundation/14-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/14-portability-foundation/14-CONTEXT.md D-01c + D-01e (the two user-approved bounds)
    - .planning/ROADMAP.md Phase 14 "AMENDED 2026-07-12" block (the same bounds, ratified into the success criteria)
  </read_first>
  <action>
    Capture the occurrence counts for each intentionally-unscrubbed area and write them into the
    SUMMARY under a section titled exactly `## Approved scope bounds — intentionally NOT scrubbed`.
    Frame these as **APPROVED BOUNDS, not gaps** (user-approved 2026-07-12; ROADMAP amended to match).

    Collect the counts with:
      `grep -rc 'grooveos' app-site/docs/ marketing-site/docs/ app-site/v0-v12/ README.md 2>/dev/null | awk -F: '{s+=$2} END {print s}'`
      `grep -rc 'grooveos' chrome-extension/ app-site/account/ 2>/dev/null | awk -F: '{s+=$2} END {print s}'`
      `grep -rc 'grooveos' .planning/ROADMAP.md .planning/STATE.md .planning/features/open-core-edition-design.md 2>/dev/null | awk -F: '{s+=$2} END {print s}'`

    The four areas to table (area | occurrences | why | revisit):
      1. `app-site/docs/**`, `marketing-site/docs/**`, `app-site/v0-v12/**`, `README.md` positioning —
         D-01e: the LIVE hosted-product marketing/docs. Their domain legitimately IS grooveos.app.
         Scrubbing them would break the live Firebase deploy and is semantically wrong. Revisit only if
         the marketing surface is extracted from the OSS repo (Phase 16).
      2. `chrome-extension/**`, `app-site/account/**` — D-01c: static browser bundles with no config
         mechanism; the frontend is REPLACED in Phase 16. Cleaning code we are about to delete is waste.
      3. `.planning/ROADMAP.md`, `.planning/STATE.md` — GSD-managed status/audit trail.
      4. `.planning/features/open-core-edition-design.md` + `.planning/phases/14-portability-foundation/**` —
         the decision record and this phase's own artifacts; they NAME the tokens being removed.
    Also note: the title-case product name `GrooveOS` (librechat.yaml promptPrefix, README) is a
    PRODUCT NAME, not a domain hardcode — out of D-01's token scope, and a case-sensitive grep for
    `grooveos` does not match it. Rebrand is a Phase-16 concern.
  </action>
  <acceptance_criteria>
    - `grep -c '## Approved scope bounds — intentionally NOT scrubbed' .planning/phases/14-portability-foundation/14-05-SUMMARY.md` returns 1.
    - The section contains all 4 areas, each with a numeric occurrence count (not "many"/"several").
    - The section explicitly uses the words "approved" and "not a gap" (so a future reader does not re-open a settled decision).
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Approved scope bounds — intentionally NOT scrubbed' .planning/phases/14-portability-foundation/14-05-SUMMARY.md && grep -qi 'not a gap' .planning/phases/14-portability-foundation/14-05-SUMMARY.md</automated>
  </verify>
  <done>The two user-approved bounds (D-01c, D-01e) plus the GSD/decision-record exclusions are logged with hard counts, framed as approved bounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary — this plan edits prose, docs, and test fixtures. No request/response path changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | Over-broad sed corrupting the decision record, the GSD status files, or 14-01's conftest files | mitigate | Explicit exclude list (design doc, phase-14 dir, ROADMAP.md, STATE.md, both conftest.py); acceptance re-asserts the keep-as-is counts are unchanged and `git diff --stat` on the conftests is empty. |
| T-14-23 | Tampering | A consistent-token sed silently breaking a test fixture relationship (e.g. an audience match/mismatch pair) | mitigate | All three pytest suites must exit 0 after the scrub (Task 3 acceptance). The swaps are consistent, so match/mismatch relationships are preserved by construction. |
| T-14-09 | Information Disclosure | The live marketing/docs site broken to `example.com` if over-scrubbed | accept (approved bound) | D-01e: explicitly out of scope, user-approved, ratified in the amended ROADMAP. Task 2 asserts these surfaces are still branded (over-scrub tripwire). |
</threat_model>

<verification>
- Exclusion-aware grep over `.planning`, `docs/`, and `apps/` (md/json/py) returns 0 files.
- Keep-as-is files (design doc, 14-CONTEXT.md, ROADMAP, STATE, both conftests) provably unchanged.
- All three pytest suites pass after the fixture scrub.
- SUMMARY logs the 4 approved bounds with numeric counts.
</verification>

<success_criteria>
- Planning history + PROJECT.md + runbooks + READMEs + fixtures carry no `grooveos` / `aibrussels` outside the documented exceptions (PORT-01, D-01).
- The approved scope bounds are transparent, counted, and labelled as approved (not silent gaps).
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-05-SUMMARY.md`
(including the `## Approved scope bounds — intentionally NOT scrubbed` table).
</output>
