---
phase: 14-portability-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memory-api/app/config.py
  - apps/memory-api/app/main.py
  - apps/memory-api/app/deps.py
  - apps/memory-api/app/services/notifications.py
  - apps/memory-api/app/routes/waitlist.py
  - apps/memory-api/tests/conftest.py
  - apps/mcp-brain/app/config.py
  - apps/mcp-brain/tests/conftest.py
  - apps/drive-sync/app/config.py
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "memory-api and mcp-brain boot with neutral public-URL defaults (no grooveos.app baked into any Settings default)"
    - "Setting OAUTH_ISSUER_URL/OAUTH_RESOURCE_URL to empty crashes both services at boot with a clear, actionable error (loud fail, never a silent wrong-audience token)"
    - "The CORS allow-origin regex is env-sourced — a self-hoster's browser origin is NOT hardcode-blocked (amended ROADMAP SC#4 names CORS explicitly)"
    - "notifications.py email body + dashboard link read domain from config (SMTP_FROM / APP_PUBLIC_URL), not a hardcoded grooveos.app string"
    - "waitlist sender default carries no brand (domain AND display name neutralized)"
    - "Both pytest suites (memory-api, mcp-brain) still COLLECT and PASS after the OAuth fail-fast validator lands"
  artifacts:
    - path: "apps/memory-api/app/config.py"
      provides: "Neutral Settings defaults + APP_PUBLIC_URL + CORS_ALLOWED_ORIGIN_REGEX + OAuth fail-fast field_validator"
      contains: "field_validator"
    - path: "apps/mcp-brain/app/config.py"
      provides: "Empty OAuth defaults + fail-fast field_validator (mitigates module-import-time landmine)"
      contains: "field_validator"
    - path: "apps/mcp-brain/tests/conftest.py"
      provides: "Test-harness env defaults so Settings() constructs at collection time"
      contains: "OAUTH_ISSUER_URL"
  key_links:
    - from: "apps/memory-api/app/main.py CORSMiddleware"
      to: "settings.CORS_ALLOWED_ORIGIN_REGEX"
      via: "allow_origin_regex=settings.CORS_ALLOWED_ORIGIN_REGEX"
      pattern: "allow_origin_regex=settings\\.CORS_ALLOWED_ORIGIN_REGEX"
    - from: "apps/memory-api/app/services/notifications.py"
      to: "settings.APP_PUBLIC_URL / settings.SMTP_FROM"
      via: "import from app.config"
      pattern: "settings\\.(APP_PUBLIC_URL|SMTP_FROM)"
    - from: "apps/mcp-brain/app/main.py:_PROTECTED_RESOURCE_METADATA_URL"
      to: "Settings() construction"
      via: "validator raises before module-level URL is computed"
      pattern: "field_validator"
---

<objective>
De-hardcode the Python server config surface (PORT-01). Neutralize every `grooveos.app` default in
`memory-api`, `mcp-brain`, and `drive-sync` Settings; add `APP_PUBLIC_URL` (notifications) and
`CORS_ALLOWED_ORIGIN_REGEX` (the request-path CORS hardcode named by the amended ROADMAP SC#4);
neutralize the waitlist sender; add an **unconditional fail-fast Pydantic `field_validator`** on the
OAuth identity URLs in BOTH services; and repair the two pytest suites, which the validator would
otherwise break at collection time.

Purpose: this is the correctness core of the phase. Public URL defaults become boot-safe
(`localhost`); OAuth identity URLs become empty + fail-fast (never a misleading `grooveos.app`
default that mints tokens bound to the wrong resource); the CORS allow-list stops silently blocking
every operator whose domain is not grooveos.app.
Output: neutral, config-driven server settings; the first `field_validator` in the codebase; two
green test suites.

NOTE ON TASK COUNT (4, above the usual 3): every edit here is surgical — ~40 lines written across
9 files, all single-line default swaps or 3-line insertions. Total context cost is that of a single
medium task; splitting would fragment one coherent config contract across two plans.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md

<interfaces>
<!-- Current Settings shape (memory-api) — the executor edits these DEFAULT VALUES, not the field names. -->
From apps/memory-api/app/config.py:
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")
    MEMORY_API_EXTERNAL_URL: str = "https://chat.grooveos.app"   # -> neutral localhost
    SMTP_FROM: str = "noreply@grooveos.app"                      # -> noreply@example.com
    CENTRIFUGO_WS_URL_PUBLIC: str = "wss://centrifugo.grooveos.app/connection/websocket"  # -> localhost ws
    OAUTH_ISSUER_URL: str = "https://api.grooveos.app"           # -> "" + fail-fast
    OAUTH_RESOURCE_URL: str = "https://mcp.grooveos.app/mcp"     # -> "" + fail-fast
    # settings = Settings() is the module-level singleton at the bottom of the file.
```

From apps/mcp-brain/app/config.py (near-identical duplicated OAuth block, 27 lines total):
```python
    OAUTH_ISSUER_URL: str = "https://api.grooveos.app"     # -> "" + fail-fast
    OAUTH_RESOURCE_URL: str = "https://mcp.grooveos.app/mcp"  # -> "" + fail-fast
```

From apps/memory-api/app/main.py:96-102 — THE CORS HARDCODE (request-path functional bug):
```python
app.add_middleware(
    CORSMiddleware,
    allow_origin_regex=r"(chrome-extension://.*|https://chat\.grooveos\.app|https://grooveos\.app|https://grooveos\.web\.app|https://dejavu-app\.web\.app|https://claude\.ai)",
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
    allow_headers=["Authorization", "X-Team-Scope", "Content-Type", "Accept"],
)
```
<!-- A self-hoster's browser (their own app-site / their extension build) is CORS-BLOCKED by this
     regex today. Amended ROADMAP SC#4 names "CORS allow-origin regex" as an explicit leak surface. -->

From apps/memory-api/tests/conftest.py:20-25 — module-level env defaults so `from app.config import settings` works:
```python
os.environ.setdefault("DATABASE_URL", "postgresql+asyncpg://test:test@localhost:5432/test")
os.environ.setdefault("BRIDGE_SHARED_SECRET", "test-bridge-secret-do-not-use-in-prod")
os.environ.setdefault("GOOGLE_CLIENT_ID", "")
os.environ.setdefault("QDRANT_URL", "http://localhost:6333")
os.environ.setdefault("ADMIN_USER_SUBS", "admin-sub-1,admin-sub-2")
```
<!-- TEST-SUITE LANDMINE: neither OAuth var is setdefault'ed here (they never needed to be — they had
     grooveos.app defaults). `apps/mcp-brain/tests/` has NO conftest.py at all, and
     test_oauth_resolve.py imports app.oauth_verify -> app.config -> module-level `settings = Settings()`.
     After the validator lands, BOTH suites ERROR AT COLLECTION unless this plan fixes them. -->
<!-- Existing tests patch the values they assert on (`patch("app.oauth_verify.settings.OAUTH_RESOURCE_URL", ...)`)
     and test_oauth_as.py:105 only asserts `meta["issuer"].startswith("https://")` — so an https test
     default satisfies every existing assertion. Do NOT rewrite test bodies here. -->

<!-- LANDMINE (14-RESEARCH.md item 3): apps/mcp-brain/app/main.py computes
     _PROTECTED_RESOURCE_METADATA_URL = _resource_host_root() + "/.well-known/..."
     at MODULE-IMPORT time from settings.OAUTH_RESOURCE_URL. urlparse("") does NOT raise,
     it silently yields "://.well-known/...". The fail-fast MUST live on the Settings class
     (runs at Settings() construction = app.config import, which happens BEFORE app.main
     imports the constant), NOT in a FastAPI startup handler or per-request check. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: memory-api Settings — neutral defaults + APP_PUBLIC_URL + CORS_ALLOWED_ORIGIN_REGEX + OAuth fail-fast validator</name>
  <files>apps/memory-api/app/config.py</files>
  <read_first>
    - apps/memory-api/app/config.py (the whole file — 165 lines; note the `settings = Settings()` singleton at the bottom)
    - apps/memory-api/pyproject.toml (confirm pydantic>=2.10,<3 — field_validator + ValidationInfo API available)
  </read_first>
  <action>
    In apps/memory-api/app/config.py:
    1. Swap these DEFAULT VALUES to neutral (keep field names/types unchanged):
       - `MEMORY_API_EXTERNAL_URL` default → `"http://localhost:8000"`
       - `SMTP_FROM` default → `"noreply@example.com"`
       - `CENTRIFUGO_WS_URL_PUBLIC` default → `"ws://localhost:8000/connection/websocket"`
    2. Change the OAuth identity defaults to EMPTY strings (D-03):
       - `OAUTH_ISSUER_URL: str = ""`
       - `OAUTH_RESOURCE_URL: str = ""`
    3. Add a NEW setting near SMTP_FROM: `APP_PUBLIC_URL: str = "http://localhost:8000"`
       with a one-line comment: "Public base URL of the deployment; used in outbound email links (D-05)."
    4. Add a NEW setting `CORS_ALLOWED_ORIGIN_REGEX: str` with the neutral default
       `r"(chrome-extension://.*|http://localhost(:\d+)?)"` and a comment:
       "Browser origins allowed to call this API (FastAPI CORSMiddleware allow_origin_regex).
        Set to your own front-end origins in .env. Wired in main.py (amended ROADMAP SC#4)."
       Rationale: today main.py hardcodes a grooveos.app-only regex — every OTHER operator's browser
       is CORS-blocked. The neutral default keeps a localhost self-host + the Chrome extension working.
    5. Add the codebase's first Pydantic v2 `field_validator` on the Settings class (import
       `field_validator` from `pydantic`). Apply it to `OAUTH_ISSUER_URL` and `OAUTH_RESOURCE_URL`;
       raise `ValueError` when the value is empty, with an actionable message naming the var and an
       example (e.g. "OAUTH_ISSUER_URL is required — set it in .env, e.g. OAUTH_ISSUER_URL=https://api.yourdomain.com").
       This runs at `Settings()` construction (module import), crashing boot loudly before Uvicorn binds.
       Use the exact pattern from 14-RESEARCH.md "Fail-Fast Risk" section (field_validator + info.field_name).
       Do NOT apply the validator to CORS_ALLOWED_ORIGIN_REGEX (it has a working default; empty is not fatal).
    Leave every other field, comment, and the `settings = Settings()` singleton unchanged.
    Do NOT touch me.py:206 (`Field(default="default")` is a token NAME, not team_scope — 14-RESEARCH.md correction).
  </action>
  <acceptance_criteria>
    - `grep -c 'grooveos' apps/memory-api/app/config.py` returns 0.
    - `grep -c 'field_validator' apps/memory-api/app/config.py` returns >= 2 (import + decorator).
    - `grep -c 'APP_PUBLIC_URL' apps/memory-api/app/config.py` returns >= 1.
    - `grep -c 'CORS_ALLOWED_ORIGIN_REGEX' apps/memory-api/app/config.py` returns >= 1.
    - `grep -c 'OAUTH_ISSUER_URL: str = ""' apps/memory-api/app/config.py` returns 1 (empty default).
    - Empty-env fail-fast: `cd apps/memory-api && OAUTH_ISSUER_URL= OAUTH_RESOURCE_URL= DATABASE_URL=x BRIDGE_SHARED_SECRET=x python -c "from app.config import Settings; Settings()"` exits NON-zero and prints a message containing `OAUTH_ISSUER_URL`.
    - Set-env success: same command with `OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && OAUTH_ISSUER_URL= OAUTH_RESOURCE_URL= DATABASE_URL=x BRIDGE_SHARED_SECRET=x python -c "from app.config import Settings; Settings()" ; test $? -ne 0 && OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp DATABASE_URL=x BRIDGE_SHARED_SECRET=x python -c "from app.config import Settings; Settings()"</automated>
  </verify>
  <done>memory-api config.py has neutral public defaults, empty+validated OAuth URLs, APP_PUBLIC_URL, and CORS_ALLOWED_ORIGIN_REGEX. Boot fails loudly when OAuth URLs empty, succeeds when set.</done>
</task>

<task type="auto">
  <name>Task 2: mcp-brain OAuth fail-fast validator (module-import landmine) + drive-sync comment scrub</name>
  <files>apps/mcp-brain/app/config.py, apps/drive-sync/app/config.py</files>
  <read_first>
    - apps/mcp-brain/app/config.py (27 lines — the duplicated OAUTH_ISSUER_URL/OAUTH_RESOURCE_URL block + the `settings = Settings()` singleton)
    - apps/mcp-brain/app/main.py lines 40-52 (the `_PROTECTED_RESOURCE_METADATA_URL` module-level constant computed from OAUTH_RESOURCE_URL — the landmine this validator protects)
    - apps/drive-sync/app/config.py (the `DRIVE_WEBHOOK_PUBLIC_URL` comment referencing grooveos.app)
  </read_first>
  <action>
    In apps/mcp-brain/app/config.py:
    1. Set `OAUTH_ISSUER_URL: str = ""` and `OAUTH_RESOURCE_URL: str = ""` (empty defaults, D-03).
    2. Add the SAME `field_validator` as Task 1 (import `field_validator` from `pydantic`) on
       `OAUTH_ISSUER_URL` + `OAUTH_RESOURCE_URL`, raising `ValueError` when empty with the actionable
       message. This is the highest-risk site: main.py computes `_PROTECTED_RESOURCE_METADATA_URL` at
       import time via `urlparse(settings.OAUTH_RESOURCE_URL)`, which does NOT raise on "" — the
       validator must crash `Settings()` construction first so the malformed constant is never built.
       Do NOT change main.py — the validator on the Settings class is the correct single-point fix.
    In apps/drive-sync/app/config.py:
    3. Scrub the `https://api.grooveos.app` reference in the `DRIVE_WEBHOOK_PUBLIC_URL` comment →
       `https://api.example.com/v1/drive-webhook`. The default value is already `""` (neutral) — do not
       change the value, only the comment.
  </action>
  <acceptance_criteria>
    - `grep -rc 'grooveos' apps/mcp-brain/app/config.py apps/drive-sync/app/config.py` returns 0 for each file.
    - `grep -c 'field_validator' apps/mcp-brain/app/config.py` returns >= 2.
    - Empty-env fail-fast: `cd apps/mcp-brain && OAUTH_ISSUER_URL= OAUTH_RESOURCE_URL= python -c "from app.config import Settings; Settings()"` exits NON-zero mentioning `OAUTH_RESOURCE_URL`.
    - Set-env: same with `OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mcp-brain && OAUTH_ISSUER_URL= OAUTH_RESOURCE_URL= python -c "from app.config import Settings; Settings()" ; test $? -ne 0 && OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp python -c "from app.config import Settings; Settings()"</automated>
  </verify>
  <done>mcp-brain crashes loudly at import when OAuth URLs empty (protecting the module-level metadata URL); drive-sync comment neutralized.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CORS from config + scrub deps.py comment + notifications/waitlist true hardcodes</name>
  <files>apps/memory-api/app/main.py, apps/memory-api/app/deps.py, apps/memory-api/app/services/notifications.py, apps/memory-api/app/routes/waitlist.py</files>
  <read_first>
    - apps/memory-api/app/main.py lines 90-103 (the French CORS comment at 92-95 + `add_middleware(CORSMiddleware, allow_origin_regex=r"(...grooveos...)")` at 96-102)
    - apps/memory-api/app/deps.py lines 296-306 (the `signed in via GitHub on grooveos.app` explanatory comment at ~301)
    - apps/memory-api/app/services/notifications.py lines 73-143 (`send_member_autojoined_email`: `dashboard_url: str = "https://grooveos.app"` param default + the `f"— xbrain (noreply@grooveos.app)\n"` body footer)
    - apps/memory-api/app/routes/waitlist.py line 14 (`WAITLIST_FROM = os.getenv("WAITLIST_FROM", "GrooveOS <waitlist@grooveos.app>")`)
    - apps/memory-api/app/config.py (confirm APP_PUBLIC_URL + CORS_ALLOWED_ORIGIN_REGEX + SMTP_FROM exist after Task 1)
  </read_first>
  <action>
    In apps/memory-api/app/main.py:
    1. Replace the hardcoded `allow_origin_regex=r"(chrome-extension://.*|https://chat\.grooveos\.app|...)"`
       with `allow_origin_regex=settings.CORS_ALLOWED_ORIGIN_REGEX`. Import `settings` from `app.config`
       if main.py does not already import it (check first — most routers do). Leave `allow_credentials`,
       `allow_methods`, `allow_headers` EXACTLY as they are.
    2. Rewrite the 3-line French comment above the middleware in ENGLISH and brand-free (project rule:
       code + comments are English only), e.g.: "CORS — browser clients (Chrome extension, web app)
       call this API cross-origin. Allowed origins come from CORS_ALLOWED_ORIGIN_REGEX (.env), never a
       hardcoded domain. Bearer-token auth remains the real access control (T-05-04-03 accepted)."
    In apps/memory-api/app/deps.py:
    3. Scrub the `grooveos.app` reference in the ~line-301 comment ("signed in via GitHub on grooveos.app
       first") → "signed in via GitHub on the web app first". Comment only — no logic change.
    In apps/memory-api/app/services/notifications.py:
    4. Change the `dashboard_url` parameter default in `send_member_autojoined_email` from
       `"https://grooveos.app"` to config-sourced AT CALL TIME: set the signature default to `None`
       (`dashboard_url: str | None = None`) and inside the function do
       `dashboard_url = dashboard_url or settings.APP_PUBLIC_URL`. Do NOT put `settings.APP_PUBLIC_URL`
       directly in the signature — Python evaluates defaults at def-time (import), which would freeze
       the value before env is applied.
    5. Replace the hardcoded footer line `f"— xbrain (noreply@grooveos.app)\n"` with
       `f"— xbrain ({settings.SMTP_FROM})\n"` (SMTP_FROM now defaults to noreply@example.com).
    In apps/memory-api/app/routes/waitlist.py:
    6. Neutralize the WAITLIST_FROM default — strip BOTH the brand domain AND the "GrooveOS" display
       name (14-RESEARCH.md Pitfall 4): `os.getenv("WAITLIST_FROM", "Example <waitlist@example.com>")`.
       Leave the `os.getenv` mechanism as-is (not worth migrating this one route to Settings).
  </action>
  <acceptance_criteria>
    - `grep -rc 'grooveos' apps/memory-api/app/main.py apps/memory-api/app/deps.py apps/memory-api/app/services/notifications.py apps/memory-api/app/routes/waitlist.py` returns 0 for EVERY file.
    - `grep -c 'allow_origin_regex=settings.CORS_ALLOWED_ORIGIN_REGEX' apps/memory-api/app/main.py` returns 1.
    - `grep -c 'settings.SMTP_FROM' apps/memory-api/app/services/notifications.py` returns >= 1.
    - `grep -c 'settings.APP_PUBLIC_URL' apps/memory-api/app/services/notifications.py` returns >= 1.
    - `grep -c 'GrooveOS' apps/memory-api/app/routes/waitlist.py` returns 0 (display name stripped).
    - `cd apps/memory-api && OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp DATABASE_URL=x BRIDGE_SHARED_SECRET=x python -c "import app.main"` exits 0 (app imports; CORS wiring valid).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp DATABASE_URL=x BRIDGE_SHARED_SECRET=x QDRANT_URL=http://localhost:6333 python -c "import app.main"</automated>
  </verify>
  <done>CORS allow-list is env-sourced (no operator is hardcode-blocked); deps.py comment neutral; both notifications.py hardcodes read from config; waitlist sender brand-free (domain + display name).</done>
</task>

<task type="auto">
  <name>Task 4: Repair both pytest suites — OAuth env defaults in conftest (memory-api patched, mcp-brain created)</name>
  <files>apps/memory-api/tests/conftest.py, apps/mcp-brain/tests/conftest.py</files>
  <read_first>
    - apps/memory-api/tests/conftest.py lines 20-26 (the existing `os.environ.setdefault(...)` block — extend it, do NOT restructure the file)
    - apps/mcp-brain/tests/ (confirm there is NO conftest.py — only __init__.py, test_oauth_resolve.py, test_resolve.py)
    - apps/mcp-brain/tests/test_oauth_resolve.py (confirm it imports app.oauth_verify -> app.config, and that it PATCHES the settings values it asserts on — so a neutral test default does not break its assertions)
  </read_first>
  <action>
    The Task 1 + Task 2 fail-fast validator makes `Settings()` raise when the OAuth vars are empty.
    Both test suites construct `Settings()` at import (module-level singleton), so WITHOUT this task
    both suites ERROR AT COLLECTION. Fix the harness, not the validator.

    1. In apps/memory-api/tests/conftest.py, add to the existing module-level setdefault block
       (right after the ADMIN_USER_SUBS line, same style):
       ```python
       os.environ.setdefault("OAUTH_ISSUER_URL", "https://api.test.example")
       os.environ.setdefault("OAUTH_RESOURCE_URL", "https://mcp.test.example/mcp")
       ```
       Change nothing else in this file.
    2. CREATE apps/mcp-brain/tests/conftest.py (new file) with the same mechanism — mcp-brain's Settings
       only needs the two OAuth vars (every other field has a working default):
       ```python
       """Shared pytest fixtures for mcp-brain.

       app.config constructs a module-level `settings = Settings()` at import, and the OAuth identity
       URLs fail-fast when empty (Phase 14, D-03). Supply neutral test values so the suite collects.
       """

       import os

       os.environ.setdefault("OAUTH_ISSUER_URL", "https://api.test.example")
       os.environ.setdefault("OAUTH_RESOURCE_URL", "https://mcp.test.example/mcp")
       ```
    Do NOT rewrite any test body. The existing tests patch the settings values they assert on
    (`patch("app.oauth_verify.settings.OAUTH_RESOURCE_URL", ...)`) and `test_oauth_as.py:105` only
    asserts `meta["issuer"].startswith("https://")` — the https test defaults satisfy both.
  </action>
  <acceptance_criteria>
    - `test -f apps/mcp-brain/tests/conftest.py` (created).
    - `grep -c 'OAUTH_ISSUER_URL' apps/mcp-brain/tests/conftest.py` returns >= 1 and `grep -c 'OAUTH_RESOURCE_URL' apps/mcp-brain/tests/conftest.py` returns >= 1.
    - `grep -c 'OAUTH_ISSUER_URL' apps/memory-api/tests/conftest.py` returns >= 1.
    - `cd apps/mcp-brain && python -m pytest -q` COLLECTS (no collection error) and exits 0.
    - `cd apps/memory-api && python -m pytest -q` COLLECTS (no collection error) and exits 0 (integration tests may SKIP if Docker is unavailable — skips are not failures).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mcp-brain && python -m pytest -q && cd ../memory-api && python -m pytest -q</automated>
  </verify>
  <done>Both suites collect and pass with the fail-fast validator active. No test body was rewritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → memory-api/mcp-brain Settings | `.env`-supplied OAUTH_ISSUER_URL / OAUTH_RESOURCE_URL cross into token-minting (issuer) and audience-check (resource) logic |
| memory-api (Authorization Server) → mcp-brain (Protected Resource) | issued `oat_` tokens carry an `aud` bound to OAUTH_RESOURCE_URL; mcp-brain independently checks it (RFC 8707) |
| browser origin → memory-api | CORSMiddleware `allow_origin_regex` decides which browser origins may make credentialed cross-origin calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | OAUTH_ISSUER_URL / OAUTH_RESOURCE_URL empty-default | mitigate | Unconditional `field_validator` on BOTH Settings classes raises at `Settings()` construction (boot) — no silent wrong-issuer/wrong-audience. Block-on: high. |
| T-14-02 | Tampering | mcp-brain `_PROTECTED_RESOURCE_METADATA_URL` (module-import-time `urlparse("")`) | mitigate | Validator runs at config import (before main.py imports the constant) → the malformed `"://.well-known/..."` string is never constructed; boot crashes instead. |
| T-14-03 | Information Disclosure | notifications.py email footer / dashboard link leaking `grooveos.app` to other operators' users | mitigate | Footer + link sourced from `settings.SMTP_FROM` / `settings.APP_PUBLIC_URL`; neutral defaults reveal only the operator's own configured domain. |
| T-14-17 | Elevation of Privilege | `CORS_ALLOWED_ORIGIN_REGEX` set too broadly (e.g. `.*`) by an operator, enabling credentialed cross-origin reads from any site | accept + document | The neutral default is NARROW (`chrome-extension://.*` + localhost) — never `.*`. `.env.example` (14-04) documents the risk of widening it. Bearer-token auth remains the real access control; CORS is defense-in-depth. Previously the regex was un-configurable, which was strictly worse for every non-grooveos operator (hard block). |
| T-14-18 | Denial of Service | The new validator crashes prod on the next deploy if the VM `.env` lacks the OAuth vars (it does today — they were served by the compose fallback) | mitigate | Pre-deploy guard: `Makefile env-check` extended (14-03b) + a blocking DEPLOY-PREREQ checkpoint (14-06). Tracked as ROADMAP SC#2(a). |
</threat_model>

<verification>
- Both services fail-fast at boot with empty OAuth URLs; both construct cleanly with `acme.example` values.
- Zero `grooveos` occurrences remain in the 9 edited files.
- `field_validator` exists in both config.py files (first use of the pattern in the codebase).
- CORS regex resolves from `settings.CORS_ALLOWED_ORIGIN_REGEX`.
- `pytest apps/memory-api -q` and `pytest apps/mcp-brain -q` both collect and pass.
</verification>

<success_criteria>
- memory-api + mcp-brain Settings carry no brand domain in any default (PORT-01, SC#1).
- OAuth identity URLs empty by default and validated fail-fast in both services (D-03, T-14-01/T-14-02).
- CORS allow-origin regex is env-sourced — no operator's browser is hardcode-blocked (SC#4).
- notifications.py + waitlist.py read domain/sender from config with neutral defaults (D-05).
- Neither test suite is broken by the new fail-fast validator.
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-01-SUMMARY.md`.
Include a `## DEPLOY-PREREQ (carry forward)` note: the VM `.env` must define `OAUTH_ISSUER_URL`,
`OAUTH_RESOURCE_URL`, and `CORS_ALLOWED_ORIGIN_REGEX` before the next deploy, or memory-api +
mcp-brain will crashloop (OAuth) / CORS-block the extension. Prod values are recorded in 14-06's SUMMARY.
</output>

