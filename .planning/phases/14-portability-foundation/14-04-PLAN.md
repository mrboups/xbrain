---
phase: 14-portability-foundation
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-03a, 14-03b]
files_modified:
  - .env.example
  - infrastructure/.env.example
  - apps/memory-api/.env.example
  - apps/librechat-bridge/.env.example
  - apps/openwebui-pipeline/.env.example
autonomous: true
requirements: [PORT-02]
must_haves:
  truths:
    - "An operator can fill the root .env.example and boot a working install pointed at their own domain WITHOUT opening any source file"
    - "Every var has a one-line comment and a safe neutral placeholder, grouped by concern, tagged [required] or [optional]"
    - "The vars that had NO template line at all — OAUTH_ISSUER_URL, OAUTH_RESOURCE_URL, CORS_ALLOWED_ORIGIN_REGEX, APP_PUBLIC_URL, XBRAIN_BASE_DOMAIN, AGENT_MENTION_ALIASES, MINIO_*, QDRANT_COLLECTION — are now present, so operators discover them without reading config.py"
    - "AGENT_MENTION_ALIASES is documented with its neutral default (`agent` -> the agent answers to `@agent` out of the box) and a note that xbrain's own deployment overrides it to `grooveos,groove,gr,g` (D-08). A rebrand is then a .env change, not a code change."
    - "OAUTH_ISSUER_URL + OAUTH_RESOURCE_URL are flagged REQUIRED with the fail-fast consequence spelled out (empty = boot crash)"
    - "The vestigial infrastructure/.env.example is deleted; the three per-service templates carry no brand"
  artifacts:
    - path: ".env.example"
      provides: "Slim, grouped, documented OSS env template with neutral placeholders"
      contains: "XBRAIN_BASE_DOMAIN"
  key_links:
    - from: ".env.example"
      to: "apps/memory-api/app/config.py + infrastructure/docker-compose.yml"
      via: "documents every var those two consume (no hidden hardcoded default)"
      pattern: "OAUTH_ISSUER_URL|APP_PUBLIC_URL|XBRAIN_BASE_DOMAIN|CORS_ALLOWED_ORIGIN_REGEX"
---

<objective>
Deliver PORT-02: a slim, documented OSS `.env.example` an operator fills without reading source.
Reorganize the current 121-var root `.env.example` into the D-06 concern groups, ADD the vars that
today have NO template line (their hidden code defaults pointed at grooveos.app — the root cause of
the whole phase), neutralize every brand placeholder, mark required-vs-optional, delete the vestigial
`infrastructure/.env.example`, and neutralize the three per-service templates.

Purpose: "fill a single slim documented .env.example and get a working install pointed at your own
domain, without opening any source file" (ROADMAP SC#3).
Output: an operator-fillable env template surface — the PORT-02 deliverable.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md
@.planning/phases/14-portability-foundation/14-01-PLAN.md
@.planning/phases/14-portability-foundation/14-03b-PLAN.md

<interfaces>
<!-- THE COMPLETE NEW/CHANGED VAR SET this file must now document (from 14-01 + 14-03a + 14-03b):
     NEW (never had a .env.example line):
       XBRAIN_BASE_DOMAIN          (14-03a — THE domain knob; drives all 8 nginx vhost server_names)
       APP_PUBLIC_URL              (14-01  — outbound email links)
       CORS_ALLOWED_ORIGIN_REGEX   (14-01  — browser origins allowed to call memory-api;
                                             MISSING => the extension + web app are CORS-blocked)
       OAUTH_ISSUER_URL            (14-01  — REQUIRED, empty = FATAL at boot)
       OAUTH_RESOURCE_URL          (14-01  — REQUIRED, empty = FATAL at boot)
       LIBRECHAT_ALLOWED_DOMAINS, BRIDGE_BASE_URL, APP_TEAMS_URL,
       LIBRECHAT_MEMORY_API_BASE, CENTRIFUGO_ALLOWED_ORIGINS, WEBUI_URL   (14-03b)
     PREVIOUSLY-HIDDEN (config.py defaults, no template line — 14-RESEARCH.md ".env.example gap"):
       MINIO_URL, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, MINIO_BUCKET, QDRANT_COLLECTION,
       GITHUB_FALLBACK_TOKEN, AGENT_RUNTIME_INTERNAL_URL, GITHUB_CATALOG_* -->

<!-- CRITICAL — the OAuth pair is the crashloop risk this phase created. .env.example is the ONLY place
     a self-hoster will ever see them. Their section header must state the consequence, not just the
     name. Cross-ref: 14-03b's `make env-check` now FAILS when they are absent. -->

<!-- Existing placeholder conventions ALREADY in root .env.example — reuse, don't invent: `__VM_HOST__`, `__FILL__`. -->
<!-- Makefile env-check required-var list AFTER 14-03b (this is the authoritative "required for boot" set):
     POSTGRES_PASSWORD GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET BRIDGE_SHARED_SECRET MEILI_MASTER_KEY
     OPENWEBUI_SECRET_KEY OAUTH_ISSUER_URL OAUTH_RESOURCE_URL CORS_ALLOWED_ORIGIN_REGEX -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root .env.example — slim, grouped, documented, neutral</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (full current file — 324 lines; note the existing `__VM_HOST__` / `__FILL__` placeholders and the 12 grooveos.app lines)
    - infrastructure/docker-compose.yml (SOURCE OF TRUTH for which vars actually exist — do NOT invent a var no service consumes; re-grep it after 14-03a/14-03b landed)
    - Makefile (the post-14-03b `env-check` list = the authoritative "[required]" set)
  </read_first>
  <action>
    Rewrite `.env.example` into these 6 sections (D-06), each var with a one-line comment, a neutral
    placeholder value, and a `# [required]` or `# [optional]` tag:

      1. `# === Required — minimal boot ===`
         POSTGRES_PASSWORD, DATABASE_URL, BRIDGE_SHARED_SECRET, JWT_ALGORITHM, QDRANT_URL,
         CENTRIFUGO_TOKEN_HMAC_SECRET, CENTRIFUGO_API_KEY, MEILI_MASTER_KEY, OPENWEBUI_SECRET_KEY.

      2. `# === LLM provider keys (at least one required) ===`
         ANTHROPIC_API_KEY, OPENAI_API_KEY, XAI_API_KEY.

      3. `# === Domain / public URLs (neutral defaults ship working; set these for a real deploy) ===`
         XBRAIN_BASE_DOMAIN  ← document as THE domain knob: "one var repoints every nginx vhost
             (chat./api./mcp./adm./lang./bridge./centrifugo./projects.<domain>). Default: localhost."
         MEMORY_API_EXTERNAL_URL, APP_PUBLIC_URL, CENTRIFUGO_WS_URL_PUBLIC, SMTP_FROM, WEBUI_URL,
         LIBRECHAT_ALLOWED_DOMAINS, BRIDGE_BASE_URL, APP_TEAMS_URL, LIBRECHAT_MEMORY_API_BASE,
         CENTRIFUGO_ALLOWED_ORIGINS,
         CORS_ALLOWED_ORIGIN_REGEX  ← comment MUST warn: "Browser origins allowed to call memory-api.
             If you serve a web app or a browser extension from your own domain, ADD IT HERE or the
             browser will be CORS-blocked. Do NOT set this to `.*` — that lets any site make
             credentialed calls. Default: chrome-extension + localhost."
         AGENT_MENTION_ALIASES  ← (14-01 Task 5, D-08) comment MUST read: "Comma-separated aliases that
             summon the agent in team chat, WITHOUT the leading '@'. Default: `agent` (so users type
             `@agent`). Set your own, e.g. `AGENT_MENTION_ALIASES=ai,assistant`. A rebrand is a change
             here, not in code. xbrain's own deployment sets `grooveos,groove,gr,g` to preserve its
             existing triggers."

      4. `# === OAuth identity — REQUIRED. Empty = memory-api and mcp-brain CRASH AT BOOT (by design) ===`
         OAUTH_ISSUER_URL   # [required] public base URL of THIS deployment's authorization server,
                            #            e.g. https://api.yourdomain.com
         OAUTH_RESOURCE_URL # [required] the mcp-brain audience issued tokens are bound to (RFC 8707),
                            #            e.g. https://mcp.yourdomain.com/mcp
         Add a 2-line note: "These have NO default. A misleading default would silently mint tokens
         bound to the wrong resource, so an empty value fails fast at boot instead. `make env-check`
         also refuses to deploy without them."
         Plus GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET.

      5. `# === Optional integrations (safe to leave blank — the feature no-ops) ===`
         GITHUB_APP_*, GITHUB_FALLBACK_TOKEN, SMTP_HOST/PORT/USER/PASSWORD, WAITLIST_*, GRANOLA_*,
         GOOGLE_CALENDAR_*, GOOGLE_DRIVE_*, RESEND_API_KEY, MINIO_URL/ACCESS_KEY/SECRET_KEY/BUCKET,
         QDRANT_COLLECTION, AGENT_RUNTIME_INTERNAL_URL, GITHUB_CATALOG_*, NEO4J_*.

      6. `# === SaaS-only / not part of OSS-light ===`
         LANGFUSE_*, GCS_BACKUP_BUCKET, VM_HOST/VM_USER/SSH_KEY.

    Replace ALL `grooveos.app` with neutral placeholders (`example.com` / `__FILL__` / the
    `${XBRAIN_BASE_DOMAIN}`-derived form). Strip the `GrooveOS` display name from WAITLIST_FROM.
    Add a short file header: "Copy to `.env`, fill every `[required]` var, leave the rest — they have
    safe defaults. Run `make env-check` to verify before deploying."
    Trim vars no service consumes. This is the slim OSS surface, not the full SaaS surface.
    Do NOT remove `BRIDGE_DEFAULT_TEAM_SCOPE` / `PIPELINE_DEFAULT_TEAM_SCOPE` — `default` is a neutral
    fallback and is KEPT (D-04, amended ROADMAP).
  </action>
  <acceptance_criteria>
    - `grep -c 'grooveos' .env.example` returns 0 and `grep -c 'GrooveOS' .env.example` returns 0.
    - All 6 section headers present: `grep -c '^# === ' .env.example` returns 6.
    - Every NEW var has a line: `grep -Ec '^(XBRAIN_BASE_DOMAIN|APP_PUBLIC_URL|CORS_ALLOWED_ORIGIN_REGEX|OAUTH_ISSUER_URL|OAUTH_RESOURCE_URL|LIBRECHAT_ALLOWED_DOMAINS|BRIDGE_BASE_URL|APP_TEAMS_URL|LIBRECHAT_MEMORY_API_BASE|CENTRIFUGO_ALLOWED_ORIGINS|WEBUI_URL)=' .env.example` returns 11.
    - Previously-hidden vars present: `grep -Ec '^(MINIO_URL|QDRANT_COLLECTION)=' .env.example` returns 2.
    - The OAuth section header states the consequence: `grep -c 'CRASH AT BOOT' .env.example` returns >= 1.
    - The CORS var carries the widening warning: the comment above `CORS_ALLOWED_ORIGIN_REGEX=` contains `CORS-blocked` (grep -B3 for it).
    - Tags applied: `grep -c '\[required\]' .env.example` returns >= 9 and `grep -c '\[optional\]' .env.example` returns >= 10.
    - **PORT-02 PROOF (no source file needed):** every var in the post-14-03b `make env-check` required
      list has a line in `.env.example`:
      `for v in POSTGRES_PASSWORD GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET BRIDGE_SHARED_SECRET MEILI_MASTER_KEY OPENWEBUI_SECRET_KEY OAUTH_ISSUER_URL OAUTH_RESOURCE_URL CORS_ALLOWED_ORIGIN_REGEX; do grep -q "^$v=" .env.example || echo "MISSING $v"; done`
      prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'grooveos' .env.example)" = "0" && test "$(grep -c '^# === ' .env.example)" = "6" && for v in POSTGRES_PASSWORD GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET BRIDGE_SHARED_SECRET MEILI_MASTER_KEY OPENWEBUI_SECRET_KEY OAUTH_ISSUER_URL OAUTH_RESOURCE_URL CORS_ALLOWED_ORIGIN_REGEX; do grep -q "^$v=" .env.example || exit 1; done</automated>
  </verify>
  <done>Root .env.example is slim, grouped, fully documented, neutral, and exposes every previously-hidden var — including the two boot-fatal OAuth vars and the CORS regex.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the vestigial infrastructure/.env.example + neutralize the per-service templates</name>
  <files>infrastructure/.env.example, apps/memory-api/.env.example, apps/librechat-bridge/.env.example, apps/openwebui-pipeline/.env.example</files>
  <read_first>
    - infrastructure/.env.example (its own header says "Canonical env template lives at REPO ROOT" — vestigial; check whether it holds any Phase-9 note not present in the root file)
    - apps/memory-api/.env.example, apps/librechat-bridge/.env.example, apps/openwebui-pipeline/.env.example (per-service subsets — grep each for grooveos)
  </read_first>
  <action>
    1. Before deleting `infrastructure/.env.example`, diff its var/comment set against the root file.
       If it carries anything the root lacks (e.g. a Phase-9 `SESSION_BRIDGE_LOG_LEVEL` note), FOLD that
       line into the root `.env.example` Optional/SaaS section FIRST. Then `git rm infrastructure/.env.example`.
    2. In each per-service `.env.example` (memory-api, librechat-bridge, openwebui-pipeline):
       - Replace any `grooveos.app` with `example.com`; neutralize any brand default.
       - memory-api's template must also gain `OAUTH_ISSUER_URL=` / `OAUTH_RESOURCE_URL=` lines (it runs
         standalone in local dev, and its Settings now fail-fast when they are empty — a dev copying this
         template without them gets an unexplained crash).
       - KEEP `BRIDGE_DEFAULT_TEAM_SCOPE` / `PIPELINE_DEFAULT_TEAM_SCOPE` as-is (`default` is neutral, D-04).
       - Add a one-line header to each: "Per-service subset for standalone local dev. The canonical
         template is the repo-root `.env.example`."
  </action>
  <acceptance_criteria>
    - `test ! -f infrastructure/.env.example` (deleted).
    - `grep -rl 'grooveos' apps/memory-api/.env.example apps/librechat-bridge/.env.example apps/openwebui-pipeline/.env.example` returns nothing.
    - `grep -Ec '^OAUTH_(ISSUER|RESOURCE)_URL=' apps/memory-api/.env.example` returns 2 (standalone dev won't hit an unexplained fail-fast).
    - `grep -c 'BRIDGE_DEFAULT_TEAM_SCOPE' apps/librechat-bridge/.env.example` returns >= 1 (neutral `default` retained per D-04).
    - Each per-service file references the root template: `grep -lc 'repo-root' apps/*/.env.example | wc -l` returns 3.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f infrastructure/.env.example && test -z "$(grep -rl 'grooveos' apps/memory-api/.env.example apps/librechat-bridge/.env.example apps/openwebui-pipeline/.env.example)" && test "$(grep -Ec '^OAUTH_(ISSUER|RESOURCE)_URL=' apps/memory-api/.env.example)" = "2"</automated>
  </verify>
  <done>Only the canonical root .env.example remains as the fill target; the per-service templates are brand-free and won't fail-fast unexplained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary — this plan edits env-template documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Information Disclosure | A real secret shipped in a placeholder value in `.env.example` | mitigate | Every value is `__FILL__` / a neutral placeholder; acceptance greps confirm no real domain and no brand; no secret literals committed. |
| T-14-14 | Spoofing | Operator leaves OAUTH_ISSUER_URL blank and gets a silently-wrong connector | mitigate | Documented as `[required]` with the consequence in the section header ("CRASH AT BOOT"); reinforced by 14-01's fail-fast validator and 14-03b's `make env-check`. |
| T-14-24 | Elevation of Privilege | An operator copy-pastes `CORS_ALLOWED_ORIGIN_REGEX=.*` to "make CORS work" | mitigate | The var's comment explicitly warns against `.*` and explains the credentialed-request risk; the shipped placeholder is the narrow default. |
</threat_model>

<verification>
- Root .env.example: 0 brand strings, 6 concern sections, OAuth section states the boot-crash consequence, CORS warning present, all 11 new vars + the 2 previously-hidden vars documented.
- Every `make env-check` required var has a line in `.env.example`.
- infrastructure/.env.example deleted (after folding anything unique into the root); the 3 per-service templates are brand-free.
</verification>

<success_criteria>
- An operator fills the root .env.example and boots pointed at their own domain without reading source (PORT-02, ROADMAP SC#3).
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-04-SUMMARY.md`.
</output>
