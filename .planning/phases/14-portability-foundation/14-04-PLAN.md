---
phase: 14-portability-foundation
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-03]
files_modified:
  - .env.example
  - infrastructure/.env.example (deleted)
  - apps/memory-api/.env.example
  - apps/librechat-bridge/.env.example
  - apps/openwebui-pipeline/.env.example
autonomous: true
requirements: [PORT-02]
must_haves:
  truths:
    - "An operator can fill the root .env.example and boot a working install pointed at their own domain WITHOUT opening any source file"
    - "Every var is documented with a one-line comment and a safe neutral placeholder, grouped by concern, marked required-for-boot vs optional"
    - "The previously-undocumented vars (OAUTH_ISSUER_URL/OAUTH_RESOURCE_URL, APP_PUBLIC_URL, XBRAIN_BASE_DOMAIN, MINIO_*, QDRANT_COLLECTION) now appear so operators discover them without reading config.py"
    - "The two OAuth identity vars are clearly flagged REQUIRED (fail-fast at boot if empty)"
    - "The vestigial infrastructure/.env.example is deleted; per-service .env.example files carry no brand"
  artifacts:
    - path: ".env.example"
      provides: "Slim, grouped, documented OSS env template with neutral placeholders"
      contains: "XBRAIN_BASE_DOMAIN"
  key_links:
    - from: ".env.example"
      to: "apps/memory-api/app/config.py + infrastructure/docker-compose.yml"
      via: "documents every var those two consume (no hidden hardcoded default)"
      pattern: "OAUTH_ISSUER_URL|APP_PUBLIC_URL|XBRAIN_BASE_DOMAIN"
---

<objective>
Deliver PORT-02: a slim, documented OSS `.env.example` an operator fills without reading source.
Reorganize the current 121-var root `.env.example` into the D-06 concern groups, add the vars
that today have NO template line (their hidden code defaults pointed at grooveos.app), neutralize
every brand placeholder, mark required-vs-optional, delete the vestigial
`infrastructure/.env.example`, and neutralize the three per-service `.env.example` files.

Purpose: "fill a single slim documented .env.example and get a working install pointed at your
own domain and default team, without opening any source file" (Success Criterion #3).
Output: an operator-fillable env template surface — the PORT-02 deliverable.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md
@.planning/phases/14-portability-foundation/14-01-PLAN.md
@.planning/phases/14-portability-foundation/14-03-PLAN.md

<interfaces>
<!-- The complete NEW/changed var set to document (from 14-01 + 14-03):
     APP_PUBLIC_URL (new, 14-01) ; OAUTH_ISSUER_URL, OAUTH_RESOURCE_URL (empty+REQUIRED) ;
     XBRAIN_BASE_DOMAIN (new, nginx) ; LIBRECHAT_ALLOWED_DOMAINS, BRIDGE_BASE_URL, APP_TEAMS_URL,
     LIBRECHAT_MEMORY_API_BASE, CENTRIFUGO_ALLOWED_ORIGINS, WEBUI_URL (14-03) ;
     previously-undocumented hidden defaults: MINIO_URL/ACCESS_KEY/SECRET_KEY/BUCKET, QDRANT_COLLECTION,
     GITHUB_FALLBACK_TOKEN, AGENT_RUNTIME_INTERNAL_URL, GITHUB_CATALOG_* (14-RESEARCH.md ".env.example gap"). -->
<!-- Existing placeholder conventions in root .env.example to reuse: __VM_HOST__, __FILL__ (D-06). -->
<!-- Makefile env-check minimal-var list (starting point for "required for boot"):
     POSTGRES_PASSWORD GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET BRIDGE_SHARED_SECRET MEILI_MASTER_KEY OPENWEBUI_SECRET_KEY -->
<!-- D-06 proposed grouping: Required-boot / LLM keys (>=1) / Domain+public URLs / OAuth identity (required for connector, fail-fast) / Optional integrations / SaaS-only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root .env.example — slim, grouped, documented, neutral</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (full current file — 324 lines; the grooveos.app + __VM_HOST__/__FILL__ lines)
    - .planning/phases/14-portability-foundation/14-RESEARCH.md ".env.example — Current State and Proposal" (the exact grouping + gap list)
    - infrastructure/docker-compose.yml (source of truth for which vars/defaults exist — do NOT invent vars not consumed anywhere)
  </read_first>
  <action>
    Rewrite `.env.example` into these clearly-headered sections (D-06), one-line comment per var,
    neutral placeholder values, and a `# [required]` / `# [optional]` tag per var:
      1. `# === Required — minimal boot ===` : POSTGRES_PASSWORD, DATABASE_URL, BRIDGE_SHARED_SECRET,
         JWT_ALGORITHM, QDRANT_URL, CENTRIFUGO_TOKEN_HMAC_SECRET, CENTRIFUGO_API_KEY, MEILI_MASTER_KEY,
         OPENWEBUI_SECRET_KEY.
      2. `# === LLM provider keys (at least one required) ===` : ANTHROPIC_API_KEY, OPENAI_API_KEY, XAI_API_KEY.
      3. `# === Domain / public URLs (neutral defaults ship working; set for real deploys) ===` :
         XBRAIN_BASE_DOMAIN (one var points every nginx vhost — document as THE domain knob),
         MEMORY_API_EXTERNAL_URL, APP_PUBLIC_URL, CENTRIFUGO_WS_URL_PUBLIC, SMTP_FROM,
         WEBUI_URL, LIBRECHAT_ALLOWED_DOMAINS, BRIDGE_BASE_URL, APP_TEAMS_URL, LIBRECHAT_MEMORY_API_BASE,
         CENTRIFUGO_ALLOWED_ORIGINS.
      4. `# === OAuth identity (REQUIRED for the ChatGPT/Claude.ai connector — empty = fail-fast at boot) ===` :
         OAUTH_ISSUER_URL, OAUTH_RESOURCE_URL (both marked REQUIRED with the fail-fast note), GOOGLE_CLIENT_ID,
         GOOGLE_CLIENT_SECRET.
      5. `# === Optional integrations (safe to leave blank — feature no-ops) ===` : GITHUB_APP_*,
         GITHUB_FALLBACK_TOKEN, SMTP_HOST/PORT/USER/PASSWORD, WAITLIST_*, GRANOLA_*, GOOGLE_CALENDAR/DRIVE_*,
         RESEND_API_KEY, MINIO_URL/ACCESS_KEY/SECRET_KEY/BUCKET, QDRANT_COLLECTION, AGENT_RUNTIME_INTERNAL_URL,
         GITHUB_CATALOG_*, NEO4J_*.
      6. `# === SaaS-only / not part of OSS-light ===` : LANGFUSE_*, GCS_BACKUP_BUCKET, VM_HOST/VM_USER/SSH_KEY.
    Replace ALL `grooveos.app` occurrences with neutral placeholders (`example.com` / `${XBRAIN_BASE_DOMAIN}`-style
    guidance / `__FILL__`); strip the "GrooveOS" display name from WAITLIST_FROM. Keep the file operator-friendly:
    a short header explaining "copy to .env, fill [required] vars, the rest have safe defaults." Trim vars that
    are not consumed by any service. This is the slim OSS surface, not the full SaaS surface.
  </action>
  <acceptance_criteria>
    - `grep -c 'grooveos.app' .env.example` returns 0; `grep -c 'GrooveOS' .env.example` returns 0.
    - `grep -c 'XBRAIN_BASE_DOMAIN' .env.example` returns >= 1; `grep -c 'APP_PUBLIC_URL' .env.example` returns >= 1.
    - `grep -Ec '^OAUTH_(ISSUER|RESOURCE)_URL' .env.example` returns 2 and the section header contains "REQUIRED"/"fail-fast".
    - `grep -Ec '^(MINIO_URL|QDRANT_COLLECTION)' .env.example` returns 2 (previously-hidden vars now present).
    - Every one of the 6 section headers (`# === ... ===`) is present: `grep -c '# === ' .env.example` returns >= 6.
  </acceptance_criteria>
  <verify>grep the patterns above; confirm 0 brand, new vars present, 6 section headers, OAuth REQUIRED note.</verify>
  <done>Root .env.example is slim, grouped, fully documented, neutral, and exposes every previously-hidden var.</done>
</task>

<task type="auto">
  <name>Task 2: Delete vestigial infrastructure/.env.example + neutralize per-service templates</name>
  <files>infrastructure/.env.example, apps/memory-api/.env.example, apps/librechat-bridge/.env.example, apps/openwebui-pipeline/.env.example</files>
  <read_first>
    - infrastructure/.env.example (its own header says "Canonical env template lives at REPO ROOT" — vestigial)
    - apps/memory-api/.env.example, apps/librechat-bridge/.env.example, apps/openwebui-pipeline/.env.example (per-service subsets; grep each for grooveos.app)
  </read_first>
  <action>
    1. Delete `infrastructure/.env.example` (superseded/misleading per 14-RESEARCH.md — it only duplicates a
       Phase-9 section and self-documents as non-canonical). If the root .env.example lost the Phase-9
       SESSION_BRIDGE_LOG_LEVEL / note, fold that one comment into the root file's Optional/SaaS section first.
    2. In each per-service `.env.example` (memory-api, librechat-bridge, openwebui-pipeline): replace any
       `grooveos.app` with `example.com` and any brand default with a neutral one. Keep `default` team_scope
       lines (BRIDGE_DEFAULT_TEAM_SCOPE / PIPELINE_DEFAULT_TEAM_SCOPE) as-is (neutral per D-04). Add a
       one-line header pointing to the canonical root `.env.example`.
  </action>
  <acceptance_criteria>
    - `test ! -f infrastructure/.env.example` (deleted).
    - `grep -rc 'grooveos.app' apps/memory-api/.env.example apps/librechat-bridge/.env.example apps/openwebui-pipeline/.env.example` returns 0 for each.
    - `grep -c 'BRIDGE_DEFAULT_TEAM_SCOPE' apps/librechat-bridge/.env.example` returns >= 1 (neutral `default` retained per D-04).
  </acceptance_criteria>
  <verify>Confirm infrastructure/.env.example gone; grep per-service files for grooveos.app (0).</verify>
  <done>Only the canonical root .env.example remains as the fill target; per-service templates are brand-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary — this plan edits env-template documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Information Disclosure | A real secret accidentally shipped in a placeholder in .env.example | mitigate | All values are `__FILL__` / neutral placeholders; acceptance greps confirm no real domain; no secret literals committed. |
| T-14-14 | Spoofing | Operator leaves OAUTH_ISSUER_URL blank and gets a silently-wrong connector | mitigate | Documented REQUIRED + the 14-01 fail-fast validator makes an empty value crash boot loudly (defense reinforced by docs). |
</threat_model>

<verification>
- Root .env.example: 0 brand strings, 6 concern sections, OAuth REQUIRED flag, previously-hidden vars present.
- infrastructure/.env.example deleted; per-service templates brand-free.
</verification>

<success_criteria>
- An operator fills the root .env.example and boots pointed at their own domain without reading source (PORT-02, SC#3).
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-04-SUMMARY.md`.
</output>
