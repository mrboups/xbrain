---
phase: 14-portability-foundation
plan: 06
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04]
files_modified:
  - infrastructure/scripts/verify-phase5.sh
  - infrastructure/scripts/verify-phase7.sh
  - infrastructure/scripts/verify-phase8.sh
  - infrastructure/scripts/verify-phase9.sh
  - infrastructure/scripts/verify-phase10.sh
  - infrastructure/scripts/verify-phase12.sh
  - infrastructure/scripts/verify-phase13.sh
  - infrastructure/scripts/brain-index.sh
  - infrastructure/scripts/verify-phase14.sh (new)
autonomous: true
requirements: [PORT-01, PORT-02]
must_haves:
  truths:
    - "Every verify-phaseN.sh (1-13) that hardcoded a grooveos.app domain fallback now uses a neutral fallback while keeping its env override — so `MEMAPI_HOST=https://api.grooveos.app ... bash verify-phaseN.sh` still reproduces prod (SC#2 regression safety)"
    - "verify-phase14.sh asserts PORT-01 (zero grooveos.app/aibrussels in runtime source outside documented exceptions) and PORT-02 (fresh .env.example boots memory-api config pointed at acme.example with no source edit)"
    - "verify-phase14.sh asserts the OAuth fail-fast (empty OAUTH_ISSUER_URL crashes Settings construction) and the nginx template renders acme.example server_names"
    - "verify-phase14.sh is SKIP-aware and exits 0 when FAIL==0"
  artifacts:
    - path: "infrastructure/scripts/verify-phase14.sh"
      provides: "PORT-01/PORT-02 acceptance gate"
      contains: "PASS"
  key_links:
    - from: "infrastructure/scripts/verify-phase14.sh"
      to: "apps/memory-api/app/config.py + .env.example + infrastructure/nginx/templates"
      via: "greps runtime source + loads Settings with acme env + renders a template"
      pattern: "acme"
---

<objective>
Close the regression-safety hard gate (Success Criterion #2) and produce the PORT-01/PORT-02
acceptance verifier. Audit the verify/infra scripts that bake a `grooveos.app` domain fallback,
neutralize the fallback value while preserving the env-override (so prod values reproduce prod),
and add `verify-phase14.sh` that proves: zero brand strings remain in runtime source (outside the
documented exceptions), a fresh `.env.example` boots pointed at a fictitious `acme.example`/`acme`
with no source edit, the OAuth fail-fast fires on empty, and the nginx templates render an
operator domain.

Purpose: the phase is only "done" when de-hardcoding is provable AND prod behavior is reproducible
by setting the vars back to prod values.
Output: parameterized verify scripts + a green `verify-phase14.sh`.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md
@.planning/phases/14-portability-foundation/14-01-PLAN.md
@.planning/phases/14-portability-foundation/14-03-PLAN.md
@.planning/phases/14-portability-foundation/14-04-PLAN.md

<interfaces>
<!-- 14-RESEARCH.md "Regression Safety — verify-phase*.sh Inventory":
     Scripts with a grooveos.app domain FALLBACK (env-overridable — only the fallback value changes):
       verify-phase5.sh  (${MEMORY_API_BASE:-https://api.grooveos.app} / ${APP_SITE_BASE:-https://grooveos.app})
       verify-phase7.sh, verify-phase8.sh, verify-phase10.sh, verify-phase12.sh  (${MEMAPI_HOST:-https://api.grooveos.app})
       verify-phase9.sh  (${BRIDGE_HOST:-bridge.grooveos.app})
       verify-phase13.sh (${MEMAPI_HOST:-...} / ${LIBRECHAT_HOST:-https://chat.grooveos.app} / ${TEST_TEAM_SCOPE:-dejavudev})
       brain-index.sh    (${MEMORY_API_URL:-https://api.grooveos.app})
     DO NOT touch the `'default'` team_scope literals in verify-phase{2,3,4,11}.sh, clean-start.sh,
     register-mcp-tools.sh (neutral, kept per D-04). Pattern to keep: `${VAR:-<neutral>}`. -->
<!-- verify-phaseN.sh house style: `#!/usr/bin/env bash` + `set -uo pipefail`, PASS/FAIL/SKIP counters,
     exit 0 when FAIL==0 (SKIP never fails). Mirror verify-phase13.sh. -->
<!-- Pitfall 2 (14-RESEARCH.md): a bare grep still returns test-fixture/README/few-shot hits — the PORT-01
     grep MUST use explicit excludes: apps/**/tests, apps/spike-mem0/test_data.json, chatgpt-actions.json,
     README.md, and the phase-14 planning dir. After this phase the FUNCTIONAL runtime source is clean. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize verify/infra scripts — neutral domain fallbacks, prod override preserved</name>
  <files>infrastructure/scripts/verify-phase5.sh, verify-phase7.sh, verify-phase8.sh, verify-phase9.sh, verify-phase10.sh, verify-phase12.sh, verify-phase13.sh, brain-index.sh</files>
  <read_first>
    - infrastructure/scripts/verify-phase13.sh (the ${VAR:-default} override pattern + TEST_TEAM_SCOPE)
    - infrastructure/scripts/verify-phase5.sh (MEMORY_API_BASE / APP_SITE_BASE fallbacks)
    - 14-RESEARCH.md "Regression Safety" table (exact fallback var names per script)
  </read_first>
  <action>
    For each script, change ONLY the fallback VALUE inside the existing `${VAR:-...}` expansion from a
    grooveos.app URL to a neutral one, preserving the env-var override name unchanged:
      - `${MEMAPI_HOST:-https://api.grooveos.app}`     → `${MEMAPI_HOST:-http://localhost:8000}`
      - `${MEMORY_API_BASE:-https://api.grooveos.app}` → `${MEMORY_API_BASE:-http://localhost:8000}`
      - `${APP_SITE_BASE:-https://grooveos.app}`       → `${APP_SITE_BASE:-http://localhost:8000}`
      - `${BRIDGE_HOST:-bridge.grooveos.app}`          → `${BRIDGE_HOST:-localhost}`
      - `${LIBRECHAT_HOST:-https://chat.grooveos.app}` → `${LIBRECHAT_HOST:-http://localhost:8080}`
      - `${MEMORY_API_URL:-https://api.grooveos.app}`  (brain-index.sh) → `${MEMORY_API_URL:-http://localhost:8000}`
      - `${TEST_TEAM_SCOPE:-dejavudev}` (verify-phase13.sh) → `${TEST_TEAM_SCOPE:-default}` (neutral team, D-04)
    Also scrub any informational comment referencing `grooveos.app/account/teams/` to a relative path.
    Because the override name is unchanged, `MEMAPI_HOST=https://api.grooveos.app TEST_TEAM_SCOPE=aibrussels
    bash verify-phaseN.sh` still reproduces the current prod assertion set (SC#2). Do NOT modify
    verify-phase{2,3,4,11}.sh, clean-start.sh, register-mcp-tools.sh (their `'default'` literals stay per D-04).
  </action>
  <acceptance_criteria>
    - `grep -rc 'grooveos.app' infrastructure/scripts/verify-phase5.sh infrastructure/scripts/verify-phase7.sh infrastructure/scripts/verify-phase8.sh infrastructure/scripts/verify-phase9.sh infrastructure/scripts/verify-phase10.sh infrastructure/scripts/verify-phase12.sh infrastructure/scripts/verify-phase13.sh infrastructure/scripts/brain-index.sh` returns 0 for every file.
    - Override preserved: `grep -c 'MEMAPI_HOST:-' infrastructure/scripts/verify-phase13.sh` returns >= 1 (env var name unchanged).
    - `for f in verify-phase5 verify-phase7 verify-phase8 verify-phase9 verify-phase10 verify-phase12 verify-phase13; do bash -n infrastructure/scripts/$f.sh || echo FAIL; done` prints no FAIL (all parse).
  </acceptance_criteria>
  <verify>grep the 8 scripts for grooveos.app (0 each) + `bash -n` each (parses) + confirm override var names intact.</verify>
  <done>Verify/infra scripts carry neutral fallbacks; prod values still reproduce prod via unchanged env overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Author verify-phase14.sh — PORT-01 + PORT-02 acceptance gate</name>
  <files>infrastructure/scripts/verify-phase14.sh</files>
  <read_first>
    - infrastructure/scripts/verify-phase13.sh (house style: set -uo pipefail, PASS/FAIL/SKIP counters, exit logic)
    - apps/memory-api/app/config.py (the fail-fast validator + APP_PUBLIC_URL — assertion targets)
    - .env.example (the fill target for the fresh-boot test)
    - infrastructure/nginx/templates/20-api.conf.template (server_name ${XBRAIN_BASE_DOMAIN} — envsubst render target)
  </read_first>
  <action>
    Create `infrastructure/scripts/verify-phase14.sh` (`#!/usr/bin/env bash`, `set -uo pipefail`,
    PASS/FAIL/SKIP counters mirroring verify-phase13.sh, exit 0 iff FAIL==0). Tests:
      (a) PORT-01 runtime-source clean: grep `apps/*/app` + `infrastructure/{docker-compose.yml,nginx,librechat,centrifugo}`
          for `grooveos.app` and `aibrussels` with EXPLICIT excludes (`--glob '!**/tests/**'`,
          `!**/spike-mem0/**`, `!**/chatgpt-actions.json`, `!**/knowledge/**` NOT excluded — KB was scrubbed).
          PASS when the count is 0. (Use `grep -r` with `--include`/prune, or `rg` if available.)
      (b) PORT-01 OAuth fail-fast: `cd apps/memory-api && OAUTH_ISSUER_URL= OAUTH_RESOURCE_URL= DATABASE_URL=x
          BRIDGE_SHARED_SECRET=x python -c "from app.config import Settings; Settings()"` — PASS when it exits
          NON-zero. Same for `cd apps/mcp-brain` with empty OAuth vars → non-zero.
      (c) PORT-02 fresh-boot at a fictitious domain: construct an env pointing at `acme.example`/`acme`
          (`MEMORY_API_EXTERNAL_URL=https://api.acme.example APP_PUBLIC_URL=https://acme.example
          OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp
          DATABASE_URL=x BRIDGE_SHARED_SECRET=x`) and assert
          `python -c "from app.config import Settings; s=Settings(); assert 'grooveos' not in (s.MEMORY_API_EXTERNAL_URL+s.OAUTH_ISSUER_URL+s.APP_PUBLIC_URL)"` exits 0 (no source edit needed).
      (d) PORT-01 nginx templating: `XBRAIN_BASE_DOMAIN=acme.example envsubst '$XBRAIN_BASE_DOMAIN' <
          infrastructure/nginx/templates/20-api.conf.template | grep -q 'server_name api.acme.example;'` → PASS.
      (e) PORT-02 .env.example is brand-free + documents required OAuth: `grep -c grooveos.app .env.example`==0
          AND the OAuth section marks REQUIRED — PASS.
      (f) SOFT (SKIP if 14-05 not yet run): `.planning` outside the phase-14 dir + design doc has 0 grooveos.app.
    Print a `PASS: N / M` summary line. Document usage in the header (env overrides to reproduce prod:
    `MEMAPI_HOST=... TEST_TEAM_SCOPE=aibrussels`).
  </action>
  <acceptance_criteria>
    - `bash -n infrastructure/scripts/verify-phase14.sh` exits 0 (parses).
    - `bash infrastructure/scripts/verify-phase14.sh` runs and prints a `PASS:` summary line; exit code is 0 (FAIL==0) once 14-01..14-04 are merged.
    - The script contains all 6 lettered checks (grep the file: `grep -Ec '\(a\)|\(b\)|\(c\)|\(d\)|\(e\)|\(f\)' infrastructure/scripts/verify-phase14.sh` returns >= 6).
    - `grep -c 'acme.example' infrastructure/scripts/verify-phase14.sh` returns >= 2 (fresh-domain + nginx render).
  </acceptance_criteria>
  <verify>`bash -n` parses; run the script → exit 0 with a PASS summary; grep confirms the 6 checks + acme assertions.</verify>
  <done>verify-phase14.sh green: runtime source clean, OAuth fail-fast proven, fresh acme.example boot works, nginx templates render, .env.example brand-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary — verification tooling. It EXERCISES the 14-01 OAuth fail-fast boundary as a test.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-15 | Repudiation | A naive PORT-01 grep passes falsely (misses hits) or fails falsely (test fixtures) | mitigate | Explicit exclude globs (14-RESEARCH.md Pitfall 2); check (a) scopes to functional runtime source only. |
| T-14-16 | Elevation of Privilege | Regression: a neutralized fallback silently changes prod behavior | mitigate | Override var NAMES unchanged; check documented so `MEMAPI_HOST=<prod> ...` reproduces prod (SC#2); the fail-fast test proves empty-OAuth crashes rather than mis-mints. |
</threat_model>

<verification>
- All 8 edited scripts: 0 grooveos.app, `bash -n` parses, env overrides intact.
- verify-phase14.sh exits 0 with a PASS summary; covers PORT-01 (source clean + fail-fast + nginx render) and PORT-02 (.env.example fresh-boot).
</verification>

<success_criteria>
- Regression-safe: prod values reproduce prod via unchanged verify-script overrides (SC#2).
- PORT-01 + PORT-02 are provable by a single green `verify-phase14.sh` (SC#1, SC#3, SC#4).
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-06-SUMMARY.md`.
</output>
