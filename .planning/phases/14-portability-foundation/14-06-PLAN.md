---
phase: 14-portability-foundation
plan: 06
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03a, 14-03b, 14-04, 14-05]
files_modified:
  - infrastructure/scripts/verify-phase5.sh
  - infrastructure/scripts/verify-phase7.sh
  - infrastructure/scripts/verify-phase8.sh
  - infrastructure/scripts/verify-phase9.sh
  - infrastructure/scripts/verify-phase10.sh
  - infrastructure/scripts/verify-phase12.sh
  - infrastructure/scripts/verify-phase13.sh
  - infrastructure/scripts/brain-index.sh
  - infrastructure/scripts/verify-phase11.sh
  - infrastructure/scripts/test-phase13-cross-frontend.py
  - infrastructure/scripts/verify-phase14.sh (new)
  - infrastructure/scripts/preflight-env.sh (new)
  - Makefile
  - apps/librechat/Dockerfile
  - .github/workflow-templates/
autonomous: false
requirements: [PORT-01, PORT-02]
must_haves:
  truths:
    - "Every verify-phaseN.sh (1-13) that hardcoded a grooveos.app domain fallback now uses a neutral fallback while keeping its env override NAME unchanged — so `MEMAPI_HOST=https://api.grooveos.app TEST_TEAM_SCOPE=aibrussels bash verify-phaseN.sh` still reproduces prod (SC#2)"
    - "verify-phase14.sh asserts PORT-01 by grepping for `grooveos` (NOT `grooveos.app` — the CORS literal is the escaped regex `grooveos\\.app`, which a dotted pattern misses) and `aibrussels`, with the documented exclude set"
    - "verify-phase14.sh asserts the OAuth fail-fast (empty OAUTH_ISSUER_URL makes Settings() raise), the env-driven CORS regex, a fresh acme.example boot with no source edit, and the nginx template render"
    - "preflight-env.sh HARD-FAILS if the target .env lacks OAUTH_ISSUER_URL / OAUTH_RESOURCE_URL / CORS_ALLOWED_ORIGIN_REGEX — because 14-03b removes the compose fallback that prod silently relies on today (crashloop guard, B3)"
    - "The live regression suite (verify-phase1..13 against a running deployment) is a DEFERRED gate — the prod VM is TERMINATED, so it cannot run now. It is recorded verbatim in 14-06-SUMMARY.md and MUST PASS at the next real deploy (SC#2b)"
  artifacts:
    - path: "infrastructure/scripts/verify-phase14.sh"
      provides: "PORT-01/PORT-02 acceptance gate"
      contains: "PASS"
    - path: "infrastructure/scripts/preflight-env.sh"
      provides: "pre-deploy crashloop guard (B3)"
      contains: "OAUTH_ISSUER_URL"
  key_links:
    - from: "infrastructure/scripts/verify-phase14.sh"
      to: "apps/memory-api/app/config.py + .env.example + infrastructure/nginx/templates"
      via: "greps runtime source + loads Settings with acme env + renders a template"
      pattern: "acme"
    - from: "infrastructure/scripts/preflight-env.sh"
      to: "infrastructure/docker-compose.yml (OAuth fallback removed by 14-03b)"
      via: "asserts the .env supplies what the compose fallback no longer does"
      pattern: "OAUTH_ISSUER_URL"
---

<objective>
Close the regression-safety gate (amended Success Criterion #2) and produce the PORT-01/PORT-02
acceptance verifier.

Three jobs:
1. **Parameterize** the verify/infra scripts that bake a `grooveos.app` fallback — neutralize the
   fallback VALUE while preserving the env-override NAME, so prod values still reproduce prod.
2. **Author `verify-phase14.sh`** — the PORT-01/PORT-02 acceptance gate (source clean, OAuth
   fail-fast, env-driven CORS, fresh `acme.example` boot, nginx render).
3. **Make the phase deploy-safe** — `preflight-env.sh` guards the crashloop that 14-03b would
   otherwise cause at the next deploy, and the live regression suite is recorded as an explicit
   DEFERRED gate (the prod VM is TERMINATED; it cannot run now).

Purpose: the phase is only "done" when de-hardcoding is PROVABLE, prod behavior is REPRODUCIBLE by
setting the vars back, and the next deploy cannot silently crashloop.
Output: parameterized verify scripts + a green `verify-phase14.sh` + a preflight guard + a recorded
deferred gate.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-portability-foundation/14-CONTEXT.md
@.planning/phases/14-portability-foundation/14-RESEARCH.md
@.planning/phases/14-portability-foundation/14-01-PLAN.md
@.planning/phases/14-portability-foundation/14-03a-PLAN.md
@.planning/phases/14-portability-foundation/14-03b-PLAN.md
@.planning/phases/14-portability-foundation/14-04-PLAN.md

<interfaces>
<!-- AMENDED ROADMAP (2026-07-12): SC#1 excludes the browser bundles (D-01c, Phase 16) and the
     hosted-product marketing/docs (D-01e). The `"default"` team_scope literal is KEPT (D-04) and is
     NOT a de-hardcoding target. This verifier must NOT assert the browser-bundle surface and must
     NOT require `default` to be env-sourced. -->
<!-- 14-RESEARCH.md "Regression Safety — verify-phase*.sh Inventory":
     Scripts with a grooveos.app domain FALLBACK (env-overridable — only the fallback VALUE changes):
       verify-phase5.sh  (${MEMORY_API_BASE:-https://api.grooveos.app} / ${APP_SITE_BASE:-https://grooveos.app})
       verify-phase7.sh, verify-phase8.sh, verify-phase10.sh, verify-phase12.sh  (${MEMAPI_HOST:-https://api.grooveos.app})
       verify-phase9.sh  (${BRIDGE_HOST:-bridge.grooveos.app})
       verify-phase13.sh (${MEMAPI_HOST:-...} / ${LIBRECHAT_HOST:-https://chat.grooveos.app} / ${TEST_TEAM_SCOPE:-dejavudev})
       brain-index.sh    (${MEMORY_API_URL:-https://api.grooveos.app})
     DO NOT touch the `'default'` team_scope literals in verify-phase{2,3,4,11}.sh, clean-start.sh,
     register-mcp-tools.sh (neutral, kept per D-04). Pattern to keep: `${VAR:-<neutral>}`. -->
<!-- verify-phaseN.sh house style: `#!/usr/bin/env bash` + `set -uo pipefail`, PASS/FAIL/SKIP counters,
     exit 0 when FAIL==0 (SKIP never fails). Mirror verify-phase13.sh. -->
<!-- W5 (plan-checker): the CORS literal in main.py is the ESCAPED regex `grooveos\.app`. A grep for
     `grooveos.app` DOES NOT MATCH IT (the `.` is escaped). The PORT-01 grep MUST search for the bare
     token `grooveos`. This is the difference between a real gate and a green light on a live leak. -->
<!-- B3 (plan-checker): OAUTH_ISSUER_URL / OAUTH_RESOURCE_URL are NOT in today's .env.example, so the
     VM .env almost certainly does not define them — prod boots off the compose fallback
     (`${OAUTH_ISSUER_URL:-https://api.grooveos.app}`, docker-compose.yml:185-186 & :845-846), which
     14-03b removes. 14-01 makes an empty value FATAL at import. Deploying without first populating
     .env => memory-api AND mcp-brain crashloop. Project memory `project_xbrain_vm_env_gotchas`
     confirms VM .env vars go missing. preflight-env.sh is the guard. -->
<!-- B4 / amended SC#2b: the prod VM is TERMINATED (cost pause, Prime pivot). verify-phase1..13 curl a
     LIVE deployment => they CANNOT run in this phase. Do not attempt. Record them as a DEFERRED gate. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize verify/infra scripts — neutral domain fallbacks, prod override preserved</name>
  <files>infrastructure/scripts/verify-phase5.sh, verify-phase7.sh, verify-phase8.sh, verify-phase9.sh, verify-phase10.sh, verify-phase11.sh, verify-phase12.sh, verify-phase13.sh, brain-index.sh, test-phase13-cross-frontend.py</files>
  <!-- BLOCKER-3 (plan-checker): verify-phase11.sh:46,82 and test-phase13-cross-frontend.py:25,88 BOTH
       carry `${MEMAPI_HOST:-https://api.grooveos.app}` and were owned by NO plan. The earlier
       "DO NOT touch verify-phase{2,3,4,11}.sh" carve-out was about their `'default'` team_scope
       literals (D-04) ONLY — it does NOT extend to a brand domain fallback. Neutralize the DOMAIN in
       these two; still do NOT touch their `'default'` team_scope literals. -->
  <read_first>
    - infrastructure/scripts/verify-phase13.sh (the ${VAR:-default} override pattern + TEST_TEAM_SCOPE)
    - infrastructure/scripts/verify-phase5.sh (MEMORY_API_BASE / APP_SITE_BASE fallbacks)
    - 14-RESEARCH.md "Regression Safety" table (exact fallback var names per script)
  </read_first>
  <action>
    For each script, change ONLY the fallback VALUE inside the existing `${VAR:-...}` expansion from a
    grooveos.app URL to a neutral one, preserving the env-var override NAME unchanged:
      - `${MEMAPI_HOST:-https://api.grooveos.app}`     -> `${MEMAPI_HOST:-http://localhost:8000}`
      - `${MEMORY_API_BASE:-https://api.grooveos.app}` -> `${MEMORY_API_BASE:-http://localhost:8000}`
      - `${APP_SITE_BASE:-https://grooveos.app}`       -> `${APP_SITE_BASE:-http://localhost:8000}`
      - `${BRIDGE_HOST:-bridge.grooveos.app}`          -> `${BRIDGE_HOST:-localhost}`
      - `${LIBRECHAT_HOST:-https://chat.grooveos.app}` -> `${LIBRECHAT_HOST:-http://localhost:8080}`
      - `${MEMORY_API_URL:-https://api.grooveos.app}`  (brain-index.sh) -> `${MEMORY_API_URL:-http://localhost:8000}`
      - `${TEST_TEAM_SCOPE:-dejavudev}` (verify-phase13.sh) -> `${TEST_TEAM_SCOPE:-default}` (neutral team, D-04)
    ALSO (BLOCKER-3 — previously unowned):
      - `verify-phase11.sh:46,82`  `${MEMAPI_HOST:-https://api.grooveos.app}` -> `${MEMAPI_HOST:-http://localhost:8000}`
      - `test-phase13-cross-frontend.py:25,88`  same `MEMAPI_HOST` default -> `http://localhost:8000`
        (Python: `os.environ.get("MEMAPI_HOST", "http://localhost:8000")` — keep the env key name.)

    ALSO (BLOCKER-5 — these are now inside check (a)'s widened scan set, so they MUST be clean or the
    gate fails; they were previously unowned by any plan):
      - `Makefile` — replace any `grooveos` literal with the env var / a neutral value. (Coordinate with
        14-03b Task 3, which already added the required-var list; do not duplicate — see Task 3 note.)
      - `apps/librechat/Dockerfile` — replace any hardcoded brand domain with a build ARG or a neutral value.
      - `.github/workflow-templates/*.yml` — these templates are COPIED INTO OPERATORS' OWN REPOS, so a
        hardcoded `api.grooveos.app` would point every self-hoster's CI at xbrain's production API.
        Replace with a repo-level variable/secret (e.g. `${{ vars.MEMORY_API_URL }}`) and document it in
        the template's comment header.
    NOT in scope here (EXEMPT per D-01e EXTENDED — hosted-product surfaces, see check (a)):
      `projects-dashboard/**` and `.github/workflows/deploy-dashboard.yml`.
    Also scrub any informational comment referencing `grooveos.app/account/teams/` to a relative path.
    Because the override NAME is unchanged, `MEMAPI_HOST=<prod-api-url> TEST_TEAM_SCOPE=<prod-team>
    bash verify-phaseN.sh` still reproduces the current prod assertion set (SC#2a) — the prod VALUES are
    recorded ONLY in 14-06-SUMMARY.md, never in a shipped script (BLOCKER-4).
    Do NOT modify clean-start.sh, register-mcp-tools.sh, or the `'default'` team_scope literals in
    verify-phase{2,3,4,11}.sh — those stay per D-04. (verify-phase11.sh's DOMAIN is in scope; its
    `'default'` literal is NOT.)
  </action>
  <acceptance_criteria>
    - `grep -c 'grooveos' infrastructure/scripts/verify-phase5.sh infrastructure/scripts/verify-phase7.sh infrastructure/scripts/verify-phase8.sh infrastructure/scripts/verify-phase9.sh infrastructure/scripts/verify-phase10.sh infrastructure/scripts/verify-phase11.sh infrastructure/scripts/verify-phase12.sh infrastructure/scripts/verify-phase13.sh infrastructure/scripts/brain-index.sh infrastructure/scripts/test-phase13-cross-frontend.py` returns 0 for every file (bare token `grooveos`, not `grooveos.app`).
    - Override preserved: `grep -c 'MEMAPI_HOST:-' infrastructure/scripts/verify-phase13.sh` returns >= 1 (env var NAME unchanged).
    - `for f in verify-phase5 verify-phase7 verify-phase8 verify-phase9 verify-phase10 verify-phase12 verify-phase13 brain-index; do bash -n infrastructure/scripts/$f.sh || echo FAIL; done` prints no FAIL (all parse).
    - Untouched-by-design: `grep -c "'default'" infrastructure/scripts/clean-start.sh` is unchanged from its pre-phase value (D-04 — `default` is NOT a de-hardcoding target).
  </acceptance_criteria>
  <verify>grep the 8 scripts for the bare token `grooveos` (0 each) + `bash -n` each (parses) + confirm override var names intact + confirm the D-04 `default` literals were NOT touched.</verify>
  <done>Verify/infra scripts carry neutral fallbacks; prod values still reproduce prod via unchanged env overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Author verify-phase14.sh — PORT-01 + PORT-02 acceptance gate</name>
  <files>infrastructure/scripts/verify-phase14.sh</files>
  <read_first>
    - infrastructure/scripts/verify-phase13.sh (house style: set -uo pipefail, PASS/FAIL/SKIP counters, exit logic)
    - apps/memory-api/app/config.py (the OAuth fail-fast field_validator + APP_PUBLIC_URL + CORS_ALLOWED_ORIGIN_REGEX — assertion targets from 14-01)
    - apps/memory-api/app/main.py (CORSMiddleware must now read settings.CORS_ALLOWED_ORIGIN_REGEX, not a literal)
    - .env.example (the fill target for the fresh-boot test, from 14-04)
    - infrastructure/nginx/templates/20-api.conf.template (server_name ${XBRAIN_BASE_DOMAIN} — envsubst render target, from 14-03a)
  </read_first>
  <action>
    Create `infrastructure/scripts/verify-phase14.sh` (`#!/usr/bin/env bash`, `set -uo pipefail`,
    PASS/FAIL/SKIP counters mirroring verify-phase13.sh, exit 0 iff FAIL==0). Tests:

      (a) PORT-01 source+infra clean. **SCAN SET must be as wide as the criterion it certifies**
          (plan-checker BLOCKER-5: the previous scan set omitted `infrastructure/scripts/`, `Makefile`
          and `.github/`, so the gate could not see leaks — including ones this very plan was about to
          re-introduce). Scan: `apps/`, `infrastructure/`, `Makefile`, `.github/`, **`CLAUDE.md`**
          (round-4 BLOCKER-3: repo-root technical docs were outside the scan set, so `CLAUDE.md:62`'s
          `team@grooveos.app` would have survived the phase while this gate certified green; 14-05 T2
          scrubs it, this check proves it).
          **Use `grep -I --exclude-dir=__pycache__ --exclude=*.pyc`** (round-4 W-C — VERIFIED: 7 compiled
          `.pyc` files under `apps/*/app/__pycache__/` contain the brand token as a compiled string
          literal RIGHT NOW. Without this, check (a) FAILS on build artifacts, which would push the
          executor to improvise a dangerously broad exclude at runtime).
          Pattern: the BARE TOKEN `grooveos` (NOT `grooveos.app` — the CORS literal is the escaped regex
          `grooveos\.app`, and a dotted pattern silently misses it; the same bare token also catches the
          `@grooveos` mention alias) AND `aibrussels`.

          EXCLUDES — **enumerate them explicitly in the script**, one-for-one with the amended ROADMAP
          SC#1 exemption list. Anything not on this list must be CLEAN:
            - `**/tests/**`, `**/spike-mem0/**`, `**/chatgpt-actions.json`, `**/README.md`
              (test fixtures + illustrative examples -> neutral placeholder, D-01)
            - `chrome-extension/**`, `app-site/**` (deferred to Phase 16, D-01c)
            - `marketing-site/**` (hosted-product surface, D-01e)
            - `projects-dashboard/**` and `.github/workflows/deploy-dashboard.yml` (D-01e EXTENDED —
              this is the hosted-product dashboard deployed at projects.<brand>, the same class as
              app-site/marketing-site: it IS the commercial service's own page, not OSS code an
              operator self-hosts)
            - `.planning/**` (exempt surface — the decision record; scrubbed separately by 14-05)
            - **`infrastructure/scripts/verify-phase14.sh` and `infrastructure/scripts/preflight-env.sh`
              — THE VERIFIER'S OWN PATTERN LITERALS.** This script must contain the strings `grooveos`
              and `aibrussels`: they ARE its grep patterns. Without this exclude, check (a) matches its
              own source and the gate can NEVER exit 0 (self-reference — plan-checker round-3 B1).
              Implement as `--exclude=verify-phase14.sh --exclude=preflight-env.sh` on the grep.
          NOT excluded (must be CLEAN — these are shipped, operator-facing surfaces):
            `apps/memory-api/app/knowledge/**` (KB scrubbed by 14-02),
            `apps/memory-api/app/services/mention_detector.py` (alias config-driven by 14-01 T5),
            `infrastructure/scripts/**` (Task 1 + the debranded preflight/verify14 of Tasks 2-3),
            `Makefile`, `apps/librechat/Dockerfile`, `.github/workflow-templates/**`.
          PASS when the match count is 0.

      (b) PORT-01 OAuth fail-fast fires (14-01's Pydantic field_validator):
          `cd apps/memory-api && OAUTH_ISSUER_URL= OAUTH_RESOURCE_URL= DATABASE_URL=x BRIDGE_SHARED_SECRET=x \
           python -c "from app.config import Settings; Settings()"` — PASS when it exits NON-zero.
          Repeat for `apps/mcp-brain` with empty OAuth vars — PASS when NON-zero.

      (c) PORT-01 CORS is env-driven (B1 — the highest-value functional leak):
          PASS when `grep -q 'allow_origin_regex=settings.CORS_ALLOWED_ORIGIN_REGEX' apps/memory-api/app/main.py`
          AND `grep -c 'grooveos' apps/memory-api/app/main.py` == 0.

      (d) PORT-02 fresh boot at a fictitious domain, no source edit. Construct an env pointing at
          `acme.example`:
            `MEMORY_API_EXTERNAL_URL=https://api.acme.example APP_PUBLIC_URL=https://acme.example
             OAUTH_ISSUER_URL=https://api.acme.example OAUTH_RESOURCE_URL=https://mcp.acme.example/mcp
             CORS_ALLOWED_ORIGIN_REGEX='https://acme\.example' DATABASE_URL=x BRIDGE_SHARED_SECRET=x`
          and assert
            `python -c "from app.config import Settings; s=Settings(); assert 'grooveos' not in (s.MEMORY_API_EXTERNAL_URL+s.OAUTH_ISSUER_URL+s.APP_PUBLIC_URL+s.CORS_ALLOWED_ORIGIN_REGEX)"`
          exits 0.

      (e) PORT-01 nginx templating renders the operator domain (14-03a):
          `XBRAIN_BASE_DOMAIN=acme.example envsubst '$XBRAIN_BASE_DOMAIN' < infrastructure/nginx/templates/20-api.conf.template | grep -q 'server_name api.acme.example;'` -> PASS.

      (f) PORT-02 `.env.example` is brand-free and documents the now-REQUIRED OAuth vars (14-04):
          `grep -c 'grooveos' .env.example` == 0 AND `grep -q '^OAUTH_ISSUER_URL=' .env.example` AND
          `grep -q '^CORS_ALLOWED_ORIGIN_REGEX=' .env.example` -> PASS.

      (g) SOFT (SKIP if 14-05 has not run): `.planning/` outside the phase-14 dir, and `docs/*.md`,
          contain 0 `grooveos`. SKIP, never FAIL — the scrub is non-blocking for boot.

    Print a `PASS: N / M` summary line. In the header, document the env override **VARIABLE NAMES ONLY** —
    `MEMAPI_HOST`, `LIBRECHAT_HOST`, `TEST_TEAM_SCOPE` — and NOT their production values. This script ships
    to self-hosters; hardcoding xbrain's prod domain in the header would re-brand the very directory Task 1
    just scrubbed to zero, and would be a PORT-01 violation created by the plan that certifies PORT-01
    (plan-checker BLOCKER-4). The actual prod values are recorded ONLY in `14-06-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - `bash -n infrastructure/scripts/verify-phase14.sh` exits 0 (parses).
    - `bash infrastructure/scripts/verify-phase14.sh` prints a `PASS:` summary line and exits 0 (FAIL==0) once 14-01..14-05 are merged.
    - The script greps the BARE token (not `grooveos.app` — the escaped CORS regex would be missed): check (a)'s pattern is the bare `grooveos`.
    - **Self-exclusion present (round-3 B1):** `grep -q 'exclude=verify-phase14.sh' infrastructure/scripts/verify-phase14.sh` AND `grep -q 'exclude=preflight-env.sh' infrastructure/scripts/verify-phase14.sh`. Without these the verifier matches its own pattern literals and can never exit 0.
    - **Self-reference smoke test:** run check (a) in isolation on a clean tree — it must report 0 matches, NOT match `verify-phase14.sh` itself.
    - **The self-exclude must NOT become a back door (round-4 W-A).** Because check (a) can no longer see inside `verify-phase14.sh`, a real brand leak could hide there. Gate it explicitly:
      `test "$(grep -c 'grooveos\.app' infrastructure/scripts/verify-phase14.sh)" = "0"` (no brand URL — the bare token `grooveos` is allowed ONLY as the grep pattern) AND `test "$(grep -c 'aibrussels' infrastructure/scripts/verify-phase14.sh)" -le 1` (at most the one pattern literal).
      Define the patterns ONCE at the top of the script (`BRAND_RE='grooveos|aibrussels'`) so there is a single occurrence to audit.
    - **Build-artifact exclude present (round-4 W-C):** `grep -q 'exclude-dir=__pycache__' infrastructure/scripts/verify-phase14.sh`.
    - **CLAUDE.md in scan set (round-4 BLOCKER-3):** `grep -q 'CLAUDE.md' infrastructure/scripts/verify-phase14.sh`.
    - The script contains all 7 lettered checks: `grep -Ec '\(a\)|\(b\)|\(c\)|\(d\)|\(e\)|\(f\)|\(g\)' infrastructure/scripts/verify-phase14.sh` returns >= 7.
    - `grep -c 'acme.example' infrastructure/scripts/verify-phase14.sh` returns >= 2 (fresh-boot + nginx render).
    - `grep -q 'CORS_ALLOWED_ORIGIN_REGEX' infrastructure/scripts/verify-phase14.sh` (check (c) present).
  </acceptance_criteria>
  <verify>`bash -n` parses; run the script -> exit 0 with a PASS summary; grep confirms the 7 checks, the bare-token pattern, the CORS assertion, and the acme assertions.</verify>
  <done>verify-phase14.sh green: runtime source clean (incl. the escaped CORS regex), OAuth fail-fast proven, CORS env-driven, fresh acme.example boot works, nginx renders, .env.example brand-free.</done>
</task>

<task type="auto">
  <name>Task 3: preflight-env.sh + Makefile env-check — the crashloop guard (B3)</name>
  <files>infrastructure/scripts/preflight-env.sh, Makefile</files>
  <read_first>
    - Makefile (the existing `env-check` target + its required-var list — extend, do not rewrite)
    - infrastructure/docker-compose.yml (confirm 14-03b removed the `${OAUTH_ISSUER_URL:-https://api.grooveos.app}` fallbacks at :185-186 and :845-846)
    - apps/memory-api/app/config.py (the field_validator that now makes an empty OAuth URL FATAL)
    - 14-03b-PLAN.md (what it removed — this guard exists because of it)
  </read_first>
  <action>
    Create `infrastructure/scripts/preflight-env.sh` (`#!/usr/bin/env bash`, `set -euo pipefail`). It takes
    an optional path to the target env file (default `.env`) and HARD-FAILS (exit 1) with an explicit,
    actionable message if any of these are missing or empty:
      - `OAUTH_ISSUER_URL`          # empty -> memory-api + mcp-brain crashloop at boot (fail-fast validator)
      - `OAUTH_RESOURCE_URL`        # same
      - `CORS_ALLOWED_ORIGIN_REGEX` # missing -> browser/extension silently CORS-blocked in prod
      - `XBRAIN_BASE_DOMAIN`        # missing -> EVERY nginx vhost renders as *.localhost = TOTAL INGRESS
                                    #   OUTAGE. 14-03a moved all 7 vhosts to envsubst templates whose
                                    #   compose default is `${XBRAIN_BASE_DOMAIN:-localhost}` — the default
                                    #   is a safe LOCAL default and a catastrophic PROD one. (round-3 W-A)
      - `AGENT_MENTION_ALIASES`     # missing -> falls back to `agent`, so @groove/@grooveos SILENTLY stop
                                    #   working in prod. Prod value: agent,grooveos,groove,gr,g (D-08)
    Implementation: for each var, `grep -qE "^${VAR}=.+" "$ENV_FILE"` — missing OR present-but-empty => FAIL.
    The failure message MUST state WHY this is fatal, and MUST be BRAND-FREE — this script SHIPS TO
    SELF-HOSTERS; putting xbrain's production domain in it would re-create the exact leak PORT-01 exists
    to remove (plan-checker BLOCKER-4). Use generic placeholders ONLY:
      "FATAL: <VAR> is not set in <env file>. This deployment removed the docker-compose fallback that
       previously supplied it, and memory-api/mcp-brain now REJECT an empty value at boot (Pydantic
       field_validator). Deploying without this var WILL crashloop both services.
       Set it to your own deployment's value, e.g.
         OAUTH_ISSUER_URL=https://api.yourdomain.example
         OAUTH_RESOURCE_URL=https://mcp.yourdomain.example/mcp
         CORS_ALLOWED_ORIGIN_REGEX=https://app\.yourdomain\.example"
    The ACTUAL xbrain production values live ONLY in `14-06-SUMMARY.md` (an exempt surface) — never in
    a shipped script. Print `PREFLIGHT OK` and exit 0 when all vars are present.

    Then the Makefile — **IDEMPOTENT, read before writing** (W-B): `14-03b` Task 3 (wave 2) ALREADY added
    some of these vars to the `env-check` required-var list and ALREADY set `deploy: env-check sync`.
    Do NOT blindly re-add (that would duplicate loop entries). READ the list first; add ONLY the vars that
    are missing — in particular `XBRAIN_BASE_DOMAIN` and `AGENT_MENTION_ALIASES`, which 14-03b did NOT
    include (round-3 W-A / B2). Then add ONLY the new `preflight` target running
    `bash infrastructure/scripts/preflight-env.sh`, and APPEND it to `deploy`'s existing prerequisite list
    (`deploy: env-check preflight sync`) — do not replace the list.
  </action>
  <acceptance_criteria>
    - `bash -n infrastructure/scripts/preflight-env.sh` exits 0.
    - Negative test: `printf 'FOO=bar\n' > /tmp/t.env && bash infrastructure/scripts/preflight-env.sh /tmp/t.env; echo $?` prints a non-zero exit code AND the output contains `FATAL: OAUTH_ISSUER_URL`.
    - Negative test (the silent-killers): an env file with the 2 OAuth vars + CORS but WITHOUT `XBRAIN_BASE_DOMAIN` exits NON-zero and names it; same for a file missing `AGENT_MENTION_ALIASES`.
    - Positive test: an env file containing all **5** vars with non-empty values exits 0 and prints `PREFLIGHT OK`.
    - The script is BRAND-FREE: `grep -c 'grooveos' infrastructure/scripts/preflight-env.sh` returns 0 (generic `yourdomain.example` placeholders only — round-2 BLOCKER-4).
    - Makefile carries all 5: `for v in OAUTH_ISSUER_URL OAUTH_RESOURCE_URL CORS_ALLOWED_ORIGIN_REGEX XBRAIN_BASE_DOMAIN AGENT_MENTION_ALIASES; do grep -q "$v" Makefile || echo "MISSING $v"; done` prints nothing.
    - No duplicates introduced: each of the 5 appears in the `env-check` required-var list exactly once.
    - `grep -q '^preflight:' Makefile` (target exists) and `deploy`'s prereq list contains BOTH `env-check` and `preflight`.
  </acceptance_criteria>
  <verify>Run the negative test (must exit non-zero and name the missing var) and the positive test (must exit 0). Confirm the Makefile carries the 3 vars + a `preflight` target.</verify>
  <done>A deploy cannot silently crashloop: the guard fails loudly and tells the operator exactly which var to set and why.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — record the DEFERRED live-regression gate (B4 / amended SC#2b)</name>
  <files>.planning/phases/14-portability-foundation/14-06-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 14 — AMENDED SC#2: the live suite is a DEFERRED gate, the VM is TERMINATED)
    - infrastructure/scripts/preflight-env.sh (created in Task 3 — step 1 of the runbook)
  </read_first>
  <action>
    The prod VM is TERMINATED (cost pause during the Prime pivot). The `verify-phase1..13.sh` scripts
    `curl` a LIVE deployment — they CANNOT be executed in this phase. Do NOT attempt to run them and do
    NOT mark SC#2 as satisfied by grep alone.

    Instead, write a `## DEFERRED GATE — run at the next real deploy` section into
    `14-06-SUMMARY.md` containing this runbook VERBATIM, then STOP and surface it to the operator:

      1. Populate the target `.env` with the now-MANDATORY vars (the compose fallback is gone). ALL FIVE
         are required — each has a silent-failure mode if omitted:
           OAUTH_ISSUER_URL=https://api.grooveos.app
           OAUTH_RESOURCE_URL=https://mcp.grooveos.app/mcp
           CORS_ALLOWED_ORIGIN_REGEX=(chrome-extension://.*|https://chat\.grooveos\.app|https://grooveos\.app|https://grooveos\.web\.app|https://claude\.ai)
           XBRAIN_BASE_DOMAIN=grooveos.app
             # ^ WITHOUT THIS, every nginx vhost renders as *.localhost -> TOTAL INGRESS OUTAGE.
           AGENT_MENTION_ALIASES=agent,grooveos,groove,gr,g
             # ^ WITHOUT THIS, aliases fall back to `agent` and @groove/@grooveos SILENTLY die in prod.
             #   `agent` is included FIRST so the shipped KB (which documents @agent) is also true here.
      2. `bash infrastructure/scripts/preflight-env.sh .env`   # MUST print PREFLIGHT OK
      3. Deploy. Then confirm both services actually booted (they now hard-fail on empty OAuth):
           `docker compose ps memory-api mcp-brain`  # both Up, RestartCount 0
      4. Run the live regression suite with the PROD values — ALL must PASS (SC#2b):
           MEMAPI_HOST=https://api.grooveos.app \
           LIBRECHAT_HOST=https://chat.grooveos.app \
           TEST_TEAM_SCOPE=aibrussels \
           bash infrastructure/scripts/verify-phase{5,7,8,9,10,12,13}.sh
      5. Sanity-check the ingress actually renders (14-03a moved all 7 vhosts to envsubst templates):
           `docker compose exec nginx nginx -t`  # syntax OK
           `curl -sI https://api.grooveos.app/health`  # 200

    Mark the phase as CODE-COMPLETE with SC#2b DEFERRED — not as fully verified.
  </action>
  <what-built>
    Phase 14 is CODE-COMPLETE: the stack is config-driven (no brand in backend/infra source), the OAuth
    fail-fast + CORS are env-sourced, nginx is templated, the agent mention alias is configurable, and
    `verify-phase14.sh` proves PORT-01 + PORT-02 offline.

    What is NOT done, and cannot be here: the LIVE regression suite. The prod VM is TERMINATED, and
    `verify-phase1..13.sh` curl a running deployment. SC#2b is therefore **DEFERRED, not passed.**

    This phase also made 5 env vars MANDATORY. Deploying without them does NOT fail loudly on its own —
    each has a distinct silent or fatal failure mode. That is why this gate is blocking.
  </what-built>
  <how-to-verify>
    Read the `## DEFERRED GATE — run at the next real deploy` section written into `14-06-SUMMARY.md`.
    It contains the 5-step runbook verbatim. Confirm you understand that AT THE NEXT DEPLOY you must:
      1. Populate the VM `.env` with all 5 mandatory vars (OAUTH_ISSUER_URL, OAUTH_RESOURCE_URL,
         CORS_ALLOWED_ORIGIN_REGEX, XBRAIN_BASE_DOMAIN, AGENT_MENTION_ALIASES).
      2. Run `bash infrastructure/scripts/preflight-env.sh .env` -> must print PREFLIGHT OK.
      3. Deploy, then confirm memory-api + mcp-brain actually booted (they now hard-fail on empty OAuth).
      4. Run the live regression suite with the prod values — ALL must PASS (this closes SC#2b).
      5. Confirm the ingress renders (`nginx -t` + a 200 on the health endpoint).
  </how-to-verify>
  <resume-signal>
    Reply "acknowledged" (or "approved") to accept Phase 14 as CODE-COMPLETE with SC#2b DEFERRED.
    Reply with your concerns instead if the deferred gate or the 5 mandatory vars are not acceptable.
  </resume-signal>
  <acceptance_criteria>
    - `14-06-SUMMARY.md` contains the literal heading `## DEFERRED GATE — run at the next real deploy`.
    - It contains all 5 numbered steps, including the exact `MEMAPI_HOST=... TEST_TEAM_SCOPE=aibrussels bash infrastructure/scripts/verify-phase{5,7,8,9,10,12,13}.sh` command line, and all 5 mandatory vars with their prod values.
    - It states explicitly that SC#2b is DEFERRED (not passed) and that the VM is TERMINATED.
    - The operator has been shown this runbook and acknowledged it (this is a BLOCKING gate — execution stops here).
  </acceptance_criteria>
  <done>The live regression is recorded as an explicit, actionable deferred gate — surfaced to a human, not silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundary — verification + deploy tooling. It EXERCISES the 14-01 OAuth fail-fast
boundary as a test, and GUARDS the deploy boundary that 14-03b widened.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-15 | Repudiation | A naive PORT-01 grep passes falsely (misses the escaped `grooveos\.app` CORS regex) or fails falsely (test fixtures) | mitigate | Check (a) greps the BARE token `grooveos` (W5) with an explicit, documented exclude set. A dotted pattern would green-light a live CORS leak. |
| T-14-16 | Tampering | Regression: a neutralized fallback silently changes prod behavior | mitigate | Override var NAMES unchanged; prod reproduced by `MEMAPI_HOST=<prod> ...`; deferred gate (Task 4) proves it at next deploy. |
| T-14-17 | Denial of Service | **Deploy crashloop**: 14-03b removes the compose OAuth fallback that prod silently relies on; 14-01 makes empty FATAL. A deploy with an unpopulated `.env` takes memory-api AND mcp-brain down. | mitigate | `preflight-env.sh` HARD-FAILS before deploy naming the exact missing var (Task 3); Makefile `deploy` depends on `preflight`; runbook step 1 in the deferred gate. **This is the highest-severity threat in the phase.** |
| T-14-18 | Spoofing | An operator sets `OAUTH_ISSUER_URL` to a wrong/attacker domain -> tokens minted for the wrong audience | mitigate | Fail-fast only guards EMPTY, not WRONG. The deferred gate step 4 runs verify-phase12 (OAuth/GitHub App) against prod values, which asserts the issuer/audience round-trip. |
</threat_model>

<verification>
- All 8 edited scripts: 0 matches for the bare token `grooveos`, `bash -n` parses, env override NAMES intact, D-04 `default` literals untouched.
- verify-phase14.sh exits 0 with a PASS summary; covers PORT-01 (source clean incl. escaped CORS regex + fail-fast + env-driven CORS + nginx render) and PORT-02 (.env.example brand-free + fresh acme.example boot).
- preflight-env.sh exits non-zero and names the missing var on an incomplete .env; exits 0 on a complete one.
- 14-06-SUMMARY.md carries the DEFERRED GATE runbook; SC#2b is marked DEFERRED, not passed.
</verification>

<success_criteria>
- PORT-01 + PORT-02 provable by a single green `verify-phase14.sh` (amended SC#1, SC#3, SC#4).
- SC#2a: prod values reproduce prod via unchanged verify-script env overrides.
- SC#2b: the live regression suite is a recorded DEFERRED gate (VM terminated) — surfaced to the operator, with a preflight guard that prevents the crashloop it would otherwise hit.
</success_criteria>

<output>
After completion, create `.planning/phases/14-portability-foundation/14-06-SUMMARY.md` — it MUST carry
the `## DEFERRED GATE — run at the next real deploy` section (Task 4).
</output>
