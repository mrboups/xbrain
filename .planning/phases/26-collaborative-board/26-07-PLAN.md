---
phase: 26-collaborative-board
plan: 07
type: execute
wave: 4
depends_on: [26-05, 26-06]
files_modified:
  - apps/memory-api/tests/test_board_gate.py
  - apps/hocuspocus/tests/gate_client.mjs
  - infrastructure/scripts/verify-phase26.sh
  - Makefile
autonomous: true
requirements: [BOARD-01]

must_haves:
  truths:
    - "Two Yjs clients connected to the REAL running Hocuspocus converge on the SAME document state — convergence is asserted, not merely 'no error'"
    - "A board token minted by the REAL memory-api minting code for team A's board is REJECTED by the REAL onAuthenticate against team B's documentName; absent / malformed / expired / wrong-signature tokens are all rejected"
    - "A Y.Doc update stored through the REAL database extension against a REAL Postgres and re-fetched into a fresh doc survives byte-identical, and the board reloads with its content after a restart"
    - "verify-phase16.sh is GREEN at the end of the phase, and the board image BUILDS via its multi-stage Dockerfile"
    - "SKIP=FAIL — under Docker every one of these must run green; only a genuinely absent Docker daemon may skip"
  artifacts:
    - path: "apps/memory-api/tests/test_board_gate.py"
      provides: "Real-Postgres gate: migration 0028 under oss AND saas, byte-exact doc round-trip, team-scope on mint, size cap"
      contains: "pytest.mark.integration"
      min_lines: 240
    - path: "apps/hocuspocus/tests/gate_client.mjs"
      provides: "The live two-client convergence + rejection-matrix driver, run inside the compose network"
      min_lines: 150
    - path: "infrastructure/scripts/verify-phase26.sh"
      provides: "The phase gate: boots the board profile, mints REAL tokens, runs the live client, proves the image builds and Phase 16 is still green"
      min_lines: 200
  key_links:
    - from: "infrastructure/scripts/verify-phase26.sh"
      to: "apps/memory-api/app/routes/board_helpers.py:mint_board_token"
      via: "docker compose exec memory-api python — the REAL minting code produces the gate's tokens"
      pattern: "mint_board_token"
    - from: "apps/hocuspocus/tests/gate_client.mjs"
      to: "the running xbrain-hocuspocus container"
      via: "two HocuspocusProvider clients on ws://hocuspocus:8108 asserting Y.Doc convergence"
      pattern: "HocuspocusProvider"
---

<objective>
Prove the phase, honestly. "Two people can draw together" is not provable by a unit test of a
React component, and "the team-scope boundary holds" is not provable by a mocked verifier.
This plan is the gate lesson made executable, and it is the plan the phase is judged on.

Five things must be true, all NON-MOCKED:
  (a) two Yjs clients against the REAL running Hocuspocus converge on the SAME document state
      — assert convergence, not "no error";
  (b) the team-scope rejection matrix runs against the REAL `onAuthenticate` with REAL tokens
      produced by the REAL memory-api minting code (a Node-minted token would only prove Node
      agrees with itself; the cross-language agreement IS the property);
  (c) a Y.Doc update stored through the REAL database extension against a REAL Postgres
      (testcontainers, migration 0028) and re-fetched survives byte-identical, and a real
      container restart does not lose the board;
  (d) `verify-phase16.sh` is still GREEN;
  (e) the board image actually BUILDS via its multi-stage Dockerfile.

SKIP=FAIL. Docker is up on this host — a skipped check is a failure, because a gate that goes
green without a live socket is precisely the defect class this gate exists to catch.

Output: a real-Postgres pytest gate, a live Node client driver, and `verify-phase26.sh` with
a `make verify-phase26` target.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-collaborative-board/26-CONTEXT.md
@.planning/phases/26-collaborative-board/26-RESEARCH.md
@.planning/phases/26-collaborative-board/26-02-SUMMARY.md
@.planning/phases/26-collaborative-board/26-03-SUMMARY.md
@.planning/phases/26-collaborative-board/26-06-SUMMARY.md

@apps/memory-api/tests/test_join_by_code_gate.py
@apps/memory-api/tests/conftest.py

<interfaces>
<!-- The scaffolding to mirror, and the exact contract under test. -->

PYTEST GATE TEMPLATE — `apps/memory-api/tests/test_join_by_code_gate.py` is the direct
precedent (Phase 25). Reuse its shape VERBATIM:
  - `pytestmark = [pytest.mark.integration, pytest.mark.asyncio]`
  - `_install_principal(user, *, kind="user")` / `_clear_principal()` overriding
    get_current_principal with a SimpleNamespace(id, source_user_id, email, display_name,
    github_username, github_id); `_as(user, method, path, **kw)` wrapping set->call->clear.
  - the `_committing_get_session` override so each request commits straight through and the
    data is visible cross-connection; `rate_limit._storage.reset()` at the top.
  - COMMITTED seed via `db_session.async_session_factory()`; teardown
    `DELETE FROM teams WHERE id = ANY(:ids)` (CASCADE) + `DELETE FROM users` in `finally`.
  - Migration group: a FRESH `PostgresContainer("postgres:17", ...)` PER EDITION with
    `.with_command("postgres -c shared_preload_libraries=pgcrypto")`, the config singleton
    patched DIRECTLY (`app_config.settings.EDITION` — os.environ alone is INERT against the
    already-frozen singleton), `command.upgrade(cfg, "head")` inside `asyncio.to_thread`,
    an `information_schema` probe, singleton + env restored in `finally`,
    `_EDITIONS = ["oss", "saas"]` parametrized, `pytest.skip` ONLY when Docker is absent.

VERIFY-SCRIPT TEMPLATE — `infrastructure/scripts/verify-phase16.sh` is the house style:
  `set -uo pipefail` (NOT -e), the ok/ko/skip counters, a single EXIT trap doing teardown,
  python (not jq) for JSON assertions, and the explicit SKIP=FAIL doctrine at the bottom
  (`if boot_failed; then ko "..."` for every downstream check rather than skipping).
  THE HOST-PATH RULE (verified the hard way, do not "simplify"): `-f` and `--env-file` take
  HOST paths and MUST be MSYS-converted, so NEVER set MSYS_NO_PATHCONV=1 on those commands.
  MSYS_NO_PATHCONV=1 belongs ONLY on commands carrying an IN-CONTAINER path
  (`docker exec <ctr> /path`, `docker run ... -w /w`).
  container_name: is GLOBAL, not namespaced by `-p` — refuse to boot if `xbrain-*` containers
  already exist (verify-phase16.sh:504-509 does exactly this).

UNDER TEST (fixed by 26-02 / 26-03 / 26-06 — confirm each against its SUMMARY before coding):
  mint:      apps/memory-api/app/routes/board_helpers.py :: mint_board_token(...)
  verify:    apps/hocuspocus/src/auth.mjs :: verifyBoardToken(token, documentName, secret)
  endpoints: POST /v1/teams/{team_id}/boards | GET  /v1/teams/{team_id}/boards
             POST /v1/boards/{board_id}/token
             GET/PUT /v1/internal/boards/{board_id}/doc   (bridge-only, octet-stream, 413 cap)
  services:  board -> 8107 (static SPA), hocuspocus -> 8108 (Yjs WS), profile "board"
  gate image: `docker build --target gate apps/hocuspocus` produces an image whose
              node_modules INCLUDE @hocuspocus/provider + yjs (26-03 Task 1)

DOCKER FACTS: daemon 29.6.1, `linux/aarch64`. The production VM is TERMINATED (cost pause), so
this gate runs LOCALLY against a real boot — exactly as Phases 24 and 25 did.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The real-Postgres pytest gate — migration 0028 under oss AND saas, byte-exact doc round-trip, team-scope on mint, and the size cap</name>
  <read_first>
    - apps/memory-api/tests/test_join_by_code_gate.py (the WHOLE file — principal override, committing session, committed seed, teardown, and the per-edition migration group; copy this scaffolding rather than inventing a second testing philosophy)
    - apps/memory-api/tests/conftest.py:55-124 (the pg_url session fixture, _docker_available, and the client fixture)
    - .planning/phases/26-collaborative-board/26-02-SUMMARY.md (the exact endpoint paths, response field names, status codes and the generic-404 detail string)
    - apps/memory-api/app/routes/board_helpers.py + app/repos/boards.py (the code actually under test)
  </read_first>
  <files>apps/memory-api/tests/test_board_gate.py</files>
  <action>
    Create `apps/memory-api/tests/test_board_gate.py`. Header docstring, English: real Postgres
    (testcontainers), NON-mocked on the security paths, SKIP=FAIL.

    GROUP 1 — `async def test_board_http_gate(client)`, one comprehensive flow with blocks
    labelled by success criterion. Seed COMMITTED via `async_session_factory()` with distinct
    slugs/subs, torn down in `finally`:
      member_a (brd-a-sub) -> creates team_a ("brd-team-a"); member_b (brd-b-sub) -> creates
      team_b ("brd-team-b", the DECOY); outsider (brd-out-sub) with no membership.

      A. CREATE (SC#1) — as member_a: POST /v1/teams/{team_a}/boards {"title":"Team board"}
         -> 201; body has `id` (a valid UUID), `open_url`, `expires_at`. Assert `open_url`
         contains `#t=` and does NOT contain `?t=` or `&token=` — the token is in the FRAGMENT.
      B. IDEMPOTENT — POST the same path again -> the SAME board `id` (one default board per
         team; the schema allows more but the endpoint returns the team's default).
      C. LIST — GET /v1/teams/{team_a}/boards -> 200, contains the board, and NO item carries
         any doc bytes or any token field.
      D. TEAM ISOLATION on create/list (SC#2) — as member_b: POST and GET the TEAM_A paths ->
         403 both times (not a member of that team). As `outsider`: same, 403.
      E. MINT + NO ORACLE (SC#2) — as member_a: POST /v1/boards/{board_a}/token -> 200 with a
         `token`, `board_id` and `ws_url`. Then as member_b: POST the SAME path -> 404 with the
         generic detail. Then as member_a: POST /v1/boards/{a random UUID}/token -> 404 with
         the IDENTICAL detail string. Assert the two 404 bodies are byte-equal — a distinct
         message would tell an outsider that a board id exists.
      F. TOKEN CLAIMS — decode the minted token with BRIDGE_SHARED_SECRET and assert the exact
         claim set from 26-02-SUMMARY: scope=="board", board_id==the board, team_scope==team_a's
         slug, team_id, sub, exp > iat. This is the contract `apps/hocuspocus/src/auth.mjs`
         verifies, so a drift here is a cross-language auth break.
      G. INTERNAL DOC — bridge-only + BYTE-EXACT (SC#3):
         - as member_a (a USER token): GET and PUT /v1/internal/boards/{board_a}/doc -> 403 both.
         - with a bridge JWT (conftest's `make_bridge_jwt`): GET -> 404 (no doc yet).
         - Build a REAL Yjs update, not random bytes: `import pycrdt` if available, else accept
           a fixed byte string that includes NUL and high bytes (e.g.
           `bytes([0,1,2,255,254,0,0,127]) * 64`) so any accidental text/base64/UTF-8 round-trip
           corrupts it visibly. State in a comment which was used.
         - PUT it with Content-Type application/octet-stream -> 204.
         - GET it back -> 200, `response.content == the exact bytes PUT`, and the response
           `content-type` is `application/octet-stream`.
         - Read `board_docs` directly via `async_session_factory()` + raw SQL and assert
           `state` equals the same bytes and `size_bytes == len(bytes)`.
         - PUT a second, DIFFERENT update -> 204; GET -> the SECOND bytes (overwrite is the
           compaction strategy; no append log).
      H. SIZE CAP (SC#3) — PUT `b"\x00" * (BOARD_MAX_DOC_BYTES + 1)` with the bridge JWT -> 413,
         and assert the STORED doc is UNCHANGED (still the block-G bytes) — a rejected oversize
         write must not partially clobber a good document.
      I. UNKNOWN BOARD — PUT to a random board UUID with the bridge JWT -> 404, and assert no
         `boards` row was created (the socket side may never create a board).

    GROUP 2 — `test_migration_0028_boards_forward_only(edition)` parametrized over
    `["oss","saas"]`, a near-verbatim adaptation of the Phase-25 migration test:
      - `_docker_available()` guard -> `pytest.skip` ONLY if Docker is absent.
      - FRESH PostgresContainer per edition, pgcrypto preloaded, `app_config.settings.EDITION`
        and DATABASE_URL patched DIRECTLY on the singleton, `command.upgrade(cfg, "head")` in
        `asyncio.to_thread`, restore + `pg.stop()` in `finally`.
      - Probe `information_schema`:
          (a) `boards` exists with columns id/team_id/title/is_default/created_at/deleted_at;
          (b) `board_docs.state` exists with `data_type == 'bytea'` and `is_nullable == 'NO'`;
          (c) the partial unique index `ux_boards_team_default` exists in `pg_indexes` and its
              `indexdef` contains both `UNIQUE` and `WHERE`.
      - Both editions must produce the IDENTICAL schema (no EDITION fork, D-26-02).
      - No reverse migration is ever invoked.
  </action>
  <verify>
    <automated>cd apps/memory-api && MSYS_NO_PATHCONV=1 python -m pytest tests/test_board_gate.py -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - The file is integration-marked and non-mocked on the security paths: `grep -c 'pytest.mark.integration' apps/memory-api/tests/test_board_gate.py` >= 1, and `grep -cE 'MagicMock|unittest.mock|monkeypatch.setattr\("app.repos.boards|monkeypatch.setattr\("app.routes.board_helpers' apps/memory-api/tests/test_board_gate.py` returns 0.
    - The byte-exactness proof is a real equality assertion on binary content, not a length check: `grep -q 'application/octet-stream' apps/memory-api/tests/test_board_gate.py` AND the test asserts `response.content == <the exact bytes PUT>` AND separately asserts the raw `board_docs.state` column equals those bytes.
    - The no-oracle property is asserted by comparing the two 404 bodies, not by eyeballing: the test contains an assertion that the "not your board" and "no such board" responses are equal.
    - The team-scope isolation block exists: member_b gets 403 on team_a's create/list paths and 404 on team_a's mint path — `grep -q 'brd-team-b' apps/memory-api/tests/test_board_gate.py`.
    - The size cap is proven not to clobber: after the 413, the test re-GETs and asserts the previously stored bytes are intact.
    - The migration test is parametrized over both editions and patches the singleton: `grep -q "\[\"oss\", *\"saas\"\]\|\['oss', *'saas'\]" apps/memory-api/tests/test_board_gate.py`, `grep -q 'settings.EDITION' apps/memory-api/tests/test_board_gate.py`, `grep -q 'information_schema' apps/memory-api/tests/test_board_gate.py`, `grep -q 'pg_indexes\|indexdef' apps/memory-api/tests/test_board_gate.py`.
    - `data_type == 'bytea'` is asserted for `board_docs.state` — `grep -q 'bytea' apps/memory-api/tests/test_board_gate.py`.
    - Under Docker, `MSYS_NO_PATHCONV=1 python -m pytest tests/test_board_gate.py -q` PASSES with zero failures AND zero skips. A skip while Docker is running is a FAILURE — the executor records the exact pass/skip counts in the SUMMARY.
  </acceptance_criteria>
  <done>Against a real Postgres: board create/list/mint enforce team membership with an indistinguishable 404 on the mint path, the minted token's claim set matches the contract the Node verifier reads, the internal doc endpoints are bridge-only and move bytes byte-identically (verified both over HTTP and directly in the `bytea` column), an oversize write is refused with 413 without clobbering the stored document, and migration 0028 produces an identical `boards` + `board_docs` schema under EDITION=oss AND saas.</done>
</task>

<task type="auto">
  <name>Task 2: The live client driver — two-client convergence and the rejection matrix against the REAL running Hocuspocus</name>
  <read_first>
    - apps/hocuspocus/src/auth.mjs and src/server.mjs (26-03 — the real handler and the exact DENY behaviour this drives)
    - apps/hocuspocus/tests/test_auth.mjs (26-03 — the unit-level matrix; this task is its LIVE counterpart, over a real socket, with tokens minted by PYTHON)
    - apps/hocuspocus/Dockerfile (26-03 — the `gate` target is the image this driver runs in; it has @hocuspocus/provider + yjs)
    - .planning/phases/26-collaborative-board/26-02-SUMMARY.md (the token claim set the Python minter emits)
    - .planning/phases/26-collaborative-board/26-RESEARCH.md Pattern 2 (the provider's `onAuthenticationFailed` / `onStatus` surface)
  </read_first>
  <files>apps/hocuspocus/tests/gate_client.mjs</files>
  <action>
    Create `apps/hocuspocus/tests/gate_client.mjs` — a standalone driver, NOT a `node:test`
    file, so `verify-phase26.sh` can run it inside a container and read its exit code and its
    line-oriented output. English throughout. It reads its inputs from env:

      HOCUSPOCUS_URL      e.g. ws://hocuspocus:8108     (inside the compose network)
      BOARD_A             board A's uuid  (team A)
      BOARD_B             board B's uuid  (team B — the decoy)
      TOKEN_A             a REAL token for board A, minted by memory-api's Python code
      TOKEN_B             a REAL token for board B, minted by memory-api's Python code
      TOKEN_A_EXPIRED     a REAL, already-expired token for board A
      TOKEN_A_BADSIG      a REAL-shaped token for board A signed with the WRONG secret

    It must NOT mint any token itself. The whole point is that the tokens come from the real
    minting code in the other language — a Node-minted token would only prove Node agrees with
    itself, and the cross-language agreement IS the property under test.

    Implement a tiny harness: `async function check(name, fn)` that records PASS/FAIL, prints
    `PASS <name>` / `FAIL <name>: <reason>`, and at the end prints
    `GATE-CLIENT SUMMARY pass=<n> fail=<n>` and exits 1 if any failed or if the pass count is
    zero (a driver that asserts nothing must not report success).

    Helper `connect(boardId, token)` -> a Promise resolving `{ provider, ydoc, status }` where
    status is "authenticated" or "denied", implemented with `onAuthenticationFailed` (denied),
    `onSynced`/`onStatus` (authenticated) and a hard timeout (~10 s) that resolves "timeout".
    Always `provider.destroy()` + `ydoc.destroy()` in a finally.

    CHECKS:

      1. CONVERGENCE (SC#1) — the headline proof. Open TWO independent providers on BOARD_A
         with TOKEN_A, each with its OWN `new Y.Doc()`.
           - client1 writes: `ydoc1.getArray("elements").push([{ id: "e1", t: Date.now() }])`
             (or `getMap`, whichever the binding uses — read 26-04's Board.tsx and use the SAME
             top-level type name so this exercises the real document shape).
           - client2 writes a DIFFERENT element.
           - Poll (not sleep-once) up to ~15 s until `Y.encodeStateAsUpdate(ydoc1)` and
             `Y.encodeStateAsUpdate(ydoc2)` describe the same content, then assert:
               * both documents contain BOTH elements;
               * `JSON.stringify(ydoc1.getArray("elements").toJSON()) ===
                  JSON.stringify(ydoc2.getArray("elements").toJSON())`.
           This asserts CONVERGENCE, not "no error". A check that only verifies the connection
           opened does NOT satisfy this criterion.

      2. TEAM-SCOPE REJECTION (SC#2) — the load-bearing invariant. Connect to BOARD_B while
         presenting TOKEN_A (team A's token against team B's documentName) -> MUST be denied.
         Then the mirror: BOARD_A with TOKEN_B -> MUST be denied. Both directions, so a
         one-sided bug cannot hide.

      3. NO TOKEN — connect to BOARD_A with `token: null` -> denied.
      4. MALFORMED — token "not.a.jwt" -> denied.
      5. EXPIRED — TOKEN_A_EXPIRED against BOARD_A -> denied.
      6. WRONG SIGNATURE — TOKEN_A_BADSIG against BOARD_A -> denied.
      7. POSITIVE CONTROL — BOARD_A with TOKEN_A -> authenticated. Without this, checks 2-6
         would also "pass" against a server that rejects everything, including a server that is
         simply down. This control is what makes the rejections meaningful.
      8. PERSISTENCE HANDOFF (SC#3, part 1) — after check 1, disconnect BOTH clients, wait past
         the store debounce (`maxDebounce` is 10 s — wait ~12 s), then connect a THIRD, FRESH
         client with a fresh token and assert it receives BOTH elements from check 1. That
         proves the Database extension's fetch/store round-trip end to end through memory-api
         and Postgres. (The full container-restart proof is verify-phase26.sh's check (c).)

    Every denial check must distinguish "denied" from "timeout": a timeout is a FAIL, not a
    pass. A server that is unreachable would otherwise silently satisfy the entire rejection
    matrix — the exact false-green this gate exists to prevent. Say so in a comment.
  </action>
  <verify>
    <automated>cd apps/hocuspocus && node --check tests/gate_client.mjs && MSYS_NO_PATHCONV=1 docker build --target gate -t xbrain/hocuspocus-gate:phase26 . && docker run --rm --entrypoint node xbrain/hocuspocus-gate:phase26 -e "import('@hocuspocus/provider').then(m=>{if(!m.HocuspocusProvider)throw new Error('provider missing');return import('yjs')}).then(()=>console.log('gate image has provider+yjs'))"</automated>
  </verify>
  <acceptance_criteria>
    - The driver mints nothing: `grep -c 'SignJWT\|createHmac\|jsonwebtoken' apps/hocuspocus/tests/gate_client.mjs` returns 0, and every token comes from `process.env` — `grep -c 'process.env.TOKEN_A' apps/hocuspocus/tests/gate_client.mjs` >= 1.
    - Convergence is asserted on CONTENT, not on connection state: `grep -q 'toJSON' apps/hocuspocus/tests/gate_client.mjs` AND the source contains an equality assertion between the two documents' array contents.
    - Both rejection directions exist: the source contains a BOARD_B+TOKEN_A case AND a BOARD_A+TOKEN_B case.
    - A positive control exists and is named as such — `grep -qi 'positive control' apps/hocuspocus/tests/gate_client.mjs`.
    - A timeout is a failure, not a pass: `grep -qi 'timeout' apps/hocuspocus/tests/gate_client.mjs` and the harness treats the "timeout" status as FAIL in the denial checks. The executor states in the SUMMARY how this is enforced.
    - The driver refuses to report success on an empty run: it exits non-zero when the pass count is zero — `grep -q 'GATE-CLIENT SUMMARY' apps/hocuspocus/tests/gate_client.mjs` and the exit condition includes the zero-pass case.
    - `node --check tests/gate_client.mjs` exits 0, `docker build --target gate` succeeds, and the built gate image can import both `@hocuspocus/provider` and `yjs` (the Task-2 verify command exits 0).
    - The persistence-handoff check waits past `maxDebounce` (10 s) before reconnecting — the wait is >= 12 s and the value is a named constant with a comment citing the debounce.
  </acceptance_criteria>
  <done>A standalone driver that, given REAL externally-minted tokens, proves against a live server: two clients converge on identical document content; a team-A token is refused on team-B's document and vice versa; absent, malformed, expired and wrong-signature tokens are all refused; a positive control authenticates (so the refusals are meaningful); a timeout is scored as a failure; and a fresh client reconnecting after the debounce window still sees the prior edits.</done>
</task>

<task type="auto">
  <name>Task 3: verify-phase26.sh — boot the profile, mint REAL tokens with the REAL Python code, run the live client, prove the restart, the image build and a green Phase 16</name>
  <read_first>
    - infrastructure/scripts/verify-phase16.sh (the WHOLE file — the ok/ko/skip counters, the EXIT trap, the host-path rule, the refuse-to-boot-over-a-live-stack guard, and the SKIP=FAIL block at the bottom that records downstream checks as FAILURES rather than skips)
    - infrastructure/scripts/oss-init.sh (how a zero-key bootable env is generated — reuse it rather than hand-writing one)
    - .planning/phases/26-collaborative-board/26-06-SUMMARY.md (the final service names, ports, profile name and the verify-phase16 summary line)
    - apps/hocuspocus/tests/gate_client.mjs (Task 2 — the env vars it expects)
    - Makefile:129-160 (the verify-phaseN target convention to follow)
  </read_first>
  <files>infrastructure/scripts/verify-phase26.sh, Makefile</files>
  <action>
    Create `infrastructure/scripts/verify-phase26.sh` in the verify-phase16 house style:
    `set -uo pipefail` (NOT -e), `ok`/`ko`/`skip` counters, a single EXIT trap that tears the
    stack down and removes temp files, python (not jq) for JSON, and an explicit SKIP=FAIL
    doctrine — if the boot fails, every downstream check is recorded as a FAILURE, never a skip.
    Header comment must state the five properties (a)-(e) and that Docker being up makes SKIP a
    FAIL. Refuse to run if any `xbrain-*` container already exists (`container_name:` is global,
    not namespaced by `-p`).

    CHECKS:

      (a) IMAGE BUILD — `docker compose --profile board build board hocuspocus` succeeds.
          Both images build from their multi-stage Dockerfiles. Log the resulting image sizes.
          A Vite app that does not build is not a deliverable.

      (b) BOOT — generate a zero-key env with `oss-init.sh --force "$ENV"`, append the board
          knobs, then `COMPOSE_PROFILES=board docker compose ... up -d --build` and poll ACTUAL
          docker health for the 10 core services PLUS `board` and `hocuspocus`. NOTE: `-f` and
          `--env-file` are HOST paths — do NOT set MSYS_NO_PATHCONV=1 on these commands.

      (c) REAL TOKENS FROM THE REAL MINTING CODE — this is what makes the rejection matrix
          honest. Create two teams and a board each, then mint with the actual Python function:
            docker compose exec -T memory-api python -c '<script>'
          The script imports `from app.routes.board_helpers import mint_board_token`, mints
          TOKEN_A / TOKEN_B, plus an already-expired token (`ttl=-60`) and a wrong-signature
          token (mint, then re-sign the same payload with a different secret via authlib), and
          prints them as one JSON object on stdout. Parse it with python.
          Seeding the two teams + boards: prefer the REAL HTTP path through nginx (register two
          accounts via /v1/auth/local/register, POST each team's /boards) so the gate walks the
          same route a user does; fall back to a `docker compose exec memory-api python` seed
          only if the HTTP path cannot express it, and say which was used in the output.
          Assert TOKEN_A != TOKEN_B and that both decode to different `board_id` claims before
          proceeding — a gate that accidentally uses one token twice proves nothing.

      (d) LIVE CLIENT — build the gate image and run the Task-2 driver INSIDE the compose
          network so it reaches the container by service name:
            MSYS_NO_PATHCONV=1 docker build --target gate -t xbrain/hocuspocus-gate:phase26 apps/hocuspocus
            docker run --rm --network xbrain_net \
              -e HOCUSPOCUS_URL=ws://hocuspocus:8108 -e BOARD_A -e BOARD_B \
              -e TOKEN_A -e TOKEN_B -e TOKEN_A_EXPIRED -e TOKEN_A_BADSIG \
              --entrypoint node xbrain/hocuspocus-gate:phase26 tests/gate_client.mjs
          Its exit code is the check. Echo its full output into the gate log so the convergence
          and rejection lines are visible in the transcript.

      (e) RESTART PERSISTENCE (SC#3) — after (d): `docker compose restart hocuspocus`, wait for
          health, then re-run ONLY the driver's convergence-read path (pass a flag, e.g.
          `GATE_MODE=verify-persisted`, that connects one fresh client with a fresh token and
          asserts BOTH elements from check 1 are present). This proves `fetch`, `store` and the
          shutdown flush together — the property "the board survives a reload" in SC#3. Mint the
          fresh token through the same real Python path.

      (f) NO RAW EXPOSURE — assert neither new container publishes a host port:
          `docker inspect xbrain-hocuspocus xbrain-board` and assert `.HostConfig.PortBindings`
          is empty for both. The socket must be reachable only through the ingress.

      (g) PHASE-16 STILL GREEN (SC#4, D-26-04) — tear THIS stack down first (verify-phase16
          refuses to boot over a live stack, by design), then run
          `bash infrastructure/scripts/verify-phase16.sh` and require exit 0. Echo its summary
          line. A red Phase-16 gate at the end of Phase 26 is a FAILURE.

    Summary line in the house format: `PASS: <n> / <total>  (SKIP: <n>)`, exit 0 only when
    FAIL == 0.

    Then add to the Makefile, following the existing convention:
      .PHONY: verify-phase26
      verify-phase26:  ## Phase 26a acceptance gate — real board boot: convergence + team-scope + persistence + Phase-16 green
      	@bash infrastructure/scripts/verify-phase26.sh
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash infrastructure/scripts/verify-phase26.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash infrastructure/scripts/verify-phase26.sh` exits 0 with `FAIL: 0` in its summary line, and the transcript shows every one of (a)-(g) as PASS. The executor pastes the full summary block into the SUMMARY.
    - The tokens come from the REAL minting code: `grep -q 'mint_board_token' infrastructure/scripts/verify-phase26.sh` AND `grep -q 'docker compose exec' infrastructure/scripts/verify-phase26.sh` — a Node-minted or shell-minted token anywhere in this script is a failure of the plan's purpose.
    - The live client runs inside the compose network against the service name, not a published port: `grep -q -- '--network xbrain_net' infrastructure/scripts/verify-phase26.sh` AND `grep -q 'ws://hocuspocus:8108' infrastructure/scripts/verify-phase26.sh`.
    - SKIP=FAIL is enforced structurally, not just documented: the script contains a boot-failure branch that records downstream checks with `ko` rather than `skip` (mirroring verify-phase16.sh:822-829) — `grep -c 'ko "' infrastructure/scripts/verify-phase26.sh` >= 8, and the final `SKIP:` count printed by a successful run is 0.
    - The restart proof is present and real: `grep -q 'restart hocuspocus' infrastructure/scripts/verify-phase26.sh` and the post-restart check asserts the prior CONTENT is present (not merely that the container came back up).
    - Raw exposure is checked: `grep -q 'PortBindings' infrastructure/scripts/verify-phase26.sh`.
    - Phase 16 is re-run and required to pass: `grep -q 'verify-phase16.sh' infrastructure/scripts/verify-phase26.sh`, and the Phase-26 transcript contains verify-phase16's own `PASS: n / n  (SKIP: 0)`-shaped summary with FAIL 0.
    - The Git-Bash host-path rule is respected: `MSYS_NO_PATHCONV=1` appears ONLY on commands carrying in-container paths (`docker run ... --entrypoint`, `docker build <ctx>`, `docker exec <ctr> /path`) and NEVER on a command with `--env-file` or `-f` — the executor lists each occurrence and its justification in the SUMMARY.
    - The Makefile target exists and runs the script: `grep -q 'verify-phase26:' Makefile` AND `grep -q 'verify-phase26.sh' Makefile`.
    - The gate refuses to boot over a live stack: `grep -q 'xbrain-' infrastructure/scripts/verify-phase26.sh` in a pre-boot guard that `ko`s (not skips) when containers already exist.
  </acceptance_criteria>
  <done>`make verify-phase26` boots the board profile from a zero-key env, builds both images, mints tokens with memory-api's REAL Python minting code, runs the live driver inside the compose network to prove two-client convergence and the full rejection matrix against the REAL onAuthenticate, restarts the Hocuspocus container and proves the board content survived, asserts neither container publishes a host port, and finishes by re-running `verify-phase16.sh` to a green result — exiting 0 only when FAIL == 0 and SKIP == 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the gate itself → the claim "the phase works" | A gate that can pass without exercising the real socket, the real verifier or the real database is the most dangerous artifact in the phase |
| Python minter ↔ Node verifier | Two independent implementations of one token contract. Only a cross-language test can prove they agree |
| a booting gate → an operator's running stack | `container_name:` is global; a careless gate could tear down live work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-43 | Repudiation | A green gate that proves nothing — a mocked verifier, a Node-minted token, or a rejection matrix that "passes" because the server is unreachable | mitigate | Tokens are minted by `docker compose exec memory-api python` calling the REAL `mint_board_token`; the driver mints nothing (grep-asserted); a POSITIVE CONTROL must authenticate before any rejection counts; a timeout is scored FAIL, not PASS; the driver exits non-zero on a zero-pass run. |
| T-26-44 | Repudiation | SKIP silently standing in for PASS | mitigate | SKIP=FAIL is structural: a boot failure records every downstream check with `ko`, mirroring verify-phase16.sh's own doctrine, and a successful run must report `SKIP: 0`. The pytest gate must report zero skips while Docker is up. |
| T-26-45 | Tampering | Convergence asserted as "no error" rather than as identical state — the classic false green for a CRDT | mitigate | The driver compares the two documents' actual array contents (`toJSON()` equality) and asserts BOTH clients' elements are present in BOTH documents, polling to a deadline rather than sleeping once. |
| T-26-46 | Elevation of privilege | The cross-team invariant regressing because the Python and Node token implementations drift apart | mitigate | The pytest gate pins the exact claim set emitted by the minter, and the live driver feeds those same real tokens to the real Node verifier — a drift breaks the gate in one direction or the other. Both rejection directions (A-token/B-board and B-token/A-board) are tested. |
| T-26-47 | Tampering | Silent persistence loss — the board looks live but nothing survives a restart | mitigate | Two independent proofs: a byte-exact `bytea` round-trip asserted both over HTTP and directly in the column (Task 1), and a real `docker compose restart hocuspocus` after which a fresh client must still see the prior content (Task 3 check (e)). |
| T-26-48 | Denial of service | The gate clobbering an operator's running stack because `container_name:` is global | mitigate | A pre-boot guard refuses to run when any `xbrain-*` container exists and records it as a FAILURE (never a skip), copied from verify-phase16.sh:504-509; a single EXIT trap always tears down what the gate itself booted. |
| T-26-49 | Information disclosure | The gate transcript leaking real tokens into CI logs | mitigate | Tokens are passed to the driver through `-e VAR` env references, never echoed; the script prints board ids and PASS/FAIL lines only. The executor confirms no token appears in the transcript. |
| T-26-50 | Spoofing | The Yjs WebSocket reachable without going through the ingress | mitigate | Check (f) inspects both containers and asserts `HostConfig.PortBindings` is empty — no host port, so the nginx vhost is the only route. |
</threat_model>

<verification>
- `cd apps/memory-api && MSYS_NO_PATHCONV=1 python -m pytest tests/test_board_gate.py -q` → all integration tests PASS, zero failures, zero skips (Docker is up).
- `cd apps/hocuspocus && node --check tests/gate_client.mjs` exits 0; `docker build --target gate` succeeds and the image can import `@hocuspocus/provider` and `yjs`.
- `bash infrastructure/scripts/verify-phase26.sh` → `FAIL: 0`, `SKIP: 0`, with (a) both images built, (b) the 12 services healthy, (c) real Python-minted tokens, (d) convergence + the full rejection matrix + the positive control green, (e) content surviving a `restart hocuspocus`, (f) no host port bindings, (g) `verify-phase16.sh` exit 0.
- `make verify-phase26` runs the same script.
</verification>

<success_criteria>
- SC#1 — two Yjs clients against the REAL running Hocuspocus converge on identical document content, proven by a content equality assertion with a positive control.
- SC#2 — the team-scope boundary holds in BOTH directions against the REAL `onAuthenticate`, with REAL tokens from the REAL minting code; absent / malformed / expired / wrong-signature tokens are all refused, and a timeout counts as a failure.
- SC#3 — a Y.Doc update survives a byte-exact round-trip through the real database extension against a REAL Postgres with migration 0028 (proven under EDITION=oss AND saas), and the board still has its content after a container restart.
- SC#4 — both images build via their multi-stage Dockerfiles, neither container publishes a host port, and `verify-phase16.sh` exits 0.
- SKIP=FAIL throughout: a run that skips anything while Docker is up has not satisfied this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/26-collaborative-board/26-07-SUMMARY.md`.
Record: the full `verify-phase26.sh` summary block, the pytest pass/skip counts, the
`GATE-CLIENT SUMMARY pass=/fail=` line, the `verify-phase16.sh` summary line, both image sizes,
the measured hocuspocus RSS under two concurrent clients (research assumption A1 and open
question 6 asked for this), and every `MSYS_NO_PATHCONV=1` occurrence with its justification.
</output>
