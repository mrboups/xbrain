---
phase: 26-collaborative-board
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/board-web/package.json
  - apps/board-web/package-lock.json
  - apps/board-web/.gitignore
  - apps/board-web/vite.config.ts
  - apps/board-web/tsconfig.json
  - apps/board-web/index.html
  - apps/board-web/src/yjs-binding/index.ts
  - apps/board-web/src/yjs-binding/diff.ts
  - apps/board-web/src/yjs-binding/helpers.ts
  - apps/board-web/src/yjs-binding/LICENSE
  - apps/board-web/src/yjs-binding/DECISION.md
autonomous: true
requirements: [BOARD-01]

must_haves:
  truths:
    - "The y-excalidraw ↔ Excalidraw 0.18.1 compatibility question is ANSWERED with evidence, not assumed — the commitToHistory → captureUpdate rename is either proven absent or fixed in code"
    - "apps/board-web/ exists with EXACT (caret-free) version pins for @excalidraw/excalidraw 0.18.1, yjs 13.6.31 and @hocuspocus/provider 4.4.0, and a Vite config that defines process.env.IS_PREACT"
    - "A downstream plan can import a binding module that typechecks against Excalidraw 0.18.1's own .d.ts — no unverified binding is carried into Wave 2"
  artifacts:
    - path: "apps/board-web/package.json"
      provides: "Exact-pinned SPA dependency set (build-time Node only)"
      contains: "\"@excalidraw/excalidraw\": \"0.18.1\""
    - path: "apps/board-web/vite.config.ts"
      provides: "Vite build config with the Excalidraw process.env.IS_PREACT define (Pitfall 3)"
      contains: "process.env.IS_PREACT"
    - path: "apps/board-web/src/yjs-binding/DECISION.md"
      provides: "The recorded D-26-05 vendor-or-npm decision with its evidence"
      min_lines: 20
  key_links:
    - from: "apps/board-web/src/yjs-binding/index.ts"
      to: "@excalidraw/excalidraw 0.18.1 updateScene"
      via: "CaptureUpdateAction.NEVER on every remote-update applied to the scene"
      pattern: "CaptureUpdateAction"
---

<objective>
Answer the one question this phase cannot be built on top of blind (D-26-05): does
`y-excalidraw@2.0.12` actually work against `@excalidraw/excalidraw@0.18.1`? The npm
build is ~19 months old, peers `^0.17.6`, and Excalidraw replaced `commitToHistory`
with `captureUpdate` in the gap — the exact API a binding calls on every remote
update. Run the spike, then EXECUTE the decision: vendoring the ~23 KB of MIT source
into `apps/board-web/src/yjs-binding/` (attribution preserved) is the EXPECTED
outcome, not a fallback.

This plan also lays the first frontend build tooling this repo has ever had:
`apps/board-web/` with EXACT version pins (a caret range on Excalidraw breaks the
binding — its unreleased `master` already carries breaking `setActiveTool` /
`scrollToContent → setViewport` changes) and the `process.env.IS_PREACT` Vite define
without which Excalidraw renders a blank canvas.

Purpose: no later wave may be built on an unverified binding. Node/npm enter this
repo as a BUILD-TIME dependency only (D-26-01) — nothing here adds a Node runtime to
the API path and nothing here touches `chrome-extension/`.

Output: `apps/board-web/` scaffold + a typechecking Yjs↔Excalidraw binding + a
`DECISION.md` recording which path was taken and why.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-collaborative-board/26-CONTEXT.md
@.planning/phases/26-collaborative-board/26-RESEARCH.md

<interfaces>
<!-- Everything below was VERIFIED in 26-RESEARCH.md against the live npm registry / GitHub
     on 2026-07-24. Do NOT re-derive from memory; do NOT relax a pin to a caret range. -->

EXACT PINS (research-verified, all MIT):
  @excalidraw/excalidraw  0.18.1     (published 2026-07-13)
  yjs                     13.6.31    (2026-05-28)
  @hocuspocus/provider    4.4.0      (2026-07-13)
  react / react-dom       18.x       (Excalidraw peer: ^17.0.2 || ^18.2.0 || ^19.0.0)
  y-excalidraw            2.0.12     (MIT, npm 2024-12-10, peers "@excalidraw/excalidraw": "^0.17.6")

THE BREAK (CITED: docs.excalidraw.com/docs/@excalidraw/excalidraw/api/excalidraw-api):
  0.17.x:  excalidrawAPI.updateScene({ elements, commitToHistory: false })
  0.18.1:  excalidrawAPI.updateScene({ elements, captureUpdate: CaptureUpdateAction.NEVER })
  CaptureUpdateAction.NEVER — "Use for updates which should never be recorded, such as
  remote updates or scene initialization." A remote update that captures history poisons
  the LOCAL user's Ctrl+Z stack with a teammate's edits (Pitfall 4).

VENDOR SOURCE (VERIFIED: GitHub git/trees API — 3 files, ~23 KB total):
  https://raw.githubusercontent.com/RahulBadenkal/y-excalidraw/main/src/index.ts    (9.9 KB)
  https://raw.githubusercontent.com/RahulBadenkal/y-excalidraw/main/src/diff.ts     (11.7 KB)
  https://raw.githubusercontent.com/RahulBadenkal/y-excalidraw/main/src/helpers.ts  (1.2 KB)
  https://raw.githubusercontent.com/RahulBadenkal/y-excalidraw/main/LICENSE
  LICENSE text is "The MIT License (MIT), Copyright (c) 2024 Rahul R Badenkal".
  (GitHub's API reports NOASSERTION only because licensee does not match the modified
  header — the LICENSE file itself is unambiguously MIT.)

VITE (CITED: docs.excalidraw.com/docs/@excalidraw/excalidraw/integration):
  define: { "process.env.IS_PREACT": JSON.stringify("true") }   // Vite strips `process`
  import "@excalidraw/excalidraw/index.css";                    // separate package export
  Excalidraw does NOT support SSR — client-render only.

REPO CONVENTIONS THIS MUST RESPECT:
  - `.gitignore:23` already ignores `node_modules/` and `:48` ignores `dist/` repo-wide.
    Still add an apps/board-web/.gitignore so the directory is self-describing.
  - English only in every string, comment and doc (CLAUDE.md).
  - Dev host is arm64, prod is amd64. `npm install` here is a pure-JS resolution step and
    is safe locally; the IMAGE is never built locally and shipped (that is 26-04/26-06).
  - Host tooling available (VERIFIED in research): node v24.15.0, npm 11.6.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/board-web with exact pins, and run the y-excalidraw spike</name>
  <read_first>
    - .planning/phases/26-collaborative-board/26-RESEARCH.md — the "Supporting" table's `y-excalidraw` warning block and Pitfall 3 / Pitfall 4 (the two things this task encodes)
    - .planning/phases/26-collaborative-board/26-CONTEXT.md — D-26-01 (apps/board-web, Docker build-time only) and D-26-05 (the spike mandate)
    - .gitignore:20-50 (node_modules/ and dist/ are already repo-ignored)
  </read_first>
  <files>apps/board-web/package.json, apps/board-web/package-lock.json, apps/board-web/.gitignore, apps/board-web/vite.config.ts, apps/board-web/tsconfig.json, apps/board-web/index.html</files>
  <action>
    Create `apps/board-web/` — the repo's FIRST frontend build target. Everything here is
    build-time only (D-26-01); no Node runtime is added to any service by this task.

    1. `apps/board-web/.gitignore`:
           node_modules/
           dist/

    2. `apps/board-web/package.json` — write the skeleton first:
           { "name": "xbrain-board-web", "private": true, "type": "module", "version": "0.1.0",
             "scripts": { "dev": "vite", "typecheck": "tsc --noEmit", "build": "tsc --noEmit && vite build" } }
       Then install with SAVE-EXACT so npm writes caret-free pins (`-E` / `-DE`), from
       inside apps/board-web:
           npm i  -E  @excalidraw/excalidraw@0.18.1 @hocuspocus/provider@4.4.0 yjs@13.6.31 react@18 react-dom@18
           npm i -DE  vite @vitejs/plugin-react typescript @types/react @types/react-dom
       After installing, OPEN package.json and confirm NO `^` and NO `~` survive anywhere in
       `dependencies` or `devDependencies`. If npm wrote one, replace it with the resolved
       exact version from `npm ls --depth=0`. Rationale (research): Excalidraw's unreleased
       master already carries breaking `setActiveTool` / `scrollToContent → setViewport` /
       toolbar-DOM changes — a caret range on Excalidraw WILL break this binding.
       Commit `package-lock.json` (it is the input to `npm ci` in the Dockerfile and in the
       26-07 gate container — determinism depends on it).

    3. `apps/board-web/vite.config.ts`:
           import { defineConfig } from "vite";
           import react from "@vitejs/plugin-react";
           export default defineConfig({
             plugins: [react()],
             // Excalidraw reads process.env.IS_PREACT at runtime; Vite strips `process`
             // entirely, so without this define the canvas renders blank
             // (Pitfall 3, CITED: docs.excalidraw.com/.../integration).
             define: { "process.env.IS_PREACT": JSON.stringify("true") },
             build: { outDir: "dist", sourcemap: false },
           });

    4. `apps/board-web/tsconfig.json` — a standard Vite react-ts config:
       target ES2022, lib ["ES2022","DOM","DOM.Iterable"], module ESNext,
       moduleResolution "bundler", jsx "react-jsx", strict true, noEmit true,
       skipLibCheck true, isolatedModules true, include ["src"].

    5. `apps/board-web/index.html` — minimal, ENGLISH ONLY:
       `<title>xbrain board</title>`, `<div id="root"></div>`,
       `<script type="module" src="/src/main.tsx"></script>`.
       (`src/main.tsx` is created by plan 26-04; this file only declares the entry.)

    6. RUN THE SPIKE (D-26-05) and capture the raw evidence into the task output — you will
       write it up in Task 2:
         a. `npm view y-excalidraw version license peerDependencies time.modified`
            → record the version, the license string, and the peer range.
         b. Install it WITHOUT saving, tolerating the peer conflict:
            `npm i --no-save --legacy-peer-deps y-excalidraw@2.0.12`
         c. Probe the shipped build for the broken API. In a POSIX shell, from apps/board-web:
            `grep -rlno "commitToHistory" node_modules/y-excalidraw/ | head`
            `grep -rlno "captureUpdate"    node_modules/y-excalidraw/ | head`
            A hit on `commitToHistory` with NO hit on `captureUpdate` is DISPOSITIVE: the
            published build calls an API that no longer exists in 0.18.1, so every remote
            update either no-ops or poisons the local undo stack.
         d. Record whether `node_modules/y-excalidraw/` ships a `src/` directory (it may be
            dist-only) — Task 2 needs to know whether to vendor from node_modules or fetch
            the three source files from raw.githubusercontent.
       Do NOT add y-excalidraw to package.json in this task. `--no-save` is deliberate:
       the decision is Task 2's.
  </action>
  <verify>
    <automated>cd apps/board-web && npm ls --depth=0 && node -e "const p=require('./package.json');const all={...p.dependencies,...p.devDependencies};const bad=Object.entries(all).filter(([k,v])=>/[\^~]/.test(v));if(bad.length){console.error('caret/tilde ranges found:',bad);process.exit(1)}if(all['@excalidraw/excalidraw']!=='0.18.1'||all['yjs']!=='13.6.31'||all['@hocuspocus/provider']!=='4.4.0'){console.error('pin mismatch',all);process.exit(1)}console.log('OK exact pins')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/board-web/package.json` pins EXACTLY: `grep -q '"@excalidraw/excalidraw": "0.18.1"'`, `grep -q '"yjs": "13.6.31"'`, `grep -q '"@hocuspocus/provider": "4.4.0"'` — all three succeed.
    - No range operators anywhere in the manifest: the Task-1 `node -e` check exits 0 (it fails on any `^` or `~` in dependencies/devDependencies).
    - `grep -q 'process.env.IS_PREACT' apps/board-web/vite.config.ts` succeeds AND `grep -q '@vitejs/plugin-react' apps/board-web/vite.config.ts` succeeds.
    - `apps/board-web/package-lock.json` exists and is committed (`test -f apps/board-web/package-lock.json`).
    - `apps/board-web/.gitignore` contains `node_modules/` and `dist/` (`grep -c 'node_modules/' apps/board-web/.gitignore` >= 1).
    - The spike evidence is captured verbatim in the task output: the `npm view` peer range for y-excalidraw, and the `commitToHistory` / `captureUpdate` grep results over `node_modules/y-excalidraw/`. A spike with no recorded grep output does NOT satisfy this criterion.
    - `cd apps/board-web && npm ls --depth=0` resolves with no UNMET dependency for the five runtime pins.
  </acceptance_criteria>
  <done>`apps/board-web/` exists with caret-free pins on the four research-verified packages, a Vite config carrying the `process.env.IS_PREACT` define, a committed lockfile, and the y-excalidraw spike run with its raw `commitToHistory` / `captureUpdate` evidence captured.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the D-26-05 decision — vendor the MIT binding (expected) and fix captureUpdate, or keep the npm dep; record which and why</name>
  <read_first>
    - The Task-1 spike output (the peer range + the commitToHistory/captureUpdate grep results) — that is the evidence this decision rests on
    - .planning/phases/26-collaborative-board/26-RESEARCH.md — the `y-excalidraw` warning block (file list + byte sizes) and Pitfall 4 (`CaptureUpdateAction.NEVER`)
    - apps/board-web/tsconfig.json (Task 1) — `npx tsc --noEmit` is the automated proof for this task
  </read_first>
  <files>apps/board-web/src/yjs-binding/index.ts, apps/board-web/src/yjs-binding/diff.ts, apps/board-web/src/yjs-binding/helpers.ts, apps/board-web/src/yjs-binding/LICENSE, apps/board-web/src/yjs-binding/DECISION.md, apps/board-web/package.json</files>
  <action>
    Take the decision the spike evidence supports. VENDORING IS THE EXPECTED OUTCOME — a
    19-month-stale, 36-star package in the CRDT critical path is a liability, not a
    dependency. Only branch B if the spike proved the published build already calls
    `captureUpdate`.

    BRANCH A — VENDOR (expected):
      1. Obtain the three MIT source files plus the LICENSE. Preferred source, in order:
           (i)  `node_modules/y-excalidraw/src/{index,diff,helpers}.ts` if Task 1 found a src/ dir;
           (ii) raw.githubusercontent.com/RahulBadenkal/y-excalidraw/main/src/{index,diff,helpers}.ts
                plus .../main/LICENSE.
         Write them to `apps/board-web/src/yjs-binding/`. Copy the LICENSE file VERBATIM —
         attribution is a licence obligation, not a courtesy. Do not reformat it.
      2. At the top of `index.ts`, add an ENGLISH provenance header:
           // Vendored from y-excalidraw@2.0.12 (MIT, Copyright (c) 2024 Rahul R Badenkal).
           // See ./LICENSE for the original notice, and ./DECISION.md for why this is
           // vendored rather than depended on (xbrain Phase 26a, D-26-05).
           // LOCAL CHANGE: updateScene({ commitToHistory }) -> updateScene({ captureUpdate })
           // for @excalidraw/excalidraw 0.18.1.
      3. Apply the API fix everywhere the binding pushes a REMOTE change into the scene:
           import { CaptureUpdateAction } from "@excalidraw/excalidraw";
           excalidrawAPI.updateScene({ elements, captureUpdate: CaptureUpdateAction.NEVER });
         Every `commitToHistory: false` becomes `captureUpdate: CaptureUpdateAction.NEVER`.
         If a call site passed `commitToHistory: true`, it is a LOCAL edit and must NOT be
         converted to NEVER — use `CaptureUpdateAction.IMMEDIATELY`. Remote updates are
         always NEVER (Pitfall 4: otherwise a teammate's edit lands in YOUR Ctrl+Z stack and
         undo starts deleting other people's work).
         After the sweep, `commitToHistory` must not appear in any non-comment line.
      4. Fix any other type error `tsc --noEmit` reports against 0.18.1's own .d.ts — that
         typecheck IS the compatibility proof this plan exists to produce. Keep the public
         surface of the module unchanged (a constructor/factory taking the excalidrawAPI, a
         `Y.Array` of elements, and an awareness instance) so 26-04 can consume it.
      5. Do NOT add `y-excalidraw` to package.json, and remove the `--no-save` install:
         `rm -rf node_modules/y-excalidraw` (or re-run `npm ci`) so the committed manifest and
         the installed tree agree.

    BRANCH B — KEEP THE NPM PACKAGE (only if the spike disproved the break):
      1. `npm i -E y-excalidraw@2.0.12` so it is an EXACT pin in package.json.
      2. Create `apps/board-web/src/yjs-binding/index.ts` as a one-line re-export
         (`export * from "y-excalidraw";`) so 26-04 imports the same module path either way.
      3. Still create LICENSE (copy from node_modules/y-excalidraw/LICENSE) and DECISION.md.

    IN BOTH BRANCHES — write `apps/board-web/src/yjs-binding/DECISION.md`, English, and make
    it evidence-bearing rather than narrative. It MUST contain:
      - A first line stating exactly one of: `Decision: VENDORED` or `Decision: NPM DEPENDENCY`.
      - The `npm view y-excalidraw` output (version, license, peerDependencies) from Task 1.
      - The raw `commitToHistory` / `captureUpdate` grep results from Task 1.
      - The licence position: MIT, Copyright (c) 2024 Rahul R Badenkal, LICENSE preserved at
        ./LICENSE — satisfying the OSS-only + attribution constraint (CLAUDE.md).
      - Under BRANCH A: the list of call sites changed to `CaptureUpdateAction.NEVER` and the
        confirmation that `npx tsc --noEmit` passes against @excalidraw/excalidraw 0.18.1.
      - An "Upstream re-check" line: if PR #13 (the 0.18 peer bump) ever merges, this vendored
        copy can be revisited — but a re-check is a decision, not an automatic revert.
  </action>
  <verify>
    <automated>cd apps/board-web && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/board-web/src/yjs-binding/DECISION.md` exists and its first non-blank line matches exactly one of `Decision: VENDORED` / `Decision: NPM DEPENDENCY` — `grep -qE '^Decision: (VENDORED|NPM DEPENDENCY)$' apps/board-web/src/yjs-binding/DECISION.md`.
    - DECISION.md carries the spike evidence, not a summary of it: `grep -q 'peerDependencies\|\^0.17.6' apps/board-web/src/yjs-binding/DECISION.md` AND `grep -q 'commitToHistory' apps/board-web/src/yjs-binding/DECISION.md`.
    - The MIT notice is preserved: `grep -q 'MIT' apps/board-web/src/yjs-binding/LICENSE` AND `grep -q 'Rahul R Badenkal' apps/board-web/src/yjs-binding/LICENSE`.
    - BRANCH A only — the API break is fixed in code, checked on NON-COMMENT lines so the provenance header cannot satisfy its own gate: `grep -v '^\s*\(//\|\*\|/\*\)' apps/board-web/src/yjs-binding/index.ts | grep -c 'commitToHistory'` returns 0, AND `grep -q 'CaptureUpdateAction' apps/board-web/src/yjs-binding/index.ts` succeeds.
    - BRANCH B only — `grep -q '"y-excalidraw": "2.0.12"' apps/board-web/package.json` succeeds (exact pin, no caret).
    - `cd apps/board-web && npx tsc --noEmit -p tsconfig.json` exits 0 — the binding typechecks against @excalidraw/excalidraw 0.18.1's own .d.ts. This is the compatibility proof; a plan that ends without it has not answered D-26-05.
    - The manifest and the installed tree agree: `cd apps/board-web && npm ls --depth=0` reports no extraneous `y-excalidraw` under BRANCH A.
  </acceptance_criteria>
  <done>The D-26-05 question is answered with recorded evidence and the answer is EXECUTED in code: a binding module lives at `apps/board-web/src/yjs-binding/`, carries the upstream MIT notice, applies `CaptureUpdateAction.NEVER` to remote updates, and typechecks against Excalidraw 0.18.1. Wave 2 can build on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → this repo's build | Third-party code entering the CRDT critical path; a stale/abandoned package here is a correctness AND a supply-chain exposure |
| upstream MIT source → vendored copy | Copying source carries a licence obligation that a careless copy silently drops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `y-excalidraw@2.0.12` in the remote-update path (19 months stale, 36 stars, unmaintained) | mitigate | Vendor the ~23 KB into `apps/board-web/src/yjs-binding/` under our own review, pin every sibling dependency EXACTLY (no caret), and prove compatibility with `npx tsc --noEmit` against 0.18.1's real .d.ts rather than trusting a peer range. |
| T-26-02 | Repudiation | Vendored MIT source shipped without its notice — a licence violation in an OSS-only product | mitigate | `LICENSE` copied verbatim beside the code, a provenance header naming the upstream version + copyright holder, and a grep acceptance criterion asserting both. |
| T-26-03 | Tampering | Silent history corruption: a remote update captured into the local undo stack lets Ctrl+Z delete a teammate's work | mitigate | Every remote-update call site converted to `captureUpdate: CaptureUpdateAction.NEVER`; asserted by a comment-filtered grep so the provenance header cannot self-satisfy the gate. |
| T-26-04 | Denial of service | A caret range silently pulling an Excalidraw release with breaking `setActiveTool` / `setViewport` changes and bricking the board | mitigate | Exact pins enforced by an automated `node -e` manifest scan that fails on any `^` or `~`. |
</threat_model>

<verification>
- `cd apps/board-web && npm ls --depth=0` → the five runtime pins resolve, no UNMET.
- The manifest scan (`node -e` in Task 1's verify) exits 0 — no caret/tilde anywhere, and the three research-verified pins are byte-exact.
- `cd apps/board-web && npx tsc --noEmit -p tsconfig.json` exits 0 — the binding compiles against @excalidraw/excalidraw 0.18.1.
- `DECISION.md` records the branch taken WITH the spike's raw grep + `npm view` evidence.
- The upstream MIT LICENSE is present and names Rahul R Badenkal.
</verification>

<success_criteria>
- The y-excalidraw / Excalidraw-0.18.1 compatibility question is answered with evidence and the answer is executed in code, not deferred.
- `apps/board-web/` is a working Vite+React build target with exact pins and the `process.env.IS_PREACT` define; Node is build-time only.
- A binding module at `apps/board-web/src/yjs-binding/` typechecks against Excalidraw 0.18.1 and applies `CaptureUpdateAction.NEVER` to remote updates.
- No later wave inherits an unverified binding.
</success_criteria>

<output>
After completion, create `.planning/phases/26-collaborative-board/26-01-SUMMARY.md`.
Record explicitly: which branch was taken (VENDORED vs NPM DEPENDENCY), the raw spike
evidence, every call site changed to `CaptureUpdateAction.NEVER`, and the resolved exact
versions of react / react-dom / vite / typescript that npm wrote into package.json.
</output>
