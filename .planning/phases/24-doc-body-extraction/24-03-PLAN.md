---
phase: 24-doc-body-extraction
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - apps/memory-api/tests/test_doc_body_extraction.py
autonomous: true
requirements: [DOCBODY-01]

must_haves:
  truths:
    - "Against a REAL Postgres + REAL Qdrant (testcontainers) with NO OpenAI key, a real PDF whose BODY has a distinctive phrase (absent from caption/filename) is extracted, chunked, and each chunk embedded as a linked memory_item"
    - "The SAME proof holds for a real DOCX and a real text/markdown document"
    - "memory_search for the distinctive BODY phrase RETRIEVES the document keyless — proving the body, NOT the caption, is embedded — with the real local fastembed model (NOT mocked)"
    - "Guards proven live: no-text-layer PDF → no_text_layer flag + zero body chunks; oversized body → truncated + bounded chunk count; unknown mime → skipped, zero chunks; extraction failure → never raises (upload path unaffected)"
    - "SKIP=FAIL — the retrieval/ranking assertions never skip; only the docker/model-materialization guard may skip"
  artifacts:
    - path: "apps/memory-api/tests/test_doc_body_extraction.py"
      provides: "The gate-lesson proof: real-infra, non-mocked, keyless body→embed→retrieve + all guards"
      contains: "pytest.mark.integration"
      min_lines: 180
  key_links:
    - from: "tests/test_doc_body_extraction.py"
      to: "app/services/doc_body_ingest.extract_and_ingest_body"
      via: "direct call with a native provider built from testcontainers (injected provider — no MinIO needed)"
      pattern: "extract_and_ingest_body"
    - from: "tests/test_doc_body_extraction.py"
      to: "provider.search (real Qdrant)"
      via: "semantic search for the distinctive body phrase"
      pattern: "\\.search\\("
---

<objective>
Prove DOCBODY-01 the honest way. A "the body is extracted" claim proves nothing
until a real document's body is embedded and retrieved keyless. This plan is the
gate lesson — Phase 19's sibling: against a REAL Postgres + REAL Qdrant
(testcontainers) with NO OpenAI key, a real PDF / DOCX / text-md whose BODY carries
a distinctive phrase (not in the caption/filename) is run through
`extract_and_ingest_body`, its chunks land as linked memory_items, and
`memory_search` for the body phrase RETRIEVES the document — using the real local
fastembed model, NOT a mock. Plus every guard is asserted live.

Purpose: a caption-only test, or a mocked embedder, would pass even if the body was
never embedded. This gate cannot. SKIP=FAIL.

Output: `tests/test_doc_body_extraction.py` — the phase's binding acceptance proof.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-doc-body-extraction/24-CONTEXT.md
@.planning/phases/24-doc-body-extraction/24-01-SUMMARY.md
@.planning/phases/24-doc-body-extraction/24-02-SUMMARY.md

@apps/memory-api/tests/test_local_embeddings.py
@apps/memory-api/tests/conftest.py

<interfaces>
<!-- The exact patterns to mirror. Extracted from the live codebase — reuse them. -->

MIRROR test_local_embeddings.py wholesale — it is the Phase-19 gate this one is
modeled on. In particular reuse these patterns VERBATIM in shape:

  - `local_env` fixture: mutate the settings singleton →
      settings.EMBEDDINGS_PROVIDER = "local"; settings.OPENAI_API_KEY = "";
      settings.MEMORY_BACKEND = "native"; settings.EMBEDDING_CACHE_DIR = <tmp>;
      deps._memory_provider_singleton = None; restore in finally.
  - `local_model_ready` fixture: warm the real fastembed model once; the ONLY
      sanctioned skip (Docker up but model neither cached nor downloadable). The
      skip guard wraps ONLY model materialization — NEVER a ranking/linkage assert.
  - `_build_native_provider()`: build NativeProvider(pg_dsn, qdrant_url,
      embedder=get_embedder(), qdrant_api_key) directly from the testcontainers
      settings (NOT via the deps singleton) so its pool/client live in THIS test's loop.
  - `_aclose(provider)`: close pool + qdrant client at the end.
  - Fixtures used by every integration test: `pg_url, qdrant_url` (session-scoped,
      Docker-gated, from conftest.py — they spin real Postgres:17 + qdrant:v1.17.1
      and run `alembic upgrade head`).
  - Marker: `@pytest.mark.integration` on every real-infra test.

From app/services/doc_body_ingest.py (Plan 02) — call it DIRECTLY (no MinIO needed;
the media_key is just a string, the injected provider does the PG+Qdrant writes):
  await extract_and_ingest_body(provider=<native>, data=<bytes>, mime=<str>,
      filename=<str>, media_key=<str>, parent_item_id=<str>, team_scope=TEAM,
      project_scope=..., truth_level="WORKING", visibility="team",
      validation_status="pending") -> IngestResult(chunk_count, no_text_layer, skipped, truncated)

From native_provider.py:
  await provider.search(query, *, team_scope, limit=10) -> list[SearchHit]
    SearchHit.item is a MemoryItem; SearchHit.item.metadata has the link fields.

Doc generation (dev deps declared in Plan 01):
  - PDF with a text layer + an EMPTY (no-text-layer) PDF: reportlab
      (from reportlab.pdfgen import canvas; canvas.Canvas(buf); c.drawString(...);
       c.showPage(); c.save()  → buf.getvalue()). Empty PDF: showPage()+save() with
       NO drawString → a real PDF whose extract_text() is "".
  - DOCX: python-docx (docx.Document(); d.add_paragraph(<distinctive phrase>);
      d.save(buf) → buf.getvalue()).
  - text/markdown: just bytes.
  Import reportlab/docx LAZILY inside the generator helpers so unrelated collection
  is unaffected on a host without those wheels (integration tests run under Docker/CI Linux).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The gate — real PDF + DOCX + text/md body → embedded → retrieved keyless (non-mocked)</name>
  <read_first>
    - apps/memory-api/tests/test_local_embeddings.py (mirror local_env, local_model_ready, _build_native_provider, _aclose, the @pytest.mark.integration ranking assert; note the 384-dim keyless model)
    - apps/memory-api/tests/conftest.py:67-159 (pg_url + qdrant_url session fixtures — real PG:17 + qdrant:v1.17.1, Docker-gated)
    - apps/memory-api/app/services/doc_body_ingest.py (extract_and_ingest_body signature + IngestResult)
    - packages/memory-models/xbrain_memory/providers/native_provider.py:89-185,208-299 (upsert embeds content; search over real Qdrant)
  </read_first>
  <files>apps/memory-api/tests/test_doc_body_extraction.py</files>
  <action>
    Create `apps/memory-api/tests/test_doc_body_extraction.py`, mirroring the discipline
    and fixtures of test_local_embeddings.py. Header docstring must state: real PG + real
    Qdrant, NO OpenAI key, real local fastembed (NOT mocked), SKIP=FAIL.

    Setup (copy the shapes from test_local_embeddings.py):
    - `local_env` fixture (force local keyless + native + reset provider singleton + tmp cache; restore in finally).
    - `local_model_ready` fixture (warm the real model once; skip ONLY on model-unavailable — never on an assertion).
    - `_build_native_provider()` + `_aclose(provider)` helpers.
    - Fixed `TEAM = "phase24-docbody"`, `PROJECT = "gate"`.
    - Distinctive body phrases that DO NOT appear in any caption/filename, e.g.:
        PDF_PHRASE  = "the peregrine falcon dives at three hundred kilometres per hour"
        DOCX_PHRASE = "the mitochondrion is the powerhouse of the quarterly budget"
        TEXT_PHRASE = "aubergines ferment best at eleven degrees in a clay amphora"
      (Choose vivid, unique phrases so a semantic hit is unambiguous.)
    - Doc generator helpers: `_make_pdf(text) -> bytes` (reportlab, lazy import),
      `_make_empty_pdf() -> bytes` (reportlab, blank page, no drawString → no text layer),
      `_make_docx(text) -> bytes` (python-docx, lazy import), text/md are literal bytes.

    Core gate tests (each `@pytest.mark.integration`, each uses pg_url, qdrant_url, local_model_ready):
    1. `test_pdf_body_extracted_embedded_retrieved_keyless`:
        - ensure_collections(); provider = _build_native_provider().
        - `data = _make_pdf(f"Meeting notes. {PDF_PHRASE}. End.")`; caption/filename carry
          NO occurrence of PDF_PHRASE (e.g. filename="q3-notes.pdf", and DON'T pass the
          phrase as any caption — the service takes no caption anyway).
        - `res = await extract_and_ingest_body(provider=provider, data=data,
            mime="application/pdf", filename="q3-notes.pdf", media_key="media/T/p.pdf",
            parent_item_id="PARENT-PDF", team_scope=TEAM, project_scope=PROJECT,
            truth_level="WORKING")`.
        - assert res.chunk_count >= 1 and res.no_text_layer is False.
        - `hits = await provider.search(PDF_PHRASE, team_scope=TEAM, limit=5)`.
        - assert hits, and hits[0].item.metadata["parent_item_id"] == "PARENT-PDF",
          and hits[0].item.metadata["media_key"] == "media/T/p.pdf",
          and hits[0].item.source == "upload:body",
          and hits[0].item.team_scope == TEAM,
          and hits[0].item.truth_level == TruthLevel.WORKING.
          (The BODY phrase — not the caption/filename — retrieved the doc. Non-mocked.)
        - _aclose(provider) in finally.
    2. `test_docx_body_extracted_embedded_retrieved_keyless` — same shape with `_make_docx(DOCX_PHRASE)`,
       mime = the exact DOCX mime, parent_item_id="PARENT-DOCX"; assert retrieval of DOCX_PHRASE.
    3. `test_text_markdown_body_extracted_embedded_retrieved_keyless` — bytes
       `f"# Recipe\n\n{TEXT_PHRASE}\n".encode()`, mime="text/markdown", parent_item_id="PARENT-MD";
       assert retrieval of TEXT_PHRASE.

    Each retrieval assertion MUST run the real embedder (no respx, no mock on this path) —
    that is the gate lesson: a fake vector could not rank the matching body chunk first.
  </action>
  <verify>
    <automated>cd apps/memory-api && python -m pytest tests/test_doc_body_extraction.py -q -k "extracted_embedded_retrieved" -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@pytest.mark.integration' apps/memory-api/tests/test_doc_body_extraction.py` >= 3 (one per format) — count with `grep -c` on the marker line.
    - The file contains NO `respx`, NO `unittest.mock`, NO `MagicMock`, NO monkeypatched embedder on the retrieval path — `grep -Eqv` check: `grep -E 'respx|MagicMock|mock' apps/memory-api/tests/test_doc_body_extraction.py` returns nothing (the embedder is real).
    - Each core test calls `extract_and_ingest_body(` and then `provider.search(` for the corresponding *_PHRASE, and asserts `hits[0].item.metadata["parent_item_id"]` equals the doc's parent id and `hits[0].item.metadata["media_key"]` is present.
    - When Docker + the fastembed model are available, `python -m pytest tests/test_doc_body_extraction.py -k "extracted_embedded_retrieved"` PASSES (all three formats retrieve their body phrase). If Docker is unavailable the tests SKIP via the conftest guard — but a wrong/empty retrieval FAILS (never skips).
    - The caption/filename never contains any *_PHRASE (grep the phrases → they appear only in the generated body bytes, never as a filename/caption literal).
  </acceptance_criteria>
  <done>Three integration tests prove, against real PG + real Qdrant with no OpenAI key and the real local embedder, that a PDF/DOCX/text-md BODY (a distinctive phrase absent from caption/filename) is extracted, embedded as linked chunk memory_items, and retrieved by memory_search — SKIP=FAIL on the ranking assertions.</done>
</task>

<task type="auto">
  <name>Task 2: The guard proofs — no_text_layer, truncation/chunk-cap, unknown mime, fail-soft (live)</name>
  <read_first>
    - apps/memory-api/tests/test_doc_body_extraction.py (Task 1 — reuse its fixtures/helpers/provider)
    - apps/memory-api/app/config.py (DOCBODY_MAX_TOTAL_CHARS / DOCBODY_MAX_CHUNKS — the caps to assert against)
    - apps/memory-api/app/services/doc_body_ingest.py (IngestResult fields the asserts read)
  </read_first>
  <files>apps/memory-api/tests/test_doc_body_extraction.py</files>
  <action>
    Add guard tests to the SAME file (each `@pytest.mark.integration`, real provider so the
    "no chunks were written" claims are proven against real PG+Qdrant, not just the return value):

    4. `test_no_text_layer_pdf_flags_and_writes_no_chunks`:
        - `data = _make_empty_pdf()` (real PDF, no text layer → extract_text()=="").
        - `res = await extract_and_ingest_body(provider=..., data=data, mime="application/pdf",
            filename="scan.pdf", media_key="media/T/scan.pdf", parent_item_id="PARENT-SCAN",
            team_scope=TEAM, project_scope=PROJECT, truth_level="WORKING")`.
        - assert res.no_text_layer is True and res.chunk_count == 0.
        - assert `await provider.search("scan.pdf", team_scope=TEAM, limit=5)` returns NO hit
          whose metadata.parent_item_id == "PARENT-SCAN" (no empty body chunk was embedded).
    5. `test_oversized_body_truncated_and_chunk_count_bounded`:
        - Build a body far larger than DOCBODY_MAX_TOTAL_CHARS (e.g. the TEXT_PHRASE repeated
          to exceed the char cap), mime="text/plain", parent_item_id="PARENT-BIG".
        - assert res.truncated is True and res.chunk_count <= settings.DOCBODY_MAX_CHUNKS.
        - (Optionally temporarily lower settings.DOCBODY_MAX_TOTAL_CHARS / DOCBODY_MAX_CHUNKS
          inside the test via the local_env-style mutate+restore so the bound is reached with a
          modest body — keep it deterministic.)
    6. `test_unknown_mime_skipped_no_chunks`:
        - `data = b"\x00\x01\x02binary-blob"`, mime="application/octet-stream".
        - assert res.skipped is True and res.chunk_count == 0; no chunk for its parent id in search.
    7. `test_extraction_failure_is_fail_soft`:
        - `data = b"%PDF-1.4 not actually a valid pdf body"`, mime="application/pdf".
        - assert the call does NOT raise (wrap in a try to fail explicitly if it does) and
          res.chunk_count == 0. This is the "extraction failure never breaks the upload" proof
          at the service seam (media.py's create_task swallows the same failure).

    Reuse the Task-1 provider build/close helpers; close the provider in finally in each test.
  </action>
  <verify>
    <automated>cd apps/memory-api && python -m pytest tests/test_doc_body_extraction.py -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - Four guard tests exist: no_text_layer (flag + 0 chunks), oversized (truncated + chunk_count <= DOCBODY_MAX_CHUNKS), unknown-mime (skipped + 0 chunks), corrupt-pdf (no raise + 0 chunks).
    - `grep -q 'no_text_layer' apps/memory-api/tests/test_doc_body_extraction.py` and the test asserts `res.no_text_layer is True` AND that no chunk for PARENT-SCAN is retrievable (empty PDF wrote zero body chunks).
    - The truncation test references `settings.DOCBODY_MAX_CHUNKS` in its bound assertion.
    - The fail-soft test asserts the call returns normally on corrupt PDF bytes (does not raise).
    - Full file passes when Docker + model available: `python -m pytest tests/test_doc_body_extraction.py -q` → all integration tests PASS (or cleanly SKIP only when Docker/model is absent; assertions never skip).
  </acceptance_criteria>
  <done>Four live guard tests prove no_text_layer flagging with zero chunks, truncation with a bounded chunk count, unknown-mime skip, and corrupt-doc fail-soft — all against the real provider, completing the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real PG + real Qdrant | The gate exercises the true write/read path (no mocks) so a broken integration cannot pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | Repudiation | a green suite that does not actually prove the body was embedded (caption-only or mocked) | mitigate | The retrieval query is the distinctive BODY phrase (absent from caption/filename); the embedder is the REAL local fastembed (no respx/mock on the path); SKIP=FAIL — only the docker/model guard may skip, never a ranking/linkage assert. |
| T-24-12 | Information disclosure | cross-team leakage of body chunks | mitigate | Every gate assert checks `hits[0].item.team_scope == TEAM`; search is team-scoped, proving chunks are isolated to the upload's team. |
| T-24-13 | Denial of Service | the guard behaviors (truncation, chunk cap, no-text-layer, malformed) regress unnoticed | mitigate | Guard tests assert bounded chunk_count against DOCBODY_MAX_CHUNKS, no_text_layer→0 chunks, unknown-mime→0 chunks, and corrupt-doc→no raise — locking the DoS mitigations from Plans 01/02 in a live test. |
</threat_model>

<verification>
- `python -m pytest tests/test_doc_body_extraction.py -q` (Docker up + model available) → all integration tests PASS; assertions never SKIP.
- No mock/respx anywhere in the file; retrieval uses the real 384-dim keyless embedder.
- The three format tests retrieve their distinctive BODY phrase; the four guard tests hold.
</verification>

<success_criteria>
- The BODY (not the caption) of a real PDF, DOCX, and text/markdown is provably embedded and retrieved keyless against real Postgres + Qdrant.
- Every guard (no_text_layer, truncation + chunk cap, unknown-mime skip, extraction-failure fail-soft) is proven live.
- SKIP=FAIL — a mocked or caption-only outcome cannot satisfy this gate.
</success_criteria>

<output>
After completion, create `.planning/phases/24-doc-body-extraction/24-03-SUMMARY.md`
(record whether the suite ran green under Docker or was environment-skipped, and where —
CI/VM/local).
</output>
