---
phase: 20-extension-chat-shadcn
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - chrome-extension/popup.css
  - chrome-extension/popup.html
autonomous: true
requirements: [PKG-02]
user_setup: []

must_haves:
  truths:
    - "The header renders as the mockup: a square group avatar (--primary), team name/selector, a presence dot in mono --muted-fg, and ghost icon buttons — no navigation added."
    - "The composer renders as the mockup: a bordered input (radius 0, --ring focus ring, --input border) with a --primary Send button and the clip/file button retained."
    - "The connection card and clip overlay render monochrome, radius 0, with visible focus states and reduced-motion respected."
    - "The team picker (#teamSelector) stays in the header (group picker, not navigation)."
  artifacts:
    - path: "chrome-extension/popup.css"
      provides: "Restyled .xb-header / .xb-team-select / .xb-presence / .xb-icon-btn / composer / connection-card / clip-overlay rules consuming shadcn tokens"
      contains: ".xb-header"
    - path: "chrome-extension/popup.html"
      provides: "Square group-avatar element in the header; existing ids preserved; product strings English"
  key_links:
    - from: "chrome-extension/popup.css shell rules"
      to: "shadcn tokens (:root)"
      via: "var(--primary|--border|--input|--ring|--muted-fg|--radius)"
      pattern: "var\\(--(primary|border|input|ring|muted-fg|radius)\\)"
    - from: ".xb-composer-pill:focus-within"
      to: "--ring"
      via: "focus ring on composer"
      pattern: "focus-within"
---

<objective>
Restyle the popup's app "chrome" — header, connection card, composer, and clip overlay — to the shadcn Neutral mockup, consuming the Plan-01 tokens. This is the frame around the message thread; the thread itself is Plan 03.

Purpose: Deliver SC#1 (Neutral header + composer) and SC#3's chrome-level accessibility (focus states, reduced motion) while preserving the single-group-chat / no-navigation structure.
Output: Token-driven, monochrome, radius-0 header/composer/connection-card/clip-overlay with a square group avatar, keeping every popup.js id/class.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-extension-chat-shadcn/20-CONTEXT.md
@.planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html

<interfaces>
<!-- Header/composer ids popup.js binds (MUST NOT rename) -->
#teamSelector #presenceBadge #presenceCount #btn-add-to-memory #btn-settings #btn-open-librechat
#btn-clip #file-picker #composer-input #btn-send
<!-- .xb-composer is queried by popup.js: document.querySelector(".xb-composer").style.display and prepend(.xb-upload-error) -->
<!-- Mockup targets: .head/.grp-av/.grp-meta/.presence/.iconbtn (header); .composer/.field/.send (composer) -->
<!-- CONTEXT discretion: emoji glyphs (⚙️/💬/📎) may become inline SVG or stay — pick per effort; keep aria-labels. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the header — square group avatar, team selector, presence, ghost icon buttons</name>
  <files>chrome-extension/popup.css, chrome-extension/popup.html</files>
  <read_first>
    - chrome-extension/popup.html (lines 96-113 — header markup + the ids to preserve; Plan 01 added the theme toggle here)
    - chrome-extension/popup.css (lines 35-108 — current .xb-header / .xb-team-select / .xb-presence / .xb-icon-btn rules)
    - .planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html (lines 50-62, 160-167 — .head/.grp-av/.grp-meta/.presence/.iconbtn/.actions)
  </read_first>
  <action>
    Restyle the header to the mockup, keeping ids:
    - `.xb-header`: bottom border `--border`, background `--bg`, padding per mockup `.head` (~.7rem .8rem), flex align center.
    - Add a square group avatar element in popup.html `.xb-header-left` BEFORE `#teamSelector` (mockup `.grp-av`): `&lt;span class="xb-group-avatar" aria-hidden="true"&gt;&lt;/span&gt;`. Style `.xb-group-avatar` as a 34px square (radius 0), `--primary` bg, `--primary-fg` text, grid-centered, font-weight 600. popup.js MAY later set its initials; for now leave it CSS-styled (empty or a static glyph). Replace the current `🧠` `.xb-logo` with this square avatar (prefer removing the emoji for the Neutral look — CONTEXT discretion).
    - `.xb-team-select` (`#teamSelector`): style into the header cleanly — transparent/`--card` bg, `--border`, radius 0, `--fg` text, `--ring` on focus. Keep max-width/ellipsis. This is the group picker (allowed — not navigation).
    - `.xb-presence` / `.xb-presence-dot`: mono `--muted-fg`, the dot `currentColor` (mockup `.presence .dot`) — drop the green success color and its glow for the monochrome preset.
    - `.xb-icon-btn` (⚙️/💬/add-to-memory) → mockup `.iconbtn`: 32px square, radius 0, transparent bg, `--muted-fg`; hover `--muted` bg + `--fg`; `:focus-visible` outline `2px solid --ring`. `.xb-text-btn` ("add to memory") keeps mono/compact treatment on the same tokens.
    Do NOT rename ids/classes. Migrate any French string you touch to English (none expected).
  </action>
  <acceptance_criteria>
    - `grep -E "\.xb-group-avatar" chrome-extension/popup.css` matches with `background: var(--primary)` and radius 0.
    - `grep -n "class=\"xb-group-avatar\"" chrome-extension/popup.html` matches, placed inside `.xb-header-left`.
    - `.xb-icon-btn:focus-visible` rule exists (`grep -E "\.xb-icon-btn:focus-visible" chrome-extension/popup.css`).
    - `grep -E "\.xb-presence-dot" chrome-extension/popup.css` no longer references `--xb-success` / green glow (monochrome).
    - All header ids still present: `node -e "const h=require('fs').readFileSync('chrome-extension/popup.html','utf8'); ['teamSelector','presenceBadge','presenceCount','btn-add-to-memory','btn-settings','btn-open-librechat'].forEach(i=>{if(!h.includes('id=\"'+i+'\"')){console.error('missing',i);process.exit(1)}}); console.log('header ids ok')"`.
    - `node chrome-extension/tests/test_popup_contract.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node chrome-extension/tests/test_popup_contract.mjs && node -e "const h=require('fs').readFileSync('chrome-extension/popup.html','utf8'); ['teamSelector','btn-settings','btn-open-librechat'].forEach(i=>{if(!h.includes('id=\"'+i+'\"'))process.exit(1)}); console.log('ok')"</automated>
  </verify>
  <done>The header matches the mockup — square --primary group avatar, clean token-styled team selector, mono presence, ghost icon buttons with visible focus — no navigation added and every popup.js id preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the composer, connection card, and clip overlay to tokens</name>
  <files>chrome-extension/popup.css, chrome-extension/popup.html</files>
  <read_first>
    - chrome-extension/popup.css (lines 110-187 connection card; 369-467 composer; 470-656 clip overlay + buttons)
    - chrome-extension/popup.html (lines 119-139 connection card; 152-169 composer; 172-235 clip overlay)
    - .planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html (lines 132-146 composer .field/.send; 40, 61 focus-visible patterns)
  </read_first>
  <action>
    Composer (`.xb-composer`, `.xb-composer-pill`, `#composer-input`, `.xb-clip-btn`, `.xb-send-btn`):
    - `.xb-composer-pill` → mockup `.composer .field`: `--bg` inner, `1px solid var(--input)`, radius 0, and on `:focus-within` set `border-color: var(--ring)` + `box-shadow: 0 0 0 3px color-mix(in srgb, var(--ring) 30%, transparent)`.
    - `.xb-send-btn` → mockup `.send`: `--primary` bg, `--primary-fg`, radius 0, `1px solid var(--primary)`, `:focus-visible` outline `--ring`; keep the `↑`/existing glyph or swap to the mockup send SVG (discretion). Disabled state uses `--muted`/`--muted-fg`.
    - `.xb-clip-btn`: ghost token button (like `.iconbtn`), radius 0; keep `#btn-clip`/`#file-picker` wiring intact.
    - `#composer-input`: transparent, `--fg`, `--sans`, placeholder `--muted-fg`; keep the min/max-height + `.is-overflowing` behavior untouched.
    Connection card (`.xb-card`, `.connect-btn-github`, `.connect-btn-secondary`, `.xb-signin-divider`, `.xb-subcard`, `.link-btn`, `#connect-status`, `.connect-btn`): monochrome + radius 0. The GitHub primary button → `--primary` bg / `--primary-fg`; the Google secondary → `--secondary`/`--border` outline. `#connect-status` success/error keep `--destructive` for error but drop blue/green; loading uses `--muted`. `.connect-btn` (used by renderEmptyTeams) restyled to `--primary`. NOTE: the `.connect-btn-github`/`.connect-btn-secondary`/`.xb-signin-divider` rules currently live in popup.html's inline &lt;style&gt; — restyle them there to tokens (radius 0, monochrome) or move them into popup.css; either way they must consume the shadcn tokens, not hardcoded `#24292f`.
    Clip overlay (`.xb-overlay*`, `.xb-field`, `.xb-clip-preview*`, `.xb-radio-pill`, `.xb-checkbox-row`, `.xb-btn-primary/secondary`, `#clip-status`): radius 0, `--card`/`--border`/`--muted` surfaces, `--ring` focus, `.xb-radio-pill:has(input:checked)` → `--primary`/`--primary-fg` (monochrome selection); `.xb-btn-primary` → `--primary`. Transitions are fine; no keyframe animation here. Keep every overlay id and `input[name="clipTruthLevel"]`.
    Do NOT rename ids/classes; keep all English strings.
  </action>
  <acceptance_criteria>
    - `grep -E "\.xb-composer-pill:focus-within" chrome-extension/popup.css` sets `border-color: var(--ring)`.
    - `grep -E "\.xb-send-btn" chrome-extension/popup.css` uses `background: var(--primary)` and `color: var(--primary-fg)`.
    - `#24292f` count across popup.html + popup.css (comments stripped) == 0 (GitHub button tokenised).
    - `grep -E "\.xb-radio-pill:has\(input:checked\)" chrome-extension/popup.css` references `var(--primary)`.
    - `grep -E "input\[name=\"clipTruthLevel\"\]" chrome-extension/popup.html` still yields the 4 radios; the clip-overlay/composer ids all remain (see verify).
    - `node chrome-extension/tests/test_popup_contract.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node chrome-extension/tests/test_popup_contract.mjs && node -e "const s=x=>require('fs').readFileSync(x,'utf8').replace(/\/\*[\s\S]*?\*\//g,''); if(/#24292f/.test(s('chrome-extension/popup.css')+s('chrome-extension/popup.html'))){console.error('hardcoded github color remains');process.exit(1)} const h=require('fs').readFileSync('chrome-extension/popup.html','utf8'); ['clip-overlay','clip-project','clip-preview-mode','clip-preview-detail','clip-status','btn-clip-close','btn-clip-cancel','btn-clip-send','clip-use-defaults','composer-input','btn-send','btn-clip','file-picker'].forEach(i=>{if(!h.includes('id=\"'+i+'\"')){console.error('missing',i);process.exit(1)}}); console.log('ok')"</automated>
  </verify>
  <done>Composer, connection card, and clip overlay render monochrome / radius 0 on the shadcn tokens with a --ring focus ring and --primary Send; the GitHub button is tokenised (no hardcoded hex); every id, class, and truth-level radio is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed composer text → DOM | text flows to popup.js send path (rendering handled in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | Tampering | header/composer markup edits | mitigate | Restyle is CSS + additive markup only; no popup.js id/event handler renamed — the contract test asserts every bound id survives |
| T-20-02-02 | Information disclosure | GitHub-button styling | accept | Cosmetic tokenisation of an existing button; no new data path or request introduced |
</threat_model>

<verification>
- `node chrome-extension/tests/test_popup_contract.mjs` passes (all header/composer/overlay ids + token contract intact).
- No hardcoded `#24292f` (GitHub) color remains; composer focus ring uses `--ring`; Send uses `--primary`.
- `node chrome-extension/tests/run_tests.mjs` green.
</verification>

<success_criteria>
- The app frame (header, composer, connection card, clip overlay) renders in shadcn Neutral (monochrome, radius 0, --ring focus) with a square group avatar and the single-group-chat structure intact; all popup.js selectors preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/20-extension-chat-shadcn/20-02-SUMMARY.md`.
</output>
