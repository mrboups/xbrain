---
phase: 20-extension-chat-shadcn
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - chrome-extension/popup.css
  - chrome-extension/popup.js
  - chrome-extension/chat_stream.js
  - chrome-extension/tests/test_chat_stream.mjs
  - chrome-extension/tests/test_popup_contract.mjs
autonomous: true
requirements: [PKG-02]
user_setup: []

must_haves:
  truths:
    - "Own messages render as a --primary bubble (radius 0); others' messages as a --muted bubble with --border; avatars are square."
    - "The agent reply renders as a --card block with a mono uppercase 'agent · from your brain' label, and — when the agent carries brain context — a <details> sources summary; truth-level chips are styled monochrome (validated = filled --primary, working = outline)."
    - "A saved-to-brain badge (mockup .savetag, mono micro-text, --secondary) renders on messages that carry a real brain-save signal (media/document indexed)."
    - "Meta/timestamps/presence use --mono / --muted-fg; day separators render between messages from different days; media thumbnails and file chips render on the tokens."
    - "Rendering stays XSS-safe (createElement/textContent, no innerHTML with message data) and no popup.js selector/data-attribute broke."
  artifacts:
    - path: "chrome-extension/popup.css"
      provides: "Message-thread rules restyled to the mockup: .xb-msg own/others/agent bubbles, square avatars, mono meta, agent-bubble card + who label + .sources <details>, monochrome truth-level chip classes, saved-to-brain badge, day separator, media thumb/file-chip"
      contains: ".xb-msg-bubble"
    - path: "chrome-extension/popup.js"
      provides: "Minimal render glue: agent 'from your brain' label + sources summary from existing metadata (memory_items / metadata.sources future-proof), saved-to-brain badge from existing signals (media), day separators from created_at — no fabricated data, no backend change"
      contains: "from your brain"
    - path: "chrome-extension/chat_stream.js"
      provides: "Pure, node-tested helpers: brainSummaryLabel, savedToBrainLabel, sameDay"
      exports: ["brainSummaryLabel", "savedToBrainLabel", "sameDay"]
    - path: "chrome-extension/tests/test_popup_contract.mjs"
      provides: "Extended class contract: truth-level chip + saved-to-brain + agent-bubble + day-separator classes asserted against the mockup spec + XSS guard"
  key_links:
    - from: "chrome-extension/popup.js buildBubbleNode (is-self/is-user/is-agent)"
      to: "chrome-extension/popup.css .xb-msg bubble rules"
      via: "className `xb-msg ${bubbleClass}` consumed by token rules"
      pattern: "xb-msg (is-self|is-user|is-agent)"
    - from: "agent metadata.memory_items"
      to: ".xb-msg-sources <details> summary"
      via: "render 'N sources from the brain' from existing count (no fabrication)"
      pattern: "memory_items"
    - from: "message metadata.media"
      to: ".xb-msg-savetag"
      via: "saved-to-brain badge on real indexed attachments"
      pattern: "metadata\\.media|savetag|saved"
---

<objective>
Restyle the message thread to the shadcn Neutral mockup and wire the mockup's rich elements to REAL existing data only — own/others/agent bubbles, mono meta, the agent "from your brain" block with a `<details>` sources summary + monochrome truth-level chip styling, a saved-to-brain badge on genuinely-indexed attachments, day separators, and token-styled media. This is a restyle plus minimal render glue; it adds NO backend change and fabricates NO data.

Purpose: Deliver SC#2/#3 for the thread — the message rows match the mockup and every existing behavior (own/others/agent text, provenance, media/clip attachments, streaming) renders correctly on the tokens.
Output: Token-driven message CSS matching the mockup + minimal, XSS-safe popup.js render additions sourced only from data the backend already sends.

Scope note (read before coding): the backend `team_chat.py` message payload today is `{id, kind, content, created_at, routed_via, metadata}` where `metadata.media` is the only rich field, and the agent stream/metadata carries `memory_items` (count) + `memory_cached` (bool) — it does NOT emit per-source rows, per-source truth levels, or a per-message saved-to-brain flag. Therefore: deliver the truth-level chip and sources styling as the CONTEXT-locked design system, populate the agent block from `memory_items` (a real count), populate the saved-to-brain badge from the real media/indexed signal, and render individual source rows ONLY if `metadata.sources` is present (future-proof — do NOT fabricate them). Per-source truth-level population is a documented backend follow-up, not this phase.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-extension-chat-shadcn/20-CONTEXT.md
@.planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html

<interfaces>
<!-- popup.js render contract (buildBubbleNode / renderMediaInto) — classes it EMITS (MUST keep, restyle only): -->
xb-msg  is-self|is-user|is-agent   xb-msg-avatar   xb-msg-meta   xb-msg-author(.is-agent)
xb-msg-time   xb-msg-provenance(.via-promax|.via-api)   xb-msg-bubble(.streaming)
xb-msg-caption   xb-msg-thumb   xb-msg-file-chip   [data-msg-id]
<!-- Agent stream frames deliver text only; the persisted agent row metadata has memory_items + memory_cached. -->
<!-- Message payload (team_chat.py _serialize_message): id, team_id, author_user_id, agent_name, kind, content, created_at, routed_via, metadata{ media{item_id,mime,size,filename,url} } -->
<!-- Mockup targets: .row(.mine)/.bubble/.avatar/.stack/.name/.meta (rows); .agent-row/.agent-av/.agent-bubble/.who/.sources/.src/.chip.validated/.chip.working; .savetag; .daysep; .att-img/.file -->
<!-- chat_stream.js: bubbleClass(msg,selfId)->is-self/is-user/is-agent; authorLabel; formatRelative; provenanceLabel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle message rows — own/others bubbles, square avatars, mono meta, media, streaming caret</name>
  <files>chrome-extension/popup.css</files>
  <read_first>
    - chrome-extension/popup.js (lines 460-633 — buildBubbleNode, renderMediaInto, the exact classes emitted; do not change class names here)
    - chrome-extension/chat_stream.js (bubbleClass / authorLabel / formatRelative / provenanceLabel — what data drives each class)
    - chrome-extension/popup.css (lines 244-367 message rows; 677-735 media attachments)
    - .planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html (lines 64-97 rows/avatars/meta/att-img/file; 78-84 bubble; 124-130 typing dots)
  </read_first>
  <action>
    Restyle the message thread CSS to the mockup, keeping every `.xb-*` class:
    - `.xb-msg` container: switch the layout to the mockup's row/stack feel (avatar + stack), gap per mockup `.row`, `margin-top` grouping. Keep the existing grid-columns approach OR adopt flex — but the emitted class names stay identical.
    - `.xb-msg.is-self` → mockup `.row.mine`: avatar on the right (or hidden), `.xb-msg-bubble` becomes a `--primary` bg / `--primary-fg` bubble, radius 0, right-aligned, max-width ~78-80%. Remove the old blue `--xb-accent-bg` rounded-corner bubble.
    - `.xb-msg.is-user` → mockup others' `.row`: `.xb-msg-bubble` = `--muted` bg, `--fg`, `1px solid var(--border)`, radius 0.
    - `.xb-msg-avatar`: 26-28px SQUARE (radius 0 — the Neutral signal), `--secondary` bg / `--secondary-fg`, `1px solid var(--border)`; the is-self/is-user/is-agent avatar color variants restyled monochrome (agent avatar = `--primary`/`--primary-fg`, mockup `.agent-av`).
    - `.xb-msg-meta` / `.xb-msg-time` / `.xb-msg-author`: `--mono` for time/meta, `--muted-fg`; author `--fg` weight 600. `.xb-msg-provenance` (via Pro/Max / via team API) → mono micro badge on `--secondary`/`--border` (drop green/blue).
    - `.xb-msg-thumb` (mockup `.att-img`) and `.xb-msg-file-chip` (mockup `.file`): radius 0, `--card`/`--border`/`--muted`, mono file meta. `.xb-msg-caption`: `--muted-fg`.
    - Streaming caret `.xb-msg-bubble.streaming::after`: keep the caret but recolor to `--primary`; wrap the blink keyframe usage in `@media (prefers-reduced-motion: reduce){ … animation:none }`.
    - Add `:focus-visible` outline (`2px solid var(--ring)`) for any focusable message element (e.g. the file-chip link `.xb-msg-file-chip`).
    Do NOT edit popup.js in this task. Do NOT rename classes.
  </action>
  <acceptance_criteria>
    - `grep -E "\.xb-msg\.is-self \.xb-msg-bubble" chrome-extension/popup.css` uses `var(--primary)` bg + `var(--primary-fg)`.
    - `grep -E "\.xb-msg\.is-user \.xb-msg-bubble" chrome-extension/popup.css` uses `var(--muted)` + `1px solid var(--border)`.
    - `.xb-msg-avatar` is square (no `border-radius: 50%`).
    - `grep -E "prefers-reduced-motion" chrome-extension/popup.css` matches (guards the caret/rise motion).
    - `grep -E "\.xb-msg-file-chip:focus-visible|\.xb-msg-thumb:focus-visible" chrome-extension/popup.css` matches at least one focus-visible rule for a focusable message element.
    - `node --check chrome-extension/popup.js` passes (unchanged) and `node chrome-extension/tests/test_popup_contract.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node chrome-extension/tests/test_popup_contract.mjs && node -e "const c=require('fs').readFileSync('chrome-extension/popup.css','utf8'); if(/\.xb-msg-avatar[^}]*border-radius:\s*50%/.test(c)){console.error('avatar still round');process.exit(1)} console.log('square avatar ok')"</automated>
  </verify>
  <done>Own messages are --primary bubbles, others' are --muted+border, avatars are square, meta/time/provenance are mono monochrome, media renders on tokens, and the streaming caret respects reduced-motion — all with the original .xb-* class names intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agent "from your brain" block, saved-to-brain badge, truth-level chips, day separators (real data only)</name>
  <files>chrome-extension/chat_stream.js, chrome-extension/tests/test_chat_stream.mjs, chrome-extension/popup.js, chrome-extension/popup.css, chrome-extension/tests/test_popup_contract.mjs</files>
  <read_first>
    - chrome-extension/popup.js (lines 401-558 handlePublication/renderAgentBubble/buildBubbleNode; 351-399 history render loop for day-separator insertion point)
    - chrome-extension/chat_stream.js (existing pure helpers — add the new ones alongside)
    - apps/memory-api/app/routes/team_chat.py (lines 80-110 _serialize_message — confirm the payload has no sources/truth_level; metadata.media only)
    - apps/memory-api/app/services/team_chat_agent.py (lines 180-300 — agent metadata carries memory_items/memory_cached, no sources)
    - .planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html (lines 99-122 savetag/agent-bubble/who/sources/chip; 66 daysep)
  </read_first>
  <behavior>
    <!-- Pure decision helpers in chat_stream.js — node-testable before wiring the DOM. -->
    - brainSummaryLabel({memory_items:2}) === "2 sources from the brain"
    - brainSummaryLabel({memory_items:1}) === "1 source from the brain"
    - brainSummaryLabel({memory_items:0}) === null                       (no <details> when nothing was used)
    - savedToBrainLabel({metadata:{media:{mime:"application/pdf"}}}) === "saved to brain · document indexed"
    - savedToBrainLabel({metadata:{media:{mime:"image/png"}}}) === "saved to brain · image indexed"
    - savedToBrainLabel({metadata:{}}) === null                          (no fabricated badge on plain text)
    - sameDay("2026-07-18T09:00:00Z","2026-07-18T23:00:00Z") === true ; different date === false
  </behavior>
  <action>
    Add the mockup's brain-aware elements, sourced ONLY from data the backend already sends. First add pure helpers to chat_stream.js and cover them in test_chat_stream.mjs (RED→GREEN): `brainSummaryLabel(agentMsgMeta)`, `savedToBrainLabel(msg)`, `sameDay(isoA,isoB)` per &lt;behavior&gt;.
    Then wire popup.js (XSS-safe: createElement/textContent only — never innerHTML with message content):
    - Agent block: in buildBubbleNode for `kind === "agent"`, wrap the body in the mockup's `.agent-bubble` treatment via a `.xb-msg-agent-label` (mono uppercase "agent · from your brain", CONTEXT-locked). When `brainSummaryLabel(msg.metadata)` is non-null, append a `.xb-msg-sources` `<details>` whose `<summary>` is that label. If `msg.metadata.sources` is an array (future-proof — absent today), render one `.xb-msg-src` row per source with a `.xb-msg-chip` styled by `data-level` (validated→filled, working→outline); if absent, render no source rows (do NOT fabricate). Style the agent card on `.xb-msg.is-agent .xb-msg-bubble` (card + --border) so the class contract is unchanged.
    - Saved-to-brain badge: when `savedToBrainLabel(msg)` is non-null (real media/indexed signal), append a `.xb-msg-savetag` span (mockup `.savetag`: mono micro-text, `--secondary`, `--border`, radius 0) after the body.
    - Day separators: in the history/append render path, insert a `.xb-msg-daysep` element (mockup `.daysep`: mono uppercase `--muted-fg`) when a message's day differs from the previous rendered message's day (use `sameDay`). Keep it out of `#message-list` de-dupe logic (separators have no data-msg-id).
    CSS: add `.xb-msg-agent-label` (mockup `.who`), `.xb-msg-sources`/`summary`/`.xb-msg-src`, `.xb-msg-chip[data-level="validated"]` (filled `--primary`/`--primary-fg`) + `[data-level="working"]` (outline `--border`/`--muted-fg`) EXACTLY per mockup `.chip.validated`/`.chip.working`, `.xb-msg-savetag` (mockup `.savetag`), `.xb-msg-daysep` (mockup `.daysep`) — all radius 0, monochrome.
    Extend test_popup_contract.mjs: assert popup.css defines `.xb-msg-chip[data-level="validated"]`, `[data-level="working"]`, `.xb-msg-savetag`, `.xb-msg-agent-label`, `.xb-msg-sources`, `.xb-msg-daysep`; assert popup.js contains no `innerHTML =` assignment that interpolates `msg.content`/`delta`/source text.
  </action>
  <acceptance_criteria>
    - `node chrome-extension/tests/test_chat_stream.mjs` passes with the new brainSummaryLabel/savedToBrainLabel/sameDay cases.
    - `grep -E "from your brain" chrome-extension/popup.js` matches (agent label) and `grep -E "brainSummaryLabel|savedToBrainLabel|sameDay" chrome-extension/chat_stream.js` shows all three exports.
    - `grep -E "\.xb-msg-chip\[data-level=\"validated\"\]" chrome-extension/popup.css` uses `var(--primary)`; `\[data-level=\"working\"\]` uses outline `var(--border)`.
    - `grep -E "\.xb-msg-savetag|\.xb-msg-daysep|\.xb-msg-agent-label|\.xb-msg-sources" chrome-extension/popup.css` matches all four.
    - XSS guard passes (no innerHTML interpolating msg.content/delta) and no fabricated source arrays (`grep -nE "sources\s*=\s*\[\s*\{" chrome-extension/popup.js` returns nothing).
    - `node chrome-extension/tests/run_tests.mjs` (full suite incl. contract) passes; `node --check chrome-extension/popup.js` passes.
  </acceptance_criteria>
  <verify>
    <automated>node chrome-extension/tests/test_chat_stream.mjs && node chrome-extension/tests/test_popup_contract.mjs && node --check chrome-extension/popup.js && node -e "const j=require('fs').readFileSync('chrome-extension/popup.js','utf8'); if(/innerHTML\s*=\s*[^;]*(msg\.content|\.delta)/.test(j)){console.error('unsafe innerHTML');process.exit(1)} if(/sources\s*=\s*\[\s*\{/.test(j)){console.error('fabricated sources');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>The agent reply shows a mono "agent · from your brain" label and a real memory_items-driven sources summary; saved-to-brain badges appear on genuinely-indexed attachments; truth-level chip + day-separator + savetag styling matches the mockup monochrome spec; rendering is XSS-safe; no data is fabricated and no backend changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| team message content (other users / agent) → popup DOM | untrusted strings rendered into the thread |
| agent metadata.sources (future) → DOM chips | untrusted labels rendered into `<details>` rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Tampering/Info-disclosure (XSS) | buildBubbleNode / sources / savetag rendering | mitigate | All DOM built with createElement + textContent; no innerHTML carrying msg.content/delta/source text — asserted by the contract test's XSS guard |
| T-20-03-02 | Spoofing (fake provenance) | "from your brain" / saved-to-brain labels | mitigate | Labels derive only from server-sent fields (memory_items, metadata.media); no client-fabricated source rows (grep gate forbids hardcoded source arrays) |
| T-20-03-03 | Info-disclosure | truth-level chip data | accept | Per-source truth levels are not emitted by the backend today; chip styling ships design-complete and renders only real data when the backend later sends metadata.sources — documented follow-up, no invented values |
</threat_model>

<verification>
- `node chrome-extension/tests/run_tests.mjs` passes (chat_stream + contract + theme + existing).
- Truth-level chip / savetag / agent-label / sources / day-separator classes exist and match the mockup monochrome spec.
- XSS guard passes; no fabricated source arrays; `node --check chrome-extension/popup.js` passes.
</verification>

<success_criteria>
- The message thread matches the mockup (own/others/agent bubbles, square avatars, mono meta, agent "from your brain" block, saved-to-brain badges on real attachments, day separators, monochrome truth-level chip styling) with all existing behaviors and selectors intact and rendering XSS-safe — no backend change, no fabricated data.
</success_criteria>

<output>
After completion, create `.planning/phases/20-extension-chat-shadcn/20-03-SUMMARY.md`.
</output>
