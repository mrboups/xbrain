---
phase: 20-extension-chat-shadcn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chrome-extension/popup.css
  - chrome-extension/popup.html
  - chrome-extension/popup.js
  - chrome-extension/theme.js
  - chrome-extension/tests/test_theme.mjs
  - chrome-extension/tests/test_popup_contract.mjs
autonomous: true
requirements: [PKG-02]
user_setup: []

must_haves:
  truths:
    - "The popup defines the shadcn Neutral token system (monochrome, radius 0, Geist stack) as CSS custom properties."
    - "An in-popup light/dark toggle flips the whole surface and its choice persists across popup opens (chrome.storage.local)."
    - "The toggle initialises from prefers-color-scheme on first open, then the stored choice wins in both directions."
    - "No id that popup.js reads/writes was renamed or removed."
  artifacts:
    - path: "chrome-extension/popup.css"
      provides: "Canonical :root token block (light) + dark override (prefers-color-scheme + [data-theme]) with exact CONTEXT hex values, --radius:0, Geist/Geist Mono stacks; legacy --xb-* aliased to the new tokens"
      contains: "--radius"
    - path: "chrome-extension/theme.js"
      provides: "Pure, dependency-free theme resolve/apply/persist logic"
      exports: ["resolveInitialTheme", "applyTheme", "THEME_STORAGE_KEY"]
    - path: "chrome-extension/popup.html"
      provides: "In-popup theme toggle control in the header; color-scheme meta updated to 'light dark'; all existing ids preserved"
    - path: "chrome-extension/tests/test_theme.mjs"
      provides: "Node test proving stored-wins-over-prefers precedence + persistence round-trip"
    - path: "chrome-extension/tests/test_popup_contract.mjs"
      provides: "Selector-contract + token gate: every popup.js id exists in popup.html; tokens/radius/Geist present; no French product strings"
  key_links:
    - from: "chrome-extension/popup.js (theme wiring)"
      to: "chrome-extension/theme.js"
      via: "import { resolveInitialTheme, applyTheme, THEME_STORAGE_KEY }"
      pattern: "from \"\\./theme\\.js\""
    - from: "theme toggle click"
      to: ":root[data-theme]"
      via: "applyTheme sets data-theme; tokens re-resolve"
      pattern: "data-theme"
    - from: "theme choice"
      to: "chrome.storage.local"
      via: "persist on toggle, read on boot"
      pattern: "chrome\\.storage\\.local"
---

<objective>
Establish the shadcn "Neutral" design-token foundation for the extension chat popup and ship the in-popup light/dark toggle — the base layer every later restyle rule consumes. No visual restyle of components yet (that is Plans 02/03); this plan lands the tokens, the theme runtime, and the selector-contract guardrail.

Purpose: A single canonical token source (monochrome, radius 0, Geist) plus a persisted theme toggle means Plans 02/03 restyle purely by consuming `var(--…)`, and the contract test catches any broken popup.js selector from the first change onward.
Output: Token block + dark/toggle overrides in popup.css, `theme.js` pure module, toggle control wired in popup.html/popup.js, and two node tests (theme logic + popup contract).
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-extension-chat-shadcn/20-CONTEXT.md
@.planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html

<interfaces>
<!-- The mockup's canonical token values (CONTEXT §Design tokens). Use these EXACT strings. -->
Light: --bg:#FFFFFF; --fg:#0A0A0A; --card:#FFFFFF; --card-fg:#0A0A0A; --muted:#F5F5F5; --muted-fg:#737373;
       --secondary:#F5F5F5; --secondary-fg:#171717; --primary:#0A0A0A; --primary-fg:#FAFAFA;
       --border:#E5E5E5; --input:#E5E5E5; --ring:#A3A3A3; --destructive:#E5322D; --radius:0px;
Dark : --bg:#0A0A0A; --fg:#FAFAFA; --card:#171717; --card-fg:#FAFAFA; --muted:#262626; --muted-fg:#A3A3A3;
       --secondary:#262626; --secondary-fg:#FAFAFA; --primary:#FAFAFA; --primary-fg:#171717;
       --border:rgba(255,255,255,.10); --input:rgba(255,255,255,.15); --ring:#787878; --destructive:#FB5A57;
Fonts: --sans:'Geist','Inter',system-ui,-apple-system,'Segoe UI',Roboto,sans-serif;
       --mono:'Geist Mono',ui-monospace,'Cascadia Code',Consolas,monospace;

<!-- Legacy token names popup.css currently consumes (must keep resolving via aliases). -->
--xb-bg --xb-surface --xb-surface-2 --xb-border --xb-border-2 --xb-text --xb-text-mute --xb-text-dim
--xb-accent --xb-accent-2 --xb-accent-bg --xb-success --xb-success-bg --xb-error --xb-error-bg --xb-radius

<!-- Existing test harness pattern (settings.js is the model for a pure, node-testable module). -->
tests run via: node chrome-extension/tests/run_tests.mjs  (walks every test_*.mjs)
settings.js exports pure fns and injects chrome.storage via a deps arg — mirror this for theme.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the shadcn Neutral token block + dark/toggle overrides, alias legacy tokens</name>
  <files>chrome-extension/popup.css, chrome-extension/popup.html</files>
  <read_first>
    - chrome-extension/popup.css (lines 1-30 — the header comment explains tokens live in popup.html's inline :root today)
    - chrome-extension/popup.html (lines 9-91 — the inline &lt;style&gt; :root --xb-* dark tokens + GitHub-button + scrollbar rules)
    - .planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html (lines 3-26 — the exact :root / prefers-color-scheme / [data-theme] token blocks to reproduce)
  </read_first>
  <action>
    Make popup.css the single source of truth for tokens.
    1. At the top of popup.css (after the `[hidden]` rule), add a `:root { … }` block defining EVERY token from the interfaces list with the EXACT light hex values, plus `--radius:0px;` and the `--sans`/`--mono` stacks verbatim (name 'Geist'/'Geist Mono' FIRST with the system fallback — CSP-safe, do NOT add @font-face or fetch a webfont).
    2. Add the dark palette TWICE so the toggle wins both directions: once under `@media (prefers-color-scheme: dark){ :root{…} }` and once under `:root[data-theme="dark"]{…}`; add `:root[data-theme="light"]{…}` with the light palette so an explicit light choice overrides a dark OS. Copy the value sets verbatim from the mockup (mockup lines 16-26).
    3. Alias the legacy names so existing rules keep resolving during Plans 02/03: `--xb-bg:var(--bg); --xb-surface:var(--card); --xb-surface-2:var(--muted); --xb-border:var(--border); --xb-border-2:var(--input); --xb-text:var(--fg); --xb-text-mute:var(--muted-fg); --xb-text-dim:var(--muted-fg); --xb-accent:var(--primary); --xb-accent-2:var(--primary); --xb-accent-bg:var(--secondary); --xb-success:var(--muted-fg); --xb-success-bg:var(--secondary); --xb-error:var(--destructive); --xb-error-bg:var(--secondary); --xb-radius:var(--radius);` — put these aliases inside `:root` (they reference the tokens, which already flip, so they need not be repeated in each dark block). The accent→primary alias is the deliberate monochrome shift (blue → black/white).
    4. In popup.html: DELETE the inline `--xb-*` token definitions in the `:root` inline &lt;style&gt; (lines ~13-31) so popup.css owns them; KEEP the GitHub-button/scrollbar rules but leave their `var(--xb-*)` references (now resolved by popup.css aliases). Change `&lt;meta name="color-scheme" content="dark"&gt;` to `content="light dark"` and remove the inline `color-scheme: dark;`.
    Do NOT rename any element id or any `.xb-*`/`.connect-*` class.
  </action>
  <acceptance_criteria>
    - `grep -E "^\s*--(bg|fg|primary|primary-fg|muted|border|input|ring|destructive|card):" chrome-extension/popup.css` returns each token with its CONTEXT hex.
    - `grep -c -- "--radius:0px" chrome-extension/popup.css` &ge; 1.
    - `grep -- "'Geist'" chrome-extension/popup.css` and `grep -- "'Geist Mono'" chrome-extension/popup.css` both match; `@font-face`/`googleapis`/`gstatic` count (comments stripped) == 0.
    - `grep -E "data-theme=\"(dark|light)\"" chrome-extension/popup.css` matches both; `grep "prefers-color-scheme: dark" chrome-extension/popup.css` matches.
    - `grep -- "--xb-accent:var(--primary)" chrome-extension/popup.css` matches (monochrome alias).
    - `grep -n "color-scheme\" content=\"light dark\"" chrome-extension/popup.html` matches.
  </acceptance_criteria>
  <verify>
    <automated>node --check chrome-extension/popup.js && node -e "const c=require('fs').readFileSync('chrome-extension/popup.css','utf8'); const need=['--bg:#FFFFFF','--primary:#0A0A0A','--radius:0px','\'Geist\'','data-theme=\"dark\"']; const miss=need.filter(t=>!c.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('tokens ok')"</automated>
  </verify>
  <done>popup.css defines the full shadcn Neutral token system with dark + [data-theme] overrides and legacy aliases; popup.html no longer defines its own --xb-* tokens and declares color-scheme "light dark". No id/class renamed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: theme.js pure module + persisted in-popup light/dark toggle</name>
  <files>chrome-extension/theme.js, chrome-extension/tests/test_theme.mjs, chrome-extension/popup.html, chrome-extension/popup.js, chrome-extension/popup.css</files>
  <read_first>
    - chrome-extension/settings.js (the pure-module + injected-storage pattern to mirror)
    - chrome-extension/tests/test_settings.mjs (the node test pattern to mirror)
    - chrome-extension/popup.js (lines 45-110 — the `$` helper, DOMContentLoaded boot, wireHeader; where to add theme wiring)
    - chrome-extension/popup.html (lines 96-113 — the header markup where the toggle control goes)
    - .planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html (lines 34-40, 149-155, 230-243 — the `.seg` segmented toggle markup + setTheme logic)
  </read_first>
  <behavior>
    - resolveInitialTheme({storedTheme:'dark', prefersDark:false}) === 'dark'  (stored wins over prefers)
    - resolveInitialTheme({storedTheme:'light', prefersDark:true}) === 'light'
    - resolveInitialTheme({storedTheme:null, prefersDark:true}) === 'dark'      (fall back to prefers)
    - resolveInitialTheme({storedTheme:null, prefersDark:false}) === 'light'
    - resolveInitialTheme({storedTheme:'garbage', prefersDark:true}) === 'dark'  (invalid stored ignored)
    - applyTheme(rootStub,'dark') sets rootStub.getAttribute('data-theme') === 'dark' and returns 'dark'
    - THEME_STORAGE_KEY is a stable string constant
  </behavior>
  <action>
    Create `chrome-extension/theme.js` as a pure, dependency-free ES module (no chrome.*, no DOM globals — accept a root element / storage via args, exactly like settings.js):
      - `export const THEME_STORAGE_KEY = "xbrain_theme_v1";`
      - `export function resolveInitialTheme({ storedTheme, prefersDark })` → returns 'dark' or 'light' (stored value wins when it is exactly 'dark' or 'light'; otherwise prefersDark ? 'dark' : 'light').
      - `export function applyTheme(rootEl, mode)` → `rootEl.setAttribute('data-theme', mode)`; returns mode.
    Create `chrome-extension/tests/test_theme.mjs` covering every case in &lt;behavior&gt; using a tiny root stub `{ _a:{}, setAttribute(k,v){this._a[k]=v}, getAttribute(k){return this._a[k]} }`. Follow test_settings.mjs assert style; exit non-zero on failure.
    Add the toggle to popup.html header: a segmented `.seg` control (reuse the mockup's markup/SVGs at lines 151-154) placed in `.xb-header-right` BEFORE `#btn-settings`, with ids `#btn-theme-light` / `#btn-theme-dark`, `role="group" aria-label="Theme"`, and `aria-pressed` reflecting state. Give `.seg`/`.seg button` token-based styles in popup.css (radius 0, `--primary`/`--primary-fg` for the pressed button, `:focus-visible` outline `--ring`).
    Wire in popup.js: import from `./theme.js`; in `boot()` (or a new `wireTheme()` called from DOMContentLoaded) read `chrome.storage.local.get([THEME_STORAGE_KEY])`, compute `resolveInitialTheme({ storedTheme, prefersDark: matchMedia('(prefers-color-scheme: dark)').matches })`, `applyTheme(document.documentElement, mode)`, set aria-pressed, and on each toggle button click apply + persist via `chrome.storage.local.set({ [THEME_STORAGE_KEY]: mode })`. Keep the control OUT of the message thread (header only).
  </action>
  <acceptance_criteria>
    - `node chrome-extension/tests/test_theme.mjs` exits 0 and prints a pass line.
    - `grep -E "resolveInitialTheme|applyTheme|THEME_STORAGE_KEY" chrome-extension/theme.js` shows all three exports.
    - `grep -n "from \"./theme.js\"" chrome-extension/popup.js` matches; the theme path calls `chrome.storage.local`.
    - `grep -E "id=\"btn-theme-(light|dark)\"" chrome-extension/popup.html` matches both; `grep "aria-label=\"Theme\"" chrome-extension/popup.html` matches.
    - `grep -E "\.seg button\[aria-pressed=\"true\"\]" chrome-extension/popup.css` matches (pressed style uses `--primary`).
    - `node --check chrome-extension/popup.js` and `node --check chrome-extension/theme.js` pass.
  </acceptance_criteria>
  <verify>
    <automated>node chrome-extension/tests/test_theme.mjs && node --check chrome-extension/popup.js && node --check chrome-extension/theme.js</automated>
  </verify>
  <done>theme.js is a tested pure module; the header carries a keyboard-focusable segmented light/dark toggle wired to persist in chrome.storage.local; theme initialises from prefers-color-scheme with the stored choice winning; nothing renders in the message thread.</done>
</task>

<task type="auto">
  <name>Task 3: Selector-contract + token guardrail test (test_popup_contract.mjs)</name>
  <files>chrome-extension/tests/test_popup_contract.mjs</files>
  <read_first>
    - chrome-extension/popup.js (the full file — enumerate every id it references and every class it emits/toggles)
    - chrome-extension/popup.html (the id/class surface that must satisfy the contract)
    - chrome-extension/tests/run_tests.mjs (how the suite discovers test_*.mjs — the new file must match `test_*.mjs`)
  </read_first>
  <action>
    Create `chrome-extension/tests/test_popup_contract.mjs` — a pure node test (fs read of popup.js/html/css, regex assertions, no browser) that FAILS (exit 1) on any broken contract. It must assert:
    1. ID contract: for the frozen list below, every id popup.js reads/writes still exists in popup.html. List: teamSelector, btn-settings, btn-open-librechat, btn-add-to-memory, presenceBadge, presenceCount, connection-card, chat-body, btn-signin-github, btn-connect-xbrain, connect-status, btn-link-github, chat-empty, history-loader, message-list, chat-scroll, composer-input, btn-send, btn-clip, file-picker, clip-overlay, clip-project, clip-preview-mode, clip-preview-detail, clip-status, btn-clip-close, btn-clip-cancel, btn-clip-send, clip-use-defaults. (Derive the "referenced" set by regex over popup.js: `getElementById\("([^"]+)"\)`, `\$\("([^"]+)"\)`, and hardcoded `#`-selectors; assert the intersection with the frozen list is fully present in popup.html.)
    2. Class contract: assert popup.css still contains a rule for each class popup.js emits/toggles: xb-composer, xb-upload-error, xb-msg, is-self, is-user, is-agent, xb-msg-avatar, xb-msg-meta, xb-msg-author, xb-msg-time, xb-msg-provenance, xb-msg-bubble, xb-msg-caption, xb-msg-thumb, xb-msg-file-chip, connect-btn.
    3. Token contract: popup.css defines --bg,--fg,--card,--muted,--muted-fg,--secondary,--primary,--primary-fg,--border,--input,--ring,--destructive with the exact CONTEXT light hex; defines --radius:0px; --sans includes 'Geist'; --mono includes 'Geist Mono'; has a `[data-theme="dark"]` and a `prefers-color-scheme: dark` block.
    4. English-only guard: assert popup.html and popup.js contain no accented Latin chars in product strings (regex `[À-ÿ]` over both files == 0 matches) — guards against copying French from the mockup.
    Keep the frozen id/class lists as top-of-file arrays with a comment that Plan 03 extends the class list with message-thread additions.
  </action>
  <acceptance_criteria>
    - `node chrome-extension/tests/test_popup_contract.mjs` exits 0 on the current tree.
    - Deliberately break it to prove it bites: temporarily rename `id="message-list"` → `id="message-listX"` in a scratch copy and confirm the test exits 1 (document the check in the SUMMARY; revert).
    - `node chrome-extension/tests/run_tests.mjs` discovers and runs both new test files (theme + contract) and the whole suite passes.
    - The test asserts `[À-ÿ]` count == 0 in popup.html + popup.js.
  </acceptance_criteria>
  <verify>
    <automated>node chrome-extension/tests/test_popup_contract.mjs && node chrome-extension/tests/run_tests.mjs</automated>
  </verify>
  <done>A node contract test guards the popup.js↔popup.html id contract, the popup.js↔popup.css class contract, the token/radius/Geist definitions, and the English-only rule; it is wired into run_tests.mjs and passes on the restyled-tokens tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS/user theme prefs → popup runtime | prefers-color-scheme + a stored string read from chrome.storage.local |
| font declaration → network | a mis-declared @font-face could trigger an external fetch (CSP / privacy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Tampering | theme value from chrome.storage.local | mitigate | resolveInitialTheme accepts only exact 'dark'/'light'; any other value falls back to prefers — no untrusted value reaches the DOM beyond a fixed data-theme attribute |
| T-20-01-02 | Information disclosure | font stack | mitigate | Name 'Geist'/'Geist Mono' first with a system fallback; NO @font-face and NO googleapis/gstatic fetch — CSP-safe, no external request (asserted by the contract test) |
</threat_model>

<verification>
- `node chrome-extension/tests/run_tests.mjs` passes (existing suite + test_theme.mjs + test_popup_contract.mjs).
- `node --check chrome-extension/popup.js` and `node --check chrome-extension/theme.js` pass.
- popup.css defines the full token system (light + dark + [data-theme]) with exact CONTEXT hex, --radius:0, Geist stacks, and legacy aliases; popup.html declares `color-scheme: light dark` and no longer defines --xb-* tokens.
</verification>

<success_criteria>
- Token foundation, persisted theme toggle, and the contract guardrail are in place; no popup.js selector broke (contract test green); no French product strings; no webfont fetch.
</success_criteria>

<output>
After completion, create `.planning/phases/20-extension-chat-shadcn/20-01-SUMMARY.md`.
</output>
