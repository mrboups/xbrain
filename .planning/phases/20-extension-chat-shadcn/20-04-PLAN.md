---
phase: 20-extension-chat-shadcn
plan: 04
type: execute
wave: 4
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - chrome-extension/tests/test_popup_contract.mjs
  - .planning/phases/20-extension-chat-shadcn/20-UAT.md
autonomous: false
requirements: [PKG-02]
user_setup: []

must_haves:
  truths:
    - "An automated gate proves the restyle mechanically: token/radius/Geist definitions, every popup.js selector still resolves, accessibility rules present (focus-visible, prefers-reduced-motion, prefers-color-scheme + [data-theme]), English-only, no webfont fetch — and the gate FAILS rather than SKIPs when a check cannot run."
    - "A real-browser check confirms the load → chat → @agent → clip loop renders correctly in the restyled popup: shadcn Neutral render, toggle flips light↔dark and persists across reopen, optimistic send + Centrifugo echo dedup, @agent streaming, media/clip + saved-to-brain, focus states."
    - "The Phase-18-deferred browser UAT is closed by this check."
  artifacts:
    - path: "chrome-extension/tests/test_popup_contract.mjs"
      provides: "Final gate assertions: accessibility rules + token/selector/English contract (extended)"
    - path: ".planning/phases/20-extension-chat-shadcn/20-UAT.md"
      provides: "Recorded UAT result (checklist + PASS/FAIL + evidence) closing the Phase-18 browser check"
  key_links:
    - from: "20-UAT.md result"
      to: "SC#5 (real-browser load→chat→@agent→clip)"
      via: "human-verify checkpoint records evidence"
      pattern: "PASS|FAIL"
---

<objective>
Prove the restyle is real — not a "looks right from reading CSS" claim. Run the automated mechanical gate (must PASS, never SKIP), then a real-browser UAT of the full chat loop in the restyled popup, recording the result. This plan closes PKG-02 and lands the browser UAT deferred from Phase 18.

Purpose: Enforce the gate lesson (UI variant) — a screenshot-free pass is not acceptable; SKIP = FAIL.
Output: Extended automated gate + a recorded `20-UAT.md` with the human-verify result.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-extension-chat-shadcn/20-CONTEXT.md
@.planning/phases/20-extension-chat-shadcn/20-shadcn-mockup.html

<interfaces>
<!-- Test harness: node chrome-extension/tests/run_tests.mjs walks every test_*.mjs -->
<!-- No browser driver (playwright/puppeteer/jsdom) is installed in this repo today. -->
<!-- Real-stack surfaces: Phase-16 OSS-light compose boots the backend; the live stack is api.grooveos.app (VM may be stopped — start it or run OSS-light locally). -->
<!-- Extension loads unpacked from chrome-extension/ (manifest.json, popup.html action popup). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalise the automated mechanical gate (accessibility + full contract), SKIP=FAIL</name>
  <files>chrome-extension/tests/test_popup_contract.mjs</files>
  <read_first>
    - chrome-extension/tests/test_popup_contract.mjs (the gate from Plans 01/03 — extend it here)
    - chrome-extension/popup.css (confirm focus-visible / prefers-reduced-motion / prefers-color-scheme / [data-theme] rules from Plans 01-03)
    - chrome-extension/tests/run_tests.mjs
  </read_first>
  <action>
    Extend test_popup_contract.mjs with the final accessibility + integrity assertions (pure node fs/regex — every assertion either PASSES or FAILS, none silently skips):
    - Accessibility: popup.css contains at least one `:focus-visible` rule for an interactive control (toggle, icon button, send, file-chip); contains a `@media (prefers-reduced-motion: reduce)` block; contains BOTH a `@media (prefers-color-scheme: dark)` block AND `:root[data-theme="dark"]` / `[data-theme="light"]` overrides (proves the toggle wins both directions).
    - No webfont fetch: `@font-face|fonts.googleapis|fonts.gstatic|@import url(` count (comments stripped) == 0 across popup.css + popup.html.
    - Radius 0: no `border-radius: 50%` remains on `.xb-msg-avatar` / `.xb-group-avatar`; `--radius:0px` defined.
    - Selector + token + English contract: re-run the full ID/class/token/[À-ÿ] assertions.
    - Optional richer DOM smoke: if `jsdom` is resolvable (`require.resolve('jsdom')`), load popup.html, apply a token, assert `document.getElementById('message-list')` etc. exist; if jsdom is NOT installed, print a clear line that the DOM smoke is unavailable and rely on the contract assertions — this is an ENHANCEMENT, so its absence is NOT a gate skip (the gate is the fs/regex contract, which always runs).
    Wire nothing new into run_tests.mjs (it auto-discovers). Make failure explicit with a non-zero exit and a printed reason per failed assertion.
  </action>
  <acceptance_criteria>
    - `node chrome-extension/tests/run_tests.mjs` exits 0 with every test_*.mjs green (theme, contract, chat_stream, existing).
    - The contract test asserts presence of `:focus-visible`, `prefers-reduced-motion`, `prefers-color-scheme: dark`, and `[data-theme="dark"]` — verify by temporarily deleting one rule in a scratch copy → test exits 1 (document in SUMMARY, revert).
    - Webfont guard passes (no `@font-face`/`googleapis`/`gstatic`/`@import url(` after stripping comments).
    - Every assertion path prints PASS/FAIL — no assertion is conditionally skipped except the optional jsdom ENHANCEMENT (additive, not part of the gate).
  </acceptance_criteria>
  <verify>
    <automated>node chrome-extension/tests/run_tests.mjs && node -e "const fs=require('fs');const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,'');const t=strip(fs.readFileSync('chrome-extension/popup.css','utf8'))+strip(fs.readFileSync('chrome-extension/popup.html','utf8'));if(/@font-face|fonts\.(googleapis|gstatic)|@import\s+url\(/.test(t)){console.error('webfont fetch present');process.exit(1)}console.log('csp-safe fonts ok')"</automated>
  </verify>
  <done>The automated gate mechanically proves tokens, radius 0, Geist, selector contract, accessibility rules, English-only, and CSP-safe fonts; it FAILS on any missing rule and does not silently skip; the full node suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-browser UAT — load the restyled popup and drive the full chat loop</name>
  <files>.planning/phases/20-extension-chat-shadcn/20-UAT.md</files>
  <action>
    Human-in-the-loop verification (the automation is already done by Task 1 + Plans 01-03). The operator loads the restyled extension in a real Chrome against a running backend, walks the chat loop below, and records a per-step PASS/FAIL with evidence into `.planning/phases/20-extension-chat-shadcn/20-UAT.md`. This is a blocking checkpoint: do not mark SC#5 satisfied from CSS inspection — a screenshot-free "looks right" is rejected (SKIP=FAIL).
  </action>
  <what-built>
    The Chrome-extension chat popup restyled to shadcn Neutral (monochrome, radius 0, Geist) with a persisted in-popup light/dark toggle, matching 20-shadcn-mockup.html — header/composer/thread/agent-block/badges — with every existing behavior and popup.js selector preserved, verified mechanically by `node chrome-extension/tests/run_tests.mjs` (all green).
  </what-built>
  <how-to-verify>
    Preconditions: a running backend the popup can reach. Either start the live stack (VM api.grooveos.app — start the GCP VM per the runbook) OR boot the Phase-16 OSS-light compose locally. Then:
    1. In Chrome → chrome://extensions → Developer mode ON → "Load unpacked" → select `D:/VSC/xbrain/chrome-extension`. Reload if already loaded.
    2. Open the popup (toolbar icon). Confirm it RENDERS in shadcn Neutral: monochrome (black/white + grey), SHARP corners (radius 0), Geist-style type, square group avatar in the header. It must NOT look like the old blue/rounded theme.
    3. Toggle: click the light/dark segmented control — the WHOLE surface flips light↔dark. Close the popup and reopen it — the chosen theme PERSISTS (chrome.storage.local). Optionally confirm a fresh profile honours OS prefers-color-scheme.
    4. Send: type a message + Enter — it appears OPTIMISTICALLY as an own (--primary) bubble immediately; when the Centrifugo echo arrives it does NOT duplicate (dedup by id).
    5. Agent: send `@agent` (or `@chad`) — a typing indicator then a STREAMED agent reply renders in the --card agent block with the mono "agent · from your brain" label; if the reply used brain context, the "N sources from the brain" summary shows.
    6. Attachments/clip: attach a file via 📎 (or "add to memory") — the media chip/thumbnail renders on the tokens and a "saved to brain" badge appears on the indexed attachment; the clip overlay opens, renders monochrome, and sends.
    7. Accessibility: Tab through the header/composer — focus outlines are VISIBLE; reduced-motion (OS setting) suppresses the caret/rise animation.
    Record the outcome (per numbered step: PASS/FAIL + screenshot/notes) into `20-UAT.md`. Capture evidence (at least the render + the toggle flip + an @agent reply). If the backend genuinely cannot be started here, record that explicitly and run the closest real check (load popup.html directly in a browser tab; confirm render + toggle + focus states + computed tokens via DevTools), noting which live-data steps (4-6) remain to be walked — do NOT mark SC#5 satisfied from CSS inspection alone.
  </how-to-verify>
  <verify>Operator records per-step PASS/FAIL + evidence in `.planning/phases/20-extension-chat-shadcn/20-UAT.md`; SC#5 is marked satisfied only on a real render (not CSS inspection).</verify>
  <resume-signal>Type "approved" (paste/link the evidence) or describe the issues to fix.</resume-signal>
  <done>20-UAT.md records the real-browser loop (render + persisted toggle + optimistic send/dedup + @agent stream + clip/saved-to-brain + focus states) with evidence; the Phase-18 browser UAT is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unpacked extension → running backend | popup exercises the real REST/WS contract during UAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Repudiation | "verified" claim without evidence | mitigate | UAT requires recorded per-step PASS/FAIL + evidence in 20-UAT.md; SKIP=FAIL; screenshot-free pass rejected |
| T-20-04-02 | Tampering | render regressions slipping past CSS review | mitigate | Automated fs/regex gate runs first and must be green before the human UAT is meaningful |
</threat_model>

<verification>
- `node chrome-extension/tests/run_tests.mjs` green (final gate incl. accessibility + CSP-safe fonts).
- `.planning/phases/20-extension-chat-shadcn/20-UAT.md` records the real-browser loop result with evidence; SC#5 marked satisfied only on a real render (not CSS inspection).
</verification>

<success_criteria>
- The restyle is proven both mechanically (green node gate, no skips) and in a real browser (recorded UAT of render + persisted toggle + optimistic send/dedup + @agent stream + clip/saved-to-brain + focus states), closing PKG-02 and the Phase-18 browser UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/20-extension-chat-shadcn/20-04-SUMMARY.md`.
</output>
