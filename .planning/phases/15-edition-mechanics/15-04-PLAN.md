---
phase: 15-edition-mechanics
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified:
  - infrastructure/scripts/verify-phase15.sh
  - Makefile
autonomous: true
requirements: [EDIT-01, EDIT-02]

must_haves:
  truths:
    - "The gate exercises the REAL deployment path — docker compose's own resolved output and real running containers — never a grep of YAML"
    - "A real OSS-light boot brings memory-api to /v1/healthz 200 with NO Neo4j container running anywhere (SC#4)"
    - "The SAME running container, with only EDITION flipped, 404s the SaaS routes as oss and reaches them as saas (SC#2, SC#3, D-15-05)"
    - "nginx reaches healthy in an OSS-light boot despite librechat/openwebui/langfuse/session-bridge/drive-sync being absent"
    - "The core `minio` is reachable from memory-api in an OSS-light install, so /v1/media/upload has a live backend (SC#1)"
    - "A silently-failed bind mount FAILS the gate rather than passing it"
  artifacts:
    - path: "infrastructure/scripts/verify-phase15.sh"
      provides: "the Phase 15 acceptance gate — compose-layer + live-boot"
      min_lines: 150
    - path: "Makefile"
      provides: "make verify-phase15"
      contains: "verify-phase15"
  key_links:
    - from: "verify-phase15.sh"
      to: "docker compose config --format json"
      via: "resolved container environment, not the YAML source"
      pattern: "config --format json"
    - from: "verify-phase15.sh"
      to: "a real running memory-api"
      via: "curl against 127.0.0.1:18000 asserting 404 vs 401"
      pattern: "healthz"
---

<objective>
Build the acceptance gate for Phase 15 — and build it so it **cannot** repeat Phase 14's failure.

Phase 14 shipped three defects its own gate could not see, all from one cause: **the check never
traversed the real deployment path.** It exercised `Settings()` directly, so it never noticed that
`docker-compose.yml` didn't pass the variable to memory-api at all — an operator's config would have
been silently ignored, and every test still passed.

Phase 15 is *entirely* compose and boot-time wiring. A gate built on grepping YAML would repeat that
mistake exactly, and it is the single most likely way this phase ships broken.

Purpose: prove EDIT-01 and EDIT-02 against real `docker compose` behavior and real running containers.
Output: `infrastructure/scripts/verify-phase15.sh` + `make verify-phase15`.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-edition-mechanics/15-CONTEXT.md
@.planning/phases/15-edition-mechanics/15-RESEARCH.md
@.planning/phases/15-edition-mechanics/15-02-SUMMARY.md
@.planning/phases/15-edition-mechanics/15-03-SUMMARY.md

## Environment facts (verified, not assumed)

- Docker Desktop 4.81.0, daemon up, `docker compose` **v5.2.0**.
- Host is **arm64** (`linux/aarch64`); prod is amd64.
- **DO NOT BUILD IMAGES.** An arm64 build is useless for the prod VM. Every check below either pulls a
  multi-arch upstream image or runs the real source inside a stock `python:3.12-slim` — the exact
  technique the Phase-15 research used to live-boot memory-api without a build.
- The **only** service in the whole compose file with no arm64 image is `xbrain-backup`
  (`google/cloud-sdk:slim` is amd64-only). It is isolated in the `ops` profile precisely so every other
  profile stays live-verifiable here. `ops` is therefore verified at the **config layer only** — say so
  in the output, do not fake a boot.
- **The Git-Bash mount trap (cost real time in Phase 14):** a host bind mount from Git Bash needs a
  Windows-style path AND `MSYS_NO_PATHCONV=1`. A POSIX `$PWD` path **silently fails to mount** — the
  container then runs against its own stock filesystem, every check passes, and the gate proves
  nothing. Task 3 therefore hard-guards the mount and **FAILS** (never SKIPs) if it did not land.

## Existing conventions to follow

`infrastructure/scripts/verify-phase14.sh` is the model: `#!/usr/bin/env bash`, a header comment
enumerating each check, SKIP-aware counters (SKIP never counts as FAIL), `exit 0` when `FAIL == 0`,
`exit 1` otherwise, and the script `cd`s to the repo root itself so it can be run from anywhere.
Read it before writing.

The script ships to self-hosters: **do not hardcode any production domain or maintainer-specific host.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase15.sh — the compose-layer gate (EDIT-01 + the EDITION passthrough)</name>
  <files>infrastructure/scripts/verify-phase15.sh</files>

  <read_first>
    - `infrastructure/scripts/verify-phase14.sh` — the whole file. Copy its structure: header, `PASS/FAIL/SKIP` counters, `pass()/fail()/skip()` helpers, the repo-root `cd`, the exit-code contract.
    - `.planning/phases/15-edition-mechanics/15-03-SUMMARY.md` — the per-profile service counts this gate diffs against.
    - `infrastructure/docker-compose.yml` — as left by 15-03.
  </read_first>

  <action>
Create `infrastructure/scripts/verify-phase15.sh`. Start with a hermetic env file, then checks (a)–(e).

**The hermetic env file.** Never read the operator's real `.env` (it may not exist, and on a dev box it
holds prod secrets). Build a throwaway one from `.env.example`, stripping the keys we must control and
appending test values — `.env.example`'s `OAUTH_*` are empty, and empty `OAUTH_ISSUER_URL` is boot-fatal
by design since Phase 14, so a boot needs real ones:

```bash
ENVF="$(mktemp)"
trap 'rm -f "$ENVF"' EXIT
grep -vE '^(OAUTH_ISSUER_URL|OAUTH_RESOURCE_URL|POSTGRES_PASSWORD|DATABASE_URL|NEO4J_PASSWORD|MINIO_ROOT_PASSWORD|BRIDGE_SHARED_SECRET|XBRAIN_BASE_DOMAIN|EDITION|COMPOSE_PROFILES)=' .env.example > "$ENVF"
cat >> "$ENVF" <<'EOF'
OAUTH_ISSUER_URL=https://api.p15.test
OAUTH_RESOURCE_URL=https://mcp.p15.test/mcp
POSTGRES_PASSWORD=p15testpassword
DATABASE_URL=postgresql+asyncpg://xbrain:p15testpassword@postgres:5432/xbrain
NEO4J_PASSWORD=p15testpassword
MINIO_ROOT_PASSWORD=p15testpassword
BRIDGE_SHARED_SECRET=p15testbridgesecret
XBRAIN_BASE_DOMAIN=p15.test
EOF
# EDITION and COMPOSE_PROFILES are deliberately ABSENT from $ENVF so the shell env controls them and
# the "default" check below is genuine.
DC=(docker compose -p xbrain-p15 -f infrastructure/docker-compose.yml --env-file "$ENVF")
```
(`-p xbrain-p15` keeps this gate's containers out of any real `xbrain` project.)

Then implement:

**(a) EDIT-01 — exactly three profiles, and no `pro`.**
`"${DC[@]}" config --profiles | sort | tr '\n' ' '` must equal `integrations ops saas `.
Also assert the output contains no `depends on undefined service` text — that single string is the
cheapest possible regression gate on the entire `depends_on` graph, and it is what surfaced both hard
blockers during research. FAIL loudly if `pro` appears.

**(b) EDIT-01 — the OSS-light core is exactly 10 services, by name and by count.**
Diff `"${DC[@]}" config --services | sort` against this list embedded in the script:
`brain-janitor centrifugo memory-api mcp-brain mcp-gateway mcp-scraper minio nginx postgres qdrant`

**(c) EDIT-01 — the independent leak assertion.** Separately from (b), assert that **none** of the 22
opt-in service names appears in the bare core set (`grep -Ex` over the deny-list: neo4j,
graphiti-service, langfuse, langfuse-worker, langfuse-clickhouse, langfuse-redis, mcp-calendar,
mcp-drive-read, mcp-deck, mcp-github, granola-sync, drive-sync, searxng, agent-runtime, session-bridge,
librechat, librechat-mongo, librechat-meili, librechat-bridge, openwebui, openwebui-pipeline,
xbrain-backup). Comment in the script *why* this duplicates (b): they are two **independent** statements
of the same intent, so someone who "fixes" one to match a bug does not thereby fix the other. A leaked
core service does not break `config` — it just silently boots, which is exactly the failure mode
research reproduced (15 services instead of 10).

**(d) EDIT-01 — each opt-in profile is independently selectable and legal.**
- `--profile integrations config --services | wc -l` → 24
- `--profile saas config --services | wc -l` → 17
- `--profile ops config --services | wc -l` → 11, and the list contains `xbrain-backup`
- all three together → 32
- `--profile ops config -q`, `--profile saas config -q`, `--profile integrations config -q` → each exit 0
  (the `ops` one is what would have caught the `xbrain-backup → librechat-mongo` cross-profile edge)
- `COMPOSE_PROFILES=integrations,saas "${DC[@]}" config --services` equals
  `--profile integrations --profile saas config --services` (the operator-facing env var really works)

**(e) EDIT-02 + SC#5 — the resolved container environment, not the YAML.**
- `env -u EDITION "${DC[@]}" config --format json | jq -r '.services["memory-api"].environment.EDITION'`
  → `oss`. **A `null` here is a FAIL** — it means compose never passes the var and an operator's setting
  is silently ignored. *This is the exact Phase-14 defect, and this line is the assertion that would
  have caught it.*
- `EDITION=saas ... | jq ... .EDITION` → `saas`.
- Only `memory-api` receives `EDITION`:
  `jq -r '[.services|to_entries[]|select(.value.environment.EDITION != null)|.key]|join(",")'` → `memory-api`
- **SC#5 / D-15-04 — data identity across every profile flip.** For each of the five combinations
  (bare, integrations, saas, ops, all-three): `memory-api.environment.QDRANT_COLLECTION` MUST equal
  `brain-janitor.environment.QDRANT_COLLECTION`, and `memory-api.environment.MINIO_ENDPOINT` MUST equal
  `mcp-deck.environment.MINIO_ENDPOINT` wherever mcp-deck is present. One mismatched row = FAIL. This is
  the `215882b` defect class (brain-janitor purging a collection memory-api never wrote to) made
  mechanically un-shippable.
- No `depends_on: neo4j` on either core service:
  `jq -e '(.services["memory-api"].depends_on|has("neo4j")|not) and (.services["brain-janitor"].depends_on|has("neo4j")|not)'`
  </action>

  <acceptance_criteria>
```bash
bash -n infrastructure/scripts/verify-phase15.sh          # exit 0
bash infrastructure/scripts/verify-phase15.sh; echo "exit=$?"
```
- Checks (a)–(e) all report **PASS**; the script exits **0**.
- Output states the exact numbers it asserted (`core=10`, `integrations=24`, `saas=17`, `ops=11`,
  `all=32`), so a reader can see *what* was proven, not just that something passed.

**Then prove the gate can actually fail** (a gate that cannot fail is not a gate). Run each of these,
confirm the script reports FAIL and exits 1, then revert:
1. Delete `profiles: ["saas"]` from `librechat-mongo` → checks (b) AND (c) must both FAIL (11 core
   services; the deny-list hit).
2. Change `EDITION: ${EDITION:-oss}` to `EDITION: oss` (hardcoded) in the memory-api env block → check
   (e)'s `EDITION=saas` assertion must FAIL.
3. Re-add `neo4j: { condition: service_healthy }` to `brain-janitor.depends_on` → check (a) must FAIL
   with `depends on undefined service`.

Record all three results in the SUMMARY.
  </acceptance_criteria>

  <verify>
    <automated>bash infrastructure/scripts/verify-phase15.sh</automated>
  </verify>

  <done>verify-phase15.sh implements checks (a)–(e) against `docker compose config` output only (zero greps of docker-compose.yml), passes, exits 0, and demonstrably FAILs on each of the three injected regressions.</done>
</task>

<task type="auto">
  <name>Task 2: verify-phase15.sh — the live OSS-light boot (SC#1, SC#4)</name>
  <files>infrastructure/scripts/verify-phase15.sh</files>

  <read_first>
    - `infrastructure/scripts/verify-phase15.sh` — as written in Task 1.
    - `infrastructure/nginx/templates/10-xbrain.conf.template` — lines 1-3 and any `proxy_pass` block. **Finding to preserve, not "fix":** every upstream already uses `resolver 127.0.0.11` + `set $var; proxy_pass $var;`, which defers DNS to request time. That is *why* nginx can start with librechat / openwebui / langfuse / session-bridge / drive-sync absent. Do NOT rewrite these templates — check (f) exists to *prove* this holds, and if it fails, that is a real finding to report, not a licence to change nginx.
    - `infrastructure/docker-compose.yml` — the `healthcheck:` blocks of postgres, qdrant, minio, centrifugo, nginx (to know what "healthy" means for each).
  </read_first>

  <action>
Add check **(f)** to the script: a **real** `docker compose up` of the OSS-light core, restricted to the
five services whose images are pullable multi-arch upstreams (**no builds**):

```bash
"${DC[@]}" up -d postgres qdrant minio centrifugo nginx
```

Guard first: if any container named `xbrain-*` already exists on this host, **SKIP the whole live
section with a loud message** rather than clobbering a running stack. (`container_name:` is explicit in
the compose file, so names are global.)

Assert:
1. All five reach `healthy` within 120s. Poll
   `docker inspect --format '{{.Name}} {{.State.Health.Status}}' xbrain-postgres xbrain-qdrant xbrain-minio xbrain-centrifugo xbrain-nginx`.
   Report each by name. **`xbrain-nginx` healthy is the load-bearing one** — it proves the ingress
   survives an OSS-light install in which five of its upstreams do not exist.
2. **No opt-in container is running.** `"${DC[@]}" ps --services` must contain none of: `neo4j`,
   `langfuse`, `langfuse-worker`, `langfuse-clickhouse`, `langfuse-redis`, `librechat`,
   `librechat-mongo`, `librechat-meili`, `librechat-bridge`, `openwebui`, `openwebui-pipeline`,
   `session-bridge`, `graphiti-service`, `agent-runtime`, `searxng`, `xbrain-backup`, and the four
   integration `mcp-*`. This catches the *inverse* leak — an SC#1-violating over-inclusion.
   Belt and braces: `docker ps --format '{{.Names}}' | grep -x xbrain-neo4j` must not match.
3. The five services that are up are exactly the five requested — nothing was pulled in by a
   `depends_on` you did not expect.

Leave the stack **up** at the end of check (f) — Task 3's memory-api harness attaches to it. Register a
single cleanup trap for the whole script:
`trap '"${DC[@]}" down -v --remove-orphans >/dev/null 2>&1; docker rm -f xbrain-p15-memapi >/dev/null 2>&1; rm -f "$ENVF"' EXIT`

Note in the script header that the other five core services (`memory-api`, `mcp-brain`, `mcp-gateway`,
`mcp-scraper`, `brain-janitor`) are `build:` services and are therefore NOT brought up by compose here —
memory-api is exercised for real in check (g) via a no-build harness, and the three mcp-* + brain-janitor
sidecars are covered at the config layer only. Say this plainly in the output; do not imply more
coverage than exists.
  </action>

  <acceptance_criteria>
```bash
bash infrastructure/scripts/verify-phase15.sh; echo "exit=$?"
```
- Check (f) reports all five of `xbrain-postgres`, `xbrain-qdrant`, `xbrain-minio`, `xbrain-centrifugo`,
  `xbrain-nginx` as **healthy**, each named individually in the output.
- Check (f) reports **zero** opt-in containers running, listing the deny-list it checked.
- The script still exits **0**.
- No image was built: `docker compose -p xbrain-p15 ... build` is never invoked, and the run performs no
  `docker build`. (Confirm by reading the script; `up -d <named services>` of pull-only images does not
  build.)

Negative proof — the check must be able to fail:
```bash
# Temporarily remove `profiles: ["integrations"]` from neo4j, re-run.
# Check (b) must FAIL (11 core services) and, if the live section runs `up` for the core, neo4j must
# appear — proving check (f)'s deny-list actually fires. Revert afterwards.
```
Record the result in the SUMMARY.
  </acceptance_criteria>

  <verify>
    <automated>bash infrastructure/scripts/verify-phase15.sh</automated>
  </verify>

  <done>A real `docker compose up` of the OSS-light core brings postgres, qdrant, minio, centrifugo and nginx to `healthy` — with nginx healthy despite five absent upstreams — and no `integrations`/`saas`/`ops` container is running.</done>
</task>

<task type="auto">
  <name>Task 3: verify-phase15.sh — the live EDITION boot, the negative case (SC#2, SC#3, SC#4, D-15-05) + make target</name>
  <files>infrastructure/scripts/verify-phase15.sh, Makefile</files>

  <read_first>
    - `apps/memory-api/Dockerfile` — it is the spec the harness must reproduce **without building**: build context is the repo root; `packages/memory-models` must be staged at `/app/packages/memory-models` because `pyproject.toml` declares `xbrain-memory @ file:///app/packages/memory-models`; the app is launched as `app.main:app`.
    - `apps/memory-api/pyproject.toml` — the dependency list `pip install .` will resolve.
    - `.planning/phases/15-edition-mechanics/15-02-SUMMARY.md` — the two SaaS-only route paths.
    - `infrastructure/scripts/verify-phase15.sh` — as written in Tasks 1-2 (the compose stack from check (f) is still up and `xbrain_net` exists).
  </read_first>

  <action>
Add check **(g)**: boot the **real** memory-api source, with the **real compose-resolved environment**,
against the **real** running Postgres/Qdrant/MinIO from check (f) — and with **no Neo4j container
anywhere** — then flip `EDITION` on the *same running container* and re-assert.

This is the check that closes the Phase-14 loop. The env is not hand-written; it is lifted straight out
of `docker compose config --format json`, so if compose fails to pass `EDITION` (or `MINIO_ENDPOINT`, or
`QDRANT_COLLECTION`), this check breaks.

```bash
# 1. Env comes from COMPOSE, not from this script's imagination.
env -u EDITION "${DC[@]}" config --format json 2>/dev/null \
  | jq -r '.services["memory-api"].environment | to_entries[] | select(.value != null) | "\(.key)=\(.value)"' \
  > "$MEMAPI_ENV"
grep -q '^EDITION=oss$' "$MEMAPI_ENV" || fail "compose did not pass EDITION to memory-api"

# 2. Host path for the bind mount — the Git-Bash trap.
REPO_ROOT="$(pwd)"
case "$(uname -s)" in
  MINGW*|MSYS*|CYGWIN*) HOST_REPO="$(cygpath -w "$REPO_ROOT")"; export MSYS_NO_PATHCONV=1 ;;
  *)                    HOST_REPO="$REPO_ROOT" ;;
esac

# 3. One long-lived stock python container. NO IMAGE BUILD.
docker run -d --name xbrain-p15-memapi --network xbrain_net \
  -p 127.0.0.1:18000:8000 --env-file "$MEMAPI_ENV" \
  -v "${HOST_REPO}:/repo:ro" -v xbrain-p15-pipcache:/root/.cache/pip \
  python:3.12-slim sleep infinity

# 4. MOUNT GUARD — a silently-unmounted volume makes every check below pass while proving NOTHING.
#    This is a FAIL, never a SKIP.
docker exec xbrain-p15-memapi test -f /repo/apps/memory-api/app/main.py \
  || fail "bind mount did not land (Git-Bash path trap) — aborting; the checks below would be meaningless"

# 5. Reproduce the Dockerfile's staging, then install. (~2-4 min first run; the pip cache volume
#    makes re-runs fast.)
docker exec xbrain-p15-memapi sh -euc '
  mkdir -p /app/packages && cp -r /repo/packages/memory-models /app/packages/
  mkdir -p /build && cp -r /repo/apps/memory-api/. /build/
  cd /build && pip install -q --disable-pip-version-check .
' || skip "pip install failed (offline?) — live EDITION check not run"
```

Then a helper `boot_edition <oss|saas>` that starts uvicorn **inside the already-prepared container**
with only `EDITION` changed, waits for `/v1/healthz`, and returns:

```bash
boot_edition() {  # $1 = oss|saas
  docker exec xbrain-p15-memapi sh -c 'pkill -f uvicorn || true'; sleep 1
  docker exec -d -e "EDITION=$1" xbrain-p15-memapi \
    sh -c "cd /build && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/$1.log 2>&1"
  for _ in $(seq 1 40); do
    [ "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:18000/v1/healthz)" = "200" ] && return 0
    sleep 2
  done
  return 1
}
```

**Same container. Same installed site-packages. Same source. Only the env differs.** That is a literal
demonstration of D-15-05 ("one image, no rebuild") — stronger than two separate boots could ever be.

Assertions:

**EDITION=oss:**
| Request | Expected | Proves |
|---|---|---|
| `GET /v1/healthz` | **200** | SC#4 — memory-api is healthy with **no Neo4j container running anywhere** |
| `docker exec ... grep -c neo4j.connectivity_failed /tmp/oss.log` | **1** | the degrade path was actually *exercised* (it tried, failed, carried on) — not that Neo4j was secretly reachable |
| `POST /v1/waitlist` (empty body) | **404** | SC#2 negative case — the router is **absent**, not merely auth-rejecting |
| `GET /v1/me/external-sessions` | **404** | same |
| `GET /.well-known/oauth-authorization-server` | **200** | the ChatGPT/Claude.ai web connector is CORE — never gated |
| `POST /v1/media/upload` (no auth) | **401 or 403** | the media router is mounted (**not 404**) |
| inside the container: `python -c "import socket;s=socket.socket();s.settimeout(3);s.connect(('minio',9000))"` | **success** | SC#1 — the promoted core `minio` is reachable in an OSS-light install, so `/v1/media/upload` has a live backend |

**EDITION=saas (same container, env flip only):**
| Request | Expected | Proves |
|---|---|---|
| `POST /v1/waitlist` (empty body) | **422** | the route now EXISTS (validation error, not 404) |
| `GET /v1/me/external-sessions` | **401 or 403** | the route now EXISTS and rejects the anonymous caller — **not 404** (absent) and **not 405** (shadowed by an earlier core route) |
| `GET /v1/healthz` | **200** | core unaffected by the flip |

**404 vs 401 is the whole point.** A 404 means the router was never registered. A 401 would mean the
SaaS surface shipped into the OSS edition and merely happened to reject this particular caller. Assert
the exact codes; never accept "not 2xx".

Finally:
- Add a `verify-phase15` target to the `Makefile` alongside the existing `env-check` / `preflight`
  targets, with a `## ` help comment matching the file's style:
  `@bash infrastructure/scripts/verify-phase15.sh`, plus `.PHONY`.
- Extend the script header comment to enumerate checks (a)–(g) and to state plainly what is **not**
  covered: `xbrain-backup` (no arm64 image → config-layer only) and the three `mcp-*` core sidecars +
  `brain-janitor` (`build:` services → config-layer only). Do not overclaim.
  </action>

  <acceptance_criteria>
```bash
make verify-phase15; echo "exit=$?"
```
- Check (g) reports, for `EDITION=oss`: `healthz=200`, `waitlist=404`, `external-sessions=404`,
  `oauth-metadata=200`, `media-upload=401|403`, `minio reachable`, and `neo4j.connectivity_failed x1`.
- Check (g) reports, for `EDITION=saas` **on the same container**: `waitlist=422`,
  `external-sessions=401|403`, `healthz=200`.
- The script exits **0**, and its summary line states PASS/FAIL/SKIP counts.
- **No `docker build` ran** anywhere in the invocation.

Negative proofs — run each, confirm FAIL + exit 1, then revert:
1. In `apps/memory-api/app/main.py`, move `waitlist` from `SAAS_ONLY_ROUTERS` into `CORE_ROUTERS`.
   → check (g) must FAIL with `waitlist=200|422 under EDITION=oss (expected 404)`. **This is the leak the
   gate exists to catch.**
2. Break the bind mount on purpose (pass a POSIX `$PWD` path on Git Bash without `MSYS_NO_PATHCONV`).
   → the mount guard must **FAIL** the run, not SKIP it and not pass it.

Record both in the SUMMARY.
  </acceptance_criteria>

  <verify>
    <automated>make verify-phase15</automated>
  </verify>

  <done>A real memory-api, running the real source with the real compose-resolved environment and no Neo4j container in existence, returns 200 on `/v1/healthz` and **404** on both SaaS routes under `EDITION=oss`; flipping only `EDITION` on that same running container makes both routes reachable (422 / 401); the mount guard fails the run rather than passing it when the bind mount does not land; `make verify-phase15` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the gate → the artifact it grades | a gate that cannot see the real deployment path is worse than no gate: it manufactures false confidence (Phase 14's root cause) |
| verification containers → the developer's host | the gate starts real containers and binds a host port |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Repudiation | the gate passing while the wiring is broken (a silently-unmounted volume, a `null` env var, a grep that matches its own comment) | mitigate | The mount guard FAILs (never SKIPs); `EDITION == null` is an explicit FAIL; every assertion reads `docker compose config --format json` or a live HTTP status code, never the YAML text. Each check is proven falsifiable by an injected regression before the task is done. |
| T-15-04-02 | Information disclosure | the harness binding memory-api to a public interface | mitigate | Published on `127.0.0.1:18000` only, never `0.0.0.0`. The compose stack is brought up under the isolated project `-p xbrain-p15` and torn down by an `EXIT` trap. |
| T-15-04-03 | Tampering | the gate clobbering a real local stack (`container_name:` is global) | mitigate | The live section aborts with a SKIP if any `xbrain-*` container already exists. Teardown uses the `xbrain-p15` project name. |
| T-15-04-04 | Information disclosure | test credentials in the throwaway env file leaking | accept | `$ENVF` is a `mktemp` file removed by the EXIT trap; its values (`p15testpassword`) are non-secret literals used only by ephemeral containers on the loopback interface. |
</threat_model>

<verification>
1. `make verify-phase15` exits 0, printing PASS/FAIL/SKIP counts and the exact numbers asserted.
2. Every check reads real `docker compose` output or a live HTTP response — **zero** greps of `docker-compose.yml`.
3. Each of the five injected regressions (untagged `librechat-mongo`; hardcoded `EDITION`; re-added `brain-janitor→neo4j`; `waitlist` moved to CORE; broken bind mount) makes the gate FAIL.
4. No `docker build` is invoked at any point.
</verification>

<success_criteria>
Phase 15's acceptance gate asserts against real `docker compose` behavior and real running containers,
proves the OSS-light core boots healthy with no Neo4j, proves an `EDITION=oss` boot **404s** the SaaS
routes while an `EDITION=saas` flip of the same container reaches them, and is demonstrably capable of
failing — so a regression in this phase cannot ship the way Phase 14's did.
</success_criteria>

<output>
After completion, create `.planning/phases/15-edition-mechanics/15-04-SUMMARY.md`.
Record: the five injected-regression results (each must have FAILed the gate), the exact per-profile
service counts the gate asserted, and — plainly — what the gate does NOT cover (`xbrain-backup`: no
arm64 image; `mcp-brain`/`mcp-gateway`/`mcp-scraper`/`brain-janitor`: `build:` services, config-layer
only). Do not overclaim coverage.
</output>
