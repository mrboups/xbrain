---
phase: 5
plan: "05-06"
subsystem: infrastructure-config
tags: [nginx, cloudflare, dns, dotenv, runbook]
dependency_graph:
  requires: [05-02, 05-05]
  provides: [30-projects.conf nginx (déjà dans 05-05), runbook Cloudflare Access, .env.example mis à jour avec vars Phase 5]
  affects: [infrastructure/nginx/conf.d/30-projects.conf (déjà créé en 05-05), infrastructure/.env.example]
tech_stack:
  added: [Cloudflare Access (zero trust free tier), Firebase Hosting custom domain DNS]
  patterns: [20-api.conf nginx pattern, Cloudflare Zero Trust Self-hosted app setup]
key_files:
  created:
    - docs/cloudflare-access-setup.md
  modified:
    - infrastructure/.env.example
    - infrastructure/nginx/conf.d/30-projects.conf
---

# Phase 5 Plan 06: nginx vhost + Cloudflare Access + .env.example

## Goal

Documenter le runbook Cloudflare Access pour protéger `projects.dejavu.cat`, mettre à jour `.env.example` avec toutes les nouvelles variables Phase 5, et finaliser `30-projects.conf` si pas déjà fait en 05-05.

## Tasks

### Task 1 — Runbook Cloudflare Access pour projects.dejavu.cat

**What:** Documenter le setup complet Cloudflare Access (zero trust free tier) pour protéger `projects.dejavu.cat`. Configuration manuelle dans le dashboard Cloudflare — zero code. Inclut les étapes DNS, Identity Provider Google, et Access Application.

**Files:**
- `docs/cloudflare-access-setup.md`

**How:**

Créer un document de runbook avec les étapes exactes issues de RESEARCH.md Q7 :

```markdown
# Cloudflare Access — Setup projects.dejavu.cat

## Prérequis
- Domaine dejavu.cat déjà sur Cloudflare (DNS géré par Cloudflare)
- Compte Cloudflare Zero Trust (plan Free — jusqu'à 50 users)
- Firebase Hosting initialisé (firebase init hosting --project xbrain-495115)
- DNS CNAME Firebase vérifié (TXT record de vérification ajouté)

## Étape 1 — Firebase Hosting custom domain

1. Firebase Console > Hosting > Add custom domain
2. Entrer : projects.dejavu.cat
3. Copier le TXT record de vérification fourni par Firebase
4. Dans Cloudflare DNS : ajouter le TXT record (mode DNS-only, pas Proxied)
5. Attendre la vérification (peut prendre jusqu'à 24h)
6. Firebase fournit ensuite des enregistrements A (deux IPs Firebase Hosting)
7. Dans Cloudflare DNS : créer deux enregistrements A :
   - Name: projects | Content: <IP Firebase 1> | Proxied: OUI (orange cloud)
   - Name: projects | Content: <IP Firebase 2> | Proxied: OUI (orange cloud)

Important : les enregistrements A DOIVENT être en mode Proxied (orange cloud) pour que
Cloudflare Access intercepte le trafic. Mode DNS-only (gris) = bypass Cloudflare Access
(Pitfall 8 RESEARCH.md).

## Étape 2 — Identity Provider Google dans Cloudflare Zero Trust

1. https://one.dash.cloudflare.com/ > Settings > Authentication > Add new > Google
2. Dans Google Cloud Console (console.cloud.google.com) :
   - APIs & Services > Credentials > Create OAuth 2.0 Client ID
   - Application type: Web application
   - Name: Cloudflare Access
   - Authorized redirect URI: https://<team>.cloudflareaccess.com/cdn-cgi/access/callback
     (remplacer <team> par votre team slug Cloudflare Zero Trust)
3. Copier Client ID et Client Secret dans Cloudflare

## Étape 3 — Application Access

1. Cloudflare Zero Trust > Access > Applications > Add an application > Self-hosted
2. Application name: xbrain Projects Dashboard
3. Application domain: projects.dejavu.cat
4. Session Duration: 24h
5. Identity providers: Google (configuré étape 2)
6. Skip pages: laisser vide (toutes les URLs sont protégées)

## Étape 4 — Policy

1. Dans l'application créée > Add a policy
2. Policy name: Team xbrain Access
3. Action: Allow
4. Rule type: Emails
5. Values: team@grooveos.app, team@grooveos.app
   (ou Rule type: Email domain, Values: grooveos.app pour plus de flexibilité)

## Résultat attendu

Tout accès à https://projects.dejavu.cat est intercepté par Cloudflare Access.
L'écran de login Google apparaît. Seuls les emails whitelistés passent.
Firebase Hosting sert le HTML généré sans modification.

## Troubleshooting

- Si le site n'est pas protégé : vérifier que les records DNS sont en mode Proxied (orange).
- Si le site retourne 523 : Firebase Hosting TLS incompatible avec Cloudflare proxy.
  Fix : dans Cloudflare SSL/TLS > Overview : passer de "Flexible" à "Full (Strict)".
- Si login loop : vérifier que le redirect URI dans Google Cloud Console correspond exactement
  au format https://<team>.cloudflareaccess.com/cdn-cgi/access/callback.
```

**Test:** Le fichier existe et contient les 4 étapes avec les commandes/URLs exactes.

---

### Task 2 — Mettre à jour infrastructure/.env.example avec les variables Phase 5

**What:** Ajouter toutes les nouvelles variables d'environnement Phase 5 à `.env.example` avec leur description et valeur par défaut. Ce fichier est la référence pour configurer une nouvelle VM.

**Files:**
- `infrastructure/.env.example`

**How:**

Lire d'abord `infrastructure/.env.example` pour voir les variables existantes (ou vérifier si le fichier s'appelle `.env.example` ou `env.example` dans ce repo). Ajouter une section `# === Phase 5 — Plateforme Projets Équipe ===` avec :

```bash
# === Phase 5 — Plateforme Projets Équipe ===

# GitHub OAuth — LibreChat (créer dans GitHub Settings > Developer Settings > OAuth Apps)
# Homepage URL: https://x.dejavu.cat
# Callback URL: https://x.dejavu.cat/oauth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=/oauth/github/callback

# GitHub Org membership verification (PAT Fine-grained, scope: read:org sur your-github-org)
# Durée recommandée : 1 an. Rotation annuelle.
GITHUB_ORG_PAT=
GITHUB_ORG_NAME=your-github-org

# OpenAI API — requis pour graphiti-service (embeddings + reranking)
# MÊME clé que pour agent-runtime — coût marginal (text-embedding-3-small ~$0.02/1M tokens)
# Si OPENAI_API_KEY déjà défini plus haut, ne pas dupliquer.
# OPENAI_API_KEY déjà défini dans la section agent-runtime — réutilisé par graphiti-service.

# Graphiti LLM model (défaut Claude Haiku — coût faible pour l'extraction)
GRAPHITI_LLM_MODEL=claude-haiku-4-5-20251001

# Graphiti service URL (interne Docker — ne pas modifier sauf si port change)
GRAPHITI_SERVICE_URL=http://graphiti-service:8300

# GCP / Firebase (pour le pipeline deploy GitOps)
# GCP Project Number (gcloud projects describe xbrain-495115 --format='value(projectNumber)')
GCP_PROJECT_NUMBER=
# Workload Identity Pool (créé dans gitops-setup.md)
WORKLOAD_IDENTITY_PROVIDER=projects/${GCP_PROJECT_NUMBER}/locations/global/workloadIdentityPools/github-pool/providers/github-provider
```

Note : `OPENAI_API_KEY` est déjà dans le `.env.example` pour agent-runtime. Vérifier et ne pas dupliquer — ajouter juste une note que graphiti-service utilise la même clé.

**Test:**
```bash
# Vérifier que toutes les nouvelles variables sont présentes
grep -E "GITHUB_CLIENT_ID|GITHUB_ORG_PAT|GRAPHITI|GCP_PROJECT_NUMBER" infrastructure/.env.example
# Attendu : 5+ lignes avec les nouvelles variables
```

---

### Task 3 — Finaliser 30-projects.conf et vérifier nginx

**What:** Si le plan 05-05 n'a pas créé `infrastructure/nginx/conf.d/30-projects.conf`, le créer maintenant. Vérifier la configuration nginx complète avec les 4 vhosts (10-xbrain.conf, 20-api.conf, 30-projects.conf, et un éventuel 40-lang.conf). Redémarrer nginx et vérifier.

**Files:**
- `infrastructure/nginx/conf.d/30-projects.conf` (créer si absent)

**How:**

1. Vérifier si le fichier existe : `ls infrastructure/nginx/conf.d/30-projects.conf`. S'il existe déjà (créé en 05-05), passer à l'étape 3.

2. Si absent, créer avec le contenu documenté dans les PATTERNS.md section 11 et le plan 05-05 Task 3 :
   ```nginx
   resolver 127.0.0.11 valid=30s ipv6=off;

   server {
       listen 80;
       server_name projects.dejavu.cat;

       real_ip_header CF-Connecting-IP;
       set_real_ip_from 0.0.0.0/0;

       # Fallback redirect vers Firebase Hosting
       # Normalement, le DNS Cloudflare pointe directement vers Firebase (CNAME proxied)
       return 301 https://xbrain-495115.web.app$request_uri;

       location /nginx-health {
           return 200 "ok\n";
           access_log off;
       }
   }
   ```

3. Tester la configuration nginx :
   ```bash
   docker compose exec nginx nginx -t
   docker compose restart nginx
   # Attendre 5s
   docker compose exec nginx curl -sf http://127.0.0.1/nginx-health
   ```

4. Vérifier que les 3+ vhosts sont tous valides (10-xbrain, 20-api, 30-projects) :
   ```bash
   ls infrastructure/nginx/conf.d/
   docker compose exec nginx nginx -t
   ```

**Test:** `docker compose exec nginx nginx -t` retourne `test is successful`.

---

## Verification

```bash
# 1. Vérifier que le runbook Cloudflare Access existe
ls docs/cloudflare-access-setup.md
wc -l docs/cloudflare-access-setup.md
# Attendu : >40 lignes

# 2. Vérifier les nouvelles variables dans .env.example
grep -c "GITHUB_CLIENT_ID\|GITHUB_ORG_PAT\|GRAPHITI_LLM_MODEL\|GCP_PROJECT_NUMBER" infrastructure/.env.example
# Attendu : >= 4

# 3. Vérifier nginx
docker compose exec nginx nginx -t
# Attendu : syntax is ok

# 4. Vérifier que les 3 vhosts existent
ls infrastructure/nginx/conf.d/{10-xbrain.conf,20-api.conf,30-projects.conf}

# 5. Check complet des variables requises Phase 5 (présence dans .env.example)
python3 -c "
required = ['GITHUB_CLIENT_ID', 'GITHUB_CLIENT_SECRET', 'GITHUB_CALLBACK_URL',
            'GITHUB_ORG_PAT', 'GITHUB_ORG_NAME', 'GRAPHITI_LLM_MODEL']
content = open('infrastructure/.env.example').read()
missing = [v for v in required if v not in content]
if missing:
    print(f'MISSING in .env.example: {missing}')
    exit(1)
print('All required Phase 5 vars in .env.example OK')
"
```

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → projects.dejavu.cat | Cloudflare Access est la seule porte d'entrée |
| Cloudflare → Firebase Hosting | Trafic proxifié par Cloudflare — origine (Firebase) non accessible directement |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Elevation of Privilege | Accès direct à xbrain-495115.web.app (URL Firebase) bypass Cloudflare Access | accept | L'URL `.web.app` est publique par nature de Firebase Hosting. Le dashboard contient des données internes (membres, URLs) — risque acceptable pour une petite équipe. Pour mitiger totalement : utiliser Firebase Auth en plus de Cloudflare Access (hors scope Phase 5). |
| T-05-06-02 | Information Disclosure | .env.example commité avec de vraies valeurs | mitigate | .env.example contient UNIQUEMENT des valeurs vides ou des valeurs par défaut non-sensibles. Jamais de secrets réels. |
| T-05-06-03 | Tampering | nginx 30-projects.conf redirige vers une URL différente de Firebase | accept | Fichier de config dans le repo versionné. Toute modification est tracée dans git. |

## Success Criteria

- `docs/cloudflare-access-setup.md` existe avec les 4 étapes documentées (Firebase custom domain, Identity Provider, Application, Policy).
- `infrastructure/.env.example` contient toutes les variables Phase 5 : GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, GITHUB_CALLBACK_URL, GITHUB_ORG_PAT, GITHUB_ORG_NAME, GRAPHITI_LLM_MODEL.
- `infrastructure/nginx/conf.d/30-projects.conf` existe et passe `nginx -t`.
- `docker compose restart nginx` réussit sans erreur.

## Output

Après complétion, créer `.planning/phases/05-plateforme-projets-equipe/05-06-SUMMARY.md`.
