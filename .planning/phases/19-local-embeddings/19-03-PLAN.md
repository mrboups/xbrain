---
phase: 19-local-embeddings
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - apps/memory-api/tests/conftest.py
  - apps/memory-api/tests/test_local_embeddings.py
  - docs/embeddings.md
autonomous: true
requirements: [EMBED-01]

must_haves:
  truths:
    - "With NO OPENAI_API_KEY set, ingesting distinct-topic documents through NativeProvider (real Postgres + real Qdrant) embeds them locally and semantic search ranks the topically-matching document FIRST — proven with the real fastembed embedder, not a mock"
    - "The GET /v1/memory/search HTTP route returns the same top hit as the provider call (the full request path works end to end, keyless)"
    - "The collection is created at 384 dims (not 1536) when EMBEDDINGS_PROVIDER=local"
    - "A garbage EMBEDDINGS_PROVIDER value falls back to local and does not raise"
    - "With EMBEDDINGS_PROVIDER=openai + a key, get_embedder() returns the OpenAI path unchanged (regression coverage)"
    - "The dimension-switch / re-embed migration story is documented, not silently broken"
  artifacts:
    - path: "apps/memory-api/tests/conftest.py"
      provides: "qdrant_url session fixture (testcontainers QdrantContainer, skip if no Docker)"
      contains: "QdrantContainer"
    - path: "apps/memory-api/tests/test_local_embeddings.py"
      provides: "the gate-lesson semantic-ranking test + dimension + fallback + OpenAI-regression tests"
      contains: "def test_"
    - path: "docs/embeddings.md"
      provides: "provider config + offline guarantee + dimension/re-embed migration runbook (D-19-03 path b)"
      contains: "re-embed"
  key_links:
    - from: "apps/memory-api/tests/test_local_embeddings.py"
      to: "app.embedders.local_embedder + NativeProvider (real Qdrant)"
      via: "no-mock semantic ranking assertion against a real testcontainers Qdrant"
      pattern: "assert.*results\\[0\\]"
    - from: "apps/memory-api/tests/conftest.py"
      to: "testcontainers.qdrant.QdrantContainer"
      via: "qdrant_url session fixture mirroring pg_url"
      pattern: "QdrantContainer"
---

<objective>
Prove EMBED-01 the way the gate lesson demands: against a REAL Postgres + REAL Qdrant (testcontainers), with
NO OpenAI key set, a document is ingested → embedded by the real local fastembed model → written to Qdrant →
retrieved by semantic search that ranks the topically-matching document first. A mocked embedder returns a
fake vector and cannot fail a semantic-ranking assertion even if the integration is broken — so the test uses
the real embedder. Also lock in the regressions: OpenAI stays selectable, garbage config falls back to local,
and the dimension-switch/re-embed path is documented.

Purpose: turn SC#1/SC#3/SC#5 into executable proof and document the D-19-03 migration path.
Output: a `qdrant_url` testcontainers fixture, `test_local_embeddings.py`, and `docs/embeddings.md`.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-local-embeddings/19-CONTEXT.md
@.planning/phases/19-local-embeddings/19-RESEARCH.md
@.planning/phases/19-local-embeddings/19-PATTERNS.md

<interfaces>
<!-- Extracted from the live codebase — work from these, no exploration needed. -->

Existing pg_url fixture to mirror (apps/memory-api/tests/conftest.py:56-123) — session-scoped, Docker-gated:
```python
def _docker_available() -> bool:
    try:
        import docker
        docker.from_env().ping()
        return True
    except Exception:
        return False

@pytest_asyncio.fixture(scope="session")
async def pg_url() -> AsyncGenerator[str, None]:
    if not _docker_available():
        pytest.skip("Docker not available — skipping integration fixture")
    from testcontainers.postgres import PostgresContainer
    pg = PostgresContainer("postgres:17", ...); pg.start()
    ...
    yield asyncpg_url
    pg.stop()
```
The `session` fixture (conftest.py:126+) yields an AsyncSession on top of pg_url. There is also an httpx
ASGI `client` fixture used by route tests (used for the GET /v1/memory/search check).

NativeProvider API (packages/memory-models/xbrain_memory/providers/native_provider.py): async `.upsert(item)`
and `.search(query, ...)`; both call `self._embedder` (the SAME injected callable) — so a write embedded with
the local model is read back with the local model. The 7-field tagging contract (team_scope, project_scope,
visibility, confidence, truth_level, source, validation_status) is REQUIRED on every write.

Embedder seam (Plan 01): `app.embedders.get_embedder()` → local_embedder by default; `get_embedding_dimension()`
→ 384 (local) / 1536 (openai); `EmbeddingDimensionMismatch` raised on an existing-collection mismatch.

RESEARCH Validation Approach (the authoritative test list) checks 1-7. This plan implements 1-4, 6, 7.
Check 5 (`docker run --network none`) is Plan 02 Task 3. respx is already a dev dep and is used by
apps/memory-api/tests/test_phase12_org_membership.py — reuse that pattern for the OpenAI-path unit test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a qdrant_url testcontainers fixture mirroring pg_url</name>
  <files>apps/memory-api/tests/conftest.py</files>
  <read_first>
    - apps/memory-api/tests/conftest.py (the _docker_available guard ~lines 56-64 and the pg_url fixture ~lines 67-123)
    - .planning/phases/19-local-embeddings/19-PATTERNS.md (conftest.py Pattern Assignment — mirror pg_url exactly)
    - .planning/phases/19-local-embeddings/19-RESEARCH.md (Wave 0 Gaps — the qdrant fixture is called out)
  </read_first>
  <action>
    Add a session-scoped `qdrant_url` fixture to `apps/memory-api/tests/conftest.py`, mirroring `pg_url`:
      - reuse the existing `_docker_available()` guard and `pytest.skip(...)` when Docker is unavailable;
      - `from testcontainers.qdrant import QdrantContainer` (provided by the `qdrant` extra Plan 01 added);
      - start a `QdrantContainer` (pin the image to match the stack, e.g. `qdrant/qdrant:v1.17.1`);
      - yield the HTTP URL the qdrant-client expects (get the mapped host+port; e.g.
        `f"http://{q.get_container_host_ip()}:{q.get_exposed_port(6333)}"` — confirm the accessor names against
        the installed testcontainers version);
      - set `os.environ["QDRANT_URL"]` to the yielded URL AND patch `app.config.settings.QDRANT_URL` directly
        (same reasoning the pg_url fixture documents: the settings singleton may already be frozen), so
        `ensure_collections()` and NativeProvider both target the container;
      - `.stop()` after the yield.
    Do NOT alter the existing pg_url/session/client fixtures.
  </action>
  <acceptance_criteria>
    - `grep -n 'from testcontainers.qdrant import QdrantContainer' apps/memory-api/tests/conftest.py` matches
    - `grep -n 'async def qdrant_url' apps/memory-api/tests/conftest.py` matches
    - `grep -n 'scope="session"' apps/memory-api/tests/conftest.py` matches for the new fixture (session-scoped)
    - `grep -c 'async def pg_url' apps/memory-api/tests/conftest.py` returns 1 (pg_url untouched)
    - `python -c "import ast; ast.parse(open('apps/memory-api/tests/conftest.py').read()); print('parse OK')"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -c "import ast; ast.parse(open('tests/conftest.py').read()); print('conftest parses')" && grep -q 'QdrantContainer' tests/conftest.py && grep -q 'async def qdrant_url' tests/conftest.py && echo "fixture present"</automated>
  </verify>
  <done>conftest.py exposes a Docker-gated, session-scoped qdrant_url fixture that starts a real Qdrant container and points settings.QDRANT_URL at it; existing fixtures unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The gate-lesson test — real local embed → Qdrant → semantic retrieval, plus regressions</name>
  <files>apps/memory-api/tests/test_local_embeddings.py</files>
  <read_first>
    - apps/memory-api/tests/conftest.py (the pg_url, session, client, and new qdrant_url fixtures)
    - apps/memory-api/tests/test_phase12_org_membership.py (the respx HTTP-mock pattern for the OpenAI-path unit test)
    - packages/memory-models/xbrain_memory/providers/native_provider.py (the .upsert / .search signatures + the item shape / required tagging fields)
    - apps/memory-api/app/routes/memory.py (the GET /v1/memory/search request/response shape for check #4)
    - .planning/phases/19-local-embeddings/19-RESEARCH.md (Validation Approach checks 1-4, 6, 7 — the authoritative assertions)
  </read_first>
  <behavior>
    - check1: with EMBEDDINGS_PROVIDER=local + no OPENAI_API_KEY, ensure_collections() creates the collection at size==384 (queried from the real Qdrant container), NOT 1536
    - check2: upserting 3-4 distinct-topic docs (e.g. "team offsite in Lisbon", "Q3 budget numbers", "deploying memory-api") each succeeds; Qdrant point count == number of docs
    - check3 (THE gate lesson): provider.search("where are we going for the team trip?") ranks the Lisbon-offsite item FIRST by score — a real semantic assertion a mocked/fake vector could not satisfy
    - check4: GET /v1/memory/search for the same query returns the same top hit through the real HTTP route
    - check7: reloading config with a garbage EMBEDDINGS_PROVIDER falls back to local and does not raise
    - check6: with EMBEDDINGS_PROVIDER=openai + OPENAI_API_KEY set, get_embedder() is openai_embedder and an upsert calls the (respx-mocked) OpenAI embeddings endpoint — the OpenAI path is unchanged
  </behavior>
  <action>
    Create `apps/memory-api/tests/test_local_embeddings.py`. Mark the integration tests with the existing
    `@pytest.mark.integration` convention and depend on the `pg_url`, `session`/`client`, and `qdrant_url`
    fixtures. Set the environment for the local path explicitly at the top of the integration tests:
    `EMBEDDINGS_PROVIDER=local`, `OPENAI_API_KEY=""` — and reload `app.config` / `app.embedders` so the
    selector picks local (mirror the reload pattern used in Plan 01's smoke).

    Implement (RESEARCH checks 1-4, 7 as integration; 6 as a respx unit test):
      1. `test_local_collection_created_at_384`: call `ensure_collections()`, then
         `client.get_collection(settings.QDRANT_COLLECTION)` and assert `config.params.vectors.size == 384`.
      2. `test_local_ingest_writes_vectors`: build the memory provider (`get_memory_provider()` / `_build_provider()`),
         upsert 3-4 items each carrying the FULL 7-field tagging contract and distinct topics; assert each
         upsert succeeds and the Qdrant point count equals the number of items.
      3. `test_local_semantic_search_ranks_expected_first` (THE gate lesson): call
         `provider.search("where are we going for the team trip?", team_scope=<same>)` and assert the
         Lisbon-offsite item is `results[0]` (top-ranked). NO mock on the embedder — the real fastembed model
         runs. Add an inline comment: "the real fastembed embedder MUST run here; a mocked/fake vector could
         not satisfy this ranking assertion — this is the gate lesson."
      4. `test_http_memory_search_returns_same_top_hit`: via the httpx `client` fixture, GET `/v1/memory/search`
         (with the auth/headers the route requires) for the same query and assert the same top item id.
      7. `test_garbage_provider_falls_back_to_local`: reload config with `EMBEDDINGS_PROVIDER="not-a-provider"`
         and assert `get_embedder() is local_embedder` and `get_embedding_dimension() == 384`, no exception.
      6. `test_openai_path_unchanged_when_selected` (respx unit, no testcontainers): set
         `EMBEDDINGS_PROVIDER=openai` + a dummy `OPENAI_API_KEY`, mock the OpenAI embeddings HTTP endpoint with
         respx returning a 1536-length vector, assert `get_embedder() is openai_embedder` and that calling it
         hits the mocked endpoint and returns len==1536.

    NOTE on the offline guarantee: this pytest runs in an environment WITH network, so fastembed may download
    the model on first use — that is acceptable here (it proves the semantic integration). The authoritative
    zero-network proof is Plan 02 Task 3's `docker run --network none`. Do not add a network-block to this test.
  </action>
  <acceptance_criteria>
    - `grep -n 'def test_local_semantic_search_ranks_expected_first' apps/memory-api/tests/test_local_embeddings.py` matches
    - the semantic test asserts on `results[0]` (top-rank), not merely on presence: `grep -n 'results\[0\]' apps/memory-api/tests/test_local_embeddings.py` matches
    - `grep -c 'mock' apps/memory-api/tests/test_local_embeddings.py` returns 0 on the local/semantic path (the only mocking allowed is respx in the OpenAI-path test — keep it in a clearly-named openai test)
    - `grep -n 'size == 384' apps/memory-api/tests/test_local_embeddings.py` matches (dimension check)
    - `grep -n 'test_garbage_provider_falls_back_to_local' apps/memory-api/tests/test_local_embeddings.py` matches
    - `grep -n 'respx' apps/memory-api/tests/test_local_embeddings.py` matches (OpenAI regression uses respx)
    - `pytest apps/memory-api/tests/test_local_embeddings.py -q` exits 0 (integration tests run when Docker is available; skip cleanly otherwise)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/memory-api && python -m pytest tests/test_local_embeddings.py -q</automated>
  </verify>
  <done>test_local_embeddings.py proves keyless local ingest→Qdrant→semantic-search ranking against real containers with NO mock on the embedder, verifies the collection is 384-dim, the HTTP route returns the same top hit, garbage config falls back to local, and the OpenAI path is unchanged; the suite passes (or skips cleanly without Docker).</done>
</task>

<task type="auto">
  <name>Task 3: Document the provider config, offline guarantee, and dimension/re-embed migration path</name>
  <files>docs/embeddings.md</files>
  <read_first>
    - .planning/phases/19-local-embeddings/19-CONTEXT.md (D-19-03 — the two dimension paths (a) fresh install, (b) documented re-embed for an existing 1536-dim install)
    - .planning/phases/19-local-embeddings/19-RESEARCH.md (Don't Hand-Roll — the re-embed is a plain script iterating memory_items + calling provider.upsert; large-corpus re-embed performance is explicitly deferred)
    - .env.example (link operators to the EMBEDDINGS_PROVIDER / LOCAL_EMBEDDING_MODEL / UVICORN_WORKERS knobs Plan 02 added)
  </read_first>
  <action>
    Write `docs/embeddings.md` (English, product docs) covering:
      1. Providers: `EMBEDDINGS_PROVIDER=local` (default — BAAI/bge-small-en-v1.5, 384-dim, in-container,
         keyless, no external call) vs `openai` (text-embedding-3-small, 1536-dim, needs OPENAI_API_KEY).
         State the "one key — Anthropic OR OpenAI OR Grok" promise: embeddings no longer force OpenAI.
      2. Offline guarantee: weights are baked into the memory-api image at build; `HF_HUB_OFFLINE=1` at runtime;
         no network call at inference (cite the `docker run --network none` proof).
      3. RAM / workers: OSS-light runs `UVICORN_WORKERS=1` so the model loads once (~256-366 MB); why 2 workers
         would duplicate it; SaaS keeps 2 workers with `EMBEDDINGS_PROVIDER=openai`.
      4. Dimension handling (D-19-03):
         - (a) Fresh install: the collection is created at the provider's dimension automatically (384 for local).
           Nothing to do.
         - (b) Switching an existing install that already has 1536-dim OpenAI vectors to `local`: Qdrant cannot
           mix dimensions in one collection, so the app FAILS LOUD at boot with `EmbeddingDimensionMismatch`
           rather than corrupting data. To migrate: either wipe+recreate the collection (superadmin wipe, then
           re-ingest) OR run a re-embed — a plain script that iterates `memory_items` in Postgres (the content
           source of truth) and calls `provider.upsert(...)` again per row so each item is re-embedded at the
           new dimension. Note large-corpus re-embed performance is out of scope for this phase; a fresh
           OSS-light install has nothing to re-embed.
      5. Selecting OpenAI: set `EMBEDDINGS_PROVIDER=openai` + `OPENAI_API_KEY` — no code change.
  </action>
  <acceptance_criteria>
    - `docs/embeddings.md` exists
    - `grep -in 'EMBEDDINGS_PROVIDER' docs/embeddings.md` matches
    - `grep -in 're-embed' docs/embeddings.md` matches (migration path documented)
    - `grep -in 'network none\|HF_HUB_OFFLINE\|offline' docs/embeddings.md` matches (offline guarantee documented)
    - `grep -in 'EmbeddingDimensionMismatch\|fail' docs/embeddings.md` matches (fail-loud behavior documented)
    - `grep -in '384' docs/embeddings.md` and `grep -in '1536' docs/embeddings.md` both match (both dimensions stated)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/embeddings.md && grep -iq 're-embed' docs/embeddings.md && grep -iq 'EMBEDDINGS_PROVIDER' docs/embeddings.md && grep -iq 'offline' docs/embeddings.md && echo "doc OK"</automated>
  </verify>
  <done>docs/embeddings.md documents provider selection, the keyless/offline guarantee, the worker/RAM note, and the fresh-install vs re-embed dimension paths (D-19-03), so the migration story is documented, not silently broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → real integration | the gate-lesson risk: a mock at this boundary yields false assurance the integration works |
| operator switching providers → existing persisted vectors | a provider switch on an existing corpus can strand incompatible-dimension vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Repudiation / false assurance | test_local_embeddings.py | mitigate | The semantic-ranking test runs the REAL fastembed embedder against a REAL Qdrant (testcontainers) with no OpenAI key and asserts top-rank ordering — a fake/mocked vector cannot satisfy the ranking assertion, so a broken integration cannot pass green (the gate lesson). Acceptance greps enforce zero mocks on the semantic path. |
| T-19-08 | Tampering / data integrity (operational) | docs/embeddings.md re-embed runbook | mitigate (fresh) / document (existing) | The dimension-switch path is documented: fresh installs create the collection at the right dimension automatically; existing 1536-dim installs get a boot-fatal `EmbeddingDimensionMismatch` (from Plan 01) instead of silent corruption, plus a documented re-embed procedure (iterate memory_items → re-upsert). Large-corpus re-embed performance is explicitly out of scope. |
</threat_model>

<verification>
- `pytest tests/test_local_embeddings.py -q` passes: 384-dim collection, keyless ingest, semantic top-rank,
  HTTP route parity, garbage→local fallback, OpenAI-path regression (respx).
- conftest.py parses and exposes the Docker-gated qdrant_url fixture.
- docs/embeddings.md documents providers, offline guarantee, worker/RAM, and the re-embed migration path.
</verification>

<success_criteria>
- The keyless local path is proven end-to-end against real Postgres + real Qdrant with a genuine semantic
  ranking assertion (no embedder mock).
- The OpenAI path and its 1536-dim behavior are covered as a regression.
- The dimension-switch / re-embed story is documented (D-19-03), not silently broken.
</success_criteria>

<output>
After completion, create `.planning/phases/19-local-embeddings/19-03-SUMMARY.md`.
</output>
