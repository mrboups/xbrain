---
phase: 02-memoire-intelligente-agents
plan: 09
type: execute
wave: 4
depends_on: ["02-06"]
files_modified:
  - infrastructure/docker-compose.yml
  - infrastructure/langfuse/.env.langfuse-template
  - apps/agent-runtime/app/observability.py
  - apps/agent-runtime/app/main.py
  - apps/agent-runtime/app/graphs/echo_with_hitl.py
  - apps/agent-runtime/app/graphs/ingestion.py
  - apps/agent-runtime/app/graphs/second_opinion.py
  - apps/openwebui-pipeline/app/observability.py
  - apps/openwebui-pipeline/app/main.py
  - .env.example
autonomous: false
requirements:
  - OBS-02
  - OBS-03
  - OBS-05
must_haves:
  truths:
    - "Langfuse self-hosted tourne sur la VM (langfuse + langfuse-worker + clickhouse + redis), accessible via https://langfuse.x.dejavu.cat (ou sub-domaine équivalent)"
    - "agent-runtime instrumente CHAQUE LangGraph run avec langfuse.CallbackHandler — chaque appel LLM génère une trace dans Langfuse"
    - "openwebui-pipeline instrumente CHAQUE chat completion (Claude + OpenAI) avec une trace Langfuse — tagged team_scope, model, sub"
    - "Langfuse dashboard montre : token spend per team, breakdown par model, top traces by latency, top errors"
    - "Admin peut filter Langfuse par metadata.team_scope='X' pour voir consommation team-isolée"
    - "VM mem usage post-Langfuse < 7 GB (gate : si plus, on splite Langfuse sur VM séparée Phase 2.5)"
  artifacts:
    - path: infrastructure/docker-compose.yml
      provides: "+ services: langfuse, langfuse-worker, clickhouse, redis (Langfuse stack)"
      contains: "langfuse"
    - path: apps/agent-runtime/app/observability.py
      provides: "Langfuse client + LangGraph callback handler factory"
      min_lines: 40
    - path: apps/openwebui-pipeline/app/observability.py
      provides: "Langfuse trace per chat completion"
      min_lines: 40
---

<objective>
Self-host Langfuse v3 sur la VM (web + worker + ClickHouse + Redis) et instrumenter agent-runtime + openwebui-pipeline pour push les traces. Métriques live : token spend par team, latency par model, error rates.

Purpose : Sans observabilité, on vole en aveugle Phase 2 (agents nondeterministes, multi-LLM). OBS-02/03/05 = visibilité prod-grade pour l'admin.

Output : Langfuse dashboard live + 2 services instrumentés + admin peut voir cost per team.
</objective>

<execution_context>
@D:/VSC/xbrain/.claude/get-shit-done/workflows/execute-plan.md
@D:/VSC/xbrain/.claude/get-shit-done/templates/summary.md
@D:/VSC/xbrain/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/02-memoire-intelligente-agents/02-RESEARCH.md
@apps/agent-runtime/app/main.py
@apps/openwebui-pipeline/app/main.py
@infrastructure/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Langfuse stack à docker-compose (langfuse + worker + clickhouse + redis)</name>
  <files>infrastructure/docker-compose.yml, infrastructure/langfuse/.env.langfuse-template</files>
  <read_first>infrastructure/docker-compose.yml, .planning/phases/02-memoire-intelligente-agents/02-RESEARCH.md (section Q7)</read_first>
  <action>
Ajouter à `infrastructure/docker-compose.yml` :

```yaml
  langfuse-clickhouse:
    image: clickhouse/clickhouse-server:24.8
    container_name: xbrain-langfuse-clickhouse
    restart: unless-stopped
    environment:
      CLICKHOUSE_DB: default
      CLICKHOUSE_USER: clickhouse
      CLICKHOUSE_PASSWORD: ${LANGFUSE_CLICKHOUSE_PASSWORD}
    volumes:
      - langfuse_clickhouse_data:/var/lib/clickhouse
    networks: [xbrain_net]
    mem_limit: 2g
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8123/ping"]
      interval: 15s
      timeout: 5s
      retries: 10

  langfuse-redis:
    image: redis:7-alpine
    container_name: xbrain-langfuse-redis
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${LANGFUSE_REDIS_PASSWORD}"]
    volumes: [langfuse_redis_data:/data]
    networks: [xbrain_net]
    mem_limit: 96m
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${LANGFUSE_REDIS_PASSWORD}", "ping"]
      interval: 15s

  langfuse-worker:
    image: langfuse/langfuse-worker:3
    container_name: xbrain-langfuse-worker
    restart: unless-stopped
    environment:
      DATABASE_URL: ${LANGFUSE_POSTGRES_URL}
      CLICKHOUSE_URL: http://langfuse-clickhouse:8123
      CLICKHOUSE_USER: clickhouse
      CLICKHOUSE_PASSWORD: ${LANGFUSE_CLICKHOUSE_PASSWORD}
      CLICKHOUSE_MIGRATION_URL: clickhouse://langfuse-clickhouse:9000
      REDIS_HOST: langfuse-redis
      REDIS_AUTH: ${LANGFUSE_REDIS_PASSWORD}
      SALT: ${LANGFUSE_SALT}
      ENCRYPTION_KEY: ${LANGFUSE_ENCRYPTION_KEY}
      TELEMETRY_ENABLED: "false"
    networks: [xbrain_net]
    mem_limit: 768m
    depends_on:
      langfuse-clickhouse: { condition: service_healthy }
      langfuse-redis: { condition: service_healthy }
      postgres: { condition: service_healthy }

  langfuse:
    image: langfuse/langfuse:3
    container_name: xbrain-langfuse
    restart: unless-stopped
    environment:
      DATABASE_URL: ${LANGFUSE_POSTGRES_URL}
      NEXTAUTH_URL: https://langfuse.x.dejavu.cat
      NEXTAUTH_SECRET: ${LANGFUSE_NEXTAUTH_SECRET}
      SALT: ${LANGFUSE_SALT}
      ENCRYPTION_KEY: ${LANGFUSE_ENCRYPTION_KEY}
      CLICKHOUSE_URL: http://langfuse-clickhouse:8123
      CLICKHOUSE_USER: clickhouse
      CLICKHOUSE_PASSWORD: ${LANGFUSE_CLICKHOUSE_PASSWORD}
      CLICKHOUSE_MIGRATION_URL: clickhouse://langfuse-clickhouse:9000
      REDIS_HOST: langfuse-redis
      REDIS_AUTH: ${LANGFUSE_REDIS_PASSWORD}
      LANGFUSE_INIT_USER_EMAIL: team@example.com
      LANGFUSE_INIT_USER_NAME: "Mr Boups"
      LANGFUSE_INIT_PROJECT_NAME: xbrain
      TELEMETRY_ENABLED: "false"
    networks: [xbrain_net]
    mem_limit: 768m
    depends_on:
      langfuse-worker: { condition: service_started }
    healthcheck:
      test: ["CMD-SHELL", "wget -qO - http://127.0.0.1:3000/api/public/health 2>/dev/null | grep -q ok"]
      interval: 30s
      retries: 5
      start_period: 60s
```

Ajouter volumes : `langfuse_clickhouse_data`, `langfuse_redis_data`.

Ajouter nginx server block pour `langfuse.x.dejavu.cat` (proxy → langfuse:3000).

Ajouter à `.env.example` :
```bash
# Langfuse self-hosted
LANGFUSE_POSTGRES_URL=postgresql://xbrain:${POSTGRES_PASSWORD}@postgres:5432/langfuse
LANGFUSE_CLICKHOUSE_PASSWORD=__FILL_RANDOM_32__
LANGFUSE_REDIS_PASSWORD=__FILL_RANDOM_32__
LANGFUSE_SALT=__FILL_RANDOM_64__
LANGFUSE_ENCRYPTION_KEY=__FILL_RANDOM_64_HEX__
LANGFUSE_NEXTAUTH_SECRET=__FILL_RANDOM_64__
LANGFUSE_PUBLIC_KEY=__SET_AFTER_FIRST_LOGIN__
LANGFUSE_SECRET_KEY=__SET_AFTER_FIRST_LOGIN__
LANGFUSE_HOST=https://langfuse.x.dejavu.cat
```

**Pré-requis** : créer la database `langfuse` dans postgres principal :
```sql
CREATE DATABASE langfuse OWNER xbrain;
```
(à scripter dans `infrastructure/scripts/init-langfuse-db.sh` avec idempotency).
  </action>
  <verify>cd D:/VSC/xbrain && python -c "import yaml; doc = yaml.safe_load(open('infrastructure/docker-compose.yml')); langfuse_services = [s for s in doc['services'] if 'langfuse' in s]; print('langfuse services:', langfuse_services)"</verify>
  <acceptance_criteria>
    - 4 nouveaux services dans compose : langfuse, langfuse-worker, langfuse-clickhouse, langfuse-redis
    - Total Langfuse mem_limit = ~3.5GB (worker 768 + ui 768 + clickhouse 2048 + redis 96)
    - 2 nouveaux volumes : langfuse_clickhouse_data, langfuse_redis_data
    - .env.example inclut LANGFUSE_* avec placeholders
    - Init script Postgres database `langfuse`
  </acceptance_criteria>
  <done>Langfuse stack dans compose</done>
</task>

<task type="auto">
  <name>Task 2: Instrument agent-runtime avec Langfuse CallbackHandler</name>
  <files>apps/agent-runtime/app/observability.py, apps/agent-runtime/app/main.py, apps/agent-runtime/app/graphs/echo_with_hitl.py, apps/agent-runtime/app/graphs/ingestion.py, apps/agent-runtime/app/graphs/second_opinion.py</files>
  <read_first>apps/agent-runtime/app/main.py, apps/agent-runtime/app/config.py</read_first>
  <action>
**`apps/agent-runtime/pyproject.toml`** : add `langfuse>=4.5`.

**`apps/agent-runtime/app/observability.py`** :
```python
"""Langfuse instrumentation."""
from langfuse.callback import CallbackHandler
from langfuse import Langfuse
from app.config import settings


_langfuse: Langfuse | None = None
def get_langfuse() -> Langfuse | None:
    global _langfuse
    if _langfuse is not None:
        return _langfuse
    if not (settings.LANGFUSE_PUBLIC_KEY and settings.LANGFUSE_SECRET_KEY):
        return None
    _langfuse = Langfuse(
        public_key=settings.LANGFUSE_PUBLIC_KEY,
        secret_key=settings.LANGFUSE_SECRET_KEY,
        host=settings.LANGFUSE_HOST,
    )
    return _langfuse


def make_callback_handler(thread_id: str, agent_name: str, team_scope: str, sub: str) -> CallbackHandler | None:
    """Create a per-run Langfuse callback. Tags: team_scope, agent, sub."""
    if not get_langfuse():
        return None
    return CallbackHandler(
        public_key=settings.LANGFUSE_PUBLIC_KEY,
        secret_key=settings.LANGFUSE_SECRET_KEY,
        host=settings.LANGFUSE_HOST,
        trace_name=f"agent:{agent_name}",
        session_id=thread_id,
        user_id=sub,
        metadata={"team_scope": team_scope, "agent": agent_name},
        tags=[f"team:{team_scope}", f"agent:{agent_name}"],
    )
```

**`app/routes/agents.py`** : pass `make_callback_handler(...)` to LangGraph `astream(config={"callbacks": [handler]})`.

Update Settings dans `config.py` : add `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST`.

Pas besoin de modifier les graphs eux-mêmes — les callbacks sont passés au runtime.
  </action>
  <verify>cd apps/agent-runtime && python -c "from app.observability import make_callback_handler, get_langfuse; print('ok, langfuse client:', get_langfuse() is None and 'not configured' or 'ready')"</verify>
  <acceptance_criteria>
    - `observability.py` exporte `make_callback_handler` (None si non configuré, sinon CallbackHandler)
    - Routes agents passent le handler à LangGraph via `config={"callbacks": [handler]}`
    - Settings inclut LANGFUSE_PUBLIC_KEY / SECRET_KEY / HOST
    - graceful degrade : si pas configuré, agent run normalement sans trace
  </acceptance_criteria>
  <done>agent-runtime instrumented</done>
</task>

<task type="auto">
  <name>Task 3: Instrument openwebui-pipeline avec Langfuse trace per chat completion</name>
  <files>apps/openwebui-pipeline/app/observability.py, apps/openwebui-pipeline/app/main.py</files>
  <read_first>apps/openwebui-pipeline/app/main.py</read_first>
  <action>
**`apps/openwebui-pipeline/pyproject.toml`** : add `langfuse>=4.5`.

**`apps/openwebui-pipeline/app/observability.py`** : Langfuse client + helper `trace_chat_completion(sub, team_scope, model, prompt_messages, response_text, latency_ms, tokens_in, tokens_out)`.

**`app/main.py`** : autour de chaque appel `anthropic_client.messages.create()` ou `openai_client.chat.completions.create()`, mesurer latency + count tokens, push trace.

```python
import time
from app.observability import trace_chat_completion

# inside _handle_anthropic non-stream:
start = time.time()
r = await anthropic_client.messages.create(...)
latency_ms = int((time.time() - start) * 1000)
trace_chat_completion(
    sub=sub, team_scope=team_scope, model=body.model,
    prompt_messages=msgs, response_text=assistant_content,
    latency_ms=latency_ms,
    tokens_in=r.usage.input_tokens if r.usage else None,
    tokens_out=r.usage.output_tokens if r.usage else None,
)
```

Idem pour OpenAI provider et streaming.
  </action>
  <verify>cd apps/openwebui-pipeline && python -c "from app.observability import trace_chat_completion; print('ok')"</verify>
  <acceptance_criteria>
    - `observability.py` exporte `trace_chat_completion`
    - Tous les paths Anthropic + OpenAI (stream + non-stream) appellent trace_chat_completion
    - Métadonnées : team_scope, sub, model, latency, tokens
    - graceful degrade si Langfuse pas configuré
  </acceptance_criteria>
  <done>openwebui-pipeline instrumented</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-needed>
1. Ajouter DNS Cloudflare : `langfuse.x.dejavu.cat A __VM_HOST__` (orange cloud / proxy).
2. Générer et coller les 5 secrets Langfuse dans `.env` :
   ```bash
   openssl rand -base64 32  # → LANGFUSE_CLICKHOUSE_PASSWORD
   openssl rand -base64 32  # → LANGFUSE_REDIS_PASSWORD
   openssl rand -base64 64  # → LANGFUSE_SALT
   openssl rand -hex 32     # → LANGFUSE_ENCRYPTION_KEY
   openssl rand -base64 64  # → LANGFUSE_NEXTAUTH_SECRET
   ```
3. Lancer init Postgres database :
   ```bash
   ssh -i ~/.ssh/xbrain_key user@__VM_HOST__ 'docker exec xbrain-postgres psql -U xbrain -c "CREATE DATABASE langfuse OWNER xbrain;"'
   ```
4. Deploy : `make deploy`
5. Une fois Langfuse healthy : naviguer https://langfuse.x.dejavu.cat → first user signup auto-créé via `LANGFUSE_INIT_*` env → settings → générer Public Key + Secret Key → coller dans `.env` (LANGFUSE_PUBLIC_KEY/SECRET_KEY)
6. Re-deploy agent-runtime + openwebui-pipeline pour pick up les keys
  </what-needed>
  <resume-signal>Type "done" quand Langfuse dashboard accessible + agent-runtime + openwebui-pipeline restartés avec keys</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Langfuse self-hosted live + 2 services instrumented.</what-built>
  <how-to-verify>
1. Visite https://langfuse.x.dejavu.cat → login (ton compte créé via LANGFUSE_INIT)
2. Dashboard "xbrain" project → tab "Traces"
3. Dans Open WebUI, lance une conversation → vérifier qu'une nouvelle trace apparaît dans Langfuse en <30s
4. Dans Open WebUI, lance `/ingest https://example.com` → vérifier qu'une trace `agent:ingestion` apparaît avec session_id = thread_id
5. Filtre Langfuse par `metadata.team_scope=default` → toutes tes traces visibles
6. `make vm-ps` → 4 services langfuse* tous healthy, total mem usage < 7 GB
  </how-to-verify>
  <resume-signal>Type "approved" si dashboard + traces visibles, sinon décris</resume-signal>
</task>

</tasks>

<verification>
- [ ] 4 services Langfuse healthy
- [ ] VM mem usage < 7 GB
- [ ] Trace per chat completion (Anthropic + OpenAI paths)
- [ ] Trace per agent run (LangGraph callback)
- [ ] Filter par team_scope dans dashboard fonctionne
</verification>

<success_criteria>
- All tasks completed
- 3 requirements couverts (OBS-02, OBS-03, OBS-05)
- Admin a un dashboard cost/latency/error per team — fin du vol en aveugle
</success_criteria>

<output>
Après completion, créer `.planning/phases/02-memoire-intelligente-agents/02-09-SUMMARY.md`
</output>
