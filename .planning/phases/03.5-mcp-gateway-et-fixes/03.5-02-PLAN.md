---
plan: 03.5-02
phase: 3.5
title: Fix verify-phase3.sh parser Test 1 — container health via docker inspect
status: pending
---

# Plan 03.5-02 — Fix verify-phase3.sh parser

## Objectif

Test 1 "Phase 3 container health" affiche `0/6 healthy` alors que tous les containers sont healthy.
Même bug que verify-phase2.sh : le grep/awk sur `docker compose ps` ne parse pas `(healthy)` correctement.

## Fichier modifié

`infrastructure/scripts/verify-phase3.sh`

## Changement

Remplacer le bloc de parsing (lignes 94-113) par un loop utilisant `docker compose ps -q` +
`docker inspect --format '{{.State.Health.Status}}'` par service.

Liste fixe des 6 services Phase 3 : `neo4j mcp-gateway mcp-scraper mcp-drive-read mcp-calendar drive-sync`

Retirer aussi la définition du tableau `PHASE3_CONTAINERS` (plus utilisée).

## Vérification

`bash infrastructure/scripts/verify-phase3.sh` affiche `[1/7] Phase 3 container health... [PASS] (6/6 healthy)`.
