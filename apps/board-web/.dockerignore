# Keep the build context lean and deterministic: node_modules is regenerated by
# `npm ci` in the build stage from the committed lockfile, and dist is produced by
# `vite build` inside the image — neither must be dragged in from the host.
node_modules
dist
.git
