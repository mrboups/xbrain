# Secrets / env
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker / runtime data
data/
volumes/
.dockerignore.local
docker-compose.override.yml

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# GSD ephemeral artifacts
.planning/sessions/
.planning/spikes/
.planning/sketches/

# Build artifacts (none yet, placeholder)
dist/
build/

# Logs
*.log

# Backups (jamais commit — voir GCS bucket xbrain-backups-prod)
backups/
*.tar.gz
*.sql.gz
*.dump

# Phase 12 GitHub App credentials (dev local — never commit)
.env.phase12

# Playwright MCP capture artifacts (page snapshots, screenshots) — never commit
.playwright-mcp/

# Defensive: additional credential file types
*.pfx
*.p12
*.jks
*.crt
*.cer
*.der
id_rsa
id_ed25519
*credentials*.json
*service-account*.json
*firebase-adminsdk*.json

# Repo-root screenshots — operational/setup artifacts captured ad-hoc;
# never commit (one leaked an OAuth secret on 2026-05-21). PNGs in
# subdirectories (chrome-extension icons, app-site assets) are still tracked.
/*.png
/*.jpg
/*.jpeg

# Scratch/temp files (agent-generated, accidental commits)
tmp_*
*.tmp

# Firebase deploy cache (machine-local, auto-regenerated)
.firebase/

# GSD ephemeral runtime state
.claude/scheduled_tasks.lock
.claude/worktrees/

# Demo video — large marketing asset, deployed to Firebase Hosting, not tracked in git
app-site/demo/*.mp4

# Large marketing video sources — kept local only, deploy copies live in app-site/demo/ also gitignored
marketing-site/*.mp4
